/bin/
# generated translation templates (scripts/dump_module_sources.py), ~15MB
/configs/module_sources/
*.db
*.db-journal
/dist/
.playwright-mcp/
/agentic-mcpd
/packaging/*.rpm
/packaging/*.deb
/packaging/agentic-mcpd

# Local agent .deb staged for the Bossman container's SSH-deploy (Block N-enroll)
/deploy-artifacts/

# Grounded package-universe: fetched cache + batch progress markers (regenerated)
configs/package_universe_real.json
configs/.universe_state.json
configs/.schema_enum_state.json
.sonnet-failmop.done
configs/.roles_features_classify_state.json

# Local secrets for docker-compose interpolation (OpenRouter token, etc.)
.env

# Stray empty dirs docker creates if compose is ever run from bossman-ui/ (wrong PWD)
/bossman-ui/configs/

# Public snapshot: internal docs + the local override are not published (README stays; use docker-compose.override.example)
/docs/
/docker-compose.override.yml

# compiled Go validator binaries (build artifacts — rebuilt via go build)
/render-check
/bossman/bin/
/bossman/deploy/agent.deb
/bossman/deploy/agent.rpm
