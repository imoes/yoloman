fqcn: yoloman.initramfs
name: initramfs
collection: yoloman
writes: true
runtime: starlark
short_description: Neutralise the source's swap/resume (fstab + RESUME=none) and rebuild the initramfs, in the target chroot (provisioning-only, PE-baked)
options:
  dry_run:
    type: bool
    description: When true, report what would change without applying it.
