{
  "cert_path": {
    "type": "string",
    "default": "/etc/molly-brown/certs/localhost.crt",
    "description": "Path to the SSL certificate file"
  },
  "key_path": {
    "type": "string",
    "default": "/etc/molly-brown/certs/localhost.key",
    "description": "Path to the SSL private key file"
  },
  "doc_base": {
    "type": "string",
    "default": "/var/lib/private/molly-brown",
    "description": "Document root base directory"
  },
  "access_log": {
    "type": "string",
    "default": "-",
    "description": "Path to the access log file (use '-' for stdout)"
  },
  "error_log": {
    "type": "string",
    "default": "",
    "description": "Path to the error log file"
  }
}
