{
  "number": {
    "type": "number",
    "default": 1,
    "description": "The number of puzzles to generate (--generate)"
  },
  "symmetry": {
    "type": "string",
    "default": "rotate180",
    "description": "Puzzle symmetry type: none, rotate90, rotate180, mirror, flip, or random (--symmetry)",
    "enum": [
      "none",
      "rotate90",
      "rotate180",
      "mirror",
      "flip",
      "random"
    ]
  },
  "difficulty": {
    "type": "string",
    "default": "any",
    "description": "Difficulty level: simple, easy, intermediate, expert, or any (--difficulty)",
    "enum": [
      "simple",
      "easy",
      "intermediate",
      "expert",
      "any"
    ]
  },
  "format": {
    "type": "string",
    "default": "readable",
    "description": "Output format: readable, compact, one-line, or csv (--readable, --compact, --one-line, --csv)",
    "enum": [
      "readable",
      "csv"
    ]
  }
}
