{
  "k5start_start": {
    "type": "string",
    "default": "",
    "description": "Whether to force starting k5start: \"yes\" to enable, empty (or any other value) to disable. Default empty (disabled unless auto-detected)."
  },
  "k5start_bin": {
    "type": "string",
    "default": "/usr/bin/k5start",
    "description": "Path to k5start binary."
  },
  "k5start_keytab": {
    "type": "string",
    "default": "/etc/krb5.keytab",
    "description": "Path to Kerberos keytab file for k5start."
  },
  "k5start_ccrefresh": {
    "type": "number",
    "default": 60,
    "description": "Seconds between Kerberos ticket refresh by k5start."
  },
  "k5start_principal": {
    "type": "string",
    "default": "host/$(hostname -f)",
    "description": "Kerberos principal for k5start."
  }
}
