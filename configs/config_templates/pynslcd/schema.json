{
  "ldap_uris": {
    "type": "list",
    "default": [
      "ldap://localhost/"
    ],
    "description": "LDAP server URIs (e.g., ldap://, ldaps://, or ldapi://). At least one required."
  },
  "ldap_base": {
    "type": "string",
    "default": "dc=example,dc=com",
    "description": "Default search base DN for directory queries."
  },
  "ldap_version": {
    "type": "number",
    "default": 3,
    "description": "LDAP protocol version to use (typically 3)."
  },
  "ldap_bind_dn": {
    "type": "string",
    "default": "",
    "description": "DN to bind as when performing authenticated searches. Leave blank for anonymous bind."
  },
  "ldap_bind_pw": {
    "type": "string",
    "default": "",
    "description": "Password for the bind DN above. Only needed if bind_dn is set."
  },
  "sasl_mech": {
    "type": "string",
    "default": "",
    "description": "SASL mechanism (e.g., GSSAPI) for Kerberos auth. Leave empty to disable."
  },
  "sasl_authzid": {
    "type": "string",
    "default": "",
    "description": "Authorization identity for SASL (e.g., uid=nslcd,cn=auth)."
  },
  "tls_cacert": {
    "type": "string",
    "default": "",
    "description": "Path to CA certificate file for verifying LDAP server TLS certificates."
  },
  "tls_cert": {
    "type": "string",
    "default": "",
    "description": "Client certificate for mutual TLS (optional)."
  },
  "tls_key": {
    "type": "string",
    "default": "",
    "description": "Client private key for mutual TLS (optional, must match cert)."
  },
  "tls_reqcert": {
    "type": "string",
    "default": "demand",
    "description": "Certificate verification level: 'never', 'allow', 'try', or 'demand'."
  },
  "ssl_mode": {
    "type": "string",
    "default": "",
    "description": "TLS mode: 'start_tls', 'ldaps', or empty string for plain LDAP."
  },
  "reconnect_retry": {
    "type": "number",
    "default": 60,
    "description": "Seconds to wait before retrying a failed LDAP connection."
  },
  "nslcd_threads": {
    "type": "number",
    "default": 5,
    "description": "Number of worker threads to handle concurrent requests."
  },
  "min_uid": {
    "type": "number",
    "default": 1000,
    "description": "Minimum UID to include in passwd/group lookups (e.g., exclude system accounts)."
  },
  "max_uid": {
    "type": "number",
    "default": 65534,
    "description": "Maximum UID to include in lookups."
  },
  "user_maps": {
    "type": "list",
    "default": [
      {
        "name": "passwd",
        "map_passwd": "uid uidNumber cn homeDirectory loginShell gidNumber gecos shadowLastChange",
        "map_shadow": "uid userPassword shadowLastChange shadowMax shadowWarning shadowInactive shadowExpire shadowFlag",
        "map_group": "cn gidNumber memberUid",
        "filter": "(objectClass=posixAccount)"
      },
      {
        "name": "shadow",
        "filter": "(objectClass=posixAccount)"
      }
    ],
    "items": {
      "name": {
        "type": "string",
        "description": "Logical name for this map (e.g., 'passwd', 'shadow')."
      },
      "map_passwd": {
        "type": "string",
        "default": "",
        "description": "LDAP \u2192 local attribute mapping for passwd entries (space-separated)."
      },
      "map_shadow": {
        "type": "string",
        "default": "",
        "description": "LDAP \u2192 local attribute mapping for shadow entries."
      },
      "map_group": {
        "type": "string",
        "default": "",
        "description": "LDAP \u2192 local attribute mapping for group entries."
      },
      "filter": {
        "type": "string",
        "default": "",
        "description": "LDAP search filter for this map (e.g., '(objectClass=posixAccount)')."
      }
    }
  },
  "group_maps": {
    "type": "list",
    "default": [
      {
        "name": "group",
        "ldap_attr": "cn",
        "local_attr": "cn"
      }
    ],
    "items": {
      "name": {
        "type": "string",
        "description": "Logical name for group map (e.g., 'group')."
      },
      "ldap_attr": {
        "type": "string",
        "description": "LDAP attribute containing group name (e.g., 'cn')."
      },
      "local_attr": {
        "type": "string",
        "description": "Local attribute name to map to (typically same as ldap_attr)."
      }
    }
  },
  "pam_maps": {
    "type": "list",
    "default": [
      {
        "name": "login",
        "filter": "(objectClass=posixAccount)",
        "login_attr": "uid",
        "ssh_authz_attr": "uid"
      }
    ],
    "items": {
      "name": {
        "type": "string",
        "description": "PAM service name (e.g., 'login', 'sshd')."
      },
      "filter": {
        "type": "string",
        "description": "LDAP filter to select users for this PAM service."
      },
      "login_attr": {
        "type": "string",
        "description": "LDAP attribute to use as PAM login name (e.g., 'uid' or 'mail')."
      },
      "ssh_authz_attr": {
        "type": "string",
        "default": "",
        "description": "LDAP attribute for SSH authorization (e.g., 'uid' or 'cn')."
      }
    }
  },
  "base_time": {
    "type": "number",
    "default": 3600,
    "description": "Base TTL (seconds) for caching directory entries (minimum)."
  },
  "reconnect_time": {
    "type": "number",
    "default": 60,
    "description": "Time to cache failed connection attempts (seconds)."
  },
  "daemon_uid": {
    "type": "string",
    "default": "nslcd",
    "description": "User account to run the nslcd daemon as."
  },
  "daemon_gid": {
    "type": "string",
    "default": "nslcd",
    "description": "Group account to run the nslcd daemon as."
  },
  "listen_addr": {
    "type": "string",
    "default": "/var/run/nslcd/socket",
    "description": "Unix socket path for local PAM/NSS communication. Leave empty to use default."
  },
  "pidfile": {
    "type": "string",
    "default": "/var/run/nslcd.pid",
    "description": "Path to PID file."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debugging output (to syslog)."
  }
}
