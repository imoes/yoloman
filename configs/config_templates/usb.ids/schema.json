{
  "hw.usb_debug": {
    "type": "number",
    "default": 0,
    "description": "Debug output level for the USB driver. 0 is debugging disabled and larger values increase debug message verbosity."
  },
  "usb_load": {
    "type": "string",
    "default": "YES",
    "description": "Loader directive to load the USB driver as a module. Set to YES to load usb(4) as a module, or NO to compile it into the kernel or not load it."
  }
}
