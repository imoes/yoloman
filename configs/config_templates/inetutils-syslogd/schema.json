{
  "syslogd_opts": {
    "type": "string",
    "default": "--no-forward",
    "description": "Command-line options passed directly to syslogd (e.g., '--no-forward', '-r', '-O /var/log/messages')."
  }
}
