# Global syslog options
option {{ global_options | join(' ') }}

# Facility/priority rules: facility.priority target
{% for rule in rules %}{{ rule.facility }}.{{ rule.priority }}	{{ rule.target }}
{% endfor %}