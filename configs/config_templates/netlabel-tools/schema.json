{
  "netlabel_ipv4_unlabeled_cidr": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IPv4 CIDR prefix for unlabeled traffic mapping"
  },
  "netlabel_ipv6_unlabeled_cidr": {
    "type": "string",
    "default": ":::0",
    "description": "IPv6 CIDR prefix for unlabeled traffic mapping"
  },
  "netlabel_loopback_cipso_doi": {
    "type": "number",
    "default": 9999,
    "description": "CIPSO DOI value used for loopback interface security labels"
  }
}
