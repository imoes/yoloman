{
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debug logging to syslog (for troubleshooting only)."
  },
  "default_timeout": {
    "type": "number",
    "default": 5000,
    "description": "Default time (in milliseconds) before a notification automatically disappears."
  },
  "max_visible": {
    "type": "number",
    "default": 5,
    "description": "Maximum number of notifications shown simultaneously (older ones will be queued)."
  },
  "font": {
    "type": "string",
    "default": "Ubuntu 11",
    "description": "Pango font description (e.g., \"Ubuntu 11\" or \"DejaVu Sans Bold 12\") for notification text."
  },
  "text_color": {
    "type": "string",
    "default": "000000",
    "description": "Hex color (RRGGBB) for notification message text."
  },
  "background_color": {
    "type": "string",
    "default": "ffffff",
    "description": "Hex color (RRGGBB) for notification background."
  },
  "border_color": {
    "type": "string",
    "default": "cccccc",
    "description": "Hex color (RRGGBB) for notification border."
  },
  "icon_size": {
    "type": "number",
    "default": 48,
    "description": "Size (in pixels) of the notification icons."
  },
  "fade_in_duration": {
    "type": "number",
    "default": 200,
    "description": "Duration (ms) of the fade-in animation."
  },
  "fade_out_duration": {
    "type": "number",
    "default": 200,
    "description": "Duration (ms) of the fade-out animation."
  },
  "anchor": {
    "type": "string",
    "default": "bottom-right",
    "description": "Position on the screen where notifications appear."
  },
  "margin": {
    "type": "number",
    "default": 20,
    "description": "Distance (in pixels) between the notification and the screen edge."
  },
  "queue_notifications": {
    "type": "bool",
    "default": true,
    "description": "If true, queue new notifications instead of replacing visible ones."
  },
  "app_overrides": {
    "type": "list",
    "default": [],
    "description": "List of per-application notification settings to override global defaults.",
    "items": {
      "name": {
        "type": "string",
        "description": "Name of the application (as it appears in the notification sender)."
      },
      "timeout": {
        "type": "number",
        "description": "Custom timeout (ms) for this app's notifications."
      },
      "icon": {
        "type": "string",
        "description": "Custom icon name or path (e.g., 'dialog-warning' or '/usr/share/icons/my-icon.png'); leave empty for default."
      }
    }
  }
}
