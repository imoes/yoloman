{
  "init_service_name": {
    "type": "string",
    "default": "cloudkitty-processor",
    "description": "Name of the service as provided in the LSB init info header (must match init script filename)"
  },
  "project_name": {
    "type": "string",
    "default": "cloudkitty",
    "description": "Project name (used for PATH, user, group, config paths)"
  },
  "os_rabbit_processname": {
    "type": "string",
    "default": "cloudkitty-processor",
    "description": "Value for OS_OSLO_MESSAGING_RABBIT__PROCESSNAME environment variable"
  }
}
