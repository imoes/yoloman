{
 "check_daily": {
  "default": "DISKFREE PASSWD SOCKETS",
  "description": "Space-separated list of checks to run daily.",
  "type": "string"
 },
 "check_diskfree": {
  "default": "TRUE",
  "description": "Enable or disable the disk space check (must be 'TRUE' or 'FALSE').\n      \"enum\": [\n        \"TRUE\",\n        \"FALSE\"\n      ]\n    },\n    \"check_passwd\": {\n      \"type\": \"string\",\n      \"default\": \"TRUE\",\n      \"description\": \"Enable or disable the password check (must be 'TRUE' or 'FALSE').\n      \"enum\": [\n        \"TRUE\",\n        \"FALSE\"\n      ]\n    },\n    \"check_setuid\": {\n      \"type\": \"string\",\n      \"default\": \"TRUE\",\n      \"description\": \"Enable or disable the setuid file check (must be 'TRUE' or 'FALSE').\n      \"enum\": [\n        \"TRUE\",\n        \"FALSE\"\n      ]\n    },\n    \"check_sockets\": {\n      \"type\": \"string\",\n      \"default\": \"TRUE\",\n      \"description\": \"Enable or disable the open sockets check (must be 'TRUE' or 'FALSE').\n      \"enum\": [\n        \"TRUE\",\n        \"FALSE\"\n      ]\n    },\n    \"check_iptables_logs\": {\n      \"type\": \"string\",\n      \"default\": \"FALSE\",\n      \"description\": \"Enable or disable the iptables logs check (must be 'TRUE' or 'FALSE').\n      \"enum\": [\n        \"TRUE\",\n        \"FALSE\"\n      ]\n    }",
  "type": "string"
 },
 "check_weekly": {
  "default": "SETUID IPTABLES_LOGS",
  "description": "Space-separated list of checks to run weekly.",
  "type": "string"
 },
 "checksecurity_email": {
  "default": "",
  "description": "Optional alternate email address for reports; if set, CHECKSECURITY_EMAIL is uncommented.",
  "type": "string"
 },
 "mailto": {
  "default": "root",
  "description": "Email address to receive alert notifications from checksecurity.",
  "type": "string"
 },
 "sample_values": {
  "description": "",
  "type": "object"
 }
}
