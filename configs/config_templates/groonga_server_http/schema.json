{
  "listen_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address the server binds to (e.g., 127.0.0.1 for localhost only, 0.0.0.0 for all interfaces)."
  },
  "listen_port": {
    "type": "number",
    "default": 10041,
    "description": "TCP port on which the HTTP server listens for incoming requests."
  },
  "worker_processes": {
    "type": "number",
    "default": 1,
    "description": "Number of worker processes to spawn (use CPU core count for optimal throughput)."
  },
  "pid_file": {
    "type": "string",
    "default": "/run/groonga/groonga.pid",
    "description": "Absolute path to the file where the server writes its process ID."
  },
  "log_level": {
    "type": "string",
    "default": "notice",
    "description": "Minimum log level; messages at this level and above are logged."
  },
  "log_path": {
    "type": "string",
    "default": "/var/log/groonga/groonga.log",
    "description": "Path to the main log file (stderr if omitted or empty)."
  },
  "db_path": {
    "type": "string",
    "default": "/var/lib/groonga/db",
    "description": "Directory where Groonga databases are stored."
  },
  "access_log_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable or disable writing access logs."
  },
  "access_log_path": {
    "type": "string",
    "default": "/var/log/groonga/access.log",
    "description": "Path to the access log file (only used if access logging is enabled)."
  },
  "default_db": {
    "type": "string",
    "default": "{{ db_path }}/default.db",
    "description": "Path to the database file used when no database is specified in the request path."
  },
  "ssl_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable TLS/SSL for HTTPS."
  },
  "ssl_certificate_path": {
    "type": "string",
    "default": "/etc/ssl/certs/groonga.crt",
    "description": "Path to the TLS certificate file (only used if SSL is enabled)."
  },
  "ssl_certificate_key_path": {
    "type": "string",
    "default": "/etc/ssl/private/groonga.key",
    "description": "Path to the TLS private key file (only used if SSL is enabled)."
  },
  "max_connections": {
    "type": "number",
    "default": 1024,
    "description": "Maximum number of simultaneous client connections allowed."
  },
  "client_timeout": {
    "type": "number",
    "default": 60,
    "description": "Idle timeout in seconds for client connections."
  },
  "client_max_body_size": {
    "type": "number",
    "default": 10485760,
    "description": "Maximum allowed size (in bytes) of the HTTP request body (e.g., for bulk imports)."
  },
  "gzip_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable response compression with gzip for supported clients."
  },
  "gzip_types": {
    "type": "list",
    "default": [
      "application/json",
      "text/plain",
      "text/html"
    ],
    "description": "MIME types eligible for gzip compression (only used if gzip is enabled)."
  },
  "server_name": {
    "type": "string",
    "default": "",
    "description": "Server name used for Host header validation in HTTP/1.1; leave empty to disable."
  },
  "maintenance_mode_enabled": {
    "type": "bool",
    "default": false,
    "description": "If enabled, the server returns HTTP 503 with the custom message below."
  },
  "maintenance_message": {
    "type": "string",
    "default": "Server under maintenance. Please try again later.",
    "description": "Plain-text message returned when maintenance mode is active."
  },
  "extra_options": {
    "type": "object",
    "default": {},
    "description": "Arbitrary Groonga core configuration options (e.g., token_filters, normalizers). Each key-value pair is written as 'key value' in the config.",
    "items": {
      "type": "string",
      "description": "Option value for the named Groonga configuration parameter."
    }
  }
}
