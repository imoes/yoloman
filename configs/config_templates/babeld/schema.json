{
  "default_interface": {
    "type": "string",
    "default": "lo",
    "description": "The primary interface babeld operates on by default"
  },
  "local_port": {
    "type": "number",
    "default": 33123,
    "description": "UDP port for Babel protocol communication"
  },
  "default_rxcost": {
    "type": "number",
    "default": 42,
    "description": "Default receive cost for interfaces not explicitly configured"
  },
  "default_hello_interval": {
    "type": "number",
    "default": 5,
    "description": "Default interval (seconds) between hello messages"
  }
}
