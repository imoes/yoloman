{
  "pem_time": {
    "type": "string",
    "default": "%F",
    "description": "Format string for date and time display, compatible with strftime(3). Default is ISO 8601 date."
  },
  "pem_data_dir": {
    "type": "string",
    "default": "~/.pem",
    "description": "The home directory where PEM stores its CSV expense files."
  },
  "pem_file": {
    "type": "string",
    "default": "expenses",
    "description": "Base name for the expense data file used when -f is not explicitly provided in the command."
  }
}
