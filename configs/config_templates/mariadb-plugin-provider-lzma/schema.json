{
  "plugin_load_add": {
    "type": "string",
    "default": "provider_lzma",
    "description": "Plugin to load for LZMA compression provider."
  },
  "provider_lzma": {
    "type": "string",
    "default": "force_plus_permanent",
    "description": "Load mode for the LZMA provider plugin.",
    "enum": [
      "on",
      "skip",
      "force_plus_permanent"
    ]
  }
}
