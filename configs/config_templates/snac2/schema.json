{
  "snac2.host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "The hostname or IP address where the SNAC2 backend service is listening."
  },
  "snac2.port": {
    "type": "number",
    "default": 8001,
    "description": "The port number where the SNAC2 backend service is listening."
  }
}
