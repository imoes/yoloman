{
  "parts_input_dir": {
    "type": "string",
    "default": "/usr/share/ldraw",
    "description": "Input directory containing the LDraw parts library."
  },
  "parts_output_file": {
    "type": "string",
    "default": "parts.lst",
    "description": "Output filename for the generated parts list."
  },
  "sort_by_number": {
    "type": "bool",
    "default": false,
    "description": "Sort the parts list by part number."
  },
  "sort_by_description": {
    "type": "bool",
    "default": false,
    "description": "Sort the parts list by description text."
  },
  "check_duplicates": {
    "type": "bool",
    "default": false,
    "description": "Check for duplicate descriptions in the parts list."
  },
  "skip_moved_parts": {
    "type": "bool",
    "default": false,
    "description": "Include parts with '~Moved to xxx' in the description."
  },
  "skip_tilde_parts": {
    "type": "bool",
    "default": false,
    "description": "Skip parts with a tilde (~) in the description."
  },
  "force_mode": {
    "type": "bool",
    "default": false,
    "description": "Force completion without user prompts."
  },
  "quiet_mode": {
    "type": "bool",
    "default": false,
    "description": "Run in quiet mode, suppressing warnings and prompts."
  },
  "use_8_3_names": {
    "type": "bool",
    "default": false,
    "description": "Use 8.3 filename format for compatibility."
  },
  "truncate_desc_80": {
    "type": "bool",
    "default": false,
    "description": "Truncate descriptions to fit within an 80-character terminal window."
  },
  "ragged_filename": {
    "type": "bool",
    "default": false,
    "description": "Use a ragged filename column size to fit short filenames."
  },
  "truncate_desc_64": {
    "type": "bool",
    "default": false,
    "description": "Truncate long descriptions at 64 characters."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Print verbose information for debugging."
  }
}
