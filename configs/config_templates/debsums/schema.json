{
  "debsums_bin": {
    "type": "string",
    "default": "/usr/bin/debsums",
    "description": "Path to the debsums binary"
  },
  "debsums_ignorefile": {
    "type": "string",
    "default": "/etc/debsums-ignore",
    "description": "Path to the ignore file for debsums"
  },
  "cron_check": {
    "type": "string",
    "default": "daily",
    "description": "Value that must match 'daily' for cron job to run; controls whether the integrity check runs automatically"
  }
}
