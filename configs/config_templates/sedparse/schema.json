{
  "scripts": {
    "type": "list",
    "default": [],
    "description": "List of sed commands/scripts to parse. Each item is a string representing a sed expression."
  },
  "script_file": {
    "type": "string",
    "default": null,
    "description": "Path to a file containing sed commands to parse."
  },
  "full_output": {
    "type": "bool",
    "default": false,
    "description": "If true, show full JSON output including empty values."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, turn on verbose mode."
  }
}
