{
  "service_name": {
    "type": "string",
    "default": {},
    "description": "Unique identifier for the service"
  },
  "enabled": {
    "type": "boolean",
    "default": {},
    "description": "Whether the service is enabled"
  },
  "run_user": {
    "type": "string",
    "default": {},
    "description": "User account under which the service runs"
  },
  "run_group": {
    "type": "string",
    "default": {},
    "description": "Group account under which the service runs"
  },
  "working_directory": {
    "type": "string",
    "default": {},
    "description": "Absolute path to the working directory"
  },
  "exec_path": {
    "type": "string",
    "default": {},
    "description": "Absolute path to the executable binary"
  },
  "exec_args": {
    "type": "list",
    "default": {},
    "description": "List of command-line arguments for the executable"
  },
  "restart_tries": {
    "type": "number",
    "default": {},
    "description": "Maximum number of restart attempts"
  },
  "restart_delay": {
    "type": "number",
    "default": {},
    "description": "Delay in seconds between restart attempts"
  },
  "restart_policy": {
    "type": "string",
    "default": {},
    "description": "Restart policy: one of 'always', 'on-failure', 'no'"
  },
  "environment_vars": {
    "type": "list",
    "default": {},
    "description": "List of environment variables to set for the service; each item is an object with 'key' and 'value' fields (both strings). Values are quoted if needed by the consuming parser (not done here \u2014 rely on caller)."
  },
  "log_directory": {
    "type": "string",
    "default": {},
    "description": "Absolute path to the log directory"
  },
  "log_level": {
    "type": "string",
    "default": {},
    "description": "Logging level: one of 'debug', 'info', 'warn', 'error'"
  },
  "log_max_size": {
    "type": "number",
    "default": {},
    "description": "Maximum size (in MB) of each log file before rotation"
  },
  "log_keep_count": {
    "type": "number",
    "default": {},
    "description": "Number of rotated log files to retain"
  },
  "dependencies": {
    "type": "list",
    "default": {},
    "description": "List of service names this service depends on"
  },
  "shutdown_timeout": {
    "type": "number",
    "default": {},
    "description": "Timeout (in seconds) to wait for graceful shutdown before force-terminating"
  },
  "health_check_url": {
    "type": "string",
    "default": {},
    "description": "Optional URL to use for health checks"
  },
  "health_check_interval": {
    "type": "number",
    "default": {},
    "description": "Interval (in seconds) between health checks (only relevant if health_check_url is provided)"
  },
  "memory_limit": {
    "type": "number",
    "default": {},
    "description": "Memory limit in MB (0 = no limit)"
  },
  "cpu_limit": {
    "type": "number",
    "default": {},
    "description": "CPU limit in cores (e.g., 0.5 = 50% of one core)"
  },
  "network_isolation": {
    "type": "string",
    "default": {},
    "description": "Network isolation level: one of 'none', 'private', 'restricted'"
  }
}
