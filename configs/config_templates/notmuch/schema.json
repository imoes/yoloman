{
  "config_path": {
    "type": "string",
    "default": "~/Mail",
    "description": "Top-level directory containing your mail (e.g., Maildir). Notmuch will recursively index it."
  },
  "primary_email": {
    "type": "string",
    "default": "user@example.com",
    "description": "Your main email address. Used to identify messages sent or received by you during threading."
  },
  "user_name": {
    "type": "string",
    "default": "",
    "description": "Your real name (optional, used in some UI contexts and email headers)."
  }
}
