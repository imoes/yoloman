{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable the DateTime indicator applet in the system panel."
  },
  "clock_format": {
    "type": "string",
    "default": "",
    "description": "Custom clock format using strftime directives (e.g., '%H:%M'). Leave empty for default."
  },
  "clock_use_24h": {
    "type": "bool",
    "default": true,
    "description": "Display time in 24-hour format (true) vs 12-hour with AM/PM (false)."
  },
  "clock_show_seconds": {
    "type": "bool",
    "default": false,
    "description": "Show seconds in the clock display when supported by the format."
  },
  "clock_show_date": {
    "type": "bool",
    "default": true,
    "description": "Display the current date below the time in the clock area."
  },
  "calendar_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable the calendar popup when clicking the DateTime indicator."
  },
  "calendar_show_events": {
    "type": "bool",
    "default": true,
    "description": "Show upcoming events from the system\u2019s default calendar."
  },
  "calendar_max_events": {
    "type": "number",
    "default": 5,
    "description": "Maximum number of upcoming events to display in the calendar popup."
  },
  "clock_timezone_mode": {
    "type": "string",
    "default": "local",
    "description": "Timezone display mode: current local time ('local'), home timezone ('home'), or both ('both').",
    "enum": [
      "local",
      "both"
    ]
  },
  "clock_home_timezone": {
    "type": "string",
    "default": "",
    "description": "Home timezone (e.g., 'Europe/London' or 'America/New_York'). Required when mode is 'home' or 'both'."
  },
  "calendar_date_format": {
    "type": "string",
    "default": "%A, %B %d",
    "description": "strftime format for dates in the calendar view (e.g., '%A, %B %d' \u2192 'Monday, January 15')."
  },
  "calendar_time_format": {
    "type": "string",
    "default": "%H:%M",
    "description": "strftime format for event times in the calendar view (e.g., '%H:%M' \u2192 '14:30')."
  },
  "event_sources": {
    "type": "list",
    "default": [],
    "description": "Additional calendar event sources (e.g., iCal files or webcal feeds). Each entry defines a custom source."
  },
  "calendar_show_weather": {
    "type": "bool",
    "default": false,
    "description": "Show current weather forecast in the calendar popup (requires weather-indicator service)."
  },
  "weather_location_id": {
    "type": "string",
    "default": "",
    "description": "Weather location identifier (e.g., OpenWeatherMap City ID). Required when weather is enabled."
  },
  "weather_units": {
    "type": "string",
    "default": "metric",
    "description": "Temperature and wind units: 'metric' (\u00b0C, m/s) or 'imperial' (\u00b0F, mph).",
    "enum": [
      "metric",
      "imperial"
    ]
  }
}
