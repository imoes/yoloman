{
  "provides": [
    {
      "capability": "app_runtime",
      "backend": "gunicorn",
      "port_field": "port",
      "default_port": 8080,
      "provisionable": {
        "database_file": "database_file",
        "data_directory": "data_directory"
      }
    }
  ],
  "requires": [
    {
      "capability": "mail_smtp",
      "backends": [
        "postfix",
        "exim",
        "sendmail",
        "opensmtpd"
      ],
      "fields": {
        "host": "smtp_server",
        "port": "smtp_port",
        "user": "smtp_username"
      }
    }
  ],
  "confidence": "high",
  "peer_injection": []
}
