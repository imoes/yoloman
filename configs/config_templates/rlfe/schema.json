{
  "command": {
    "type": "string",
    "default": "ftp",
    "description": "The target application command and any initial arguments to be wrapped by rlfe."
  },
  "log_file": {
    "type": "string",
    "default": "",
    "description": "Path to the log file for rlfe session logging. Empty string disables logging."
  },
  "append_log": {
    "type": "bool",
    "default": false,
    "description": "If true, append to the log file instead of overwriting it. Corresponds to the -a flag."
  },
  "app_name": {
    "type": "string",
    "default": "",
    "description": "Name to use for the readline application. Corresponds to the -n flag."
  }
}
