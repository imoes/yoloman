{
  "accounts": {
    "type": "list",
    "default": [],
    "description": "List of accounts with Sieve configuration. One entry per IMAP account using Sieve filters via the managesieve plugin. Each account must define basic IMAP identity to map to Sieve credentials where needed. Sieve settings are only applied if sieve_enabled is true for that account."
  }
}
