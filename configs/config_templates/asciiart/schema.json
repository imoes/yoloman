{
  "cli_args": {
    "type": "list",
    "default": [],
    "description": "List of command-line argument strings to pass to asciiart; each item must be a complete CLI argument (e.g., '-w 120', '--color'). Values are inserted literally; no escaping or transformation is applied by the template engine."
  }
}
