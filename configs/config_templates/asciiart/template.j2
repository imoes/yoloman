# asciiart configuration file
# This file defines runtime options for the `asciiart` utility.
# Directives are generated from the `cli_args` list.

# cli_args: List of command-line argument strings to pass to asciiart.
# Each item in this list becomes a line in the configuration, treated as a
# complete CLI argument (e.g., '-w 120', '--color').
{% for arg in (cli_args | default([])) %}
{{ arg }}
{% endfor %}