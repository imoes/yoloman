{
  "friendlyname": {
    "type": "string",
    "default": "Virtual PCD",
    "description": "The friendly name displayed for this virtual PCD device in the PC/SC middleware."
  },
  "devicename": {
    "type": "string",
    "default": "/dev/null:0x8C7B",
    "description": "The device identifier path and channel ID for the virtual card reader."
  },
  "libpath": {
    "type": "string",
    "default": "/usr/lib/pcsc/drivers/serial/libifdvpcd.so",
    "description": "The path to the IFD handler shared library for the virtual card driver."
  },
  "channelid": {
    "type": "string",
    "default": "0x8C7B",
    "description": "The hex channel ID associated with the virtual card reader."
  }
}
