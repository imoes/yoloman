{
  "brotli.entries": {
    "type": "list",
    "default": [],
    "description": "List of brotli command invocations to configure; each entry corresponds to one command line invocation (e.g., compression or decompression)."
  }
}
