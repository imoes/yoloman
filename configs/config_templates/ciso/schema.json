{
  "default_level": {
    "type": "number",
    "default": 5,
    "description": "Default compression level (0 for decompress, 1\u20139 for compress). 5 is a common balanced default."
  },
  "file_pairs": {
    "type": "list",
    "default": [],
    "description": "List of input-output file path pairs to process. Each item defines a source ISO/CSO and target path. Only one direction (compress or decompress) per run per default_level. Use separate entries for mixed operations if needed."
  }
}
