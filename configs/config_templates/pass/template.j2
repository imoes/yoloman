# ~/.password-store: Password Store configuration
# pass uses this path to locate the password store directory.
# It is typically a Git-tracked directory containing GPG-encrypted .gpg files.
# This value overrides the default location (~/.password-store) if defined.
STORE_PATH="{{ store_path | default('~/.password-store') }}"