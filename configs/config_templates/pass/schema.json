{
  "store_path": {
    "type": "string",
    "default": "~/.password-store",
    "description": "Path to the password store directory (typically a symlink to a Git repo). Pass stores encrypted .gpg files here. Use absolute or tilde-expanded paths."
  }
}
