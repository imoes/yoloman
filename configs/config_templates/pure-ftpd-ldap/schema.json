{
  "ssdaemonlogopts": {
    "type": "string",
    "default": "--quiet",
    "description": "Options passed to start-stop-daemon for logging verbosity."
  },
  "pidfile": {
    "type": "string",
    "default": "/var/run/pure-ftpd/pure-ftpd.pid",
    "description": "Path to the PID file for the pure-ftpd daemon."
  },
  "standalone_or_inetd": {
    "type": "string",
    "default": "inetd",
    "description": "Run mode: 'inetd' for xinetd/dovecot-ldap style or 'standalone' for standalone daemon.",
    "enum": [
      "inetd",
      "standalone"
    ]
  },
  "virtualchroot": {
    "type": "bool",
    "default": false,
    "description": "Enable virtual chroot mode if true."
  }
}
