{
  "hosts": {
    "type": "list",
    "default": [
      "unix:///var/run/docker.sock"
    ],
    "description": "List of addresses for the Docker daemon to listen on (e.g., tcp://0.0.0.0:2375, unix:///var/run/docker.sock)."
  },
  "data_root": {
    "type": "string",
    "default": "/var/lib/docker",
    "description": "Path to the directory where Docker stores data (images, containers, volumes, etc.)."
  },
  "storage_driver": {
    "type": "string",
    "default": "overlay2",
    "description": "Storage driver to use for managing container layers (e.g., overlay2, vfs, btrfs). Must be supported by the host filesystem."
  },
  "log_driver": {
    "type": "string",
    "default": "json-file",
    "description": "Default logging driver for containers."
  },
  "log_opts": {
    "type": "object",
    "default": null,
    "description": "Options for the default logging driver (e.g., max-size, max-file). If null or empty, no specific options are set beyond defaults."
  },
  "dns": {
    "type": "list",
    "default": [],
    "description": "List of DNS servers for containers to use for name resolution."
  },
  "dns_search": {
    "type": "list",
    "default": [],
    "description": "List of DNS search domains for containers."
  },
  "insecure_registries": {
    "type": "list",
    "default": [],
    "description": "List of private registry hostnames/IPs that are allowed to use HTTP or HTTPS with untrusted certificates."
  },
  "default_runtime": {
    "type": "string",
    "default": "runc",
    "description": "Default OCI runtime to use for running containers."
  },
  "runtimes": {
    "type": "object",
    "default": null,
    "description": "Additional OCI-compliant runtimes available for use (e.g., 'runsc' for gVisor, 'kata' for Kata Containers). Each entry maps a name to path and args. If null, no additional runtimes are defined."
  }
}
