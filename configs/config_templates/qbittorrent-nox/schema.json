{
  "daemon_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable daemon mode to run qbittorrent-nox in the background."
  },
  "webui_port": {
    "type": "number",
    "default": 8080,
    "description": "The port number for the Web UI interface."
  }
}
