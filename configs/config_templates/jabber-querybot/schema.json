{
  "hostname": {
    "type": "string",
    "default": "jabberserver.tld",
    "description": "The XMPP/Jabber server hostname to connect to."
  },
  "user": {
    "type": "string",
    "default": "",
    "description": "The Jabber user ID (bare JID or full JID) for the bot."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "The password for the bot's Jabber account."
  },
  "ident": {
    "type": "string",
    "default": "Testbot",
    "description": "The bot's IRC-style ident or nickname."
  },
  "bot_admin": {
    "type": "string",
    "default": "@swissjabber.ch",
    "description": "The JID or domain of the bot administrator(s)."
  },
  "port": {
    "type": "string",
    "default": "5222",
    "description": "The port number for the XMPP connection."
  },
  "timeout": {
    "type": "string",
    "default": "5",
    "description": "The connection timeout in seconds."
  }
}
