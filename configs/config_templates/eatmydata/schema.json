{
  "use_manual_env": {
    "type": "bool",
    "default": false,
    "description": "Whether to output manual environment variable exports for LD_PRELOAD usage"
  },
  "symlinks": {
    "type": "list",
    "default": [],
    "description": "List of symlinks to create for symlink mode operation; each item maps a basename to its target command path (optional, defaults to basename in PATH)"
  }
}
