{
  "post_invoke_cmd": {
    "type": "string",
    "default": "if [ -x /usr/sbin/localepurge ] && [ $(ps w -p $PPID | grep -E -c '(remove|purge)') != 1 ]; then /usr/sbin/localepurge; else exit 0; fi",
    "description": "The shell command executed post-invoke by dpkg to run localepurge if needed."
  }
}
