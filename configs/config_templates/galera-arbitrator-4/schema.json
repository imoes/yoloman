{
  "GALERA_NODES": {
    "type": "string",
    "default": "",
    "description": "A comma-separated list of node addresses (address[:port]) in the cluster"
  },
  "GALERA_GROUP": {
    "type": "string",
    "default": "",
    "description": "Galera cluster name, should be the same as on the rest of the nodes"
  },
  "GALERA_OPTIONS": {
    "type": "string",
    "default": "",
    "description": "Optional Galera internal options string (e.g. SSL settings)"
  },
  "LOG_FILE": {
    "type": "string",
    "default": "",
    "description": "Log file for garbd. Optional, by default logs to syslog"
  },
  "WORK_DIR": {
    "type": "string",
    "default": "",
    "description": "Where to persist necessary data"
  }
}
