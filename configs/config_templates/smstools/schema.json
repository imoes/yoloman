{
  "user": {
    "type": "string",
    "default": "smsd",
    "description": "User under which the smsd daemon runs."
  },
  "group": {
    "type": "string",
    "default": "dialout",
    "description": "Group under which the smsd daemon runs."
  },
  "pidfile": {
    "type": "string",
    "default": "/var/run/smstools/smsd.pid",
    "description": "Path to the PID file for the smsd daemon."
  },
  "infofile": {
    "type": "string",
    "default": "/var/run/smstools/smsd.working",
    "description": "Path to the working/info file for the smsd daemon."
  }
}
