# MinIO Client (mc) Configuration File
# Generated by Configuration Template System
#
# This file represents the `config.json` structure used by the `mc` binary.
# It defines aliases for S3-compatible storage endpoints.
# Each alias entry contains connection details, API versioning, and lookup styles.

{
  "version": "10",
  "aliases": {
    # The alias name key (e.g., "production")
    # alias_name: The name for the MinIO server alias (e.g., 'local', 'prod'). This is used in commands like 'mc ls prod/bucket'.
    "{{ (aliases | default({})).alias_name | default('production') }}": {
      # URL of the MinIO server or S3-compatible endpoint
      # server_url: The URL of the MinIO server or S3-compatible endpoint.
      "url": "{{ server_url | default('https://play.min.io') }}",
      # API signature version to use ('S3v4' or 'S3v2')
      # api: The API signature version to use. Common values are 'S3v4' or 'S3v2'.
      "api": "{{ api | default('S3v4') }}",
      # Bucket lookup style ('auto', 'dns', 'path')
      # lookup: Bucket lookup style. Options are 'auto', 'dns', or 'path'.
      "lookup": "{{ lookup | default('auto') }}",
      # Access key (username) for authentication
      # access_key: The access key (username) for authenticating with the MinIO server.
      "accessKey": "{{ access_key | default('minioadmin') }}",
      # Secret key (password) for authentication
      # secret_key: The secret key (password) for authenticating with the MinIO server.
      "secretKey": "{{ secret_key | default('minioadmin') }}"
    }
  }
}