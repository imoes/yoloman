{
  "provides": {
    "type": "string",
    "default": "istgt",
    "description": "Service name provided by the package"
  },
  "short_description": {
    "type": "string",
    "default": "iSCSI userspace target for kFreeBSD systems",
    "description": "Short description for LSB headers"
  },
  "description": {
    "type": "string",
    "default": "iSCSI userspace target for kFreeBSD systems",
    "description": "Long description for LSB headers"
  },
  "author_email": {
    "type": "string",
    "default": "debian@toell.net",
    "description": "Author email address in comment"
  },
  "desc": {
    "type": "string",
    "default": "istgt iSCSI target",
    "description": "Descriptive string for the daemon"
  },
  "name": {
    "type": "string",
    "default": "istgt",
    "description": "Base name of the service/daemon"
  }
}
