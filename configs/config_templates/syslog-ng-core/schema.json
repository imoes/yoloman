{
  "console_log_level": {
    "type": "string",
    "default": "1",
    "description": "Log level of messages which should go to console. See syslog(3) for details."
  },
  "syslogng_opts": {
    "type": "string",
    "default": "--no-caps",
    "description": "Command line options to syslog-ng."
  }
}
