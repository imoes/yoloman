{
  "mode": {
    "type": "string",
    "default": "tcp",
    "description": "Operation mode: unix, tcp, or nat46",
    "enum": [
      "unix",
      "tcp",
      "nat46"
    ]
  },
  "listen": {
    "type": "list",
    "default": [
      "0.0.0.0:443"
    ],
    "description": "List of socket addresses or paths to listen on"
  },
  "backend_cidr": {
    "type": "list",
    "default": [
      "10.0.0.0/8"
    ],
    "description": "List of CIDR blocks for allowed backend addresses"
  },
  "backend_port": {
    "type": "number",
    "default": null,
    "description": "Backend port number (optional, inherits from listen if not set)"
  },
  "default_hostname": {
    "type": "string",
    "default": null,
    "description": "Default SNI hostname if client provides none"
  },
  "proxy_proto": {
    "type": "bool",
    "default": false,
    "description": "Enable PROXY protocol with backends"
  },
  "unix_directory": {
    "type": "string",
    "default": null,
    "description": "Directory containing backend UNIX sockets (required in unix mode)"
  },
  "nat46_prefix": {
    "type": "string",
    "default": null,
    "description": "IPv6 prefix for NAT46 source translation"
  }
}
