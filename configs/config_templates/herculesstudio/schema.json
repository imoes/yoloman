{
  "hercules_config_file": {
    "type": "string",
    "default": "/etc/hercules.conf",
    "description": "Path to the Hercules virtual machine configuration file (passed via -f)"
  },
  "hercules_run_commands_file": {
    "type": "string",
    "default": "",
    "description": "Path to the Hercules run commands file (passed via -r); empty string to omit"
  }
}
