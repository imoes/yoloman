{
  "kamailio_db_url": {
    "type": "string",
    "default": "mysql://kamailio:kamailiorw@localhost/kamailio",
    "description": "The database URL for Kamailio MySQL connection. Must include host, user, password, and database name."
  }
}
