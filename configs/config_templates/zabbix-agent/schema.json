{
  "zabbix_agent_user": {
    "type": "string",
    "default": "zabbix",
    "description": "The user account under which the zabbix-agent runs."
  },
  "zabbix_agent_group": {
    "type": "string",
    "default": "zabbix",
    "description": "The group account under which the zabbix-agent runs."
  }
}
