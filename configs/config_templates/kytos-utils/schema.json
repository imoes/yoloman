{
  "kytos_api_url": {
    "type": "string",
    "default": "http://localhost:8181",
    "description": "The base URL of the Kytos API endpoint"
  },
  "request_timeout": {
    "type": "number",
    "default": 30,
    "description": "Timeout in seconds for HTTP requests made by the utility"
  },
  "verbose_logging": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable verbose debug logging"
  },
  "default_language": {
    "type": "string",
    "default": "en",
    "description": "Default language code (ISO 639-1) for translations"
  },
  "supported_languages": {
    "type": "list",
    "default": [
      "en",
      "es",
      "fr",
      "de",
      "pt"
    ],
    "description": "List of supported language codes for translation features"
  }
}
