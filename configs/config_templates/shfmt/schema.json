{
  "language_dialect": {
    "type": "string",
    "default": "auto",
    "description": "Language dialect: auto, bash, posix, mksh, bats",
    "enum": [
      "auto",
      "bash",
      "posix",
      "mksh",
      "bats"
    ]
  },
  "indent_size": {
    "type": "number",
    "default": 4,
    "description": "Indentation size: 0 for tabs, >0 for number of spaces"
  },
  "binary_next_line": {
    "type": "bool",
    "default": false,
    "description": "Whether binary ops like && and | start a new line"
  },
  "case_indent": {
    "type": "bool",
    "default": false,
    "description": "Whether switch cases are indented"
  },
  "space_redirects": {
    "type": "bool",
    "default": false,
    "description": "Whether redirect operators are followed by a space"
  },
  "keep_padding": {
    "type": "bool",
    "default": false,
    "description": "Whether to keep column alignment paddings"
  },
  "func_next_line": {
    "type": "bool",
    "default": false,
    "description": "Whether function opening braces are placed on a separate line"
  },
  "ignored_paths": {
    "type": "list",
    "default": [],
    "description": "List of shell glob patterns or directory names to ignore"
  }
}
