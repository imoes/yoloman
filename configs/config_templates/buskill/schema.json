{
  "trigger": {
    "type": "string",
    "default": "lock-screen",
    "description": "The trigger to execute on USB hotplug removal event. Valid values are those listed by `buskill --list-triggers`, e.g., 'lock-screen', 'soft-shutdown', 'hard-shutdown'.",
    "enum": [
      "lock-screen",
      "soft-shutdown"
    ]
  },
  "run_trigger_on_start": {
    "type": "bool",
    "default": false,
    "description": "If true, immediately execute the specified trigger when buskill starts (for testing)."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, increase output verbosity for debugging."
  }
}
