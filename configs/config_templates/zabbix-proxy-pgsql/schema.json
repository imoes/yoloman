{
  "log_file_path": {
    "type": "string",
    "default": "/var/log/zabbix-proxy/zabbix_proxy.log",
    "description": "Path to the Zabbix Proxy log file."
  },
  "log_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "Number of rotated log files to keep."
  },
  "log_create_mode": {
    "type": "string",
    "default": "0640",
    "description": "Permissions for newly created log files."
  },
  "log_user": {
    "type": "string",
    "default": "zabbix",
    "description": "User owner of newly created log files."
  },
  "log_group": {
    "type": "string",
    "default": "zabbix",
    "description": "Group owner of newly created log files."
  }
}
