{
  "project_name": {
    "type": "string",
    "default": "manila",
    "description": "The OpenStack project name used for paths and identifiers."
  },
  "uwsgi_port": {
    "type": "number",
    "default": 8786,
    "description": "The port number the UWSGI server listens on."
  },
  "uwsgi_ini_path": {
    "type": "string",
    "default": "/etc/manila/manila-api-uwsgi.ini",
    "description": "The path to the UWSGI configuration file."
  },
  "uwsgi_ini_app": {
    "type": "string",
    "default": "/usr/bin/manila-wsgi",
    "description": "The path to the UWSGI application script or binary."
  }
}
