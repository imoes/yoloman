{
  "input_file": {
    "type": "string",
    "default": "/var/lib/iat/images/image.img",
    "description": "Path to the input DVD/CD-ROM image file"
  },
  "output_file": {
    "type": "string",
    "default": "/var/lib/iat/images/output.iso",
    "description": "Path to the output converted image file"
  },
  "output_format": {
    "type": "string",
    "default": "iso",
    "description": "Output format: iso, cue, or toc",
    "enum": [
      "iso",
      "cue",
      "toc"
    ]
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode for detailed logging"
  }
}
