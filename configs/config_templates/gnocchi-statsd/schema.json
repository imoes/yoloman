{
  "service_name": {
    "type": "string",
    "default": "gnocchi-statsd",
    "description": "Service name used in init header and logging messages."
  },
  "project_name": {
    "type": "string",
    "default": "gnocchi",
    "description": "Project name; used in paths, user/group names, config file paths, etc."
  },
  "config_file": {
    "type": "string",
    "default": "/etc/gnocchi/gnocchi.conf",
    "description": "Path to the main configuration file."
  },
  "pidfile": {
    "type": "string",
    "default": "/var/run/gnocchi/gnocchi-statsd.pid",
    "description": "Path to PID file."
  },
  "logfile": {
    "type": "string",
    "default": "/var/log/gnocchi/gnocchi-statsd.log",
    "description": "Path to log file."
  },
  "system_user": {
    "type": "string",
    "default": "gnocchi",
    "description": "System user to run the daemon as."
  },
  "system_group": {
    "type": "string",
    "default": "gnocchi",
    "description": "System group for the daemon."
  },
  "scriptname": {
    "type": "string",
    "default": "/etc/init.d/gnocchi-statsd",
    "description": "Full path to the init script."
  },
  "daemon": {
    "type": "string",
    "default": "/usr/bin/gnocchi-statsd",
    "description": "Path to the executable daemon."
  },
  "daemon_args": {
    "type": "string",
    "default": "",
    "description": "Additional arguments passed to the daemon."
  },
  "use_syslog": {
    "type": "bool",
    "default": false,
    "description": "Whether to log to syslog (true/false)."
  },
  "use_logfile": {
    "type": "bool",
    "default": true,
    "description": "Whether to log to a file (true/false)."
  },
  "pyargv": {
    "type": "string",
    "default": "",
    "description": "Arguments to be passed via --pyargv (rarely used)."
  }
}
