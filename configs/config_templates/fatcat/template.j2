# fatcat configuration
# This file defines the command-line arguments passed to the fatcat service.
# Each list item in `fatcat.args` corresponds to one argument on the fatcat CLI.

# List of fatcat command-line arguments.
# Default: empty list (no extra arguments).
# Each item is a single argument string (e.g., '-i', '-x /tmp/output').
# The arguments are rendered one per line, suitable for a command-line wrapper or environment variable expansion.
{% for arg in ((fatcat | default({})).args | default([])) %}
{{ arg | default('') | string }}
{% endfor %}