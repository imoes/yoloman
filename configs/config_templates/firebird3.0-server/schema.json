{
  "security_db_filename": {
    "type": "string",
    "default": "security3.fdb",
    "description": "Filename of the master security database"
  },
  "security_db_remote_access": {
    "type": "bool",
    "default": false,
    "description": "Whether remote access to the security database is enabled"
  },
  "security_db_cache_pages": {
    "type": "number",
    "default": 50,
    "description": "Number of database cache pages for the security database"
  }
}
