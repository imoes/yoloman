{
  "input_file": {
    "type": "string",
    "default": "/var/log/capture.pcap",
    "description": "Path to the input pcap/pcapng file to analyze."
  },
  "output_format": {
    "type": "string",
    "default": "rfc",
    "description": "The display format for protocol headers. 'rfc' aligns fields as per RFCs."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output to stderr/stdout."
  },
  "protocols": {
    "type": "list",
    "default": [
      "IPv4",
      "TCP"
    ],
    "description": "List of protocols to display. Supported: Ethernet, SLL, ARP, ICMP, ICMPv6, IPv4, IPv6, TCP, UDP."
  }
}
