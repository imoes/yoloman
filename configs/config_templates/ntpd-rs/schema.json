{
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Log level: trace, debug, info, warn, error",
    "enum": [
      "trace",
      "debug",
      "info",
      "warn",
      "error"
    ]
  },
  "observation_path": {
    "type": "string",
    "default": "/var/run/ntpd-rs/observe",
    "description": "Path to the observation socket"
  },
  "source_mode": {
    "type": "string",
    "default": "pool",
    "description": "Source mode: pool or server",
    "enum": [
      "pool",
      "server",
      "nts"
    ]
  },
  "source_address": {
    "type": "string",
    "default": "ntpd-rs.pool.ntp.org",
    "description": "Address or hostname of the NTP source/pool"
  },
  "source_count": {
    "type": "number",
    "default": 4,
    "description": "Number of sources to discover when in pool mode"
  },
  "single_step_panic_threshold": {
    "type": "number",
    "default": 1800,
    "description": "Maximum step size in seconds during normal operation"
  },
  "startup_step_backward_threshold": {
    "type": "number",
    "default": 86400,
    "description": "Maximum backward jump in seconds on startup"
  }
}
