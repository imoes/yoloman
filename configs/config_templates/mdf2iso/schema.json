{
 "base_name": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "format_type": {
  "default": "iso",
  "description": "The desired output format. 'iso' generates a plain .iso file. 'cue' generates .bin and .cue files. 'toc' generates .dat and .toc files.",
  "enum": [
   "iso",
   "cue",
   "toc"
  ],
  "type": "string"
 },
 "mdf_file": {
  "default": "/var/lib/mdf2iso/input.mdf",
  "description": "The path to the input Alcohol 120% .mdf image file to be converted.",
  "type": "string"
 },
 "output_dir": {
  "default": "/var/lib/mdf2iso/output",
  "description": "The destination directory where the converted image files will be written.",
  "type": "string"
 }
}
