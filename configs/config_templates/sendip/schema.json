{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose mode to display packet details during construction and transmission."
  },
  "socket_options": {
    "type": "string",
    "default": "",
    "description": "Socket options string. Valid chars: 'b' (SO_BROADCAST), 'i' (IP_HDRINCL), '6' (IPV6_*). Leave empty for defaults.",
    "enum": [
      "b",
      "i",
      "6"
    ]
  },
  "data_file": {
    "type": "string",
    "default": "",
    "description": "Path to a file containing raw packet data to be sent. If provided, payload_data is ignored."
  },
  "payload_data": {
    "type": "string",
    "default": "string",
    "description": "Raw data string to append to the packet. Supports 'rN' (random), '0x' (hex), '0' (octal). Ignored if data_file is set.",
    "enum": [
      "rN",
      "0x",
      "0",
      "string"
    ]
  },
  "modules": {
    "type": "list",
    "default": [
      {
        "name": "ipv4",
        "options": ""
      }
    ],
    "description": "List of modules to load in order. Modules are nested headers inside previous ones."
  },
  "target_host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Destination hostname or IP address for the packet."
  }
}
