{
  "terminal_mode": {
    "type": "string",
    "default": "auto",
    "description": "Initial display mode for the lower data view: 'hex', 'ascii', or 'auto' (default).",
    "enum": [
      "hex",
      "ascii",
      "auto"
    ]
  },
  "scroll_lines": {
    "type": "number",
    "default": 20,
    "description": "Number of lines to scroll vertically with Page Up/Down keys in the data view."
  },
  "block_size": {
    "type": "number",
    "default": 0,
    "description": "Number of bytes each block represents in the overview diagram. 0 (default) means auto-detect based on terminal width."
  }
}
