{
  "search_domains": {
    "type": "list",
    "default": [],
    "description": "List of domain suffixes to append to unqualified hostnames when performing DNS lookups. Order matters: first match wins.",
    "items": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9]([a-zA-Z0-9\\-]*[a-zA-Z0-9])?(\\.[a-zA-Z0-9]([a-zA-Z0-9\\-]*[a-zA-Z0-9])?)*$"
    }
  },
  "nameservers": {
    "type": "list",
    "default": [
      "127.0.0.53"
    ],
    "description": "Ordered list of IP addresses of DNS resolvers (nameservers) to use. Each must be a valid IPv4 or IPv6 address.",
    "items": {
      "type": "string",
      "pattern": "^(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])$|^([0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}$"
    }
  }
}
