# http://webapps-common.alioth.debian.org/draft/html/ch-httpd.html#s-httpd-location
# Alias /pixelmed {{ pixelmed_alias_path | default('/usr/share/pixelmed') }}
# The filesystem path where PixelMed web resources are stored, mapped to the URL alias /pixelmed.
Alias /pixelmed {{ pixelmed_alias_path | default('/usr/share/pixelmed') }}