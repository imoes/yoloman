{
  "pixelmed_alias_path": {
    "type": "string",
    "default": "/usr/share/pixelmed",
    "description": "The filesystem path where PixelMed web resources are stored, mapped to the URL alias /pixelmed."
  }
}
