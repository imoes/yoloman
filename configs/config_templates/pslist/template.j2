# pslist configuration template
# This package configures `pslist`, a utility for listing process status information.
# It is a simple, stateless tool with no persistent daemon or network service.
# No configuration directives are required for operation.