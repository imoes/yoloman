{
  "server": {
    "type": "string",
    "default": "localhost",
    "description": "GNTP server hostname or IP address"
  },
  "port": {
    "type": "number",
    "default": 23053,
    "description": "GNTP server port (IANA-assigned default: 23053)"
  },
  "application": {
    "type": "string",
    "default": "GNTP-Send",
    "description": "Application name used in GNTP notification headers"
  },
  "notification": {
    "type": "string",
    "default": "Message",
    "description": "Default notification name (used if not specified at send time)"
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Authentication password for GNTP server (empty = no authentication)"
  },
  "icon": {
    "type": "string",
    "default": "",
    "description": "Default icon path or URL for notifications"
  },
  "url": {
    "type": "string",
    "default": "",
    "description": "Default URL to open when notification is clicked"
  }
}
