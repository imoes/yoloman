{
  "rcmd_type": "ssh",
  "fanout": 64,
  "connect_timeout": 15,
  "command_timeout": 300,
  "remote_user": "admin",
  "ssh_args": "-2 -a -x -oStrictHostKeyChecking=no -l%u %h",
  "ssh_args_append": "-q",
  "targets": [
    "compute[001-010]",
    "login[01-05]",
    "!offline-node"
  ]
}
