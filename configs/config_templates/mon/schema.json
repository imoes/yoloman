{
  "mon_user": {
    "type": "string",
    "default": "mon",
    "description": "User account under which the mon daemon runs"
  },
  "mon_group": {
    "type": "string",
    "default": "mon",
    "description": "Group account under which the mon daemon runs"
  },
  "mon_home": {
    "type": "string",
    "default": "/var/lib/mon",
    "description": "Home directory for the mon user"
  },
  "daemon_opts": {
    "type": "string",
    "default": "-f ",
    "description": "Options passed to the mon daemon at startup (e.g., `-f` for foreground, `-d` for debug)"
  }
}
