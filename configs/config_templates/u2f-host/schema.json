{
  "origin": {
    "type": "string",
    "default": "https://localhost",
    "description": "Origin URL to use for U2F token binding."
  },
  "action": {
    "type": "string",
    "default": "register",
    "description": "Action to take. Possible values: register, authenticate, sendrecv.",
    "enum": [
      "register",
      "authenticate",
      "sendrecv"
    ]
  },
  "touch_enabled": {
    "type": "bool",
    "default": true,
    "description": "Invert user-presence flag. Default is on (true)."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Print debug information to standard error."
  },
  "command": {
    "type": "string",
    "default": "",
    "description": "Command for sendrecv action."
  }
}
