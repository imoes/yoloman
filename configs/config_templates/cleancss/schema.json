{
  "compatibility": {
    "description": "Force compatibility mode: 'ie7' or 'ie8'.",
    "enum": [
      "ie7",
      "ie8"
    ],
    "type": "string"
  },
  "debug": {
    "default": false,
    "description": "Enable debug output (minification time & compression efficiency).",
    "type": "bool"
  },
  "format_options": {
    "default": [],
    "description": "Output formatting options (e.g., ['beautify'], ['indentBy:2', 'indentWith:space']).",
    "type": "list"
  },
  "inline": {
    "default": "local",
    "description": "Inlining policy: 'local', 'remote', 'all', or 'none'.",
    "enum": [
      "local",
      "remote",
      "all",
      "none"
    ],
    "type": "string"
  },
  "inline_timeout": {
    "default": 5,
    "description": "Timeout (seconds) for fetching remote stylesheets during inlining.",
    "type": "number"
  },
  "input_source_map": {
    "default": "",
    "description": "Path to input source map file for rebase and sourcemap handling.",
    "type": "string"
  },
  "optimizations": {
    "default": "1",
    "description": "Optimization level (0, 1, 2) or a fine-grained options string (e.g., 'removeQuotes:off;roundingPrecision:4').",
    "enum": [
      "0",
      "1",
      "2"
    ],
    "type": "string"
  },
  "output": {
    "default": "",
    "description": "Output file path; if empty, output goes to stdout.",
    "type": "string"
  },
  "remove_inlined_files": {
    "default": false,
    "description": "Remove files inlined via @import or command-line after processing.",
    "type": "bool"
  },
  "skip_rebase": {
    "default": false,
    "description": "Disable URL rebasing.",
    "type": "bool"
  },
  "source_map": {
    "default": false,
    "description": "Enable building input source map.",
    "type": "bool"
  },
  "source_map_inline_sources": {
    "default": false,
    "description": "Inline source content in source maps.",
    "type": "bool"
  },
  "sources": {
    "default": [],
    "description": "Input CSS files to process (required).",
    "type": "list"
  }
}
