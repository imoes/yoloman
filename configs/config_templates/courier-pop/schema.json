{
  "init_info_provides": {
    "type": "string",
    "default": "courier-pop",
    "description": "The name this init script provides (used by system services manager)"
  },
  "init_info_required_start": {
    "type": "string",
    "default": "courier-authdaemon $remote_fs $syslog",
    "description": "Dependencies that must start before this service"
  },
  "init_info_required_stop": {
    "type": "string",
    "default": "$remote_fs $syslog",
    "description": "Dependencies required to stop this service"
  },
  "init_info_default_start": {
    "type": "string",
    "default": "2 3 4 5",
    "description": "Runlevels in which this service starts by default"
  },
  "init_info_default_stop": {
    "type": "string",
    "default": "0 1 6",
    "description": "Runlevels in which this service stops by default"
  },
  "init_info_short_description": {
    "type": "string",
    "default": "Courier POP3 server",
    "description": "Short description of the service"
  },
  "daemon_path": {
    "type": "string",
    "default": "/usr/sbin/pop3d",
    "description": "Path to the POP3 daemon executable"
  },
  "desc": {
    "type": "string",
    "default": "Courier POP3 server",
    "description": "Human-readable description of the service"
  },
  "pop3d_config_path": {
    "type": "string",
    "default": "/etc/courier/pop3d",
    "description": "Path to the main Courier POP3 configuration file"
  },
  "init_d_script_courier_path": {
    "type": "string",
    "default": "/usr/lib/courier/init-d-script-courier",
    "description": "Path to the Courier-specific init-d-script helper"
  }
}
