{
  "G15DEBUG": {
    "type": "string",
    "default": "",
    "description": "Set to 'on' to enable debugging of g15 init.d and udev scripts; otherwise leave empty."
  },
  "DAEMON_OPTS": {
    "type": "string",
    "default": "",
    "description": "Additional options passed to the g15daemon at startup (e.g., '-s' to specify a switch key or other runtime flags)."
  }
}
