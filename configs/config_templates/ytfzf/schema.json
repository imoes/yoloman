{
  "is_download": {
    "type": "string",
    "default": "0",
    "description": "Whether to download selected videos. 0: play, 1: download. Maps to --download/-d."
  },
  "is_audio_only": {
    "type": "string",
    "default": "0",
    "description": "Whether to play audio only. 0: off, 1: on. Maps to --audio-only/-m."
  },
  "is_detach": {
    "type": "string",
    "default": "0",
    "description": "Whether to detach the video player. 0: off, 1: on. Maps to --detach."
  },
  "is_loop": {
    "type": "string",
    "default": "0",
    "description": "Whether to loop the menu. 0: off, 1: on. Maps to --loop/-l."
  },
  "search_again": {
    "type": "string",
    "default": "0",
    "description": "Whether to search again after closing fzf. 0: off, 1: on. Maps to --search-again/-s."
  },
  "show_thumbnails": {
    "type": "string",
    "default": "0",
    "description": "Whether to show thumbnails. 0: off, 1: on. Maps to --show-thumbnails/-t."
  },
  "async_thumbnails": {
    "type": "string",
    "default": "1",
    "description": "Whether to download thumbnails asynchronously. 0: off, 1: on. Maps to --async-thumbnails."
  },
  "skip_thumb_download": {
    "type": "string",
    "default": "0",
    "description": "Whether to skip thumbnail download. 0: off, 1: on. Maps to --skip-thumb-download."
  },
  "notify_playing": {
    "type": "string",
    "default": "1",
    "description": "Whether to show notifications. 0: off, 1: on. Maps to --notify-playing."
  },
  "is_sort": {
    "type": "string",
    "default": "0",
    "description": "Whether to sort videos by upload date. 0: off, 1: on. Maps to --sort."
  },
  "fancy_subs": {
    "type": "string",
    "default": "0",
    "description": "Whether to have separators between subscriptions. 0: off, 1: on. Maps to --fancy-subs."
  },
  "enable_back_button": {
    "type": "string",
    "default": "1",
    "description": "Whether to enable the back button. 0: disable, 1: enable. Maps to --disable-back."
  },
  "enable_actions": {
    "type": "string",
    "default": "1",
    "description": "Whether to enable actions. 0: disable, 1: enable. Maps to --disable-actions."
  },
  "enable_submenus": {
    "type": "string",
    "default": "1",
    "description": "Whether to enable submenus. 0: disable, 1: enable. Maps to --disable-submenus."
  },
  "keep_vars": {
    "type": "string",
    "default": "1",
    "description": "Whether to keep vars in submenus. 0: off, 1: on. Maps to --keep-vars."
  },
  "interface": {
    "type": "string",
    "default": "fzf",
    "description": "Interface type. 'fzf' for fzf, 'ext' for external menu (default dmenu). Maps to --interface/-i or --external-menu/-D.",
    "enum": [
      "fzf",
      "ext"
    ]
  },
  "fzf_preview_side": {
    "type": "string",
    "default": "right",
    "description": "Preview side in fzf. Options: left, right, up, down. Maps to --preview-side.",
    "enum": [
      "left",
      "right",
      "up",
      "down"
    ]
  },
  "format_selection_screen": {
    "type": "string",
    "default": "normal",
    "description": "Format selection screen type. Options: normal, simple. Maps to --format-selection.",
    "enum": [
      "normal",
      "simple"
    ]
  },
  "format_selection_sort": {
    "type": "string",
    "default": "",
    "description": "Format sort for ytdl. Maps to --format-sort."
  },
  "video_pref": {
    "type": "string",
    "default": "",
    "description": "Video format preference for ytdl. Maps to --video-pref."
  },
  "audio_pref": {
    "type": "string",
    "default": "",
    "description": "Audio format preference for ytdl. Maps to --audio-pref."
  },
  "ytdl_pref": {
    "type": "string",
    "default": "",
    "description": "General ytdl format preference. Maps to --ytdl-pref."
  },
  "info_wait_action": {
    "type": "string",
    "default": "link",
    "description": "Action when info-wait is enabled. Options: link, video-json, json, format, raw. Maps to --info-action.",
    "enum": [
      "link",
      "video-json",
      "json",
      "format",
      "raw"
    ]
  },
  "info_wait": {
    "type": "string",
    "default": "0",
    "description": "Wait after printing info. 0: off, 1: on. Maps to --info-wait."
  },
  "url_handler_opts": {
    "type": "string",
    "default": "",
    "description": "Options to pass to url handler (e.g. mpv flags). Maps to --url-handler-opts."
  },
  "submenu_opts": {
    "type": "string",
    "default": "",
    "description": "Options to use in submenus. Maps to --submenu-opts."
  },
  "link_count": {
    "type": "string",
    "default": "1",
    "description": "Number of videos to select with auto-select. Maps to --link-count/-n."
  },
  "pages_to_scrape": {
    "type": "string",
    "default": "1",
    "description": "Amount of pages to scrape. Maps to --pages."
  },
  "pages_start": {
    "type": "string",
    "default": "1",
    "description": "Page to start scraping on. Maps to --pages-start."
  },
  "max_thread_count": {
    "type": "string",
    "default": "4",
    "description": "Max threads for scraping. Maps to --max-threads."
  },
  "odysee_video_search_count": {
    "type": "string",
    "default": "",
    "description": "Amount of videos to scrape on odysee. Maps to --odysee-video-count."
  },
  "nsfw": {
    "type": "string",
    "default": "0",
    "description": "Whether to search for nsfw videos (Odysee). 0: off, 1: on. Maps to --nsfw."
  },
  "search_source": {
    "type": "string",
    "default": "args",
    "description": "Source for search query. Options: args, prompt, hist. Maps to -q/--search-source.",
    "enum": [
      "args",
      "prompt",
      "hist",
      "next",
      "fn-args"
    ]
  },
  "keep_cache": {
    "type": "string",
    "default": "0",
    "description": "Whether to keep cache. 0: off, 1: on. Maps to --keep-cache."
  },
  "ytdl_path": {
    "type": "string",
    "default": "",
    "description": "Path to youtube-dl or fork. Maps to --ytdl-path."
  },
  "invidious_instance": {
    "type": "string",
    "default": "",
    "description": "Invidious instance URL. Maps to --ii/--invidious-instance."
  },
  "scrapers": {
    "type": "string",
    "default": "youtube",
    "description": "Comma-separated list of scrapers to use. Maps to -c/--scrape.",
    "enum": [
      "youtube",
      "youtube-channel",
      "invidious-channel",
      "video-recommended",
      "youtube-playlist",
      "invidious-playlist",
      "youtube-trending",
      "from-cache",
      "multi",
      "youtube-subscriptions",
      "scrape-list",
      "peertube",
      "odysee",
      "history",
      "url",
      "comments"
    ]
  },
  "multi_search": {
    "type": "string",
    "default": "0",
    "description": "Whether to use multi search. 0: off, 1: on. Maps to --multi-search."
  },
  "force_youtube": {
    "type": "string",
    "default": "0",
    "description": "Force youtube links with youtube scraper. 0: off, 1: on. Maps to --force-youtube."
  },
  "force_invidious": {
    "type": "string",
    "default": "0",
    "description": "Force invidious links with youtube scraper. 0: off, 1: on. Maps to --force-invidious."
  },
  "video_duration": {
    "type": "string",
    "default": "",
    "description": "Video duration filter. Options: short, long. Maps to --video-duration.",
    "enum": [
      "short",
      "long"
    ]
  },
  "type": {
    "type": "string",
    "default": "video",
    "description": "Type of results. Options: video, playlist, channel, all. Maps to --type.",
    "enum": [
      "video",
      "playlist",
      "channel",
      "all"
    ]
  },
  "thumbnail_quality": {
    "type": "string",
    "default": "high",
    "description": "Thumbnail quality. Options: maxres, maxresdefault, sddefault, high, medium, default, start, middle, end. Maps to --thumbnail-quality.",
    "enum": [
      "maxres",
      "maxresdefault",
      "sddefault",
      "high",
      "medium",
      "default",
      "start",
      "middle",
      "end"
    ]
  },
  "features": {
    "type": "string",
    "default": "",
    "description": "Comma-separated features to filter by. Maps to --features.",
    "enum": [
      "hd",
      "subtitles",
      "creative_commons",
      "3d",
      "live",
      "4k",
      "360",
      "location",
      "hdr"
    ]
  },
  "region": {
    "type": "string",
    "default": "",
    "description": "Region country code. Maps to --region."
  },
  "sort_by": {
    "type": "string",
    "default": "",
    "description": "Sort by criteria. Options: relevance, rating, upload_date, oldest_first, view_count. Maps to --sort-by.",
    "enum": [
      "relevance",
      "rating",
      "upload_date",
      "oldest_first",
      "view_count"
    ]
  },
  "upload_date": {
    "type": "string",
    "default": "",
    "description": "Upload date time-frame. Options: hour, today, week, month, year. Maps to --upload-date.",
    "enum": [
      "hour",
      "today",
      "week",
      "month",
      "year"
    ]
  },
  "download_shortcut": {
    "type": "string",
    "default": "alt-d",
    "description": "Shortcut for download action. Maps to download_shortcut."
  },
  "video_shortcut": {
    "type": "string",
    "default": "alt-v",
    "description": "Shortcut for watch video action. Maps to video_shortcut."
  },
  "audio_shortcut": {
    "type": "string",
    "default": "alt-m",
    "description": "Shortcut for audio only action. Maps to audio_shortcut."
  },
  "detach_shortcut": {
    "type": "string",
    "default": "alt-e",
    "description": "Shortcut for detach action. Maps to detach_shortcut."
  },
  "print_link_shortcut": {
    "type": "string",
    "default": "alt-l",
    "description": "Shortcut for print link action. Maps to print_link_shortcut."
  },
  "show_formats_shortcut": {
    "type": "string",
    "default": "alt-f",
    "description": "Shortcut for show formats action. Maps to show_formats_shortcut."
  },
  "info_shortcut": {
    "type": "string",
    "default": "alt-i",
    "description": "Shortcut for info action. Maps to info_shortcut."
  },
  "search_again_shortcut": {
    "type": "string",
    "default": "alt-s",
    "description": "Shortcut for search again action. Maps to search_again_shortcut."
  },
  "next_page_action_shortcut": {
    "type": "string",
    "default": "ctrl-p",
    "description": "Shortcut for next page action. Maps to next_page_action_shortcut."
  },
  "url_handlers": {
    "type": "list",
    "default": [],
    "description": "List of url handlers to load. Maps to load_url_handler directives."
  },
  "thumbnail_viewers": {
    "type": "list",
    "default": [],
    "description": "List of thumbnail viewers to load. Maps to load_thumbnail_viewer directives."
  },
  "interfaces": {
    "type": "list",
    "default": [],
    "description": "List of custom interfaces to load. Maps to load_interface directives."
  },
  "extensions": {
    "type": "list",
    "default": [],
    "description": "List of extensions to load. Maps to load_extension directives."
  }
}
