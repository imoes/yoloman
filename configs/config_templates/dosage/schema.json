{
 "adult": {
  "default": false,
  "description": "If true, confirms user age for adult-content comics (equivalent to --adult)",
  "type": "bool"
 },
 "all": {
  "default": false,
  "description": "If true, retrieves all comic strips (equivalent to --all)",
  "type": "bool"
 },
 "allow_multiple": {
  "default": false,
  "description": "If true, permits concurrent dosage runs (equivalent to --allow-multiple)",
  "type": "bool"
 },
 "basepath": {
  "default": "Comics",
  "description": "Path to directory where comic subdirectories are created",
  "type": "string"
 },
 "baseurl": {
  "default": "",
  "description": "Base URL of the comics directory (for HTML/RSS output); must match --basepath",
  "type": "string"
 },
 "comics": {
  "default": [],
  "description": "List of comic module names or case-insensitive substrings to process by default",
  "type": "list"
 },
 "continue": {
  "default": false,
  "description": "If true, retrieves strips until an already-downloaded one is found (equivalent to --continue)",
  "type": "bool"
 },
 "no_downscale": {
  "default": false,
  "description": "If true, disables downscaling for HTML/RSS output handlers",
  "type": "bool"
 },
 "numstrips": {
  "description": "Number of strips to retrieve; empty or unset means rely on ALL or CONTINUE mode or latest",
  "type": "number"
 },
 "output": {
  "default": "html",
  "description": "Output handler format: 'html', 'json', 'rss', or empty (plain files)",
  "enum": [
   "html",
   "json",
   "rss"
  ],
  "type": "string"
 },
 "parallel": {
  "default": 1,
  "description": "Number of parallel connections; default 1 means sequential fetching",
  "type": "number"
 }
}
