{
  "CONF_SWAPFILE": {
    "type": "string",
    "default": "/var/swap",
    "description": "Directory where to store swap files"
  },
  "CONF_SWAPSIZE": {
    "type": "number",
    "default": 100,
    "description": "Max size of the swap file in MB"
  },
  "CONF_SWAP_KEEP": {
    "type": "bool",
    "default": false,
    "description": "If you want to keep swapfiles even when not enabled"
  },
  "DISKCHECK": {
    "type": "string",
    "default": "/usr/bin/disk-check",
    "description": "Path to the disk check utility"
  }
}
