{
  "no_daemonize": {
    "type": "bool",
    "default": true,
    "description": "If true, adds the -d (--no-daemonize) flag to prevent the daemon from backgrounding itself. Recommended for systemd service managers."
  },
  "extra_options": {
    "type": "list",
    "default": [],
    "description": "A list of additional command-line arguments to pass to seahorse-daemon (e.g., GTK theme settings or debugging flags). See `seahorse-daemon --help` for standard GNOME options."
  }
}
