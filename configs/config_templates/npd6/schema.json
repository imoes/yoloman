{
  "desc": {
    "type": "string",
    "default": "npd6 - Neighbor proxy Daemon IPv6",
    "description": "Description of the npd6 daemon displayed in logs."
  },
  "name": {
    "type": "string",
    "default": "npd6",
    "description": "Name of the npd6 service/daemon."
  },
  "daemon_args": {
    "type": "string",
    "default": "",
    "description": "Command line arguments passed to the npd6 daemon (e.g., --debug -l /var/log/npd6.log)."
  }
}
