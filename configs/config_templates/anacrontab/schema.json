{
  "shell": {
    "type": "string",
    "default": "/bin/sh",
    "description": "Shell used to execute commands"
  },
  "home": {
    "type": "string",
    "default": "/root",
    "description": "Home directory for the user running anacron"
  },
  "logname": {
    "type": "string",
    "default": "root",
    "description": "Login name of the user running anacron"
  },
  "jobs": {
    "type": "list",
    "default": [],
    "description": "List of scheduled jobs"
  },
  "jobs[].period": {
    "type": "string",
    "default": "",
    "description": "Period in days or special marker (e.g., '1', '7', '@monthly')"
  },
  "jobs[].delay": {
    "type": "number",
    "default": 0,
    "description": "Delay in minutes before running the job after anacron starts"
  },
  "jobs[].name": {
    "type": "string",
    "default": "",
    "description": "Unique name for the job (used as identifier)"
  },
  "jobs[].command": {
    "type": "string",
    "default": "",
    "description": "Command to execute (full path recommended)"
  }
}
