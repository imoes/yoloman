{
  "jobs": {
    "type": "list",
    "default": []
  },
  "jobs[].period": {
    "type": "string",
    "default": "1",
    "description": "Period in days or special marker (e.g., '1', '7', 'monthly') \u2014 'monthly' supported per man page"
  },
  "jobs[].delay": {
    "type": "number",
    "default": 0,
    "description": "Delay in minutes before running the job after anacron starts"
  },
  "jobs[].name": {
    "type": "string",
    "default": "",
    "description": "Unique name for the job (used as identifier and timestamp file name); must not contain '/'"
  },
  "jobs[].command": {
    "type": "string",
    "default": "",
    "description": "Command to execute (full path recommended)"
  }
}
