{
  "config": {
    "type": "object",
    "description": "Root configuration object for netwag. Note: In Jinja2, object keys are accessed via dot notation or dict lookup. For this template, we assume a flat structure passed directly or a dict. To strictly adhere to 'values_schema' var -> type, we will flatten the schema for the variables used in the template, but netwag is a GUI frontend with *no* standard persistent configuration file on disk according to the man page. It reads netwox availability at runtime. However, to fulfill the prompt's request for a 'Class-B configuration template' for this service, we must infer a plausible minimal config structure often found in desktop app settings (e.g., in XDG config dirs), as none are documented in section 5. We will create a template for a hypothetical local config file `~/.config/netwag/netwag.conf` which is a common pattern for such GTK/Qt apps, parametrizing standard UI/execution settings.\n\n*Correction*: The prompt asks for a template for *this package*. The man page says `netwag` is a graphical frontend. It does *not* have a configuration file in section 5. It simply launches netwox tools. Generating a config file for a GUI app that doesn't have one is inventing. However, the prompt requires a template. I will provide a template for a standard XDG user configuration file, which is the only logical place for such an app to store settings if it doesn't use a global conf file. I will parametrize standard GUI preferences.\n\nActually, looking closer at the prompt's constraint: \"Base every variable on a real documented setting; invent nothing.\" Since there are *no* documented settings in the man page (it only mentions `netwag` and `netwox`), and `netwag` itself has no config file mentioned, I must state that `netwag` does not have a configuration file based on the provided documentation. \n\n*Re-evaluating*: If I cannot invent, and the man page has no config, what do I return? The prompt asks for a "
  },
  " values_schema": {
    "type": "object",
    "description": "Configuration variables"
  }
}
