{
  "name": {
    "type": "string",
    "default": "xapp-sn-watcher",
    "description": "The display name of the application entry."
  },
  "comment": {
    "type": "string",
    "default": "A service that provides the org.kde.StatusNotifierWatcher interface for XApps",
    "description": "Description of the application's purpose."
  },
  "exec_path": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu/xapps/xapp-sn-watcher",
    "description": "Full path to the executable binary."
  },
  "autostart_phase": {
    "type": "string",
    "default": "Panel",
    "description": "The phase in which the application should start (e.g., Panel, Applications).",
    "enum": [
      "Panel",
      "X-GNOME-Autostart-Phase"
    ]
  }
}
