{
  "window_x": {
    "type": "number",
    "default": 0,
    "description": "X position of window on screen, in pixels."
  },
  "window_y": {
    "type": "number",
    "default": 0,
    "description": "Y position of window on screen, in pixels."
  },
  "window_width": {
    "type": "number",
    "default": 800,
    "description": "Client width of window, in pixels."
  },
  "window_height": {
    "type": "number",
    "default": 600,
    "description": "Client height of window, in pixels."
  },
  "grid_size": {
    "type": "number",
    "default": 8,
    "description": "Grid size on start (allowed values: 4,6,8,10,12,14,16,20,24)."
  },
  "refresh_interval": {
    "type": "string",
    "default": "off",
    "description": "Set refresh interval (allowed values: off,5s,10s,30s,1m,2m,5m).",
    "enum": [
      "off",
      "5s",
      "10s",
      "30s",
      "1m",
      "2m",
      "5m"
    ]
  },
  "use_binary_units": {
    "type": "bool",
    "default": false,
    "description": "Use binary unit prefixes instead of decimal."
  },
  "show_log_panel": {
    "type": "bool",
    "default": false,
    "description": "Show log panel at bottom on start."
  },
  "safe_mode": {
    "type": "bool",
    "default": false,
    "description": "Turn off optimizations (safe mode)."
  }
}
