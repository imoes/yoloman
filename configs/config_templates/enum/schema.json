{
  "commands": {
    "type": "list",
    "default": [],
    "description": "List of enum command lines (complete invocations) to execute or serialize. Each entry must be a valid enum(1) command string as per the manpage syntax. Example: 'enum -r 1 .. 3x .. 10'. Defaults to a single 'enum 1 10' if empty."
  }
}
