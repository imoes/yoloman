{
  "devices": {
    "type": "list",
    "default": [],
    "description": "List of device configurations to apply. Each item is a flat map of device settings."
  },
  "global_match_string": {
    "type": "string",
    "default": "",
    "description": "Global --match substring to filter devices if not specified per device."
  },
  "global_non_volatile": {
    "type": "bool",
    "default": false,
    "description": "If true, applies --non-volatile flag to set commands."
  },
  "global_verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, adds --verbose flag to liquidctl commands."
  }
}
