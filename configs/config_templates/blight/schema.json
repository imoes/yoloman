{
  "device": {
    "type": "string",
    "default": "",
    "description": "Specific backlight device name to target (e.g., intel_backlight, amdgpu_bl1). Leave empty to use default."
  },
  "restore_at_boot": {
    "type": "bool",
    "default": true,
    "description": "Whether to restore saved brightness level on boot via the systemd service."
  },
  "sweep_mode": {
    "type": "bool",
    "default": false,
    "description": "Enable gradual brightness change (smoothing) during setup if supported."
  }
}
