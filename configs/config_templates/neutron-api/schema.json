{
  "UWSGI_PORT": {
    "type": "number",
    "default": 9696,
    "description": "The port on which the UWSGI server listens."
  },
  "UWSGI_INI_PATH": {
    "type": "string",
    "default": "/etc/neutron/neutron-api-uwsgi.ini",
    "description": "Path to the UWSGI configuration file."
  },
  "UWSGI_INI_APP": {
    "type": "string",
    "default": "/usr/bin/neutron-api",
    "description": "Path to the UWSGI application executable."
  },
  "TMPDIR_PATH": {
    "type": "string",
    "default": "/var/lib/neutron/tmp",
    "description": "Temporary directory used by Neutron."
  }
}
