{
  "output_box": {
    "type": "string",
    "default": "%Y-%m",
    "description": "Mailbox format for archived output, supporting Date::Format specifiers like %Y (year), %m (month), etc."
  },
  "days": {
    "type": "number",
    "default": "",
    "description": "Only archive messages older than this many days (integer). Leave empty to disable age filter."
  },
  "date": {
    "type": "string",
    "default": "",
    "description": "Only archive messages older than this date (e.g., '2024-01-01' or Perl Date::Parse format). Leave empty to disable date filter."
  },
  "only_read": {
    "type": "bool",
    "default": false,
    "description": "If true, only archive messages marked as read/seen."
  },
  "delete_immediately": {
    "type": "bool",
    "default": false,
    "description": "If true, sync mailbox after every move \u2014 slower but improves crash recovery."
  },
  "preserve_timestamp": {
    "type": "bool",
    "default": false,
    "description": "If true, preserve atime/mtime of input mailbox files (applies to file-based formats only)."
  },
  "dry_run": {
    "type": "bool",
    "default": false,
    "description": "If true, simulate archiving without modifying any mailbox."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, emit informational messages. Overrides quiet mode."
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "If true, suppress all output except errors. Ignored if verbose is true."
  },
  "mailboxes": {
    "type": "list",
    "default": [],
    "description": "List of input mailbox paths/URLs to archive (supports mbox, Maildir, MH via Mail::Box)."
  }
}
