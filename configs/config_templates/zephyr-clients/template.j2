# $Id$

# Zephyr default variables file.  Format is:
#
#   var = value

# Exposure setting for Zephyr messages (e.g., realm-visible, private).
# This controls how messages are visible to other users.
exposure = {{ exposure | default('realm-visible') }}

# Initialization programs.
#initprogs = /etc/athena/zinit