{
  "service_name": {
    "type": "string",
    "default": "lvm2-vg-lv",
    "description": "Name of the device-mapper device (e.g., 'lvm2-vg-lv' or custom)"
  },
  "tables": {
    "type": "list",
    "default": [],
    "description": "List of table lines defining the device-mapper table (one line per target). Each entry is a string of the form: '<start_sector> <num_sectors> <target_type> <target_args...>'"
  }
}
