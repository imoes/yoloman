{
  "allow_coredumps": {
    "type": "string",
    "default": "0",
    "description": "Set to '1' to allow Dovecot daemons to produce core dumps (disabled by default)",
    "enum": [
      "0",
      "1"
    ]
  }
}
