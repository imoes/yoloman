{
  "sequence": {
    "type": "string",
    "default": "/path/to/input.fasta",
    "description": "Path to the input sequence file or sequence identifier."
  },
  "newfile": {
    "type": "bool",
    "default": false,
    "description": "Whether to create a new output file (overwriting if exists)."
  },
  "outfile": {
    "type": "string",
    "default": "/path/to/output.txt",
    "description": "Path to the output file for the sequence reference list."
  }
}
