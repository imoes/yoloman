{
  "KEY_ID": {
    "type": "string",
    "default": "",
    "description": "GnuPG ID used for signing the reprepro repository (e.g. DEADBEEF)."
  },
  "TRUNK_RELEASE": {
    "type": "string",
    "default": "",
    "description": "Repository name to copy built packages to, creating a central trunk repository."
  },
  "MAIN_ARCHITECTURE": {
    "type": "string",
    "default": "",
    "description": "Architecture on which to build 'Architecture: all' packages. Defaults to host architecture if unset."
  },
  "PBUILDER_HOOKDIR": {
    "type": "string",
    "default": "/usr/share/jenkins-debian-glue/pbuilder-hookdir/",
    "description": "Directory containing hook scripts for pbuilder/cowbuilder."
  },
  "PBUILDER_CONFIG": {
    "type": "string",
    "default": "",
    "description": "Path to a user-specific pbuilder configuration file."
  },
  "DEFAULT_REPOSITORY": {
    "type": "string",
    "default": "/srv/repository",
    "description": "Base directory for reprepro repositories."
  },
  "USE_ORIG_VERSION": {
    "type": "bool",
    "default": false,
    "description": "If true, preserve the version number from the changelog without padding."
  },
  "REPOSITORY_KEYRING": {
    "type": "string",
    "default": "",
    "description": "Path to the keyring file holding the public key for signing repositories."
  },
  "REMOVE_FROM_RELEASE": {
    "type": "bool",
    "default": false,
    "description": "If true, remove packages from a release repository before processing incoming."
  },
  "DEB_KEEP_BUILD_ENV": {
    "type": "bool",
    "default": false,
    "description": "If true, keep a copy of the build environment on failure."
  },
  "REPOSITORY_EXTRA": {
    "type": "string",
    "default": "",
    "description": "Comma-separated list of extra repositories for resolving dependencies."
  },
  "REPOSITORY_EXTRA_KEYS": {
    "type": "string",
    "default": "",
    "description": "Comma-separated list of URLs with keys for extra repositories."
  },
  "ADT": {
    "type": "string",
    "default": "internal",
    "description": "Control autopkgtest execution: internal, external, all, or skip.",
    "enum": [
      "internal",
      "external",
      "all",
      "skip"
    ]
  },
  "USE_EATMYDATA": {
    "type": "bool",
    "default": false,
    "description": "Control usage of eatmydata to speed up builds (true/false or auto)."
  },
  "USE_CCACHE": {
    "type": "bool",
    "default": false,
    "description": "Enable ccache for fast recompilation of C/C++ code."
  },
  "TIMESTAMP_FORMAT": {
    "type": "string",
    "default": "%Y%m%d%H%M%S",
    "description": "Format string for the generated build timestamp."
  },
  "UNRELEASED_APPEND_COMMIT": {
    "type": "bool",
    "default": false,
    "description": "If true, append SCM commit to version number even if marked UNRELEASED."
  }
}
