{
  "dhcp6c_bin": {
    "type": "string",
    "default": "/usr/sbin/dhcp6c",
    "description": "Path to the dhcp6c binary executable."
  },
  "dhcp6c_pid_file": {
    "type": "string",
    "default": "/var/run/dhcp6c.pid",
    "description": "Path to the PID file for the dhcp6c daemon."
  },
  "dhcp6c_name": {
    "type": "string",
    "default": "dhcp6c",
    "description": "Name identifier for the service."
  },
  "dhcp6c_desc": {
    "type": "string",
    "default": "WIDE DHCPv6 client",
    "description": "Human-readable description of the service."
  },
  "stop_retry_schedule": {
    "type": "string",
    "default": "TERM/20/forever/KILL/1",
    "description": "Retry schedule for stopping the daemon (signal/delay/count/signal/delay)."
  }
}
