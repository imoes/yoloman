{
  "listen_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address (e.g., 127.0.0.1, 192.168.1.10, or ::) to bind iiod listening socket. Use 0.0.0.0 for all interfaces."
  },
  "port": {
    "type": "number",
    "default": 1024,
    "description": "TCP port for remote IIO connections. Ports < 1024 require root; use \u2265 1024 for non-root operation."
  },
  "transport": {
    "type": "string",
    "default": "ip",
    "description": "Communication transport method: 'ip' (network), 'usb' (USB-IO), or 'local' (local IIO devices only)."
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Logging verbosity. One of: debug, info, warning, error. Higher levels suppress less critical logs."
  },
  "enable_tls": {
    "type": "bool",
    "default": false,
    "description": "Enable TLS encryption for remote connections. Requires valid cert/key paths."
  },
  "tls_cert_path": {
    "type": "string",
    "default": "/etc/iiod/tls/server.crt",
    "description": "Path to PEM-encoded TLS server certificate. Required if TLS is enabled."
  },
  "tls_key_path": {
    "type": "string",
    "default": "/etc/iiod/tls/server.key",
    "description": "Path to PEM-encoded TLS private key. Required if TLS is enabled."
  },
  "ca_cert_path": {
    "type": "string",
    "default": "",
    "description": "Path to PEM-encoded CA certificate to validate client certs (mutual TLS). Leave empty to disable client verification."
  },
  "allowed_clients": {
    "type": "list",
    "default": [],
    "description": "List of allowed client hostnames or IP addresses (exact match only). Empty list = allow all.",
    "items": {
      "type": "string",
      "description": "Client hostname or IP address string (e.g., 192.168.1.50, iio-client.local)."
    }
  },
  "enable_discovery": {
    "type": "bool",
    "default": false,
    "description": "Enable mDNS/DNS-SD publishing of this IIO daemon so clients can auto-discover it."
  },
  "discovery_name": {
    "type": "string",
    "default": "",
    "description": "Custom mDNS name to advertise (e.g., 'my-iio-server'). If omitted, hostname is used."
  },
  "backend_options": {
    "type": "object",
    "default": {},
    "description": "Key-value options passed directly to the active backend (e.g., timeout_ms, keepalive). See backend documentation for supported keys."
  }
}
