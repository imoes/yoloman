{
  "at_allow": {
    "type": "list",
    "default": [],
    "description": "List of usernames explicitly permitted to submit scheduled jobs using `at`. If this list is non-empty, only these users may use `at`; the `at.deny` file is ignored.",
    "items": {
      "type": "string",
      "description": "System username allowed to schedule one-time jobs.",
      "default": ""
    }
  },
  "at_deny": {
    "type": "list",
    "default": [],
    "description": "List of usernames explicitly blocked from submitting `at` jobs. Ignored if `at_allow` is non-empty.",
    "items": {
      "type": "string",
      "description": "System username denied from using `at`.",
      "default": ""
    }
  }
}
