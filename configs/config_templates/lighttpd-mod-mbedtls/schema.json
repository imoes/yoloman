{
  "mod_name": {
    "type": "string",
    "default": "mod_mbedtls",
    "description": "The name of the lighttpd SSL module to load."
  },
  "ssl_pemfile": {
    "type": "string",
    "default": "/etc/lighttpd/server.pem",
    "description": "Path to the SSL PEM file containing the server certificate and private key."
  },
  "ssl_listen_addr": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "The IP address to bind the SSL listener to."
  },
  "ssl_listen_port": {
    "type": "number",
    "default": 443,
    "description": "The port number for the SSL listener."
  }
}
