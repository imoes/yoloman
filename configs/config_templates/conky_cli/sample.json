{
  "update_interval": 3,
  "time_format": "%a %b %d %H:%M:%S %Y",
  "alignment": "right",
  "font": "DejaVu Sans Mono:size=9",
  "default_color": "#A0A0A0",
  "default_shade_color": "#202020",
  "default_outline_color": "#000000",
  "disks": [
    {
      "mountpoint": "/"
    },
    {
      "mountpoint": "/home"
    }
  ],
  "network_interfaces": [
    {
      "name": "enp3s0"
    },
    {
      "name": "wlp4s0"
    }
  ],
  "cpu_cpu_num": 4,
  "show_swap": false,
  "max_user_text": 131072,
  "process_sort": "percent_cpu",
  "show_user_processes": true,
  "text_header": "Conky CLI Monitor\n=================",
  "text_footer": "=================\nEnd of report",
  "text_body": "${color lightgrey}Hostname:${color} $nodename\n${color lightgrey}Uptime:${color} $uptime\n${color lightgrey}CPU:${color} ${cpu}%\n${color lightgrey}Load:${color} ${loadavg 1} ${loadavg 2} ${loadavg 3}\n${color lightgrey}Memory:${color} $mem / $memmax\n${color lightgrey}Disk /:${color} ${fs_used /} / ${fs_size /}\n${color lightgrey}Disk /home:${color} ${fs_used /home} / ${fs_size /home}\n${color lightgrey}Net enp3s0:${color} ${upspeed enp3s0} \u2191 / ${downspeed enp3s0} \u2193\n${color lightgrey}Top CPU:${color}\n${top name 1} (${top cpu 1}%) ${top mem 1}%\n${top name 2} (${top cpu 2}%) ${top mem 2}%"
}
