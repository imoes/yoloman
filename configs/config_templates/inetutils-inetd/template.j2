# Configuration for inetutils inetd (Internet super-server).
# Controls global command-line options passed to the inetd daemon process.

# inetd_opts: Command-line options for inetd. Defaults to empty string.
# This variable is mapped directly to the environment variable or service definition.
INETD_OPTS={{ (inetd_opts | default('')) | quote }}