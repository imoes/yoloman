{
  "inetd_opts": {
    "type": "string",
    "default": "",
    "description": "Command-line options for inetd. Defaults to empty string.",
    "enum": [
      "--foreground",
      "-d",
      "--debug",
      "--environment",
      "-p",
      "--pidfile",
      "-R",
      "--rate",
      "--resolve",
      "-V",
      "--version",
      "-?",
      "--help",
      "--usage"
    ]
  }
}
