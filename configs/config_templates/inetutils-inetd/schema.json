{
 "inetd_opts": {
  "description": "Command-line options for inetd. Defaults to empty string.",
  "enum": [
   "--foreground",
   "-d",
   "--debug",
   "--environment",
   "-p",
   "--pidfile",
   "-R",
   "--rate",
   "--resolve",
   "-V",
   "--version",
   "-?",
   "--help",
   "--usage"
  ],
  "type": "string"
 }
}
