{
 "external_ids": {
  "description": "Key-value pairs to attach as external IDs to the VTEP logical switch in OVN databases",
  "type": "object"
 },
 "log_file": {
  "default": "/var/log/ovn/ovn-controller-vtep.log",
  "description": "Path to log file (use '-' for stdout, empty for default)",
  "type": "string"
 },
 "log_level": {
  "default": "info",
  "description": "Logging verbosity level (one of: info, debug, err, warn)",
  "type": "string"
 },
 "max_tunnel_key": {
  "default": 16777215,
  "description": "Upper limit for VNI (tunnel key) allocation (must be ≤ 16777215)",
  "type": "number"
 },
 "max_vtep_connections": {
  "default": 10,
  "description": "Maximum number of VTEP connections (tunnels) to maintain",
  "type": "number"
 },
 "nb_db_connection": {
  "default": "tcp:127.0.0.1:6641",
  "description": "Connection string to OVN Northbound database (format: <protocol>:<host>:<port>)",
  "type": "string"
 },
 "sb_db_connection": {
  "default": "tcp:127.0.0.1:6642",
  "description": "Connection string to OVN Southbound database (format: <protocol>:<host>:<port>)",
  "type": "string"
 },
 "ssl_enable": {
  "default": false,
  "description": "Enable SSL/TLS for database connections (requires certificate files if true)",
  "type": "boolean"
 },
 "tls_ca_cert": {
  "default": "",
  "description": "Path to CA certificate for TLS database connection (leave empty to disable TLS)",
  "type": "string"
 },
 "tls_cert": {
  "default": "",
  "description": "Path to server certificate for TLS (required if TLS enabled)",
  "type": "string"
 },
 "tls_private_key": {
  "default": "",
  "description": "Path to server private key for TLS (required if TLS enabled)",
  "type": "string"
 },
 "vtep_enabled": {
  "default": true,
  "description": "Enable VTEP functionality on this controller",
  "type": "boolean"
 },
 "vtep_logical_switch_uuid": {
  "default": "",
  "description": "UUID of the VTEP logical switch to manage (must exist in OVN NB DB)",
  "type": "string"
 },
 "vtep_physical_interface": {
  "default": "br-vxlan",
  "description": "Name of the Linux network interface used for VTEP tunnel endpoints",
  "type": "string"
 },
 "vtep_physical_ip": {
  "default": "",
  "description": "IPv4/IPv6 address to bind VTEP tunnels to (typically the IP of vtep_physical_interface)",
  "type": "string"
 },
 "vtep_physical_networks": {
  "default": [],
  "description": "List of additional physical network mappings (beyond the main interface)",
  "items": {
   "interface": {
    "description": "Linux interface name to bind this network to",
    "type": "string"
   },
   "name": {
    "description": "Name of the physical network",
    "type": "string"
   }
  },
  "type": "list"
 }
}
