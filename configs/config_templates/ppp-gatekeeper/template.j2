# Don't forward local domains or local addies
domain-needed
bogus-priv

# Resolver config: Path to the resolver configuration file used by ppp-gatekeeper.
# Uses /etc/ppp/resolv-pppgk.conf by default.
resolv-file={{ resolv_file | default('/etc/ppp/resolv-pppgk.conf') }}