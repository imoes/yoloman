{
  "output_format": {
    "type": "string",
    "default": "text",
    "description": "Output format: 'text' (human-readable) or 'json' (machine-readable)",
    "enum": [
      "text",
      "json"
    ]
  },
  "distance_precision": {
    "type": "number",
    "default": 2,
    "description": "Number of decimal places for distance values (e.g., meters/kilometers)"
  },
  "time_precision": {
    "type": "number",
    "default": 2,
    "description": "Number of decimal places for time values (e.g., seconds/hours)"
  },
  "elevation_precision": {
    "type": "number",
    "default": 1,
    "description": "Number of decimal places for elevation values (e.g., meters)"
  },
  "gpx_files": {
    "type": "list",
    "default": [],
    "description": "List of GPX file paths to process by default (can be overridden by command-line arguments)"
  }
}
