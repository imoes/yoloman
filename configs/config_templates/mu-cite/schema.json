{
  "emacs_flavor_default": {
    "type": "string",
    "default": "emacs",
    "description": "The default Emacs flavor name used if debian-emacs-flavor is not bound. Typically 'emacs' for GNU Emacs or 'xemacs' for XEmacs."
  }
}
