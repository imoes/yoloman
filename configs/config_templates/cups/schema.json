{
  "snmp_address": {
    "type": "string",
    "default": "@LOCAL",
    "description": "SNMP address for CUPS SNMP monitoring (typically '@LOCAL' for local SNMP agent, or an IP address)"
  },
  "snmp_community": {
    "type": "string",
    "default": "public",
    "description": "SNMP community string used for polling (usually 'public' for read-only access)"
  }
}
