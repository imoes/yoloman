-- PostgreSQL PL/Lua procedural language extension
-- No runtime daemon or config file — this is a PostgreSQL extension loaded at session/database creation
-- Install the extension in a database using: CREATE EXTENSION pllua;
-- This template exists only to satisfy the configuration management framework.
-- Actual configuration is done via PostgreSQL GUCs and Lua runtime settings within the database session.
