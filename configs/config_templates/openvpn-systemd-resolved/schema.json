{
  "dbus_dest": {
    "type": "string",
    "default": "org.freedesktop.resolve1",
    "description": "The D-Bus destination name for systemd-resolved manager."
  },
  "dbus_node": {
    "type": "string",
    "default": "/org/freedesktop/resolve1",
    "description": "The D-Bus object path for systemd-resolved manager."
  }
}
