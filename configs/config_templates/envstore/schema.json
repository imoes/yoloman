{
  "envstore_file": {
    "type": "string",
    "default": "/tmp/envstore-EUID",
    "description": "File path used to store environment variables. Defaults to /tmp/envstore-EUID if unset."
  },
  "euid": {
    "type": "string",
    "default": "",
    "description": "Effective user ID string; used only when envstore_file is not specified and default path expansion is needed."
  }
}
