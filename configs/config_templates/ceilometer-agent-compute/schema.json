{
  "init_provides": {
    "type": "string",
    "default": "ceilometer-agent-compute",
    "description": "Provides directive for LSB init header"
  },
  "short_description": {
    "type": "string",
    "default": "ceilometer central compute",
    "description": "Short description for LSB init header"
  },
  "description": {
    "type": "string",
    "default": "ceilometer central compute",
    "description": "Full description for LSB init header"
  },
  "desc": {
    "type": "string",
    "default": "OpenStack Ceilometer Agent Compute (ceilometer-agent-compute)",
    "description": "DESC variable value"
  },
  "project_name": {
    "type": "string",
    "default": "ceilometer",
    "description": "PROJECT_NAME variable value"
  }
}
