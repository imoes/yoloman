{
  "rule_sets": {
    "type": "list",
    "default": [],
    "description": "List of rule sets. Each rule set contains an optional user/group key record and one or more rule records. Each rule record specifies a component, option, flag, and optional value to override globally for all users in the key group. See 'gpgconf.conf' format in gpgconf(1)."
  }
}
