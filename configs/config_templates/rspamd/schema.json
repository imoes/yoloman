{
  "init_daemon_user": {
    "type": "string",
    "default": "_rspamd",
    "description": "System user to run Rspamd daemons (used in init.d script)."
  },
  "init_daemon_group": {
    "type": "string",
    "default": "_rspamd",
    "description": "System group to run Rspamd daemons (used in init.d script)."
  },
  "rspamd_normal_bind": {
    "type": "string",
    "default": "localhost:11333",
    "description": "Bind address and port for the Rspamd normal worker."
  },
  "rspamd_controller_bind": {
    "type": "string",
    "default": "localhost:11334",
    "description": "Bind address and port for the Rspamd controller worker."
  },
  "rspamd_proxy_bind": {
    "type": "string",
    "default": "localhost:11332",
    "description": "Bind address and port for the Rspamd proxy worker."
  },
  "rspamd_fuzzy_bind": {
    "type": "string",
    "default": "localhost:11335",
    "description": "Bind address and port for the Rspamd fuzzy worker."
  }
}
