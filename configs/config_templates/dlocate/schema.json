{
  "PATH": {
    "type": "string",
    "default": "$PATH:/usr/bin:/bin:/usr/sbin:/sbin",
    "description": "The PATH environment variable used when executing update-dlocatedb"
  },
  "UPDATE_DLOCATEDB_CMD": {
    "type": "string",
    "default": "/usr/sbin/update-dlocatedb -b",
    "description": "The command executed to update the dlocate database"
  }
}
