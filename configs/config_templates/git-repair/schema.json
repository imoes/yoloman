{
  "force": {
    "type": "bool",
    "default": false,
    "description": "If true, enables --force mode to rewrite history when standard repair fails. Never touches tags."
  },
  "numtests": {
    "type": "number",
    "default": 10,
    "description": "Number of random corruption tests to perform in self-test mode (--test)."
  },
  "retry": {
    "type": "number",
    "default": 3,
    "description": "Number of retries for each self-test in case of transient failure (--test)."
  }
}
