{
  "command_line_args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments to pass to gameconqueror. Each item must be a string representing a single argument (e.g., '--search=42', '-s', etc.). Only documented options (-v, -h, -s/--search=) should be used, per the man page. Arguments must be complete and valid as per gameconqueror(1)."
  }
}
