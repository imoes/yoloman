# gameconqueror configuration - command-line arguments
# This file contains a list of command-line arguments to pass to gameconqueror.
# Each item is written as a separate line, indented by 2 spaces.
# The list is defined by the 'command_line_args' variable.
{% for arg in command_line_args | default([]) -%}
  {{ arg }}
{% endfor -%}