{
  "path_env": {
    "type": "string",
    "default": "/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin",
    "description": "The PATH environment variable for the cron job."
  },
  "cron_interval": {
    "type": "string",
    "default": "15",
    "description": "The minute interval (in minutes) at which sanoid runs via cron. Default is every 15 minutes."
  }
}
