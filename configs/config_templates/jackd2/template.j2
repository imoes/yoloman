# JACK server startup configuration (for jackd2)
# This file is sourced by jackd2 via the -C option or jack_control
# Uncomment and/or edit options as needed for your system
# See `man jackd2` and `man jack_control` for full documentation

# Server startup mode: -d (driver), -r (realtime), -p (period), -n (nperiods), -s (sample rate), -i (input channels), -o (output channels)
{% if driver == 