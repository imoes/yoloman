{
  "format": {
    "type": "string",
    "default": "svg",
    "description": "Output format: 'svg' or 'html'. Corresponds to the --format option.",
    "enum": [
      "svg",
      "html"
    ]
  }
}
