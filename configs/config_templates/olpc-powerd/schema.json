{
  "switchd_poll_interval": {
    "type": "number",
    "default": 10,
    "description": "Interval in seconds for polling power events"
  },
  "switchd_report_interval": {
    "type": "number",
    "default": 6,
    "description": "Periodic timer interval in minutes for reporting"
  },
  "switchd_pid_file": {
    "type": "string",
    "default": "/var/run/powerevents",
    "description": "Path to the PID file for the switchd process"
  }
}
