{
  "error_on_empty_stdout": {
    "type": "bool",
    "default": false,
    "description": "Whether to treat empty standard output as an error condition (not part of cronic itself; user must implement in wrapper script if desired)"
  }
}
