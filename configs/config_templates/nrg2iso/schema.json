{
  "nrg_file": {
    "type": "string",
    "default": "/tmp/input.nrg",
    "description": "Path to the input Nero .nrg file to be converted."
  },
  "iso_file": {
    "type": "string",
    "default": "/tmp/output.iso",
    "description": "Path to the output ISO9660 file."
  },
  "display_help": {
    "type": "bool",
    "default": false,
    "description": "If true, displays the help screen instead of converting."
  },
  "display_version": {
    "type": "bool",
    "default": false,
    "description": "If true, displays the version instead of converting."
  }
}
