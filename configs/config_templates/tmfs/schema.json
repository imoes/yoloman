{
  "hfs_root_mount": {
    "type": "string",
    "default": "/mnt/hfs",
    "description": "The path to the mount point of your HFS+ Time Machine volume."
  },
  "time_machine_mount": {
    "type": "string",
    "default": "/mnt/tm",
    "description": "The path where the Time Machine will be mounted. This should be an empty directory."
  },
  "fuse_options": {
    "type": "string",
    "default": "uid=1000,gid=1000,allow_other",
    "description": "A comma separated list of options that will be passed to fuse(8)."
  }
}
