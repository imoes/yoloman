{
  "host": {
    "type": "string",
    "default": "ldap.forumsys.com",
    "description": "LDAP server hostname"
  },
  "port": {
    "type": "number",
    "default": 389,
    "description": "LDAP server port"
  },
  "timeout": {
    "type": "string",
    "default": "30s",
    "description": "LDAP connection timeout (e.g., '30s')"
  },
  "bind_dn_suffix": {
    "type": "string",
    "default": "dc=example,dc=com",
    "description": "Suffix appended to 'uid=%s,' for single-bind mode bind-dn"
  },
  "admin_search_base_dn": {
    "type": "string",
    "default": "dc=example,dc=com",
    "description": "Base DN for user search in admin-bind mode"
  },
  "admin_bind_dn": {
    "type": "string",
    "default": "cn=read-only-admin,dc=example,dc=com",
    "description": "Admin DN to bind as in admin-bind mode"
  },
  "admin_bind_password": {
    "type": "string",
    "default": "password",
    "description": "Password for admin-bind DN"
  },
  "require_group_1": {
    "type": "string",
    "default": "ou=mathematicians,dc=example,dc=com",
    "description": "Required group DN (example); add more via require-group_N variables if needed"
  },
  "group_search_base_dn": {
    "type": "string",
    "default": "dc=example,dc=com",
    "description": "Base DN for group search"
  },
  "member_of_attribute": {
    "type": "string",
    "default": "memberOf",
    "description": "User attribute used for group membership (e.g., 'memberOf' in AD)"
  }
}
