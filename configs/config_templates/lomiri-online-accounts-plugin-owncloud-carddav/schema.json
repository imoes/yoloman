{
  "profile_name": {
    "type": "string",
    "default": "lomiri-owncloud-carddav",
    "description": "The name of the main profile."
  },
  "profile_type": {
    "type": "string",
    "default": "sync",
    "description": "The type of the main profile."
  },
  "category_value": {
    "type": "string",
    "default": "contacts",
    "description": "The category for this profile."
  },
  "remote_service_name": {
    "type": "string",
    "default": "lomiri-owncloud-carddav",
    "description": "The remote service name identifier."
  },
  "destinationtype_value": {
    "type": "string",
    "default": "online",
    "description": "The destination type for the profile."
  },
  "enabled_value": {
    "type": "string",
    "default": "false",
    "description": "Whether the main profile is enabled.",
    "enum": [
      "true",
      "false"
    ]
  },
  "hidden_value": {
    "type": "string",
    "default": "false",
    "description": "Whether the main profile is hidden.",
    "enum": [
      "true",
      "false"
    ]
  },
  "use_accounts_value": {
    "type": "string",
    "default": "true",
    "description": "Whether to use accounts for this profile.",
    "enum": [
      "true",
      "false"
    ]
  },
  "client_profile_type": {
    "type": "string",
    "default": "client",
    "description": "The type of the client profile."
  },
  "client_profile_name": {
    "type": "string",
    "default": "carddav",
    "description": "The name of the client profile."
  },
  "sync_direction_value": {
    "type": "string",
    "default": "two-way",
    "description": "The sync direction for the client profile."
  },
  "sync_protocol_value": {
    "type": "string",
    "default": "carddav",
    "description": "The sync protocol for the client profile."
  },
  "sync_transport_value": {
    "type": "string",
    "default": "HTTP",
    "description": "The sync transport for the client profile."
  },
  "conflictpolicy_value": {
    "type": "string",
    "default": "prefer remote",
    "description": "The conflict resolution policy for the client profile."
  },
  "schedule_enabled": {
    "type": "string",
    "default": "false",
    "description": "Whether the schedule is enabled.",
    "enum": [
      "true",
      "false"
    ]
  },
  "schedule_interval": {
    "type": "string",
    "default": "",
    "description": "The sync interval string."
  },
  "schedule_days": {
    "type": "string",
    "default": "1,2,3,4,5,6,7",
    "description": "Comma-separated list of days for the schedule."
  },
  "schedule_syncconfiguredtime": {
    "type": "string",
    "default": "",
    "description": "The sync configured time string."
  },
  "schedule_time": {
    "type": "string",
    "default": "05:00:00",
    "description": "The time for the scheduled sync."
  }
}
