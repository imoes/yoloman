{
  "listen_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address the finger daemon listens on (e.g., '0.0.0.0' for all interfaces, or '127.0.0.1' for localhost only)."
  },
  "listen_port": {
    "type": "number",
    "default": 79,
    "description": "TCP port the finger daemon listens on. Standard finger port is 79."
  },
  "userinfo_enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether to display user information (login name, full name, idle time, etc.) when a user is queried."
  },
  "hostname_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether to resolve and display hostnames for remote IP addresses."
  },
  "syslog_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether to log requests and errors to the system syslog."
  },
  "max_connections": {
    "type": "number",
    "default": 100,
    "description": "Maximum number of simultaneous connections the daemon will accept."
  },
  "welcome_file": {
    "type": "string",
    "default": "/etc/finger.welcome",
    "description": "Path to the file containing the welcome message shown on client connect (if it exists)."
  },
  "banner_file": {
    "type": "string",
    "default": "/etc/finger.banner",
    "description": "Path to the file containing the banner shown before user information (if it exists)."
  },
  "search_dirs": {
    "type": "list",
    "default": [
      "/etc",
      "/var/run"
    ],
    "description": "Ordered list of directories where the daemon looks for per-user information files."
  },
  "allowed_hosts": {
    "type": "list",
    "default": [],
    "description": "Whitelist of hosts permitted to query the finger daemon. Leave empty to allow all."
  },
  "denied_hosts": {
    "type": "list",
    "default": [],
    "description": "Blacklist of hosts blocked from querying the finger daemon. Empty by default."
  },
  "longformat_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable the `-l` long listing format (shows login shell, home directory, etc.)."
  },
  "timeout_seconds": {
    "type": "number",
    "default": 60,
    "description": "Idle timeout in seconds for client connections. Default per man page is 60."
  }
}
