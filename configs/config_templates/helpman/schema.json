{
  "manual_search_default": {
    "type": "string",
    "default": "",
    "description": "Default text in the 'Select Manual' text/combo box, used for quick manual lookup"
  },
  "manual_sections": {
    "type": "list",
    "default": [
      1,
      2,
      3,
      4,
      5,
      6,
      7,
      8
    ],
    "description": "List of manual sections (1\u20138) to show in the 'Manual Type' list box; corresponds to man sections (e.g., 1=user commands, 8=system administration)"
  }
}
