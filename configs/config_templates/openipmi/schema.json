{
  "DEV_IPMI": {
    "default": "yes",
    "description": "Enable /dev/ipmi0 interface, used by ipmitool, ipmicmd, and other userspace IPMI-using applications.",
    "type": "string",
    "enum": [
      "yes",
      "no"
    ]
  },
  "IPMI_IMB": {
    "default": "no",
    "description": "Enable \"legacy\" interfaces for applications Intel IMB driver interface",
    "type": "string",
    "enum": [
      "yes",
      "no"
    ]
  },
  "IPMI_POWERCYCLE": {
    "default": "no",
    "description": "Enable IPMI_POWERCYCLE if you want the system to be power-cycled on reboot",
    "type": "string",
    "enum": [
      "yes",
      "no"
    ]
  },
  "IPMI_POWEROFF": {
    "default": "no",
    "description": "Enable IPMI_POWEROFF if you want the IPMI poweroff module to be loaded.",
    "type": "string",
    "enum": [
      "yes",
      "no"
    ]
  },
  "IPMI_SI": {
    "default": "yes",
    "description": "Enable standard hardware interfaces (KCS, BT, SMIC)",
    "type": "string",
    "enum": [
      "yes",
      "no"
    ]
  },
  "IPMI_WATCHDOG": {
    "default": "no",
    "description": "Enable IPMI_WATCHDOG if you want the IPMI watchdog to reboot the system if it hangs",
    "type": "string",
    "enum": [
      "yes",
      "no"
    ]
  },
  "IPMI_WATCHDOG_OPTIONS": {
    "default": "timeout=60",
    "description": "Watchdog options - modinfo ipmi_watchdog for details",
    "type": "string"
  }
}
