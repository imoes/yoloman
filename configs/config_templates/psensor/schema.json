{
  "psensor_url": {
    "type": "string",
    "default": "http://localhost:3131",
    "description": "The URL of the psensor-server (e.g., http://hostname:3131)"
  },
  "psensor_debug_level": {
    "type": "number",
    "default": 0,
    "description": "Debug level, integer between 0 and 3"
  },
  "psensor_new_instance": {
    "type": "bool",
    "default": false,
    "description": "Force the creation of a new Psensor application instance"
  }
}
