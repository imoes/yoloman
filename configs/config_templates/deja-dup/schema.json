{
  "name": {
    "type": "string",
    "default": "Backup Monitor",
    "description": "Display name of the desktop entry"
  },
  "comment": {
    "type": "string",
    "default": "Schedules backups at regular intervals",
    "description": "Description/comment of the desktop entry"
  },
  "icon": {
    "type": "string",
    "default": "org.gnome.DejaDup",
    "description": "Icon name or path for the desktop entry"
  },
  "exec": {
    "type": "string",
    "default": "/usr/libexec/deja-dup/deja-dup-monitor",
    "description": "Command to execute when launching the application"
  },
  "autostart_delay": {
    "type": "number",
    "default": 120,
    "description": "Delay (in seconds) before starting the autostart application"
  },
  "startup_notify": {
    "type": "bool",
    "default": false,
    "description": "Whether to send startup notification"
  },
  "no_display": {
    "type": "bool",
    "default": true,
    "description": "Whether to hide the desktop entry from menus"
  },
  "type": {
    "type": "string",
    "default": "Application",
    "description": "Type of desktop entry",
    "enum": [
      "Application",
      "Link"
    ]
  },
  "categories": {
    "type": "string",
    "default": "Utility;Archiving",
    "description": "Categories in which this desktop entry should appear"
  }
}
