{
  "config_diagnostics": {
    "type": "number",
    "default": 0,
    "description": "Enable diagnostic output; set to 1 to make config errors fatal, 0 to ignore them."
  },
  "openssl_conf": {
    "type": "string",
    "default": "openssl_init",
    "description": "Name of the initialization section containing module selectors."
  },
  "includes": {
    "type": "list",
    "default": [],
    "description": "List of file/directory paths to include via .include directives."
  },
  "env_defaults": {
    "type": "object",
    "default": {},
    "description": "Map of environment variable names to default values for fallbacks."
  },
  "oid_section": {
    "type": "string",
    "default": "",
    "description": "Name of section containing custom OIDs (e.g., 'oids')."
  },
  "oids": {
    "type": "object",
    "default": {},
    "description": "Map of OID short names to 'long_name, 1.2.3.4' values."
  },
  "providers": {
    "type": "string",
    "default": "",
    "description": "Name of providers config section (e.g., 'providers')."
  },
  "providers_config": {
    "type": "object",
    "default": {},
    "description": "Map of provider names (e.g., 'default') to their section names (e.g., 'default_provider')."
  },
  "provider_identities": {
    "type": "object",
    "default": {},
    "description": "Map of provider names to custom identity overrides."
  },
  "provider_modules": {
    "type": "object",
    "default": {},
    "description": "Map of provider names to shared library paths."
  },
  "provider_activated": {
    "type": "object",
    "default": {},
    "description": "Map of provider names to activation flags (values are ignored, presence activates)."
  },
  "provider_extra": {
    "type": "object",
    "default": {},
    "description": "Map of provider names to additional key-value config pairs."
  },
  "alg_section": {
    "type": "string",
    "default": "",
    "description": "Name of EVP properties section (e.g., 'evp_properties')."
  },
  "evp_default_properties": {
    "type": "string",
    "default": "",
    "description": "EVP default_properties query string (e.g., 'fips=yes')."
  },
  "ssl_conf": {
    "type": "string",
    "default": "",
    "description": "Name of SSL/TLS configuration section (e.g., 'ssl_configuration')."
  },
  "ssl_configs": {
    "type": "object",
    "default": {},
    "description": "Map of SSL config names (e.g., 'system_default', 'server') to section names."
  },
  "server_certs": {
    "type": "object",
    "default": {},
    "description": "Map of certificate types (e.g., 'RSA', 'ECDSA') to cert file paths (for server config)."
  },
  "extra_ssl_opts": {
    "type": "object",
    "default": {},
    "description": "Map of section names to extra SSL_CONF_cmd directives."
  },
  "min_proto_tls": {
    "type": "string",
    "default": "",
    "description": "Minimum TLS protocol version (e.g., '1.2') for system_default."
  },
  "min_proto_dtls": {
    "type": "string",
    "default": "",
    "description": "Minimum DTLS protocol version (e.g., '1.2') for system_default."
  },
  "engines": {
    "type": "string",
    "default": "",
    "description": "Name of ENGINE configuration section (e.g., 'engines')."
  },
  "engines_config": {
    "type": "object",
    "default": {},
    "description": "Map of engine names to config section names."
  },
  "engine_ids": {
    "type": "object",
    "default": {},
    "description": "Map of engine names to custom engine_id overrides."
  },
  "engine_paths": {
    "type": "object",
    "default": {},
    "description": "Map of engine names to dynamic_path values (shared library paths)."
  },
  "engine_inits": {
    "type": "object",
    "default": {},
    "description": "Map of engine names to init flags (0 or 1)."
  },
  "engine_default_algs": {
    "type": "object",
    "default": {},
    "description": "Map of engine names to default_algorithms strings (e.g., 'ALL')."
  },
  "engine_ctrls": {
    "type": "object",
    "default": {},
    "description": "Map of engine names to additional ctrl commands and values."
  },
  "random": {
    "type": "string",
    "default": "CTR-DRBG",
    "description": "Name of random config section (e.g., 'random').",
    "enum": [
      "CTR-DRBG",
      "HASH-DRBG",
      "HMAC-DRBG"
    ]
  },
  "rng_type": {
    "type": "string",
    "default": "CTR-DRBG",
    "description": "Random bit generator type: CTR-DRBG, HASH-DRBG, or HMAC-DRBG.",
    "enum": [
      "CTR-DRBG",
      "HASH-DRBG",
      "HMAC-DRBG"
    ]
  },
  "rng_cipher": {
    "type": "string",
    "default": "AES-256-CTR",
    "description": "Cipher for CTR-DRBG (ignored by others)."
  },
  "rng_digest": {
    "type": "string",
    "default": "",
    "description": "Digest for HASH/HMAC-DRBG (ignored by others)."
  },
  "rng_properties": {
    "type": "string",
    "default": "",
    "description": "Property query string for RNG and underlying algorithms."
  },
  "rng_seed": {
    "type": "string",
    "default": "",
    "description": "Randomness source path (e.g., /dev/urandom)."
  },
  "rng_seed_properties": {
    "type": "string",
    "default": "",
    "description": "Property query for the randomness source."
  }
}
