{
  "redsocks_base_ip": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "Base IP address used for internal redsocks operations (usually unchanged)."
  },
  "redsocks_base_port": {
    "type": "number",
    "default": 12345,
    "description": "Base port used for internal redsocks operations (usually unchanged)."
  },
  "redsocks_log_level": {
    "type": "string",
    "default": "warn",
    "description": "Verbosity of logs: debug, info, notice, warn, error, crit, alert, or emerg."
  },
  "redsocks_log_file": {
    "type": "string",
    "default": "/var/log/redsocks/redsocks.log",
    "description": "Path to log file. Use 'stderr' to log to standard error."
  },
  "redsocks_user": {
    "type": "string",
    "default": "nobody",
    "description": "System user to drop privileges to after startup (empty string retains root)."
  },
  "redsocks_group": {
    "type": "string",
    "default": "nogroup",
    "description": "System group to drop privileges to after startup."
  },
  "redsocks_connect_timeout": {
    "type": "number",
    "default": 30,
    "description": "Timeout (in seconds) for establishing upstream connections."
  },
  "redsocks_max_connections": {
    "type": "number",
    "default": 1024,
    "description": "Maximum number of simultaneous client connections allowed."
  },
  "redsocks_daemon": {
    "type": "bool",
    "default": true,
    "description": "Whether to run as a background daemon (true) or foreground process (false)."
  },
  "redsocks_type": {
    "type": "string",
    "default": "tcp",
    "description": "Protocol type handled \u2014 currently only 'tcp' is supported by redsocks."
  },
  "redsocks_servers": {
    "type": "list",
    "default": [],
    "description": "List of upstream SOCKS proxy servers to redirect traffic through."
  }
}
