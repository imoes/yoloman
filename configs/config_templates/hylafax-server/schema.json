{
  "spool_log_file": {
    "type": "string",
    "default": "/var/spool/hylafax/log/xferfaxlog",
    "description": "Path to the HylaFAX fax log file."
  },
  "email_address": {
    "type": "string",
    "default": "faxmaster",
    "description": "Email address to send monthly fax statistics to."
  },
  "log_rotation_count": {
    "type": "number",
    "default": 5,
    "description": "Number of log files to keep during rotation."
  }
}
