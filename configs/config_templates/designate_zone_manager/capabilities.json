{
  "provides": [
    {
      "capability": "dns_authoritative",
      "backend": "bind9",
      "port_field": "bind_port",
      "default_port": 9001,
      "provisionable": {}
    }
  ],
  "requires": [
    {
      "capability": "database",
      "backends": [
        "mysql",
        "mariadb",
        "postgresql"
      ],
      "backend_field": "storage_sql_connection",
      "fields": {
        "host": "storage_sql_connection",
        "name": "storage_sql_connection",
        "user": "storage_sql_connection",
        "password": "storage_sql_connection"
      }
    },
    {
      "capability": "message_queue",
      "backends": [
        "rabbitmq"
      ],
      "backend_field": "transport_url",
      "fields": {
        "host": "transport_url",
        "port": "transport_url",
        "user": "transport_url",
        "password": "transport_url",
        "vhost": "transport_url"
      }
    }
  ],
  "peer_injection": [],
  "confidence": "high"
}
