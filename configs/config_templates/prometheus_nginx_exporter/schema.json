{
  "args": {
    "type": "list",
    "default": [
      "--nginx.scrape-uri=http://127.0.0.1:80/nginx_status"
    ],
    "description": "Command-line arguments passed to the exporter. Must include at least one --nginx.scrape-uri pointing to a valid NGINX stub_status endpoint (e.g., http://localhost:80/nginx_status)."
  },
  "env": {
    "type": "object",
    "default": {},
    "description": "Optional environment variables (e.g., HTTP_PROXY, NO_PROXY). Each key-value pair will be exported to the service environment.",
    "items": {
      "type": "string",
      "description": "Value for the environment variable."
    }
  }
}
