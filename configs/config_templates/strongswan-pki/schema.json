{
  "pki_plugins": {
    "type": "list",
    "default": [],
    "description": "Comma-separated list of plugins to load in the pki tool."
  },
  "pki_scep_http_bind": {
    "type": "string",
    "default": "",
    "description": "Source IP address to bind for HTTP operations."
  },
  "pki_scep_http_timeout": {
    "type": "string",
    "default": "30s",
    "description": "Timeout for HTTP operations."
  },
  "pki_scep_renewal_via_pkcs_req": {
    "type": "string",
    "default": "no",
    "description": "Whether to allow renewal via PKCSReq instead of RenewalReq."
  }
}
