{
  "server": {
    "type": "object",
    "default": [],
    "description": "Core server runtime settings (used only for cron cache directory configuration in this snippet; full server config resides in main lighttpd.conf)"
  },
  "server.cache_dir": {
    "type": "string",
    "default": "/var/cache/lighttpd",
    "description": "The base directory for lighttpd caches, including compress and uploads subdirectories."
  },
  "server.runas_user": {
    "type": "string",
    "default": "www-data",
    "description": "The system user account under which the lighttpd cron job executes."
  }
}
