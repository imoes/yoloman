{
  "server.document_root": {
    "type": "string",
    "default": "/var/www/html",
    "description": "The root directory from which lighttpd serves files."
  },
  "server.upload_dir": {
    "type": "string",
    "default": "/var/cache/lighttpd",
    "description": "The base directory for lighttpd caches, including uploads subdirectory."
  },
  "server.error_log": {
    "type": "string",
    "default": "/var/log/lighttpd/error.log",
    "description": "The path to the error log file."
  },
  "server.pid_file": {
    "type": "string",
    "default": "/run/lighttpd.pid",
    "description": "The path to the PID file."
  },
  "server.runas_user": {
    "type": "string",
    "default": "www-data",
    "description": "The system user account under which lighttpd runs."
  },
  "server.port": {
    "type": "number",
    "default": 80,
    "description": "The port lighttpd listens on."
  }
}
