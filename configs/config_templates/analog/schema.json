{
  "log_path": {
    "type": "string",
    "default": "/var/log/apache2/access.log*",
    "description": "Path pattern for the log files to analyze."
  },
  "report_output_file": {
    "type": "string",
    "default": "Report.html",
    "description": "Path to the output HTML report file."
  }
}
