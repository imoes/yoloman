{
  "user_create_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable user creation functionality."
  },
  "user_create_min_uname_len": {
    "type": "number",
    "default": 4,
    "description": "Minimum length for the local part of the user's username."
  },
  "user_create_uid_min": {
    "type": "number",
    "default": 5000,
    "description": "Minimum UID for newly created users."
  },
  "user_create_uid_max": {
    "type": "number",
    "default": 8999,
    "description": "Maximum UID for newly created users."
  },
  "user_create_name_in_title": {
    "type": "bool",
    "default": true,
    "description": "Include the user's full name in the title/header of user pages."
  },
  "user_create_max_per_user": {
    "type": "number",
    "default": 5,
    "description": "Maximum number of users a single administrator can create."
  }
}
