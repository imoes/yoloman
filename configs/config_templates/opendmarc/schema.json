{
  "user": {
    "type": "string",
    "default": "opendmarc",
    "description": "System user account under which OpenDMARC runs (must have minimal privileges)."
  },
  "group": {
    "type": "string",
    "default": "opendmarc",
    "description": "System group for the OpenDMARC process."
  },
  "run_dir": {
    "type": "string",
    "default": "/run/opendmarc",
    "description": "Runtime directory for PID file and Unix socket (e.g., /run/opendmarc or /var/spool/postfix/run/opendmarc)."
  }
}
