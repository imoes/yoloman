{
  "nextcloud_server_url": {
    "type": "string",
    "default": "https://cloud.example.org",
    "description": "URL to the Nextcloud server instance."
  },
  "nextcloud_username": {
    "type": "string",
    "default": "example",
    "description": "Username for Nextcloud API authentication."
  },
  "nextcloud_password": {
    "type": "string",
    "default": "example",
    "description": "Password for Nextcloud API authentication."
  },
  "exporter_listen_address": {
    "type": "string",
    "default": ":9205",
    "description": "Address and port for the Prometheus exporter to listen on."
  },
  "exporter_timeout": {
    "type": "string",
    "default": "5s",
    "description": "Timeout for HTTP requests to the Nextcloud server."
  },
  "exporter_tls_skip_verify": {
    "type": "bool",
    "default": false,
    "description": "Skip TLS certificate verification when connecting to Nextcloud."
  },
  "exporter_info_apps_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable gathering of apps-related metrics."
  },
  "exporter_info_update_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable gathering of system update-related metrics."
  }
}
