{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options to pass to fwbuilder. Each item is a single option string, e.g., '-f /path/to/file.fwb'. Options must match documented flags: -f FILE, -r, -d, -h, -o FILE, -P OBJECT_NAME. Each option string should be a complete, single argument (no spaces unless quoted in shell)."
  }
}
