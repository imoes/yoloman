{
  "db_host": {
    "type": "string",
    "default": "localhost",
    "description": "Hostname or IP of the PostgreSQL server."
  },
  "db_port": {
    "type": "number",
    "default": 5432,
    "description": "Port number of the PostgreSQL server."
  },
  "db_name": {
    "type": "string",
    "default": "davical",
    "description": "Name of the PostgreSQL database used by Davical."
  },
  "db_user": {
    "type": "string",
    "default": "davical",
    "description": "Database username for Davical."
  },
  "db_password": {
    "type": "string",
    "default": "",
    "description": "Password for the database user (store securely in production\u2014e.g., via environment variables or secrets).",
    "sensitive": true
  },
  "site_url": {
    "type": "string",
    "default": "https://davical.example.com/",
    "description": "Publicly accessible base URL for this Davical instance (including protocol and trailing slash)."
  },
  "default_lang": {
    "type": "string",
    "default": "en",
    "description": "Default language for the interface (ISO 639-1 two-letter code)."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debug logging to syslog or PHP error log (disable in production)."
  },
  "max_connections": {
    "type": "number",
    "default": 100,
    "description": "Maximum number of concurrent database connections allowed."
  },
  "force_https": {
    "type": "bool",
    "default": true,
    "description": "Redirect all requests to HTTPS and enforce secure cookies (recommended for production)."
  },
  "allow_basic_auth": {
    "type": "bool",
    "default": false,
    "description": "Allow HTTP Basic authentication (needed for older clients; avoid if OAuth or certs are used)."
  },
  "allow_ssl_client_cert_auth": {
    "type": "bool",
    "default": false,
    "description": "Enable authentication via SSL client certificates."
  },
  "enable_carddav": {
    "type": "bool",
    "default": true,
    "description": "Enable CardDAV address book support."
  },
  "enable_caldav": {
    "type": "bool",
    "default": true,
    "description": "Enable CalDAV calendar support."
  },
  "allow_new_users": {
    "type": "bool",
    "default": false,
    "description": "Allow new user registration via the web UI (disabled by default in most production deployments)."
  },
  "default_user_role": {
    "type": "string",
    "default": "user",
    "description": "Default role assigned to newly created users (e.g., 'user' or 'admin')."
  },
  "allowed_domains": {
    "type": "list",
    "default": [],
    "description": "List of allowed email domains for self-registration (empty = all domains allowed). Each item is an email domain (e.g., 'example.com').",
    "items": {
      "type": "string",
      "description": "Allowed email domain (without '@', e.g., 'company.com')."
    }
  },
  "cal_prefix": {
    "type": "string",
    "default": "calendars/",
    "description": "URL path prefix for user calendars (must end with '/')."
  },
  "addr_prefix": {
    "type": "string",
    "default": "addrbooks/",
    "description": "URL path prefix for address books (must end with '/')."
  },
  "use_apc": {
    "type": "bool",
    "default": true,
    "description": "Use APCu for caching (improves performance; requires PHP APCu extension)."
  },
  "session_timeout": {
    "type": "number",
    "default": 3600,
    "description": "Session inactivity timeout in seconds (0 = session ends at browser close)."
  },
  "timezone": {
    "type": "string",
    "default": "UTC",
    "description": "PHP timezone identifier (e.g., 'America/New_York' or 'Europe/Berlin')."
  },
  "enable_push": {
    "type": "bool",
    "default": false,
    "description": "Enable server-side push notifications (requires a separate push server like 'davical-push')."
  },
  "push_server_url": {
    "type": "string",
    "default": "",
    "description": "Base URL of the push server (e.g., 'https://push.example.com/'). Leave empty to disable."
  },
  "enable_rate_limiting": {
    "type": "bool",
    "default": true,
    "description": "Enable rate limiting per IP to mitigate abuse."
  },
  "rate_limit_max_requests": {
    "type": "number",
    "default": 60,
    "description": "Maximum allowed requests per minute per IP before throttling."
  },
  "trusted_proxies": {
    "type": "list",
    "default": [],
    "description": "List of IP addresses or CIDR subnets of reverse proxies (e.g., load balancers) that set X-Forwarded-* headers.",
    "items": {
      "type": "string",
      "description": "Trusted proxy IP or subnet (e.g., '10.0.0.0/24' or '192.168.1.5')."
    }
  },
  "enable_2fa": {
    "type": "bool",
    "default": false,
    "description": "Enable two-factor authentication (2FA) for users (requires additional setup like libotp)."
  },
  "enable_password_policy": {
    "type": "bool",
    "default": true,
    "description": "Enforce password strength rules (length, complexity)."
  },
  "password_min_length": {
    "type": "number",
    "default": 12,
    "description": "Minimum required password length (increase for stricter security)."
  },
  "password_complexity": {
    "type": "number",
    "default": 3,
    "description": "Password complexity level: 0=none, 1=uppercase + lowercase, 2=+digit, 3=+special char."
  }
}
