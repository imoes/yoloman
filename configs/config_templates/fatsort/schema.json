{
  "device": {
    "type": "string",
    "default": "/dev/sda",
    "description": "Target device (e.g., /dev/sdb1) to sort"
  },
  "options": {
    "type": "list",
    "default": [],
    "description": "List of option arrays \u2014 each sub-array contains one fatsort option and its arguments (if any), as separate tokens (e.g., ['-n'], ['-d', 'Music'], ['-t', '-r'])"
  }
}
