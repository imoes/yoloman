{
  "system_user": {
    "type": "string",
    "default": "root",
    "description": "The system user running the octavia-agent daemon."
  },
  "system_group": {
    "type": "string",
    "default": "root",
    "description": "The system group running the octavia-agent daemon."
  },
  "config_file": {
    "type": "string",
    "default": "/etc/octavia/amphora-agent.conf",
    "description": "Path to the octavia agent configuration file."
  },
  "system_user_default": {
    "type": "string",
    "default": "octavia",
    "description": "Default system user if SYSTEM_USER is not set (falls back to project name)."
  },
  "system_group_default": {
    "type": "string",
    "default": "octavia",
    "description": "Default system group if SYSTEM_GROUP is not set (falls back to project name)."
  }
}
