{
  "installroot": {
    "type": "string",
    "default": "/",
    "description": "Root filesystem path for DNF operations."
  },
  "arch": {
    "type": "string",
    "default": "",
    "description": "Override architecture (e.g., x86_64, aarch64). Leave empty for auto-detection."
  },
  "basearch": {
    "type": "string",
    "default": "",
    "description": "Override base architecture (e.g., x86_64, i386). Leave empty for auto-detection."
  },
  "releasever": {
    "type": "string",
    "default": "",
    "description": "Override release version (e.g., 39, 8.10). Leave empty for auto-detection from RPMDB."
  },
  "debuglevel": {
    "type": "number",
    "default": 2,
    "description": "Debug message output level (0\u201310)."
  },
  "errorlevel": {
    "type": "number",
    "default": 3,
    "description": "Error message output level (0\u201310). Note: deprecated and overridden by --verbose CLI option."
  },
  "logfilelevel": {
    "type": "number",
    "default": 9,
    "description": "Log file message level (0\u201310)."
  },
  "keepcache": {
    "type": "bool",
    "default": false,
    "description": "Keep downloaded packages in cache after transaction."
  },
  "autocheck_running_kernel": {
    "type": "bool",
    "default": true,
    "description": "Automatically check for newer kernel modules with security updates."
  },
  "install_weak_deps": {
    "type": "bool",
    "default": true,
    "description": "Install packages linked via weak dependencies (recommends/supplements)."
  },
  "clean_requirements_on_remove": {
    "type": "bool",
    "default": true,
    "description": "Remove dependencies that become unused when a package is removed."
  },
  "protect_running_kernel": {
    "type": "bool",
    "default": true,
    "description": "Protect the running kernel from removal."
  },
  "group_package_types": {
    "type": "list",
    "default": [
      "default",
      "mandatory"
    ],
    "description": "Package types (optional/default/mandatory) included in groupinstall."
  },
  "installonlypkgs": {
    "type": "list",
    "default": [
      "kernel"
    ],
    "description": "Packages that should only be installed, never upgraded (e.g., kernels)."
  },
  "installonly_limit": {
    "type": "number",
    "default": 3,
    "description": "Maximum number of installonly packages installed concurrently (minimum 2). Value 0 means unlimited."
  },
  "excludepkgs": {
    "type": "list",
    "default": [],
    "description": "Packages to exclude globally (comma-separated names or globs)."
  },
  "exclude_from_weak": {
    "type": "list",
    "default": [],
    "description": "Package names or globs to exclude from weak dependency installation."
  },
  "exclude_from_weak_autodetect": {
    "type": "bool",
    "default": true,
    "description": "Autodetect and avoid installing providers of unmet weak dependencies."
  },
  "plugins": {
    "type": "bool",
    "default": true,
    "description": "Enable DNF plugins."
  },
  "assumeyes": {
    "type": "bool",
    "default": false,
    "description": "Assume 'yes' for all prompts (non-interactive mode)."
  },
  "assumeno": {
    "type": "bool",
    "default": false,
    "description": "Assume 'no' for all prompts."
  },
  "defaultyes": {
    "type": "bool",
    "default": false,
    "description": "Default to 'yes' when user is prompted for confirmation (not non-interactive)."
  },
  "exit_on_lock": {
    "type": "bool",
    "default": false,
    "description": "Exit immediately if another DNF process holds the lock."
  },
  "gpgkey_dns_verification": {
    "type": "bool",
    "default": false,
    "description": "Attempt to verify GPG keys via DNSSEC."
  },
  "color": {
    "type": "string",
    "default": "auto",
    "description": "Control colored output: auto, never, always.",
    "enum": [
      "auto",
      "never",
      "always"
    ]
  },
  "diskspacecheck": {
    "type": "bool",
    "default": true,
    "description": "Perform disk space checks before transactions."
  },
  "check_config_file_age": {
    "type": "bool",
    "default": true,
    "description": "Expire metadata when configuration files change."
  },
  "zchunk": {
    "type": "bool",
    "default": true,
    "description": "Use zchunk-compressed metadata (if available)."
  },
  "deltarpm": {
    "type": "bool",
    "default": true,
    "description": "Use delta RPMs to reduce bandwidth usage."
  },
  "max_parallel_downloads": {
    "type": "number",
    "default": 3,
    "description": "Maximum number of simultaneous downloads (max 20)."
  },
  "retries": {
    "type": "number",
    "default": 10,
    "description": "Total download retries (0 = unlimited)."
  },
  "timeout": {
    "type": "number",
    "default": 30,
    "description": "Connection timeout in seconds."
  },
  "minrate": {
    "type": "string",
    "default": "1000",
    "description": "Minimum download rate (bytes/sec) before abort; supports suffixes k/M/G."
  },
  "throttle": {
    "type": "string",
    "default": "",
    "description": "Download speed limit (bytes/sec or percentage of bandwidth); supports suffixes k/M/G."
  },
  "bandwidth": {
    "type": "string",
    "default": "",
    "description": "Total available bandwidth for throttling; supports suffixes k/M/G."
  },
  "countme": {
    "type": "bool",
    "default": false,
    "description": "Send countme flag to repositories for lifecycle statistics."
  },
  "proxy": {
    "type": "string",
    "default": "",
    "description": "Proxy URL (e.g., http://proxy.example.com:3128)."
  },
  "proxy_username": {
    "type": "string",
    "default": "",
    "description": "Username for proxy authentication."
  },
  "proxy_password": {
    "type": "string",
    "default": "",
    "description": "Password for proxy authentication."
  },
  "proxy_auth_method": {
    "type": "string",
    "default": "any",
    "description": "Proxy authentication method: basic/digest/negotiate/ntlm/digest_ie/ntlm_wb/none/any.",
    "enum": [
      "basic",
      "digest",
      "negotiate",
      "ntlm",
      "digest_ie",
      "ntlm_wb",
      "none",
      "any"
    ]
  },
  "ip_resolve": {
    "type": "string",
    "default": "",
    "description": "IP resolution preference: 4/IPv4 or 6/IPv6.",
    "enum": [
      "4",
      "IPv4",
      "6",
      "IPv6"
    ]
  },
  "module_obsoletes": {
    "type": "bool",
    "default": false,
    "description": "Apply modular obsoletes during transactions."
  },
  "module_stream_switch": {
    "type": "bool",
    "default": false,
    "description": "Allow stream switching for modules."
  },
  "module_platform_id": {
    "type": "string",
    "default": "",
    "description": "Override platform ID (e.g., 'fedora:39')."
  },
  "upgrade_group_objects_upgrade": {
    "type": "bool",
    "default": true,
    "description": "Run group upgrades during system upgrade."
  },
  "varsdir": {
    "type": "list",
    "default": [
      "/etc/dnf/vars"
    ],
    "description": "Directories searched for variable files."
  },
  "reposdir": {
    "type": "list",
    "default": [
      "/etc/yum.repos.d"
    ],
    "description": "Directories searched for repository (.repo) files."
  },
  "repositories": {
    "type": "list",
    "default": [],
    "description": "List of repositories to configure. Each item defines a repo section in the config file. Required fields: id, enabled. Optional: baseurl/metalink/mirrorlist, name, gpgkey, etc. See repo_* schema below for details per repo item."
  },
  "repo.id": {
    "type": "string",
    "default": "",
    "description": "Unique repository ID (used as section header: [id]). Allowed characters: a-zA-Z0-9-_.:"
  },
  "repo.enabled": {
    "type": "bool",
    "default": true,
    "description": "Include this repository as a package source."
  },
  "repo.name": {
    "type": "string",
    "default": "",
    "description": "Human-readable name for the repository. Defaults to repo.id if omitted."
  },
  "repo.baseurl": {
    "type": "list",
    "default": [],
    "description": "List of URLs (file://, http://, https://) to repository metadata."
  },
  "repo.metalink": {
    "type": "string",
    "default": "",
    "description": "Metalink URL for repository metadata."
  },
  "repo.mirrorlist": {
    "type": "string",
    "default": "",
    "description": "Mirrorlist URL to determine fastest mirrors."
  },
  "repo.gpgkey": {
    "type": "list",
    "default": [],
    "description": "URLs of GPG keys used to sign packages in this repository."
  },
  "repo.gpgcheck": {
    "type": "bool",
    "default": false,
    "description": "Verify GPG signatures on packages from this repository."
  },
  "repo.repo_gpgcheck": {
    "type": "bool",
    "default": false,
    "description": "Verify GPG signatures on metadata from this repository."
  },
  "repo.cost": {
    "type": "number",
    "default": 1000,
    "description": "Relative access cost; lower values are preferred when priorities are equal."
  },
  "repo.priority": {
    "type": "number",
    "default": 99,
    "description": "Repository priority (lower = higher precedence)."
  },
  "repo.module_hotfixes": {
    "type": "bool",
    "default": false,
    "description": "Disable module filtering to include all RPMs from this repo."
  },
  "repo.excludepkgs": {
    "type": "list",
    "default": [],
    "description": "Packages to exclude from this repository only."
  },
  "repo.includepkgs": {
    "type": "list",
    "default": [],
    "description": "Packages to include from this repository (inverse of excludepkgs)."
  },
  "repo.enablegroups": {
    "type": "bool",
    "default": true,
    "description": "Allow package group operations for this repository."
  },
  "repo.skip_if_unavailable": {
    "type": "bool",
    "default": false,
    "description": "Skip and disable this repo if synchronization fails."
  },
  "repo.sslverify": {
    "type": "bool",
    "default": true,
    "description": "Verify remote SSL certificates."
  },
  "repo.sslcacert": {
    "type": "string",
    "default": "",
    "description": "Path to CA certificate file for SSL verification."
  },
  "repo.sslclientcert": {
    "type": "string",
    "default": "",
    "description": "Path to SSL client certificate file."
  },
  "repo.sslclientkey": {
    "type": "string",
    "default": "",
    "description": "Path to SSL client key file."
  },
  "repo.proxy": {
    "type": "string",
    "default": "",
    "description": "Proxy URL for this repo; set to empty string ('') or '_none_' to inherit none."
  },
  "repo.proxy_sslverify": {
    "type": "bool",
    "default": true,
    "description": "Verify proxy SSL certificates."
  },
  "repo.proxy_sslcacert": {
    "type": "string",
    "default": "",
    "description": "Path to CA cert file for proxy SSL verification."
  },
  "repo.proxy_sslclientcert": {
    "type": "string",
    "default": "",
    "description": "Path to SSL client certificate for proxy."
  },
  "repo.proxy_sslclientkey": {
    "type": "string",
    "default": "",
    "description": "Path to SSL client key for proxy."
  },
  "repo.username": {
    "type": "string",
    "default": "",
    "description": "HTTP basic auth username for the repository."
  },
  "repo.password": {
    "type": "string",
    "default": "",
    "description": "HTTP basic auth password for the repository."
  },
  "repo.metadata_expire": {
    "type": "number",
    "default": 172800,
    "description": "Metadata expiration time in seconds. Use -1 or 'never' for never-expiring metadata."
  },
  "repo.type": {
    "type": "string",
    "default": "",
    "description": "Metadata format type (e.g., rpm-md); aliases: rpm, repomd, rpmmd, yum, YUM.",
    "enum": [
      "rpm-md",
      "rpm",
      "repomd",
      "rpmmd",
      "yum",
      "YUM"
    ]
  }
}
