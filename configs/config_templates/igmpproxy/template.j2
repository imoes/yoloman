# Defaults for igmpproxy initscript
# sourced by /etc/init.d/igmpproxy
# installed at /etc/default/igmpproxy by the maintainer scripts
#
# This is a POSIX shell fragment.
# Self-documented: each variable is described below.

# Additional options that are passed to the Daemon.
# Schema: daemon_opts (string, default: '')
DAEMON_OPTS="{{ (daemon_opts | default('')) | quote }}"

# Note: The following schema fields are not currently exposed in this default config file.
# If the service supports them via environment variables or a separate configuration file,
# they should be added there. Currently, igmpproxy configuration is typically handled
# via /etc/igmpproxy.conf, not the initscript defaults.

# upstream_interface: default='eth0'
# desc='Network interface connected to the upstream (source) network — e.g., the interface facing the internet or multicast source.'
# (Not defined in this specific default file, usually part of main config)

# downstream_networks: default=[]
# desc='List of downstream (local) networks that will receive multicast traffic. Each entry defines an interface and allowed subnet.'
# (Not defined in this specific default file, usually part of main config)