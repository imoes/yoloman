{
  "desc": {
    "type": "string",
    "default": "OpenStack Trove Conductor",
    "description": "Description string for the service"
  },
  "project_name": {
    "type": "string",
    "default": "trove",
    "description": "The project name used for directories and file paths"
  },
  "config_file": {
    "type": "string",
    "default": "/etc/trove/trove.conf",
    "description": "Path to the main configuration file"
  },
  "rabbit_process_name": {
    "type": "string",
    "default": "trove-conductor",
    "description": "Process name exported for RabbitMQ messaging"
  }
}
