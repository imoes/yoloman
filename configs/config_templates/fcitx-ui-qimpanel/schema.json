{
  "exec_path": {
    "type": "string",
    "default": "/usr/share/fcitx-qimpanel/fcitx-qimpanel-autostart",
    "description": "Path to the Fcitx Qt IMPanel autostart script"
  },
  "icon_name": {
    "type": "string",
    "default": "fcitx",
    "description": "Name of the icon (should be present in the current icon theme)"
  }
}
