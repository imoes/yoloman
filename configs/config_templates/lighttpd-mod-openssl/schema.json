{
  "ssl.pemfile": {
    "type": "string",
    "default": "/etc/lighttpd/server.pem",
    "description": "Path to the SSL PEM file containing the certificate and private key."
  },
  "ssl.host": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to bind the SSL listener to."
  },
  "ssl.port": {
    "type": "number",
    "default": 443,
    "description": "Port number for the SSL listener."
  }
}
