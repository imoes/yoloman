{
  "VIP_IP": {
    "type": "string",
    "default": "10.0.0.1",
    "description": "The virtual IP address that will be managed"
  },
  "VIP_MASK": {
    "type": "string",
    "default": "255.255.255.0",
    "description": "The netmask that is associated with the subnet"
  },
  "VIP_NETMASK": {
    "type": "string",
    "default": "255.255.255.0",
    "description": "The netmask alias"
  },
  "VIP_IFACE": {
    "type": "string",
    "default": "eth0",
    "description": "A local network interface on the machine"
  },
  "VIP_KEY": {
    "type": "string",
    "default": "/service/cluster/leader",
    "description": "The key in the DCS that will be monitored"
  },
  "VIP_TRIGGER_KEY": {
    "type": "string",
    "default": "/service/cluster/leader",
    "description": "The trigger key alias"
  },
  "VIP_HOST": {
    "type": "string",
    "default": "vip-manager-node1",
    "description": "The hostname of the patroni cluster member"
  },
  "VIP_TRIGGER_VALUE": {
    "type": "string",
    "default": "vip-manager-node1",
    "description": "The trigger value alias"
  },
  "VIP_TYPE": {
    "type": "string",
    "default": "etcd",
    "description": "The type of DCS that vip-manager will use",
    "enum": [
      "etcd",
      "consul"
    ]
  },
  "VIP_DCS_ENDPOINTS": {
    "type": "string",
    "default": "http://127.0.0.1:2379",
    "description": "A url that defines where to reach the DCS"
  },
  "VIP_ENDPOINT": {
    "type": "string",
    "default": "http://127.0.0.1:2379",
    "description": "The DCS endpoint alias"
  }
}
