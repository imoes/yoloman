{
  "sbin_binary_name": {
    "type": "string",
    "default": "globus-gram-audit",
    "description": "Name of the globus-gram-audit binary in sbin directory"
  },
  "sbin_binary_path_prefix": {
    "type": "string",
    "default": "/usr",
    "description": "The default prefix path if GLOBUS_LOCATION is not set, typically /usr or /opt/globus"
  }
}
