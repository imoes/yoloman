#! /bin/sh

prefix="${{ GLOBUS_LOCATION-{{ sbin_binary_path_prefix }}}}"
exec_prefix="${{ prefix }}"

if [ -x ${{ exec_prefix }}/sbin/{{ sbin_binary_name }} ] ; then
${{ exec_prefix }}/sbin/{{ sbin_binary_name }} --quiet
fi

exit 0