{
  "aat_compact": {
    "default": false,
    "description": "aat-frontend: Enable compact output.",
    "type": "bool"
  },
  "aat_coords": {
    "default": false,
    "description": "aat-frontend: Show geo coordinates.",
    "type": "bool"
  },
  "aat_monochrome": {
    "default": false,
    "description": "aat-frontend: Enable monochrome output.",
    "type": "bool"
  },
  "backend": {
    "default": "openweathermap",
    "description": "The weather data backend to use (e.g. openweathermap, openmeteo, caiyun, weatherapi, worldweatheronline, aat, md, json).",
    "enum": [
      "openweathermap",
      "openmeteo",
      "caiyun",
      "weatherapi",
      "worldweatheronline"
    ],
    "type": "string",
    "enum_open": true
  },
  "cache_ttl": {
    "default": "15m",
    "description": "Cache TTL duration. Set to '0' to disable caching.",
    "type": "string"
  },
  "caiyun_api_key": {
    "default": null,
    "description": "caiyun backend: API key.",
    "type": "string"
  },
  "caiyun_debug": {
    "default": false,
    "description": "caiyun backend: Print raw requests and responses.",
    "type": "bool"
  },
  "caiyun_lang": {
    "default": "en",
    "description": "caiyun backend: Language for API requests.",
    "type": "string"
  },
  "days": {
    "default": 3,
    "description": "Number of days of weather forecast to display.",
    "type": "number"
  },
  "frontend": {
    "default": "aat",
    "description": "Output frontend to use (e.g. aat, md, json).",
    "type": "string"
  },
  "json_no_indent": {
    "default": false,
    "description": "json-frontend: Do not indent the output.",
    "type": "bool"
  },
  "location": {
    "default": "New York, NY",
    "description": "The location to query for weather data.",
    "type": "string"
  },
  "md_coords": {
    "default": false,
    "description": "md-frontend: Show geo coordinates.",
    "type": "bool"
  },
  "openmeteo_api_key": {
    "default": null,
    "description": "openmeteo backend: API key for commercial usage. Leave empty if not using commercial plan.",
    "type": "string"
  },
  "openmeteo_debug": {
    "default": false,
    "description": "openmeteo backend: Print raw requests and responses.",
    "type": "bool"
  },
  "openweathermap_api_key": {
    "default": null,
    "description": "openweathermap backend: API key.",
    "type": "string"
  },
  "openweathermap_debug": {
    "default": false,
    "description": "openweathermap backend: Print raw requests and responses.",
    "type": "bool"
  },
  "openweathermap_lang": {
    "default": "en",
    "description": "openweathermap backend: Language for API responses.",
    "type": "string"
  },
  "units": {
    "default": "metric",
    "description": "Unitsystem for output. Choices: metric, imperial, si, metric-ms.",
    "enum": [
      "metric",
      "imperial",
      "si",
      "metric-ms"
    ],
    "type": "string"
  },
  "weatherapi_debug": {
    "default": false,
    "description": "weatherapi backend: Print raw requests and responses.",
    "type": "bool"
  },
  "weatherapi_key": {
    "default": null,
    "description": "weatherapi backend: API key.",
    "type": "string"
  },
  "weatherapi_lang": {
    "default": "en",
    "description": "weatherapi backend: Language for API requests.",
    "type": "string"
  },
  "worldweatheronline_debug": {
    "default": false,
    "description": "worldweatheronline backend: Print raw requests and responses.",
    "type": "bool"
  },
  "worldweatheronline_key": {
    "default": null,
    "description": "worldweatheronline backend: API key.",
    "type": "string"
  },
  "worldweatheronline_lang": {
    "default": "en",
    "description": "worldweatheronline backend: Language for API requests.",
    "type": "string"
  }
}
