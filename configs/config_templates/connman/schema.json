{
  "DAEMON": {
    "type": "string",
    "default": "/usr/sbin/connmand",
    "description": "Path to the connmand daemon executable."
  },
  "DESC": {
    "type": "string",
    "default": "Connection Manager",
    "description": "Description of the service."
  },
  "NAME": {
    "type": "string",
    "default": "connmand",
    "description": "Name of the service."
  },
  "CONNMAN_DEFAULT_CONFIG": {
    "type": "string",
    "default": "/etc/default/connman",
    "description": "Path to the default configuration file for connman."
  },
  "RUN_STATE_DIR": {
    "type": "string",
    "default": "/run/connman",
    "description": "Directory for runtime state files like resolv.conf symlink."
  },
  "DAEMON_OPTS": {
    "type": "string",
    "default": "",
    "description": "Command-line options passed to connmand at startup (e.g., '--debug --offline')"
  }
}
