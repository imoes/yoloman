{
  "provides": {
    "type": "string",
    "default": "gfmd",
    "description": "Name this init script provides (used by LSB)"
  },
  "short_description": {
    "type": "string",
    "default": "Gfarm filesystem metaserver",
    "description": "Short description of the service"
  },
  "description": {
    "type": "string",
    "default": "Gfarm file system metadata server",
    "description": "Full description of the service"
  },
  "desc": {
    "type": "string",
    "default": "Gfarm filesystem metaserver",
    "description": "Description variable used in script messages"
  },
  "name": {
    "type": "string",
    "default": "gfmd",
    "description": "Service binary name and identifier"
  },
  "pidfile": {
    "type": "string",
    "default": "gfmd",
    "description": "Base name for the PID file (without .pid extension)"
  }
}
