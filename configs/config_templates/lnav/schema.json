{
  "lnav_home": {
    "type": "string",
    "default": "/home/{{ user }}",
    "description": "The base directory for lnav user data, typically $HOME/.lnav"
  },
  "config_dir": {
    "type": "string",
    "default": "~/.lnav",
    "description": "The directory where lnav stores configuration and installed formats"
  },
  "user": {
    "type": "string",
    "default": "root",
    "description": "The system user account under which lnav is run"
  },
  "extra_format_dirs": {
    "type": "list",
    "default": [],
    "description": "List of additional directories to search for log format definition files"
  }
}
