{
  "ksshaskpass.ssh_sock_path": {
    "type": "string",
    "default": "/tmp/ssh-agent.sock",
    "description": "Path to the SSH agent socket file."
  },
  "ksshaskpass.kde_config_db": {
    "type": "string",
    "default": "/etc/ksshaskpass/kdefile.kcfg",
    "description": "Path to the KDE configuration database file."
  },
  "ksshaskpass.enable_dialog": {
    "type": "bool",
    "default": true,
    "description": "Whether to enable the passphrase dialog."
  },
  "ksshaskpass.timeout": {
    "type": "number",
    "default": 30,
    "description": "Timeout in seconds for the passphrase prompt."
  },
  "ksshaskpass.icon_path": {
    "type": "string",
    "default": "/usr/share/icons/hicolor/48x48/apps/ksshaskpass.png",
    "description": "Path to the icon for the dialog window."
  },
  "ksshaskpass.ssh_commands": {
    "type": "list",
    "default": [
      "ssh-add"
    ],
    "description": "List of SSH commands to handle."
  },
  "ksshaskpass.verbose": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level (0=quiet, 1=normal, 2=verbose)."
  },
  "ksshaskpass.lock_file": {
    "type": "string",
    "default": "/var/lock/ksshaskpass.lock",
    "description": "Path to the lock file to prevent multiple instances."
  }
}
