{
  "default_upscript": {
    "type": "string",
    "default": "/usr/share/ucarp/vip-up",
    "description": "Path to the script executed when the virtual IP comes up."
  },
  "default_downscript": {
    "type": "string",
    "default": "/usr/share/ucarp/vip-down",
    "description": "Path to the script executed when the virtual IP goes down."
  }
}
