{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options to pass to cpufetch; each item is a bare string representing one complete option flag and value (e.g., '--color intel', '--style retro') or bare flag (e.g., '--verbose'). No items are required, but at least one is recommended for a meaningful config. Each string must match documented cpufetch options exactly as per man page (section 5)."
  }
}
