{
  "data_file": {
    "type": "string",
    "default": "~/.tina",
    "description": "Path to the default personal information file."
  },
  "lock_file": {
    "type": "string",
    "default": "~/.tina.lock",
    "description": "Path to the default lock file."
  },
  "username": {
    "type": "string",
    "default": "User",
    "description": "The name of the user managing the information."
  },
  "entries": {
    "type": "list",
    "default": [],
    "description": "A list of personal information entries to initialize in the data file. Each entry is a map with type, title, and content fields."
  }
}
