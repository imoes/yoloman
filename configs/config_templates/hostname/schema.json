{
  "hostname": {
    "type": "string",
    "default": "localhost",
    "description": "The system's static hostname (short name, no domain part). Must contain only alphanumeric characters, hyphens, and dots, but typically only alphanumeric + hyphens. Must not start or end with a hyphen."
  }
}
