{
  "datadir": {
    "type": "string",
    "default": "/var/www/html",
    "description": "Document root directory for the web server"
  },
  "host": {
    "type": "string",
    "default": "localhost",
    "description": "Hostname or IP address to bind the HTTP server to"
  },
  "user": {
    "type": "string",
    "default": "nobody",
    "description": "User account to run the server process as"
  },
  "log_dest": {
    "type": "string",
    "default": "/dev/stdout",
    "description": "Log output destination"
  }
}
