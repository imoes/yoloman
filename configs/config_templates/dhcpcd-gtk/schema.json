{
  "exec": {
    "type": "string",
    "default": "dhcpcd-gtk",
    "description": "Command to execute when launching the application"
  },
  "icon": {
    "type": "string",
    "default": "dhcpcd",
    "description": "Icon name or path used for the application"
  },
  "only_show_in": {
    "type": "string",
    "default": "GNOME;LXDE;MATE;XFCE;ROX;Cinnamon",
    "description": "Semicolon-separated list of desktop environments in which this entry should be displayed"
  }
}
