# Lomiri Thumbnailer Service Configuration
# This package provides a thumbnail generation service for the Lomiri desktop environment.
# It is a self-contained utility service and does not expose network ports, databases, or external dependencies
# in this configuration context. It primarily processes local file resources.

# Path to the thumbnailer executable binary.
# Defined by the system packaging, typically not configurable at runtime.
/usr/libexec/lomiri-thumbnailer/vs-thumb