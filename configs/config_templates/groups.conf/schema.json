{
  "groups": {
    "type": "list",
    "default": [],
    "description": "List of group definitions. Each group maps a name to one or more members (hostnames, host patterns, or other group names).",
    "items": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the group (e.g., 'web', 'db', 'all')"
        },
        "members": {
          "type": "list",
          "description": "List of members (hostnames, patterns, or nested group names) for this group",
          "items": {
            "type": "string"
          },
          "default": []
        }
      },
      "required": [
        "name",
        "members"
      ],
      "additionalProperties": false
    }
  }
}
