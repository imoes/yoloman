{# Define group mappings: each key is a group name, value is a comma/space-separated list of hosts or other groups #}
{% for group_name, members in groups.items() %}{{ group_name }}: {{ members | join(', ') }}
{% endfor %}