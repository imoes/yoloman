{
  "listen_addr": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address on which to listen for incoming Nebula connections."
  },
  "port": {
    "type": "number",
    "default": 4242,
    "description": "UDP port to listen on for Nebula traffic."
  },
  "ip": {
    "type": "string",
    "default": "",
    "description": "Public IP address of this host, visible to peers (optional; used for NAT traversal and lighthouse registration)."
  },
  "subnet": {
    "type": "string",
    "default": "192.168.40.1/24",
    "description": "IPv4 CIDR subnet to assign to this node's Nebula interface (must match network-wide plan)."
  },
  "mtu": {
    "type": "number",
    "default": 1300,
    "description": "Maximum transmission unit (MTU) for the Nebula tunnel interface."
  },
  "cert_file": {
    "type": "string",
    "default": "/etc/nebula/nebula.crt",
    "description": "Path to the TLS certificate file used to authenticate this node."
  },
  "key_file": {
    "type": "string",
    "default": "/etc/nebula/nebula.key",
    "description": "Path to the private key file corresponding to the certificate."
  },
  "ca_file": {
    "type": "string",
    "default": "/etc/nebula/ca.crt",
    "description": "Path to the CA certificate file used to validate peer certificates."
  },
  "lighthouse": {
    "type": "bool",
    "default": false,
    "description": "Whether this node should act as a lighthouse (participant in peer discovery)."
  },
  "lighthouse_interval": {
    "type": "number",
    "default": 30,
    "description": "Interval (seconds) between lighthouse beacon announcements."
  },
  "lighthouse_remotes": {
    "type": "list",
    "default": [],
    "items": {
      "host": {
        "type": "string",
        "default": "",
        "description": "Public host:port of another lighthouse (e.g., 'lighthouse.example.com:4242')"
      },
      "port": {
        "type": "number",
        "default": 4242,
        "description": "Lighthouse port (optional if embedded in host string)."
      }
    },
    "description": "List of known lighthouse addresses to query during bootstrap."
  },
  "static_host_mapping": {
    "type": "object",
    "default": {},
    "items": {
      "key": {
        "type": "string",
        "description": "Nebula IP (e.g., '192.168.100.2')"
      },
      "value": {
        "type": "list",
        "items": {
          "type": "string",
          "description": "Host:port pair, e.g., '1.2.3.4:4242'"
        }
      }
    },
    "description": "Static mapping from Nebula IPs to public endpoints for peers without dynamic discovery."
  },
  "relays_enabled": {
    "type": "bool",
    "default": false,
    "description": "If true, this node may act as a relay to forward traffic between peers behind symmetric NATs."
  },
  "relay_hosts": {
    "type": "list",
    "default": [],
    "items": {
      "type": "string"
    },
    "description": "List of known relay host IPs or hostnames."
  },
  "firewall_outbound": {
    "type": "string",
    "default": "drop",
    "description": "Default outbound action ('drop' or 'allow')."
  },
  "firewall_inbound": {
    "type": "string",
    "default": "drop",
    "description": "Default inbound action ('drop' or 'allow')."
  },
  "firewall_default_outbound_action": {
    "type": "string",
    "default": "drop",
    "description": "Fallback action for unmatched outbound traffic."
  },
  "firewall_default_inbound_action": {
    "type": "string",
    "default": "drop",
    "description": "Fallback action for unmatched inbound traffic."
  },
  "firewall_rules": {
    "type": "list",
    "default": [],
    "items": {
      "proto": {
        "type": "string",
        "default": "",
        "description": "Protocol (tcp, udp, icmp, or '' to match all)"
      },
      "port": {
        "type": "string",
        "default": "",
        "description": "Port or range (e.g., '22', '80-90', or '' for all)"
      },
      "group": {
        "type": "string",
        "default": "",
        "description": "Destination group name (e.g., 'servers')"
      },
      "host": {
        "type": "string",
        "default": "",
        "description": "Destination Nebula IP (e.g., '192.168.100.2')"
      },
      "action": {
        "type": "string",
        "default": "drop",
        "description": "Action: 'allow' or 'drop'"
      }
    },
    "description": "List of firewall rules to control traffic routing within the overlay."
  },
  "tun.dev": {
    "type": "string",
    "default": "nebula0",
    "description": "Name of the tun interface to create (Linux only)."
  },
  "tun.disabled_offloads": {
    "type": "list",
    "default": [
      "gro",
      "gso",
      "tso",
      "checksum"
    ],
    "items": {
      "type": "string"
    },
    "description": "List of kernel offload features to disable on the tun device (prevents checksum errors)."
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Logging verbosity."
  },
  "log_format": {
    "type": "string",
    "default": "text",
    "description": "Log output format."
  },
  "log_file": {
    "type": "string",
    "default": "",
    "description": "Optional path to write logs to (stdout if empty)."
  },
  "psk_file": {
    "type": "string",
    "default": "",
    "description": "Path to a file containing a per-connection pre-shared key (optional, adds an extra layer of encryption)."
  },
  "control.listen": {
    "type": "string",
    "default": "127.0.0.1:4243",
    "description": "Address for local control/mgmt API (e.g., health, metrics, push)."
  },
  "control_cert.cert_file": {
    "type": "string",
    "default": "/etc/nebula/nebula-control.crt",
    "description": "TLS cert for control API (mTLS required)."
  },
  "control_cert.key_file": {
    "type": "string",
    "default": "/etc/nebula/nebula-control.key",
    "description": "TLS key for control API."
  },
  "control_cert.ca_file": {
    "type": "string",
    "default": "/etc/nebula/ca.crt",
    "description": "CA cert to validate control API clients."
  },
  "metrics.listen": {
    "type": "string",
    "default": "127.0.0.1:4244",
    "description": "Prometheus metrics endpoint (e.g., '127.0.0.1:4244')."
  },
  "pulse.enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable pulse probing to check health of connected peers."
  },
  "pulse.interval": {
    "type": "number",
    "default": 60,
    "description": "Interval (seconds) between peer health checks."
  },
  "cert_expiry.warn_days": {
    "type": "number",
    "default": 14,
    "description": "Alert operator when certificate expires in fewer than this many days."
  },
  "cert_expiry.renew_days": {
    "type": "number",
    "default": 7,
    "description": "Start automatic certificate renewal this many days before expiry."
  }
}
