<?php
// Database connection settings for Cacti
$db_type   = '{{ db.type }}';
$db_default = '{{ db.name }}';
$db_hostname = '{{ db.host }}';
$db_username = '{{ db.user }}';
$db_password = '{{ db.pass }}';
$db_port     = {{ db.port }};
$db_retries  = {{ db.retries }};
$db_charset  = '{{ db.charset | default('utf8mb4') }}';

// Apache authentication settings (if using mod_auth)
$auth_client = '{{ auth.client | default('') }}';

// System paths and utilities
$cacti_server_os = '{{ server.os | default('linux') }}';
$php_binary      = '{{ paths.php }}';
$rrdtool_binary  = '{{ paths.rrdtool }}';
$snmpwalk_binary = '{{ paths.snmpwalk }}';
$snmpget_binary  = '{{ paths.snmpget }}';
$mysqldump_binary= '{{ paths.mysqldump }}';
$mysql_binary    = '{{ paths.mysql }}';

// Poller configuration
$poller_interval = {{ poller.interval }};
$poller_type     = '{{ poller.type }}';
$max_threads     = {{ poller.threads | default(1) }};

// Log and session settings
$log_level       = {{ log.level | default(2) }};
$log_method      = '{{ log.method | default('database') }}';
$session_timeout = {{ session.timeout | default(600) }};

// Theme and UI options
$graph_settings['theme']      = '{{ ui.theme | default('modern') }}';
$graph_settings['timezone']   = '{{ ui.timezone | default('UTC') }}';
$graph_settings['locale']     = '{{ ui.locale | default('en_US.UTF-8') }}';
$graph_settings['language']   = '{{ ui.language | default('en_US') }}';
$graph_settings['date_format'] = '{{ ui.date_format | default('Y-m-d H:i:s') }}';
