{
  "mailto": {
    "type": "string",
    "default": "root",
    "description": "Email recipient for cron job output (e.g., 'root' or 'admin@example.com')"
  },
  "poller_log_file": {
    "type": "string",
    "default": "/var/log/cacti/poller-error.log",
    "description": "Absolute path to the poller error log file"
  }
}
