{
  "confroot": {
    "type": "string",
    "default": "/home/mikutter/.mikutter",
    "description": "The root directory for mikutter's configuration, plugins, and cache data."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode for development or troubleshooting."
  },
  "skip_version_check": {
    "type": "bool",
    "default": true,
    "description": "Skip library and environment version checks at startup to speed up launch."
  },
  "plugins": {
    "type": "list",
    "default": [
      "mastodon",
      "plurk"
    ],
    "description": "List of plugin slugs to load. See mikutter plugin repository for available slugs."
  },
  "daemon": {
    "type": "bool",
    "default": false,
    "description": "Run the application in daemon/background mode."
  },
  "color": {
    "type": "bool",
    "default": true,
    "description": "Colorize terminal output."
  },
  "clean": {
    "type": "bool",
    "default": false,
    "description": "Delete all caches and duplicated files on startup. Use with caution."
  }
}
