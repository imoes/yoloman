{
  "listen_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address for the web interface to listen on. Default is all interfaces."
  },
  "listen_port": {
    "type": "number",
    "default": 2638,
    "description": "IP network port for the web interface. Default is 2638."
  },
  "skin_path": {
    "type": "string",
    "default": "/usr/local/share/doc/sispmctl/skin",
    "description": "Directory path where web pages (skin) are located."
  },
  "password_file": {
    "type": "string",
    "default": "/etc/sispmctl/password",
    "description": "Path to the file containing the web-user and password."
  }
}
