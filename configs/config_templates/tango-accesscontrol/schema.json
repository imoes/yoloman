{
  "NAME": {
    "type": "string",
    "default": "tango-accesscontrol",
    "description": "The name of the service/daemon"
  },
  "NAME_REAL": {
    "type": "string",
    "default": "TangoAccessControl",
    "description": "The real name of the daemon binary"
  },
  "DAEMON_USER": {
    "type": "string",
    "default": "tango",
    "description": "The user account under which the daemon runs"
  },
  "DAEMON_GROUP": {
    "type": "string",
    "default": "tango",
    "description": "The group account under which the daemon runs"
  }
}
