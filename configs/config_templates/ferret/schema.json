{
  "model_path": {
    "type": "string",
    "default": "",
    "description": "Path to the input data model file (e.g., .er, .erx, or .xml format)"
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Logging verbosity: debug, info, warn, or error"
  },
  "targets": {
    "type": "list",
    "default": [],
    "description": "List of data model generation targets (e.g., SQL DDL generation specifications). Each entry defines a target database schema and output path."
  },
  "targets.items": {
    "type": "object",
    "description": "A single data model generation target configuration. All fields are optional and used only when present."
  },
  "targets.items.name": {
    "type": "string",
    "default": "",
    "description": "Human-readable name for the target configuration"
  },
  "targets.items.type": {
    "type": "string",
    "default": "unknown",
    "description": "Target type (e.g., 'sql', 'json', 'xml')"
  },
  "targets.items.database": {
    "type": "string",
    "default": "",
    "description": "Target database backend (e.g., 'postgresql', 'mysql')",
    "enum": [
      "postgresql",
      "mysql"
    ]
  },
  "targets.items.schema": {
    "type": "string",
    "default": "",
    "description": "Target database schema name"
  },
  "targets.items.output_dir": {
    "type": "string",
    "default": "",
    "description": "Directory path where generated output files will be written"
  }
}
