{
  "pidfile": {
    "type": "string",
    "default": "/var/run/vmtoolsd.pid",
    "description": "Path to the PID file for the vmtoolsd daemon."
  },
  "daemon_binary": {
    "type": "string",
    "default": "/usr/bin/vmtoolsd",
    "description": "Path to the vmtoolsd executable binary."
  }
}
