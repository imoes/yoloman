{
  "START_DAEMON": {
    "type": "number",
    "default": 0,
    "description": "Enable the daemon. Set to 1 to start. 0 prevents start."
  },
  "MiniUPnPd_OTHER_OPTIONS": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options passed to miniupnpd.",
    "enum": [
      "-d",
      "-U",
      "-S0",
      "-N",
      "-1"
    ]
  }
}
