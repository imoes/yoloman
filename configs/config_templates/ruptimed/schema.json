{
  "spool_dir": {
    "type": "string",
    "default": "/var/spool/ruptime",
    "description": "Directory where ruptimed stores its data files containing uptime information of queried hosts."
  },
  "port": {
    "type": "number",
    "default": 37,
    "description": "TCP/UDP port number for the ruptime service."
  },
  "bind_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to bind the server socket to."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, enables verbose logging output."
  },
  "allowed_hosts": {
    "type": "list",
    "default": [],
    "description": "List of hostnames or IP addresses allowed to connect to the service. Empty list means no restriction."
  }
}
