{
  "init_provides": {
    "type": "string",
    "default": "barbican-keystone-listener",
    "description": "The name of the service as provided by the init script, used by LSB."
  },
  "project_name": {
    "type": "string",
    "default": "barbican",
    "description": "The OpenStack project name (used for paths, users, config files)."
  }
}
