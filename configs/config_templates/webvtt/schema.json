{
  "source_path": {
    "type": "string",
    "default": "/var/www/subtitles/captions.vtt",
    "description": "Path to the input WebVTT file to be segmented."
  },
  "output_dir": {
    "type": "string",
    "default": "/var/www/subtitles/segments",
    "description": "Directory where the segmented WebVTT files will be written."
  },
  "target_duration": {
    "type": "number",
    "default": 10,
    "description": "Target duration in seconds for each generated segment."
  },
  "mpegts": {
    "type": "number",
    "default": 900000,
    "description": "Presentation timestamp value (timebase) used for segment timing."
  }
}
