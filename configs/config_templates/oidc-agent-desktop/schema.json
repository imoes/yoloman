{
  "OIDC_AGENT_SERVICE": {
    "type": "string",
    "default": "/usr/bin/oidc-agent-service",
    "description": "Path to the oidc-agent-service executable."
  },
  "OIDC_AGENT_OPTS": {
    "type": "string",
    "default": "",
    "description": "Arguments passed to the agent when started via Xsession."
  }
}
