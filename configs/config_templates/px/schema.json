{
  "debug_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode for verbose output"
  },
  "filter": {
    "type": "string",
    "default": "",
    "description": "Filter string to match against process names or user names"
  },
  "target_pid": {
    "type": "number",
    "default": 0,
    "description": "Specific Process ID to inspect"
  }
}
