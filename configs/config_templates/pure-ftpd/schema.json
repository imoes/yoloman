{
  "standalone_or_inetd": {
    "type": "string",
    "default": "inetd",
    "description": "Mode to run Pure-FTPd in: 'standalone' for dedicated servers, 'inetd' for managed services.",
    "enum": [
      "inetd",
      "standalone"
    ]
  }
}
