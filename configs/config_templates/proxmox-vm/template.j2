name: {{ name }}
cores: {{ cores }}
sockets: {{ sockets }}
memory: {{ memory }}
ostype: {{ ostype }}
scsihw: {{ scsihw }}
{% for d in disks %}{{ d.bus }}: {{ d.storage }}:{{ d.volume }},size={{ d.size }}
{% endfor %}{% for n in nets %}net{{ loop.index0 }}: {{ n.model }}={{ n.mac }},bridge={{ n.bridge }}{% if n.tag %},tag={{ n.tag }}{% endif %}
{% endfor %}boot: order={{ boot_order }}
onboot: {{ onboot }}
agent: {{ agent }}
