{
  "spip_alias_path": {
    "type": "string",
    "default": "/usr/share/spip",
    "description": "The URL path alias for the SPIP application."
  },
  "spip_document_root": {
    "type": "string",
    "default": "/usr/share/spip",
    "description": "The filesystem path to the SPIP web root directory."
  },
  "spip_sites_path": {
    "type": "string",
    "default": "/var/lib/spip/sites",
    "description": "The filesystem path to the SPIP sites data directory that requires protection."
  }
}
