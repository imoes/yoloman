{
  "name": {
    "type": "string",
    "default": "walldns",
    "description": "The name of the service, used for identification and path construction."
  },
  "desc": {
    "type": "string",
    "default": "reverse DNS wall",
    "description": "The human-readable description of the service."
  },
  "daemon": {
    "type": "string",
    "default": "/usr/sbin/walldns",
    "description": "The path to the walldns daemon binary."
  }
}
