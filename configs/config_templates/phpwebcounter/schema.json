{
  "webcounter_id": {
    "type": "string",
    "default": "0",
    "description": "Counter identifier, allows multiple counters to be used."
  },
  "path_hits_file": {
    "type": "string",
    "default": "/var/lib/phpwebcounter",
    "description": "Path to the hits control file. The directory must have R/W permission for the web server owner."
  },
  "visible": {
    "type": "bool",
    "default": true,
    "description": "Whether the counter display is visible or invisible (counting only)."
  },
  "path_images": {
    "type": "string",
    "default": "/phpwebcounter/images/basic1",
    "description": "URL path to the digit images used for the counter display. Leave blank for text style."
  },
  "counter_pad": {
    "type": "number",
    "default": 6,
    "description": "Minimum number of digits to be displayed for the hit count."
  },
  "step": {
    "type": "number",
    "default": 1,
    "description": "Counter step increment value."
  },
  "banner": {
    "type": "number",
    "default": 1,
    "description": "Whether to show the project banner (1) or not (0)."
  }
}
