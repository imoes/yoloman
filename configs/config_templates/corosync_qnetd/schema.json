{
  "host": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address the qnetd daemon binds to. Use '0.0.0.0' for all interfaces or a specific IP."
  },
  "port": {
    "type": "number",
    "default": 5403,
    "description": "TCP port used for incoming qnetd connections. Must match the client\u2019s expected port."
  },
  "certdir": {
    "type": "string",
    "default": "/etc/corosync/qnetd",
    "description": "Directory containing TLS certificates (CA, server cert, server key). Must be readable by the qnetd user."
  },
  "crl_file": {
    "type": "string",
    "default": "",
    "description": "Path to Certificate Revocation List (CRL) file. Leave empty if CRL is not used."
  },
  "tls_algorithm": {
    "type": "string",
    "default": "high",
    "description": "TLS cipher strength preference. Use 'high', 'medium', 'low', or a specific OpenSSL cipher string."
  },
  "work_threads": {
    "type": "number",
    "default": 0,
    "description": "Number of worker threads. Set to 0 to auto-detect (recommended)."
  },
  "max_connections": {
    "type": "number",
    "default": 50,
    "description": "Maximum number of simultaneous client connections allowed."
  },
  "log_level": {
    "type": "string",
    "default": "notice",
    "description": "Minimum logging level. Messages at or above this level are logged."
  },
  "log_file": {
    "type": "string",
    "default": "",
    "description": "Path to qnetd log file. Leave empty to log to stdout/syslog instead."
  },
  "algorithm": {
    "type": "string",
    "default": "ffsplit",
    "description": "Quorum algorithm used: 'ffsplit' (fixed failover) or 'lms' (linear model scoring)."
  },
  "clients": {
    "type": "list",
    "default": [],
    "description": "List of trusted client configurations (by certificate CN or IP). If empty, all verified clients are allowed.",
    "items": {
      "cn": {
        "type": "string",
        "default": "",
        "description": "Client certificate Common Name (CN) or IP address."
      },
      "algorithm": {
        "type": "string",
        "default": "",
        "description": "Optional algorithm override for this client. Use empty to inherit global setting."
      },
      "node_id": {
        "type": "string",
        "default": "",
        "description": "Optional custom node ID (1\u201315 alphanumeric characters). Must be unique across cluster nodes."
      }
    }
  },
  "timeout": {
    "type": "number",
    "default": 5000,
    "description": "Time in milliseconds before qnetd considers a client request timed out."
  },
  "max_missed": {
    "type": "number",
    "default": 2,
    "description": "Number of consecutive heartbeat misses before a client is marked as failed."
  },
  "interval": {
    "type": "number",
    "default": 1000,
    "description": "Heartbeat interval in milliseconds between qnetd and its clients."
  },
  "minimum_nodes": {
    "type": "number",
    "default": 2,
    "description": "Minimum number of nodes required in the cluster to maintain quorum (including qdevice)."
  }
}
