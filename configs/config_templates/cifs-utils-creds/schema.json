{
  "credentials": {
    "type": "list",
    "default": [],
    "description": "List of credential entries for remote CIFS/SMB servers",
    "items": {
      "host": {
        "type": "string",
        "description": "Hostname or IP address of the CIFS/SMB server",
        "pattern": "^[a-zA-Z0-9.-]+$|^\\d{1,3}(\\.\\d{1,3}){3}$"
      },
      "username": {
        "type": "string",
        "description": "Username to use when authenticating to this server"
      },
      "password": {
        "type": "string",
        "description": "Password (plaintext) for the user. WARNING: Stored in plaintext on disk \u2014 restrict file permissions!",
        "sensitive": true
      },
      "domain": {
        "type": "string",
        "default": "",
        "description": "Optional Windows domain or workgroup name for authentication"
      }
    }
  }
}
