{
  "mode": {
    "type": "string",
    "default": "extract",
    "description": "Operation mode: 'list' (-l), 'extract' (-x), or 'both' (-l -x)",
    "enum": [
      "list",
      "extract",
      "both"
    ]
  },
  "create_dir": {
    "type": "bool",
    "default": false,
    "description": "If true, adds -d flag to create output directory"
  },
  "input_file": {
    "type": "string",
    "default": "bios.bin",
    "description": "Path to the AMI BIOS image file to decompress"
  }
}
