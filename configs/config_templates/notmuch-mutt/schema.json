{
  "notmuch_mutt_command": {
    "type": "string",
    "default": "notmuch-mutt",
    "description": "The command to invoke for notmuch-mutt operations."
  },
  "notmuch_mutt_results_dir": {
    "type": "string",
    "default": "notmuch/mutt/results",
    "description": "The relative path to the directory where notmuch-mutt stores its results."
  }
}
