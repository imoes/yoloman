{
  "icecast_config_file": {
    "type": "string",
    "default": "/etc/icecast2/icecast.xml",
    "description": "Full path to the Icecast server configuration file."
  },
  "icecast_user": {
    "type": "string",
    "default": "icecast2",
    "description": "User account under which the Icecast daemon runs."
  },
  "icecast_group": {
    "type": "string",
    "default": "icecast",
    "description": "Group under which the Icecast daemon runs."
  }
}
