{
  "tun_name": {
    "type": "string",
    "default": "tun0",
    "description": "Custom name for the TUN interface (e.g., 'clat0'); if empty, defaults to 'tun0'."
  },
  "plat_dev": {
    "type": "string",
    "default": "",
    "description": "PLAT-facing network device (e.g., 'eth0'); auto-detected if unset."
  },
  "v4_conncheck_enable": {
    "type": "number",
    "default": 1,
    "description": "Exit if IPv4 default route already exists (0=disabled, 1=enabled)."
  },
  "v4_conncheck_delay": {
    "type": "number",
    "default": 10,
    "description": "Seconds before checking for v4 conn."
  },
  "forwarding_enable": {
    "type": "number",
    "default": 1,
    "description": "Enable kernel IPv6 forwarding."
  },
  "clat_ipv4": {
    "type": "string",
    "default": "192.0.0.1",
    "description": "IPv4 address to assign to the internal CLAT interface (must be in 192.0.0.0/24 per RFC 6877)."
  },
  "er_ipv4_int": {
    "type": "string",
    "default": "192.0.0.2",
    "description": "IPv4 address on the ER\u2019s internal-facing interface (for downstream IPv4 clients)."
  },
  "dns_servers": {
    "type": "list",
    "default": [],
    "description": "List of custom DNS server addresses (IPv4 or IPv6) to override system defaults."
  },
  "mtu": {
    "type": "number",
    "default": 0,
    "description": "MTU for the CLAT/Tun interface; 0 means auto-detect (recommended)."
  },
  "loglevel": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level: 0=error, 1=warn, 2=info (default), 3=debug, 4=trace. Not directly configurable in this config file \u2014 passed via CLI or systemd override."
  }
}
