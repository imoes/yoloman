{
  "ipmiutil_bin_dir": {
    "type": "string",
    "default": "/usr/bin",
    "description": "The directory containing the ipmiutil binaries."
  },
  "ipmiutil_sel_dir": {
    "type": "string",
    "default": "/var/lib/ipmiutil",
    "description": "The directory used to store SEL history files and indices."
  }
}
