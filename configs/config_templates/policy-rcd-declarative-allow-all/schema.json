{
  "regex": {
    "type": "string",
    "default": ".*",
    "description": "Regular expression matching package names."
  },
  "action_service": {
    "type": "string",
    "default": ".*",
    "description": "Regular expression matching service names."
  },
  "action": {
    "type": "string",
    "default": "allow",
    "description": "Action to take: allow, deny, or ignore."
  }
}
