{
  "yp_servers": {
    "type": "list",
    "default": [],
    "description": "List of NIS server hostnames or IP addresses to bind to. If empty, no manual servers are configured and broadcasting is used."
  }
}
