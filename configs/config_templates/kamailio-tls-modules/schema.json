{
  "verify_certificate_server_default": {
    "type": "string",
    "default": "no",
    "description": "Whether to verify the certificate of the server in the default server profile."
  },
  "require_certificate_server_default": {
    "type": "string",
    "default": "no",
    "description": "Whether to require a client certificate in the default server profile."
  },
  "tls_server_default_private_key": {
    "type": "string",
    "default": "/etc/kamailio/kamailio-selfsigned.key",
    "description": "Path to the private key file for the default server profile."
  },
  "tls_server_default_certificate": {
    "type": "string",
    "default": "/etc/kamailio/kamailio-selfsigned.pem",
    "description": "Path to the certificate file for the default server profile."
  },
  "tls_server_default_ca_list": {
    "type": "string",
    "default": "/etc/kamailio/tls/cacert.pem",
    "description": "Path to the CA list file for the default server profile."
  },
  "tls_server_default_crl": {
    "type": "string",
    "default": "/etc/kamailio/tls/crl.pem",
    "description": "Path to the CRL file for the default server profile."
  },
  "verify_certificate_client_default": {
    "type": "string",
    "default": "yes",
    "description": "Whether to verify the certificate of the server in the default client profile."
  },
  "require_certificate_client_default": {
    "type": "string",
    "default": "yes",
    "description": "Whether to require a client certificate in the default client profile."
  },
  "tls_server_loopback_private_key": {
    "type": "string",
    "default": "/etc/kamailio/tls/local_key.pem",
    "description": "Path to the private key file for the loopback server profile."
  },
  "tls_server_loopback_certificate": {
    "type": "string",
    "default": "/etc/kamailio/tls/local_cert.pem",
    "description": "Path to the certificate file for the loopback server profile."
  },
  "tls_server_loopback_ca_list": {
    "type": "string",
    "default": "/etc/kamailio/tls/local_ca.pem",
    "description": "Path to the CA list file for the loopback server profile."
  },
  "tls_server_loopback_crl": {
    "type": "string",
    "default": "/etc/kamailio/tls/local_crl.pem",
    "description": "Path to the CRL file for the loopback server profile."
  },
  "tls_client_example_private_key": {
    "type": "string",
    "default": "/etc/kamailio/tls/example_key.pem",
    "description": "Path to the private key file for the example client profile."
  },
  "tls_client_example_certificate": {
    "type": "string",
    "default": "/etc/kamailio/tls/example_cert.pem",
    "description": "Path to the certificate file for the example client profile."
  },
  "tls_client_example_ca_list": {
    "type": "string",
    "default": "/etc/kamailio/tls/example_ca.pem",
    "description": "Path to the CA list file for the example client profile."
  },
  "tls_client_example_crl": {
    "type": "string",
    "default": "/etc/kamailio/tls/example_crl.pem",
    "description": "Path to the CRL file for the example client profile."
  },
  "tls_server_any_domain_private_key": {
    "type": "string",
    "default": "/etc/kamailio/tls/mysipserver_org_key.pem",
    "description": "Path to the private key file for the any-domain server profile."
  },
  "tls_server_any_domain_certificate": {
    "type": "string",
    "default": "/etc/kamailio/tls/mysipserver_org_cert.pem",
    "description": "Path to the certificate file for the any-domain server profile."
  },
  "tls_server_any_domain_ca_list": {
    "type": "string",
    "default": "/etc/kamailio/tls/mysipserver_org_ca.pem",
    "description": "Path to the CA list file for the any-domain server profile."
  },
  "tls_server_any_domain_crl": {
    "type": "string",
    "default": "/etc/kamailio/tls/mysipserver_org_crl.pem",
    "description": "Path to the CRL file for the any-domain server profile."
  },
  "tls_server_any_subdomain_private_key": {
    "type": "string",
    "default": "/etc/kamailio/tls/mysipserver_net_key.pem",
    "description": "Path to the private key file for the any-subdomain server profile."
  },
  "tls_server_any_subdomain_certificate": {
    "type": "string",
    "default": "/etc/kamailio/tls/mysipserver_net_cert.pem",
    "description": "Path to the certificate file for the any-subdomain server profile."
  },
  "tls_server_any_subdomain_ca_list": {
    "type": "string",
    "default": "/etc/kamailio/tls/mysipserver_net_ca.pem",
    "description": "Path to the CA list file for the any-subdomain server profile."
  },
  "tls_server_any_subdomain_crl": {
    "type": "string",
    "default": "/etc/kamailio/tls/mysipserver_net_crl.pem",
    "description": "Path to the CRL file for the any-subdomain server profile."
  }
}
