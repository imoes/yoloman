# ClusterSSH cluster definition file
# This file groups hosts into named clusters for simultaneous administration via `css`.
# Format: cluster_name = host1 host2 ...
# Generated by Ansible; do not edit manually.

# List of clusters to define for ClusterSSH. Each cluster groups hosts for convenient simultaneous access.
# Example cluster item structure:
#   - name: web
#     description: Web servers
#     hosts:
#       - web1.example.com
#       - web2.example.com
{% for cluster in clusters | default([], boolean=false) %}
# {{ cluster.description | default('No description provided') }}
{{ cluster.name | default('unnamed_cluster') }} = {% for host in cluster.hosts | default([], boolean=false) %}{{ host }}{% if not loop.last %} {% endif %}{% endfor %}
{% endfor %}