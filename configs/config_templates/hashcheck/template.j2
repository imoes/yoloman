# Hashcheck Configuration
# Manages ISO verification integrity checks at boot time.

[Hashcheck]
# Enable hashcheck verification at boot; verifies the live ISO against md5sum.txt
Enabled={{ (enabled | default(true)) | lower }}