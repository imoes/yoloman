{
  "start_ncpd": {
    "type": "string",
    "default": "yes",
    "description": "Set to 'yes' to start ncpd during boot.",
    "enum": [
      "yes",
      "no"
    ]
  },
  "ncpd_args": {
    "type": "string",
    "default": "",
    "description": "Arguments passed to ncpd."
  },
  "start_plpfuse": {
    "type": "string",
    "default": "no",
    "description": "Set to 'yes' to start plpfuse during boot.",
    "enum": [
      "yes",
      "no"
    ]
  },
  "plpfuse_args": {
    "type": "string",
    "default": "",
    "description": "Arguments passed to plpfuse."
  },
  "start_plpprintd": {
    "type": "string",
    "default": "no",
    "description": "Set to 'yes' to start plpprintd during boot.",
    "enum": [
      "yes",
      "no"
    ]
  },
  "plpprintd_args": {
    "type": "string",
    "default": "",
    "description": "Arguments passed to plpprintd."
  }
}
