{
  "NSNCD_WORKER_COUNT": {
    "type": "number",
    "default": 8,
    "description": "Number of worker threads nsncd will spawn to process requests."
  },
  "NSNCD_HANDOFF_TIMEOUT": {
    "type": "number",
    "default": 3,
    "description": "Number of seconds nsncd will wait for a worker thread to become available."
  },
  "NSNCD_IGNORE_GROUP": {
    "type": "string",
    "default": "false",
    "description": "If set to \"true\", nsncd will not respond to requests for the group database.",
    "enum": [
      "false",
      "true"
    ]
  },
  "NSNCD_IGNORE_HOSTS": {
    "type": "string",
    "default": "false",
    "description": "If set to \"true\", nsncd will not respond to requests for the hosts database.",
    "enum": [
      "false",
      "true"
    ]
  },
  "NSNCD_IGNORE_INITGROUPS": {
    "type": "string",
    "default": "false",
    "description": "If set to \"true\", nsncd will not respond to requests for the initgroups database.",
    "enum": [
      "false",
      "true"
    ]
  },
  "NSNCD_IGNORE_NETGROUP": {
    "type": "string",
    "default": "false",
    "description": "If set to \"true\", nsncd will not respond to requests for the netgroup database.",
    "enum": [
      "false",
      "true"
    ]
  },
  "NSNCD_IGNORE_PASSWD": {
    "type": "string",
    "default": "false",
    "description": "If set to \"true\", nsncd will not respond to requests for the passwd database.",
    "enum": [
      "false",
      "true"
    ]
  },
  "NSNCD_IGNORE_SERVICES": {
    "type": "string",
    "default": "false",
    "description": "If set to \"true\", nsncd will not respond to requests for the services database.",
    "enum": [
      "false",
      "true"
    ]
  }
}
