{
  "verbose": {
    "type": "string",
    "default": "no",
    "description": "Set to 'yes' to enable verbose logging for ifup/ifdown commands.",
    "enum": [
      "yes",
      "no"
    ]
  },
  "skip_down_at_sysreset": {
    "type": "string",
    "default": "yes",
    "description": "Set to 'yes' to skip deconfiguring interfaces during reboot/shutdown to speed up the process.",
    "enum": [
      "yes",
      "no"
    ]
  }
}
