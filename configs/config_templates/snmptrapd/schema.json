{
  "snmptrapd": {
    "type": "object",
    "description": "Top-level configuration block for snmptrapd"
  },
  "trapd_opts": {
    "type": "string",
    "default": "-LSwd -p /run/snmptrapd.pid",
    "description": "Command-line options passed to snmptrapd. The default enables logging to syslog, runs as daemon, and writes PID file."
  }
}
