{
  "flags": {
    "type": "string",
    "default": "-A",
    "description": "Flags to be passed to ipband(8) during startup via /etc/init.d/ipband."
  },
  "config_files": {
    "type": "string",
    "default": "/etc/ipband.conf",
    "description": "Space-separated list of configuration files. One instance of ipband will be launched for each configuration file listed here."
  }
}
