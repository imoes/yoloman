{
  "myproxy_user": {
    "type": "string",
    "default": "myproxy",
    "description": "The system user account under which MyProxy runs."
  },
  "myproxy_data_dir": {
    "type": "string",
    "default": "/var/lib/myproxy",
    "description": "The directory where MyProxy stores its database and credentials."
  },
  "x509_host_cert": {
    "type": "string",
    "default": "/etc/grid-security/myproxy/hostcert.pem",
    "description": "Path to the host X.509 certificate for the MyProxy server."
  },
  "x509_host_key": {
    "type": "string",
    "default": "/etc/grid-security/myproxy/hostkey.pem",
    "description": "Path to the host X.509 private key for the MyProxy server."
  }
}
