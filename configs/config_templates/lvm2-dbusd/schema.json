{
  "root_user": {
    "type": "string",
    "default": "root",
    "description": "The Unix user allowed to own and send messages to the LVM D-Bus service."
  },
  "dbus_service_name": {
    "type": "string",
    "default": "com.redhat.lvmdbus1",
    "description": "The D-Bus service name for the LVM daemon."
  }
}
