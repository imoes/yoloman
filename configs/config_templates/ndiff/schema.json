{
  "targets": {
    "type": "string",
    "default": "targets",
    "description": "Space-separated list of targets or file containing targets for Nmap"
  },
  "nmap_options": {
    "type": "string",
    "default": "-v -T4 -F -sV",
    "description": "Command-line options for Nmap scan"
  },
  "date_format": {
    "type": "string",
    "default": "F",
    "description": "Date format string for date command to use in filenames"
  },
  "scan_base_dir": {
    "type": "string",
    "default": "/root/scans",
    "description": "Directory where scan results and diffs are stored"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Whether to include all hosts and ports in ndiff output, not only those that have changed"
  }
}
