{
  "interval": {
    "type": "number",
    "default": 60,
    "description": "Interval in seconds between battery data collection samples"
  },
  "flush": {
    "type": "number",
    "default": 10,
    "description": "Number of samples to accumulate before flushing to storage"
  }
}
