{# glmark2-es2-drm configuration - one benchmark per line #}
{%- for benchmark in benchmarks %}
{% if loop.first and default_options %}:{{ default_options|join(':') }}{% endif %}
{% if default_options and not loop.first %}:{{ default_options|join(':') }}{% endif %}{{ benchmark.scene }}{% if benchmark.scene_options %}:{{ benchmark.scene_options|join(':') }}{% endif %}
{%- endfor %}