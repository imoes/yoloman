{
  "args": {
    "type": "string",
    "default": "",
    "description": "Command-line arguments to pass to the prometheus-mongodb-exporter server."
  },
  "mongodb_uri": {
    "type": "string",
    "default": "mongodb://localhost:27017",
    "description": "MongoDB connection URI. Can also be set via MONGODB_URL environment variable."
  }
}
