{
  "cron_user": {
    "type": "string",
    "default": "bind",
    "description": "The system user under whom the cron jobs should execute."
  },
  "ldap2bind_path": {
    "type": "string",
    "default": "/usr/sbin/ldap2bind",
    "description": "The absolute path to the ldap2bind executable."
  }
}
