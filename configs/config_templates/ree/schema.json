{
  "input_file": {
    "type": "string",
    "default": "/usr/share/ree/roms/default.rom",
    "description": "Path to the input ROM file containing extensions to be extracted."
  },
  "output_file": {
    "type": "string",
    "default": "/tmp/extracted_rom.bin",
    "description": "Path where the extracted ROM extension data will be written."
  },
  "verbosity": {
    "type": "number",
    "default": 1,
    "description": "Control the amount of output printed to stdout during extraction."
  }
}
