{
  "project_name": {
    "type": "string",
    "default": "swift",
    "description": "The project name used for directory paths and process names."
  },
  "system_user": {
    "type": "string",
    "default": "swift",
    "description": "The system user account running the Swift container server."
  },
  "system_group": {
    "type": "string",
    "default": "swift",
    "description": "The system group account running the Swift container server."
  },
  "uwsgi_port": {
    "type": "number",
    "default": 6001,
    "description": "The port number for the uWSGI interface."
  }
}
