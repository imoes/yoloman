{
  "bind_port": {
    "type": "number",
    "default": 6201,
    "description": "The port number for the container server to bind to."
  },
  "system_user": {
    "type": "string",
    "default": "swift",
    "description": "The system user account running the Swift container server."
  }
}
