{
  "var_run_path": {
    "type": "string",
    "default": "/var/run/linuxlogo",
    "description": "Path to the runtime directory for linuxlogo."
  },
  "debian_version_label": {
    "type": "string",
    "default": "Debian Version $(cat /etc/debian_version)",
    "description": "The text string passed as the title to the linux_logo daemon. Usually contains the debian version."
  },
  "issue_file_ansi": {
    "type": "string",
    "default": "/etc/issue.linuxlogo",
    "description": "The output file path for the ANSI colored logo."
  },
  "issue_file_ascii": {
    "type": "string",
    "default": "/etc/issue.linuxlogo.ascii",
    "description": "The output file path for the ASCII-only logo."
  }
}
