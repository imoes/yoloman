{
  "ircd_binary": {
    "type": "string",
    "default": "/usr/sbin/ircd",
    "description": "Path to the ircd binary executable"
  },
  "ircd_service_name": {
    "type": "string",
    "default": "ircd",
    "description": "Name of the ircd service process"
  },
  "pid_file_path": {
    "type": "string",
    "default": "/var/run/ircd/ircd.pid",
    "description": "Full path to the PID file for the ircd daemon"
  },
  "pid_dir_path": {
    "type": "string",
    "default": "/var/run/ircd",
    "description": "Directory where the PID file is stored"
  },
  "pid_dir_user": {
    "type": "string",
    "default": "irc",
    "description": "User owner of the PID directory"
  },
  "pid_dir_group": {
    "type": "string",
    "default": "irc",
    "description": "Group owner of the PID directory"
  },
  "daemon_user": {
    "type": "string",
    "default": "irc",
    "description": "User under which the ircd daemon runs"
  }
}
