{
  "default_backend": {
    "type": "string",
    "default": "qscan",
    "description": "The command-line interface tool used by qpxtool (e.g., qscan or cdvdcontrol).",
    "enum": [
      "qscan",
      "cdvdcontrol"
    ]
  },
  "window_width": {
    "type": "number",
    "default": 800,
    "description": "Default width of the application window in pixels."
  },
  "window_height": {
    "type": "number",
    "default": 600,
    "description": "Default height of the application window in pixels."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, enables verbose output for debugging."
  },
  "device_path": {
    "type": "string",
    "default": "/dev/cdrom",
    "description": "Path to the CD/DVD drive device file."
  }
}
