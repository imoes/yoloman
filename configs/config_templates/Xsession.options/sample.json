{
  "options": [
    {
      "key": "allow_terminal",
      "value": "true",
      "comment": "Permit launching a terminal emulator from the session menu."
    },
    {
      "key": "no_start_agent",
      "comment": "Do not start the SSH/PGP agent automatically (default: agents are started)."
    },
    {
      "key": "use_etc_xsessionrc",
      "value": "true",
      "comment": "Execute /etc/X11/Xsession.rc after the session starts."
    },
    {
      "key": "fallback_xsession",
      "value": "xfce4-session",
      "comment": "Fallback session to launch if no user preference exists."
    },
    {
      "key": "debug",
      "comment": "Enable verbose logging of session startup (for troubleshooting)."
    }
  ]
}
