{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of configuration options, each with a key, optional value, and optional comment for documentation.",
    "items": {
      "type": "object",
      "fields": {
        "key": {
          "type": "string",
          "default": "",
          "description": "The option name (e.g., 'allow_terminal', 'debug'). Required."
        },
        "value": {
          "type": "string",
          "default": null,
          "description": "The option value (e.g., 'true', 'false'). Omit for boolean flags."
        },
        "comment": {
          "type": "string",
          "default": "",
          "description": "Optional human-readable comment explaining the option (will be rendered as a preceding comment line)."
        }
      }
    }
  }
}
