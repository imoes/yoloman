{
  "ExecStart": {
    "type": "string",
    "default": "/usr/bin/granite-7-demo",
    "description": "Absolute path to the granite-7-demo binary (as per installed binary location)"
  },
  "Environment": {
    "type": "list",
    "default": [],
    "description": "Environment variables to pass to the service (e.g., ['G_MESSAGES_DEBUG=all', 'GTK_DEBUG=interactive'])"
  },
  "Restart": {
    "type": "string",
    "default": "on-failure",
    "description": "Restart policy: one of no, on-success, on-failure, always, on-abnormal, on-abort, or on-watchdog"
  }
}
