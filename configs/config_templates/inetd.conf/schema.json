{
 "host_prefix": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "services": {
  "default": [],
  "description": "List of services managed by inetd/xinetd. Each item defines a service to expose via the Internet super-server. Supports optional host address specifier and socket buffer tuning as per inetd.conf(5).",
  "type": "list"
 }
}
