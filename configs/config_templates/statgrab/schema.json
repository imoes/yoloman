{
  "format": {
    "type": "string",
    "default": "linux",
    "description": "Output format: linux, bsd, mrtg, or plain.",
    "enum": [
      "linux",
      "bsd",
      "mrtg",
      "plain"
    ]
  },
  "update_mode": {
    "type": "string",
    "default": "once",
    "description": "Update mode: once, repeated, or diff_once."
  },
  "tdelay": {
    "type": "number",
    "default": 1,
    "description": "Delay in seconds between updates for repeating modes."
  },
  "cpu_percentage": {
    "type": "bool",
    "default": false,
    "description": "Display CPU usage as percentages."
  },
  "factor": {
    "type": "number",
    "default": 1,
    "description": "Scaling factor for floating-point values."
  },
  "byte_unit": {
    "type": "string",
    "default": "none",
    "description": "Byte unit: none, K, M, or G."
  },
  "stats": {
    "type": "list",
    "default": [],
    "description": "List of specific stats to display. Empty list shows all."
  }
}
