{
  "default_account": {
    "type": "string",
    "default": "00",
    "description": "Default account ID used for new operations."
  },
  "max_lines": {
    "type": "number",
    "default": 40,
    "description": "Maximum number of output lines before piping to 'less'."
  },
  "terminal_encoding": {
    "type": "string",
    "default": "UTF-8",
    "description": "Encoding expected for terminal input (fallback if sys.stdin.encoding fails)."
  },
  "database_encoding": {
    "type": "string",
    "default": "UTF-8",
    "description": "Encoding of data files used by clipf."
  }
}
