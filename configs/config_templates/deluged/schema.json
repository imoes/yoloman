{
  "log_path": {
    "type": "string",
    "default": "/var/log/deluged/daemon.log",
    "description": "Path to the log file to rotate"
  },
  "log_rotate_count": {
    "type": "number",
    "default": 8,
    "description": "Number of rotated log files to keep"
  }
}
