{
  "config_lines": {
    "type": "list",
    "default": [],
    "description": "List of configuration directive lines. Each item is a single-line string with key=value or comment lines (starting with #). Directives are defined in bat.conf(5) and match the format used in the canonical bat.conf sample file distributed with Bacula. Only standard directives from upstream documentation are used here (e.g., Director, Console, WorkingDirectory)."
  }
}
