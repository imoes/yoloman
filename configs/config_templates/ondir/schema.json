{
  "umask_default": {
    "type": "string",
    "default": "077",
    "description": "The default umask to apply when no specific directory rule matches or as a baseline."
  },
  "rules": {
    "type": "list",
    "default": [],
    "description": "List of directory rules containing enter/leave scripts. Each item is a map with name, entries, etc. (See items schema). Note: 'items' field is provided below as per schema requirement for list-of-objects, but since Jinja2 doesn't have a strict 'items' type in this context, we define the structure of the objects within the list here conceptually, but in the schema object, we use 'items' as a flat map key for the inner object properties? No, the prompt says: 'For a list-of-objects, add items as a FLAT map field-name -> {type, default, description} (NOT {type:object, properties:{\u2026}})'. This implies the 'items' key in the schema for the 'rules' variable should contain the schema for the elements. However, standard JSON schema doesn't work exactly like that in the prompt's weird definition. Let's re-read: 'add items as a FLAT map field-name -> {type, default, description}'. This usually means the schema for the 'rules' variable should look like: { "
  }
}
