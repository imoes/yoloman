{
  "run_daemon": {
    "type": "string",
    "default": "no",
    "description": "Whether to start ftp-proxy at boot via the init script; set to 'yes' to enable",
    "enum": [
      "no",
      "yes"
    ]
  }
}
