{
  "http_port": {
    "type": "list",
    "default": [
      "3128"
    ],
    "description": "List of ports on which Squid listens for HTTP requests (e.g., ['3128', '8080'])."
  },
  "transparent": {
    "type": "bool",
    "default": false,
    "description": "Enable transparent proxy mode (requires iptables NAT redirection)."
  },
  "transparent_port": {
    "type": "string",
    "default": "3128",
    "description": "Port for transparent mode (must match one in http_port if enabled). Only used when transparent=true."
  },
  "cache_dirs": {
    "type": "list",
    "default": [
      {
        "index": 1,
        "location": "/var/spool/squid",
        "size": 1024,
        "max_size": 2048,
        "l1": 16,
        "l2": 256
      }
    ],
    "description": "Disk cache directories for persistent caching. UFS format only."
  },
  "memory_cache_size": {
    "type": "number",
    "default": 256,
    "description": "Size of RAM cache in megabytes. Keep \u226450% of system RAM for safety."
  },
  "log_location": {
    "type": "string",
    "default": "/var/log/squid",
    "description": "Directory for log files."
  },
  "log_rotate_count": {
    "type": "number",
    "default": 5,
    "description": "Number of rotated log files to keep."
  },
  "access_log": {
    "type": "string",
    "default": "/var/log/squid/access.log",
    "description": "Path to access log file."
  },
  "cache_log": {
    "type": "string",
    "default": "/var/log/squid/cache.log",
    "description": "Path to cache log file."
  },
  "pid_filename": {
    "type": "string",
    "default": "/run/squid.pid",
    "description": "File to store the process ID."
  },
  "cache_effective_user": {
    "type": "string",
    "default": "proxy",
    "description": "User Squid runs as."
  },
  "cache_effective_group": {
    "type": "string",
    "default": "proxy",
    "description": "Group Squid runs as."
  },
  "max_in_mem_cache_size": {
    "type": "number",
    "default": null,
    "description": "Maximum size (MB) of objects kept in memory cache. Set to null to omit."
  },
  "acls": {
    "type": "list",
    "default": [],
    "description": "Access Control List definitions. Each entry defines a named ACL with type and value."
  },
  "http_access_rules": {
    "type": "list",
    "default": [],
    "description": "Order matters. First matching rule wins. Use 'deny' before 'allow' to implement defaults."
  }
}
