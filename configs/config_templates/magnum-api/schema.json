{
  "project_name": {
    "type": "string",
    "default": "magnum",
    "description": "The OpenStack project name used for directory structures, PID files, and log paths."
  },
  "uwsgi_port": {
    "type": "number",
    "default": 9511,
    "description": "The port number on which the UWSGI server binds for magnum-api."
  },
  "uwsgi_ini_path": {
    "type": "string",
    "default": "/etc/magnum/magnum-api-uwsgi.ini",
    "description": "The file path to the UWSGI configuration INI file."
  },
  "uwsgi_ini_app": {
    "type": "string",
    "default": "/usr/bin/magnum-api-wsgi",
    "description": "The file path to the UWSGI application entry point script."
  }
}
