{
  "tmpdir": {
    "type": "string",
    "default": "/tmp",
    "description": "Specifies a location to place temporary files, other than the default."
  },
  "default_branch": {
    "type": "string",
    "default": "master",
    "description": "Branch to store Git LFS metadata on. Corresponds to the -b/--branch option."
  }
}
