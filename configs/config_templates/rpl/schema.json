{
  "operations": {
    "type": "list",
    "default": [],
    "description": "List of search/replace operations to perform. Each item is a map containing old_text, new_text, files, and optional flags."
  },
  "default_encoding": {
    "type": "string",
    "default": "utf-8",
    "description": "Default character encoding for files if not specified per operation."
  }
}
