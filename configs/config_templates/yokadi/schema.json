{
  "datadir": {
    "type": "string",
    "default": "/var/lib/yokadi",
    "description": "The directory where the SQLite database file will be stored. Corresponds to the --datadir command line option."
  }
}
