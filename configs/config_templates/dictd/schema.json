{
  "listen_to": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address (and optionally port) for dictd to listen on. Default is localhost-only.",
    "enum": [
      "127.0.0.1",
      "*"
    ]
  },
  "db_list_path": {
    "type": "string",
    "default": "/var/lib/dictd/db.list",
    "description": "Path to the file listing available dictionary databases to include."
  }
}
