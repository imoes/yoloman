# Configuration for the Request Tracker (RT) command-line client (/usr/bin/rt).
# This file defines the endpoint URL for the RT API that the client connects to.

# The URL of the RT server endpoint for the command-line client.
# Must be a valid HTTP(S) URL pointing to the RT instance's REST API or main entry point.
server {{ rt_server_url | default('http://localhost/rt') }}