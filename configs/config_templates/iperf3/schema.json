{
  "pidfile": {
    "type": "string",
    "default": "/run/iperf3.pid",
    "description": "Path to the PID file for the iperf3 daemon."
  },
  "daemon_path": {
    "type": "string",
    "default": "/usr/bin/iperf3",
    "description": "Path to the iperf3 executable."
  },
  "interval": {
    "type": "string",
    "default": "0",
    "description": "Interval for reporting results in seconds. 0 means continuous reporting."
  },
  "start_daemon_default": {
    "type": "string",
    "default": "no",
    "description": "Default value for the START_DAEMON variable. Set to 'yes' to enable starting iperf3 as a daemon.",
    "enum": [
      "no",
      "yes"
    ]
  }
}
