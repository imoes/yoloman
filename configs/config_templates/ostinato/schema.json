{
  "controller_only": {
    "type": "bool",
    "default": false,
    "description": "Run Ostinato in controller-only mode, skipping the local Drone and portgroup."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Show debug logs during execution."
  },
  "session_file": {
    "type": "string",
    "default": "/etc/ostinato/session.ogs",
    "description": "The session file path to open automatically at startup."
  }
}
