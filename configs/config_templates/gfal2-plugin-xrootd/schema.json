{
  "COPY_CHECKSUM_TYPE": {
    "type": "string",
    "default": "ADLER32",
    "description": "Default checksum type for third party copies"
  },
  "NORMALIZE_PATH": {
    "type": "string",
    "default": "true",
    "description": "Whether to normalize paths by turning root://host/path into root://host//path"
  }
}
