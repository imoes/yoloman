{
  "mode": {
    "type": "string",
    "default": "summary",
    "description": "The output mode for ohcount. Valid values: summary, annotate, detect, individual, license, raw."
  },
  "paths": {
    "type": "list",
    "default": [],
    "description": "List of files or directories to count lines of code for."
  }
}
