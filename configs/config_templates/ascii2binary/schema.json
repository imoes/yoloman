{
  "type": {
    "type": "string",
    "default": "uc",
    "description": "Output type and size. Must be one of: d, f, sc, ss, si, sl, sq, uc, us, ui, ul, uq (per -t/--type flag in manpage)",
    "enum": [
      "d",
      "f",
      "sc",
      "ss",
      "si",
      "sl",
      "sq",
      "uc",
      "us",
      "ui",
      "ul",
      "uq"
    ]
  },
  "base": {
    "type": "string",
    "default": "d",
    "description": "Base for integer conversions: 2-36, or b/o/d/h (per -b/--base flag). For non-integer types (-t d/f), this flag is ignored.",
    "enum": [
      "2",
      "3",
      "4",
      "5",
      "21",
      "30",
      "36",
      "b",
      "o",
      "d",
      "h"
    ]
  },
  "locale": {
    "type": "string",
    "default": "C",
    "description": "LC_NUMERIC locale to use (per -L/--locale flag)"
  }
}
