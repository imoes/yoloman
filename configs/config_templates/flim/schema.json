{
  "package_name": {
    "type": "string",
    "default": "flim",
    "description": "Name of the Debian package (used for paths and variable binding)"
  },
  "default_flavor": {
    "type": "string",
    "default": "emacs",
    "description": "Default Emacs flavor name when 'debian-emacs-flavor' is not bound"
  }
}
