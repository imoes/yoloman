{
  "main_queue_size": {
    "type": "number",
    "default": 10000,
    "description": "Maximum number of messages in the main queue before blocking or dropping"
  },
  "work_threads": {
    "type": "number",
    "default": 1,
    "description": "Number of worker threads to process messages (0\u201316 recommended)"
  },
  "action_file_enable_sync": {
    "type": "boolean",
    "default": false,
    "description": "If true, syncs log files after each write (enables durability at cost of performance)"
  },
  "input_modules": {
    "type": "list",
    "default": [
      {
        "name": "imuxsock",
        "purpose": "local syslog via /dev/log"
      },
      {
        "name": "imklog",
        "purpose": "kernel log messages (\u66ff\u4ee3 klogd)"
      }
    ],
    "description": "List of input modules to load; each must be an object with 'name' (module) and 'purpose' (human-readable description)"
  },
  "inputs": {
    "type": "list",
    "default": [],
    "description": "List of input definitions; each item must have 'type' (imuxsock/imudp/imtcp/imrelp), and port/path/address as appropriate"
  },
  "use_rfc3339_timestamps": {
    "type": "boolean",
    "default": true,
    "description": "If true, use RFC3339 (ISO 8601) timestamps in file logs"
  },
  "dynamic_host_logs": {
    "type": "boolean",
    "default": false,
    "description": "If true, create per-host log files using the DynHostFile template"
  },
  "custom_default_template": {
    "type": "string",
    "default": null,
    "description": "Optional custom default template format string (e.g., '%timegenerated% %HOSTNAME% %syslogtag%%msg%\\n')"
  },
  "rulesets": {
    "type": "list",
    "default": [],
    "description": "List of custom rulesets; each must have 'name', 'description', and 'rules' (list of selector/property filter rules)"
  },
  "remote_forwards": {
    "type": "list",
    "default": [],
    "description": "List of upstream syslog servers to forward to; each must have 'host', 'port', and 'protocol' (udp/tcp/relp)"
  },
  "discard_local6": {
    "type": "boolean",
    "default": false,
    "description": "If true, discard all messages from the local6 facility (useful to silence noisy devices)"
  }
}