{
  "webdav_sqlite_db_name": {
    "type": "string",
    "default": "/var/cache/lighttpd/lighttpd.webdav.db",
    "description": "The full path to the SQLite database file used for WebDAV properties and locks."
  }
}
