{
 "alternate": {
  "default": false,
  "description": "Enable alternate progress bar display.",
  "type": "bool"
 },
 "axel": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "headers": {
  "default": [],
  "description": "List of HTTP headers to add (format: \"Header: Value\").",
  "type": "list"
 },
 "insecure": {
  "default": false,
  "description": "Disable SSL/TLS certificate verification.",
  "type": "bool"
 },
 "ipv4": {
  "default": false,
  "description": "Use only IPv4 for connections.",
  "type": "bool"
 },
 "ipv6": {
  "default": false,
  "description": "Use only IPv6 for connections.",
  "type": "bool"
 },
 "max_redirect": {
  "default": 20,
  "description": "Maximum number of HTTP redirections to follow.",
  "type": "number"
 },
 "max_speed": {
  "default": 0,
  "description": "Maximum average download speed in bytes per second (0 = unlimited).",
  "type": "number"
 },
 "no_clobber": {
  "default": false,
  "description": "Skip download if destination file already exists (no state file present).",
  "type": "bool"
 },
 "no_proxy": {
  "default": false,
  "description": "Disable all proxy usage.",
  "type": "bool"
 },
 "num_connections": {
  "default": 4,
  "description": "Maximum number of connections to use per download.",
  "type": "number"
 },
 "quiet": {
  "default": false,
  "description": "Suppress all stdout output.",
  "type": "bool"
 },
 "search_default": {
  "default": 0,
  "description": "Default number of mirrors to use when `-S` mirror search is invoked.",
  "type": "number"
 },
 "timeout": {
  "default": 30,
  "description": "I/O and connection timeout in seconds.",
  "type": "number"
 },
 "user_agent": {
  "default": "",
  "description": "Custom User-Agent string. Leave empty to use default (includes Axel version and platform).",
  "type": "string"
 },
 "v_level": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "verbose": {
  "default": 0,
  "description": "Verbosity level (0 = quiet unless `quiet` is set; >0 adds `verbose` lines, one per level).",
  "type": "number"
 }
}
