{
  "read_env": {
    "type": "string",
    "default": "yes",
    "description": "Whether to read the system's default environment files (typically 'yes' or 'no').",
    "enum": [
      "yes",
      "no"
    ]
  },
  "extra_opts": {
    "type": "string",
    "default": "",
    "description": "Extra options for cron daemon (e.g., '-L 5' for logging exit statuses)."
  }
}
