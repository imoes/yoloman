{
  "default_operation_mode": {
    "type": "string",
    "default": "move",
    "description": "The default action to perform: 'move' corresponds to the -m flag, 'copy' corresponds to the -c flag.",
    "enum": [
      "move",
      "copy"
    ]
  },
  "file_pattern": {
    "type": "string",
    "default": "*.jpg",
    "description": "The glob pattern used to select files for renaming. Should be quoted."
  },
  "name_prefix": {
    "type": "string",
    "default": "renamed_",
    "description": "The prefix string to prepend to the selected files' names."
  }
}
