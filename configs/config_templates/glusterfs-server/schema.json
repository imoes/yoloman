{
  "glusterd_log_file": {
    "type": "string",
    "default": "/var/log/glusterfs/glusterd.log",
    "description": "Path to the GlusterFS management daemon log file."
  },
  "glusterd_log_level": {
    "type": "string",
    "default": "INFO",
    "description": "Logging level for the GlusterFS management daemon (e.g., 'INFO', 'DEBUG', 'WARNING')."
  },
  "glusterd_options": {
    "type": "string",
    "default": "",
    "description": "Additional key-value configuration options for the management daemon (e.g., 'rpc-auth-allow-insecure', 'cli-timeout')."
  }
}
