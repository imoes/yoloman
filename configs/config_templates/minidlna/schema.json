{
  "friendly_name": {
    "type": "string",
    "default": "MiniDLNA",
    "description": "Name of the DLNA/UPnP server as advertised on the network."
  },
  "interface": {
    "type": "string",
    "default": "",
    "description": "Network interface to bind to (e.g., 'eth0'); leave empty to bind to all interfaces."
  },
  "http_port": {
    "type": "number",
    "default": 8200,
    "description": "TCP port for HTTP media streaming and web UI (if enabled)."
  },
  "ssdp_port": {
    "type": "number",
    "default": 1900,
    "description": "UDP port for SSDP/UPnP discovery announcements."
  },
  "media_dirs": {
    "type": "list",
    "default": [
      {
        "path": "/var/lib/minidlna",
        "name": ""
      }
    ],
    "description": "List of media directories (path and optional friendly name).",
    "items": {
      "path": {
        "type": "string",
        "default": "",
        "description": "Absolute path to a media directory (must be readable by minidlna user)."
      },
      "name": {
        "type": "string",
        "default": "",
        "description": "Friendly name for this directory (shown in UPnP browser); leave blank to use path basename."
      }
    }
  },
  "db_dir": {
    "type": "string",
    "default": "/var/cache/minidlna",
    "description": "Directory to store the metadata database and thumbnail cache."
  },
  "log_dir": {
    "type": "string",
    "default": "/var/log/minidlna",
    "description": "Directory to store runtime log files."
  },
  "uuid": {
    "type": "string",
    "default": "",
    "description": "Unique identifier (UUID) for the server. Leave empty to auto-generate."
  },
  "root_container": {
    "type": "string",
    "default": ".",
    "description": "Root container to display at top level: '.' (root), 'F' (filesystem), 'M' (music), 'V' (video), 'P' (pictures)."
  },
  "inotify": {
    "type": "boolean",
    "default": true,
    "description": "Enable inotify-based automatic updates when media files change."
  },
  "strict_dlna": {
    "type": "boolean",
    "default": false,
    "description": "Enable strict DLNA compliance (disables some non-standard but widely used features)."
  },
  "enable_transcoding": {
    "type": "boolean",
    "default": false,
    "description": "Enable external transcoding (requires transcoding_cmd to be set)."
  },
  "transcoding_cmd": {
    "type": "string",
    "default": "",
    "description": "Command to invoke for audio/video transcoding (e.g., '/usr/bin/ffmpeg -i %s -f mp3 -')."
  },
  "max_connections": {
    "type": "number",
    "default": 50,
    "description": "Maximum number of simultaneous client connections."
  },
  "model_name": {
    "type": "string",
    "default": "MiniDLNA",
    "description": "Model name advertised in UPnP device description."
  },
  "model_number": {
    "type": "string",
    "default": "1",
    "description": "Model number advertised in UPnP device description."
  },
  "serial_number": {
    "type": "string",
    "default": "1",
    "description": "Serial number advertised in UPnP device description."
  },
  "friendly_name_model": {
    "type": "string",
    "default": "DLNA Server",
    "description": "Friendly description for UPnP device category (e.g., 'Digital Media Server')."
  },
  "album_art_names": {
    "type": "list",
    "default": [
      "Cover.jpg",
      "Folder.jpg"
    ],
    "description": "List of filenames to recognize as album art (case-insensitive)."
  },
  "enable_upnp": {
    "type": "boolean",
    "default": true,
    "description": "Enable UPnP server advertisement via SSDP."
  },
  "presentation_url": {
    "type": "string",
    "default": "",
    "description": "URL to a web UI for management (e.g., '/'). Leave blank to omit."
  },
  "device_id": {
    "type": "string",
    "default": "",
    "description": "Device ID (optional; auto-generated if empty)."
  },
  "cache_dir": {
    "type": "string",
    "default": "",
    "description": "Directory for temporary cache (e.g., thumbnails); defaults to db_dir if empty."
  },
  "language": {
    "type": "string",
    "default": "",
    "description": "ISO 639-2/B three-letter language code (e.g., 'eng')."
  },
  "tivo_support": {
    "type": "boolean",
    "default": false,
    "description": "Enable TiVo compatibility mode (disables some features, adds TiVo-specific headers)."
  },
  "hide_root": {
    "type": "boolean",
    "default": false,
    "description": "Hide the root container from UPnP browse responses."
  },
  "enable_ipv6": {
    "type": "boolean",
    "default": false,
    "description": "Enable IPv6 support (requires IPv6 networking stack)."
  },
  "metadata_extractors": {
    "type": "list",
    "default": [],
    "description": "List of external metadata extractor scripts (paths separated by ':')."
  },
  "listen_addresses": {
    "type": "list",
    "default": [],
    "description": "List of IP addresses to listen on (overrides interface binding)."
  }
}
