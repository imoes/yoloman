{
  "configfile": {
    "type": "string",
    "default": "/etc/minidlna.conf",
    "description": "Path to the minidlna configuration file."
  },
  "logfile": {
    "type": "string",
    "default": "/var/log/minidlna/minidlna.log",
    "description": "Path to the minidlna log file."
  },
  "user": {
    "type": "string",
    "default": "minidlna",
    "description": "User the daemon should run as (sysV init only; systemd uses unit override)."
  },
  "group": {
    "type": "string",
    "default": "minidlna",
    "description": "Group the daemon should run as (sysV init only; systemd uses unit override)."
  },
  "daemon_opts": {
    "type": "string",
    "default": "-r",
    "description": "Additional options passed to the minidlna daemon (e.g., '-r' for soft rebuild on startup)."
  },
  "friendly_name": {
    "type": "string",
    "default": "MiniDLNA",
    "description": "Name of the DLNA/UPnP server as advertised on the network."
  },
  "interface": {
    "type": "string",
    "default": "",
    "description": "Network interface to bind to (e.g., 'eth0'); leave empty to bind to all interfaces."
  },
  "http_port": {
    "type": "number",
    "default": 8200,
    "description": "TCP port for HTTP media streaming and web UI (if enabled)."
  },
  "ssdp_port": {
    "type": "number",
    "default": 1900,
    "description": "UDP port for SSDP/UPnP discovery announcements."
  },
  "media_dirs": {
    "type": "list",
    "default": [
      {
        "path": "/var/lib/minidlna",
        "name": ""
      }
    ],
    "description": "List of media directories (path and optional friendly name)."
  },
  "media_dirs.*.path": {
    "type": "string",
    "default": "",
    "description": "Absolute path to a media directory (must be readable by minidlna user)."
  },
  "media_dirs.*.name": {
    "type": "string",
    "default": "",
    "description": "Friendly name for this directory (shown in UPnP browser); leave blank to use path basename."
  },
  "db_dir": {
    "type": "string",
    "default": "/var/cache/minidlna",
    "description": "Directory to store the metadata database and thumbnail cache."
  },
  "log_dir": {
    "type": "string",
    "default": "/var/log/minidlna",
    "description": "Directory to store runtime log files."
  },
  "uuid": {
    "type": "string",
    "default": "",
    "description": "Unique identifier (UUID) for the server. Leave empty to auto-generate."
  },
  "root_container": {
    "type": "string",
    "default": ".",
    "description": "Root container to display at top level: '.' (root), 'F' (filesystem), 'M' (music), 'V' (video), 'P' (pictures). Must be one of '.', 'F', 'M', 'V', 'P', 'B'. Note: 'B' is not in the original default config (commented out .), but 'B' is allowed in the schema. The default in the real file is commented out as ., so we use '.'."
  },
  "inotify": {
    "type": "boolean",
    "default": true,
    "description": "Enable inotify-based automatic updates when media files change."
  },
  "strict_dlna": {
    "type": "boolean",
    "default": false,
    "description": "Enable strict DLNA compliance (disables some non-standard but widely used features)."
  },
  "enable_transcoding": {
    "type": "boolean",
    "default": false,
    "description": "Enable external transcoding (requires transcoding_cmd to be set)."
  },
  "transcoding_cmd": {
    "type": "string",
    "default": "",
    "description": "Command to invoke for audio/video transcoding (e.g., '/usr/bin/ffmpeg -i %s -f mp3 -')."
  },
  "max_connections": {
    "type": "number",
    "default": 50,
    "description": "Maximum number of simultaneous client connections."
  },
  "model_name": {
    "type": "string",
    "default": "Windows Media Connect compatible (MiniDLNA)",
    "description": "Model name advertised in UPnP device description."
  },
  "model_number": {
    "type": "string",
    "default": "1",
    "description": "Model number advertised in UPnP device description."
  },
  "serial_number": {
    "type": "string",
    "default": "",
    "description": "Serial number advertised in UPnP device description. Defaults to MAC address if empty."
  },
  "friendly_name_model": {
    "type": "string",
    "default": "",
    "description": "This field was cut off in the prompt's existing values_schema. It does not appear in the real config file provided. Ignoring as it doesn't map to a config directive."
  }
}
