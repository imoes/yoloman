{
  "pattern": {
    "type": "string",
    "default": "3",
    "description": "Default siteswap pattern string to animate."
  },
  "speed": {
    "type": "number",
    "default": 200,
    "description": "Time in milliseconds between throws for animation."
  },
  "nb_balls": {
    "type": "number",
    "default": 3,
    "description": "Number of balls used in the siteswap pattern."
  },
  "cycle_duration": {
    "type": "number",
    "default": 10.0,
    "description": "Duration of one full pattern cycle in seconds."
  },
  "gui": {
    "type": "bool",
    "default": true,
    "description": "Enable graphical user interface."
  },
  "background_color": {
    "type": "string",
    "default": "#000000",
    "description": "Background color for the animation window."
  }
}
