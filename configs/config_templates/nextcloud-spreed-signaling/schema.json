{
  "http_listen": {
    "type": "string",
    "default": "127.0.0.1:8080",
    "description": "IP and port to listen on for HTTP requests."
  },
  "http_readtimeout": {
    "type": "number",
    "default": 15,
    "description": "HTTP socket read timeout in seconds."
  },
  "http_writetimeout": {
    "type": "number",
    "default": 15,
    "description": "HTTP socket write timeout in seconds."
  },
  "https_listen": {
    "type": "string",
    "default": "127.0.0.1:8443",
    "description": "IP and port to listen on for HTTPS requests."
  },
  "https_readtimeout": {
    "type": "number",
    "default": 15,
    "description": "HTTPS socket read timeout in seconds."
  },
  "https_writetimeout": {
    "type": "number",
    "default": 15,
    "description": "HTTPS socket write timeout in seconds."
  },
  "https_certificate": {
    "type": "string",
    "default": "/etc/nginx/ssl/server.crt",
    "description": "Certificate to use for the HTTPS server."
  },
  "https_key": {
    "type": "string",
    "default": "/etc/nginx/ssl/server.key",
    "description": "Private key to use for the HTTPS server."
  },
  "app_debug": {
    "type": "bool",
    "default": false,
    "description": "Set to true to install pprof debug handlers."
  },
  "app_allowsubscribeany": {
    "type": "bool",
    "default": false,
    "description": "Set to true to allow subscribing any streams."
  },
  "sessions_hashkey": {
    "type": "string",
    "default": "the-secret-for-session-checksums",
    "description": "Secret value used to generate checksums of sessions."
  },
  "sessions_blockkey": {
    "type": "string",
    "default": "-encryption-key-",
    "description": "Optional key for encrypting data in the sessions."
  },
  "clients_internalsecret": {
    "type": "string",
    "default": "the-shared-secret-for-internal-clients",
    "description": "Shared secret for connections from internal clients."
  },
  "backend_backendtype": {
    "type": "string",
    "default": "static",
    "description": "Type of backend configuration.",
    "enum": [
      "static",
      "etcd"
    ]
  },
  "backend_backends": {
    "type": "string",
    "default": "backend-id, another-backend",
    "description": "Comma-separated list of backend ids for static backend type."
  },
  "backend_backendprefix": {
    "type": "string",
    "default": "/signaling/backend",
    "description": "Key prefix of backend entries for etcd backend type."
  },
  "backend_allowall": {
    "type": "bool",
    "default": false,
    "description": "Allow any hostname as backend endpoint."
  },
  "backend_secret": {
    "type": "string",
    "default": "the-shared-secret-for-allowall",
    "description": "Common shared secret for requests from and to the backend servers."
  },
  "backend_timeout": {
    "type": "number",
    "default": 10,
    "description": "Timeout in seconds for requests to the backend."
  },
  "backend_connectionsperhost": {
    "type": "number",
    "default": 8,
    "description": "Maximum number of concurrent backend connections per host."
  },
  "backend_skipverify": {
    "type": "bool",
    "default": false,
    "description": "Skip certificate validation of backend endpoints."
  },
  "backend_backend_id_url": {
    "type": "string",
    "default": "https://cloud.domain.invalid",
    "description": "URL of the Nextcloud instance for a specific backend."
  },
  "backend_backend_id_secret": {
    "type": "string",
    "default": "the-shared-secret",
    "description": "Shared secret for a specific backend."
  },
  "backend_backend_id_sessionlimit": {
    "type": "number",
    "default": 10,
    "description": "Limit the number of sessions for a specific backend."
  },
  "backend_backend_id_maxstreambitrate": {
    "type": "number",
    "default": 1048576,
    "description": "Maximum bitrate per publishing stream for a specific backend."
  },
  "backend_backend_id_maxscreenbitrate": {
    "type": "number",
    "default": 2097152,
    "description": "Maximum bitrate per screensharing stream for a specific backend."
  },
  "backend_another_backend_url": {
    "type": "string",
    "default": "https://cloud.otherdomain.invalid",
    "description": "URL of the Nextcloud instance for another backend."
  },
  "backend_another_backend_secret": {
    "type": "string",
    "default": "the-shared-secret",
    "description": "Shared secret for another backend."
  },
  "nats_url": {
    "type": "string",
    "default": "nats://localhost:4222",
    "description": "Url of NATS backend to use."
  },
  "mcu_type": {
    "type": "string",
    "default": "",
    "description": "The type of the MCU to use.",
    "enum": [
      "janus",
      "proxy"
    ]
  },
  "mcu_url": {
    "type": "string",
    "default": "",
    "description": "URL to the MCU server or proxy servers."
  },
  "mcu_maxstreambitrate": {
    "type": "number",
    "default": 1048576,
    "description": "Maximum bitrate per publishing stream."
  },
  "mcu_maxscreenbitrate": {
    "type": "number",
    "default": 2097152,
    "description": "Maximum bitrate per screensharing stream."
  },
  "mcu_proxytimeout": {
    "type": "number",
    "default": 2,
    "description": "Timeout in seconds for requests to the proxy server."
  },
  "mcu_urltype": {
    "type": "string",
    "default": "static",
    "description": "Type of URL configuration for proxy servers.",
    "enum": [
      "static",
      "etcd"
    ]
  },
  "mcu_skipverify": {
    "type": "bool",
    "default": false,
    "description": "Skip certificate validation of proxy servers."
  },
  "mcu_token_id": {
    "type": "string",
    "default": "server1",
    "description": "The id of the token to use when connecting to proxy servers."
  },
  "mcu_token_key": {
    "type": "string",
    "default": "privkey.pem",
    "description": "The private key for the configured token id."
  },
  "mcu_dnsdiscovery": {
    "type": "bool",
    "default": true,
    "description": "Enable DNS discovery on hostname of configured URL."
  },
  "mcu_keyprefix": {
    "type": "string",
    "default": "/signaling/proxy/server",
    "description": "Key prefix of MCU proxy entries for etcd URL type."
  },
  "turn_apikey": {
    "type": "string",
    "default": "the-api-key-for-the-rest-service",
    "description": "API key that the MCU will need to send when requesting TURN credentials."
  },
  "turn_secret": {
    "type": "string",
    "default": "6d1c17a7-c736-4e22-b02c-e2955b7ecc64",
    "description": "The shared secret to use for generating TURN credentials."
  },
  "turn_servers": {
    "type": "string",
    "default": "turn:1.2.3.4:9991?transport=udp,turn:1.2.3.4:9991?transport=tcp",
    "description": "A comma-separated list of TURN servers to use."
  },
  "geoip_license": {
    "type": "string",
    "default": "",
    "description": "License key to use when downloading the MaxMind GeoIP database."
  },
  "geoip_url": {
    "type": "string",
    "default": "",
    "description": "Optional URL to download a MaxMind GeoIP database from."
  },
  "geoip_overrides_127_0_0_1": {
    "type": "string",
    "default": "DE",
    "description": "Override for GeoIP lookup for 127.0.0.1."
  },
  "geoip_overrides_192_168_0_0_24": {
    "type": "string",
    "default": "DE",
    "description": "Override for GeoIP lookup for 192.168.0.0/24."
  },
  "continent_overrides_af": {
    "type": "string",
    "default": "EU",
    "description": "Override for continent mapping for AF."
  },
  "continent_overrides_sa": {
    "type": "string",
    "default": "NA",
    "description": "Override for continent mapping for SA."
  },
  "stats_allowed_ips": {
    "type": "string",
    "default": "",
    "description": "Comma-separated list of IP addresses allowed to access the stats endpoint."
  },
  "etcd_endpoints": {
    "type": "string",
    "default": "127.0.0.1:2379,127.0.0.1:22379,127.0.0.1:32379",
    "description": "Comma-separated list of static etcd endpoints to connect to."
  },
  "etcd_discoverysrv": {
    "type": "string",
    "default": "example.com",
    "description": "Options to perform endpoint discovery through DNS SRV."
  },
  "etcd_discoveryservice": {
    "type": "string",
    "default": "foo",
    "description": "Service name for DNS SRV discovery."
  },
  "etcd_clientkey": {
    "type": "string",
    "default": "/path/to/etcd-client.key",
    "description": "Path to private key for etcd TLS authentication."
  },
  "etcd_clientcert": {
    "type": "string",
    "default": "/path/to/etcd-client.crt",
    "description": "Path to client certificate for etcd TLS authentication."
  },
  "etcd_cacert": {
    "type": "string",
    "default": "/path/to/etcd-ca.crt",
    "description": "Path to CA certificate for etcd TLS authentication."
  },
  "grpc_listen": {
    "type": "string",
    "default": "0.0.0.0:9090",
    "description": "IP and port to listen on for GRPC requests."
  },
  "grpc_servercertificate": {
    "type": "string",
    "default": "/path/to/grpc-server.crt",
    "description": "Certificate to use for the GRPC server."
  },
  "grpc_serverkey": {
    "type": "string",
    "default": "/path/to/grpc-server.key",
    "description": "Private key to use for the GRPC server."
  },
  "grpc_serverca": {
    "type": "string",
    "default": "/path/to/grpc-ca.crt",
    "description": "CA certificate that is allowed to issue certificates of GRPC servers."
  },
  "grpc_clientcertificate": {
    "type": "string",
    "default": "/path/to/grpc-client.crt",
    "description": "Certificate to use for the GRPC client."
  },
  "grpc_clientkey": {
    "type": "string",
    "default": "/path/to/grpc-client.key",
    "description": "Private key to use for the GRPC client."
  },
  "grpc_clientca": {
    "type": "string",
    "default": "/path/to/grpc-ca.crt",
    "description": "CA certificate that is allowed to issue certificates of GRPC clients."
  },
  "grpc_targettype": {
    "type": "string",
    "default": "static",
    "description": "Type of GRPC target configuration.",
    "enum": [
      "static",
      "etcd"
    ]
  },
  "grpc_targets": {
    "type": "string",
    "default": "192.168.0.1:9090, 192.168.0.2:9090",
    "description": "Comma-separated list of GRPC targets to connect to."
  },
  "grpc_dnsdiscovery": {
    "type": "bool",
    "default": true,
    "description": "Enable DNS discovery on hostnames of GRPC target."
  },
  "grpc_targetprefix": {
    "type": "string",
    "default": "/signaling/cluster/grpc",
    "description": "Key prefix of GRPC target entries for etcd target type."
  }
}
