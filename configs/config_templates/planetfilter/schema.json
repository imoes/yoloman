{
  "mailto": {
    "type": "string",
    "default": "root",
    "description": "Email address for cron job output notifications."
  },
  "cron_minute": {
    "type": "number",
    "default": 51,
    "description": "Minute (0-59) when the cron job should run."
  },
  "cron_hour": {
    "type": "number",
    "default": "*",
    "description": "Hour (0-23 or *) when the cron job should run."
  },
  "cron_user": {
    "type": "string",
    "default": "planetfilter",
    "description": "User account under which the cron job executes."
  }
}
