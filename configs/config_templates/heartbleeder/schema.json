{
  "listen": {
    "type": "string",
    "default": "localhost:5000",
    "description": "Address to serve HTTP dashboard from."
  },
  "refresh": {
    "type": "string",
    "default": "10m0s",
    "description": "Seconds to wait before rechecking secure hosts."
  },
  "retry": {
    "type": "string",
    "default": "10s",
    "description": "Seconds to wait before retesting a host after an unfavorable response."
  },
  "timeout": {
    "type": "string",
    "default": "5s",
    "description": "Timeout after sending heartbeat."
  },
  "workers": {
    "type": "number",
    "default": 40,
    "description": "Number of workers to scan hosts with."
  },
  "hostfile": {
    "type": "string",
    "default": "",
    "description": "Path to a newline separated file with hosts or IPs."
  },
  "pg": {
    "type": "bool",
    "default": false,
    "description": "Check PostgreSQL TLS."
  }
}
