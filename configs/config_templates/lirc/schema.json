{
 "irexec": {
  "default": null,
  "description": "irexec daemon configuration (system-wide). Typically only used for edge cases — prefer session-scoped irexec in most deployments.",
  "type": "object"
 },
 "irexec_button_configs": {
  "default": [
   {
    "button": "KEY_RED",
    "config": "echo \"KEY_RED\""
   },
   {
    "button": "KEY_BLUE",
    "config": "echo \"KEY_BLUE\""
   },
   {
    "button": "KEY_1",
    "config": "echo \"KEY_1\""
   },
   {
    "button": "KEY_2",
    "config": "echo \"KEY_2\""
   },
   {
    "button": "KEY_3",
    "config": "echo \"KEY_3\""
   },
   {
    "button": "KEY_OK",
    "config": "echo \"KEY_OK\""
   },
   {
    "button": "KEY_LEFT",
    "config": "echo \"KEY_LEFT\""
   },
   {
    "button": "KEY_RIGHT",
    "config": "echo \"KEY_RIGHT\""
   }
  ],
  "description": "List of button-action pairs for irexec. Each item defines a `prog`, `button`, and `config` line in the irexec config block.",
  "type": "list"
 }
}
