{
  "flags": {
    "type": "list",
    "default": [],
    "description": "List of brotli-rs command-line flags to include. Each item must be a valid flag string, e.g., '--magic', '-c', '-q9'."
  },
  "input_file": {
    "type": "string",
    "default": "",
    "description": "Path to input file. If empty, input is taken from stdin."
  },
  "output_file": {
    "type": "string",
    "default": "",
    "description": "Path to output file. If empty, output goes to stdout."
  }
}
