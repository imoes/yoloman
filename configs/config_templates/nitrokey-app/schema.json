{
  "admin_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable extra administrative functions (matches --admin flag)"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug options (matches --debug flag)"
  },
  "debug_all": {
    "type": "bool",
    "default": false,
    "description": "Enable extensive debug options (matches --debugAll flag)"
  },
  "lock_hardware": {
    "type": "bool",
    "default": false,
    "description": "Enable hardware lock option (matches --lock-hardware flag)"
  },
  "cmd": {
    "type": "string",
    "default": "",
    "description": "Start a command line session with the specified command (matches --cmd flag)"
  }
}
