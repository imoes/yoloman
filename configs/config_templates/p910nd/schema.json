{
  "p910nd_num": {
    "type": "string",
    "default": "",
    "description": "Printer number (empty string for default/only printer; non-empty string used for multi-printer setups)."
  },
  "p910nd_opts": {
    "type": "string",
    "default": "",
    "description": "Additional command-line arguments to pass directly to p910nd (e.g., '-f' to fork to background)."
  },
  "p910nd_start": {
    "type": "number",
    "default": 0,
    "description": "Set to 1 to enable start by default on Debian (0 = disabled by default)."
  }
}
