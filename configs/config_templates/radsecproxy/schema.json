{
  "log_level": {
    "type": "number",
    "default": 2,
    "description": "Log verbosity level: 0=none, 1=error, 2=warn, 3=info, 4=debug"
  },
  "debug_flag": {
    "type": "string",
    "default": "2",
    "description": "Debug flag '-d' if debug is enabled, empty string otherwise",
    "enum": [
      "1",
      "2",
      "3",
      "4",
      "5"
    ]
  },
  "tls_config": {
    "type": "string",
    "default": "",
    "description": "TLS configuration flags string. If use_tls is true, this should contain relevant TLS options. Empty if use_tls is false."
  }
}
