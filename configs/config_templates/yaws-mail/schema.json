{
  "ebin_dir": {
    "type": "string",
    "default": "/usr/lib/yaws-mail/ebin",
    "description": "Path to the directory where the mail beam code is placed."
  },
  "port": {
    "type": "number",
    "default": 8080,
    "description": "Port number for the virtual server webmail application."
  },
  "listen": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to listen on for the virtual server."
  },
  "docroot": {
    "type": "string",
    "default": "/usr/share/yaws-mail/www",
    "description": "Document root for the virtual server webmail application."
  }
}
