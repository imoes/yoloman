{
 "api_token_file": {
  "default": "/etc/debci/api-token",
  "description": "Path to a file containing the API token used to authenticate with the coordinator",
  "type": "string"
 },
 "cleanup_age_days": {
  "default": 7,
  "description": "Delete test artifacts older than this many days",
  "type": "number"
 },
 "cleanup_enabled": {
  "default": true,
  "description": "Automatically remove old test artifacts (logs, builds, etc.) to free disk space",
  "type": "boolean"
 },
 "coordinator_url": {
  "default": "https://coordinator.example.com",
  "description": "Base URL of the debci coordinator server",
  "type": "string"
 },
 "debug": {
  "default": false,
  "description": "Enable debug logging (includes all info/warning/error messages plus detailed debug traces)",
  "type": "boolean"
 },
 "environment": {
  "description": "Key-value pairs of environment variables to export to test jobs (e.g., TZ, DEBEMAIL)",
  "items": {
   "type": "string"
  },
  "type": "object"
 },
 "job_limits": {
  "default": {
   "cpu_quota": 50000,
   "memory_limit": "2G"
  },
  "description": "Resource limits per job (applies to systemd/cgroupv2)",
  "items": {
   "cpu_quota": {
    "description": "CPU quota in microseconds per 100ms period (e.g., 50000 = 50% of one CPU core)",
    "type": "number"
   },
   "memory_limit": {
    "description": "Maximum memory per job (e.g., '2G', '1024M')",
    "type": "string"
   }
  },
  "type": "object"
 },
 "job_timeout": {
  "default": 3600,
  "description": "Maximum time (in seconds) a single test job may run before being killed",
  "type": "number"
 },
 "lxc_architecture": {
  "default": "amd64",
  "description": "Architecture for LXC containers (should match supported_architectures)",
  "type": "string"
 },
 "lxc_distribution": {
  "default": "debian",
  "description": "Distribution name (used with 'download' template)",
  "type": "string"
 },
 "lxc_enabled": {
  "default": false,
  "description": "Run each test job inside an isolated LXC container instead of the host",
  "type": "boolean"
 },
 "lxc_release": {
  "default": "bookworm",
  "description": "Release codename (e.g., 'bullseye', 'focal') — used with 'download' template",
  "type": "string"
 },
 "lxc_template": {
  "default": "debian",
  "description": "LXC template to use (e.g., 'debian', 'ubuntu', 'download')",
  "type": "string"
 },
 "max_jobs": {
  "default": 2,
  "description": "Maximum number of concurrent test jobs this worker can run simultaneously",
  "type": "number"
 },
 "max_retries": {
  "default": 3,
  "description": "Maximum number of retry attempts for a failed job",
  "type": "number"
 },
 "package_cache_dir": {
  "default": "/var/cache/debci/packages",
  "description": "Directory where downloaded test packages and metadata are cached",
  "type": "string"
 },
 "post_test_hook": {
  "default": "",
  "description": "Optional absolute path to a script to run after each test job completes",
  "type": "string"
 },
 "pre_test_hook": {
  "default": "",
  "description": "Optional absolute path to a script to run before each test job begins",
  "type": "string"
 },
 "retry_delay": {
  "default": 30,
  "description": "Number of seconds to wait before retrying a failed job (e.g., network error)",
  "type": "number"
 },
 "supported_architectures": {
  "default": [
   "amd64",
   "i386"
  ],
  "description": "List of CPU architectures this worker can build and test (e.g., 'amd64', 'arm64')",
  "items": {
   "type": "string"
  },
  "type": "list"
 },
 "supported_releases": {
  "default": [
   "bullseye",
   "bookworm",
   "trixie"
  ],
  "description": "List of Debian/Ubuntu distribution releases this worker supports",
  "items": {
   "type": "string"
  },
  "type": "list"
 },
 "verbose": {
  "default": false,
  "description": "Enable verbose logging (info level and above)",
  "type": "boolean"
 },
 "work_dir": {
  "default": "/var/lib/debci",
  "description": "Root directory for worker state, logs, and test artifacts",
  "type": "string"
 },
 "worker_id": {
  "default": "worker-01",
  "description": "Unique name for this worker instance (used by the coordinator to identify it)",
  "type": "string"
 }
}
