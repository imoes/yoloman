{
  "policy_regex": {
    "type": "string",
    "default": ".*",
    "description": "Regular expression matching package names to apply the policy to."
  },
  "sub_regex": {
    "type": "string",
    "default": ".*",
    "description": "Regular expression matching service names to apply the policy to."
  },
  "action": {
    "type": "string",
    "default": "deny",
    "description": "Action to take: allow, deny, or force-reload."
  }
}
