{
  "device": {
    "type": "string",
    "default": "/dev/nvram",
    "description": "Path to the nvram device"
  },
  "next_boot_only": {
    "type": "bool",
    "default": false,
    "description": "Set boot volume for next boot only"
  },
  "list_volumes": {
    "type": "bool",
    "default": false,
    "description": "List boot volume candidates"
  },
  "set_boot": {
    "type": "string",
    "default": "",
    "description": "Set boot volume by name or index"
  },
  "set_boot_macos": {
    "type": "bool",
    "default": false,
    "description": "Set boot volume to macOS if unambiguous"
  },
  "yes": {
    "type": "bool",
    "default": false,
    "description": "Do not ask for confirmation"
  },
  "get_boot": {
    "type": "bool",
    "default": false,
    "description": "Get currently selected boot target"
  },
  "clear_next": {
    "type": "bool",
    "default": false,
    "description": "Clear the selected next boot target"
  }
}
