{
  "default_application_name": {
    "type": "string",
    "default": "Xfce Notification Daemon",
    "description": "The display name of the Xfce notification daemon application entry."
  },
  "default_icon_path": {
    "type": "string",
    "default": "org.xfce.notification",
    "description": "The icon identifier for the notification daemon."
  }
}
