{
  "upx_defaults": {
    "type": "list",
    "default": [],
    "description": "List of default UPX command-line options (e.g., compression levels, overlay handling) that are prefixed to every invocation via the UPX environment variable. See man page for valid options like -9, --lzma, --overlay=strip."
  }
}
