{
  "prog_path": {
    "type": "string",
    "default": "/usr/bin/ocsinventory-agent",
    "description": "Path to the ocsinventory-agent executable."
  },
  "run_dir": {
    "type": "string",
    "default": "/var/run",
    "description": "Directory where run files are stored."
  },
  "enabled_file": {
    "type": "string",
    "default": "/var/run/ocsinventory-client-enabled",
    "description": "Path to the file indicating if the service is enabled."
  }
}
