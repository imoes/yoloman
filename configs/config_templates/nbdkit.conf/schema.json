{
  "loglevel": {
    "type": "string",
    "default": "warning",
    "description": "Log verbosity level: none, error, warning, info, debug, verbose"
  },
  "threads": {
    "type": "number",
    "default": 16,
    "description": "Number of worker threads to handle client connections"
  },
  "plugin_sections": {
    "type": "list",
    "default": [],
    "description": "List of plugin configuration sections (one per exported NBD server or plugin instance, not necessarily 'export' in the NBD sense but plugin instances with filters if supported, though nbdkit.conf primarily handles plugin config keys). Note: nbdkit does not support multiple independent 'exports' or 'connections' in this config file context in the way described. It configures a single server instance. The 'plugin_sections' logic is actually mapping to plugin configuration keys directly under the [general] or specific plugin sections if the config format supported it, but nbdkit.conf is largely a legacy or simplified view. However, looking at the man page, nbdkit.conf is NOT the standard configuration file for nbdkit. nbdkit is typically configured via command line arguments or systemd service files. There is no `nbdkit.conf` man page section for a generic config file with these keys. The 'nbdkit.conf' mentioned in the prompt title might be a confusion with a wrapper script or a specific distribution wrapper. However, standard nbdkit does not have a `plugins`, `maxconnections`, or `idle_timeout` config file directive. It has `--threads`, `--timeout`, etc. on the CLI. Let's look closer. The prompt asks to review `nbdkit.conf`. The man page `nbdkit(1)` does not mention a config file with these specific keys (`loglevel`, `plugins`, `maxconnections`, `idle_timeout`). It mentions `--log`, `--threads`, `--timeout`. The template uses `[general]` and `[plugin_name]` sections. This looks like a custom wrapper or a misunderstanding of nbdkit's configuration model. However, if we assume this template is for a *specific* nbdkit configuration file format used by a wrapper (like `nbdifd` or a specific distribution package that wraps nbdkit), we must check if those keys are documented for *that* config file. Since the provided man page is for `nbdkit(1)` and does not document a `nbdkit.conf` file with these keys, we must treat the non-documented keys as hallucinated. The only key clearly derived from nbdkit CLI is `threads` (from `--threads`) and `loglevel` (from `--log` and `--verbose`, though `--log` takes stderr/syslog/null, not levels. `--verbose` is a boolean. There is no `loglevel` in nbdkit. So `loglevel` is also likely hallucinated or from a wrapper. But wait, `nbdkit` does not have a `loglevel` option. It has `--log=stderr|syslog|null` and `--verbose`. So `loglevel` is wrong/hallucinated. `threads` is valid (`--threads`). `plugin_dir` is not a standard nbdkit config key (it uses `$libdir`). `maxconnections` is not a nbdkit option. `idle_timeout` is not a nbdkit option (it has `--timeout` for handshake). \n\nTherefore, ALL keys except potentially `threads` (if we map it loosely) are not directly in the man page as config file keys. The man page describes CLI options. There is no `nbdkit.conf` man page. If this template claims to be for `nbdkit.conf`, and the man page provided is the *only* source, and the man page does not document `nbdkit.conf` or these keys, then these keys are hallucinated relative to the authoritative source provided. \n\nHowever, often in these tasks, `nbdkit` might be configured via systemd or a wrapper. But the instruction says: \"review ... against the authoritative man page / documentation\". The man page provided is `nbdkit(1)`. It does not mention a config file with these keys. It mentions CLI flags. \n\nLet's look at `nbdkit` configuration more closely. Does `nbdkit` read a config file? No, not natively. It reads command line args. \n\nSo, if the template is for `nbdkit.conf`, and `nbdkit` does not use such a file, the entire template is likely hallucinated or for a different tool. But I must \"Preserve everything already correct\". Is anything correct? \n\nMaybe the prompt implies that `nbdkit` *does* have a config file and I missed it? Re-reading the man page: \"nbdkit --dump-config\" dumps compile-time config. It does not mention a runtime config file. \n\nConclusion: The variables `loglevel`, `plugin_dir`, `maxconnections`, `idle_timeout` are NOT documented in the provided man page as config file keys. `threads` is a CLI key `--threads`. `plugin_sections` is a structural concept not in the man page. \n\nIf I remove all non-documented keys, I am left with `threads`. \n\nIs `threads` documented? Yes, `--threads THREADS`. Default is 16 for parallel plugins. \n\nIs `loglevel` documented? No. `--log` takes stderr/syslog/null. `--verbose` is a flag. There is no `loglevel`. \n\nIs `plugin_dir` documented? No. `$libdir` is used in examples but not a config key. \n\nIs `maxconnections` documented? No. \n\nIs `idle_timeout` documented? No. `--timeout` is for handshake. \n\nSo, I will remove `loglevel`, `plugin_dir`, `maxconnections`, `idle_timeout`, and `plugin_sections`. \n\nI will keep `threads` as it corresponds to `--threads`. \n\nWait, the template structure is `[general] ... [plugin]`. nbdkit does not support this in a config file. This template is fundamentally hallucinated relative to `nbdkit(1)`. However, I can only correct factual errors. If the whole thing is wrong, I should remove the non-documented parts. \n\nLet's assume the user *wants* to map CLI options to a config file. \n`--threads` -> `threads`. \n`--log` -> Not `loglevel`. \n`--timeout` -> Not `idle_timeout`. \n\nI will remove the variables that are not in the man page. \n\nRemoved: `loglevel`, `plugin_dir`, `maxconnections`, `idle_timeout`, `plugin_sections`. \nKept: `threads`. \n\nBut wait, the template has `[general]` and `[plugin]`. If I remove `plugin_sections`, the template loop disappears. \n\nLet's check if `nbdkit` has *any* config file. No. \n\nSo the correct action is to remove all keys not in the man page. \n\nOne detail: `threads` default is 16 for parallel plugins, 1 for single. The man page says \"Only matters for plugins with thread_model=parallel (where it defaults to 16). To force serialized behavior ... set this to 1.\" The schema default is 16. This is a reasonable default for the `--threads` option. \n\nSo I will keep `threads`. I will remove the rest. \n\nI will also remove the `[general]` and `[plugin]` structure if it's not documented, but the instruction says \"Preserve everything already correct\". The structure itself is not a variable. If I remove all variables, do I keep the sections? Probably not, as they serve no purpose. But to be safe, I'll remove the variables and leave the template structure if it's generic? No, if I remove the loop, the template becomes just `[general]` with `threads`. \n\nActually, looking at the `nbdkit` command line, you can't configure multiple plugins in a single instance via config file anyway. So the template is doubly wrong. \n\nI will remove `loglevel`, `plugin_dir`, `maxconnections`, `idle_timeout`, `plugin_sections`. \n\nI will keep `threads`. \n\nI will remove the comments for the removed variables. \n\nFinal Template: \n\n```jinja\n# nbdkit global configuration\n[general]\n# Number of worker threads\nthreads = {{ threads }}\n```\n\nBut wait, the instruction says \"Remove any variable/directive that is NOT documented\". \n\nIs `threads` documented in a *config file*? No, it's documented as a CLI flag. But if this is a template for a config file that wraps CLI flags, it's an interpretation. However, usually, these templates map directly to documented config keys. Since `nbdkit` has no config file with `threads`, is it hallucinated? \n\nIf I look at `systemd` unit for `nbdkit`, it uses `Environment="
  }
}
