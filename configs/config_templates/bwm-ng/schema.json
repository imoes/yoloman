{
  "input_method": {
    "type": "string",
    "default": "",
    "description": "Input method for network data; e.g., proc, getifaddrs, kstat, sysctl, netstat, libstatgrab. Must be compiled-in.",
    "enum": [
      "proc",
      "getifaddrs",
      "kstat",
      "sysctl",
      "netstat",
      "libstatgrab",
      "disk",
      "kstatdisk",
      "devstat",
      "ioservice",
      "libstatdisk"
    ]
  },
  "procfile": {
    "type": "string",
    "default": "/proc/net/dev",
    "description": "Path to proc file used by proc input method."
  },
  "diskstatsfile": {
    "type": "string",
    "default": "/proc/diskstats",
    "description": "Path to diskstats file used by disk input method."
  },
  "partitionsfile": {
    "type": "string",
    "default": "/proc/partitions",
    "description": "Path to partitions file used by disk input method on older kernels."
  },
  "output_method": {
    "type": "string",
    "default": "",
    "description": "Output method: curses, curses2, plain, csv, html.",
    "enum": [
      "curses",
      "curses2",
      "plain",
      "csv",
      "html"
    ]
  },
  "unit": {
    "type": "string",
    "default": "",
    "description": "Unit to display: bytes, bits, packets, errors.",
    "enum": [
      "bytes",
      "bits",
      "packets",
      "errors"
    ]
  },
  "stat_type": {
    "type": "string",
    "default": "",
    "description": "Type of stats: rate, max, sum, avg.",
    "enum": [
      "rate",
      "max",
      "sum",
      "avg"
    ]
  },
  "count": {
    "type": "number",
    "default": 0,
    "description": "Number of outputs for plain/csv mode; 0 in csv mode skips initial zero output."
  },
  "csv_char": {
    "type": "string",
    "default": ";",
    "description": "Delimiter character for CSV output."
  },
  "outfile": {
    "type": "string",
    "default": "",
    "description": "Output file path (required for daemon mode with csv/html output)."
  },
  "html_refresh": {
    "type": "number",
    "default": 0,
    "description": "HTML page refresh interval in seconds."
  },
  "html_header": {
    "type": "bool",
    "default": true,
    "description": "If true, print full HTML header (including meta refresh)."
  },
  "timeout": {
    "type": "number",
    "default": 500,
    "description": "Interval between measurements in milliseconds."
  },
  "dynamic": {
    "type": "bool",
    "default": true,
    "description": "If true, display dynamic units (K/M/G)."
  },
  "allif": {
    "type": "number",
    "default": 1,
    "description": "Interface selection: 0=up+selected, 1=all up, 2=all (incl. down)."
  },
  "interfaces": {
    "type": "string",
    "default": "",
    "description": "Comma-separated list of interface names. Prefix with '%' for blacklist."
  },
  "sumhidden": {
    "type": "bool",
    "default": false,
    "description": "If true, include hidden interfaces in totals."
  },
  "avglength": {
    "type": "number",
    "default": 30,
    "description": "Length of averaging window in seconds for avg stat_type."
  },
  "daemon": {
    "type": "bool",
    "default": false,
    "description": "If true, fork into background (requires outfile)."
  }
}
