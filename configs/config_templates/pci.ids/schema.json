{
  "update_date": {
    "type": "string",
    "default": "N/A",
    "description": "Date the PCI ID list was last updated."
  },
  "entries": {
    "type": "list",
    "default": [],
    "description": "List of PCI entries (classes and vendors). Each item must have a 'type' (string), 'id' (string), 'name' (string), and optionally 'subclasses' or 'devices'. Use the 'items' schema for the structure of each list element's nested objects if needed, but here we flatten the top-level list items into a map of fields. Note: Since the top level contains heterogenous types (Class vs Vendor), the items schema describes the common fields, and specific nested structures are handled within the template logic or implied by the 'type' field. For strict JSON schema compliance with Jinja, we define the 'items' as the structure of a single entry in the list. However, since Class and Vendor structures differ significantly, we will define the 'items' schema for the most complex part: a Vendor entry, and assume Classes are simpler. To make it robust, we will define the 'items' as a map of fields that all entries might share, plus specific arrays. \n\nActually, the prompt requires: 'For a list-of-objects, add items as a FLAT map field-name -> {type, default, description}'. This implies the list items are homogeneous. PCI.ids has heterogeneous top-level entries (Classes vs Vendors). To adhere to the constraint and produce a valid template, we will assume the user provides a flattened structure or we pick the most common complex type. Let's assume the user populates 'entries' with a mix, but the schema must be static. \n\nAlternative approach: The prompt asks for a config template for `pci.ids`. The `pci.ids` file is static data. It's not a service configuration in the traditional sense (like nginx.conf). It's a data file. \n\nIf I treat it as a data file, the 'configuration' is the content itself. \n\nLet's refine the schema to allow for the specific structure of the `entries` list. Since Jinja2 loops over the list, and the list contains different types, a strict JSON schema for `items` is tricky. However, I can define the `items` schema based on the fields available in the `entries` list. \n\nLet's look at the structure again. \n\nClass: \nC <id> <name>\n\t<subclass_id> <subclass_name>\n\t\t<progif_id> <progif_name>\n\nVendor: \n<vendor_id> <vendor_name>\n\t<device_id> <device_name>\n\t\t<subsystem_id> <subsystem_name>\n\nI will define the `entries` list items as having fields that can accommodate both, or I will split them? No, the template handles both in one loop. \n\nI will define the `items` schema for the 'Vendor' structure as it is more complex, and assume 'Class' entries might just have 'type': 'class' and other fields null or empty, OR I will make the schema reflect the union. \n\nActually, the best way to satisfy 'FLAT map field-name' for a list of objects where objects vary is to define the fields that are present in the most common or complex case, or just define the fields for the top-level entry and its immediate children. \n\nLet's try to make `entries` a list of 'Vendor' objects, and handle Classes separately? No, the template does one loop. \n\nLet's assume the user provides a list of 'Vendor' entries and 'Class' entries, and the schema defines the fields for a generic 'Entry'. \n\nFields for a generic entry in `entries`:\n- type: string ('class' or 'vendor')\n- id: string\n- name: string\n- subclasses: list (only if type is 'class')\n- devices: list (only if type is 'vendor')\n\nThe `items` schema will describe the fields of an object in `entries`. Since `subclasses` and `devices` are nested lists, I need to define their item schemas. But the prompt says `items` is a FLAT map. This suggests I should not have nested `properties` in `items`. \n\nWait, the prompt says: "
  },
  "sample_values": {
    "type": "object",
    "default": "",
    "description": ""
  }
}
