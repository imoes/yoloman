{
  "environments": {
    "type": "list",
    "default": [],
    "description": "List of command lines to offer as login session options; each entry is executed if selected by the user at login. Must be newline-separated in the rendered file. Use empty list for default (no pre-configured options). See ENVIRONMENTS in man page (5)."
  }
}
