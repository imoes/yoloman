{
  "DESC": {
    "type": "string",
    "default": "OpenStack Mistral Engine",
    "description": "Description string for the service"
  },
  "PROJECT_NAME": {
    "type": "string",
    "default": "mistral",
    "description": "Project name used for paths, user/group names, and PID files"
  },
  "DAEMON": {
    "type": "string",
    "default": "/usr/bin/mistral-server",
    "description": "Path to the mistral engine daemon executable"
  },
  "DAEMON_ARGS": {
    "type": "string",
    "default": "--server engine",
    "description": "Default arguments passed to the daemon"
  }
}
