{
  "ssl.pemfile": {
    "type": "string",
    "default": "/etc/lighttpd/server.pem",
    "description": "Path to the SSL PEM certificate file."
  },
  "listen_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to listen on for SSL connections."
  },
  "listen_port": {
    "type": "string",
    "default": "443",
    "description": "Port number to listen on for SSL connections."
  }
}
