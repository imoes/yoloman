# Configuration for postfix-mysql package
# This template is auto-generated and self-documenting.
# It configures the loading of the postfix-mysql module, which allows Postfix to
# query a MySQL/MariaDB database for lookups (e.g., virtual maps, relayhost lists).

# The path to the postfix-mysql shared library object.
# This file is loaded by Postfix at startup to provide database lookup support.
#
# Schema Field: module_path
# Default: /usr/lib/postfix/postfix-mysql.so
# The variable `module_path` is expected to be defined in the Ansible inventory
# or defaults, pointing to the compiled `.so` file for the mysql lookup module.
{{ module_path | default('/usr/lib/postfix/postfix-mysql.so') | trim }}:f:root:-:755