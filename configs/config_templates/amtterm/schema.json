{
  "host": {
    "type": "string",
    "default": "localhost",
    "description": "Hostname or IP address of the Intel AMT machine"
  },
  "port": {
    "type": "number",
    "default": 16994,
    "description": "TCP port for AMT redirection; standard is 16994"
  },
  "user": {
    "type": "string",
    "default": "admin",
    "description": "Username for AMT authentication"
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Password for AMT authentication (optional; prefer AMT_PASSWORD env var or prompt)"
  },
  "verbosity": {
    "type": "number",
    "default": 1,
    "description": "Verbosity level: 0=quiet, 1=default, 2=verbose"
  }
}
