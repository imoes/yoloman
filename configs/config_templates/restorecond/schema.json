{
  "provides": {
    "type": "string",
    "default": "restorecond",
    "description": "Service name provided by the init script."
  },
  "short_description": {
    "type": "string",
    "default": "Daemon used to maintain SELinux file context",
    "description": "Short description of the service."
  },
  "description": {
    "type": "string",
    "default": "This daemon uses inotify to look for creation of new files\n                   listed in the /etc/selinux/restorecond.conf file,\n                   and restores the correct security context.",
    "description": "Detailed description of the service."
  },
  "author_email": {
    "type": "string",
    "default": "Laurent Bigonville <bigon@debian.org>",
    "description": "Author of the init script."
  },
  "desc": {
    "type": "string",
    "default": "SELinux file context maintaining daemon",
    "description": "Description string used in log messages."
  },
  "name": {
    "type": "string",
    "default": "restorecond",
    "description": "Service/daemon name."
  },
  "daemon_args": {
    "type": "string",
    "default": "",
    "description": "Additional arguments passed to the daemon."
  }
}
