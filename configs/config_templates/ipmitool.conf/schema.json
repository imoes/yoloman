{
  "host": {
    "type": "string",
    "default": "localhost",
    "description": "BMC hostname/IP or IPMI URI for remote access (e.g., 'ipmi://user:pass@192.168.1.10:623')"
  },
  "username": {
    "type": "string",
    "default": "",
    "description": "BMC username for authentication (leave blank to use environment variable or prompt)"
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "BMC password for authentication (leave blank to use environment variable or prompt)"
  },
  "interface": {
    "type": "string",
    "default": "lan",
    "description": "IPMI interface type to use (e.g., 'lanplus' for IPMI 2.0 over LAN)",
    "enum": [
      "lan",
      "lanplus",
      "open",
      "bmc",
      "lipmi",
      "free",
      "imb"
    ]
  },
  "ciphersuite": {
    "type": "number",
    "default": 17,
    "description": "RMCP+ cipher suite number for 'lanplus' interface (17 = AES-CBC-128 with SHA256)"
  },
  "authlevel": {
    "type": "number",
    "default": 6,
    "description": "RMCP+ authentication level (6 = HMAC-SHA1) for 'lanplus' interface"
  },
  "timeout": {
    "type": "number",
    "default": 1500,
    "description": "RMCP+ session timeout in milliseconds"
  },
  "retries": {
    "type": "number",
    "default": 3,
    "description": "Number of RMCP+ packet retries before failing"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debug output (equivalent to '-v' command-line flag)"
  },
  "sol_keepalive": {
    "type": "bool",
    "default": true,
    "description": "Enable periodic keep-alive packets during SOL sessions"
  },
  "device": {
    "type": "string",
    "default": "/dev/ipmi0",
    "description": "Local IPMI device path (used with kernel interface like 'kcs', 'bt')"
  },
  "local_locking": {
    "type": "bool",
    "default": true,
    "description": "Enable file locking on the local device path for safe concurrent access"
  },
  "use_env": {
    "type": "bool",
    "default": true,
    "description": "Allow credentials and settings to be overridden via environment variables"
  }
}
