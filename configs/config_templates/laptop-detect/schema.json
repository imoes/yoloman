{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging output (maps to -v flag)."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode output (maps to -D flag)."
  },
  "paths": {
    "type": "list",
    "default": [],
    "description": "Additional system paths or identifiers to prioritize during detection. Usually left empty for standard auto-detection."
  }
}
