{
  "spamassassin": {
    "type": "object",
    "description": "SpamAssassin plugin configuration settings (Claws Mail plugin section). Must be a mapping; leave empty or set 'enabled: false' to disable plugin loading. All fields under this key are optional unless specified otherwise by real documented behavior in claws-mail-spamassassin(7)."
  },
  "spamassassin.enabled": {
    "type": "bool",
    "default": false,
    "description": "Set to true to load the SpamAssassin plugin (spamassassin.so) at startup. If false, plugin is not loaded."
  },
  "spamassassin.spamd_host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Hostname or IP address of the SpamAssassin daemon (spamd) to connect to. Must be reachable."
  },
  "spamassassin.spamd_port": {
    "type": "number",
    "default": 783,
    "description": "TCP port on which spamd listens (default 783). Must match spamd configuration."
  },
  "spamassassin.spamd_timeout": {
    "type": "number",
    "default": 30,
    "description": "Connection timeout to spamd in seconds. Must be \u2265 1."
  },
  "spamassassin.local_learning": {
    "type": "bool",
    "default": false,
    "description": "If true, use external 'sa-learn' command for training instead of 'spamd' in TCP mode (requires SpamAssassin \u2265 3.1.x)."
  },
  "spamassassin.spam_folder": {
    "type": "string",
    "default": "",
    "description": "Fully qualified folder path (e.g., 'INBOX.Spam') where spam messages will be moved. If empty or missing, messages go to the default Trash folder."
  },
  "spamassassin.toolbar_buttons": {
    "type": "bool",
    "default": true,
    "description": "If true, allow toolbar button customization for spam/ham marking."
  },
  "spamassassin.toolbar_buttons_list": {
    "type": "list",
    "default": [],
    "description": "List of windows in which to show toolbar spam button. Valid values: ['main_window', 'message_window']. Ignored if toolbar_buttons is false."
  },
  "spamassassin.toolbar_buttons_list.items": {
    "type": "string",
    "default": "",
    "description": "One of 'main_window' or 'message_window' to enable spam button in that window."
  }
}
