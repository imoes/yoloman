{
  "topdir": {
    "type": "string",
    "default": "/var/spool/mlmmj",
    "description": "The top-level directory where mlmmj mailboxes are stored."
  },
  "templatedir_suffix": {
    "type": "string",
    "default": "templates",
    "description": "The suffix for the templates directory path relative to the script's directory."
  }
}
