<?php

# The top-level directory where mlmmj mailboxes are stored.
$topdir = "{{ topdir | default('/var/spool/mlmmj') }}";

# Configuration directory: the directory containing the script itself.
$confdir = dirname(__FILE__);

# The suffix for the templates directory path relative to the script's directory.
# Constructed as: <script_dir>/<templatedir_suffix>
$templatedir = dirname(__FILE__)."/{{ templatedir_suffix | default('templates') }}";

?>