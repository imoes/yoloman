{
  "zip_file": {
    "type": "string",
    "default": "",
    "description": "Path to the ZIP archive to mount (required)"
  },
  "mount_point": {
    "type": "string",
    "default": "",
    "description": "Mount point directory (must exist and be empty)"
  },
  "user": {
    "type": "string",
    "default": "root",
    "description": "User identity under which the fuse-zip process runs"
  },
  "group": {
    "type": "string",
    "default": "fuse",
    "description": "Group identity for access control"
  },
  "mount_options": {
    "type": "list",
    "default": [],
    "description": "List of additional mount options for fuse-zip (e.g., ['default_permissions', 'force_precise_time'])"
  }
}
