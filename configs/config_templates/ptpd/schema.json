{
  "enabled": {
    "type": "boolean",
    "default": true,
    "description": "Start the PTP daemon on service startup."
  },
  "PTPD_OPTS": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options to pass to ptpd (e.g., --interface eth0 --domain 0)."
  }
}
