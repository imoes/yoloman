{
  "credentials": {
    "type": "list",
    "default": [],
    "description": "List of CIFS credential entries used to authenticate when mounting shares. Each entry corresponds to a server, username, and password; optional domain may be included.",
    "items": {
      "type": "object",
      "properties": {
        "server": {
          "type": "string",
          "default": "",
          "description": "Hostname or IP address of the CIFS/SMB server."
        },
        "username": {
          "type": "string",
          "default": "",
          "description": "Username used to authenticate with the CIFS server."
        },
        "password": {
          "type": "string",
          "default": "",
          "description": "Password used to authenticate with the CIFS server. Keep this secret!"
        },
        "domain": {
          "type": "string",
          "default": "",
          "description": "Windows domain (optional) for domain-joined authentication."
        }
      },
      "required": [
        "server",
        "username",
        "password"
      ]
    }
  }
}
