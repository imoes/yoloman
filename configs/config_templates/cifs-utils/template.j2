# /etc/cifs-utils/cifscreds — Store per-user CIFS credentials for auto-login
# Each line defines a CIFS credential entry: <server> <username> <password> [domain]
{% for entry in credentials %}{{ entry.server }} {{ entry.username }} {{ entry.password }}{% if entry.domain is defined %} {{ entry.domain }}{% endif %}
{% endfor %}