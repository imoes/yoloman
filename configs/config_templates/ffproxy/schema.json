{
  "ffproxy_start": {
    "type": "bool",
    "default": true,
    "description": "Whether to start the ffproxy service on boot."
  },
  "ffproxy_usechroot": {
    "type": "bool",
    "default": true,
    "description": "Whether to run the ffproxy daemon inside a chroot jail."
  },
  "user": {
    "type": "string",
    "default": "nobody",
    "description": "System username to drop privileges to after startup (must exist)."
  },
  "group": {
    "type": "string",
    "default": "nogroup",
    "description": "System group for the privileged user."
  }
}
