# x86info command-line arguments configuration
# Generates the command-line flags for the x86info utility.
# Based on x86info(1) OPTIONS section.
# Valid values: 'all', 'cache', 'flags', 'msr', 'mhz', 'reg'

# List of x86info command-line flags to enable.
{% for flag in flags | default(['all']) %}
--{{ flag }}
{% endfor %}