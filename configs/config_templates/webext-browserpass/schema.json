{
  "browserpass_native_path": {
    "type": "string",
    "default": "/usr/lib/browserpass/browserpass-native",
    "description": "The file system path to the Browserpass native messaging host binary."
  }
}
