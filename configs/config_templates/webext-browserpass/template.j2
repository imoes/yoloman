{
    # "name": The unique identifier for the native messaging host manifest.
    # This value must match the ID declared in the browser extension's manifest.json.
    "name": "com.github.browserpass.native",

    # "description": A human-readable description of the native messaging host.
    # This is displayed by the browser when asking for permission to use the extension.
    "description": "Browserpass native component for the Chromium extension",

    # "path": The absolute file system path to the Browserpass native messaging host binary.
    # The browser will attempt to execute this binary to communicate with the extension.
    "path": "{{ browserpass_native_path | default('/usr/lib/browserpass/browserpass-native') }}",

    # "type": The protocol used for communication between the browser extension and the native host.
    # Browserpass exclusively uses standard input/output (stdio) for this purpose.
    "type": "stdio",

    # "allowed_origins": A list of browser extension IDs permitted to connect to this native host.
    # This acts as a security whitelist, ensuring only trusted extensions can access password storage.
    # Each ID corresponds to a specific browser extension build.
    "allowed_origins": [
        "chrome-extension://naepdomgkenhinolocfifgehidddafch/",
        "chrome-extension://cbfnbkdnigbahbkcikfppmaonggdbcpa/",
        "chrome-extension://pjmbgaakjkbhpopmakjoedenlfdmcdgm/",
        "chrome-extension://klfoddkbhleoaabpmiigbmpbjfljimgb/"
    ]
}