{
  "plugin_directory": {
    "type": "string",
    "default": "/usr/share/munin/plugins",
    "description": "Specify the directory used to look up plugins."
  },
  "extension_stripping": {
    "type": "bool",
    "default": false,
    "description": "Enable extension stripping when given."
  },
  "hostname": {
    "type": "string",
    "default": "",
    "description": "Specify the hostname with which the node should greet clients."
  }
}
