{
  "icon_theme_path": {
    "type": "string",
    "default": "/usr/share/icons/hicolor",
    "description": "Path to the top-level icon theme directory (must contain index.theme)"
  },
  "options": {
    "type": "list",
    "default": [],
    "description": "List of options to pass to gtk-update-icon-cache. Each item must be a string matching one of the documented long/short options (e.g. 'force', 'quiet')."
  }
}
