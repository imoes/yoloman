{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging. Corresponds to the -v / --verbose flag."
  },
  "local_only": {
    "type": "bool",
    "default": false,
    "description": "Disable Tor usage. Only for development. Corresponds to --local-only."
  },
  "mode": {
    "type": "string",
    "default": "share",
    "description": "Operating mode: share, receive, public, or website. Corresponds to mode selection in description.",
    "enum": [
      "share",
      "receive",
      "public",
      "website"
    ]
  },
  "filenames": {
    "type": "list",
    "default": [],
    "description": "List of file paths to share or serve. Corresponds to --filenames argument."
  }
}
