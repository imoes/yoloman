{
  "domain": {
    "type": "string",
    "default": "",
    "description": "Domain name to test (required)"
  },
  "connect": {
    "type": "bool",
    "default": false,
    "description": "Attempt HTTP connection to non-RFC 1918 hosts"
  },
  "wide": {
    "type": "bool",
    "default": false,
    "description": "Scan entire class C of discovered records"
  },
  "traverse": {
    "type": "number",
    "default": null,
    "description": "Scan IPs near discovered records (integer offset); null omits flag"
  },
  "search": {
    "type": "list",
    "default": [],
    "description": "Filter on these domains when expanding lookup"
  },
  "range": {
    "type": "string",
    "default": "",
    "description": "Scan an internal IP range in CIDR notation"
  },
  "delay": {
    "type": "number",
    "default": null,
    "description": "Time to wait between lookups in seconds; null omits flag"
  },
  "subdomains": {
    "type": "list",
    "default": [],
    "description": "Use these subdomains"
  },
  "subdomain_file": {
    "type": "string",
    "default": "",
    "description": "Use subdomains specified in this file (one per line)"
  },
  "dns_servers": {
    "type": "list",
    "default": [],
    "description": "Use these DNS servers for reverse lookups"
  },
  "dns_file": {
    "type": "string",
    "default": "",
    "description": "Use DNS servers specified in this file for reverse lookups (one per line)"
  },
  "tcp": {
    "type": "bool",
    "default": false,
    "description": "Use TCP instead of UDP"
  }
}
