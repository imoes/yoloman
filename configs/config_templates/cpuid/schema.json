{
  "text_font_name": {
    "type": "string",
    "default": "Verdana",
    "description": "Font name for information text boxes"
  },
  "text_font_size": {
    "type": "number",
    "default": 13,
    "description": "Font size (in points) for information text boxes"
  },
  "text_font_color": {
    "type": "string",
    "default": "000060",
    "description": "Text color in RRGGBB hex format"
  },
  "label_font_name": {
    "type": "string",
    "default": "Verdana",
    "description": "Font name for label text boxes"
  },
  "label_font_size": {
    "type": "number",
    "default": 13,
    "description": "Font size (in points) for label text boxes"
  },
  "pci": {
    "type": "bool",
    "default": true,
    "description": "Enable PCI bus scanning"
  },
  "max_pci_bus": {
    "type": "number",
    "default": 256,
    "description": "Maximum PCI bus number to scan"
  },
  "dmi": {
    "type": "bool",
    "default": true,
    "description": "Enable DMI/SMBIOS information gathering"
  },
  "sensor": {
    "type": "bool",
    "default": true,
    "description": "Enable hardware sensor detection and voltage measurements"
  },
  "smbus": {
    "type": "bool",
    "default": true,
    "description": "Enable SMBus information gathering"
  },
  "display": {
    "type": "bool",
    "default": true,
    "description": "Enable video card information reporting"
  },
  "use_display_api": {
    "type": "bool",
    "default": false,
    "description": "Use display driver API to read adapter info"
  },
  "bus_clock": {
    "type": "bool",
    "default": true,
    "description": "Enable real-time CPU clock measurement"
  },
  "chipset": {
    "type": "bool",
    "default": true,
    "description": "Enable chipset identification"
  },
  "spd": {
    "type": "bool",
    "default": true,
    "description": "Enable SPD memory module information reading"
  },
  "check_updates": {
    "type": "bool",
    "default": true,
    "description": "Automatically check for application updates on startup"
  },
  "show_duty_cycles": {
    "type": "bool",
    "default": false,
    "description": "Use alternate clock computation method based on duty cycles"
  }
}
