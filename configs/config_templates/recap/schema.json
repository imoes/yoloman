{
  "bindir": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Directory where the recap binary is located."
  },
  "recap_cron_schedule": {
    "type": "string",
    "default": "*/10 * * * * root timeout 5m \"${BINDIR}\"/recap",
    "description": "Cron schedule entry for running recap. Must be a single cron line starting with the time specification."
  }
}
