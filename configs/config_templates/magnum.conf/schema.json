{
  "api_paste_config": {
    "source": "inferred-from-template",
    "type": "string"
  },
  "api_workers": {
    "default": 1,
    "description": "Number of worker processes for the API service.",
    "type": "number"
  },
  "bind_host": {
    "default": "0.0.0.0",
    "description": "IP address the API server listens on.",
    "type": "string"
  },
  "ca_cert": {
    "default": "/etc/magnum/ssl/ca.crt",
    "description": "Path to CA certificate used to sign cluster certificates (used with 'local' cert_manager).",
    "type": "string"
  },
  "ca_key": {
    "default": "/etc/magnum/ssl/ca.key",
    "description": "Path to CA private key used to sign cluster certificates (used with 'local' cert_manager).",
    "type": "string"
  },
  "cert_manager_type": {
    "default": "keystone",
    "description": "Certificate management backend. Supported: 'keystone', 'local', 'barbican'.",
    "type": "string",
    "enum": [
      "keystone",
      "local",
      "barbican"
    ]
  },
  "cluster_cert_ttl": {
    "default": 8760,
    "description": "Certificate validity period (hours) for clusters.",
    "type": "number"
  },
  "cluster_create_timeout": {
    "default": 120,
    "description": "Timeout (minutes) for cluster creation before it is considered failed.",
    "type": "number"
  },
  "conductor_timeout": {
    "default": 600,
    "description": "Timeout (seconds) for conductor operations.",
    "type": "number"
  },
  "conductor_workers": {
    "default": 1,
    "description": "Number of worker processes for the Conductor service.",
    "type": "number"
  },
  "db_connection": {
    "default": "mysql+pymysql://magnum:DB_PASS@controller/magnum",
    "description": "Database connection string for Magnum.",
    "type": "string"
  },
  "default_cluster_template": {
    "default": "kubernetes",
    "description": "Default cluster template used when none is specified during cluster creation.",
    "type": "string"
  },
  "default_docker_volume_size": {
    "default": 20,
    "description": "Default size (GiB) of the Docker volume attached to nodes.",
    "type": "number"
  },
  "default_node_count": {
    "default": 1,
    "description": "Default number of worker nodes for newly created clusters.",
    "type": "number"
  },
  "enable_ssl_api": {
    "default": false,
    "description": "Enable HTTPS for the Magnum API endpoints.",
    "type": "boolean"
  },
  "enable_v1_api": {
    "default": true,
    "description": "Enable Magnum v1 API endpoints.",
    "type": "boolean"
  },
  "enable_v2_api": {
    "default": true,
    "description": "Enable Magnum v2 API endpoints.",
    "type": "boolean"
  },
  "enabled_cluster_templates": {
    "default": [
      "kubernetes",
      "swarm",
      "mesos"
    ],
    "description": "List of cluster template types enabled in this deployment.",
    "items": {
      "description": "Name of an enabled container orchestrator.",
      "enum": [
        "kubernetes",
        "swarm",
        "mesos",
        "swarm-mode",
        "swarm-kubernetes"
      ],
      "type": "string"
    },
    "type": "list"
  },
  "k8s_docker_storage_driver": {
    "default": "overlay2",
    "description": "Docker storage driver used in Kubernetes cluster nodes.",
    "type": "string"
  },
  "k8s_enable_dns": {
    "default": true,
    "description": "Enable Kubernetes DNS add-on in clusters.",
    "type": "boolean"
  },
  "k8s_enable_hpa": {
    "default": false,
    "description": "Enable Kubernetes Horizontal Pod Autoscaler by default.",
    "type": "boolean"
  },
  "k8s_enable_pod_security_policy": {
    "default": false,
    "description": "Enable Kubernetes PodSecurityPolicy by default.",
    "type": "boolean"
  },
  "k8s_insecure_registries": {
    "default": [
      "192.168.1.0/24"
    ],
    "description": "List of insecure (HTTP) Docker registries allowed in cluster nodes.",
    "items": {
      "description": "CIDR or IP address range for an insecure registry.",
      "type": "string"
    },
    "type": "list"
  },
  "k8s_registry_mirror": {
    "default": "",
    "description": "Registry mirror URL to accelerate image pulls (e.g., 'https://mirror.gcr.io').",
    "type": "string"
  },
  "keystone_auth_url": {
    "default": "http://controller:5000",
    "description": "Keystone authentication URL.",
    "type": "string"
  },
  "keystone_project_domain_name": {
    "default": "Default",
    "description": "Keystone project domain name for Magnum service user.",
    "type": "string"
  },
  "keystone_project_name": {
    "default": "service",
    "description": "Keystone project (tenant) name for Magnum service user.",
    "type": "string"
  },
  "keystone_region_name": {
    "default": "RegionOne",
    "description": "Region name used by Magnum to discover services in Keystone.",
    "type": "string"
  },
  "keystone_service_password": {
    "default": "SERVICE_PASS",
    "description": "Password for the Magnum service user in Keystone.",
    "type": "string"
  },
  "keystone_service_user": {
    "default": "magnum",
    "description": "Username for the Magnum service user in Keystone.",
    "type": "string"
  },
  "keystone_user_domain_name": {
    "default": "Default",
    "description": "Keystone user domain name for Magnum service user.",
    "type": "string"
  },
  "logdir": {
    "default": "/var/log/magnum",
    "description": "Directory for Magnum log files.",
    "type": "string"
  },
  "max_cluster_nodes": {
    "default": 100,
    "description": "Maximum number of nodes allowed per cluster.",
    "type": "number"
  },
  "pki_enabled": {
    "default": false,
    "description": "Enable PKI-based certificate generation (deprecated in favor of keystone/barbican).",
    "type": "boolean"
  },
  "pki_file_path": {
    "default": "/etc/magnum/ssl/pki",
    "description": "Base directory for PKI certificate storage (used when pki_enabled=true).",
    "type": "string"
  },
  "ssl_cert_file": {
    "default": "/etc/ssl/certs/magnum.crt",
    "description": "Path to SSL certificate file (required when enable_ssl_api=true).",
    "type": "string"
  },
  "ssl_key_file": {
    "default": "/etc/ssl/private/magnum.key",
    "description": "Path to SSL private key file (required when enable_ssl_api=true).",
    "type": "string"
  },
  "state_path": {
    "default": "/var/lib/magnum",
    "description": "Directory for Magnum state and temporary files.",
    "type": "string"
  },
  "trust_enabled": {
    "default": true,
    "description": "Enable trust mechanism for cluster operations (required for cluster scaling, etc.).",
    "type": "boolean"
  },
  "trustee_domain_admin": {
    "default": "",
    "description": "Username of the trustee domain admin (used when trustee_domain_id is set).",
    "type": "string"
  },
  "trustee_domain_admin_password": {
    "default": "",
    "description": "Password of the trustee domain admin (used when trustee_domain_id is set).",
    "type": "string"
  },
  "trustee_domain_id": {
    "default": "",
    "description": "Domain ID for the trustee (if using delegated trust).",
    "type": "string"
  }
}
