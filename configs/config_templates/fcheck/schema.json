{
  "fcheck_sbin_path": {
    "type": "string",
    "default": "/usr/sbin/fcheck",
    "description": "Full path to the fcheck executable binary"
  },
  "fcheck_config_path": {
    "type": "string",
    "default": "/etc/fcheck/fcheck.cfg",
    "description": "Full path to the fcheck configuration file"
  },
  "fcheck_cron_user": {
    "type": "string",
    "default": "root",
    "description": "User running the fcheck cron job",
    "enum": [
      "root",
      "fcheck"
    ]
  },
  "fcheck_log_output_path": {
    "type": "string",
    "default": "/var/run/fcheck.out",
    "description": "Path to the temporary log output file for fcheck"
  },
  "fcheck_notify_email": {
    "type": "string",
    "default": "root",
    "description": "Email address to send fcheck alerts to"
  }
}
