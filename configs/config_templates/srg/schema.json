{
  "srg_binary_path": {
    "type": "string",
    "default": "/usr/bin/srg",
    "description": "Path to the SRG binary executable."
  },
  "mail_utility_path": {
    "type": "string",
    "default": "/usr/bin/mail",
    "description": "Path to the mail utility used to send reports."
  },
  "srg_log_files": {
    "type": "string",
    "default": "/var/log/squid/access.log",
    "description": "Space-separated list of logfiles to process."
  }
}
