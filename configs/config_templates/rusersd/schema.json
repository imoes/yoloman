{
  "rusersd_bind_address": {
    "type": "list",
    "default": [],
    "description": "List of IP addresses to bind the service to. Empty list binds to all interfaces (0.0.0.0). Entries are strings in IPv4 format."
  },
  "rusersd_debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging for troubleshooting."
  }
}
