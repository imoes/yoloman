{
  "auto_install_tasks": {
    "type": "list",
    "default": [],
    "description": "List of task names to be installed automatically on new installs (e.g., ['server'])."
  },
  "exclude_tasks": {
    "type": "list",
    "default": [],
    "description": "List of task names to exclude from installation."
  },
  "debconf_password": {
    "type": "string",
    "default": "",
    "description": "Debconf password for tasksel, if required."
  }
}
