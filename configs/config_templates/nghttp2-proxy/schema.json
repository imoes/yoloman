{
  "frontend": {
    "type": "string",
    "default": "127.0.0.1,3000",
    "description": "Frontend bind address and port (e.g., 0.0.0.0,3000 or 127.0.0.1,8080)."
  },
  "backend": {
    "type": "string",
    "default": "127.0.0.1,80",
    "description": "Backend server address and port to proxy to."
  },
  "workers": {
    "type": "number",
    "default": 1,
    "description": "Number of worker processes."
  }
}
