{
  "frequency": {
    "type": "string",
    "default": "weekly",
    "description": "Log rotation frequency (e.g., daily, weekly, monthly, or custom interval like 'daily' or 'weekly')",
    "enum": [
      "hourly",
      "daily",
      "weekly",
      "monthly",
      "yearly"
    ]
  },
  "user": {
    "type": "string",
    "default": "root",
    "description": "User to switch to when rotating logs (for su directive)"
  },
  "group": {
    "type": "string",
    "default": "adm",
    "description": "Group to switch to when rotating logs (for su directive)"
  },
  "rotate_count": {
    "type": "number",
    "default": 4,
    "description": "Number of rotated log files to keep"
  },
  "create": {
    "type": "bool",
    "default": true,
    "description": "Whether to create new (empty) log files after rotation"
  },
  "dateext": {
    "type": "bool",
    "default": false,
    "description": "Whether to use date extension (e.g., .20230401) for rotated files"
  },
  "compress": {
    "type": "bool",
    "default": false,
    "description": "Whether to compress rotated log files"
  }
}
