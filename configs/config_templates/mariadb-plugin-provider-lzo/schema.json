{
  "plugin_load_add": {
    "type": "string",
    "default": "provider_lzo",
    "description": "Name of the LZO compression plugin to load for MariaDB."
  },
  "provider_lzo": {
    "type": "string",
    "default": "force_plus_permanent",
    "description": "Loading mode for the LZO plugin (force_plus_permanent, force, etc.)."
  }
}
