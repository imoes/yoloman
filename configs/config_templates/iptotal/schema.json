{
  "document_root_url": {
    "type": "string",
    "default": "/var/lib/iptotal/",
    "description": "The URL alias path for the iptotal web interface."
  },
  "document_root_path": {
    "type": "string",
    "default": "/var/lib/iptotal",
    "description": "The filesystem path to the iptotal document root directory."
  },
  "directory_index": {
    "type": "string",
    "default": "template.html",
    "description": "The default index file served when accessing the directory."
  }
}
