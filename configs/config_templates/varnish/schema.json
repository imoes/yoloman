{
  "backend_default_host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Hostname or IP address of the default backend server."
  },
  "backend_default_port": {
    "type": "string",
    "default": "8080",
    "description": "Port number of the default backend server."
  },
  "default_ttl": {
    "type": "number",
    "default": 3600,
    "description": "Default cache TTL (seconds) for successful responses (2xx/3xx)"
  },
  "error_ttl": {
    "type": "number",
    "default": 60,
    "description": "Cache TTL (seconds) for client/server errors (4xx/5xx). Shorter to avoid propagating stale errors."
  },
  "debug_headers": {
    "type": "bool",
    "default": false,
    "description": "Include detailed debugging headers in responses (X-Varnish-Debug). Only enable in non-production environments."
  },
  "add_x_varnish_headers": {
    "type": "bool",
    "default": true,
    "description": "Add X-Cache (HIT/MISS) and X-Varnish headers to responses. Useful for diagnostics."
  },
  "add_age_header": {
    "type": "bool",
    "default": false,
    "description": "Preserve original Age header in a custom header before unsetting it (for debugging)."
  },
  "serve_stale_on_error": {
    "type": "bool",
    "default": true,
    "description": "Allow serving stale content on backend errors and enable grace period for revalidation. Improves resilience."
  }
}
