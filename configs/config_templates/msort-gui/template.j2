# msort-gui configuration template
# This package is a graphical user interface for sorting/manipulating text files.
# It does NOT listen on network ports, does NOT run as a daemon, and does NOT
# require connection to databases, caches, or other infrastructure services.
# All settings are local to the user's session or stored in user-specific config files.
# No Jinja2 variables are needed for infrastructure wiring.