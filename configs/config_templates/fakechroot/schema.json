{
  "fakechroot_bindir": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Directory where fakechroot wrappers for system binaries are installed."
  },
  "FAKECHROOT_EXCLUDE_PATH": {
    "type": "string",
    "default": "/dev:/proc:/sys",
    "description": "Colon-separated list of paths to exclude from chroot emulation (i.e., bind-mounted or ignored)."
  }
}
