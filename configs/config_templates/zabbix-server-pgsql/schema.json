{
  "logrotate_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "Number of rotated log files to keep."
  },
  "logrotate_create_mode": {
    "type": "string",
    "default": "0640",
    "description": "Octal permission mode for newly created log files."
  },
  "logrotate_create_owner": {
    "type": "string",
    "default": "zabbix",
    "description": "Owner of newly created log files."
  },
  "logrotate_create_group": {
    "type": "string",
    "default": "zabbix",
    "description": "Group of newly created log files."
  }
}
