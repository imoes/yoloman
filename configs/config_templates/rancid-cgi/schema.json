{
  "log_file_name": {
    "type": "string",
    "default": "lg.log",
    "description": "The name of the RANCID CGI log file."
  },
  "log_file_mode": {
    "type": "string",
    "default": "640",
    "description": "The octal permission mode for the log file (without the leading 0, which is added in the template)."
  },
  "log_file_user": {
    "type": "string",
    "default": "www-data",
    "description": "The owner user of the log file."
  },
  "log_file_group": {
    "type": "string",
    "default": "www-data",
    "description": "The owner group of the log file."
  },
  "log_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "The number of rotated log files to keep."
  }
}
