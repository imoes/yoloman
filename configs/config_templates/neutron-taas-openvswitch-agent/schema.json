{
  "project_name": {
    "type": "string",
    "default": "neutron",
    "description": "The project name used for variable substitution (e.g., neutron, nova, cinder). Must match the package prefix."
  }
}
