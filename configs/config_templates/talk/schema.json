{
  "port": {
    "type": "number",
    "default": 517,
    "description": "UDP port used by the ntalk daemon"
  },
  "log_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable logging for the talk daemon"
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/talk.log",
    "description": "Path to the log file for talk connections"
  },
  "max_sessions": {
    "type": "number",
    "default": 10,
    "description": "Maximum concurrent talk sessions"
  },
  "timeout": {
    "type": "number",
    "default": 300,
    "description": "Idle connection timeout in seconds"
  },
  "allowed_users": {
    "type": "list",
    "default": [],
    "description": "List of usernames allowed to use talk; empty means all users"
  }
}
