# nfacct accounting object configuration
# This file defines Netfilter accounting objects for the nfacct service.
# Format: name bytes packets
# Each object corresponds to an 'add' command argument for nfacct.

{% for acct in accounts | default([]) | list %}
{# accounts.items.name: The name of the Netfilter accounting object. #}
{# accounts.items.bytes: Initial byte counter value for the accounting object. #}
{# accounts.items.packets: Initial packet counter value for the accounting object. #}
{{ acct.name | default('') }} {{ acct.bytes | default(0) }} {{ acct.packets | default(0) }}
{% endfor %}