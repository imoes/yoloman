{
  "plugins": {
    "type": "list",
    "default": [
      "cpu",
      "memory",
      "df",
      "load",
      "interfaces"
    ],
    "description": "List of plugin names to enable. These correspond to the internal plugins supported by munin-plugins-c (e.g., cpu, memory, df, load, interfaces, io, interrupts, etc.)."
  },
  "timeout": {
    "type": "number",
    "default": 10,
    "description": "Timeout in seconds for plugin execution."
  },
  "user": {
    "type": "string",
    "default": "munin",
    "description": "User account to run the plugins under."
  },
  "group": {
    "type": "string",
    "default": "munin",
    "description": "Group account to run the plugins under."
  }
}
