{
  "signing_key_path": "/etc/debusine/signing.key",
  "signing_key_passphrase": "correct-horse-battery-staple",
  "signing_algorithm": "rsa-pss-sha256",
  "signing_key_id": "debusine-signer-2024",
  "signing_signature_ttl": 8640000,
  "signing_audiences": [
    "debusine-worker",
    "debusine-api",
    "ci-runner"
  ],
  "verify_trusted_keys_path": "/etc/debusine/keys/trusted/",
  "verify_require_trusted_key": true,
  "verify_max_signature_age": 2592000,
  "verify_allowed_audiences": [
    "debusine-worker",
    "debusine-api",
    "ci-runner"
  ]
}
