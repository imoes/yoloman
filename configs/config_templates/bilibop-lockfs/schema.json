{
  "exec": {
    "type": "string",
    "default": "lockfs-notify",
    "description": "The command to execute when the desktop entry is launched."
  },
  "icon": {
    "type": "string",
    "default": "changes-prevent",
    "description": "Icon name or path for the application."
  },
  "startup_notify": {
    "type": "bool",
    "default": false,
    "description": "Whether startup notification should be enabled."
  },
  "terminal": {
    "type": "bool",
    "default": false,
    "description": "Whether the application should run in a terminal window."
  },
  "hidden": {
    "type": "bool",
    "default": false,
    "description": "Whether the desktop entry should be hidden from menus."
  }
}
