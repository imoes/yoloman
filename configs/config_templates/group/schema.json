{
  "groups": {
    "type": "list",
    "default": [],
    "description": "List of group name-to-ID mappings. Each entry defines a custom routing group.",
    "items": {
      "type": "object",
      "fields": {
        "name": {
          "type": "string",
          "default": "",
          "description": "Name of the group (e.g., 'vpn', 'backup', 'internal'). Must be unique and follow naming rules."
        },
        "id": {
          "type": "number",
          "default": 0,
          "description": "Numeric group ID (0\u20134294967295). Use 0 to indicate automatic assignment (not recommended in static configs)."
        }
      }
    }
  }
}
