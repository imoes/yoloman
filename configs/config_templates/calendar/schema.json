{
  "default_calendar_config_file": {
    "type": "string",
    "default": "calendar",
    "description": "Name of the default configuration file to source under /etc/default/"
  },
  "sendmail_path": {
    "type": "string",
    "default": "/usr/sbin/sendmail",
    "description": "Full path to the sendmail executable"
  },
  "calendar_binary": {
    "type": "string",
    "default": "/usr/bin/calendar",
    "description": "Full path to the calendar binary"
  }
}
