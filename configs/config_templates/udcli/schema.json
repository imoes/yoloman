{
  "mode": {
    "type": "string",
    "default": "32",
    "description": "Disassembly bit mode: 16, 32, or 64.",
    "enum": [
      "16",
      "32",
      "64"
    ]
  },
  "syntax": {
    "type": "string",
    "default": "intel",
    "description": "Instruction syntax format: intel or att."
  },
  "vendor": {
    "type": "string",
    "default": "intel",
    "description": "CPU Vendor identification: intel or amd.",
    "enum": [
      "intel",
      "amd"
    ]
  },
  "pc": {
    "type": "number",
    "default": 0,
    "description": "Initial Program Counter value."
  },
  "skip": {
    "type": "number",
    "default": 0,
    "description": "Number of bytes to skip before disassembly starts."
  },
  "count": {
    "type": "number",
    "default": 0,
    "description": "Number of bytes to disassemble (0 for all)."
  },
  "hex_input": {
    "type": "bool",
    "default": false,
    "description": "Enable hex input mode for whitespace separated hex numbers."
  },
  "no_offset": {
    "type": "bool",
    "default": false,
    "description": "Suppress display of instruction offsets."
  },
  "no_hex": {
    "type": "bool",
    "default": false,
    "description": "Suppress display of hexadecimal machine code."
  },
  "show_eflags": {
    "type": "bool",
    "default": false,
    "description": "Display EFLAGS register status."
  },
  "show_access": {
    "type": "bool",
    "default": false,
    "description": "Display operand access information."
  },
  "show_implicit": {
    "type": "bool",
    "default": false,
    "description": "Display implicit registers modified by instructions."
  }
}
