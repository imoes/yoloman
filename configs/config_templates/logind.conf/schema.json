{
  "n_auto_vts": {
    "type": "number",
    "default": 6,
    "description": "Number of virtual terminals (VTs) to manage automatically"
  },
  "reserve_vts": {
    "type": "list",
    "default": [
      6
    ],
    "description": "List of VT numbers to reserve and not manage (e.g., [6] to reserve the sixth VT). Values are comma-separated in the config file when uncommented."
  },
  "kill_user_processes": {
    "type": "string",
    "default": "no",
    "description": "Whether to kill user processes when they log out. Accepts: yes, no, on-last-log",
    "enum": [
      "yes",
      "no"
    ]
  },
  "kill_only_users": {
    "type": "list",
    "default": [],
    "description": "List of users whose processes will be killed on logout (if KillUserProcesses=yes). Empty means all users."
  },
  "kill_exclude_users": {
    "type": "list",
    "default": [
      "root"
    ],
    "description": "List of users whose processes will be excluded from killing on logout. Default includes 'root'."
  },
  "handle_power_key": {
    "type": "string",
    "default": "poweroff",
    "description": "Action to take when power button is pressed. Accepts: ignore, poweroff, reboot, halt, kexec, suspend, hibernate, hybrid-sleep, suspend-hybrid, shutdown",
    "enum": [
      "ignore",
      "poweroff",
      "reboot",
      "halt",
      "kexec",
      "suspend",
      "hibernate",
      "hybrid-sleep",
      "suspend-then-hibernate",
      "lock",
      "factory-reset"
    ]
  },
  "handle_suspend_key": {
    "type": "string",
    "default": "suspend",
    "description": "Action to take when suspend button is pressed",
    "enum": [
      "ignore",
      "poweroff",
      "reboot",
      "halt",
      "kexec",
      "suspend",
      "hibernate",
      "hybrid-sleep",
      "suspend-then-hibernate",
      "lock",
      "factory-reset"
    ]
  },
  "handle_hibernate_key": {
    "type": "string",
    "default": "hibernate",
    "description": "Action to take when hibernate button is pressed",
    "enum": [
      "ignore",
      "poweroff",
      "reboot",
      "halt",
      "kexec",
      "suspend",
      "hibernate",
      "hybrid-sleep",
      "suspend-then-hibernate",
      "lock",
      "factory-reset"
    ]
  },
  "handle_lid_switch": {
    "type": "string",
    "default": "suspend",
    "description": "Action to take when laptop lid is closed. Accepts: ignore, poweroff, reboot, halt, kexec, suspend, hibernate, hybrid-sleep, suspend-hybrid, shutdown",
    "enum": [
      "ignore",
      "poweroff",
      "reboot",
      "halt",
      "kexec",
      "suspend",
      "hibernate",
      "hybrid-sleep",
      "suspend-then-hibernate",
      "lock",
      "factory-reset"
    ]
  },
  "handle_lid_switch_docked": {
    "type": "string",
    "default": "ignore",
    "description": "Action to take when lid is closed while docked/externally powered (overrides HandleLidSwitch). In shipped default, HandleLidSwitchExternalPower defaults to 'suspend'; HandleLidSwitchDocked defaults to 'ignore'. Here we follow shipped default comments.",
    "enum": [
      "ignore",
      "poweroff",
      "reboot",
      "halt",
      "kexec",
      "suspend",
      "hibernate",
      "hybrid-sleep",
      "suspend-then-hibernate",
      "lock",
      "factory-reset"
    ]
  },
  "remove_ipc": {
    "type": "string",
    "default": "yes",
    "description": "Whether to remove user IPC objects (shared memory, etc.) on logout. Accepts: yes, no, on-last-log",
    "enum": [
      "yes",
      "no"
    ]
  }
}
