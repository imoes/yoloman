{
 "name": {
  "default": "gfsd",
  "description": "The name of the daemon process; used for $NAME, $PIDFILE, and $DAEMON.",
  "type": "string"
 },
 "options": {
  "description": "Additional command-line options passed to the gfsd daemon.",
  "enum": [
   "-c",
   "-cc",
   "-ccc",
   "-d",
   "-v",
   "-?"
  ],
  "type": "string"
 }
}
