{
  "warning": {
    "type": "string",
    "default": "ClamTK does not use a configuration file. Settings are stored in the user's home directory (~/.config/clamtk/) via Gtk settings. No template generation is possible.",
    "description": "Explanatory note about ClamTK's configuration model"
  }
}
