{
  "hosts": {
    "type": "list",
    "default": [],
    "description": "List of host addresses to bind. If empty, defaults to '0.0.0.0' and '::'."
  },
  "port": {
    "type": "number",
    "default": 8080,
    "description": "Port to bind. Cannot be used with unix_socket."
  },
  "unix_socket": {
    "type": "string",
    "default": "",
    "description": "Path to a Unix socket to bind. Mutually exclusive with host/port."
  },
  "unix_socket_perms": {
    "type": "number",
    "default": 384,
    "description": "Filesystem permissions (octal) for the Unix socket. Defaults to 384 (0o600)."
  },
  "backlog": {
    "type": "number",
    "default": 1024,
    "description": "Socket connection backlog. Maximum number of queued connections."
  },
  "static_routes": {
    "type": "list",
    "default": [],
    "description": "List of static route mappings (path \u2192 directory). Each entry must have 'path' (starts with '/', no trailing '/') and 'directory'."
  },
  "static_cors": {
    "type": "string",
    "default": "",
    "description": "CORS header value for static files. '*' for all origins, or a specific origin string. Empty means no CORS header."
  },
  "script_name": {
    "type": "string",
    "default": "",
    "description": "URL prefix for the WSGI app. Must start with '/', no trailing '/' if non-empty."
  },
  "url_scheme": {
    "type": "string",
    "default": "",
    "description": "URL scheme hint ('http' or 'https'). Empty means auto-detected.",
    "enum": [
      "http",
      "https"
    ]
  },
  "threads": {
    "type": "number",
    "default": 4,
    "description": "Number of threads for WSGI app processing."
  },
  "inbuf_overflow": {
    "type": "number",
    "default": 524288,
    "description": "Bytes threshold before spilling request body to temp file. Defaults to 524288 (512 KiB)."
  },
  "max_request_body_size": {
    "type": "number",
    "default": 1073741824,
    "description": "Maximum allowed request body size in bytes. Larger requests get HTTP 413. Defaults to 1 GiB."
  },
  "shutdown_timeout": {
    "type": "number",
    "default": 60.0,
    "description": "Timeout (seconds) for closing client connections on shutdown."
  },
  "verbose": {
    "type": "number",
    "default": 0,
    "description": "Number of -v flags (increase verbosity)."
  },
  "quiet": {
    "type": "number",
    "default": 0,
    "description": "Number of -q flags (decrease verbosity)."
  },
  "app_module": {
    "type": "string",
    "default": "",
    "description": "WSGI module:application (e.g., 'wsgiapp:main'). Required."
  }
}
