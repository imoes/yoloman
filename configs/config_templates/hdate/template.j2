# hdate configuration — parameters passed to hdate(1) command line
# All values mirror the documented command-line options.
# Each item in the list becomes a single argument string on the command line.
{% for item in hdate_args | default([]) %}{{ item }}
{% endfor %}