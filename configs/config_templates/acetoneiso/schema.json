{
  "help_flag": {
    "type": "bool",
    "default": false,
    "description": "Whether to print help and exit (equivalent to --help flag)"
  },
  "mount_file": {
    "type": "string",
    "default": "",
    "description": "Path to CD/DVD image file to mount on startup (optional)"
  },
  "supported_formats": {
    "type": "list",
    "default": [
      "iso",
      "bin",
      "nrk",
      "mdf",
      "img",
      "nrg"
    ],
    "description": "List of image formats supported by acetoneiso (case-insensitive; based on GUI features described in man page)"
  }
}
