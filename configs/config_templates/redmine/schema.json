{
  "redmine_instances_ownership": {
    "type": "string",
    "default": "www-data:www-data",
    "description": "Owner:group for Redmine instance directories and files (e.g., 'www-data:www-data')."
  },
  "redmine_instances_follow_fhs": {
    "type": "boolean",
    "default": true,
    "description": "Whether to follow Filesystem Hierarchy Standard paths (e.g., /var/lib/redmine)."
  },
  "redmine_instances_root": {
    "type": "string",
    "default": "/var/lib/redmine",
    "description": "Root directory for Redmine instances (must be writable by web server)."
  }
}
