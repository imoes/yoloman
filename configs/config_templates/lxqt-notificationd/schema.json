{
  "try_exec": {
    "type": "string",
    "default": "lxqt-notificationd",
    "description": "Path to the executable for TryExec check"
  },
  "exec": {
    "type": "string",
    "default": "lxqt-notificationd",
    "description": "Command line to execute the notification daemon"
  },
  "only_show_in": {
    "type": "string",
    "default": "LXQt;",
    "description": "Semicolon-separated list of desktop environments where this entry is shown"
  },
  "x_lxqt_module": {
    "type": "bool",
    "default": true,
    "description": "Boolean flag indicating this is an LXQt module"
  }
}
