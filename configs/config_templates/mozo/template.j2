# ============================================================================
# mozo (Mozo) Configuration Template
# ============================================================================
# mozo is a utility to run commands or programs when a button on a hardware
# device (like a mouse or keyboard) is pressed. It is NOT a network service,
# database, or daemon that connects to other infrastructure components.
# It operates locally on the host system.
#
# This template is intentionally minimal because the underlying schema
# for 'mozo' is empty (no configurable fields provided in the schema).
# If this were a real service with fields, they would be documented below
# with their defaults and descriptions.
# ============================================================================

# No configuration directives found in the schema.
# The mozo service relies on default system paths and user configuration
# files (typically ~/.config/mozo/mozorc or /etc/mozo/mozorc) which are
# not exposed in the provided Ansible schema.

# Placeholder for any future standard config options.
# If 'mozo' gains network or backend connectivity in future schema versions,
# it would likely be for logging or remote management, but current
# capabilities indicate it is a local hardware event mapper.