{
  "snoop_default_wildcard_suffix": {
    "type": "string",
    "default": "",
    "description": "Suffix to append to the wildcard tty entry (usually empty)."
  },
  "snoop_default_socket_suffix": {
    "type": "string",
    "default": "",
    "description": "Suffix for the socket type entry (usually empty)."
  },
  "snoop_default_login_suffix": {
    "type": "string",
    "default": "",
    "description": "Suffix for the login type entry (usually empty)."
  },
  "snoop_default_exec_suffix": {
    "type": "string",
    "default": "",
    "description": "Suffix for the exec program entry (usually empty)."
  }
}
