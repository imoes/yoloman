{
 "anonymize": {
  "default": false,
  "description": "Anonymize IP addresses in output",
  "type": "bool"
 },
 "buckets": {
  "default": 0,
  "description": "Number of buckets for hash tables (0 = default)",
  "type": "number"
 },
 "count_queries": {
  "default": true,
  "description": "Count query messages (default: true); set to false to exclude queries",
  "type": "bool"
 },
 "count_replies": {
  "default": false,
  "description": "Count reply messages (default: false)",
  "type": "bool"
 },
 "device": {
  "default": "",
  "description": "Ethernet device name (e.g., fxp0); fallback if interface not available",
  "type": "string"
 },
 "domain_filter": {
  "default": "2",
  "description": "Limit counting to a specific domain name (e.g., example.com)",
  "enum": [
   "1",
   "2",
   "3",
   "4",
   "5",
   "6",
   "7",
   "8",
   "9"
  ],
  "type": "string"
 },
 "filter_expr": {
  "default": "udp port 53",
  "description": "BPF filter expression (default: capture DNS on UDP port 53)",
  "type": "string"
 },
 "ignore_addrs": {
  "default": [],
  "description": "List of IP addresses to ignore (e.g., internal DNS server IPs); multiple allowed",
  "type": "list"
 },
 "input_filter": {
  "description": "Apply built-in input filter: 'unknown-tlds', 'new-gtlds', 'A-for-A', 'rfc1918-ptr', 'refused', 'servfail', 'nxdomain', or 'qtype-any'",
  "enum": [
   "unknown-tlds",
   "new-gtlds",
   "A-for-A",
   "rfc1918-ptr",
   "refused",
   "servfail",
   "nxdomain",
   "qtype-any"
  ],
  "type": "string"
 },
 "interface": {
  "default": "",
  "description": "Network interface name (e.g., eth0)",
  "type": "string"
 },
 "ip_version": {
  "description": "Filter by IP version; allowed: 'ipv4', 'ipv6', or empty for both",
  "enum": [
   "ipv4",
   "ipv6"
  ],
  "type": "string"
 },
 "max_levels": {
  "default": 2,
  "description": "Maximum domain name label depth for table categorization (1–9)",
  "type": "number"
 },
 "progress_messages": {
  "default": false,
  "description": "Print progress to stderr in non-interactive mode",
  "type": "bool"
 },
 "promiscuous": {
  "default": true,
  "description": "Enable promiscuous mode (default: true)",
  "type": "bool"
 },
 "redraw_interval": {
  "default": 1,
  "description": "Screen redraw interval in seconds",
  "type": "number"
 },
 "savefile": {
  "default": "",
  "description": "Path to pcap savefile for offline analysis",
  "type": "string"
 },
 "skip_sources": {
  "default": false,
  "description": "Skip sources + query name counters to reduce memory usage",
  "type": "bool"
 }
}
