{
  "FileBase": {
    "type": "string",
    "default": "output",
    "description": "Base name for output files (without extension)"
  },
  "LengthUnit": {
    "type": "number",
    "default": 1,
    "description": "Output length unit in mm (e.g., 0.001 for microns)"
  },
  "MetalLayer": {
    "type": "list",
    "default": [],
    "description": "List of GDSII layer numbers to treat as metal layers; may be specified multiple times in invocation"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output"
  },
  "SeparateLayers": {
    "type": "bool",
    "default": false,
    "description": "Write separate output files for objects on each layer"
  },
  "output_format": {
    "type": "string",
    "default": "GMSH",
    "description": "Output format: 'raw', 'analyze', 'GMSH', or 'scuff-rf'",
    "enum": [
      "raw",
      "GMSH",
      "scuff-rf"
    ]
  }
}
