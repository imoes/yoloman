{
  "input_files": {
    "type": "list",
    "default": [],
    "description": "List of input file paths containing IP ranges, CIDRs, or hostnames."
  },
  "mode": {
    "type": "string",
    "default": "merge",
    "description": "Operation mode: merge, common, except, diff, ipset-reduce.",
    "enum": [
      "merge",
      "common",
      "except",
      "diff"
    ],
    "enum_open": true
  },
  "reduce_factor": {
    "type": "number",
    "default": 20,
    "description": "Percentage of allowed entry increase for ipset-reduce mode."
  },
  "reduce_entries": {
    "type": "number",
    "default": 16384,
    "description": "Max entries allowed in ipset-reduce mode."
  },
  "output_format": {
    "type": "string",
    "default": "cidr",
    "description": "Output type: cidr, ranges, single_ips, csv.",
    "enum": [
      "cidr",
      "ranges",
      "csv"
    ]
  },
  "print_csv_header": {
    "type": "bool",
    "default": false,
    "description": "Whether to print CSV header."
  },
  "default_prefix": {
    "type": "number",
    "default": 32,
    "description": "Default CIDR prefix for single IPs."
  },
  "min_prefix": {
    "type": "number",
    "default": 0,
    "description": "Minimum prefix length (0 means no limit)."
  },
  "prefixes": {
    "type": "string",
    "default": "",
    "description": "Comma-separated list of specific prefixes to use."
  },
  "dns_threads": {
    "type": "number",
    "default": 5,
    "description": "Number of parallel DNS queries."
  },
  "dns_silent": {
    "type": "bool",
    "default": false,
    "description": "Silence DNS errors."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output."
  },
  "dont_fix_network": {
    "type": "bool",
    "default": false,
    "description": "Disable automatic network address fixing for CIDRs."
  },
  "output_prefix": {
    "type": "string",
    "default": "",
    "description": "String to print before each output line."
  },
  "output_suffix": {
    "type": "string",
    "default": "",
    "description": "String to print after each output line."
  }
}
