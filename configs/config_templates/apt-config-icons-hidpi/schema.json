{
  "default_enabled": {
    "type": "string",
    "default": "true",
    "description": "Whether to enable download of HiDPI icons (must be \"true\" or \"false\" as a quoted string)",
    "enum": [
      "true",
      "false"
    ]
  }
}
