{
  "listen_host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address or hostname for the primary listen socket (e.g., '0.0.0.0' for all interfaces)."
  },
  "listen_port": {
    "type": "number",
    "default": 11300,
    "description": "Port number for the primary listen socket."
  },
  "data_dir": {
    "type": "string",
    "default": "/var/lib/beanstalkd",
    "description": "Directory to persist jobs to disk (enables durability). Empty string disables persistence."
  }
}
