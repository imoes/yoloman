# safe-rm excluded paths configuration
# Lists directories that safe-rm will never allow to be removed.
# See: https://github.com/pauldreik/safe-rm

{% for path in (excluded_paths | default(['/', '/bin', '/boot', '/dev', '/etc', '/home', '/initrd', '/lib', '/proc', '/root', '/sbin', '/sys', '/usr', '/usr/bin', '/usr/include', '/usr/lib', '/usr/local', '/usr/local/bin', '/usr/local/include', '/usr/local/sbin', '/usr/local/share', '/usr/sbin', '/usr/share', '/usr/src', '/var'])) %}
{{ path }}
{% endfor %}