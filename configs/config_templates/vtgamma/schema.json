{
  "gamma": {
    "type": "string",
    "default": "1.0",
    "description": "Gamma correction value(s). Single float for all channels or space-separated red green blue floats."
  },
  "reverse": {
    "type": "bool",
    "default": false,
    "description": "Enable black on white mode."
  },
  "permanent": {
    "type": "bool",
    "default": false,
    "description": "Set palette permanently on Linux console (VT) using ioctl."
  },
  "use_lab": {
    "type": "bool",
    "default": false,
    "description": "Use LCHab color space instead of RGB."
  },
  "palette": {
    "type": "string",
    "default": "vga",
    "description": "Color palette name (vga, gruvbox, monokai, ubuntu). Empty string uses default.",
    "enum": [
      "vga",
      "gruvbox",
      "monokai",
      "ubuntu"
    ]
  }
}
