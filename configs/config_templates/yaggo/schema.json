{
  "purpose": {
    "type": "string",
    "default": "Default CLI application",
    "description": "Short purpose of the application."
  },
  "description": {
    "type": "string",
    "default": "A description of the application's function.",
    "description": "Longer description of the application."
  },
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command line options to parse."
  },
  "options.items": {
    "type": "object",
    "default": null,
    "description": "Individual option definition."
  },
  "options.items.short": {
    "type": "string",
    "default": "",
    "description": "Short option character (e.g. 'f')."
  },
  "options.items.long": {
    "type": "string",
    "default": "",
    "description": "Long option name (e.g. 'flag')."
  },
  "options.items.description": {
    "type": "string",
    "default": "Option description",
    "description": "Description of the option."
  },
  "options.items.type": {
    "type": "string",
    "default": "flag",
    "description": "Type of option: flag, int, or string.",
    "enum": [
      "flag",
      "int",
      "string"
    ]
  },
  "options.items.default": {
    "type": "string",
    "default": null,
    "description": "Default value for the option (if applicable)."
  },
  "args": {
    "type": "list",
    "default": [],
    "description": "List of positional arguments to parse."
  },
  "args.items": {
    "type": "object",
    "default": null,
    "description": "Individual argument definition."
  },
  "args.items.name": {
    "type": "string",
    "default": "",
    "description": "Name of the positional argument."
  },
  "args.items.description": {
    "type": "string",
    "default": "Argument description",
    "description": "Description of the argument."
  }
}
