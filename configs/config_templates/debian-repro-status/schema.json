{
 "architecture": {
  "default": "",
  "description": "Target architecture (e.g., amd64, arm64); empty means auto-detect",
  "type": "string"
 },
 "dpkg_query_output": {
  "default": "",
  "description": "Path to pre-captured dpkg-query output; empty means run binary",
  "type": "string"
 },
 "filter": {
  "description": "Package status filter: one of GOOD, BAD, UNKWN",
  "enum": [
   "GOOD",
   "BAD",
   "UNKWN"
  ],
  "type": "string"
 },
 "rebuilderd": {
  "default": "https://rebuilds.debian.net",
  "description": "URL of rebuilderd API endpoint",
  "type": "string"
 },
 "rebuilderd_query_output": {
  "default": "",
  "description": "Path to pre-captured rebuilderd package list; empty means query live",
  "type": "string"
 },
 "summary": {
  "default": false,
  "description": "If true, output only summary counts (no per-package details)",
  "type": "bool"
 }
}
