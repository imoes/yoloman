# StarDict GTK Configuration
# Global settings for StarDict

[General]
# If true, the startup splash screen is hidden (-h flag).
# Defaults to 'True'. Rendered as 'false' if true, 'true' if false (inverted boolean logic for GTK setting).
splash_screen={{ hide_splash_screen | default(true) | ternary('false', 'true') }}