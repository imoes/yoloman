{
  "debug": {
    "type": "boolean",
    "default": false,
    "description": "Enable verbose debug logging. Disable in production unless troubleshooting."
  },
  "log_file": {
    "type": "string",
    "default": "",
    "description": "Path to log file. Leave empty to log to stderr or use syslog if configured."
  },
  "log_level": {
    "type": "string",
    "default": "INFO",
    "description": "Minimum log level. Options: DEBUG, INFO, WARNING, ERROR, CRITICAL."
  },
  "workers": {
    "type": "integer",
    "default": 1,
    "description": "Number of worker processes for the API service. Increase for higher concurrency."
  },
  "database": {
    "type": "object",
    "default": {},
    "description": "Database connection settings (required for stateful deployments).",
    "items": {
      "connection": {
        "type": "string",
        "default": "sqlite:////var/lib/glance/glance.sqlite",
        "description": "SQLAlchemy database connection URL (e.g., 'mysql+pymysql://glance:PASS@db/glance')."
      },
      "max_retries": {
        "type": "integer",
        "default": -1,
        "description": "Maximum database connection retry attempts. -1 = unlimited."
      }
    }
  },
  "keystone_authtoken": {
    "type": "object",
    "default": {},
    "description": "Keystone authentication settings (required for production deployments).",
    "items": {
      "auth_url": {
        "type": "string",
        "default": "http://localhost:5000/v3",
        "description": "Keystone identity endpoint URL."
      },
      "username": {
        "type": "string",
        "default": "glance",
        "description": "Service user name in Keystone."
      },
      "password": {
        "type": "string",
        "default": "",
        "description": "Password for the service user (sensitive\u2014use secrets management in production)."
      },
      "project_name": {
        "type": "string",
        "default": "service",
        "description": "Project (tenant) name for the Glance service user."
      },
      "project_domain_name": {
        "type": "string",
        "default": "Default",
        "description": "Domain name for the project."
      },
      "user_domain_name": {
        "type": "string",
        "default": "Default",
        "description": "Domain name for the user."
      },
      "insecure": {
        "type": "boolean",
        "default": false,
        "description": "Disable TLS certificate verification. Set to false in production."
      },
      "cafile": {
        "type": "string",
        "default": "",
        "description": "Path to CA certificate bundle for TLS verification."
      }
    }
  },
  "paste_deploy": {
    "type": "object",
    "default": {},
    "description": "PasteDeploy configuration (controls middleware pipeline).",
    "items": {
      "flavor": {
        "type": "string",
        "default": "keystone",
        "description": "Pipeline flavor. Typically 'keystone' for production with auth."
      }
    }
  },
  "stores": {
    "type": "list",
    "default": [
      {
        "name": "file",
        "description": "Local filesystem storage"
      }
    ],
    "description": "List of image storage backends (e.g., file, swift, s3, ceph). Must include at least one store.",
    "items": {
      "name": {
        "type": "string",
        "description": "Store backend name (e.g., file, swift, s3, ceph, vsphere)."
      },
      "description": {
        "type": "string",
        "default": "",
        "description": "Optional human-readable description of the store."
      },
      "region": {
        "type": "string",
        "default": "",
        "description": "Region name (for cloud stores like Swift or S3)."
      }
    }
  },
  "default_store": {
    "type": "string",
    "default": "file",
    "description": "Default storage backend name (must match one in 'stores')."
  },
  "filesystem_store_datadir": {
    "type": "string",
    "default": "/var/lib/glance/images",
    "description": "Local directory path for the 'file' store (only required if using file backend)."
  },
  "image_cache_dir": {
    "type": "string",
    "default": "/var/lib/glance/image-cache",
    "description": "Directory for caching transformed images (e.g., converted formats)."
  },
  "image_formats": {
    "type": "list",
    "default": [
      "qcow2",
      "raw",
      "vhd",
      "vdi",
      "vmdk",
      "bare",
      "docker",
      "aki",
      "ari",
      "ami"
    ],
    "description": "List of allowed image formats (for validation)."
  },
  "task": {
    "type": "object",
    "default": {},
    "description": "Task processor settings (used for import/export and image conversion).",
    "items": {
      "enable": {
        "type": "boolean",
        "default": true,
        "description": "Enable the task system."
      },
      "workers": {
        "type": "integer",
        "default": 1,
        "description": "Number of task worker processes."
      }
    }
  },
  "notifications_driver": {
    "type": "string",
    "default": "noop",
    "description": "Notification driver(s) for event logging (e.g., 'noop', 'routing', 'log')."
  }
}
