{
  "interface": {
    "type": "string",
    "default": "auto",
    "description": "Network interface to monitor. Use 'auto' for automatic selection."
  },
  "run_duration": {
    "type": "number",
    "default": 300,
    "description": "Number of seconds to run when monitoring an interface."
  },
  "filter_expr": {
    "type": "string",
    "default": "",
    "description": "Packet filter expression (tcpdump syntax). Leave empty for all traffic."
  },
  "output_format": {
    "type": "string",
    "default": "Time:%S\\tn=%n\\tavg=%a\\tstddev=%d\\tbps=%b\\n",
    "description": "Format string for output, using % substitutions."
  },
  "interval": {
    "type": "number",
    "default": 5.0,
    "description": "Sample interval in seconds."
  }
}
