{
  "hosts_file_path": {
    "type": "string",
    "default": "/etc/hosts",
    "description": "The target hosts file location managed by hostsed."
  },
  "entries": {
    "type": "list",
    "default": [],
    "description": "List of host entries to be managed by hostsed. Each entry contains a host IP and a list of hostnames/aliases for that IP. Items are flat maps with 'host' (string) and 'hosts' (list of strings). If using the 'items' format is required by schema constraints, see below. However, the prompt asks for FLAT map for items. Since 'entries' is a list of objects, I must provide an 'items' field in the schema for 'entries' that describes the flat structure of an item. But wait, the schema for 'entries' type is list. The 'items' constraint applies to the variable 'entries' itself if it were an object? No, 'items' is a field *on* the variable definition for list-of-objects types in the prompt's description: 'For a list-of-objects, add `items` as a FLAT map field-name -> {type, default, description}'. This is slightly ambiguous. Usually, 'items' is not part of the JSON schema draft, but the prompt asks for it in the *values_schema* definition. I will add an 'items' key to the 'entries' definition in values_schema to describe the structure, but 'default' must be a bare array. Since I can't put a nested object in default, I will put an empty array or a simple array of strings if possible. But the entries are objects. The prompt says 'default' MUST be a BARE scalar... or array. For a list of objects, can I have a default like `[{"
  }
}
