{
  "default_modules": {
    "type": "list",
    "default": [],
    "description": "List of Perl module names to be processed by pmtools by default (e.g., CGI, IO::Socket)."
  },
  "search_path": {
    "type": "string",
    "default": "",
    "description": "Optional custom base directory to prepend to Perl library paths for module resolution."
  }
}
