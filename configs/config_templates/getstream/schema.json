{
  "config_file": {
    "type": "string",
    "default": "/etc/getstream.conf",
    "description": "Mandatory path to the configuration file used by getstream (required by -c option)"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode (maps to -d flag); sets DEBUG=1 in environment"
  }
}
