{
  "args": {
    "type": "list",
    "default": [],
    "description": "List of convmv CLI argument strings to be rendered as-is. Each element should be a full CLI argument or quoted group matching documented options."
  }
}
