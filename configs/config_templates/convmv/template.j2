# convmv configuration
# This file is NOT read by convmv directly; it documents typical CLI arguments used to configure convmv invocations.
# Use these values to generate convmv command lines via templating (e.g., in automation or deployment tools).
# The 'args' variable is a list of strings, each representing a full CLI argument or a quoted group.
# The template renders each argument on a new line.

# List of convmv CLI argument strings to be rendered as-is. Each element should be a full CLI argument or quoted group matching documented opt
{% for arg in (args | default([])) %}# Argument: {{ arg | string }}
{{ arg | string }}
{% endfor %}
# No CLI arguments provided. Default behavior is UTF-8 handling with no special options.