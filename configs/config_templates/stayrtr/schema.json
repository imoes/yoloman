{
  "bind_address": {
    "type": "string",
    "default": ":323",
    "description": "The network address and port for the RTR server to bind to."
  },
  "cache_dir": {
    "type": "string",
    "default": "/var/lib/rpki-client/json",
    "description": "The directory where the RPKI cache data is stored."
  }
}
