{
  "patroni_conf_path": {
    "type": "string",
    "default": "/etc/patroni/postgres.yml",
    "description": "Path to the Patroni configuration file (YAML)"
  },
  "patroni_logfile_path": {
    "type": "string",
    "default": "/var/log/patroni.log",
    "description": "Path to the Patroni log file"
  },
  "patroni_user": {
    "type": "string",
    "default": "postgres",
    "description": "Unix user running Patroni"
  },
  "patroni_group": {
    "type": "string",
    "default": "postgres",
    "description": "Unix group running Patroni"
  },
  "patroni_postgres_version": {
    "type": "string",
    "default": "",
    "description": "Explicit PostgreSQL version to use for PGPATH (e.g. '14'). Leave empty to auto-detect via postgresql-common."
  }
}
