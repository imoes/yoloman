{
 "jobs": {
  "default": [],
  "description": "List of cputool job definitions (per-process or per-command limiting). Each item must specify either pid/pid_pgrp OR command, and may optionally set cpu_limit and/or load_limit.",
  "type": "list"
 },
 "omit": {
  "source": "inferred-from-template",
  "type": "string"
 }
}
