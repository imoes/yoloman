{
  "verbosity": {
    "type": "number",
    "default": 0,
    "description": "The verbosity level (-v flag). 0 is silent, 1 enables verbose output."
  },
  "usage_report": {
    "type": "string",
    "default": "disable",
    "description": "Whether to enable or disable anonymous usage reports. Valid values are 'enable' or 'disable'."
  }
}
