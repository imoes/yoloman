{
  "host": {
    "type": "string",
    "default": "",
    "description": "FTP server hostname or IP address"
  },
  "path": {
    "type": "string",
    "default": "",
    "description": "Local mount point path"
  },
  "ftp_opts": {
    "type": "list",
    "default": [],
    "description": "Comma-separated list of curlftpfs FTP/FUSE options (e.g., 'ssl,uid=1000,gid=1000') \u2014 each item must be a valid option string as per curlftpfs(1)"
  }
}
