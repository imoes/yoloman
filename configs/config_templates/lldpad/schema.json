{
  "lldpad_options": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options passed to the lldpad daemon.",
    "enum": [
      "-h",
      "-d",
      "-k",
      "-p",
      "-s",
      "-t",
      "-v",
      "-f",
      "-V"
    ]
  }
}
