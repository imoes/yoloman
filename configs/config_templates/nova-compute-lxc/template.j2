[DEFAULT]
# The compute driver used by Nova.
compute_driver={{ compute_driver | default('libvirt.LibvirtDriver') }}

[libvirt]
# The type of virtualization driver used by Libvirt.
virt_type={{ virt_type | default('lxc') }}