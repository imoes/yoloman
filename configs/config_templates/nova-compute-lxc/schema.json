{
  "compute_driver": {
    "type": "string",
    "default": "libvirt.LibvirtDriver",
    "description": "The compute driver used by Nova."
  },
  "virt_type": {
    "type": "string",
    "default": "lxc",
    "description": "The type of virtualization driver used by Libvirt."
  }
}
