{
  "database_uri": {
    "type": "string",
    "default": "postgresql://localhost/yoyo",
    "description": "The database connection URI used by yoyo to manage migrations."
  },
  "migrations_folder": {
    "type": "string",
    "default": "migrations",
    "description": "The directory path where yoyo looks for migration SQL/Python files."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output (--verbose / -v)."
  },
  "batch_mode": {
    "type": "bool",
    "default": false,
    "description": "Run in batch mode, turning off all user prompts (--batch / -b)."
  }
}
