{
  "interface": {
    "type": "string",
    "default": "",
    "description": "Network interface to monitor; empty string means monitor all interfaces."
  },
  "update_delay_ms": {
    "type": "number",
    "default": 500,
    "description": "Update interval in milliseconds; must be a positive integer."
  },
  "units": {
    "type": "string",
    "default": "bytes",
    "description": "Display units: 'bits' or 'bytes'.",
    "enum": [
      "bits",
      "bytes"
    ]
  },
  "include_interfaces": {
    "type": "list",
    "default": [],
    "description": "List of specific interfaces to include when monitoring all interfaces; ignored if `interface` is non-empty."
  }
}
