{
  "rules": {
    "type": "list",
    "default": [],
    "description": "List of group assignment rules. Each rule maps a user/service condition to a group.",
    "items": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "default": "group",
          "description": "Rule type. Common: 'group', 'time', 'user', 'service'."
        },
        "condition": {
          "type": "string",
          "default": "*",
          "description": "Condition expression (e.g., user name pattern, service name, or '*' for any)."
        },
        "group": {
          "type": "string",
          "default": "",
          "description": "Target group name to assign when condition matches."
        },
        "comment": {
          "type": "string",
          "default": "",
          "description": "Optional human-readable note for this rule."
        },
        "description": {
          "type": "string",
          "default": "",
          "description": "High-level purpose of the rule (shown in install UI)."
        }
      },
      "required": [
        "type",
        "condition",
        "group"
      ]
    }
  }
}
