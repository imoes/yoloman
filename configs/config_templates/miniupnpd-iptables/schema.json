{
  "ipv6": {
    "type": "string",
    "default": "1",
    "description": "Enable IPv6 support",
    "enum": [
      "0",
      "1"
    ]
  },
  "addcmd": {
    "type": "string",
    "default": "-I",
    "description": "Whether to insert rules at head (-I) or append at end (-A) of the chain",
    "enum": [
      "-I",
      "-A"
    ]
  }
}
