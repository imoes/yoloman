{
  "args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments to pass to codfis (e.g., ['--name', 'Mario', '--surname', 'Rossi', '--gender', 'M', '--birthdate', '19800101', '--place', 'Rome']). Each argument must be a string matching what the codfis(1) CLI accepts."
  }
}
