# codfis configuration template
# This template generates the command-line arguments for the codfis service.
# The arguments are derived from the 'args' variable provided by the Ansible role.

{% if args | default([]) | length > 0 %}
# Command-line arguments for codfis
# Defined by the 'args' variable in the schema
{% for arg in args | default([]) %}
{{ arg }}
{% endfor %}
{% endif %}