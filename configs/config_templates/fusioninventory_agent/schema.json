{
  "server": {
    "type": "string",
    "default": "http://localhost:62353",
    "description": "FusionInventory server URL (or local mode with `no-server = 1`)"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debugging output (includes all log levels)"
  },
  "no_server": {
    "type": "bool",
    "default": false,
    "description": "Run in local-only mode (no server communication); output written to `localdir`"
  },
  "loglevel": {
    "type": "number",
    "default": 2,
    "description": "Logging verbosity: 0=error, 1=warn, 2=info, 3=debug"
  },
  "logfile": {
    "type": "string",
    "default": "/var/log/fusioninventory/fusioninventory.log",
    "description": "Path to log file"
  },
  "log_to_syslog": {
    "type": "bool",
    "default": false,
    "description": "Send logs to system syslog in addition to file"
  },
  "interval": {
    "type": "number",
    "default": 3600,
    "description": "Time (in seconds) between automatic inventory runs"
  },
  "hardware": {
    "type": "bool",
    "default": true,
    "description": "Include hardware inventory (CPU, memory, disks, etc.)"
  },
  "software": {
    "type": "bool",
    "default": true,
    "description": "Include installed software inventory"
  },
  "network": {
    "type": "bool",
    "default": true,
    "description": "Include network interfaces and IP configuration"
  },
  "pci": {
    "type": "bool",
    "default": true,
    "description": "Include PCI devices inventory"
  },
  "usb": {
    "type": "bool",
    "default": true,
    "description": "Include USB devices inventory"
  },
  "sound": {
    "type": "bool",
    "default": true,
    "description": "Include sound hardware inventory"
  },
  "video": {
    "type": "bool",
    "default": true,
    "description": "Include video/graphics hardware inventory"
  },
  "storage": {
    "type": "bool",
    "default": true,
    "description": "Include storage devices (disks, partitions, etc.)"
  },
  "printer": {
    "type": "bool",
    "default": true,
    "description": "Include printer inventory"
  },
  "virtualization": {
    "type": "bool",
    "default": true,
    "description": "Include virtualization guests/containers inventory (e.g., Docker, LXC)"
  },
  "processes": {
    "type": "bool",
    "default": true,
    "description": "Include running processes inventory"
  },
  "services": {
    "type": "bool",
    "default": true,
    "description": "Include system services inventory"
  },
  "local_inventory": {
    "type": "bool",
    "default": false,
    "description": "Write inventory to local files instead of sending to server (when `no-server = 1`)"
  },
  "localdir": {
    "type": "string",
    "default": "/var/lib/fusioninventory-agent",
    "description": "Directory to store local inventory files"
  },
  "snmp": {
    "type": "bool",
    "default": false,
    "description": "Enable SNMP scanning of local/network devices (requires additional setup)"
  },
  "wmi": {
    "type": "bool",
    "default": false,
    "description": "Enable WMI queries on Windows (ignored on Linux)"
  },
  "registry": {
    "type": "bool",
    "default": false,
    "description": "Enable Windows registry scanning (ignored on Linux)"
  },
  "tags": {
    "type": "object",
    "default": {},
    "description": "Custom inventory tags as key-value pairs (e.g., environment=production)"
  },
  "proxy_host": {
    "type": "string",
    "default": "",
    "description": "HTTP proxy hostname (empty disables proxy)"
  },
  "proxy_port": {
    "type": "number",
    "default": 8080,
    "description": "HTTP proxy port"
  },
  "proxy_user": {
    "type": "string",
    "default": "",
    "description": "HTTP proxy username (if authentication required)"
  },
  "proxy_password": {
    "type": "string",
    "default": "",
    "description": "HTTP proxy password (if authentication required)"
  },
  "servers": {
    "type": "list",
    "default": [
      {
        "name": "default",
        "url": "http://localhost:62353",
        "username": "fusioninventory",
        "password": "fusioninventory",
        "ssl_verify": true,
        "enabled": true
      }
    ],
    "description": "List of FusionInventory servers to report inventory to (supports multiple)",
    "items": {
      "name": {
        "type": "string",
        "description": "Unique identifier for this server entry"
      },
      "url": {
        "type": "string",
        "description": "Base URL of the FusionInventory server (e.g., http://fusioninventory.example.com:62353)"
      },
      "username": {
        "type": "string",
        "description": "Username for authentication with this server"
      },
      "password": {
        "type": "string",
        "description": "Password for authentication with this server (store securely)"
      },
      "ssl_verify": {
        "type": "bool",
        "description": "Verify SSL certificates for HTTPS connections (set to `false` only with self-signed certs)"
      },
      "ca_bundle": {
        "type": "string",
        "default": "",
        "description": "Optional path to custom CA bundle (PEM format); leaves blank to use system CA store"
      },
      "enabled": {
        "type": "bool",
        "description": "Enable this server (set to `false` to disable reporting)"
      }
    }
  }
}
