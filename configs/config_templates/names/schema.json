{
  "amount": {
    "type": "number",
    "default": 1,
    "description": "Number of names to generate"
  },
  "number_suffix": {
    "type": "bool",
    "default": false,
    "description": "Adds a random number to the name(s)"
  }
}
