{
  "desc": {
    "type": "string",
    "default": "OpenStack Neutron Metering Agent (neutron-metering-agent)",
    "description": "Description string for the init script daemon"
  },
  "project_name": {
    "type": "string",
    "default": "neutron",
    "description": "The OpenStack project name used for paths and variables"
  },
  "rabbit_processname": {
    "type": "string",
    "default": "neutron-metering-agent",
    "description": "RabbitMQ process name for the messaging agent"
  }
}
