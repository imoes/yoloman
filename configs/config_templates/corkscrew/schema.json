{
  "proxy_host": {
    "type": "string",
    "default": "proxy.example.com",
    "description": "Hostname or IP address of the HTTP proxy server that supports the CONNECT method."
  },
  "proxy_port": {
    "type": "number",
    "default": 3128,
    "description": "Port number on which the HTTP proxy is listening."
  },
  "auth_file": {
    "type": "string",
    "default": "",
    "description": "Path to an optional authentication file containing 'username:password' on a single line. Leave empty to omit authentication."
  }
}
