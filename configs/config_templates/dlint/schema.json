{
  "zone": {
    "type": "string",
    "default": "",
    "description": "Fully-qualified domain name of the DNS zone to scan (must end with a period)"
  },
  "recursive": {
    "type": "bool",
    "default": true,
    "description": "Whether to recursively traverse subdomains (equivalent to omitting -n)"
  },
  "nameserver": {
    "type": "string",
    "default": "",
    "description": "Optional IP address or FQDN of the authoritative nameserver to query directly"
  }
}
