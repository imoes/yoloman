{
  "start_page": {
    "type": "string",
    "default": "about:favourites",
    "description": "The URL loaded for new tabs and windows."
  },
  "search_engine": {
    "type": "string",
    "default": "gemini://geminisearch.org/search/%1",
    "description": "Search engine URL template with %1 placeholder."
  },
  "ui_theme": {
    "type": "string",
    "default": "OS Default",
    "description": "UI Theme: Light, Dark, or OS Default.",
    "enum": [
      "Light",
      "Dark",
      "OS Default"
    ]
  },
  "ui_density": {
    "type": "string",
    "default": "Classic",
    "description": "UI Density: Compact or Classic.",
    "enum": [
      "Compact",
      "Classic"
    ]
  },
  "max_redirections": {
    "type": "number",
    "default": 5,
    "description": "Maximum number of redirections allowed. 0 disables redirections."
  },
  "network_timeout": {
    "type": "number",
    "default": 30,
    "description": "Network timeout in seconds."
  },
  "enable_gemini": {
    "type": "bool",
    "default": true,
    "description": "Enable Gemini protocol."
  },
  "enable_http": {
    "type": "bool",
    "default": true,
    "description": "Enable HTTP protocol."
  },
  "enable_gopher": {
    "type": "bool",
    "default": false,
    "description": "Enable Gopher protocol."
  },
  "cache_total_size": {
    "type": "number",
    "default": 512,
    "description": "Total cache size limit in KiB. 0 disables caching."
  },
  "cache_item_size_threshold": {
    "type": "number",
    "default": 400,
    "description": "Max size of a single cached item in KiB."
  },
  "cache_item_life": {
    "type": "number",
    "default": -1,
    "description": "Cached item life in minutes. -1 for unlimited item life."
  },
  "render_emojis": {
    "type": "bool",
    "default": true,
    "description": "Toggle rendering of emojis."
  },
  "text_rendering": {
    "type": "string",
    "default": "Fancy",
    "description": "Text rendering mode: Fancy or Always plain text.",
    "enum": [
      "Fancy",
      "Always plain text"
    ]
  },
  "use_typographers_quotes": {
    "type": "bool",
    "default": false,
    "description": "Use typographer's quotes."
  },
  "ansi_escape_sequences": {
    "type": "string",
    "default": "Ignore",
    "description": "Handling of ANSI escape sequences: Ignore, Interpret (preformatted text only), Strip.",
    "enum": [
      "Ignore",
      "Interpret (preformatted text only)",
      "Strip"
    ]
  },
  "enable_text_highlights": {
    "type": "bool",
    "default": true,
    "description": "Enable text highlights (bold/underline) in gemtext."
  },
  "gopher_map_style": {
    "type": "string",
    "default": "Iconized",
    "description": "Gopher map display style: Iconized or Textual.",
    "enum": [
      "Iconized",
      "Textual"
    ]
  },
  "show_hidden_files": {
    "type": "bool",
    "default": false,
    "description": "Show hidden files in local file:// directories."
  },
  "strip_nav_from_html": {
    "type": "bool",
    "default": false,
    "description": "Strip <nav> tags from HTML pages."
  },
  "standard_font": {
    "type": "string",
    "default": "Sans Serif",
    "description": "Default font family for standard text."
  },
  "background_color": {
    "type": "string",
    "default": "#ffffff",
    "description": "Background color in hex format."
  },
  "indent_size": {
    "type": "number",
    "default": 15,
    "description": "Indent size in pixels."
  },
  "trust_level_gemini": {
    "type": "string",
    "default": "TOFU",
    "description": "Gemini trust level: TOFU, Everything, or Manual.",
    "enum": [
      "TOFU",
      "Everything",
      "Manual"
    ]
  },
  "use_local_cas_gemini": {
    "type": "bool",
    "default": true,
    "description": "Use local certificate authorities for Gemini."
  },
  "trust_level_https": {
    "type": "string",
    "default": "TOFU",
    "description": "HTTPS trust level: TOFU, Everything, or Manual.",
    "enum": [
      "TOFU",
      "Everything",
      "Manual"
    ]
  },
  "use_local_cas_https": {
    "type": "bool",
    "default": true,
    "description": "Use local certificate authorities for HTTPS."
  }
}
