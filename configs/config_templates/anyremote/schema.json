{
  "peers": {
    "type": "list",
    "default": [
      "bluetooth",
      "tcp:5197",
      "web:5080",
      "avahi"
    ],
    "description": "List of peer specifications (e.g., tcp:5197, bluetooth, web:5080, avahi, etc.)"
  },
  "sdp_service_name": {
    "type": "string",
    "default": "anyRemote",
    "description": "SDP service name used for Bluetooth and TCP service discovery"
  },
  "password_auth": {
    "type": "bool",
    "default": false,
    "description": "Enable password authentication (requires ~/.anyRemote/password file)"
  },
  "backend_port": {
    "type": "number",
    "default": null,
    "description": "Port for GUI frontend backend connection (e.g., -fe 5198)"
  },
  "auto_reconnect": {
    "type": "bool",
    "default": false,
    "description": "Enable automatic reconnect in AT mode"
  },
  "verbose_logging": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging to ~/.anyRemote/anyremote.log"
  },
  "config_dir": {
    "type": "string",
    "default": null,
    "description": "Override default directory for configuration files"
  },
  "tmp_dir": {
    "type": "string",
    "default": null,
    "description": "Override default directory for temporary files"
  },
  "user": {
    "type": "string",
    "default": null,
    "description": "Effective user to drop privileges to (if started as root)"
  }
}
