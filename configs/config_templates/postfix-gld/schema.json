{
  "log_date_format": {
    "type": "string",
    "default": "%b",
    "description": "The month abbreviation format for syslog timestamps."
  },
  "log_time_width": {
    "type": "string",
    "default": " 11",
    "description": "The fixed width padding for the time component in syslog messages."
  }
}
