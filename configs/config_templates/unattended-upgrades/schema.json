{
  "name": {
    "type": "string",
    "default": "unattended-upgrades-shutdown",
    "description": "The base name of the init script service."
  },
  "description": {
    "type": "string",
    "default": "unattended package upgrades shutdown",
    "description": "Human-readable description of the service."
  },
  "shutdown_helper": {
    "type": "string",
    "default": "/usr/share/unattended-upgrades/unattended-upgrade-shutdown",
    "description": "Path to the Python script that checks for running unattended upgrades."
  }
}
