{
  "provides": [
    {
      "capability": "message_queue",
      "backends": [
        "redis"
      ],
      "port_field": "port",
      "default_port": 5000,
      "provisionable": {
        "workers": "workers"
      }
    }
  ],
  "requires": [
    {
      "capability": "cache",
      "backends": [
        "redis"
      ],
      "backend_field": "redis_url",
      "fields": {
        "host": "redis_url",
        "port": "redis_url",
        "password": "redis_url"
      }
    }
  ],
  "peer_injection": [],
  "confidence": "high"
}
