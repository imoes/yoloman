{
 "console_num": {
  "description": "Virtual console number (1-63); if omitted, the currently active console is used",
  "type": "number"
 },
 "geometry": {
  "default": "",
  "description": "Console size in COLSxROWS format (e.g., '80x25'); required only if conspy fails to auto-detect",
  "type": "string"
 },
 "group": {
  "default": "tty",
  "description": "Group ownership for the service",
  "type": "string"
 },
 "restart_policy": {
  "default": "no",
  "description": "Restart behavior: 'no', 'on-failure', 'always', etc.",
  "enum": [
   "no",
   "always"
  ],
  "type": "string"
 },
 "user": {
  "default": "root",
  "description": "User account under which the service runs",
  "type": "string"
 },
 "viewonly": {
  "default": false,
  "description": "If true, run conspy in view-only mode (do not send keystrokes)",
  "type": "bool"
 }
}
