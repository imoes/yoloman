{
  "arj_virus_scan_command": {
    "type": "string",
    "default": "/usr/bin/scan",
    "description": "Command used to run the virus scanner for ARJ archives."
  },
  "arj_archive_create_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for creating ARJ archives (used with `arj a`)."
  },
  "arj_archive_extract_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for extracting ARJ archives (used with `arj x`)."
  },
  "zip_archive_create_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for creating ZIP archives."
  },
  "zip_archive_extract_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for extracting ZIP archives."
  },
  "rar_archive_create_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for creating RAR archives."
  },
  "rar_archive_extract_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for extracting RAR archives."
  },
  "tarZ_archive_create_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for creating tar.Z archives."
  },
  "tarZ_archive_extract_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for extracting tar.Z archives."
  },
  "tarGZ_archive_create_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for creating tar.gz archives."
  },
  "tarGZ_archive_extract_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for extracting tar.gz archives."
  },
  "tgz_archive_create_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for creating tgz archives (same as tar.gz)."
  },
  "tgz_archive_extract_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for extracting tgz archives."
  },
  "tarBZ2_archive_create_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for creating tar.bz2 archives."
  },
  "tarBZ2_archive_extract_pattern": {
    "type": "string",
    "default": "%s",
    "description": "Command-line pattern for extracting tar.bz2 archives."
  }
}
