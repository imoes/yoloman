{
  "DAEMON": {
    "type": "string",
    "default": "/usr/sbin/kea-dhcp-ddns",
    "description": "Path to the kea-dhcp-ddns daemon binary."
  },
  "DAEMONUSER": {
    "type": "string",
    "default": "_kea",
    "description": "System user under which the kea-dhcp-ddns daemon runs."
  },
  "KEA_PIDFILE_DIR": {
    "type": "string",
    "default": "/run/kea",
    "description": "Directory where the PID file for kea-dhcp-ddns is stored."
  },
  "KEA_LOCKFILE_DIR": {
    "type": "string",
    "default": "/run/lock/kea",
    "description": "Directory where lock files for kea-dhcp-ddns are stored."
  }
}
