{
  "project_name": {
    "type": "string",
    "default": "neutron",
    "description": "The OpenStack project name used for paths and service naming."
  },
  "config_file": {
    "type": "string",
    "default": "/etc/neutron/neutron_ovn_metadata_agent.ini",
    "description": "Path to the OVN metadata agent specific configuration file."
  },
  "system_user": {
    "type": "string",
    "default": "root",
    "description": "The user account under which the metadata agent daemon runs."
  },
  "system_group": {
    "type": "string",
    "default": "root",
    "description": "The group account under which the metadata agent daemon runs."
  }
}
