{
 "disable_local_auth": {
  "default": false,
  "description": "Set to true to disable local username/password authentication (forces PAM-only login).",
  "type": "bool"
 },
 "group_rules": {
  "default": [],
  "description": "Map of Unix group names → allowed RDP session types. Users in the group may launch sessions of the listed types.",
  "type": "list"
 },
 "listen_address": {
  "default": "0.0.0.0",
  "description": "IP address the XRDP server listens on (e.g., 0.0.0.0 for all, or 127.0.0.1 for localhost-only).",
  "type": "string"
 },
 "log_level": {
  "default": 0,
  "description": "Verbosity of logs: 0=none, 1=error, 2=warn, 3=info, 4=debug.",
  "type": "number"
 },
 "log_path": {
  "default": "/var/log/xrdp.log",
  "description": "Absolute path to the main XRDP log file.",
  "type": "string"
 },
 "max_sessions": {
  "default": 10,
  "description": "Maximum number of concurrent RDP sessions allowed.",
  "type": "number"
 },
 "modules": {
  "default": [],
  "description": "List of RDP modules (channels) to enable. Each object defines a module configuration (e.g., 'Xvnc', 'Xorg').",
  "type": "list"
 },
 "rdp_port": {
  "default": 3389,
  "description": "TCP port used for RDP connections.",
  "type": "number"
 },
 "sesman_options": {
  "default": "",
  "description": "Command-line options passed to the sesman process (e.g., '-v' for verbose). Empty by default.",
  "type": "string"
 },
 "sesman_start": {
  "default": "yes",
  "description": "Set to 'yes' to start sesman, anything else to disable it. Note: this setting is not honored by systemd service files.",
  "type": "string"
 },
 "session_timeout": {
  "default": 60,
  "description": "Idle session timeout in seconds (0 = no timeout).",
  "type": "number"
 },
 "tls_enabled": {
  "default": false,
  "description": "Enable TLS encryption for RDP sessions (requires valid keys in /etc/xrdp/rsakeys.txt).",
  "type": "bool"
 },
 "tls_level": {
  "default": "high",
  "description": "TLS encryption strength: low (RC4), medium (3DES), high (AES). Only used if TLS is enabled.",
  "type": "string"
 },
 "use_pam": {
  "default": true,
  "description": "Use PAM for authentication (required for most Linux login integrations like LDAP, Kerberos).",
  "type": "bool"
 },
 "xrdp_options": {
  "default": "",
  "description": "Command-line options passed to the xrdp process. Empty by default.",
  "type": "string"
 }
}
