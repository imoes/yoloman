{
  "enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable or disable the libravatar plugin"
  },
  "default": {
    "type": "string",
    "default": "mm",
    "description": "Default avatar fallback method: '404', 'mm', 'identicon', 'monsterid', 'wavatar', 'retro', 'blank', or 'url'"
  },
  "custom_url": {
    "type": "string",
    "default": "",
    "description": "Custom fallback URL if default='url' is selected"
  },
  "use_gravatar": {
    "type": "bool",
    "default": true,
    "description": "Use Gravatar as fallback when libravatar.org returns no image"
  },
  "cache_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable caching of retrieved images"
  },
  "cache_dir": {
    "type": "string",
    "default": "~/.claws-cache/libravatar",
    "description": "Cache directory for retrieved avatar images"
  },
  "cache_expiration": {
    "type": "number",
    "default": 604800,
    "description": "Cache expiration time in seconds (default: 7 days)"
  }
}
