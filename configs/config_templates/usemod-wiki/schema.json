{
  "usemod_wiki_alias_path": {
    "type": "string",
    "default": "/usemod-wiki/",
    "description": "The URL path alias for the UseModWiki images directory."
  },
  "usemod_wiki_document_root": {
    "type": "string",
    "default": "/usr/share/usemod-wiki",
    "description": "The filesystem path to the UseModWiki shared files (images, etc)."
  }
}
