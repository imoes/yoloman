{
  "PROJECT_NAME": {
    "type": "string",
    "default": "neutron",
    "description": "The name of the OpenStack project, used to construct names and paths."
  },
  "SYSTEM_USER": {
    "type": "string",
    "default": "neutron",
    "description": "The system user to run the daemon as."
  },
  "SYSTEM_GROUP": {
    "type": "string",
    "default": "neutron",
    "description": "The system group to run the daemon as."
  }
}
