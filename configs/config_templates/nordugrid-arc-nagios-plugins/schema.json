{
  "variable.service_suffix": {
    "type": "string",
    "default": "",
    "description": "Suffix appended to all passive service names."
  },
  "variable.local_hostname": {
    "type": "string",
    "default": "hostname",
    "description": "The host name of the Nagios server."
  }
}
