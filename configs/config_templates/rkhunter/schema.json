{
  "installdir": {
    "type": "string",
    "default": "/usr",
    "description": "Base installation prefix of rkhunter (parent of bin/, share/, etc.)"
  },
  "NICE": {
    "type": "number",
    "default": 0,
    "description": "Nice level for the rkhunter cron job"
  },
  "RUN_CHECK_ON_BATTERY": {
    "type": "string",
    "default": "false",
    "description": "Whether to run rkhunter when on battery power (true/false)"
  },
  "CRON_DAILY_RUN": {
    "type": "string",
    "default": "no",
    "description": "Enable daily cron job execution (yes/no)"
  },
  "REPORT_EMAIL": {
    "type": "string",
    "default": "",
    "description": "Email address to receive daily rkhunter warning reports"
  }
}
