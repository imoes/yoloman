{
  "compute_driver": {
    "type": "string",
    "default": "libvirt.LibvirtDriver",
    "description": "The compute driver to use for KVM instances."
  },
  "neutron_ovs_bridge": {
    "type": "string",
    "default": "br-int",
    "description": "The name of the Open vSwitch bridge used for Neutron networking."
  },
  "virt_type": {
    "type": "string",
    "default": "kvm",
    "description": "The type of virtualization to use (e.g., kvm, qemu)."
  },
  "vif_driver": {
    "type": "string",
    "default": "nova.virt.libvirt.vif.LibvirtGenericVIFDriver",
    "description": "The VIF (Virtual Interface) driver to use for connecting instances to the network."
  },
  "use_virtio_for_bridges": {
    "type": "bool",
    "default": true,
    "description": "Whether to use virtio model for network bridges."
  }
}
