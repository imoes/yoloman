{
  "case_insensitive": {
    "type": "bool",
    "default": true,
    "description": "Enable case-insensitive matching of directory names (equivalent to passing '-si' to bd)"
  },
  "search_paths": {
    "type": "list",
    "default": [],
    "description": "Additional colon-separated paths where bd should start searching upward for parent directories"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose diagnostics (print matched paths to stderr)"
  }
}
