{
 "chardev_path": {
  "default": "/dev/tpm0",
  "description": "Path to the character device for chardev interface.",
  "type": "string"
 },
 "ctrl_config": {
  "default": "type=unixio,path=/run/swtpm/swtpm-tpm0",
  "description": "Control channel configuration string. Format: type=[unixio|tcp][,path=<path>][,port=<port>][,bindaddr=<addr>][,ifname=<if>].",
  "type": "string"
 },
 "cuse_maj": {
  "default": 10,
  "description": "Major number for the CUSE device.",
  "type": "number"
 },
 "cuse_min": {
  "default": 226,
  "description": "Minor number for the CUSE device.",
  "type": "number"
 },
 "cuse_name": {
  "default": "swtpm",
  "description": "Name for the CUSE device created in /dev.",
  "type": "string"
 },
 "daemonize": {
  "default": false,
  "description": "Whether to daemonize the swtpm process.",
  "type": "bool"
 },
 "flags": {
  "description": "Comma-separated flags: not-need-init, startup-clear, startup-state, startup-deactivated, startup-none.",
  "enum": [
   "not-need-init",
   "startup-clear",
   "startup-state",
   "startup-deactivated",
   "startup-none"
  ],
  "type": "string"
 },
 "interface": {
  "default": "socket",
  "description": "The interface type to use: socket, chardev, or cuse.",
  "enum": [
   "socket",
   "chardev",
   "cuse"
  ],
  "type": "string"
 },
 "locality": {
  "description": "Locality settings: reject-locality-4, allow-set-locality.",
  "enum": [
   "reject-locality-4",
   "allow-set-locality"
  ],
  "type": "string"
 },
 "log_file": {
  "default": "-",
  "description": "Path to the log file. Use '-' to suppress logging.",
  "type": "string"
 },
 "log_level": {
  "default": 0,
  "description": "Logging level. 5 enables libtpms debug logging.",
  "type": "number"
 },
 "log_prefix": {
  "default": "",
  "description": "Prefix for all log lines.",
  "type": "string"
 },
 "log_truncate": {
  "default": false,
  "description": "Truncate the log file at startup.",
  "type": "bool"
 },
 "pid_file": {
  "default": "/run/swtpm/swtpm-tpm0.pid",
  "description": "Path to the PID file.",
  "type": "string"
 },
 "runas": {
  "default": "",
  "description": "User to switch to after startup (requires root).",
  "type": "string"
 },
 "seccomp_action": {
  "default": "kill",
  "description": "Seccomp action on syscall violation: none, log, or kill.",
  "enum": [
   "none",
   "log",
   "kill"
  ],
  "type": "string"
 },
 "server_config": {
  "default": "type=tcp,bindaddr=127.0.0.1,disconnect",
  "description": "Server configuration string for socket interface. Format: type=tcp[,port=<port>][,bindaddr=<addr>][,ifname=<if>][,fd=<fd>][,disconnect].",
  "type": "string"
 },
 "socket_port": {
  "default": 2321,
  "description": "TCP port for the socket interface (if not using server config).",
  "type": "number"
 },
 "tpm_version": {
  "default": "2.0",
  "description": "TPM version to emulate: 1.2 or 2.0.",
  "enum": [
   "1.2",
   "2.0"
  ],
  "type": "string"
 },
 "tpmstate_dir": {
  "default": "/var/lib/swtpm",
  "description": "Directory for TPM persistent state storage.",
  "type": "string"
 },
 "tpmstate_dir_mode": {
  "default": "0640",
  "description": "File mode bits for the TPM state directory (octal string).",
  "type": "string"
 },
 "vtpm_proxy": {
  "default": false,
  "description": "Create a Linux vTPM proxy device instance.",
  "type": "bool"
 }
}
