{
  "DAEMON_OPTS": {
    "type": "string",
    "default": "",
    "description": "Additional options passed to the gammu-smsd daemon at startup."
  },
  "USER": {
    "type": "string",
    "default": "gammu",
    "description": "System user account under which the gammu-smsd service runs."
  }
}
