{
  "DESC": {
    "type": "string",
    "default": "OpenStack Aodh API (aodh-api)",
    "description": "Human-readable description of the service."
  },
  "PROJECT_NAME": {
    "type": "string",
    "default": "aodh",
    "description": "OpenStack project name (used for paths, users, config, etc.)."
  },
  "UWSGI_PORT": {
    "type": "number",
    "default": 8042,
    "description": "TCP port for uWSGI to bind to."
  },
  "UWSGI_INI_PATH": {
    "type": "string",
    "default": "/etc/aodh/aodh-api-uwsgi.ini",
    "description": "Path to the uWSGI configuration INI file."
  }
}
