{
  "config_filename": {
    "type": "string",
    "default": "50xcite.el",
    "description": "The filename used for the Emacs startup file under /etc/emacs/site-start.d/"
  },
  "package_name": {
    "type": "string",
    "default": "xcite",
    "description": "The name of the Debian package, used to construct paths and in comments"
  }
}
