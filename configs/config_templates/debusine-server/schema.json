{
  "socket_path": {
    "type": "string",
    "default": "/var/lib/debusine/server/daphne.sock",
    "description": "Path to the Unix socket used by daphne for upstream proxying"
  },
  "client_max_body_size": {
    "type": "string",
    "default": "100M",
    "description": "Maximum allowed size of client request body"
  },
  "static_root": {
    "type": "string",
    "default": "/var/lib/debusine/server/static/",
    "description": "Filesystem path to serve static assets from"
  }
}
