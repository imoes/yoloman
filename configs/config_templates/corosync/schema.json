{
  "cluster_name": {"type": "string", "description": "Proxmox/corosync cluster name"},
  "config_version": {"type": "number", "default": 1, "description": "Bump on every change; corosync reloads only a higher version"},
  "nodes": {
    "type": "list",
    "description": "Cluster members: [{name, nodeid, ring0_addr, ring1_addr?, quorum_votes?}]. ring0_addr is the corosync link IP."
  }
}
