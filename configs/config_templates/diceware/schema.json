{
  "diceware": {
    "type": "object",
    "default": null,
    "description": "Top-level section for diceware configuration (in diceware.ini, named [diceware]). All keys below are optional and have defaults per the man page."
  }
}
