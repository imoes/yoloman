{
  "document_root": {
    "type": "string",
    "default": "/var/www/html",
    "description": "Directory where absolute image filenames (starting with '/') may be found."
  },
  "no_overwrite": {
    "type": "bool",
    "default": false,
    "description": "If true, prevents overwriting existing HEIGHT and WIDTH attributes in IMG tags."
  }
}
