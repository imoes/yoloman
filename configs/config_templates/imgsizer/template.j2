# Directory where absolute image filenames (starting with '/') may be found.
--document-root {{ document_root | default('/var/www/html') }}

# If true, prevents overwriting existing HEIGHT and WIDTH attributes in IMG tags.
{% if no_overwrite | default(False) %}--no-overwrite{% endif %}