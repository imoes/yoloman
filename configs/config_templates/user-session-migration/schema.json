{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable verbose output for debugging configuration issues."
  },
  "dry_run": {
    "type": "bool",
    "default": false,
    "description": "Whether to run the migration without launching scripts or marking them as migrated."
  },
  "script_file": {
    "type": "string",
    "default": "",
    "description": "Path to a specific script file to consider, ignoring the XDG_DATA_DIR/user-session-migration/scripts directory."
  }
}
