{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether ddrutility is enabled in the current environment; controls whether to emit active configuration vs comments."
  },
  "version": {
    "type": "string",
    "default": "2.8",
    "description": "Package version string (e.g., '2.8') as reported by --version."
  },
  "help_flag": {
    "type": "bool",
    "default": false,
    "description": "If true, include placeholder note about invoking --help for subcommands."
  },
  "version_flag": {
    "type": "bool",
    "default": false,
    "description": "If true, include placeholder note about invoking --version for subcommands."
  },
  "email": {
    "type": "string",
    "default": "maximus57@hotmail.com",
    "description": "Bug-report email address from manpage."
  },
  "homepage": {
    "type": "string",
    "default": "http://sourceforge.net/projects/ddrutility/",
    "description": "Project homepage URL from manpage."
  }
}
