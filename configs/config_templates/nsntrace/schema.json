{
  "device": {
    "type": "string",
    "default": "",
    "description": "The network device to use in trace."
  },
  "use_public_dns": {
    "type": "bool",
    "default": false,
    "description": "Override resolv.conf in namespace to use public nameservers."
  },
  "outfile": {
    "type": "string",
    "default": "nsntrace.pcap",
    "description": "Write the trace output to this file."
  },
  "user": {
    "type": "string",
    "default": "",
    "description": "Run program with the user ID, group ID and supplementary groups of this user."
  },
  "filter": {
    "type": "string",
    "default": "",
    "description": "The capture filter to use while capturing (pcap-filter syntax)."
  },
  "target_program": {
    "type": "string",
    "default": "",
    "description": "The program to launch and trace inside the network namespace."
  },
  "target_args": {
    "type": "list",
    "default": [],
    "description": "Arguments to pass to the target program. Items are strings."
  }
}
