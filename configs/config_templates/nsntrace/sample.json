{
  "device": "eth0",
  "use_public_dns": true,
  "outfile": "/var/log/traces/session1.pcap",
  "user": "capturer",
  "filter": "port 80 or port 443",
  "target_program": "curl",
  "target_args": [
    "-s",
    "https://example.com"
  ]
}
