{
  "PREFIX_STR": {
    "type": "string",
    "default": "/",
    "description": "Prefix string for the FastCGI SCRIPT_NAME variable; typically corresponds to the URL path under which the web UI is served.",
    "enum": [
      "/",
      "/our-ipv6-network/",
      "/bosixnet/"
    ]
  },
  "LOG_DIR": {
    "type": "string",
    "default": "/var/tmp/bosixnet",
    "description": "Directory where auxiliary files will be stored."
  },
  "USERID": {
    "type": "string",
    "default": "bosixnet",
    "description": "User under which the bosixnet-webui process will run."
  },
  "GROUPID": {
    "type": "string",
    "default": "bosixnet",
    "description": "Group under which the bosixnet-webui process will run."
  },
  "FCGIPORT": {
    "type": "number",
    "default": 33169,
    "description": "TCP port on localhost to which to bind for FastCGI."
  }
}
