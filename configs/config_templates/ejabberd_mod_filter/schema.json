{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable or disable the ejabberd_mod_filter module globally."
  },
  "rules": {
    "type": "list",
    "default": [],
    "description": "List of packet filtering rules. Each rule defines how to handle matching packets using access control or custom filters."
  },
  "removed": {
    "type": "list",
    "default": [],
    "description": "List of variables removed due to lack of documentation support."
  }
}
