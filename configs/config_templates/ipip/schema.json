{
  "flags": {
    "type": "string",
    "default": "defaults 50",
    "description": "Default flags passed to the ipip daemon (e.g., 'defaults 50')."
  },
  "start_flags": {
    "type": "string",
    "default": "",
    "description": "Additional arguments passed specifically when starting the daemon."
  }
}
