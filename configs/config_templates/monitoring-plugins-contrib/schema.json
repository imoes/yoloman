{
  "check_multi_config_dir": {
    "type": "string",
    "default": "/etc/check_multi",
    "description": "Directory containing check_multi command files."
  },
  "check_apt_command_path": {
    "type": "string",
    "default": "/usr/lib/nagios/plugins/check_statusfile /var/cache/nagios_status/apt",
    "description": "Path to the check_apt replacement command (usually check_statusfile with apt status)."
  },
  "check_statusfile_path": {
    "type": "string",
    "default": "/usr/lib/nagios/plugins/check_statusfile",
    "description": "Path to the check_statusfile plugin binary."
  },
  "check_libs_statusfile_path": {
    "type": "string",
    "default": "/var/cache/nagios_status/check_libs",
    "description": "Path to the statusfile for check_libs."
  },
  "check_running_kernel_path": {
    "type": "string",
    "default": "/usr/lib/nagios/plugins/check_running_kernel",
    "description": "Path to the check_running_kernel plugin binary."
  }
}
