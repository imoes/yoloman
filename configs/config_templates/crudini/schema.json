{
  "config_sections": {
    "type": "list",
    "default": [],
    "description": "List of config sections; each has a name (string) and list of parameters (list of objects with name and value). Use empty string or \"DEFAULT\" for global parameters. Parameters must be flat name-value pairs. Values are strings; lists are stored as space/comma-separated strings per INI conventions. No nested objects allowed in defaults or samples \u2014 only flat name=value pairs per section."
  }
}
