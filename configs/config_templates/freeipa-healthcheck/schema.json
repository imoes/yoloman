{
  "report_dir": {
    "type": "string",
    "default": "/var/lib/ipa/healthcheck",
    "description": "The directory where FreeIPA health check reports are stored."
  },
  "rp_user": {
    "type": "string",
    "default": "apache",
    "description": "The user running the health checks."
  },
  "rp_group": {
    "type": "string",
    "default": "apache",
    "description": "The group running the health checks."
  },
  "timeout": {
    "type": "number",
    "default": 30,
    "description": "The timeout for individual health check functions in seconds."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Whether to run health checks in debug mode."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/ipa/healthcheck.log",
    "description": "The log file path."
  },
  "log_level": {
    "type": "string",
    "default": "WARNING",
    "description": "The log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)."
  }
}
