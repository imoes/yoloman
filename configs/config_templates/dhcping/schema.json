{
  "hosts": {
    "type": "list",
    "default": [],
    "description": "List of monitored hosts for DHCP testing; each host requires name, MAC, and IP address matching the dhcpd.conf syntax used for static reservations. Used by dhcping(8) to verify server responsiveness via DHCPREQUEST/DHCPINFORM/DHCPRELEASE cycles. See 'SETUP' in dhcping(8)."
  }
}
