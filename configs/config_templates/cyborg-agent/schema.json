{
  "project_name": {
    "type": "string",
    "default": "cyborg",
    "description": "OpenStack project name; used for directories, config paths, and system user/group"
  },
  "rabbitmq_process_name": {
    "type": "string",
    "default": "cyborg-agent",
    "description": "Process name for RabbitMQ messaging; must match expected value in OpenStack config"
  }
}
