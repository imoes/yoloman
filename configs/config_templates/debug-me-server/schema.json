{
  "debug_me_from_email": {
    "type": "string",
    "default": "postmaster",
    "description": "The email address that debug-me uses when emailing logs to users."
  },
  "debug_me_log_dir": {
    "type": "string",
    "default": "/var/log/debug-me/",
    "description": "The directory where debug-me stores logs."
  }
}
