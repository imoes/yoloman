{
  "load_plugin": {
    "type": "bool",
    "default": true,
    "description": "Whether to load the python.so plugin at Claws Mail startup."
  },
  "script_dir": {
    "type": "string",
    "default": "",
    "description": "Absolute or relative path to the directory containing Python scripts for the plugin."
  },
  "console_autostart": {
    "type": "bool",
    "default": false,
    "description": "Whether to automatically open the Python console on startup for interactive testing."
  }
}
