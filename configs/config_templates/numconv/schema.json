{
  "numconv.input_system": {
    "type": "string",
    "default": "Western_Lower",
    "description": "The number system of the input data.",
    "enum": [
      "all",
      "Western_Lower",
      "Roman",
      "Mandarin_Regular_Simplified",
      "Devanagari",
      "Perso_Arabic"
    ]
  },
  "numconv.output_system": {
    "type": "string",
    "default": "Western_Lower",
    "description": "The number system of the output data.",
    "enum": [
      "all",
      "Western_Lower",
      "Roman",
      "Mandarin_Regular_Simplified",
      "Devanagari",
      "Perso_Arabic"
    ]
  },
  "numconv.input_base": {
    "type": "number",
    "default": 10,
    "description": "Input base for Western numbers, 1-36."
  },
  "numconv.output_base": {
    "type": "number",
    "default": 10,
    "description": "Output base for Western numbers, 1-36."
  },
  "numconv.group_size": {
    "type": "number",
    "default": 3,
    "description": "Size of general digit groups (e.g., thousands)."
  },
  "numconv.low_group_size": {
    "type": "number",
    "default": 3,
    "description": "Size of the low-order digit group."
  },
  "numconv.group_separator": {
    "type": "string",
    "default": ",",
    "description": "Character used as the thousands separator."
  },
  "numconv.roman_superscript": {
    "type": "bool",
    "default": false,
    "description": "Use superscript macron for Roman numerals > 1000."
  },
  "numconv.use_locale": {
    "type": "bool",
    "default": false,
    "description": "Apply locale settings for grouping parameters."
  },
  "numconv.input_file": {
    "type": "string",
    "default": "",
    "description": "Path to input file. Empty means stdin."
  },
  "numconv.output_file": {
    "type": "string",
    "default": "",
    "description": "Path to output file. Empty means stdout."
  }
}
