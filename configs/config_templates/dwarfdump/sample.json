{
  "lines": [
    "",
    "# Suppress URI interpretation for command-line parsing",
    "--format-suppress-uri",
    "",
    "# Limit DIE output to 100 compilation units",
    "--format-limit=100",
    "",
    "# Use generic 500-register ABI for frame dumps",
    "-x abi=generic500",
    "",
    "# Enable verbose output for DIE and frame sections",
    "-vvvv",
    "",
    "# Use dense output (one DIE per line)",
    "--format-dense",
    "",
    "# Omit section offsets in printed output",
    "--format-suppress-offsets",
    ""
  ]
}
