{
  "lines": {
    "type": "list",
    "default": [],
    "description": "List of configuration lines to include in dwarfdump.conf; each line is a raw option string (e.g., '--format-limit=10' or '-H10') as accepted by dwarfdump(1). Empty lines are preserved as-is and do not affect parsing. No validation of option syntax is performed by dwarfdump itself \u2014 invalid options cause warnings or errors at runtime."
  }
}
