{
  "database_path": {
    "type": "string",
    "default": "/etc/membernator/members.csv",
    "description": "Path to the CSV database containing membership records."
  },
  "id_col": {
    "type": "string",
    "default": "ID",
    "description": "Column name in the CSV database for the member ID."
  },
  "name_col": {
    "type": "string",
    "default": "NAME",
    "description": "Column name in the CSV database for the member name."
  },
  "scan_delay": {
    "type": "number",
    "default": 2.5,
    "description": "Delay in seconds between scans."
  },
  "width": {
    "type": "number",
    "default": 800,
    "description": "Window width in pixels. Use 0 for fullscreen."
  },
  "height": {
    "type": "number",
    "default": 480,
    "description": "Window height in pixels. Use 0 for fullscreen."
  },
  "logfile_path": {
    "type": "string",
    "default": "/var/log/membernator/log.csv",
    "description": "Path to the logfile for scan results."
  }
}
