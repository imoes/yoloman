{
  "OPTS": {
    "type": "string",
    "default": "-d2",
    "description": "Command-line options passed to the ipa-client-install script; typically used to set debug level or other install-time flags."
  }
}
