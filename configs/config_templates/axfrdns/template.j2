# AXFRDNS rules file — defines access control for zone transfers
# Format: one rule per line: IP/PREFIX allow|deny[:tag]
# Empty lines and lines starting with # are ignored

{% for rule in rules %}{{ rule.cidr }} {{ rule.action }}{% if rule.tag is defined %}:{{ rule.tag }}{% endif %}{% endfor %}
