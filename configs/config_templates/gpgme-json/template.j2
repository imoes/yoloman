{
  # Service name identifier (e.g., 'gpgmejson')
  "name": "{{ name | default('gpgmejson') }}",
  # Human-readable description of the service
  "description": "{{ description | default('JavaScript binding for GnuPG') }}",
  # Absolute path to the gpgme-json executable
  "path": "{{ path | default('/usr/bin/gpgme-json') }}",
  # Communication type (e.g., 'stdio')
  "type": "{{ type | default('stdio') }}",
  # WebExtension origin allowed to communicate with the service
  # Note: This is a list, but typically contains a single origin string.
  "allowed_origins": ["{{ allowed_origins | default('chrome-extension://kajibbejlbohfaggdiogboambcijhkke/') }}"]
}