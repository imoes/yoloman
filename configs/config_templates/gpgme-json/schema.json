{
  "name": {
    "type": "string",
    "default": "gpgmejson",
    "description": "Service name identifier"
  },
  "description": {
    "type": "string",
    "default": "JavaScript binding for GnuPG",
    "description": "Human-readable description of the service"
  },
  "path": {
    "type": "string",
    "default": "/usr/bin/gpgme-json",
    "description": "Absolute path to the gpgme-json executable"
  },
  "type": {
    "type": "string",
    "default": "stdio",
    "description": "Communication type (e.g., stdio)"
  },
  "allowed_origins": {
    "type": "string",
    "default": "chrome-extension://kajibbejlbohfaggdiogboambcijhkke/",
    "description": "WebExtension origin allowed to communicate with the service"
  }
}
