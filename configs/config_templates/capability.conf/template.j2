# /etc/security/capability.conf — PAM capability configuration file
# Maps users or groups to Linux capabilities for login sessions
# Format: one entry per line: <selector> <capability-list>
# Selectors: user <username>, group <groupname>, @all

{% for entry in entries %}{{ entry.selector }} {{ entry.capabilities | join(' ') }}{% endfor %}