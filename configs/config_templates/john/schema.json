{
  "cron_start_time": {
    "type": "string",
    "default": "00 1",
    "description": "Cron schedule expression (hour and minute) for starting the john password cracking cronjob."
  },
  "cron_stop_time": {
    "type": "string",
    "default": "00 7",
    "description": "Cron schedule expression (hour and minute) for stopping the john password cracking cronjob."
  }
}
