{
  "directory": {
    "type": "string",
    "default": "/var/log",
    "description": "The target directory on a mounted filesystem to collect file metadata from. This corresponds to the <DIRECTORY> argument in mac-robber synopsis."
  }
}
