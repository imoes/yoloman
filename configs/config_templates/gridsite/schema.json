{
  "gridsite_document_root": {
    "type": "string",
    "default": "/var/lib/gridsite/",
    "description": "Document root directory for GridSite content."
  },
  "gridsite_header_file": {
    "type": "string",
    "default": "gridsitehead.txt",
    "description": "Filename (or absolute path) for the standard HTML header file."
  },
  "gridsite_footer_file": {
    "type": "string",
    "default": "gridsitefoot.txt",
    "description": "Filename (or absolute path) for the standard HTML footer file."
  },
  "gridsite_gsi_proxy_limit": {
    "type": "number",
    "default": 9,
    "description": "Maximum number of GSI proxies allowed in delegation chain (0 disables proxy support)."
  }
}
