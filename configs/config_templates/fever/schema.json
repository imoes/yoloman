{
  "fever_args": {
    "type": "string",
    "default": "-n 0 --logfile /var/log/fever.log",
    "description": "Command-line arguments for the fever daemon, including number of workers and log file path."
  }
}
