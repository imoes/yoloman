{
  "slay_mode": {
    "type": "string",
    "default": "mean",
    "description": "The operation mode for slay. 'mean' enforces privilege checks for non-root users. 'nice' disables these checks. Valid values: mean, nice.",
    "enum": [
      "mean",
      "nice"
    ]
  },
  "slay_message_style": {
    "type": "string",
    "default": "normal",
    "description": "The style of messages displayed by slay. 'butthead' uses humorous messages. 'normal' uses standard messages. Valid values: butthead, normal.",
    "enum": [
      "butthead",
      "normal"
    ]
  }
}
