# Default configuration for mosquitto_rr, mosquitto_sub, mosquitto_pub, mosquitto_ctrl
# These client tools use optional command-line arguments; no persistent config file exists.
# The packages 'mosquitto-clients' provide only CLI tools, not a server or per-instance config.
# No Jinja2 template is applicable — this package does not install a config file.
# To customize behavior, pass arguments directly (e.g., --port, --id, -h) in scripts or wrappers.
