{
  "vit_task_data_dir": {
    "type": "string",
    "default": "~/.task",
    "description": "The path to the Taskwarrior data directory."
  },
  "vit_audit_enabled": {
    "type": "bool",
    "default": false,
    "description": "When true, enables the -audit switch to print task commands to stderr."
  },
  "vit_titlebar_enabled": {
    "type": "bool",
    "default": true,
    "description": "When true, enables the -titlebar switch to set the xterm titlebar."
  }
}
