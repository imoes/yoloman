{
 "config": {
  "description": "Root key for configuration object",
  "type": "object"
 },
 "config.channels": {
  "default": [],
  "description": "List of channels to join upon connecting to IRC",
  "type": "list"
 },
 "config.identify": {
  "default": false,
  "description": "Whether to attempt identification with Nickserv",
  "type": "bool"
 },
 "config.listen": {
  "default": "0.0.0.0",
  "description": "Network interface address to listen on",
  "type": "string"
 },
 "config.nick": {
  "default": "nadoka",
  "description": "Nickname to use on IRC",
  "type": "string"
 },
 "config.password": {
  "default": "",
  "description": "Password for client authentication",
  "type": "string"
 },
 "config.port": {
  "default": 6667,
  "description": "Port number for client connections",
  "type": "number"
 },
 "config.realname": {
  "default": "nadoka",
  "description": "Real name / user info for the IRC bot",
  "type": "string"
 },
 "config.server.host": {
  "default": "irc.libera.chat",
  "description": "Hostname of the IRC server to connect to",
  "type": "string"
 },
 "config.server.port": {
  "default": 6667,
  "description": "Port number of the IRC server",
  "type": "number"
 },
 "config.server.ssl": {
  "default": false,
  "description": "Whether to use SSL/TLS for the IRC connection",
  "type": "bool"
 },
 "config.username": {
  "default": "",
  "description": "Username for Nickserv identification",
  "type": "string"
 }
}
