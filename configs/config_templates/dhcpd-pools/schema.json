{
  "config_path": {
    "type": "string",
    "default": "/etc/dhcp/dhcpd.conf",
    "description": "Path to the ISC dhcpd configuration file"
  },
  "leases_path": {
    "type": "string",
    "default": "/var/lib/dhcp/dhcpd.leases",
    "description": "Path to the ISC dhcpd lease file"
  },
  "format": {
    "type": "string",
    "default": "t",
    "description": "Output format: t (text), H (html), c (csv), x (xml), X (extended xml), j (json), J (extended json)",
    "enum": [
      "t",
      "H",
      "c",
      "x",
      "X",
      "j",
      "J"
    ]
  },
  "output_path": {
    "type": "string",
    "default": "",
    "description": "File path for output; empty or absent means stdout"
  },
  "limit": {
    "type": "string",
    "default": "77",
    "description": "Limit output using chmod-like string (e.g., '11', '66'); first digit = headers, second = data",
    "enum": [
      "01",
      "02",
      "04",
      "10",
      "11",
      "14",
      "20",
      "22",
      "32",
      "34",
      "40",
      "50",
      "57",
      "66",
      "70",
      "75",
      "77"
    ]
  },
  "sort": {
    "type": "string",
    "default": "",
    "description": "Sort keys: n (name), i (first ip), m (max), c (cur), p (percent), t (touch), T (t+c), e (t+c perc)",
    "enum": [
      "i",
      "c",
      "t",
      "T",
      "e"
    ]
  },
  "reverse": {
    "type": "bool",
    "default": false,
    "description": "Sort results in reverse order"
  },
  "warning": {
    "type": "number",
    "default": 80,
    "description": "Percentage threshold for warning alarm (supports fractional values like 75.5)"
  },
  "critical": {
    "type": "number",
    "default": 90,
    "description": "Percentage threshold for critical alarm (supports fractional values)"
  },
  "warn_count": {
    "type": "number",
    "default": 4294967296,
    "description": "Number of free leases before warning alarm (2^32 = default IPv4 space)"
  },
  "crit_count": {
    "type": "number",
    "default": 4294967296,
    "description": "Number of free leases before critical alarm"
  },
  "snet_alarms": {
    "type": "bool",
    "default": false,
    "description": "Suppress range alarms that are part of shared networks"
  },
  "minsize": {
    "type": "number",
    "default": 0,
    "description": "Minimum pool size to consider (ignore pools \u2264 this size in alarming mode)"
  },
  "perfdata": {
    "type": "bool",
    "default": false,
    "description": "Print performance data in alarming mode"
  },
  "ip_version": {
    "type": "string",
    "default": "",
    "description": "Force parsing mode: '4' (IPv4) or '6' (IPv6); leave empty for auto",
    "enum": [
      "4",
      "6"
    ]
  },
  "all_as_shared": {
    "type": "bool",
    "default": false,
    "description": "Treat standalone subnets as shared networks"
  }
}
