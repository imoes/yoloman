{# GnuPG Verification (gpgv) configuration #}
{# This package configures the command-line arguments for the gpgv utility. #}
{# gpgv is used to verify signatures of files using a specified keyring. #}
{# All options are passed via CLI arguments; there is no separate config file. #}

{# List of CLI arguments to pass to gpgv. Each item must be a single argument. #}
{# Examples: '--keyring /path/to/keyring', '-v'. Order is preserved. #}
{# If no arguments are provided, the default keyring and no verbose output are used. #}
{% for arg in (args | default([])) %}
{{ arg }}
{% endfor %}