{
 "group": {
  "default": "",
  "description": "Group name or GID to filter sockets by (-G flag).",
  "type": "string"
 },
 "ipv4_only": {
  "default": false,
  "description": "If true, restrict output to IPv4 sockets only (-4 flag).",
  "type": "bool"
 },
 "ipv6_only": {
  "default": false,
  "description": "If true, restrict output to IPv6 sockets only (-6 flag).",
  "type": "bool"
 },
 "ports": {
  "default": "",
  "description": "Comma-separated list of port numbers or ranges (e.g., '80,443,8000-9000') to filter Internet sockets by (-p flag).",
  "type": "string"
 },
 "process": {
  "default": "",
  "description": "Process name or PID to filter sockets by (-P flag).",
  "type": "string"
 },
 "protocol": {
  "description": "Filter sockets by protocol. Valid values: 'raw', 'tcp', 'udp', 'unix'. Empty string disables filtering (-R flag).",
  "enum": [
   "raw",
   "tcp",
   "udp",
   "unix"
  ],
  "type": "string"
 },
 "show_connected": {
  "default": false,
  "description": "If true, show only connected sockets (-c flag). Default behavior shows both.",
  "type": "bool"
 },
 "show_listening": {
  "default": false,
  "description": "If true, show only listening sockets (-l flag). Default behavior shows both.",
  "type": "bool"
 },
 "show_unix": {
  "default": true,
  "description": "If true, include Unix domain sockets in the output (-u flag).",
  "type": "bool"
 },
 "user": {
  "default": "",
  "description": "Username or UID to filter sockets by (-U flag).",
  "type": "string"
 }
}
