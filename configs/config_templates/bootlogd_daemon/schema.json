{
  "bootlogd_enable": {
    "type": "bool",
    "default": true,
    "description": "Enable the boot logging daemon to capture messages during system startup."
  },
  "bootlogd_logfile": {
    "type": "string",
    "default": "/var/log/boot",
    "description": "Path to the file where boot messages will be logged."
  },
  "bootlogd_log_to_syslog": {
    "type": "bool",
    "default": false,
    "description": "If enabled, also send boot messages to syslog (e.g., systemd-journald)."
  },
  "bootlogd_extra_opts": {
    "type": "list",
    "default": [],
    "description": "Additional command-line arguments passed directly to bootlogd (e.g., `-f` for forced overwrite, `-q` for quiet). Items are strings. No other valid options are documented for this service."
  }
}
