{
  "commands": [
    "belenios-tool election create --public-key /etc/belenios/public.pem --private-key /etc/belenios/private.pem --name 'Demo Election' --trustees 3",
    "belenios-tool setup template --output /var/lib/belenios/templates/demoelection.tar.gz"
  ]
}
