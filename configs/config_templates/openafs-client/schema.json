{
  "cache_dir": {
    "type": "string",
    "default": "/etc/openafs/cacheinfo",
    "description": "Path to the OpenAFS cache info configuration file."
  },
  "module_root": {
    "type": "string",
    "default": "/lib/modules/`uname -r`",
    "description": "Root directory for kernel modules."
  },
  "afsd_bin_path": {
    "type": "string",
    "default": "/usr/sbin/afsd",
    "description": "Path to the afsd binary executable."
  }
}
