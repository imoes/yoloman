{
  "desc": {
    "type": "string",
    "default": "yubihsm-connector",
    "description": "Description string for the service"
  },
  "name": {
    "type": "string",
    "default": "yubihsm-connector",
    "description": "Service name used for PID and log files"
  },
  "config_file": {
    "type": "string",
    "default": "/etc/yubihsm-connector.yaml",
    "description": "Path to the yubihsm-connector YAML configuration file"
  },
  "daemon_user": {
    "type": "string",
    "default": "yubihsm-connector",
    "description": "System user account to run the daemon as"
  },
  "stop_schedule_var": {
    "type": "string",
    "default": "STOP_SCHEDULE",
    "description": "Environment variable name holding the stop schedule, used in the default value fallback"
  },
  "stop_schedule_default": {
    "type": "string",
    "default": "/QUIT/5/TERM/5/KILL/5",
    "description": "Default stop schedule if the environment variable is not set"
  }
}
