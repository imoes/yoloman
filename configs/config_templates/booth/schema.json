{
  "run_arbitrator": {
    "type": "string",
    "default": "no",
    "description": "Whether to start the booth arbitrator at boot. Valid values are 'yes' or 'no'.\n      enum: ['no', 'yes']",
    "enum": [
      "no",
      "yes"
    ]
  }
}
