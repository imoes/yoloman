# snapserver configuration file
# This file sets command-line options for the snapserver daemon.
# For detailed usage, see snapserver(1) or the snapserver documentation.

# Command-line options for snapserver.
# Leave empty for defaults. If provided, these override built-in defaults.
# Example: '-f /var/run/snapserver.pid'
SNAPSERVER_OPTS="{{ snapserver_opts | default('') }}"