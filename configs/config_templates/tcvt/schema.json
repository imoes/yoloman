{
  "SHELL": {
    "type": "string",
    "default": "/bin/bash",
    "description": "The system shell path, used for the shebang line."
  },
  "CMD": {
    "type": "string",
    "default": "/bin/sh",
    "description": "The program and its arguments to execute within tcvt."
  },
  "COLUMNS": {
    "type": "number",
    "default": 2,
    "description": "The number of columns for the virtual terminal."
  },
  "REVERSE": {
    "type": "bool",
    "default": false,
    "description": "Whether to reverse the column order (last column left-most)."
  }
}
