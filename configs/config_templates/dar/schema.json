{
  "darrc_default_include_path": {
    "type": "string",
    "default": "/etc/darrc",
    "description": "Path to the system-wide default darrc config file to include."
  },
  "par2_dcf_path": {
    "type": "string",
    "default": "/usr/share/doc/dar/examples/dar_par.dcf",
    "description": "Path to the PAR2-related DCF config file."
  },
  "samba_dst_offset_hours": {
    "type": "number",
    "default": 1,
    "description": "Hour offset for DST correction on Samba filesystems."
  },
  "full_from_diff_regex_pattern": {
    "type": "string",
    "default": "'{!(~I)}[Rr] {~S}[O*] P* ; {~s}[*o] *p'",
    "description": "Regex pattern used by full-from-diff target to match reference and diff files."
  }
}
