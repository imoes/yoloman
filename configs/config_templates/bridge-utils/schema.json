{
  "BRIDGE_HOTPLUG": {
    "type": "bool",
    "default": false,
    "description": "Whether to automatically add ports to a bridge when they are hotplugged."
  },
  "BRIDGE_DISABLE_LINKLOCAL_IPV6_ALSO_PHYS": {
    "type": "bool",
    "default": true,
    "description": "Whether to disable link-local IPv6 on physical interfaces of VLAN ports."
  }
}
