{
  "foreground_color": {
    "type": "string",
    "default": "yellow",
    "description": "Foreground color for the main window."
  },
  "background_color": {
    "type": "string",
    "default": "blue4",
    "description": "Background color for the main window."
  },
  "help_foreground_color": {
    "type": "string",
    "default": "green",
    "description": "Foreground color for help text."
  },
  "help_background_color": {
    "type": "string",
    "default": "red4",
    "description": "Background color for help text."
  },
  "window_width": {
    "type": "number",
    "default": 600,
    "description": "Width of the XDu window in pixels."
  },
  "window_height": {
    "type": "number",
    "default": 480,
    "description": "Height of the XDu window in pixels."
  },
  "font_family": {
    "type": "string",
    "default": "-*-helvetica-bold-r-normal--14-*",
    "description": "X11 font family string for the application."
  },
  "number_of_columns": {
    "type": "number",
    "default": 5,
    "description": "Number of columns in the view."
  },
  "show_size": {
    "type": "bool",
    "default": true,
    "description": "Whether to show file sizes in the view."
  },
  "order_type": {
    "type": "string",
    "default": "first",
    "description": "Sorting order for files (first, last, alpha, ralpha, size, rsize).",
    "enum": [
      "first",
      "last",
      "alpha",
      "ralpha",
      "size",
      "rsize",
      "reverse"
    ]
  }
}
