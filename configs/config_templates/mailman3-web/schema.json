{
  "cron_user": {
    "type": "string",
    "default": "www-data",
    "description": "User account under which the cron jobs run."
  },
  "cron_lock_dir": {
    "type": "string",
    "default": "/var/run/mailman3-web",
    "description": "Directory used for storing cron job lock files to prevent overlapping execution."
  }
}
