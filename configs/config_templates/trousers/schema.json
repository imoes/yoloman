{
  "name": {
    "type": "string",
    "default": "tcsd",
    "description": "The name of the service/daemon script identifier."
  },
  "user": {
    "type": "string",
    "default": "tss",
    "description": "The system user account under which the tcsd daemon runs."
  }
}
