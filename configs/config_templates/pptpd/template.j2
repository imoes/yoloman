#
# PPTPD Configuration Template
# This fragment configures the pptpd daemon options.
# PPTPD (Point-to-Point Tunneling Protocol Daemon) is a VPN server.
# It does not expose database, cache, or messaging services.
#

# Additional command-line options passed to the pptpd daemon.
# Defaults to an empty string if not specified in the role variables.
DAEMON_OPTS='{{ daemon_opts | default("") }}'