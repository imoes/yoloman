{
  "document_root": {
    "type": "string",
    "default": "/usr/share/phpliteadmin",
    "description": "The absolute path to the phpLiteAdmin installation directory on the server filesystem."
  }
}
