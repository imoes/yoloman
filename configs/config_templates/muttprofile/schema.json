{
  "mutt_dir": {
    "type": "string",
    "default": "~/.mutt",
    "description": "The directory where Mutt profile files are stored."
  },
  "symlink_name": {
    "type": "string",
    "default": "profile.active",
    "description": "The filename for the symbolic link that points to the currently active profile."
  },
  "profile_regex": {
    "type": "string",
    "default": ".*.mutt",
    "description": "A Perl-compatible regular expression to filter which files in the mutt_dir are considered valid profiles."
  }
}
