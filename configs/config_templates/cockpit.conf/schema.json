{
 "login_banner": {
  "default": false,
  "description": "Show or hide the login banner. By default (per cockpit.conf(5)), no banner is displayed unless specified. Set to true to show it.",
  "type": "boolean"
 },
 "login_banner_text": {
  "default": "",
  "description": "Banner text shown on the login page. Empty string suppresses banner text. Not used unless Banner is true.",
  "type": "string"
 },
 "session_idle_timeout_minutes": {
  "default": null,
  "description": "Idle timeout in minutes before logging out due to inactivity. Set to 0 to disable. Note: IdleTimeout only applies to interactive password logins.",
  "type": "number"
 },
 "session_max_per_user": {
  "default": null,
  "description": "Maximum number of concurrent sessions per user. Set to 0 for unlimited. Not documented in cockpit.conf(5); may be handled via PAM or system limits.",
  "type": "number"
 },
 "session_recording_exclude_users": {
  "default": [],
  "description": "List of usernames whose sessions should NOT be recorded. By default, all users (including root) are recorded if session recording is enabled.",
  "type": "list"
 },
 "session_recording_path": {
  "default": "",
  "description": "Absolute directory path where session recordings will be stored (e.g., `/var/log/cockpit`). Leave empty or omit to disable recording.",
  "type": "string"
 },
 "session_timeout_minutes": {
  "default": null,
  "description": "Session idle timeout in minutes (key: IdleTimeout). 0 disables timeout. Not recommended for production.",
  "type": "number"
 },
 "ws_address": {
  "default": "*",
  "description": "IP address or hostname to bind the Cockpit web service to. Use `*` to listen on all network interfaces.",
  "type": "string"
 },
 "ws_allow_unencrypted": {
  "default": false,
  "description": "If true, cockpit will accept unencrypted HTTP connections. Otherwise, HTTP is redirected to HTTPS. Defaults to false. This controls HTTP/HTTPS behavior when TLS is enabled.",
  "type": "boolean"
 },
 "ws_cors_domains": {
  "default": [],
  "description": "List of origins permitted to make cross-origin requests to Cockpit. Leave empty or omit to disable CORS. The config key is `Origins` in section `[WebService]`.",
  "type": "list"
 },
 "ws_port": {
  "default": 9090,
  "description": "TCP port on which the Cockpit web UI listens. Note: Cockpit listens on port 9090 by default and this cannot be changed in cockpit.conf.",
  "type": "number"
 },
 "ws_tls": {
  "default": true,
  "description": "Enable TLS (HTTPS) encryption for Cockpit connections. TLS is implicitly enabled when TLSCert/TLSKey are configured. To allow HTTP fallback, use ws_allow_unencrypted=false.",
  "type": "boolean"
 },
 "ws_tls_cert": {
  "default": "/etc/cockpit/ws-certs.d/0-self-signed.cert",
  "description": "File path to the TLS certificate used for HTTPS. Must be readable by the `cockpit-ws` user.",
  "type": "string"
 },
 "ws_tls_key": {
  "default": "/etc/cockpit/ws-certs.d/0-self-signed.key",
  "description": "File path to the TLS private key. Must be secure and readable by `cockpit-ws`.",
  "type": "string"
 }
}
