{
  "verbose": {
    "type": "string",
    "default": "yes",
    "description": "Whether to enable verbose output when invoking the uWSGI Emperor init.d script.",
    "enum": [
      "yes",
      "no"
    ]
  }
}
