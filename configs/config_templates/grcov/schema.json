{
  "paths": {
    "type": "list",
    "default": [],
    "description": "List of input paths (files/directories) to analyze for coverage data."
  },
  "output_types": {
    "type": "list",
    "default": [
      "lcov"
    ],
    "description": "List of output formats. Supported: html, coveralls, lcov, covdir, coveralls+, ade, files, markdown, cobertura, cobertura-pretty."
  },
  "output_path": {
    "type": "string",
    "default": "",
    "description": "Output path \u2014 a single file for one output type, a directory for multiple."
  },
  "source_dir": {
    "type": "string",
    "default": "",
    "description": "Root directory of source files."
  },
  "prefix_dir": {
    "type": "string",
    "default": "",
    "description": "Prefix path to remove from file paths in coverage report."
  },
  "binary_path": {
    "type": "string",
    "default": "",
    "description": "Path to the compiled binary used to generate coverage data."
  },
  "llvm_path": {
    "type": "string",
    "default": "",
    "description": "Path to LLVM bin directory (e.g., /usr/lib/llvm-14/bin)."
  },
  "filter": {
    "type": "string",
    "default": "",
    "description": "Filter files: 'covered' or 'uncovered'.",
    "enum": [
      "covered",
      "uncovered"
    ]
  },
  "ignore": {
    "type": "list",
    "default": [],
    "description": "List of glob patterns to exclude from coverage analysis."
  },
  "keep_only": {
    "type": "list",
    "default": [],
    "description": "List of glob patterns \u2014 only files matching these are included."
  },
  "path_mapping": {
    "type": "string",
    "default": "",
    "description": "Path mapping rule in format 'from:to'."
  },
  "branch": {
    "type": "bool",
    "default": false,
    "description": "Enable parsing of branch coverage data."
  },
  "ignore_not_existing": {
    "type": "bool",
    "default": false,
    "description": "Ignore source files that don't exist on disk."
  },
  "llvm": {
    "type": "bool",
    "default": false,
    "description": "Optimize parsing for LLVM-generated coverage data only."
  },
  "token": {
    "type": "string",
    "default": "",
    "description": "Coveralls repository token (required for 'coveralls' and 'coveralls+' output types)."
  },
  "commit_sha": {
    "type": "string",
    "default": "",
    "description": "Git commit SHA corresponding to the coverage data."
  },
  "service_name": {
    "type": "string",
    "default": "",
    "description": "CI service name (e.g., 'github-actions', 'travis-ci')."
  },
  "service_number": {
    "type": "string",
    "default": "",
    "description": "CI service build or run number."
  },
  "service_job_id": {
    "type": "string",
    "default": "",
    "description": "CI service job ID."
  },
  "service_pull_request": {
    "type": "string",
    "default": "",
    "description": "CI service pull request number."
  },
  "service_flag_name": {
    "type": "string",
    "default": "",
    "description": "Flag name for Coveralls parallel builds."
  },
  "parallel": {
    "type": "bool",
    "default": false,
    "description": "Enable parallel build mode for Coveralls."
  },
  "threads": {
    "type": "number",
    "default": 1,
    "description": "Number of threads to use for processing."
  },
  "precision": {
    "type": "number",
    "default": 2,
    "description": "Decimal precision for coverage percentages in output reports."
  },
  "vcs_branch": {
    "type": "string",
    "default": "master",
    "description": "VCS branch name for Coveralls report."
  },
  "log": {
    "type": "string",
    "default": "stderr",
    "description": "Log file path, or 'stdout'/'stderr'.",
    "enum": [
      "stdout",
      "stderr"
    ]
  },
  "log_level": {
    "type": "string",
    "default": "ERROR",
    "description": "Log level: OFF, ERROR, WARN, INFO, DEBUG, TRACE.",
    "enum": [
      "OFF",
      "ERROR",
      "WARN",
      "INFO",
      "DEBUG",
      "TRACE"
    ]
  },
  "excl_line": {
    "type": "string",
    "default": "",
    "description": "Regex marker to exclude lines from line coverage analysis."
  },
  "excl_start": {
    "type": "string",
    "default": "",
    "description": "Regex marking start of excluded section for line coverage."
  },
  "excl_stop": {
    "type": "string",
    "default": "",
    "description": "Regex marking end of excluded section for line coverage."
  },
  "excl_br_line": {
    "type": "string",
    "default": "",
    "description": "Regex marker to exclude lines from branch coverage analysis."
  },
  "excl_br_start": {
    "type": "string",
    "default": "",
    "description": "Regex marking start of excluded section for branch coverage."
  },
  "excl_br_stop": {
    "type": "string",
    "default": "",
    "description": "Regex marking end of excluded section for branch coverage."
  },
  "no_demangle": {
    "type": "bool",
    "default": false,
    "description": "Disable symbol demangling."
  },
  "output_config_file": {
    "type": "string",
    "default": "",
    "description": "Path to write output configuration file (used internally)."
  },
  "sort_output_types": {
    "type": "list",
    "default": [
      "markdown"
    ],
    "description": "List of output types to sort files lexicographically for."
  },
  "guess_directory_when_missing": {
    "type": "bool",
    "default": false,
    "description": "Guess directory structure when missing in coverage data."
  }
}
