{
  "scopes": {
    "type": "list",
    "default": [],
    "description": "List of configuration scopes: 'default' for global settings, plus additional named scopes (e.g., 'production', 'dev') for environment-specific configs. Each scope's config must use documented keys only."
  }
}
