{
  "KEYFILE_PATTERN": {
    "type": "string",
    "default": "",
    "description": "Shell pattern to match key files from /etc/crypttab to include in the initramfs image. Empty string means no key files are copied (default)."
  },
  "ASKPASS": {
    "type": "string",
    "default": "y",
    "description": "Whether to include the askpass binary in the initramfs image ('y' or 'n'). Default is 'y'.\n      ",
    "enum": [
      "y",
      "n"
    ]
  }
}
