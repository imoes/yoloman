{
  "SYSFS_CONF_FILE": {
    "type": "string",
    "default": "/etc/sysfs.conf",
    "description": "Path to the main sysfs configuration file"
  },
  "SYSFS_CONF_DIR": {
    "type": "string",
    "default": "/etc/sysfs.d",
    "description": "Directory containing additional sysfs configuration files"
  },
  "MAINTAINER_EMAIL": {
    "type": "string",
    "default": "mpitt@debian.org",
    "description": "Email address of the package maintainer referenced in comments"
  }
}
