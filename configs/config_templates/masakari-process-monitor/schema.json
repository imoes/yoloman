{
  "provides": {
    "type": "string",
    "default": "masakari-process-monitor",
    "description": "The name of the service as provided by the init script."
  },
  "should_start_services": {
    "type": "string",
    "default": "postgresql mysql keystone rabbitmq-server ntp chrony",
    "description": "Space-separated list of services that should be started before this service."
  },
  "should_stop_services": {
    "type": "string",
    "default": "postgresql mysql keystone rabbitmq-server ntp chrony",
    "description": "Space-separated list of services that should be stopped before this service stops."
  },
  "short_description": {
    "type": "string",
    "default": "Masakari process-monitor",
    "description": "Short description of the service for init info header."
  },
  "author_email": {
    "type": "string",
    "default": "Thomas Goirand <zigo@debian.org>",
    "description": "Primary author contact information."
  },
  "description_full": {
    "type": "string",
    "default": "OpenStack Masakari Process Monitor (masakari-process-monitor)",
    "description": "Full description used in the DESC variable."
  },
  "project_name": {
    "type": "string",
    "default": "masakari",
    "description": "The OpenStack project name, used for paths and variable prefixes."
  },
  "author_email_2": {
    "type": "string",
    "default": "Ond\u0159ej Nov\u00fd <novy@ondrej.org>",
    "description": "Secondary author contact information."
  }
}
