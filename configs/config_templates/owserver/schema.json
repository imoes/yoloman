{
  "desc": {
    "type": "string",
    "default": "1-Wire TCP Server",
    "description": "Description of the service shown in log messages and status output."
  },
  "name": {
    "type": "string",
    "default": "owserver",
    "description": "The name of the daemon binary and service, used in paths, PID files, and log messages."
  },
  "pid_dir": {
    "type": "string",
    "default": "/var/run/owfs",
    "description": "Directory where the PID file for the owserver daemon is stored."
  }
}
