{
  "datapacker_args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments for datapacker (e.g., '-s 600m -a hardlink -b /dest/%03d'). Each item must be a single argument string."
  },
  "input_files": {
    "type": "list",
    "default": [],
    "description": "List of file paths or glob patterns to pack into bins. Must be non-empty. May include '-' to read from stdin (combined with -0/-d/-p flags)."
  }
}
