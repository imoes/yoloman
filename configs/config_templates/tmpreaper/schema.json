{
  "showwarning_default": {
    "type": "string",
    "default": "''",
    "description": "Initial value for SHOWWARNING variable. Default is empty string."
  },
  "delay_default": {
    "type": "number",
    "default": 256,
    "description": "Default delay value for TMPREAPER_DELAY if not set elsewhere."
  },
  "time_default": {
    "type": "string",
    "default": "7d",
    "description": "Default time value for TMPREAPER_TIME (e.g., '7d' for 7 days)."
  },
  "dirs_default": {
    "type": "string",
    "default": "'/tmp/.'",
    "description": "Default directories to clean for TMPREAPER_DIRS."
  }
}
