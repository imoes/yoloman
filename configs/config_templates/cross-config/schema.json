{
  "ac_cv_c_bigendian": {
    "type": "string",
    "default": "no",
    "description": " guessed endianness of the target C compiler (yes = big-endian, no = little-endian)"
  },
  "ac_cv_c_char_unsigned": {
    "type": "string",
    "default": "no",
    "description": "whether target C compiler treats 'char' as unsigned (yes/no)"
  },
  "maintainer_email": {
    "type": "string",
    "default": "roman@debian.org",
    "description": "email address for submitting additions to the cross-config.cache file"
  }
}
