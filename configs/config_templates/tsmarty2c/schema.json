{
  "cmd": {
    "type": "string",
    "default": "tsmarty2c",
    "description": "The path or name of the tsmarty2c executable."
  },
  "c_output": {
    "type": "string",
    "default": "gettext_c_strings.c",
    "description": "The target C source file where the riped strings will be written."
  },
  "template_sources": {
    "type": "list",
    "default": [
      "templates"
    ],
    "description": "A list of file paths or directories containing Smarty templates to process."
  },
  "extra_xgettext_flags": {
    "type": "list",
    "default": [
      "--add-comments"
    ],
    "description": "List of additional flags to be used when invoking xgettext on the resulting C file, as suggested in the man page."
  }
}
