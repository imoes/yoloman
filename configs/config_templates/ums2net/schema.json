{
  "conffile_path": {
    "type": "string",
    "default": "/etc/ums2net.conf",
    "description": "Path to the main ums2net configuration file."
  },
  "default_options": {
    "type": "string",
    "default": "",
    "description": "Default options passed to ums2net, overriding settings in CONFFILE."
  }
}
