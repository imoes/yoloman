{
  "realm": {
    "type": "string",
    "default": "EXAMPLE.COM",
    "description": "The Kerberos realm to operate on."
  },
  "principal": {
    "type": "string",
    "default": "admin",
    "description": "The principal name to target for operations like list, add, or modify."
  },
  "operation": {
    "type": "string",
    "default": "list",
    "description": "The operation to perform. Valid values: list, add, modify, remove, dump, key-add, key-remove.",
    "enum": [
      "list",
      "add",
      "modify",
      "remove",
      "dump"
    ]
  },
  "encryption_type": {
    "type": "string",
    "default": "",
    "description": "Override default key encryption type. Valid values: aes128, aes256, 3des, des-md5, des-md4, des-crc, arcfour.",
    "enum": [
      "aes128",
      "aes256",
      "aes",
      "3des",
      "des-md5",
      "des-md4",
      "des-crc",
      "des",
      "arcfour"
    ]
  },
  "force": {
    "type": "bool",
    "default": false,
    "description": "Allow removal of non-empty realms (-f, --force)."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Produce verbose output (-v)."
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Don't produce any diagnostic output (-q)."
  },
  "enabled_only": {
    "type": "bool",
    "default": false,
    "description": "Only dump or list enabled principals (--enabled)."
  },
  "disabled_only": {
    "type": "bool",
    "default": false,
    "description": "Only dump or list disabled principals (--disabled)."
  },
  "show_keys": {
    "type": "bool",
    "default": false,
    "description": "Print cryptographic key and password in hostkey format (--keys)."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Derive key from this password (--password)."
  },
  "key_version": {
    "type": "number",
    "default": 0,
    "description": "Version of key (--key-version)."
  },
  "priority": {
    "type": "number",
    "default": 0,
    "description": "Specify key priority (--priority). Higher value means higher priority."
  },
  "salt": {
    "type": "string",
    "default": "",
    "description": "Use specified salt for deriving key (--salt). Defaults to realm+principal concatenation."
  },
  "random_key": {
    "type": "bool",
    "default": true,
    "description": "Use a random key (--random). Default if no password or random is not specified."
  }
}
