{# Access Control List for Kerberos Admin Server (kadmin) #}
{% for entry in acl_entries %}{# Permission entry: {{ entry.principal | default('') }} has {{ entry.permissions | join(', ') }} access to {{ entry.pattern | default('*') }} #}
{{ entry.principal | default('*') }}	{{ entry.permissions | join(', ') }}	{{ entry.pattern | default('*') }}
{% endfor %}