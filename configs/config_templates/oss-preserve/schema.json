{
  "ossctl_path": {
    "type": "string",
    "default": "/usr/lib/oss-preserve/ossctl",
    "description": "Path to the ossctl binary used to save/restore OSS mixer settings."
  },
  "fixed_settings_path": {
    "type": "string",
    "default": "/etc/default/oss-preserve",
    "description": "Path to the fixed settings file for OSS mixer configuration."
  },
  "previous_settings_path": {
    "type": "string",
    "default": "/var/lib/oss-preserve/settings",
    "description": "Path to the previous settings file where OSS mixer state is stored."
  }
}
