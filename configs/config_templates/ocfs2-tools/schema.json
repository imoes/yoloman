{
  "cluster_name": {
    "type": "string",
    "default": "mycluster",
    "description": "Name of the OCFS2 cluster. Must be unique within your network and match across all nodes."
  },
  "number_nodes": {
    "type": "number",
    "default": 2,
    "description": "Total number of nodes in the cluster. Must match the count of nodes listed below."
  },
  "nodes": {
    "type": "list",
    "default": [],
    "description": "List of cluster nodes. Each node must be reachable by the other nodes via the specified IP address."
  }
}
