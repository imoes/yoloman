{
  "bringup_timeout": {
    "type": "number",
    "default": 5,
    "description": "How long to wait for things to start."
  },
  "def_heartbeat_threshold": {
    "type": "number",
    "default": 31,
    "description": "The default heartbeat threshold value, synced with the kernel."
  },
  "def_idle_timeout_ms": {
    "type": "number",
    "default": 30000,
    "description": "The default idle timeout in ms, synced with the kernel."
  },
  "def_keepalive_delay_ms": {
    "type": "number",
    "default": 2000,
    "description": "The default keepalive delay in ms, synced with the kernel."
  },
  "def_reconnect_delay_ms": {
    "type": "number",
    "default": 2000,
    "description": "The default reconnect delay in ms, synced with the kernel."
  },
  "min_heartbeat_threshold": {
    "type": "number",
    "default": 7,
    "description": "Minimum heartbeat threshold value."
  },
  "min_idle_timeout_ms": {
    "type": "number",
    "default": 5000,
    "description": "Minimum idle timeout in ms value."
  },
  "min_keepalive_delay_ms": {
    "type": "number",
    "default": 1000,
    "description": "Minimum keepalive delay in ms value."
  },
  "min_reconnect_delay_ms": {
    "type": "number",
    "default": 2000,
    "description": "Minimum reconnect delay in ms value."
  },
  "version": {
    "type": "string",
    "default": "1.8.8",
    "description": "Version string of the o2cb script."
  }
}
