{
  "path": {
    "type": "string",
    "default": "",
    "description": "Path to the input flashfile (BIOS image) to decompress"
  },
  "mode": {
    "type": "string",
    "default": "list",
    "description": "Operation mode: 'list', 'list-all', 'extract', 'extract-all', or 'extract-raw'",
    "enum": [
      "list",
      "extract"
    ]
  },
  "output_file": {
    "type": "string",
    "default": "",
    "description": "Output file path when using extraction modes; empty string disables output file (stdout only)"
  }
}
