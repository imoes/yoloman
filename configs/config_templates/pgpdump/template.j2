# pgpdump configuration template
# pgpdump is a command-line utility for decoding PGP packets; it has NO server daemon,
# NO configuration file, and NO persistent state. It relies solely on its binary and
# system libraries (e.g., libgpg-error, libgcrypt).
# This template is intentionally empty because no configuration is required or supported.
# If this role needs to invoke pgpdump, ensure the package is installed via the role's
# package management tasks (e.g., apt, yum, dnf).