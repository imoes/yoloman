{
  "variable.voms": {
    "type": "string",
    "default": "",
    "description": "The VOMS name for the VO."
  },
  "variable.igtf_base_url": {
    "type": "string",
    "default": "http://repository.egi.eu/sw/production/cas/1/current/meta",
    "description": "The base URL for IGTF CA certificate release files."
  },
  "variable.top_bdii": {
    "type": "string",
    "default": "ldap://lcg-bdii.cern.ch:2170",
    "description": "The URI of the BDII server to query for storage element information."
  },
  "variable.se_vo_dir": {
    "type": "string",
    "default": "",
    "description": "Configuration block for VO-specific storage directory discovery via LDAP."
  },
  "variable.good_ses_file": {
    "type": "string",
    "default": "",
    "description": "Path to a local file containing a list of known good storage elements for staging tests."
  },
  "variable.se_host": {
    "type": "string",
    "default": "",
    "description": "The host name of the storage element to use for staging tests."
  },
  "variable.se_test_dir": {
    "type": "string",
    "default": "",
    "description": "A directory on se_host where test files can be written."
  },
  "variable.stage_stamp": {
    "type": "string",
    "default": "",
    "description": "Configuration for the timestamp format used in staging test output files."
  },
  "arcce.dist-caversion": {
    "type": "string",
    "default": "",
    "description": "Configuration block for checking IGTF CA certificate versions."
  },
  "arcce.dist-stage-https": {
    "type": "string",
    "default": "",
    "description": "Configuration block for HTTPS staging tests."
  },
  "arcce.dist-stage-srm": {
    "type": "string",
    "default": "",
    "description": "Configuration block for SRM staging tests."
  }
}
