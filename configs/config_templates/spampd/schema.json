{
  "bind_address": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address for spampd to bind to (e.g., loopback or public interface)"
  },
  "bind_port": {
    "type": "number",
    "default": 10025,
    "description": "Port to listen on for incoming SMTP connections"
  },
  "spamc_host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Hostname or IP where SpamAssassin's spamc/spamd daemon listens"
  },
  "spamc_port": {
    "type": "number",
    "default": 783,
    "description": "Port of SpamAssassin's spamd daemon"
  },
  "connect_timeout": {
    "type": "number",
    "default": 30,
    "description": "Maximum seconds to wait when connecting to spamd"
  },
  "read_timeout": {
    "type": "number",
    "default": 60,
    "description": "Maximum seconds to wait for data from spamd after connection"
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Verbosity level: 'debug', 'info', 'notice', 'warning', 'error'",
    "enum": [
      "debug",
      "info",
      "notice",
      "warning",
      "error"
    ]
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/spampd/spampd.log",
    "description": "Path to log file (use '-' for stderr)"
  },
  "user": {
    "type": "string",
    "default": "spampd",
    "description": "Unix user to run the service as (for security)"
  },
  "group": {
    "type": "string",
    "default": "spampd",
    "description": "Unix group to run the service as"
  },
  "pid_file": {
    "type": "string",
    "default": "/run/spampd/spampd.pid",
    "description": "Path to PID file (set to '' or omit to disable)"
  },
  "max_children": {
    "type": "number",
    "default": 10,
    "description": "Maximum number of concurrent child processes (connections)"
  },
  "skip_scanning_domains": {
    "type": "list",
    "default": [],
    "description": "List of domains to skip spam scanning for (e.g., internal mail)"
  },
  "custom_rules": {
    "type": "list",
    "default": [],
    "description": "Custom SpamAssassin rules or score adjustments"
  },
  "whitelist_senders": {
    "type": "list",
    "default": [],
    "description": "Email addresses or patterns to always trust (bypass spam scoring)"
  },
  "blacklist_senders": {
    "type": "list",
    "default": [],
    "description": "Email addresses or patterns to always treat as spam"
  }
}
