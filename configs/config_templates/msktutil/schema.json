{
  "autoupdate_enabled": {
    "type": "string",
    "default": "false",
    "description": "Whether the auto-update feature is enabled.",
    "enum": [
      "true",
      "false"
    ]
  },
  "autoupdate_options": {
    "type": "string",
    "default": "",
    "description": "Additional options passed to mskutil in auto-update mode."
  }
}
