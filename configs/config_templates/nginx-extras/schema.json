{
  "nginx_user": {
    "type": "string",
    "default": "www-data",
    "description": "System user account under which NGINX worker processes run for security isolation."
  },
  "nginx_group": {
    "type": "string",
    "default": "www-data",
    "description": "System group for NGINX worker processes."
  },
  "nginx_worker_processes": {
    "type": "number",
    "default": 1,
    "description": "Number of worker processes. Set to 'auto' for automatic CPU core detection (use 1 as safe default; advanced users may override via string)."
  },
  "nginx_pid_file": {
    "type": "string",
    "default": "/run/nginx.pid",
    "description": "Path to the PID file storing the main NGINX process ID."
  },
  "nginx_error_log_path": {
    "type": "string",
    "default": "/var/log/nginx/error.log",
    "description": "Path to the global error log file."
  },
  "nginx_error_log_level": {
    "type": "string",
    "default": "warn",
    "description": "Log level for errors (debug, info, notice, warn, error, crit, alert, emerg)."
  },
  "nginx_events_worker_connections": {
    "type": "number",
    "default": 768,
    "description": "Maximum number of simultaneous connections each worker can handle. Higher values increase capacity but require more file descriptors."
  },
  "nginx_events_use": {
    "type": "string",
    "default": "epoll",
    "description": "Event notification mechanism (e.g., epoll for Linux, kqueue for BSD)."
  },
  "nginx_mimetypes_file": {
    "type": "string",
    "default": "/etc/nginx/mime.types",
    "description": "Path to the file containing MIME type definitions."
  },
  "nginx_default_type": {
    "type": "string",
    "default": "application/octet-stream",
    "description": "Default MIME type for responses when type cannot be determined."
  },
  "nginx_access_log_path": {
    "type": "string",
    "default": "/var/log/nginx/access.log",
    "description": "Path to the main access log file."
  },
  "nginx_sendfile": {
    "type": "boolean",
    "default": true,
    "description": "Enable/disable sendfile(2) for high-performance static file delivery."
  },
  "nginx_tcp_nopush": {
    "type": "boolean",
    "default": true,
    "description": "Enable TCP_NOPUSH (or TCP_CORK) to send full TCP packets for improved network efficiency."
  },
  "nginx_keepalive_timeout": {
    "type": "number",
    "default": 65,
    "description": "Timeout (in seconds) for keep-alive connections with clients."
  },
  "nginx_gzip_compression_level": {
    "type": "number",
    "default": 3,
    "description": "Gzip compression level (1\u20139). Higher = better compression but more CPU."
  },
  "nginx_gzip_min_length": {
    "type": "number",
    "default": 256,
    "description": "Minimum response size (bytes) required to enable gzip compression."
  },
  "nginx_gzip_types": {
    "type": "list",
    "default": [
      "text/plain",
      "text/css",
      "application/json",
      "application/javascript",
      "text/xml",
      "application/xml"
    ],
    "description": "MIME types to compress with gzip. Applies only to responses with matching Content-Type headers."
  },
  "nginx_client_max_body_size": {
    "type": "string",
    "default": "1m",
    "description": "Maximum allowed size of client request body (e.g., '10m' for 10 MB)."
  },
  "nginx_client_body_buffer_size": {
    "type": "string",
    "default": "16k",
    "description": "Buffer size for client request bodies before spilling to disk."
  },
  "nginx_open_file_cache_max": {
    "type": "number",
    "default": 10000,
    "description": "Maximum number of open files in the cache."
  },
  "nginx_open_file_cache_inactive": {
    "type": "string",
    "default": "60s",
    "description": "Time after which cached entries become inactive if not accessed."
  },
  "nginx_open_file_cache_valid": {
    "type": "string",
    "default": "30s",
    "description": "Interval for validating cached file metadata."
  },
  "nginx_open_file_cache_min": {
    "type": "number",
    "default": 2,
    "description": "Minimum number of accesses before a file is cached."
  },
  "nginx_conf_d_path": {
    "type": "string",
    "default": "/etc/nginx/conf.d",
    "description": "Directory containing additional site-specific configuration files (*.conf)."
  },
  "nginx_custom_servers": {
    "type": "list",
    "default": [],
    "description": "List of custom server blocks to include inline (use for monitoring endpoints, fallback servers, or module-specific configs).",
    "items": {
      "type": "object",
      "fields": {
        "description": {
          "type": "string",
          "default": "",
          "description": "Human-readable description of this server block."
        },
        "listen": {
          "type": "string",
          "default": "80",
          "description": "Listen address/port (e.g., '80', '[::]:443 ssl', 'unix:/var/run/nginx.sock')."
        },
        "server_name": {
          "type": "list",
          "default": [
            "_"
          ],
          "description": "List of server names or wildcard patterns (e.g., ['example.com', '*.example.com'])."
        },
        "ssl": {
          "type": "boolean",
          "default": false,
          "description": "Enable SSL/TLS for this server."
        },
        "ssl_certificate": {
          "type": "string",
          "default": "",
          "description": "Path to the SSL certificate file."
        },
        "ssl_certificate_key": {
          "type": "string",
          "default": "",
          "description": "Path to the SSL private key file."
        },
        "ssl_protocols": {
          "type": "list",
          "default": [
            "TLSv1.2",
            "TLSv1.3"
          ],
          "description": "Supported SSL/TLS protocols (e.g., ['TLSv1.2', 'TLSv1.3'])."
        },
        "ssl_ciphers": {
          "type": "string",
          "default": "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256",
          "description": "Allowed cipher suites for SSL."
        },
        "root": {
          "type": "string",
          "default": "",
          "description": "Document root directory for this server."
        },
        "index": {
          "type": "list",
          "default": [
            "index.html",
            "index.htm"
          ],
          "description": "List of index files to try in order."
        },
        "locations": {
          "type": "list",
          "default": [],
          "description": "List of location blocks for URL path routing.",
          "items": {
            "type": "object",
            "fields": {
              "description": {
                "type": "string",
                "default": "",
                "description": "Description of this location block."
              },
              "path": {
                "type": "string",
                "default": "/",
                "description": "URL path or regex pattern to match (e.g., '/api/', '^~ /static/')."
              },
              "root": {
                "type": "string",
                "default": "",
                "description": "Override document root for this location."
              },
              "try_files": {
                "type": "list",
                "default": [],
                "description": "Fallback chain for file resolution (e.g., ['$uri', '$uri/', '=404'])."
              },
              "proxy_pass": {
                "type": "string",
                "default": "",
                "description": "Backend URL for reverse proxy (e.g., 'http://127.0.0.1:8080')."
              },
              "auth_basic": {
                "type": "string",
                "default": "",
                "description": "Enable HTTP Basic Auth; value sets the realm string."
              },
              "auth_basic_user_file": {
                "type": "string",
                "default": "",
                "description": "Path to .htpasswd file for auth users."
              },
              "extra_config": {
                "type": "list",
                "default": [],
                "description": "Custom directives for nginx-extras modules (e.g., auth_request, limit_req, etc.).",
                "items": {
                  "type": "object",
                  "fields": {}
                }
              }
            }
          }
        }
      }
    }
  }
}
