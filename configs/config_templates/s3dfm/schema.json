{
  "binary_path": {
    "type": "string",
    "default": "/usr/bin/s3dfm",
    "description": "Absolute path to the s3dfm executable."
  },
  "s3d_url": {
    "type": "string",
    "default": "http://localhost:3564",
    "description": "The S3D server URL to connect to directly, bypassing environment auto-discovery."
  }
}
