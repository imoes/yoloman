{
  "prefix_url": {
    "type": "string",
    "default": "http://example.com",
    "description": "Domain/URL used as prefix when returning the cluster ID"
  },
  "web_service_port": {
    "type": "number",
    "default": 8087,
    "description": "Port on which the web service binds"
  },
  "etcd_host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Host address of the etcd cluster"
  },
  "etcd_port": {
    "type": "number",
    "default": 2379,
    "description": "Port of the etcd cluster"
  }
}
