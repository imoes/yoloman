{
  "args": {
    "type": "string",
    "default": "",
    "description": "Command-line arguments to pass to the prometheus-mysqld-exporter server, e.g., '--config.my-cnf /etc/mysql/debian.cnf'. Note that backslashes for regexes need double escaping in this shell variable context."
  }
}
