{
  "provides": [
    {
      "capability": "sip_proxy",
      "backend": "kamailio",
      "port_field": "listeners",
      "default_port": 5060,
      "provisionable": {
        "databases": "db_url",
        "aliases": "aliases"
      }
    }
  ],
  "requires": [
    {
      "capability": "database",
      "backends": [
        "mysql",
        "mariadb",
        "postgresql",
        "sqlite"
      ],
      "backend_field": "db_url",
      "fields": {
        "host": "db_url",
        "port": "db_url",
        "name": "db_url",
        "user": "db_url",
        "password": "db_url"
      }
    }
  ],
  "peer_injection": [],
  "confidence": "review"
}
