{
  "service_command": {
    "type": "string",
    "default": "/usr/local/bin/myapp",
    "description": "The full command to execute the service (e.g., path to binary with arguments)."
  },
  "multilog_opts": {
    "type": "object",
    "default": {},
    "description": "Options passed to multilog to configure log rotation. Note: these are *not* passed as a shell-quoted string in the template; each option is expanded separately below.",
    "fields": {
      "format": {
        "type": "string",
        "default": "n80",
        "description": "Log format: 'n' = no timestamp, 's' = seconds since epoch, 'c' = compact human-readable (default 'n80' = no timestamp, 80-byte buffer)."
      },
      "max_size": {
        "type": "number",
        "default": 1000000,
        "description": "Maximum size (in bytes) of a log file before rotation."
      },
      "keep_count": {
        "type": "number",
        "default": 30,
        "description": "Number of rotated log files to retain."
      },
      "max_time": {
        "type": "number",
        "default": 0,
        "description": "Maximum age (in seconds) for a log file before rotation (0 = disable time-based rotation)."
      },
      "directory": {
        "type": "string",
        "default": "/var/log/service",
        "description": "Base directory where log files are stored (must exist and be writable)."
      }
    }
  }
}
