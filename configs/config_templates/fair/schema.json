{
  "id_file": {
    "type": "string",
    "default": "fair.default 8058 2005-07-07 13:06:53Z guus",
    "description": "The file ID comment line (static metadata from shipped file)"
  },
  "url": {
    "type": "string",
    "default": "https://infix.uvt.nl/its-id/trunk/sources/fair/package/debian/fair.default",
    "description": "The URL comment line (static metadata from shipped file)"
  },
  "carrousel": {
    "type": "bool",
    "default": false,
    "description": "Whether to start the fair-carrousel service (SysV init only)"
  },
  "transponder": {
    "type": "bool",
    "default": false,
    "description": "Whether to start the fair-transponder service (SysV init only)"
  },
  "balancers": {
    "type": "string",
    "default": "",
    "description": "Whitespace-separated list of balancer instances to enable (empty by default)"
  }
}
