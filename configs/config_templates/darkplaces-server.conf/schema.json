{
  "hostname": {
    "type": "string",
    "default": "Darkplaces Server",
    "description": "Publicly visible server name shown in server browser."
  },
  "port": {
    "type": "number",
    "default": 27500,
    "description": "Main UDP port used for gameplay traffic (must be open in firewall)."
  },
  "maxplayers": {
    "type": "number",
    "default": 16,
    "description": "Maximum number of concurrent players."
  },
  "mapcyclefile": {
    "type": "string",
    "default": "mapcycle.txt",
    "description": "Relative or absolute path to map rotation file. Relative paths resolve relative to server root."
  },
  "dedicated": {
    "type": "bool",
    "default": true,
    "description": "Run in dedicated server mode (no local client). Required for public servers."
  },
  "server_rules": {
    "type": "list",
    "default": [],
    "description": "List of cvars to expose as server rules (e.g., for mod-specific settings)."
  },
  "map_change_commands": {
    "type": "list",
    "default": [],
    "description": "List of commands to run on every map change (e.g., to enforce mod rules)."
  },
  "extra_cvars": {
    "type": "list",
    "default": [],
    "description": "Custom configuration overrides not covered by other sections."
  }
}
