{
  "community_file": {
    "type": "string",
    "default": "/etc/onesixtyone/community",
    "description": "Path to the file with community names to try."
  },
  "host_file": {
    "type": "string",
    "default": "/etc/onesixtyone/hosts",
    "description": "Path to the file with target hosts to scan."
  },
  "output_file": {
    "type": "string",
    "default": "/var/log/onesixtyone/scan.log",
    "description": "Output log file for scan results."
  },
  "port": {
    "type": "number",
    "default": 161,
    "description": "Specify an alternate destination SNMP port."
  },
  "wait_time": {
    "type": "number",
    "default": 10,
    "description": "Wait n milliseconds (1/1000 of a second) between sending packets."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode."
  },
  "short_mode": {
    "type": "bool",
    "default": false,
    "description": "Enable short mode, only print IP addresses."
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Enable quiet mode, do not print log to stdout, use with -o."
  }
}
