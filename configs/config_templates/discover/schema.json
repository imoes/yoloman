{
  "types": {
    "type": "string",
    "default": "all",
    "description": "Device types to detect; use \"all\" to detect all device types.",
    "enum": [
      "all",
      "audio",
      "bridge",
      "broadband",
      "display",
      "fixeddisk",
      "humaninput",
      "imaging",
      "miscellaneous",
      "modem",
      "network",
      "optical",
      "printer",
      "removabledisk",
      "tape",
      "video"
    ]
  },
  "skip": {
    "type": "string",
    "default": "",
    "description": "Space-separated list of modules to skip loading (commented out by default in shipped config)."
  }
}
