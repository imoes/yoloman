{
  "debug_enabled": {
    "type": "string",
    "default": "no",
    "description": "Set to 'yes' to enable debugging output for dhclient-script.",
    "enum": [
      "no",
      "yes"
    ]
  },
  "debug_log_path": {
    "type": "string",
    "default": "/tmp/dhclient-script.debug",
    "description": "Path to the log file for debug output."
  }
}
