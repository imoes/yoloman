{
  "radicale_opts": {
    "type": "string",
    "default": "",
    "description": "Command line options passed to radicale, typically empty if config file is used."
  },
  "radicale_verbose": {
    "type": "bool",
    "default": true,
    "description": "Enable verbose output from the initscript."
  }
}
