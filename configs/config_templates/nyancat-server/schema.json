{
  "port": {
    "type": "number",
    "default": 3000,
    "description": "The TCP port for the nyancat-server to listen on"
  },
  "telnet_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable telnet mode for remote connections"
  },
  "show_intro": {
    "type": "bool",
    "default": false,
    "description": "Show introduction/about information on startup"
  },
  "frames": {
    "type": "number",
    "default": 0,
    "description": "Number of frames to display before quitting (0 for infinite loop)"
  },
  "width": {
    "type": "number",
    "default": 80,
    "description": "Crop the animation to the given width in columns"
  },
  "height": {
    "type": "number",
    "default": 24,
    "description": "Crop the animation to the given height in rows"
  },
  "display_counter": {
    "type": "bool",
    "default": true,
    "description": "Display the animation timer"
  },
  "set_title": {
    "type": "bool",
    "default": true,
    "description": "Set the terminal titlebar text"
  },
  "clear_display": {
    "type": "bool",
    "default": true,
    "description": "Clear the display between animation frames"
  }
}
