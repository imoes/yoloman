{
  "mail_address": {
    "type": "string",
    "default": "",
    "description": "Email address to send filesystem error notifications to."
  },
  "stats_interval": {
    "type": "string",
    "default": "1h",
    "description": "Approximate interval for statistics collection (e.g., 1h, 1d).\", \"enum\": [\"1h\", \"never\"]",
    "enum": [
      "never",
      "1h",
      "3d",
      "1M",
      "2M",
      "3M"
    ]
  },
  "scrub_interval": {
    "type": "string",
    "default": "1M",
    "description": "Approximate interval for scrub operations (e.g., 1M = once per month).\", \"enum\": [\"1M\", \"2M\", \"never\"]",
    "enum": [
      "never",
      "1h",
      "3d",
      "1M",
      "2M",
      "3M"
    ]
  },
  "balance_interval": {
    "type": "string",
    "default": "never",
    "description": "Approximate interval for balance operations (e.g., never, 1w).\", \"enum\": [\"never\", \"3M\"]",
    "enum": [
      "never",
      "1h",
      "3d",
      "1M",
      "2M",
      "3M"
    ]
  }
}
