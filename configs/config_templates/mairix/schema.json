{
 "case_insensitive_search": {
  "default": true,
  "description": "Enable case-insensitive search (recommended for most users).",
  "type": "bool"
 },
 "custom_folders": {
  "description": "Map of custom folder names to patterns (e.g., `{'Archive': 'Archives/*'}`).",
  "items": {
   "name": {
    "description": "Logical name for the folder.",
    "type": "string"
   },
   "pattern": {
    "description": "Path pattern relative to `mail_base` (e.g., `Archive/2023*`).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "db_path": {
  "default": ".mairix",
  "description": "Path to the index database file, relative to `mail_base` or absolute.",
  "type": "string"
 },
 "external_indexer": {
  "default": "",
  "description": "Path to an external script that handles indexing (e.g., for non-standard formats). Must accept a folder path as its only argument.",
  "type": "string"
 },
 "extra_folders": {
  "default": [],
  "description": "Additional folders to index (e.g., secondary inboxes or backups). Optional.",
  "type": "list"
 },
 "folder_prefix": {
  "default": "",
  "description": "Prefix to strip from folder names in search results (e.g., `Mail/` → shows `Inbox` instead of `Mail/Inbox`).",
  "type": "string"
 },
 "ignore_pattern": {
  "default": ".*, tmp",
  "description": "Comma-separated list of file/folder patterns to ignore (e.g., for Maildir: `.*, tmp, cache`).",
  "type": "string"
 },
 "include_message_ids": {
  "default": true,
  "description": "Include message IDs in search output (helpful for integration with mail clients).",
  "type": "bool"
 },
 "mail_base": {
  "default": "~",
  "description": "Top-level directory where your mail folders reside (e.g., `~/Mail` for Maildir, `/var/mail` for MBox).",
  "type": "string"
 },
 "mail_folders": {
  "default": [
   "."
  ],
  "description": "List of mail folders to index (relative to `mail_base`). Supports wildcards (e.g., `Inbox*`, `Sent*`).",
  "type": "list"
 },
 "result_limit": {
  "default": 100,
  "description": "Maximum number of matching messages to display in search results. Use `0` for unlimited.",
  "type": "number"
 },
 "search_bodies": {
  "default": true,
  "description": "Search in message body content (text/plain and text/html parts).",
  "type": "bool"
 },
 "search_headers": {
  "default": true,
  "description": "Search in message headers (From, To, Subject, etc.).",
  "type": "bool"
 },
 "sendmail_path": {
  "default": "/usr/sbin/sendmail",
  "description": "Path to an MTA-compatible sendmail binary used when composing/replying to messages.",
  "type": "string"
 }
}
