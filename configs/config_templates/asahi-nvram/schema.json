{
  "device": {
    "type": "string",
    "default": "/dev/nvram",
    "description": "Path to the Apple Silicon NVRAM device file."
  },
  "operations": {
    "type": "list",
    "default": [],
    "description": "Ordered list of NVRAM operations (read/write/delete). Each entry corresponds to one asahi-nvram subcommand invocation. WARNING: write operations may require DFU restore if misused."
  }
}
