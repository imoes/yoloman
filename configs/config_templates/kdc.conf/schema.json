{
  "kdc": {
    "type": "object",
    "default": [],
    "description": "Configuration options for the Kerberos KDC service (kdc.conf format). Top-level keys may be used for defaults, or realms can be defined explicitly in the 'realms' list below. This schema supports both global and per-realm settings per kdc.conf(5)."
  }
}
