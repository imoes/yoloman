{
  "video_dir": {
    "type": "string",
    "default": "./videos",
    "description": "Full path to your video(s) directory"
  },
  "preferred_languages": {
    "type": "list",
    "default": [
      "en"
    ],
    "description": "List of preferred subtitle languages for download and upload"
  },
  "cli_mode": {
    "type": "bool",
    "default": false,
    "description": "Run application in CLI mode instead of GUI"
  },
  "log_level": {
    "type": "string",
    "default": "warning",
    "description": "Logging severity level: debug, warning, error, or quiet",
    "enum": [
      "debug",
      "warning",
      "error"
    ]
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/subdownloader/subdownloader.log",
    "description": "Path name of the log file; empty to disable file logging"
  },
  "recursive": {
    "type": "bool",
    "default": false,
    "description": "Search for subtitles recursively in subdirectories"
  },
  "proxy": {
    "type": "string",
    "default": "",
    "description": "Proxy server URL to use for internet connections"
  },
  "naming_strategy": {
    "type": "string",
    "default": "lang",
    "description": "Naming convention for downloaded subtitles: online, video, lang, or uploader"
  },
  "providers": {
    "type": "list",
    "default": [],
    "description": "List of provider configurations with name and parameters"
  }
}
