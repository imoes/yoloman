{
  "cols": {
    "type": "number",
    "default": 2,
    "description": "Number of columns per sheet"
  },
  "rows": {
    "type": "list",
    "default": [],
    "description": "List of credential rows; each row contains fields matching CSV columns and used in template VAR-* placeholders"
  }
}
