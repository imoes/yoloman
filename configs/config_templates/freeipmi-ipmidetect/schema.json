{
  "hostnames": {
    "type": "list",
    "default": [],
    "description": "List of hostnames or IP addresses to monitor."
  },
  "port": {
    "type": "number",
    "default": 623,
    "description": "UDP port number for IPMI messages (typically 623)."
  },
  "timeout_len": {
    "type": "number",
    "default": 2,
    "description": "Timeout in seconds for each detection attempt."
  }
}
