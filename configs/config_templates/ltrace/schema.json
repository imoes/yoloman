{
  "max_syscalls": {
    "type": "number",
    "default": 4096,
    "description": "Maximum number of system calls traced per process."
  },
  "max_stack": {
    "type": "number",
    "default": 1024,
    "description": "Maximum number of stack frames traced."
  },
  "max_str_len": {
    "type": "number",
    "default": 64,
    "description": "Maximum length of strings traced."
  },
  "max_rec_depth": {
    "type": "number",
    "default": 32,
    "description": "Maximum depth of recursive library calls traced."
  },
  "ignore_libs": {
    "type": "list",
    "default": [
      "ld.so",
      "linux-gate.so"
    ],
    "description": "List of library names to ignore during tracing."
  }
}
