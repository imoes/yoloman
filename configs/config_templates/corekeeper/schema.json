{
  "corekeeper_cache_dir": {
    "type": "string",
    "default": "/var/cache/corekeeper",
    "description": "The directory where corekeeper stores its state files."
  },
  "corekeeper_crash_retention_days": {
    "type": "number",
    "default": 7,
    "description": "Number of days to retain core dump files before automatic deletion."
  }
}
