{
  "inputs": {
    "type": "list",
    "default": [],
    "description": "List of SCSS input files to compile."
  },
  "output_style": {
    "type": "string",
    "default": "expanded",
    "description": "CSS output format. Must be one of: expanded, compressed.",
    "enum": [
      "expanded",
      "compressed"
    ]
  },
  "precision": {
    "type": "number",
    "default": 5,
    "description": "Number of digits of precision for decimal numbers."
  },
  "load_paths": {
    "type": "list",
    "default": [],
    "description": "Directories to search for included resources. Uses 'items' schema for path structure."
  }
}
