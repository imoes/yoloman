{
  "BASEDIR": {
    "type": "string",
    "default": "/var/lib/autopsy",
    "description": "Directory where cases and forensic images are located."
  },
  "TSKDIR": {
    "type": "string",
    "default": "/usr/bin",
    "description": "Directory where The Sleuth Kit binaries are located."
  },
  "NSRLDB": {
    "type": "string",
    "default": "/var/lib/nsrl",
    "description": "Location of the NIST National Software Reference Library database."
  },
  "INSTALLDIR": {
    "type": "string",
    "default": "/usr/share/autopsy",
    "description": "Directory where Autopsy was installed."
  },
  "GREP_EXE": {
    "type": "string",
    "default": "grep",
    "description": "Path to grep(1) binary."
  },
  "STRINGS_EXE": {
    "type": "string",
    "default": "strings",
    "description": "Path to strings(1) binary."
  },
  "USE_STIMEOUT": {
    "type": "bool",
    "default": false,
    "description": "When true, server exits after STIMEOUT seconds of inactivity (default: 3600)."
  },
  "STIMEOUT": {
    "type": "number",
    "default": 3600,
    "description": "Inactivity timeout in seconds (used only if USE_STIMEOUT=true)."
  }
}
