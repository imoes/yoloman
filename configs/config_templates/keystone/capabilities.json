{
  "provides": [
    {
      "capability": "app_runtime",
      "backend": "uwsgi",
      "port_field": "keystone_uwsgi_port",
      "default_port": 5000
    }
  ],
  "requires": [
    {
      "capability": "database",
      "backends": [
        "mysql",
        "mariadb"
      ],
      "backend_field": "database.connection",
      "fields": {
        "host": "database.connection",
        "port": "database.connection",
        "name": "database.connection",
        "user": "database.connection",
        "password": "database.connection"
      }
    },
    {
      "capability": "cache",
      "backends": [
        "memcached"
      ],
      "backend_field": "cache.memcache_servers",
      "fields": {
        "host": "cache.memcache_servers"
      }
    }
  ],
  "confidence": "high",
  "peer_injection": []
}
