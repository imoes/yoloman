{
  "network_dynamic_configuration_debug_level": {
    "type": "number",
    "default": 0,
    "description": "Debug level for network dynamic configuration. 0 for silent, >0 for verbose."
  },
  "application_database_file_name": {
    "type": "string",
    "default": "/tmp/testserverdb",
    "description": "Path where database support files are stored."
  },
  "application_saved_images_folder_name": {
    "type": "string",
    "default": "/tmp/testserverimages",
    "description": "Path where images stored in the database are saved."
  },
  "application_database_server_name": {
    "type": "string",
    "default": "testserverdb",
    "description": "Name used for external TCP access to the database."
  },
  "display_full_screen": {
    "type": "bool",
    "default": false,
    "description": "True if full-screen rather than tiled window mode."
  },
  "dicom_storage_scu_compression_level": {
    "type": "number",
    "default": 2,
    "description": "Compression level for DICOM storage SCU. 0=none, 1=deflate, 2=deflate+bzip2."
  },
  "dicom_listening_port": {
    "type": "number",
    "default": 4006,
    "description": "Port for the DicomImageViewer application to listen on for incoming associations."
  },
  "dicom_called_ae_title": {
    "type": "string",
    "default": "LOCALHOST",
    "description": "AE Title the application calls itself when accepting an association."
  },
  "dicom_calling_ae_title": {
    "type": "string",
    "default": "LOCALHOST",
    "description": "AE Title the application calls itself when initiating an association."
  },
  "dicom_primary_device_type": {
    "type": "string",
    "default": "WSD",
    "description": "Type of the primary device (e.g., WSD, ARCHIVE).",
    "enum": [
      "WSD",
      "ARCHIVE"
    ]
  },
  "dicom_remote_aes": {
    "type": "string",
    "default": "localhost remote1 remote2",
    "description": "Space or comma separated list of all available remote AE names."
  },
  "dicom_currently_selected_storage_target_ae": {
    "type": "string",
    "default": "remote1",
    "description": "Default storage target AE name."
  },
  "dicom_currently_selected_query_target_ae": {
    "type": "string",
    "default": "remote1",
    "description": "Default remote query target AE name."
  },
  "dicom_remote_aes_localhost_called_ae_title": {
    "type": "string",
    "default": "LOCALHOST",
    "description": "Called AE Title for the 'localhost' remote AE entry."
  },
  "dicom_remote_aes_localhost_hostname_or_ip_address": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Hostname or IP Address for the 'localhost' remote AE entry."
  },
  "dicom_remote_aes_localhost_port": {
    "type": "number",
    "default": 4006,
    "description": "Port for the 'localhost' remote AE entry."
  },
  "dicom_remote_aes_remote1_called_ae_title": {
    "type": "string",
    "default": "REMOTE1",
    "description": "Called AE Title for the 'remote1' remote AE entry."
  },
  "dicom_remote_aes_remote1_hostname_or_ip_address": {
    "type": "string",
    "default": "10.217.142.23",
    "description": "Hostname or IP Address for the 'remote1' remote AE entry."
  },
  "dicom_remote_aes_remote1_port": {
    "type": "number",
    "default": 3010,
    "description": "Port for the 'remote1' remote AE entry."
  },
  "dicom_remote_aes_remote2_called_ae_title": {
    "type": "string",
    "default": "REMOTE2",
    "description": "Called AE Title for the 'remote2' remote AE entry."
  },
  "dicom_remote_aes_remote2_hostname_or_ip_address": {
    "type": "string",
    "default": "remote2.organization.com",
    "description": "Hostname or IP Address for the 'remote2' remote AE entry."
  },
  "dicom_remote_aes_remote2_port": {
    "type": "number",
    "default": 4006,
    "description": "Port for the 'remote2' remote AE entry."
  }
}
