{
  "FAKE_HOME": {
    "type": "string",
    "default": "/etc/fake",
    "description": "Base directory for fake configuration"
  },
  "ARP_DELAY": {
    "type": "number",
    "default": 1,
    "description": "Delay in seconds between gratuitous ARP"
  },
  "CLEAR_ROUTERS_FILE_RELATIVE": {
    "type": "string",
    "default": "clear_routers",
    "description": "Relative path (from FAKE_HOME) to the file containing list of routers to rsh"
  },
  "PID_DIR": {
    "type": "string",
    "default": "/var/run",
    "description": "Directory where PID files are kept"
  },
  "INSTANCE_CONFIG_DIR_RELATIVE": {
    "type": "string",
    "default": "instance_config",
    "description": "Relative path (from FAKE_HOME) to directory for instance-specific configuration files"
  }
}
