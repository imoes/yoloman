{
  "fadvise_calls_count": {
    "type": "number",
    "default": 1,
    "description": "The number of times to execute the posix_fadvise system call in a row. This corresponds to the -n <n> argument in nocache(1)."
  }
}
