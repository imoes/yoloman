{
  "compute_driver": {
    "type": "string",
    "default": "ironic.IronicDriver",
    "description": "The driver to use for managing ironic compute nodes."
  },
  "ram_allocation_ratio": {
    "type": "number",
    "default": 1.0,
    "description": "Ratio used for floating point comparisons. RAM allocation ratio for ironic compute nodes."
  },
  "reserved_host_memory_mb": {
    "type": "number",
    "default": 0,
    "description": "Amount of host memory (in MB) to leave reserved for the host."
  },
  "virt_type": {
    "type": "string",
    "default": "qemu",
    "description": "The type of virtualization to use for ironic compute nodes."
  }
}
