{
  "start_miredo": {
    "type": "bool",
    "default": true,
    "description": "Whether to start miredo on system startup. Set to true to enable, false to disable."
  },
  "daemon_args": {
    "type": "string",
    "default": "",
    "description": "Additional command-line arguments passed to the miredo daemon."
  }
}
