# clex configuration file
# Generated via Jinja2 template engine

# Shell program used to execute commands invoked by clex.
# If not set, the shell from the user database is used instead.
SHELL={{ shell | default('') }}

# User's home directory.
# If not set, the directory from the user database is used instead.
HOME={{ home | default('') }}