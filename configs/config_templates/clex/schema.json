{
  "shell": {
    "type": "string",
    "default": "",
    "description": "Shell program used to execute commands invoked by clex. If not set, the shell from the user database is used instead."
  },
  "home": {
    "type": "string",
    "default": "",
    "description": "User's home directory. If not set, the directory from the user database is used instead."
  }
}
