{
  "command": {
    "type": "string",
    "default": "date",
    "description": "The command to be executed within the sdate environment."
  },
  "epoch": {
    "type": "string",
    "default": "1993-09",
    "description": "The epoch date for the Eternal September, formatted as YYYY-MM."
  },
  "covid_mode": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable COVID mode."
  },
  "covid_variant": {
    "type": "string",
    "default": "",
    "description": "The COVID variant string used to set the epoch in COVID mode."
  },
  "library": {
    "type": "string",
    "default": "/usr/lib/libsdate/libsdate.so",
    "description": "Path to the shared library containing the wrapper functions."
  }
}
