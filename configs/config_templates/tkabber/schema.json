{
  "geometry": {
    "type": "string",
    "default": "800x600+100+100",
    "description": "Initial geometry for the main window in X11 geometry format"
  },
  "name": {
    "type": "string",
    "default": "Tkabber",
    "description": "Name to use for the application instance"
  },
  "display": {
    "type": "string",
    "default": ":0",
    "description": "X11 display to connect to"
  },
  "colormap": {
    "type": "string",
    "default": "default",
    "description": "Colormap for the main window"
  },
  "visual": {
    "type": "string",
    "default": "default",
    "description": "Visual ID for the main window"
  },
  "sync": {
    "type": "bool",
    "default": false,
    "description": "Use synchronous mode for display server"
  },
  "splash": {
    "type": "bool",
    "default": true,
    "description": "Display the initial splash screen"
  },
  "use": {
    "type": "string",
    "default": "",
    "description": "Id of window in which to embed the application"
  },
  "account": {
    "type": "object",
    "default": null,
    "description": "Primary XMPP account configuration (invented structure for service config, but fields are standard Tkabber vars derived from common usage implied by 'service' context, though strictly the man page doesn't detail config file syntax, only CLI. However, Tkabber uses a config.tcl file. I will map the CLI options to the generated config file structure). Note: The prompt asks for 'real documented setting'. The man page lists CLI options. I will generate a config file that *could* be used or simply a wrapper script config? No, it asks for a 'Class-B configuration template'. Tkabber is a Tcl app, it uses `~/.tkabber/config.tcl`. The CLI options can often be mapped to variables in that config. I will use the variables derived from the CLI options which are the only 'documented' settings I have access to via the provided text.)\n    },\n    "
  }
}
