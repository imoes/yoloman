{
  "daemon.conf_path": "/etc/daemon.conf",
  "generic_options": "core,verbose=1",
  "services": [
    "name=webserver,command=/usr/local/bin/nginx,respawn,errlog=local0.err",
    "name=worker,command=/usr/local/bin/worker.sh,respawn,attempts=3,delay=60"
  ]
}
