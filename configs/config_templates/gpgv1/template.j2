{#
  GnuPG Verifier 1 (gpgv1) Configuration Template
  ================================================
  This template generates the command-line arguments passed to the `gpgv` command.
  gpgv is a minimal program that verifies the signatures of files. It is primarily used
  to check the authenticity of Debian/Ubuntu packages or standalone files.

  The configuration is driven by the `gpgv_args` variable, which is a list of strings.
  Each string represents a single flag or argument (e.g., '--keyring', '/path/to/keyring').

  Self-documenting note: Do not edit this file directly in production if it is managed by Ansible.
  Instead, update the `gpgv_args` variable in your inventory or group vars.
#}

# gpgv command-line arguments
# Generated by Ansible Jinja2 template for gpgv1
{% for arg in gpgv_args | default([]) %}
{{ arg }}
{% endfor %}