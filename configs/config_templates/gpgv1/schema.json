{
  "gpgv_args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments passed directly to gpgv. Each item must be a string representing a single argument or flag (e.g., '--quiet', '--keyring=/path/to/keyring'). Arguments must match documented gpgv options and be valid for command-line use. Order is preserved in rendered output."
  }
}
