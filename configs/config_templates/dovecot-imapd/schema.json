{
  "imap_port": {
    "type": "string",
    "default": "143/tcp",
    "description": "IMAP service port and protocol"
  },
  "imaps_port": {
    "type": "string",
    "default": "993/tcp",
    "description": "IMAPS (secure IMAP) service port and protocol"
  }
}
