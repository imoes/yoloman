{
  "allow_acl_path": {
    "type": "string",
    "default": "/etc/jesred.acl",
    "description": "Path to the ACL file containing IP addresses for which URL rewriting is allowed or denied."
  },
  "rules_path": {
    "type": "string",
    "default": "/etc/jesred.rules",
    "description": "Path to the main rules file for URL rewriting."
  },
  "redirect_log_path": {
    "type": "string",
    "default": "/var/log/squid/jesred-redirect.log",
    "description": "Path to the log file for general, error, and debug messages."
  },
  "debug_mode": {
    "type": "bool",
    "default": false,
    "description": "Enable debug logging if the DEBUG option was compiled in."
  }
}
