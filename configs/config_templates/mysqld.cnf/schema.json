{
  "mysql_user": {
    "type": "string",
    "default": "mysql",
    "description": "User account under which mysqld runs."
  },
  "mysql_gid": {
    "type": "string",
    "default": "mysql",
    "description": "Group ID for the MySQL user (often same as mysql_user)."
  },
  "mysql_datadir": {
    "type": "string",
    "default": "/var/lib/mysql",
    "description": "Directory where MySQL stores its data files."
  },
  "mysql_tmpdir": {
    "type": "string",
    "default": "/tmp",
    "description": "Directory for temporary files."
  },
  "mysql_socket": {
    "type": "string",
    "default": "/var/run/mysqld/mysqld.sock",
    "description": "Unix socket file for local connections."
  },
  "mysql_bind_address": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address to bind to. Use '0.0.0.0' to accept all remote connections."
  },
  "mysql_port": {
    "type": "number",
    "default": 3306,
    "description": "TCP/IP port number to listen on."
  },
  "mysql_character_set_server": {
    "type": "string",
    "default": "utf8mb4",
    "description": "Default character set for new databases and tables."
  },
  "mysql_collation_server": {
    "type": "string",
    "default": "utf8mb4_general_ci",
    "description": "Default collation for new databases and tables."
  },
  "mysql_time_zone": {
    "type": "string",
    "default": "SYSTEM",
    "description": "Default server time zone. Use 'SYSTEM' to inherit the OS time zone."
  },
  "mysql_innodb_buffer_pool_size": {
    "type": "string",
    "default": "256M",
    "description": "Size of InnoDB buffer pool (memory for caching data and indexes)."
  },
  "mysql_innodb_log_file_size": {
    "type": "string",
    "default": "48M",
    "description": "Size of each InnoDB log file."
  },
  "mysql_innodb_flush_log_at_trx_commit": {
    "type": "number",
    "default": 1,
    "description": "Controls durability/speed tradeoff (1= safest, 0/2= faster)."
  },
  "mysql_innodb_file_per_table": {
    "type": "boolean",
    "default": true,
    "description": "Store each InnoDB table in its own .ibd file."
  },
  "mysql_innodb_flush_method": {
    "type": "string",
    "default": "fdatasync",
    "description": "How InnoDB flushes data to disk (common values: fdatasync, O_DIRECT)."
  },
  "mysql_query_cache_type": {
    "type": "string",
    "default": "OFF",
    "description": "Query cache mode (ON/OFF/DEMAND). Deprecated in MySQL 8.0+."
  },
  "mysql_query_cache_size": {
    "type": "string",
    "default": "0",
    "description": "Total size of query cache (set >0 to enable). Deprecated in MySQL 8.0+."
  },
  "mysql_query_cache_limit": {
    "type": "string",
    "default": "256K",
    "description": "Maximum size of individual query results that can be cached."
  },
  "mysql_max_connections": {
    "type": "number",
    "default": 151,
    "description": "Maximum number of simultaneous client connections."
  },
  "mysql_max_allowed_packet": {
    "type": "string",
    "default": "64M",
    "description": "Maximum size of one packet or any generated/intermediate string."
  },
  "mysql_thread_cache_size": {
    "type": "number",
    "default": 15,
    "description": "Number of threads to keep in cache for reuse."
  },
  "mysql_thread_stack": {
    "type": "string",
    "default": "262144",
    "description": "Stack size for each thread (in bytes)."
  },
  "mysql_general_log": {
    "type": "boolean",
    "default": false,
    "description": "Enable general query log (logs all statements)."
  },
  "mysql_general_log_file": {
    "type": "string",
    "default": "/var/log/mysql/general.log",
    "description": "Path to general query log file."
  },
  "mysql_slow_query_log": {
    "type": "boolean",
    "default": false,
    "description": "Enable slow query log (logs queries taking longer than long_query_time)."
  },
  "mysql_slow_query_log_file": {
    "type": "string",
    "default": "/var/log/mysql/slow.log",
    "description": "Path to slow query log file."
  },
  "mysql_long_query_time": {
    "type": "number",
    "default": 2,
    "description": "Threshold (seconds) for slow queries."
  },
  "mysql_log_error": {
    "type": "string",
    "default": "/var/log/mysql/error.log",
    "description": "Path to error log file. Set to empty string to disable."
  },
  "mysql_log_bin": {
    "type": "string",
    "default": "/var/log/mysql/mysql-bin",
    "description": "Base name for binary log files (includes path, excludes extension)."
  },
  "mysql_binlog_format": {
    "type": "string",
    "default": "ROW",
    "description": "Binary log format: STATEMENT, ROW, or MIXED."
  },
  "mysql_expire_logs_days": {
    "type": "number",
    "default": 7,
    "description": "Automatically purge binary logs older than this many days."
  },
  "mysql_max_binlog_size": {
    "type": "string",
    "default": "100M",
    "description": "Maximum size of a single binary log file."
  },
  "mysql_binlog_cache_size": {
    "type": "string",
    "default": "32K",
    "description": "Size of cache to hold binary log transactions."
  },
  "mysql_replication_slaves": {
    "type": "list",
    "default": [],
    "description": "List of replication slave configurations. Each entry defines a replica.",
    "items": {
      "host": {
        "type": "string",
        "default": "",
        "description": "Hostname or IP of the master server (for reference only in config comment)."
      },
      "server_id": {
        "type": "number",
        "default": 2,
        "description": "Unique server ID for this instance (required for replication)."
      },
      "replicate_do_db": {
        "type": "list",
        "default": [],
        "description": "List of database names to replicate (optional)."
      },
      "master_host": {
        "type": "string",
        "default": "",
        "description": "Master server hostname (if used in dynamic config)."
      }
    }
  },
  "mysql_ssl_ca": {
    "type": "string",
    "default": "",
    "description": "Path to SSL certificate authority file (leave blank to disable SSL)."
  },
  "mysql_ssl_cert": {
    "type": "string",
    "default": "",
    "description": "Path to SSL server certificate file."
  },
  "mysql_ssl_key": {
    "type": "string",
    "default": "",
    "description": "Path to SSL server private key file."
  },
  "mysql_local_infile": {
    "type": "boolean",
    "default": false,
    "description": "Enable LOAD DATA LOCAL INFILE (set true only if needed; potential security risk)."
  },
  "mysql_join_buffer_size": {
    "type": "string",
    "default": "256K",
    "description": "Buffer size for full joins (non-indexed joins)."
  },
  "mysql_sort_buffer_size": {
    "type": "string",
    "default": "256K",
    "description": "Buffer size for sorting operations per thread."
  },
  "mysql_read_buffer_size": {
    "type": "string",
    "default": "128K",
    "description": "Buffer size for sequential table scans per thread."
  },
  "mysql_read_rnd_buffer_size": {
    "type": "string",
    "default": "256K",
    "description": "Buffer size for random reads after a sort operation."
  },
  "mysql_tmp_table_size": {
    "type": "string",
    "default": "64M",
    "description": "Maximum size of in-memory temporary tables."
  },
  "mysql_max_heap_table_size": {
    "type": "string",
    "default": "64M",
    "description": "Maximum size of user-created MEMORY tables."
  },
  "mysql_open_files_limit": {
    "type": "number",
    "default": 1024,
    "description": "Maximum number of open files for mysqld."
  },
  "mysql_table_open_cache": {
    "type": "number",
    "default": 2000,
    "description": "Number of table definitions to cache."
  },
  "mysql_thread_concurrency": {
    "type": "number",
    "default": 0,
    "description": "Suggested number of concurrent threads (0 = auto; only effective on older MySQL versions)."
  },
  "mysql_default_storage_engine": {
    "type": "string",
    "default": "InnoDB",
    "description": "Default storage engine for new tables."
  }
}
