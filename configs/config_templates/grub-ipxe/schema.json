{
  "GRUB_DEVICE_BOOT": {
    "type": "string",
    "default": "/dev/disk/by-uuid/placeholder-uuid",
    "description": "Boot device path used by grub-mkconfig_lib functions (e.g., /dev/sda1, /dev/disk/by-uuid/...). Must be a valid block device path."
  }
}
