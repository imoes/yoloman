{
  "directories": {
    "type": "list",
    "default": [],
    "description": "List of directory paths to scan for Git repositories"
  },
  "mode": {
    "type": "string",
    "default": "run",
    "description": "Application start mode (e.g., run, tray)"
  },
  "recursive_depth": {
    "type": "number",
    "default": 0,
    "description": "Maximum depth for recursive directory scanning; 0 means no recursion"
  },
  "log_level": {
    "type": "string",
    "default": "error",
    "description": "Logging level; one of trace, debug, info, warn, error",
    "enum": [
      "trace",
      "debug",
      "info",
      "warn",
      "error"
    ]
  },
  "quick": {
    "type": "bool",
    "default": false,
    "description": "Run in quick mode: no GUI, fetches/pulls remote upstream"
  }
}
