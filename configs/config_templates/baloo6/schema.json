{
  "architecture": {
    "type": "string",
    "default": "x86_64-linux-gnu",
    "description": "Target architecture triplet used in the path to the Baloo executable (e.g., x86_64-linux-gnu, aarch64-linux-gnu)."
  }
}
