{
  "archive_path": {
    "type": "string",
    "default": "/var/lib/smpq/archive.mpq",
    "description": "Path to the MPQ archive file"
  },
  "action": {
    "type": "string",
    "default": "info",
    "description": "Action to perform: create, append, delete, extract, list, info",
    "enum": [
      "create",
      "append",
      "rename",
      "list",
      "extract",
      "info",
      "help",
      "version"
    ]
  },
  "mpq_version": {
    "type": "number",
    "default": 4,
    "description": "MPQ archive version (1, 2, 3, or 4)"
  },
  "compression_method": {
    "type": "string",
    "default": "zlib",
    "description": "Compression method: none, implode, pkware, huffman, adpcm_mono, adpcm_stereo, zlib, bz2, sparse, lzma, huffman+adpcm_mono, huffman+adpcm_stereo, zlib+pkware, bz2+pkware, sparse+zlib, sparse+pkware, sparse+bz2, sparse+zlib+pkware, sparse+bz2+pkware",
    "enum": [
      "none",
      "implode",
      "pkware",
      "huffmann",
      "adpcm_mono",
      "adpcm_stereo",
      "zlib",
      "sparse",
      "lzma",
      "huffmann+adpcm_mono",
      "huffmann+adpcm_stereo",
      "zlib+pkware",
      "sparse+zlib",
      "sparse+pkware",
      "sparse+zlib+pkware"
    ]
  },
  "locale_id": {
    "type": "number",
    "default": 0,
    "description": "Locale ID (default neutral=0)"
  },
  "force_overwrite": {
    "type": "bool",
    "default": false,
    "description": "Enable overwrite file(s)"
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Be quiet, do not show any output"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output"
  },
  "sector_crc": {
    "type": "bool",
    "default": false,
    "description": "Store/Check CRC for each sector"
  },
  "no_system_listfiles": {
    "type": "bool",
    "default": false,
    "description": "Do not load system listfile(s)"
  },
  "no_archive_listfile": {
    "type": "bool",
    "default": false,
    "description": "Do not use/create archive listfile"
  },
  "no_attributes": {
    "type": "bool",
    "default": false,
    "description": "Do not allow using file attributes"
  },
  "max_file_count": {
    "type": "number",
    "default": 0,
    "description": "Maximum file count of archive (power of 2, 0 for autodetect)"
  },
  "encrypt": {
    "type": "bool",
    "default": false,
    "description": "Store as encrypted"
  },
  "fix_key": {
    "type": "bool",
    "default": false,
    "description": "Encryption key will be adjusted according to file size in the archive"
  },
  "deletion_marker": {
    "type": "bool",
    "default": false,
    "description": "Set deletion marker"
  },
  "single_unit": {
    "type": "bool",
    "default": false,
    "description": "Add file as single unit, cannot be encrypted"
  },
  "partial": {
    "type": "bool",
    "default": false,
    "description": "Archive is partial (default: autodetect)"
  },
  "not_encrypted": {
    "type": "bool",
    "default": false,
    "description": "Archive is not encrypted (default: autodetect)"
  },
  "prefix_archives": {
    "type": "list",
    "default": [],
    "description": "List of prefix:archive pairs for opening more (patched) archives with directory prefix. Format: 'prefix:archive'. Items are strings representing the full prefix:archive argument value. No nested objects; this is a flat list of strings."
  },
  "listfile": {
    "type": "string",
    "default": "",
    "description": "Additional external listfile"
  },
  "files": {
    "type": "list",
    "default": [],
    "description": "List of files to create, append, delete, or extract."
  }
}
