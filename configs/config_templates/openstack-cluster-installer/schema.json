{
  "oci_document_root": {
    "type": "string",
    "default": "/usr/share/openstack-cluster-installer",
    "description": "The document root directory for the OCI (OpenStack Cluster Installer) web interface alias /oci."
  },
  "oci_inc_dir": {
    "type": "string",
    "default": "/usr/share/openstack-cluster-installer/inc",
    "description": "The subdirectory containing internal/inc files for the OCI web interface, which should be denied access."
  },
  "webapp_data_dir": {
    "type": "string",
    "default": "/var/lib/openstack-cluster-installer",
    "description": "The data directory for the openstack-cluster-installer web application, served under the alias /openstack-cluster-installer."
  }
}
