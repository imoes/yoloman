{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether faketime commands should be included in the output"
  },
  "commands": {
    "type": "list",
    "default": [],
    "description": "List of faketime command definitions to include in the config file (typically shell script or init config usage context). Each item configures one faketime invocation."
  }
}
