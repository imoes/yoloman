{
  "packup.external_solver": {
    "type": "string",
    "default": "minisat+ -ansi",
    "description": "Command for the external solver used by packup."
  },
  "packup.multiplication_string": {
    "type": "string",
    "default": "*",
    "description": "String between coefficients and variables when communicating to the solver."
  },
  "packup.temporary_directory": {
    "type": "string",
    "default": "/tmp",
    "description": "Directory where temporary files are created."
  },
  "packup.leave_temporary_files": {
    "type": "bool",
    "default": false,
    "description": "Do not delete temporary files."
  },
  "packup.criterion": {
    "type": "string",
    "default": "trendy",
    "description": "Optimization criterion. Use 'trendy' for -t, 'paranoid' for -p, or a custom lexicographic combination like '-removed,-changed'.",
    "enum": [
      "trendy",
      "paranoid",
      "custom"
    ]
  }
}
