{
  "hime_table_dir": {
    "type": "string",
    "default": "",
    "description": "Custom directory for input method tables (overrides /usr/share/hime/table)"
  },
  "hime_connect_msg_on": {
    "type": "bool",
    "default": false,
    "description": "Enable display of socket connection messages"
  },
  "hime_dbg_tmp": {
    "type": "bool",
    "default": false,
    "description": "Enable additional debug messages"
  },
  "hime_err_coredump": {
    "type": "bool",
    "default": false,
    "description": "Enable core dump on error (for debugging)"
  },
  "hime_im_client_no_auto_exec": {
    "type": "bool",
    "default": false,
    "description": "Disable auto-evoke in hime protocol"
  },
  "hime_output_filter": {
    "type": "string",
    "default": "",
    "description": "Path or name of output filter to process data before IM server sends to clients"
  },
  "locale": {
    "type": "string",
    "default": "zh_TW.UTF-8",
    "description": "Locale setting (LC_CTYPE), typically zh_TW.UTF-8, zh_CN.UTF-8, ja_JP.UTF-8, etc."
  }
}
