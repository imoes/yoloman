{
  "NPROC": {
    "type": "number",
    "default": 7,
    "description": "Number of processes for the FastCGI server"
  },
  "SOCKET": {
    "type": "string",
    "default": "/run/llng-fastcgi-server/llng-fastcgi.sock",
    "description": "Unix socket path for the FastCGI server"
  },
  "PID": {
    "type": "string",
    "default": "/run/llng-fastcgi-server/llng-fastcgi-server.pid",
    "description": "Path to the PID file for the FastCGI server"
  },
  "USER": {
    "type": "string",
    "default": "www-data",
    "description": "User to run the FastCGI server process as"
  },
  "GROUP": {
    "type": "string",
    "default": "www-data",
    "description": "Group to run the FastCGI server process as"
  }
}
