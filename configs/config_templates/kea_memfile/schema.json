{
 "auth": {
  "description": "Authentication configuration for database access (not used by memfile, but accepted for config compatibility). Leave empty unless overriding.",
  "type": "object"
 },
 "db_interval": {
  "default": 3600,
  "description": "Interval (seconds) between periodic writes of lease data to the file (only effective when persist=true).",
  "type": "number"
 },
 "hooks_libraries": {
  "default": [],
  "description": "List of hook libraries to load (e.g., for statistics, logging, or custom logic). Each element defines a library to load and optional parameters.",
  "items": {
   "library": {
    "default": "",
    "description": "Path to the shared library (e.g., /usr/lib/kea/hooks/liblog.so).",
    "type": "string"
   },
   "parameters": {
    "default": {},
    "description": "JSON object with library-specific configuration parameters.",
    "type": "object"
   }
  },
  "type": "list"
 },
 "lease_database_lfc_interval": {
  "default": 600,
  "description": "Lease File Cleanup (LFC) interval in seconds; how often KEA compacts and rewrites the lease file.",
  "type": "number"
 },
 "lease_database_name": {
  "default": "/var/lib/kea/kea-leases4.csv",
  "description": "Path to the CSV file used for lease persistence when persist=true.",
  "type": "string"
 },
 "lease_database_persist": {
  "default": true,
  "description": "Whether to persist lease data to disk (file) even though it's in-memory storage. Must be true to enable file-based backup.",
  "type": "bool"
 },
 "loggers": {
  "default": [
   {
    "debuglevel": 0,
    "name": "kea-dhcp4",
    "severity": "INFO"
   }
  ],
  "description": "Configure logging for KEA components. Each object defines a logger name, severity, and output options.",
  "items": {
   "debuglevel": {
    "default": 0,
    "description": "Debug verbosity level (0–99); higher = more verbose; only effective if compiled with debug support.",
    "type": "number"
   },
   "flush": {
    "default": true,
    "description": "Whether to flush output immediately after each log entry (slower but safer).",
    "type": "bool"
   },
   "max_size": {
    "default": 10485760,
    "description": "Maximum size (bytes) before log rotation (only applies to file outputs).",
    "type": "number"
   },
   "max_versions": {
    "default": 5,
    "description": "Number of rotated log files to retain.",
    "type": "number"
   },
   "name": {
    "default": "kea-dhcp4",
    "description": "Logger name (e.g., 'kea-dhcp4', 'kea-dhcp4.dhcpsrv').",
    "type": "string"
   },
   "output": {
    "default": "stdout",
    "description": "Output destination: 'stdout', 'stderr', or a file path (e.g., '/var/log/kea/kea.log').",
    "type": "string"
   },
   "severity": {
    "default": "INFO",
    "description": "Logging level: DEBUG, INFO, WARN, ERROR, FATAL.",
    "type": "string"
   }
  },
  "type": "list"
 },
 "reconnect_wait_time": {
  "default": 0,
  "description": "Seconds to wait before reconnecting to the lease database after a disconnect (0 = no delay; only relevant for non-memfile DBs; kept for structure).",
  "type": "number"
 }
}
