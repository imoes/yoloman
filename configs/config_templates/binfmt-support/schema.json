{
  "name": {
    "type": "string",
    "default": "binfmt-support",
    "description": "The name of the service/script"
  },
  "desc": {
    "type": "string",
    "default": "additional executable binary formats",
    "description": "The description of the service displayed in log messages"
  }
}
