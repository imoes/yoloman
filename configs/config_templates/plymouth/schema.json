{
  "plymouth_service_name": {
    "type": "string",
    "default": "plymouth",
    "description": "The name of the service provided by this init script."
  },
  "plymouth_required_start": {
    "type": "string",
    "default": "udev $remote_fs $all",
    "description": "Services that must be started before this one."
  },
  "plymouth_required_stop": {
    "type": "string",
    "default": "$remote_fs",
    "description": "Services that must be stopped before this one."
  },
  "plymouth_should_start": {
    "type": "string",
    "default": "$x-display-manager",
    "description": "Services that this one should start if they are present."
  },
  "plymouth_should_stop": {
    "type": "string",
    "default": "$x-display-manager",
    "description": "Services that this one should stop if they are present."
  },
  "plymouth_default_start": {
    "type": "string",
    "default": "2 3 4 5",
    "description": "Runlevels at which the service should start."
  },
  "plymouth_default_stop": {
    "type": "string",
    "default": "0 6",
    "description": "Runlevels at which the service should stop."
  },
  "plymouth_short_description": {
    "type": "string",
    "default": "Stop plymouth during boot and start it on shutdown",
    "description": "Short description of the service."
  },
  "plymouth_path": {
    "type": "string",
    "default": "/sbin:/bin:/usr/sbin:/usr/bin",
    "description": "PATH environment variable for the script."
  },
  "plymouth_name": {
    "type": "string",
    "default": "plymouth",
    "description": "Internal name variable used for configuration file path."
  },
  "plymouth_desc": {
    "type": "string",
    "default": "Boot splash manager",
    "description": "Description of the service."
  },
  "plymouth_splash_default": {
    "type": "bool",
    "default": true,
    "description": "Default value for showing the splash screen, unless overridden by kernel cmdline."
  }
}
