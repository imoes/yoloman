{
  "exec_command": {
    "type": "string",
    "default": "budgie-extras-daemon",
    "description": "The command to execute for the Budgie-Extras background service daemon."
  },
  "terminal": {
    "type": "bool",
    "default": false,
    "description": "Whether the application should be run in a terminal window."
  },
  "categories": {
    "type": "string",
    "default": "GNOME;GTK;System;Core;",
    "description": "Desktop entry categories, semicolon-separated."
  },
  "no_display": {
    "type": "bool",
    "default": true,
    "description": "Whether the application should be hidden from the menu."
  },
  "startup_notify": {
    "type": "bool",
    "default": true,
    "description": "Whether startup notification should be enabled."
  },
  "auto_restart": {
    "type": "bool",
    "default": true,
    "description": "Whether the daemon should auto-restart on failure."
  }
}
