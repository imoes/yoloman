{
  "prefix": {
    "type": "string",
    "default": "/usr",
    "description": "Base installation prefix where appdata, metainfo, applications, and icons directories are located"
  },
  "output_dir": {
    "type": "string",
    "default": "/usr/share/app-info",
    "description": "Output directory where generated AppStream XML files and icons will be placed"
  },
  "applications": {
    "type": "list",
    "default": [],
    "description": "List of application (desktop file) base names to process; if empty, all applications under prefix are included"
  }
}
