{
  "log_path": {
    "type": "string",
    "default": "/var/log/testdisk.log",
    "description": "Path to the log file created by the /log option."
  },
  "debug_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable debug information output via /debug."
  },
  "target_device": {
    "type": "string",
    "default": "/dev/sda",
    "description": "The device, image file (.dd or .e01), or partition to scan and repair."
  },
  "list_partitions": {
    "type": "bool",
    "default": false,
    "description": "Whether to display current partitions via /list."
  },
  "dump_raw": {
    "type": "bool",
    "default": false,
    "description": "Whether to dump raw sectors via /dump."
  }
}
