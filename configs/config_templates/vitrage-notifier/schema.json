{
  "project_name": {
    "type": "string",
    "default": "vitrage",
    "description": "The project name for the OpenStack service, used for naming directories, PID files, log files, and process names."
  }
}
