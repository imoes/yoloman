{
  "qt_qpa_platformtheme_default": {
    "type": "string",
    "default": "qt5ct",
    "description": "The default platform theme to use for Qt5 applications when QT_QPA_PLATFORMTHEME is not already set and KDE is not running."
  }
}
