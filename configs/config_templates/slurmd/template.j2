# Additional command-line options passed to the slurmd daemon.
# These are appended directly to the SLURMD_OPTIONS environment variable or
# command line argument used to start the slurmd service.
# Ensure options are space-separated if multiple are provided.
SLURMD_OPTIONS="{{ (slurm | default({})).slurmd_options | default('') | trim }}"