{
  "buffer_size": {
    "type": "string",
    "default": "",
    "description": "Maximum buffer size in bytes; supports k/M/G suffixes (e.g., '10m' for 10 MB)"
  },
  "delay_time": {
    "type": "string",
    "default": "",
    "description": "Delay duration; supports s/m/h suffixes (e.g., '5s' for 5 seconds)"
  }
}
