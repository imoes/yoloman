{
  "track_list": {
    "type": "list",
    "default": [],
    "description": "List of tracks to inspect; cd5 auto-detects tracks, so this is unused at runtime but retained for documentation clarity. Each item describes a CD track's metadata (e.g., from a TOC dump)."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose diagnostics. Not supported by upstream cd5 \u2014 kept for compatibility with future or patched variants."
  }
}
