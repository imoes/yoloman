# Laurel Configuration Template
# Auto-generated by Ansible. Do not edit manually.

# active = {{ active | default('yes') }}
# desc: Whether the service is active (yes/no)
active = {{ active | default('yes') }}

# direction = {{ direction | default('out') }}
# desc: The direction of the event stream (out/in/out-in)
direction = {{ direction | default('out') }}

# type = {{ type | default('always') }}
# desc: The type of process (always/on-demand)
type = {{ type | default('always') }}

# format = {{ format | default('string') }}
# desc: The format of the output (string/json)
format = {{ format | default('string') }}

# path = {{ path | default('/usr/sbin/laurel') }}
# desc: The path to the laurel binary
path = {{ path | default('/usr/sbin/laurel') }}

# args = --config /etc/laurel/config.toml
# desc: Fixed arguments passed to the laurel binary
args = --config /etc/laurel/config.toml