{
  "input_chm_file": {
    "type": "string",
    "default": "/path/to/file.chm",
    "description": "Absolute path to the input CHM file to decompress."
  },
  "output_directory": {
    "type": "string",
    "default": "/tmp/chm-extract",
    "description": "Absolute path to the directory where CHM contents will be extracted. Will be created if missing."
  }
}
