{
  "key_file": {
    "type": "string",
    "default": "/etc/fence_virt.key",
    "description": "Path to the key file used for multicast authentication"
  },
  "address": {
    "type": "string",
    "default": "225.0.0.12",
    "description": "Multicast group address for listener communication"
  },
  "interface": {
    "type": "string",
    "default": "virbr0",
    "description": "Network interface to bind the multicast listener to (system-specific)"
  },
  "uri": {
    "type": "string",
    "default": "qemu:///system",
    "description": "libvirt connection URI for backend operations"
  }
}
