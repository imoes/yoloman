{
  "PROJECT_NAME": {
    "type": "string",
    "default": "magnum",
    "description": "The OpenStack project name."
  },
  "SYSTEM_USER": {
    "type": "string",
    "default": "magnum",
    "description": "The system user running the service."
  },
  "SYSTEM_GROUP": {
    "type": "string",
    "default": "magnum",
    "description": "The system group running the service."
  }
}
