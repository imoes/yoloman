{
  "project_name": {
    "type": "string",
    "default": "neutron",
    "description": "The OpenStack project name for directory and process naming."
  },
  "rabbit_processname": {
    "type": "string",
    "default": "neutron-openvswitch-agent",
    "description": "The process name exposed to RabbitMQ for monitoring."
  },
  "system_user": {
    "type": "string",
    "default": "neutron",
    "description": "The system user that runs the daemon."
  },
  "system_group": {
    "type": "string",
    "default": "neutron",
    "description": "The system group that runs the daemon."
  }
}
