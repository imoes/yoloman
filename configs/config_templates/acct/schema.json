{
  "default_log_retention_days": {
    "type": "number",
    "default": 30,
    "description": "Number of days to retain old accounting logs (used by savelog -c)"
  },
  "account_log_directory": {
    "type": "string",
    "default": "/var/log/account",
    "description": "Directory where accounting log files are stored"
  }
}
