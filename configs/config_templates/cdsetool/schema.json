{
  "concurrency": {
    "type": "number",
    "default": 1,
    "description": "Number of concurrent connections for downloads (positive integer)"
  },
  "overwrite_existing": {
    "type": "bool",
    "default": false,
    "description": "Whether to overwrite existing downloaded files"
  },
  "search_terms": {
    "type": "list",
    "default": [],
    "description": "List of search term expressions in key=value format (e.g., 'platform=web' 'instrument=Sentinel-2')"
  }
}
