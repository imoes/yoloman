{
  "ntsc_mode": {
    "type": "bool",
    "default": false,
    "description": "Enable NTSC mode instead of default PAL mode"
  },
  "decode": {
    "type": "bool",
    "default": false,
    "description": "Decode DV stream instead of encoding"
  },
  "verify": {
    "type": "bool",
    "default": false,
    "description": "Verify DV stream integrity (crc check)"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output"
  },
  "recover": {
    "type": "bool",
    "default": false,
    "description": "Recover corrupted data from DV stream"
  },
  "enable_audio": {
    "type": "bool",
    "default": false,
    "description": "Enable audio data channel (uncompressed audio area used for user data)"
  },
  "set_backup_title": {
    "type": "string",
    "default": "",
    "description": "Set backup title metadata in DV stream"
  },
  "set_picture": {
    "type": "string",
    "default": "",
    "description": "Path to PPM file to embed as preview picture"
  },
  "prefix": {
    "type": "number",
    "default": 0,
    "description": "Number of leading null frames to prefix backup stream"
  },
  "test_count": {
    "type": "number",
    "default": 0,
    "description": "Run test mode with specified number of frames"
  }
}
