{
  "server_ip": {
    "type": "string",
    "default": "kwartz.lyceejeanbart.fr",
    "description": "The primary server hostname or IP address for the Kwartz cloud service."
  },
  "alt_ip": {
    "type": "string",
    "default": "serveur.lycee.jb",
    "description": "An alternative server hostname or IP address for the Kwartz cloud service."
  },
  "tls_port": {
    "type": "number",
    "default": 8443,
    "description": "The TLS port used for connecting to the Kwartz cloud service."
  },
  "davpath": {
    "type": "string",
    "default": "owncloud/remote.php/webdav",
    "description": "The WebDAV path suffix for the cloud storage service."
  }
}
