{
  "addon_sha256": {
    "default": "",
    "description": "The sha256sum value of the addon to inject.",
    "type": "string"
  },
  "cert_source": {
    "default": "/usr/share/sslsniff/certs/wildcard",
    "description": "Path to CA cert/key file or directory containing certificates.",
    "type": "string"
  },
  "firefox_update_location": {
    "default": "",
    "description": "Location of any Firefox XML update files.",
    "type": "string"
  },
  "http_port": {
    "default": 0,
    "description": "Port to listen on for HTTP interception. Set to 0 to disable.",
    "type": "number"
  },
  "intercept_addon_update": {
    "default": "",
    "description": "URL to intercept Mozilla Addon Updates.",
    "type": "string"
  },
  "intercept_browsers": {
    "description": "Comma-separated list of browsers to intercept (ff,ie,safari,opera).",
    "enum": [
      "ff",
      "ie",
      "safari",
      "opera"
    ],
    "type": "string",
    "enum_open": true
  },
  "intermediary_certificates": {
    "default": "",
    "description": "Location of any intermediary certificates.",
    "type": "string"
  },
  "log_file": {
    "default": "/tmp/sslsniff.log",
    "description": "File to log activity to.",
    "type": "string"
  },
  "log_only_post": {
    "default": false,
    "description": "Only log HTTP POSTs.",
    "type": "bool"
  },
  "mode": {
    "default": "authority",
    "description": "Mode of operation: 'authority' or 'targeted'.",
    "enum": [
      "authority",
      "targeted"
    ],
    "type": "string"
  },
  "ocsp_deny": {
    "default": false,
    "description": "Deny OCSP requests for our certificates.",
    "type": "bool"
  },
  "ssl_port": {
    "default": 4433,
    "description": "Port to listen on for SSL interception.",
    "type": "number"
  }
}
