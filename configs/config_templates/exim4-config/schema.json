{
  "admin_email": {
    "type": "string",
    "default": "admin@example.com",
    "description": "Email address for the system administrator, used for outgoing mail from local users without explicit mappings."
  },
  "postmaster_email": {
    "type": "string",
    "default": "postmaster@example.com",
    "description": "Email address for postmaster, used for outgoing mail from other local users without explicit mappings."
  }
}
