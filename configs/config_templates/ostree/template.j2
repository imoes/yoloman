# =============================================================================
# OSTree Repository Configuration Template
# =============================================================================
# This template configures the `ostree repo` internal structure and remote
# repository connections. It follows the INI-style format used by ostree.
#
# Self-documenting: every directive is preceded by a comment explaining
# its purpose and default value.
# =============================================================================

[core]

# ---------------------------------------------------------------------------
# Directory name for the repository objects
# Default: objects
# ---------------------------------------------------------------------------
objectsdir={{ (repo | default({})).objectsdir | default('objects') }}

# ---------------------------------------------------------------------------
# Directory name for the repository refs
# Default: refs
# ---------------------------------------------------------------------------
refsdir={{ (repo | default({})).refsdir | default('refs') }}

# ---------------------------------------------------------------------------
# Directory name for the repository state
# Default: state
# ---------------------------------------------------------------------------
statedir={{ (repo | default({})).statedir | default('state') }}

# ---------------------------------------------------------------------------
# Directory name for the repository cache
# Default: cache
# ---------------------------------------------------------------------------
cachedir={{ (repo | default({})).cachedir | default('cache') }}

# ---------------------------------------------------------------------------
# Directory name for the repository tmp
# Default: tmp
# ---------------------------------------------------------------------------
tmpdir={{ (repo | default({})).tmpdir | default('tmp') }}

# ---------------------------------------------------------------------------
# Directory name for the repository symlinks
# Default: symlinks
# ---------------------------------------------------------------------------
symlinksdir={{ (repo | default({})).symlinksdir | default('symlinks') }}

# ---------------------------------------------------------------------------
# Directory name for the repository metadata
# Default: metadata
# ---------------------------------------------------------------------------
metadata={{ (repo | default({})).metadata | default('metadata') }}

# ---------------------------------------------------------------------------
# Directory name for the repository summary
# Default: summary
# ---------------------------------------------------------------------------
summary={{ (repo | default({})).summary | default('summary') }}

# ---------------------------------------------------------------------------
# Directory name for the repository cache2
# Default: cache2
# ---------------------------------------------------------------------------
cache2dir={{ (repo | default({})).cache2dir | default('cache2') }}

# ---------------------------------------------------------------------------
# Directory name for the repository cache2 metadata
# Default: metadata2
# ---------------------------------------------------------------------------
metadata2={{ (repo | default({})).metadata2 | default('metadata2') }}

# ---------------------------------------------------------------------------
# Directory name for the repository cache2 summary
# Default: summary2
# ---------------------------------------------------------------------------
summary2={{ (repo | default({})).summary2 | default('summary2') }}

# ---------------------------------------------------------------------------
# Directory name for the repository cache2 refs
# Default: refs2
# ---------------------------------------------------------------------------
refs2={{ (repo | default({})).refs2 | default('refs2') }}

# ---------------------------------------------------------------------------
# Directory name for the repository cache2 symlinks
# Default: symlinks2
# ---------------------------------------------------------------------------
symlinks2={{ (repo | default({})).symlinks2 | default('symlinks2') }}

# ---------------------------------------------------------------------------
# Remote repository configuration section
# Configures how this ostree repo connects to a remote source.
# The section name is the remote identifier.
# ---------------------------------------------------------------------------
[remote "{{ (remote | default({})).name | default('') }}"]

# ---------------------------------------------------------------------------
# URL of the remote repository
# This is the primary endpoint ostree uses to fetch objects and refs.
# ---------------------------------------------------------------------------
url={{ (remote | default({})).url | default('') }}

# ---------------------------------------------------------------------------
# GPG key file path(s) for verification, comma-separated
# Path to one or more GPG public keys used to verify repository signatures.
# ---------------------------------------------------------------------------
gpgkeypath={{ (remote | default({})).gpgkeypath | default('') }}

# ---------------------------------------------------------------------------
# Whether to verify GPG signatures
# Default: true
# ---------------------------------------------------------------------------
gpgverify={{ (remote | default({})).gpgverify | default('true') | lower }}

# ---------------------------------------------------------------------------
# Whether to allow anonymous access (no auth)
# If true, the client will not attempt authentication.
# Default: false
# ---------------------------------------------------------------------------
anonymous={{ (remote | default({})).anonymous | default('false') | lower }}

# ---------------------------------------------------------------------------
# Username for basic authentication
# Used when the remote requires HTTP Basic Auth.
# ---------------------------------------------------------------------------
username={{ (remote | default({})).username | default('') }}

# ---------------------------------------------------------------------------
# Password for basic authentication
# Used when the remote requires HTTP Basic Auth.
# ---------------------------------------------------------------------------
password={{ (remote | default({})).password | default('') }}

# ---------------------------------------------------------------------------
# Proxy URL for network access
# Overrides system settings; specifies a direct proxy for HTTP requests.
# ---------------------------------------------------------------------------
proxy={{ (remote | default({})).proxy | default('') }}

# ---------------------------------------------------------------------------
# System proxy URL for network access
# Uses the system environment variables (e.g. HTTP_PROXY) for connectivity.
# ---------------------------------------------------------------------------
systemproxy={{ (remote | default({})).systemproxy | default('') }}

# ---------------------------------------------------------------------------
# HTTP header to add to requests
# Custom HTTP header(s) to include in every request to the remote.
# ---------------------------------------------------------------------------
header={{ (remote | default({})).header | default('') }}

# ---------------------------------------------------------------------------
# HTTP user agent string
# Identifies this ostree client to the remote server.
# ---------------------------------------------------------------------------
useragent={{ (remote | default({})).useragent | default('') }}

# ---------------------------------------------------------------------------
# HTTP timeout in seconds
# Maximum time to wait for the HTTP response body.
# Default: 30
# ---------------------------------------------------------------------------
timeout={{ (remote | default({})).timeout | default('30') }}

# ---------------------------------------------------------------------------
# HTTP connect timeout in seconds
# Maximum time to wait for the TCP/TLS connection handshake.
# Default: 10
# ---------------------------------------------------------------------------
connecttimeout={{ (remote | default({})).connecttimeout | default('10') }}

# ---------------------------------------------------------------------------
# Whether to enable HTTP/2
# Default: false
# ---------------------------------------------------------------------------
http2={{ (remote | default({})).http2 | default('false') | lower }}

# ---------------------------------------------------------------------------
# Whether to enable HTTP caching
# Default: true
# ---------------------------------------------------------------------------
cache={{ (remote | default({})).cache | default('true') | lower }}

# ---------------------------------------------------------------------------
# Whether to verify SSL certificates
# Default: true
# ---------------------------------------------------------------------------
sslverify={{ (remote | default({})).sslverify | default('true') | lower }}

# ---------------------------------------------------------------------------
# CA certificate file path
# Path to a custom CA bundle for SSL verification.
# ---------------------------------------------------------------------------
sslcafile={{ (remote | default({})).sslcafile | default('') }}

# ---------------------------------------------------------------------------
# Client certificate file path
# Path to the client TLS certificate for mutual TLS authentication.
# ---------------------------------------------------------------------------
sslcertfile={{ (remote | default({})).sslcertfile | default('') }}

# ---------------------------------------------------------------------------
# Client private key file path
# Path to the client TLS private key.
# ---------------------------------------------------------------------------
sslkeyfile={{ (remote | default({})).sslkeyfile | default('') }}

# ---------------------------------------------------------------------------
# Client private key password
# Password to decrypt the client private key if it is encrypted.
# ---------------------------------------------------------------------------
sslkeypassword={{ (remote | default({})).sslkeypassword | default('') }}

# ---------------------------------------------------------------------------
# Whether to use TLS 1.3 only
# Default: false
# ---------------------------------------------------------------------------
tls13={{ (remote | default({})).tls13 | default('false') | lower }}

# ---------------------------------------------------------------------------
# Whether to use ECH (Encrypted Client Hello)
# Default: false
# ---------------------------------------------------------------------------
ech={{ (remote | default({})).ech | default('false') | lower }}

# ---------------------------------------------------------------------------
# Whether to use DNS over HTTPS
# Default: false
# ---------------------------------------------------------------------------
doh={{ (remote | default({})).doh | default('false') | lower }}

# ---------------------------------------------------------------------------
# DNS over HTTPS URL
# URL of the DoH resolver endpoint.
# ---------------------------------------------------------------------------
dohurl={{ (remote | default({})).dohurl | default('') }}

# ---------------------------------------------------------------------------
# Whether to use DNS over TLS
# Default: false
# ---------------------------------------------------------------------------
dot={{ (remote | default({})).dot | default('false') | lower }}

# ---------------------------------------------------------------------------
# DNS over TLS host
# Hostname of the DNS-over-TLS resolver.
# ---------------------------------------------------------------------------
dothost={{ (remote | default({})).dothost | default('') }}

# ---------------------------------------------------------------------------
# DNS over TLS port
# Default: 853
# ---------------------------------------------------------------------------
dotport={{ (remote | default({})).dotport | default('853') }}

# ---------------------------------------------------------------------------
# DNS over TLS server name
# SNI server name for the DNS-over-TLS connection.
# ---------------------------------------------------------------------------
dotservername={{ (remote | default({})).dotservername | default('') }}

# ---------------------------------------------------------------------------
# DNS over TLS verify mode
# Options: full, strict, none (or similar, depending on ostree version)
# Default: full
# ---------------------------------------------------------------------------
dotverify={{ (remote | default({})).dotverify | default('full') }}