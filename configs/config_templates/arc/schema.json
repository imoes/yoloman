{
  "commands": {
    "type": "list",
    "default": [],
    "description": "List of arc command lines to execute. Each item must be a single string representing one arc command invocation, including switches and arguments, as they would appear on the command line. Lines are written in order to /etc/arc.conf, one per line. Comments may be placed above or below the list via other means, but not embedded inline within items of this list per template constraints."
  }
}
