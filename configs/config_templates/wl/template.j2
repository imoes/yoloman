# Emacs XLwMenu label customizations
# Generated by Ansible Jinja2. All labels are UTF-8.
# Each variable uses the `default()` filter to provide a sensible fallback
# if the variable is not defined in the playbook or inventory.

# === Folder Menu Labels ===

# Label for the folder menu item
Emacs*XlwMenu.folder.labelString: {{ folder_label | default('フォルダ') }}

# Label for entering a folder
Emacs*XlwMenu.enterCurrentFolder.labelString: {{ enter_current_folder_label | default('フォルダへ移動') }}

# Label for previous folder
Emacs*XlwMenu.prevFolder.labelString: {{ prev_folder_label | default('前のフォルダ') }}

# Label for next folder
Emacs*XlwMenu.nextFolder.labelString: {{ next_folder_label | default('次のフォルダ') }}

# Label for checking latest messages
Emacs*XlwMenu.checkCurrentFolder.labelString: {{ check_current_folder_label | default('最新メッセージのチェック') }}

# Label for updating summary
Emacs*XlwMenu.syncCurrentFolder.labelString: {{ sync_current_folder_label | default('サマリを更新') }}

# Label for removing unsynchronized items
Emacs*XlwMenu.dropCurrentFolder.labelString: {{ drop_current_folder_label | default('未同期をなくす') }}

# Label for prefetching
Emacs*XlwMenu.prefetchCurrentFolder.labelString: {{ prefetch_current_folder_label | default('プリフェッチ') }}

# Label for sending queued messages
Emacs*XlwMenu.flushQueue.labelString: {{ flush_queue_label | default('キューにあるメッセージを送信') }}

# Label for adding a folder
Emacs*XlwMenu.addFolder.labelString: {{ add_folder_label | default('フォルダを追加') }}

# Label for adding a group
Emacs*XlwMenu.addGroup.labelString: {{ add_group_label | default('グループを追加') }}

# Label for setting a petname
Emacs*XlwMenu.setPetname.labelString: {{ set_petname_label | default('あだ名をつける') }}

# Label for exiting
Emacs*XlwMenu.exit.labelString: {{ exit_label | default('終了') }}

# Label for toggling plug status
Emacs*XlwMenu.togglePlugStatus.labelString: {{ toggle_plug_status_label | default('オンライン/オフライン') }}

# Label for managing plug status
Emacs*XlwMenu.changePlugStatus.labelString: {{ change_plug_status_label | default('プラグ状態管理') }}

# Label for saving current status
Emacs*XlwMenu.saveCurrentStatus.labelString: {{ save_current_status_label | default('現在の状態をセーブ') }}

# Label for marking all as read in current folder
Emacs*XlwMenu.markAsReadAllCurrentFolder.labelString: {{ mark_as_read_all_current_folder_label | default('すべて読んだことにする') }}

# Label for expiring items
Emacs*XlwMenu.expireCurrentFolder.labelString: {{ expire_current_folder_label | default('Expire (自動削除/アーカイブ)') }}

# Label for folder manager
Emacs*XlwMenu.folderManager.labelString: {{ folder_manager_label | default('フォルダ編集') }}

# Label for renaming
Emacs*XlwMenu.rename.labelString: {{ rename_label | default('名前変更') }}

# Label for opening all unread folders
Emacs*XlwMenu.openAllUnreadFolder.labelString: {{ open_all_unread_folder_label | default('全ての未読フォルダを開く') }}

# Label for updating status
Emacs*XlwMenu.updateStatus.labelString: {{ update_status_label | default('状態を更新') }}

# Label for emptying trash
Emacs*XlwMenu.emptyTrash.labelString: {{ empty_trash_label | default('ゴミ箱を空にする') }}

# Label for emptying trash (duplicate entry in original config)
Emacs*XlwMenu.emptyTrash.labelString: {{ empty_trash_label_2 | default('ゴミ箱を空にする') }}

# Label for unsubscribing
Emacs*XlwMenu.unsubscribe.labelString: {{ unsubscribe_label | default('購読をやめる(アクセス型)') }}

# Label for displaying all
Emacs*XlwMenu.displayAll.labelString: {{ display_all_label | default('すべて表示(アクセス型)') }}

# === Summary Menu Labels ===

# Label for summary
Emacs*XlwMenu.summary.labelString: {{ summary_label | default('サマリ') }}

# Label for reading
Emacs*XlwMenu.read.labelString: {{ read_label | default('読み進める') }}

# Label for previous page
Emacs*XlwMenu.prevPage.labelString: {{ prev_page_label | default('前のページ') }}

# Label for next page
Emacs*XlwMenu.nextPage.labelString: {{ next_page_label | default('次のページ') }}

# Label for previous message
Emacs*XlwMenu.prev.labelString: {{ prev_label | default('前のメッセージ') }}

# Label for next message
Emacs*XlwMenu.next.labelString: {{ next_label | default('次のメッセージ') }}

# Label for previous unread message
Emacs*XlwMenu.up.labelString: {{ up_label | default('前の未読メッセージ') }}

# Label for next unread message
Emacs*XlwMenu.down.labelString: {{ down_label | default('次の未読メッセージ') }}

# Label for moving to parent message
Emacs*XlwMenu.parentMessage.labelString: {{ parent_message_label | default('親メッセージへ移動') }}

# Label for setting delete mark
Emacs*XlwMenu.setDeleteMark.labelString: {{ set_delete_mark_label | default('削除マークをつける') }}

# Label for setting refile mark
Emacs*XlwMenu.setRefileMark.labelString: {{ set_refile_mark_label | default('リファイルマークをつける') }}

# Label for setting target mark
Emacs*XlwMenu.setTargetMark.labelString: {{ set_target_mark_label | default('まとめ処理用マークをつける') }}

# Label for removing temporary mark
Emacs*XlwMenu.unmark.labelString: {{ unmark_label | default('一時マークを消す') }}

# Label for removing all temporary marks
Emacs*XlwMenu.unmarkAll.labelString: {{ unmark_all_label | default('すべての一時マークを消す') }}

# Label for entering message
Emacs*XlwMenu.enterTheMessage.labelString: {{ enter_the_message_label | default('メッセージへ移動') }}

# Label for marking as important
Emacs*XlwMenu.markAsImportant.labelString: {{ mark_as_important_label | default('しおり(重要マークをつける)') }}

# Label for marking as unread
Emacs*XlwMenu.markAsUnread.labelString: {{ mark_as_unread_label | default('読まなかったことにする') }}

# Label for canceling news article
Emacs*XlwMenu.cancelPostedNews.labelString: {{ cancel_posted_news_label | default('ニュース記事をキャンセルする') }}

# Label for superseding news article
Emacs*XlwMenu.supersedesMessage.labelString: {{ supersedes_message_label | default('ニュース記事を上書きする') }}

# Label for resending bounced mail
Emacs*XlwMenu.resendBouncedMail.labelString: {{ resend_bounced_mail_label | default('エラーメールを再送する') }}

# Label for resending message
Emacs*XlwMenu.resendMessage.labelString: {{ resend_message_label | default('再送する') }}

# Label for syncing
Emacs*XlwMenu.sync.labelString: {{ sync_label | default('同期をとる') }}

# Label for executing refile/copy/delete marks
Emacs*XlwMenu.execute.labelString: {{ execute_label | default('リファイル/コピー/削除マークを実行') }}

# Label for going to other folder
Emacs*XlwMenu.goToOtherFolder.labelString: {{ go_to_other_folder_label | default('他のフォルダへ移動') }}

# Label for picking and marking
Emacs*XlwMenu.pick.labelString: {{ pick_label | default('検索してマーク') }}

# Label for marking all as read
Emacs*XlwMenu.markAsReadAll.labelString: {{ mark_as_read_all_label | default('全部読んだことにする') }}

# Label for sticky summary
Emacs*XlwMenu.stick.labelString: {{ stick_label | default('スティッキーサマリにする') }}

# Label for message operation
Emacs*XlwMenu.messageOperation.labelString: {{ message_operation_label | default('メッセージの操作') }}

# Label for thread operation
Emacs*XlwMenu.threadOperation.labelString: {{ thread_operation_label | default('スレッドの操作') }}

# Label for region operation
Emacs*XlwMenu.regionOperation.labelString: {{ region_operation_label | default('リージョンの操作') }}

# Label for mark operation
Emacs*XlwMenu.markOperation.labelString: {{ mark_operation_label | default('まとめ処理用マークの操作') }}

# Label for marking as read
Emacs*XlwMenu.markAsRead.labelString: {{ mark_as_read_label | default('読んだことにする') }}

# Label for setting copy mark
Emacs*XlwMenu.setCopyMark.labelString: {{ set_copy_mark_label | default('コピーマークを付ける') }}

# Label for toggling message display
Emacs*XlwMenu.toggleDisplayMessage.labelString: {{ toggle_display_message_label | default('メッセージの表示をトグル') }}

# Label for displaying folder list
Emacs*XlwMenu.displayFolder.labelString: {{ display_folder_label | default('フォルダ一覧を表示') }}

# Label for piping message
Emacs*XlwMenu.pipeMessage.labelString: {{ pipe_message_label | default('パイプする') }}

# Label for writing messages
Emacs*XlwMenu.writingMessages.labelString: {{ writing_messages_label | default('メッセージを書く') }}

# Label for writing a message
Emacs*XlwMenu.writeAMessage.labelString: {{ write_a_message_label | default('メッセージを書く') }}

# Label for replying
Emacs*XlwMenu.reply.labelString: {{ reply_label | default('返事を書く') }}

# Label for replying with citation
Emacs*XlwMenu.replyWithCitation.labelString: {{ reply_with_citation_label | default('引用して返事を書く') }}

# Label for forwarding
Emacs*XlwMenu.forward.labelString: {{ forward_label | default('転送する') }}

# Label for prefetching
Emacs*XlwMenu.prefetch.labelString: {{ prefetch_label | default('プリフェッチ') }}

# Label for printing message
Emacs*XlwMenu.printMessage.labelString: {{ print_message_label | default('印刷する') }}

# Label for opening/closing thread
Emacs*XlwMenu.openOrClose.labelString: {{ open_or_close_label | default('スレッドを開閉') }}

# Label for opening all threads
Emacs*XlwMenu.openAll.labelString: {{ open_all_label | default('スレッドを全部開く') }}

# Label for closing all threads
Emacs*XlwMenu.closeAll.labelString: {{ close_all_label | default('スレッドを全部閉じる') }}

# Label for exiting current summary
Emacs*XlwMenu.exitCurrentFolder.labelString: {{ exit_current_folder_label | default('現在のサマリを終了') }}

# Label for toggling threading
Emacs*XlwMenu.toggleThreading.labelString: {{ toggle_threading_label | default('スレッド表示の On/Off') }}

# Label for sorting
Emacs*XlwMenu.sort.labelString: {{ sort_label | default('ソート') }}

# Label for sorting by number
Emacs*XlwMenu.byNumber.labelString: {{ by_number_label | default('メッセージ番号でソート') }}

# Label for sorting by date
Emacs*XlwMenu.byDate.labelString: {{ by_date_label | default('日付けでソート') }}

# Label for sorting by from
Emacs*XlwMenu.byFrom.labelString: {{ by_from_label | default('送信者でソート') }}

# Label for sorting by subject
Emacs*XlwMenu.bySubject.labelString: {{ by_subject_label | default('サブジェクトでソート') }}

# === Score Operation Labels ===

# Label for score operation
Emacs*XlwMenu.scoreOperation.labelString: {{ score_operation_label | default('スコア操作') }}

# Label for switching score file
Emacs*XlwMenu.switchCurrentScoreFile.labelString: {{ switch_current_score_file_label | default('スコアファイルを変更') }}

# Label for editing current score file
Emacs*XlwMenu.editCurrentScoreFile.labelString: {{ edit_current_score_file_label | default('現在のスコアファイルを編集') }}

# Label for editing score file
Emacs*XlwMenu.editScoreFile.labelString: {{ edit_score_file_label | default('スコアファイルを編集') }}

# Label for marking low score messages
Emacs*XlwMenu.setMarkBelow.labelString: {{ set_mark_below_label | default('得点が低いメッセージにマーク') }}

# Label for expunging low score messages
Emacs*XlwMenu.setExpungeBelow.labelString: {{ set_expunge_below_label | default('得点が低いメッセージを消す') }}

# Label for rescoring buffer
Emacs*XlwMenu.rescoreBuffer.labelString: {{ rescore_buffer_label | default('スコア評価をやり直す。') }}

# Label for increasing score
Emacs*XlwMenu.increaseScore.labelString: {{ increase_score_label | default('得点を上げる') }}

# Label for lowering score
Emacs*XlwMenu.lowerScore.labelString: {{ lower_score_label | default('得点を下げる') }}

# === Plug Management Labels ===

# Label for plug management
Emacs*XlwMenu.plugged.labelString: {{ plugged_label | default('プラグ管理') }}

# Label for toggling plug on/off
Emacs*XlwMenu.togglePlugged.labelString: {{ toggle_plugged_label | default('プラグのオン/オフ') }}

# Label for toggling plug on/off (duplicate entry in original config)
Emacs*XlwMenu.togglePlugged.labelString: {{ toggle_plugged_label_2 | default('プラグのオン/オフ') }}

# Label for toggling all plugs on/off
Emacs*XlwMenu.toggleAllPlugged.labelString: {{ toggle_all_plugged_label | default('全プラグのオン/オフ') }}

# Label for toggling all plugs on/off (duplicate entry in original config)
Emacs*XlwMenu.toggleAllPlugged.labelString: {{ toggle_all_plugged_label_2 | default('全プラグのオン/オフ') }}

# Label for previous port
Emacs*XlwMenu.prevPort.labelString: {{ prev_port_label | default('上のポートヘ') }}

# Label for next port
Emacs*XlwMenu.nextPort.labelString: {{ next_port_label | default('下のポートへ') }}

# Label for previous server
Emacs*XlwMenu.prevServer.labelString: {{ prev_server_label | default('上のサーバヘ') }}

# Label for next server
Emacs*XlwMenu.nextServer.labelString: {{ next_server_label | default('下のサーバヘ') }}