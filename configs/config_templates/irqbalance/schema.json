{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable the irqbalance daemon. When false, the service will not run."
  },
  "banned_cpus": {
    "type": "string",
    "default": "0",
    "description": "Hexadecimal CPU affinity mask for CPUs excluded from interrupt handling (e.g., 'f' excludes CPUs 0\u20133). Set to '0' to allow all CPUs."
  },
  "banned_cpu_list": {
    "type": "string",
    "default": "",
    "description": "Comma-separated CPU list (e.g., '0-1,3') for CPUs excluded from interrupt handling. Overrides banned_cpus if set."
  },
  "irqbalance_oneshot": {
    "type": "string",
    "default": "",
    "description": "Set to '1' to run irqbalance once and exit after initial balancing. Empty to run continuously."
  },
  "irqbalance_args": {
    "type": "string",
    "default": "",
    "description": "Additional arguments to pass to irqbalance daemon, e.g., '--foreground --debug'."
  },
  "pwr_thrst_threshold": {
    "type": "number",
    "default": 30,
    "description": "Power threshold (percentage) above which aggressive power-saving balancing is triggered. 0 disables power-based balancing."
  },
  "up_thresh": {
    "type": "number",
    "default": 90,
    "description": "CPU usage threshold (percentage) above which performance-oriented balancing is triggered. Must be greater than pwr_thrst_threshold."
  },
  "banlist": {
    "type": "list",
    "default": [],
    "description": "List of IRQ numbers to permanently exclude from balancing (e.g., ['1', '9', '120'])."
  },
  "ignore_irqs": {
    "type": "list",
    "default": [
      "1",
      "9",
      "120"
    ],
    "description": "List of IRQs to skip during periodic reassignment (e.g., legacy ISA IRQs)."
  },
  "rebalance_interval": {
    "type": "number",
    "default": 600,
    "description": "Interval (seconds) between full rebalances of all IRQs, regardless of current distribution."
  },
  "topology_mode": {
    "type": "string",
    "default": "core",
    "description": "Topology unit to use for load balancing decisions: 'package' (sockets), 'core' (physical cores), or 'thread' (hyperthreads). (Note: The provided default config file does not expose variables for these advanced settings, so they are excluded from the template.)",
    "enum": [
      "package",
      "core",
      "thread"
    ]
  },
  "policies": {
    "type": "list",
    "default": [
      {
        "name": "interrupts",
        "level": "balance"
      },
      {
        "name": "interrupts",
        "level": "power"
      },
      {
        "name": "interrupts",
        "level": "performance"
      }
    ],
    "description": "Custom balancing policies. (Note: The provided default config file does not expose variables for these advanced settings, so they are excluded from the template.)"
  }
}
