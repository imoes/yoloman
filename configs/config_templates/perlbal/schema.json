{
  "perlbal_enable": {
    "type": "bool",
    "default": false,
    "description": "If true, Perlbal will be started via the init system."
  },
  "user": {
    "type": "string",
    "default": "www-data",
    "description": "User account under which Perlbal runs."
  },
  "group": {
    "type": "string",
    "default": "www-data",
    "description": "Group under which Perlbal runs. Optional; comment out to omit."
  },
  "extra_opts": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options passed to the Perlbal daemon. Empty by default."
  }
}
