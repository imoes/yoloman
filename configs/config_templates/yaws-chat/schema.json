{
  "ebin_dir": {
    "type": "string",
    "default": "/usr/lib/yaws-chat/ebin",
    "description": "Path to the directory where yaws-chat beam code is placed."
  },
  "port": {
    "type": "number",
    "default": 8080,
    "description": "Port number for the yaws-chat virtual server."
  },
  "listen": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to listen on for the yaws-chat virtual server."
  },
  "docroot": {
    "type": "string",
    "default": "/usr/share/yaws-chat/www",
    "description": "Document root for the yaws-chat virtual server."
  }
}
