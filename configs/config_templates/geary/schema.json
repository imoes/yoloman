{
  "impl": {
    "type": "string",
    "default": "launchpad",
    "description": "The bug tracker implementation to use."
  },
  "project": {
    "type": "string",
    "default": "geary",
    "description": "The project name for bug tracking."
  },
  "bug_pattern_base": {
    "type": "string",
    "default": "None",
    "description": "Optional base URL or pattern for bug reporting; None means default behavior."
  }
}
