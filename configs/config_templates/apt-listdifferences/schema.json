{
  "dpkg_pre_install_pkgs_command": {
    "type": "string",
    "default": "test -x /usr/sbin/apt-listdifferences || exit 0; /usr/sbin/apt-listdifferences --apt || true",
    "description": "The DPkg pre-install hook command for apt-listdifferences"
  }
}
