{
  "primary_members": {
    "type": "list",
    "default": [],
    "description": "Not applicable for `members` command config, replaced by `group_name` and `mode`."
  },
  "secondary_members": {
    "type": "list",
    "default": [],
    "description": "Not applicable for `members` command config, replaced by `group_name` and `mode`."
  }
}
