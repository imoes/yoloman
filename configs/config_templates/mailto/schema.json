{
  "permitted_recipients": {
    "type": "string",
    "default": "root",
    "description": "List of permitted email recipients, each line formatted as 'email (realname)' or just 'email'. By default 'root' is permitted."
  }
}
