{
  "lang": {
    "type": "string",
    "default": "pol",
    "description": "Preferred subtitles language code (ISO 639-2/3)"
  },
  "lang_backup": {
    "type": "string",
    "default": "eng",
    "description": "Alternative subtitles language code if primary is not found"
  },
  "format": {
    "type": "string",
    "default": "SRT",
    "description": "Target subtitles file format (mDVD, MPL2, SRT, TMP)",
    "enum": [
      "mDVD",
      "MPL2",
      "SRT",
      "TMP"
    ]
  },
  "extension": {
    "type": "string",
    "default": ".srt",
    "description": "Target subtitles file extension"
  },
  "console": {
    "type": "bool",
    "default": true,
    "description": "Enable console mode download"
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Download quietly without messages or windows"
  },
  "show_list": {
    "type": "bool",
    "default": false,
    "description": "Show list of subtitles (works only with console mode)"
  },
  "dont_show_list": {
    "type": "bool",
    "default": false,
    "description": "Do not show list of subtitles (overrides show_list)"
  }
}
