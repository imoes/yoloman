{
  "debug_level": {
    "type": "number",
    "default": 0,
    "description": "Debug level: 1-4 for verbose output, 0 for quiet. Corresponds to -d/--debug."
  },
  "exclude_defaults": {
    "type": "bool",
    "default": false,
    "description": "Exclude default valued options from configuration file format. Corresponds to -e/--exclude-defaults."
  },
  "file_format": {
    "type": "bool",
    "default": true,
    "description": "Output received router advertisements in the format of the radvd configuration file. Corresponds to -f/--file-format."
  }
}
