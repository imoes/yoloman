{
  "DESC": {
    "type": "string",
    "default": "zurl",
    "description": "Short description of the service displayed in logs"
  },
  "NAME": {
    "type": "string",
    "default": "zurl",
    "description": "Short name of the server/daemon"
  },
  "DAEMON": {
    "type": "string",
    "default": "/usr/bin/zurl",
    "description": "Absolute path to the server daemon executable"
  }
}
