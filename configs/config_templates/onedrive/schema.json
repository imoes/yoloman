{
  "logrotate_user": {
    "type": "string",
    "default": "root",
    "description": "The user that logrotate should use."
  },
  "logrotate_group": {
    "type": "string",
    "default": "users",
    "description": "The group that logrotate should use."
  },
  "logrotate_count": {
    "type": "number",
    "default": 4,
    "description": "Number of weeks worth of log files to keep."
  },
  "logrotate_create_mode": {
    "type": "string",
    "default": "",
    "description": "Empty string if 'create' has no arguments, otherwise the mode."
  },
  "logrotate_create_owner": {
    "type": "string",
    "default": "",
    "description": "Owner of the new log file."
  },
  "logrotate_create_group": {
    "type": "string",
    "default": "",
    "description": "Group of the new log file."
  },
  "logrotate_create_perms": {
    "type": "string",
    "default": "",
    "description": "Permissions of the new log file."
  }
}
