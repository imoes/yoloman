{
  "app_id_prefix": {
    "type": "string",
    "default": "net",
    "description": "The first component of the application ID regex prefix."
  },
  "app_id_suffix": {
    "type": "string",
    "default": "cmrg.impass",
    "description": "The remaining components of the application ID regex suffix."
  },
  "binary_name": {
    "type": "string",
    "default": "impass",
    "description": "The name of the executable binary for the GUI."
  }
}
