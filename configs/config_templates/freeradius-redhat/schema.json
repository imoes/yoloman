{
 "cleanup_delay": {
  "default": "5",
  "description": "Time in seconds to wait before cleaning up a reply",
  "type": "number"
 },
 "correct_escapes": {
  "default": true,
  "description": "Use correct backslash escaping",
  "type": "bool"
 },
 "exec_prefix": {
  "default": "/usr",
  "description": "The execution prefix directory",
  "type": "string"
 },
 "hostname_lookups": {
  "default": "no",
  "description": "Log names of clients or just their IP addresses",
  "type": "string"
 },
 "libdir": {
  "default": "/usr/lib64/freeradius",
  "description": "Directory where rlm_* modules are located",
  "type": "string"
 },
 "localstatedir": {
  "default": "/var",
  "description": "The variable state directory",
  "type": "string"
 },
 "log_auth": {
  "default": false,
  "description": "Log all authentication results to the log file",
  "type": "bool"
 },
 "log_auth_badpass": {
  "default": false,
  "description": "Log password if it's rejected",
  "type": "bool"
 },
 "log_auth_goodpass": {
  "default": false,
  "description": "Log password if it's correct",
  "type": "bool"
 },
 "log_colourise": {
  "default": true,
  "description": "Highlight important messages sent to stderr and stdout",
  "type": "bool"
 },
 "log_destination": {
  "default": "files",
  "description": "Destination for log messages (files, syslog, stdout, stderr)",
  "type": "string"
 },
 "log_stripped_names": {
  "default": false,
  "description": "Log the full User-Name attribute as found in the request",
  "type": "bool"
 },
 "log_syslog_facility": {
  "default": "daemon",
  "description": "Syslog facility to use if destination is syslog",
  "type": "string"
 },
 "max_request_time": {
  "default": "30",
  "description": "Maximum time in seconds to handle a request",
  "type": "number"
 },
 "max_requests": {
  "default": "16384",
  "description": "Maximum number of requests the server keeps track of",
  "type": "number"
 },
 "name": {
  "default": "radiusd",
  "description": "Name of the running server",
  "type": "string"
 },
 "prefix": {
  "default": "/usr",
  "description": "The installation prefix directory",
  "type": "string"
 },
 "proxy_requests": {
  "default": true,
  "description": "Turns proxying of RADIUS requests on or off",
  "type": "bool"
 },
 "sbindir": {
  "default": "/usr/sbin",
  "description": "The system binaries directory",
  "type": "string"
 },
 "security_allow_core_dumps": {
  "default": false,
  "description": "Allow core dumps",
  "type": "bool"
 },
 "security_group": {
  "default": "radiusd",
  "description": "Group to run radiusd as",
  "type": "string"
 },
 "security_limit_proxy_state": {
  "default": "auto",
  "description": "Limit the combination of Proxy-State and Message-Authenticator",
  "type": "string"
 },
 "security_max_attributes": {
  "default": "200",
  "description": "Maximum number of attributes permitted in a RADIUS packet",
  "type": "number"
 },
 "security_reject_delay": {
  "default": "1",
  "description": "Delay in seconds when sending an Access-Reject",
  "type": "number"
 },
 "security_require_message_authenticator": {
  "default": "auto",
  "description": "Require Message-Authenticator in Access-* packets",
  "type": "string"
 },
 "security_status_server": {
  "default": true,
  "description": "Whether the server responds to Status-Server requests",
  "type": "bool"
 },
 "security_user": {
  "default": "radiusd",
  "description": "User to run radiusd as",
  "type": "string"
 },
 "sysconfdir": {
  "default": "/etc",
  "description": "The system configuration directory",
  "type": "string"
 },
 "thread_pool_auto_limit_acct": {
  "default": false,
  "description": "Automatically limit the number of accounting requests",
  "type": "bool"
 },
 "thread_pool_max_requests_per_server": {
  "default": "0",
  "description": "Clean up old threads periodically (0 means infinity)",
  "type": "number"
 },
 "thread_pool_max_servers": {
  "default": "32",
  "description": "Limit on the total number of servers running",
  "type": "number"
 },
 "thread_pool_max_spare_servers": {
  "default": "10",
  "description": "Maximum number of spare servers",
  "type": "number"
 },
 "thread_pool_min_spare_servers": {
  "default": "3",
  "description": "Minimum number of spare servers",
  "type": "number"
 },
 "thread_pool_start_servers": {
  "default": "5",
  "description": "Number of servers to start initially",
  "type": "number"
 }
}
