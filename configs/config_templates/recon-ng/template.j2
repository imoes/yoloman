# ==============================================================================
# recon-ng Configuration Template
# ==============================================================================
# This file configures the recon-ng framework.
# Variables are sourced from the Ansible inventory/variables and are safe to reuse.
# ==============================================================================

# ------------------------------------------------------------------------------
# Workspace Configuration
# ------------------------------------------------------------------------------
# Path to the directory where recon-ng stores workspaces and databases.
# Corresponds to the `-w <workspace>` CLI argument.
# Default: /home/recon-ng/.recon-ng
# ------------------------------------------------------------------------------
workspace: {{ workspace | default('/home/recon-ng/.recon-ng') }}

# ------------------------------------------------------------------------------
# Resource File
# ------------------------------------------------------------------------------
# Path to a resource file containing commands to be loaded and executed on startup.
# Corresponds to the `-r <filename>` CLI argument.
# If empty, no external resource file is loaded.
# Default: (empty string)
# ------------------------------------------------------------------------------
resource_file: {{ resource_file | default('') }}

# ------------------------------------------------------------------------------
# Version Check
# ------------------------------------------------------------------------------
# If true, disables the version check on startup.
# Corresponds to the `--no-version` CLI argument.
# Default: true
# ------------------------------------------------------------------------------
no_version: {{ no_version | default(true) | lower }}

# ------------------------------------------------------------------------------
# Analytics Reporting (General)
# ------------------------------------------------------------------------------
# If true, disables analytics reporting.
# Corresponds to the `--no-analytics` CLI argument.
# Default: true
# ------------------------------------------------------------------------------
no_analytics: {{ no_analytics | default(true) | lower }}

# ------------------------------------------------------------------------------
# Marketplace
# ------------------------------------------------------------------------------
# If true, disables remote module management from the marketplace.
# Corresponds to the `--no-marketplace` CLI argument.
# Default: false
# ------------------------------------------------------------------------------
no_marketplace: {{ no_marketplace | default(false) | lower }}

# ------------------------------------------------------------------------------
# Stealth Mode
# ------------------------------------------------------------------------------
# If true, enables stealth mode which disables all passive requests.
# Corresponds to the `--stealth` CLI argument.
# Default: false
# ------------------------------------------------------------------------------
stealth: {{ stealth | default(false) | lower }}

# ------------------------------------------------------------------------------
# Google Analytics
# ------------------------------------------------------------------------------
# If true, enables Google analytics reporting.
# Corresponds to the `--analytics` CLI argument.
# Note: Usually mutually exclusive with no_analytics in practice, but supported by CLI.
# Default: false
# ------------------------------------------------------------------------------
analytics: {{ analytics | default(false) | lower }}