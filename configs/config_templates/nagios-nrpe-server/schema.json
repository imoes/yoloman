{
  "NRPE_OPTS": {
    "type": "string",
    "default": "#NRPE_OPTS=\"-n\"",
    "description": "Extra cmdline parameters to pass to the nrpe daemon. Commented out by default, with -n disabling SSL."
  },
  "NICENESS": {
    "type": "number",
    "default": "#NICENESS=5",
    "description": "Nice priority for the nrpe server. Only used by the init script. Commented out by default."
  },
  "INETD": {
    "type": "number",
    "default": "#INETD=0",
    "description": "Whether to run the server via inetd (1) or as a daemon (0). Only used by the init script. Commented out by default."
  }
}
