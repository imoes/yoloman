{
  "project_name": {
    "type": "string",
    "default": "designate",
    "description": "The OpenStack project name (used for user, group, config paths, etc.)"
  },
  "system_user": {
    "type": "string",
    "default": "designate",
    "description": "System user under which the service runs"
  },
  "system_group": {
    "type": "string",
    "default": "designate",
    "description": "System group for the service user"
  }
}
