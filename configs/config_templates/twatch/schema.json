{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output (-v)"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode (-d)"
  },
  "execute": {
    "type": "string",
    "default": "",
    "description": "Run project by its filename after download (-e)"
  },
  "trackers": {
    "type": "list",
    "default": [],
    "description": "List of torrent tracker URLs to watch"
  },
  "watch_dirs": {
    "type": "list",
    "default": [],
    "description": "List of directories to watch for changes (if supported by config)"
  }
}
