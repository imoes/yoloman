{
  "sbin_path": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Path to the system binary directory containing the globus-job-manager executable"
  },
  "binary_name": {
    "type": "string",
    "default": "globus-job-manager",
    "description": "Name of the Globus job manager binary"
  },
  "conf_file": {
    "type": "string",
    "default": "/etc/globus/globus-gram-job-manager.conf",
    "description": "Path to the Globus GRAM job manager configuration file"
  },
  "job_manager_type": {
    "type": "string",
    "default": "lsf",
    "description": "Type of job manager backend (e.g., lsf, pbs, sge)"
  },
  "seg_module": {
    "type": "string",
    "default": "lsf",
    "description": "Name of the SEG module to use for the job manager"
  }
}
