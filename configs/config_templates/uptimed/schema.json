{
  "desc": {
    "type": "string",
    "default": "uptime daemon",
    "description": "Description of the daemon used in status messages."
  },
  "user": {
    "type": "string",
    "default": "daemon",
    "description": "System user to run the uptimed daemon as."
  },
  "piddir": {
    "type": "string",
    "default": "/run/uptimed",
    "description": "Directory where the PID file is stored."
  }
}
