{
  "state": {
    "type": "string",
    "default": "",
    "description": "Bluetooth device state: 'on', 'off', 'toggle', or empty to leave unchanged",
    "enum": [
      "on",
      "off",
      "toggle"
    ]
  },
  "start_delay": {
    "type": "number",
    "default": 0,
    "description": "Seconds to wait before enabling Bluetooth on boot"
  },
  "stop_delay": {
    "type": "number",
    "default": 0,
    "description": "Seconds to wait before disabling Bluetooth on shutdown"
  },
  "runtime_pm_driver_deny_list": {
    "type": "list",
    "default": [],
    "description": "List of drivers to exclude from Runtime PM (e.g., to prevent Bluetooth power-off issues); always preserve 'btusb' if present"
  },
  "runtime_pm_driver_deny_list.items": {
    "type": "string",
    "default": "",
    "description": "Driver name to deny for Runtime PM"
  }
}
