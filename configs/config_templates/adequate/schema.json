{
  "adequate_enabled": {
    "type": "string",
    "default": "false",
    "description": "Whether adequate should run on every install, reporting results via debconf.\n    \"enum\": [\"true\", \"false\"]\n    },",
    "enum": [
      "true",
      "false"
    ]
  },
  "adequate_user": {
    "type": "string",
    "default": "nobody",
    "description": "User to run adequate as during dpkg hooks."
  },
  "adequate_version": {
    "type": "string",
    "default": "2",
    "description": "Version of DPkg::Tools::Options for adequate."
  }
}
