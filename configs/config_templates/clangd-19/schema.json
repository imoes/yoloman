{
  "compile_commands_dir": {
    "type": "string",
    "default": "",
    "description": "Path to look for compile_commands.json. If empty, clangd searches current directory and parent paths."
  },
  "query_driver": {
    "type": "list",
    "default": [],
    "description": "Comma-separated list of glob patterns for whitelisting gcc-compatible drivers to extract system includes."
  },
  "all_scopes_completion": {
    "type": "bool",
    "default": false,
    "description": "Include index symbols not defined in visible scopes for code completion."
  },
  "background_index": {
    "type": "bool",
    "default": true,
    "description": "Index project code in the background and persist index on disk."
  },
  "background_index_priority": {
    "type": "string",
    "default": "background",
    "description": "Thread priority for background index: background, low, or normal.",
    "enum": [
      "background",
      "low",
      "normal"
    ]
  },
  "clang_tidy": {
    "type": "bool",
    "default": false,
    "description": "Enable clang-tidy diagnostics."
  },
  "completion_style": {
    "type": "string",
    "default": "detailed",
    "description": "Granularity of code completion: detailed or bundled.",
    "enum": [
      "detailed",
      "bundled"
    ]
  },
  "experimental_modules_support": {
    "type": "bool",
    "default": false,
    "description": "Enable experimental support for C++ modules."
  },
  "fallback_style": {
    "type": "string",
    "default": "",
    "description": "clang-format style to apply by default when .clang-format is missing."
  },
  "function_arg_placeholders": {
    "type": "bool",
    "default": true,
    "description": "Include placeholders for function parameters in completions."
  },
  "header_insertion": {
    "type": "string",
    "default": "iwyu",
    "description": "Include directive insertion policy: iwyu or never.",
    "enum": [
      "iwyu",
      "never"
    ]
  },
  "header_insertion_decorators": {
    "type": "bool",
    "default": true,
    "description": "Prepend circular dot/space to indicate include insertion in completions."
  },
  "import_insertions": {
    "type": "bool",
    "default": false,
    "description": "Add #import directives for Objective-C when header insertion is enabled."
  },
  "limit_references": {
    "type": "number",
    "default": 1000,
    "description": "Maximum number of references returned (0 = no limit)."
  },
  "limit_results": {
    "type": "number",
    "default": 100,
    "description": "Maximum number of results returned (0 = no limit)."
  },
  "project_root": {
    "type": "string",
    "default": "",
    "description": "Path to project root (requires remote_index_address)."
  },
  "remote_index_address": {
    "type": "string",
    "default": "",
    "description": "Address of the remote index server (e.g., http://localhost:1234)."
  },
  "rename_file_limit": {
    "type": "number",
    "default": 50,
    "description": "Maximum files affected by symbol renaming (0 = no limit)."
  },
  "enable_config": {
    "type": "bool",
    "default": false,
    "description": "Enable reading user and project configuration from YAML files."
  },
  "threads": {
    "type": "number",
    "default": 0,
    "description": "Number of async workers (0 = auto-detect). Corresponds to -j flag."
  },
  "malloc_trim": {
    "type": "bool",
    "default": false,
    "description": "Release memory periodically via malloc_trim."
  },
  "pch_storage": {
    "type": "string",
    "default": "memory",
    "description": "PCH storage location: memory or disk.",
    "enum": [
      "memory",
      "disk"
    ]
  },
  "log": {
    "type": "string",
    "default": "info",
    "description": "Log verbosity: error, info, or verbose.",
    "enum": [
      "error",
      "info",
      "verbose"
    ]
  },
  "offset_encoding": {
    "type": "string",
    "default": "",
    "description": "Override offset encoding: utf-8, utf-16, or utf-32 (empty = auto-negotiate).",
    "enum": [
      "utf-8",
      "utf-16",
      "utf-32"
    ]
  },
  "path_mappings": {
    "type": "list",
    "default": [],
    "description": "List of <client>=<server> path translation pairs."
  },
  "pretty": {
    "type": "bool",
    "default": false,
    "description": "Pretty-print JSON output (for --check mode or stdin)."
  }
}
