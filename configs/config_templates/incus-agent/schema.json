{
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug logging (--debug flag)"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging (--verbose flag)"
  }
}
