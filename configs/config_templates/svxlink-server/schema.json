{
  "log_file_mode": {
    "type": "string",
    "default": "0644",
    "description": "The permission mode for the log files created by logrotate."
  },
  "log_user": {
    "type": "string",
    "default": "svxlink",
    "description": "The user that owns the log files."
  },
  "log_group": {
    "type": "string",
    "default": "adm",
    "description": "The group that owns the log files."
  }
}
