{
  "DHCPD_DEFAULT": {
    "type": "string",
    "default": "/etc/default/isc-dhcp-server",
    "description": "Path to the init script defaults file (source'd before start/stop)."
  },
  "DHCPDv4_CONF": {
    "type": "string",
    "default": "/etc/dhcp/dhcpd.conf",
    "description": "Path to IPv4 DHCP server configuration file."
  },
  "DHCPDv6_CONF": {
    "type": "string",
    "default": "/etc/dhcp/dhcpd6.conf",
    "description": "Path to IPv6 DHCP server configuration file."
  },
  "DHCPDv4_PID": {
    "type": "string",
    "default": "/var/run/dhcpd.pid",
    "description": "PID file path for the IPv4 DHCP daemon."
  },
  "DHCPDv6_PID": {
    "type": "string",
    "default": "/var/run/dhcpd6.pid",
    "description": "PID file path for the IPv6 DHCP daemon."
  }
}
