{
  "log_level": {
    "type": "string",
    "default": {
      "value": "INFO"
    },
    "description": "Logging verbosity level (e.g., DEBUG, INFO, WARNING, ERROR)."
  },
  "log_file": {
    "type": "string",
    "default": {
      "value": ""
    },
    "description": "Path to log file; empty string logs to stderr."
  },
  "log_dir": {
    "type": "string",
    "default": {
      "value": "/var/log/designate"
    },
    "description": "Directory for rotating log files."
  },
  "bind_host": {
    "type": "string",
    "default": {
      "value": "0.0.0.0"
    },
    "description": "IP address the agent binds to (for general service endpoints)."
  },
  "bind_port": {
    "type": ":{"
  },
  "default": {
    "type": "number"
  },
  "description": {
    "type": "Port the agent listens on for incoming requests."
  }
}
