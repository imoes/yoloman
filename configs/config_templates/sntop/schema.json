{
  "gator_display_name": {
    "type": "string",
    "default": "Gator",
    "description": "Display name for the first host entry (Gator)."
  },
  "gator_host_or_ip": {
    "type": "string",
    "default": "192.168.23.1",
    "description": "IP address or hostname for the first host entry."
  },
  "gator_display_comments": {
    "type": "string",
    "default": "local linux/alpha server",
    "description": "Display comments for the first host entry."
  },
  "yahoo_display_name": {
    "type": "string",
    "default": "Yahoo",
    "description": "Display name for the second host entry (Yahoo)."
  },
  "yahoo_host_or_ip": {
    "type": "string",
    "default": "www.yahoo.com",
    "description": "IP address or hostname for the second host entry."
  },
  "yahoo_display_comments": {
    "type": "string",
    "default": "something on the outside",
    "description": "Display comments for the second host entry."
  },
  "localhost_display_name": {
    "type": "string",
    "default": "localhost",
    "description": "Display name for the third host entry."
  },
  "localhost_host_or_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address or hostname for the third host entry."
  },
  "localhost_display_comments": {
    "type": "string",
    "default": "does loopback even work?",
    "description": "Display comments for the third host entry."
  }
}
