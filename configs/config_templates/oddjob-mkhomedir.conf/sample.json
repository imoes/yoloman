{
  "policies": [
    {
      "description": "Allow system services like 'sssd' and 'sshd' to invoke mkhomedir",
      "group": "wheel",
      "allow_own": "",
      "allow_send": "com.redhat.oddjob.mkhomedir"
    },
    {
      "description": "Allow 'nobody' to request home directory creation (for PAM use)",
      "user": "nobody",
      "allow_send": "com.redhat.oddjob.mkhomedir",
      "receive_sender": "system-bus-*"
    },
    {
      "description": "Explicitly deny unprivileged users from owning or calling mkhomedir",
      "group": "users",
      "deny_send": "com.redhat.oddjob.mkhomedir"
    }
  ]
}
