{
  "data_dir": {
    "type": "string",
    "default": "/var/lib/debusine-signing",
    "description": "Path to the directory where signing keys and metadata are stored"
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Logging verbosity: debug, info, warn, error, critical"
  },
  "max_concurrent_signing_requests": {
    "type": "number",
    "default": 10,
    "description": "Maximum number of concurrent signing requests to handle"
  },
  "api_bind_address": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Network address the API server binds to"
  },
  "api_port": {
    "type": "number",
    "default": 8080,
    "description": "Port the API server listens on"
  },
  "api_timeout_seconds": {
    "type": "number",
    "default": 30,
    "description": "Timeout for API requests, in seconds"
  },
  "api_allowed_origins": {
    "type": "list",
    "default": [],
    "description": "CORS allowed origins (list of URIs)"
  }
}
