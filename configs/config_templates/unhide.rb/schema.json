{
  "unhide_exec_path": {
    "type": "string",
    "default": "/usr/bin/unhide.rb",
    "description": "The absolute path to the unhide.rb executable."
  },
  "unhide_output_redirect": {
    "type": "bool",
    "default": true,
    "description": "If true, redirects stdout (progress messages) to null as per man page recommendation."
  }
}
