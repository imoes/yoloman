{
  "arc_log_file": {
    "type": "string",
    "default": "/var/log/arc/arex.log",
    "description": "Path to the main A-REX log file."
  },
  "arc_job_log_file": {
    "type": "string",
    "default": "/var/log/arc/arex-jobs.log",
    "description": "Path to the A-REX jobs log file."
  },
  "arc_helper_error_log_file": {
    "type": "string",
    "default": "/var/log/arc/job.helper.errors",
    "description": "Path to the job helper errors log file."
  },
  "arc_datastaging_log_file": {
    "type": "string",
    "default": "/var/log/arc/datastaging.log",
    "description": "Path to the data staging log file."
  },
  "arc_log_rotate_count": {
    "type": "number",
    "default": 14,
    "description": "Number of rotated log files to keep for A-REX main logs."
  },
  "arc_pid_file": {
    "type": "string",
    "default": "/run/arched-arex.pid",
    "description": "Path to the PID file for the A-REX daemon."
  },
  "arc_ws_log_file": {
    "type": "string",
    "default": "/var/log/arc/ws-interface.log",
    "description": "Path to the A-REX Web Service interface log file."
  },
  "arc_ws_log_rotate_count": {
    "type": "number",
    "default": 14,
    "description": "Number of rotated log files to keep for A-REX WS logs."
  },
  "arc_ws_pid_file": {
    "type": "string",
    "default": "/run/arched-arex-ws.pid",
    "description": "Path to the PID file for the A-REX Web Service daemon."
  },
  "arc_infoprovider_log_file": {
    "type": "string",
    "default": "/var/log/arc/infoprovider.log",
    "description": "Path to the InfoProvider log file."
  },
  "arc_cache_cleaner_log_file": {
    "type": "string",
    "default": "/var/log/arc/cache-cleaner.log",
    "description": "Path to the Cache Cleaner log file."
  },
  "arc_jura_log_file": {
    "type": "string",
    "default": "/var/log/arc/jura.log",
    "description": "Path to the Jura log file."
  },
  "arc_external_processes_log_rotate_count": {
    "type": "number",
    "default": 14,
    "description": "Number of rotated log files to keep for external processes logs."
  },
  "arc_perfdata_dir": {
    "type": "string",
    "default": "/var/log/arc/perfdata",
    "description": "Directory for performance data logs."
  },
  "arc_perfdata_log_rotate_count": {
    "type": "number",
    "default": 14,
    "description": "Number of rotated log files to keep for performance data logs."
  }
}
