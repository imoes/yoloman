{
  "existing_kernel": {
    "type": "string",
    "default": "",
    "description": "The connection file ID of an existing kernel to connect to. If empty, connects to the most recent kernel."
  },
  "no_confirm_exit": {
    "type": "bool",
    "default": false,
    "description": "If true, skips the confirmation prompt when exiting the console."
  },
  "kernel_name": {
    "type": "string",
    "default": "python3",
    "description": "The name of the kernel to start or use (e.g., python3, julia, ir)."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "If true, enables debug logging output."
  }
}
