{
  "resources": {
    "type": "list",
    "default": [],
    "description": "List of resource definitions (key-value pairs) in xrdb format. Each entry must include 'key' and 'value'. Optional 'comment' adds a comment above the line for clarity. Only documented c3270 resources are allowed. Each item is a flat map; nested objects are disallowed per spec requirements."
  }
}
