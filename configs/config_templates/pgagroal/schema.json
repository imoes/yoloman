{
  "host": {
    "type": "string",
    "default": "localhost",
    "description": "Network interface to bind pgagroal to"
  },
  "port": {
    "type": "number",
    "default": 2345,
    "description": "Port for pgagroal to listen on"
  },
  "unix_socket_dir": {
    "type": "string",
    "default": "/run/postgresql",
    "description": "Directory for Unix domain socket files"
  },
  "max_connections": {
    "type": "number",
    "default": 100,
    "description": "Maximum total number of connections pgagroal can open to PostgreSQL servers"
  },
  "idle_timeout": {
    "type": "number",
    "default": 600,
    "description": "Idle server connection timeout in seconds"
  },
  "primary_host": {
    "type": "string",
    "default": "localhost",
    "description": "Host or IP of the primary PostgreSQL server"
  },
  "primary_port": {
    "type": "number",
    "default": 5432,
    "description": "Port of the primary PostgreSQL server"
  }
}
