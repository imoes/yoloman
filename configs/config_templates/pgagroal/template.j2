[pgagroal]
host = {{ host }}
port = {{ port }}

log_type = console
log_level = info
log_path = 

max_connections = {{ max_connections }}
idle_timeout = {{ idle_timeout }}
validation = off
unix_socket_dir = {{ unix_socket_dir }}

[primary]
host = {{ primary_host }}
port = {{ primary_port }}
