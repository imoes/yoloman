{
  "sucrack_su_path": {
    "type": "string",
    "default": "/bin/su",
    "description": "The path to the su executable."
  },
  "sucrack_auth_failure": {
    "type": "string",
    "default": "su: Authentication failure",
    "description": "The message su returns on an authentication failure."
  },
  "sucrack_auth_success": {
    "type": "string",
    "default": "SUCRACK_SUCCESS",
    "description": "The message that indicates an authentication success."
  },
  "sucrack_user": {
    "type": "string",
    "default": "root",
    "description": "The user account to attempt to su to."
  },
  "sucrack_threads": {
    "type": "number",
    "default": 4,
    "description": "Number of threads to run with."
  },
  "sucrack_stats_interval": {
    "type": "number",
    "default": 10,
    "description": "Statistics display interval in seconds."
  },
  "sucrack_rules": {
    "type": "string",
    "default": "ADF",
    "description": "Rules for the rewriting process (A=upper, D=prepend digit, F=first upper)."
  }
}
