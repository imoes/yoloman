{
 "action": {
  "default": "scan",
  "description": "The primary action to execute: scan, show, create, or remove.",
  "enum": [
   "scan",
   "show",
   "create",
   "remove",
   "quit",
   "exit"
  ],
  "type": "string"
 },
 "scan_devices": {
  "default": [],
  "description": "List of block devices to scan for LDM metadata (e.g., /dev/sda). Corresponds to -d/--device option.",
  "type": "list"
 },
 "show_diskgroup_guid": {
  "default": "",
  "description": "The GUID of the disk group to show details for.",
  "type": "string"
 },
 "show_object_name": {
  "default": "",
  "description": "The name of the volume, partition, or disk to show details for within the group.",
  "type": "string"
 },
 "show_type": {
  "description": "The entity type to show when action is 'show'. One of: diskgroup, volume, partition, disk.",
  "enum": [
   "diskgroup",
   "volume",
   "partition",
   "disk"
  ],
  "type": "string"
 }
}
