{
  "syslog_server": {
    "type": "string",
    "default": "",
    "description": "Remote syslog server hostname or IP address to forward all logs to (e.g., '192.168.1.100'). Leave blank to disable remote forwarding."
  },
  "custom_rules": {
    "type": "list",
    "default": [],
    "description": "List of custom logging rules (beyond the defaults). Each rule specifies a facility, log level, and target file/device.\", \"items\": {"
  }
}
