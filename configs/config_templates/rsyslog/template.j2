# /etc/rsyslog.conf configuration file for rsyslog
#
# For more information install rsyslog-doc and see
# /usr/share/doc/rsyslog-doc/html/configuration/index.html
#
# Default logging rules can be found in /etc/rsyslog.d/50-default.conf

#################
#### MODULES ####
#################

module(load="imuxsock") # provides support for local system logging
{% if load_immark %}module(load="immark")  # provides --MARK-- message capability{% endif %}

{% if load_imudp %}# provides UDP syslog reception
module(load="imudp"){% endif %}
{% if load_imudp_port is defined %}input(type="imudp" port="{{ load_imudp_port }}"){% endif %}

{% if load_imtcp %}# provides TCP syslog reception
module(load="imtcp"){% endif %}
{% if load_imtcp_port is defined %}input(type="imtcp" port="{{ load_imtcp_port }}"){% endif %}

# provides kernel logging support and enable non-kernel klog messages
module(load="imklog" permitnonkernelfacility="on")

###########################
#### GLOBAL DIRECTIVES ####
###########################

# Filter duplicated messages
$RepeatedMsgReduction on

#
# Set the default permissions for all log files.
#
$FileOwner {{ file_owner }}
$FileGroup {{ file_group }}
$FileCreateMode {{ file_create_mode }}
$DirCreateMode {{ dir_create_mode }}
$Umask {{ umask }}
$PrivDropToUser {{ privdrop_to_user }}
$PrivDropToGroup {{ privdrop_to_group }}

#
# Where to place spool and state files
#
$WorkDirectory {{ work_directory }}

#
# Include all config files in /etc/rsyslog.d/
#
$IncludeConfig /etc/rsyslog.d/*.conf