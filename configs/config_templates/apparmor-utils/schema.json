{
  "POLICYGROUPS_DIR": {
    "type": "string",
    "default": "/usr/share/apparmor/easyprof/policygroups",
    "description": "Path to the system policygroups directory"
  },
  "TEMPLATES_DIR": {
    "type": "string",
    "default": "/usr/share/apparmor/easyprof/templates",
    "description": "Path to the system templates directory"
  }
}
