{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether to embed the auditable dependency JSON in the binary"
  },
  "lockfile": {
    "type": "string",
    "default": "",
    "description": "Optional explicit path to Cargo.lock to use. Empty string means use default."
  }
}
