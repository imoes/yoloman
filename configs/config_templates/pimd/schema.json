{
 "access_lists": {
  "default": [],
  "description": "Access-lists to filter multicast groups and sources.",
  "type": "list"
 },
 "boundaries": {
  "default": [],
  "description": "List of boundary filters (inbound ACLs) per interface to restrict multicast sources or groups.",
  "type": "list"
 },
 "bsr_enabled": {
  "default": false,
  "description": "Enable Bootstrap Router (BSR) functionality for dynamic RP discovery.",
  "type": "bool"
 },
 "cp_threshold": {
  "default": 0,
  "description": "TTL threshold (0–255) for multicast packets to be forwarded. 0 = forward all.",
  "type": "number"
 },
 "debug": {
  "default": false,
  "description": "Enable debugging output to syslog.",
  "type": "bool"
 },
 "debug_level": {
  "description": "Debug level suffix (e.g., ' 2' for more verbose logs); empty to use default level.",
  "enum": [
   "packet",
   "prunes",
   "routes",
   "rtdetail",
   "peers",
   "cache",
   "timeout",
   "interface",
   "groups",
   "mtrace",
   "igmp",
   "icmp",
   "rsrr",
   "pim",
   "pim_routes",
   "pim_bsr",
   "pim_detail",
   "pim_hello"
  ],
  "type": "string"
 },
 "interfaces": {
  "default": [
   {
    "dr_priority": 1,
    "hello_interval": 30,
    "igmp_querier": true,
    "mode": "sm",
    "name": "eth0",
    "pim_disabled": false
   }
  ],
  "description": "Per-interface PIM/IGMP settings.",
  "type": "list"
 },
 "pimdm_enabled": {
  "default": false,
  "description": "Enable PIM-Dense Mode (rarely used; disable unless explicitly required).",
  "type": "bool"
 },
 "pimsm_enabled": {
  "default": true,
  "description": "Enable PIM-Sparse Mode (required for most production deployments).",
  "type": "bool"
 },
 "rp_address": {
  "default": "192.168.1.1",
  "description": "IP address of the Rendezvous Point (RP) for PIM-SM.",
  "type": "string"
 },
 "static_routes": {
  "default": [],
  "description": "List of static (*,G) multicast routes for manual forwarding control.",
  "type": "list"
 },
 "vrid": {
  "default": 1,
  "description": "Virtual Router ID (0–255) for VRRP interoperability. Only relevant if using VRRP with PIM.",
  "type": "number"
 }
}
