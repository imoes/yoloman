{
  "daemon_path": {
    "type": "string",
    "default": "/usr/bin/irkerd",
    "description": "Path to the irkerd daemon binary."
  },
  "service_name": {
    "type": "string",
    "default": "irkerd",
    "description": "The name of the service process."
  },
  "service_description": {
    "type": "string",
    "default": "IRC notifications",
    "description": "Human-readable description of the service."
  },
  "pidfile_path": {
    "type": "string",
    "default": "/var/run/irker.pid",
    "description": "Path to the PID file for the daemon."
  },
  "run_user": {
    "type": "string",
    "default": "irker",
    "description": "System user account under which the daemon runs."
  },
  "log_file_path": {
    "type": "string",
    "default": "/var/log/irker/irker.log",
    "description": "Path to the log file for the daemon."
  }
}
