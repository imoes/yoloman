{
 "encryption": {
  "default": "luks2",
  "description": "Default LUKS encryption format used when creating new encrypted volumes. 'luks2' (LUKS2) is the modern standard with stronger key derivation and metadata integrity; use 'luks1' only if legacy boot loaders or old distros must unlock the volume.",
  "enum": [
   "luks1",
   "luks2"
  ],
  "type": "string"
 },
 "modules": {
  "default": [
   "*"
  ],
  "description": "Comma separated list of modules to load, or '*' to load all modules. Common values: lvm2 (LVM volume groups), btrfs (Btrfs subvolumes), iscsi (iSCSI targets), zram (compressed RAM block devices). Leave empty or set to '*' for automatic behavior. Note: udisks2 interprets '*' literally — do not replace it with a list in most cases unless you want fine-grained control.",
  "type": "string"
 },
 "modules_load_preference": {
  "default": "ondemand",
  "description": "Controls when listed modules are initialised. 'ondemand' defers loading until the module is first referenced (lower startup cost); 'onstartup' loads all listed modules when the daemon starts (avoids first-use latency).",
  "enum": [
   "ondemand",
   "onstartup"
  ],
  "type": "string"
 }
}
