{
  "generate_completions": {
    "type": "bool",
    "default": false,
    "description": "Whether to include shell completion setup comments in the configuration."
  },
  "default_output_format": {
    "type": "string",
    "default": "json",
    "description": "The default output format for sqlite-utils query/rows commands.",
    "enum": [
      "json",
      "csv",
      "tsv",
      "table",
      "nl"
    ]
  },
  "enable_wal_by_default": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable WAL mode on newly created databases."
  },
  "detect_types_default": {
    "type": "bool",
    "default": false,
    "description": "Whether to automatically detect column types for CSV/TSV inserts by default."
  },
  "extension_paths": {
    "type": "list",
    "default": [],
    "description": "List of file paths to SQLite extension libraries to load by default."
  }
}
