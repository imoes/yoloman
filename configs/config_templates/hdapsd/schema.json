{
  "device": {
    "type": "string",
    "default": "sda",
    "description": "The block device path to monitor for disk activity (e.g., /dev/sda)."
  },
  "sensitivity": {
    "type": "number",
    "default": 15,
    "description": "Minimum movement amplitude (in arbitrary sensor units) required to trigger disk unloading. Lower = more sensitive."
  },
  "options": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options to pass to hdapsd."
  },
  "forceenable": {
    "type": "string",
    "default": "no",
    "description": "Set to 'yes' to force-enable HDAPS support on kernels >=2.6.28 where it might be wrongly detected as unsupported."
  }
}
