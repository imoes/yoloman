{
  "system_table_dir": {
    "type": "string",
    "default": "/etc/incron.d",
    "description": "Directory where system incron table files are stored."
  },
  "user_table_dir": {
    "type": "string",
    "default": "/var/spool/incron",
    "description": "Directory where per-user inotify table files are stored."
  },
  "allowed_users_file": {
    "type": "string",
    "default": "/etc/incron.allow",
    "description": "Path to file containing list of allowed users."
  },
  "denied_users_file": {
    "type": "string",
    "default": "/etc/incron.deny",
    "description": "Path to file containing list of denied users."
  },
  "lockfile_dir": {
    "type": "string",
    "default": "/var/run",
    "description": "Directory used for creating the incron lockfile."
  },
  "lockfile_name": {
    "type": "string",
    "default": "incrond",
    "description": "Base name for the lockfile (appended by '.pid')."
  },
  "editor": {
    "type": "string",
    "default": "vim",
    "description": "Default editor used for editing user incron tables."
  }
}
