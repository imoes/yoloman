{
  "data_dir": {
    "type": "string",
    "default": "/var/lib/guerillabackup/data",
    "description": "Directory for storage of backup data elements generated locally (maps to DefaultFileSystemSinkBaseDir)."
  }
}
