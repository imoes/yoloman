{
  "outputdir": {
    "type": "string",
    "default": "output/",
    "description": "Directory where generated vcard, qr code, and printable cards are saved."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug logging level."
  },
  "localsign": {
    "type": "bool",
    "default": true,
    "description": "Whether to locally sign the OpenPGP key within the generated card."
  },
  "firstname": {
    "type": "string",
    "default": "",
    "description": "First name for the business card."
  },
  "lastname": {
    "type": "string",
    "default": "",
    "description": "Last name for the business card."
  },
  "mail": {
    "type": "string",
    "default": "",
    "description": "Email address for the business card."
  },
  "fingerprint": {
    "type": "string",
    "default": "",
    "description": "OpenPGP key fingerprint to include in the card."
  }
}
