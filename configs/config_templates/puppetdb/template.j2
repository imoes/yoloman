#!/usr/bin/env ruby
# PuppetDB init script
#
# This is the default configuration used when installing PuppetDB via the
# Puppet Labs package repositories. It is *not* intended to be edited
# directly.
#
# To override settings, create a file named puppetdb.ini in the same
# directory as this script and define your overrides there.
#
# See the PuppetDB documentation for more information.

# Memory and JVM options
JAVA_ARGS="{{ java_args }}"