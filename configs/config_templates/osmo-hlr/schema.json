{
  "log_level_main": {
    "type": "string",
    "default": "notice",
    "description": "Logging level for the main component",
    "enum": [
      "error",
      "notice",
      "info"
    ]
  },
  "log_level_db": {
    "type": "string",
    "default": "notice",
    "description": "Logging level for the database component",
    "enum": [
      "error",
      "notice",
      "info"
    ]
  },
  "log_level_auc": {
    "type": "string",
    "default": "notice",
    "description": "Logging level for the AUC component",
    "enum": [
      "error",
      "notice",
      "info"
    ]
  },
  "log_level_ss": {
    "type": "string",
    "default": "notice",
    "description": "Logging level for the SS component",
    "enum": [
      "error",
      "notice",
      "info"
    ]
  },
  "log_level_linp": {
    "type": "string",
    "default": "error",
    "description": "Logging level for the LINP component",
    "enum": [
      "error",
      "notice",
      "info"
    ]
  },
  "bind_vty_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address to bind the VTY (line vty) interface"
  },
  "bind_ctrl_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address to bind the Ctrl interface"
  },
  "bind_gsup_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address to bind the GSUP interface"
  },
  "apn_1": {
    "type": "string",
    "default": "internet",
    "description": "APN for PDP profile 1"
  },
  "apn_2": {
    "type": "string",
    "default": "*",
    "description": "APN for PDP profile 2"
  }
}
