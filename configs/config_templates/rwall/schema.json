{
  "disable": {
    "type": "bool",
    "default": true,
    "description": "Whether to disable the rwall server service. If true, the service will not accept connections."
  },
  "only_from": {
    "type": "string",
    "default": "",
    "description": "A comma-separated list of networks or hostnames allowed to connect to the rwall service. If empty, the service is typically disabled for security."
  }
}
