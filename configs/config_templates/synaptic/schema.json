{
  "filter_file": {
    "type": "string",
    "default": "",
    "description": "Path to an alternative filter settings file. If empty, default filters are used."
  },
  "initial_filter": {
    "type": "number",
    "default": 0,
    "description": "Index of the filter to apply at startup. 0 corresponds to the first filter in the filter file."
  },
  "options": {
    "type": "list",
    "default": [],
    "description": "List of internal options to set. Each item is a string in 'key=value' format. For experts only."
  }
}
