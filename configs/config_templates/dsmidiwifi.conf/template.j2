[global]
# MIDI note number that A4 (440 Hz) is mapped to
note = {{ config.note }}

# Log level: 0=error, 1=warn, 2=info, 3=debug
log_level = {{ config.log_level }}

# Enable/disable logging to syslog
syslog = {{ config.syslog|lower }}
