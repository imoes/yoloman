{
  "datadirs_default_1_path": {
    "type": "string",
    "default": "/here/are/some/files",
    "description": "First directory path for Round Robin Database and Event files."
  },
  "datadirs_default_2_path": {
    "type": "string",
    "default": "/here/are/other/files",
    "description": "Second directory path for Round Robin Database and Event files."
  },
  "saved_dir_path": {
    "type": "string",
    "default": "/var/lib/drraw",
    "description": "Directory where drraw saves persistent data."
  },
  "tmp_dir_path": {
    "type": "string",
    "default": "/var/cache/drraw",
    "description": "Directory where drraw stores temporary files."
  },
  "clean_cache_seconds": {
    "type": "number",
    "default": 21600,
    "description": "Time in seconds between purges of expired cache entries."
  }
}
