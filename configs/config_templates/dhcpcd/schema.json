{
  "pidfile": {
    "type": "string",
    "default": "/var/run/dhcpcd.pid",
    "description": "Path to the PID file for dhcpcd"
  },
  "interfaces_file": {
    "type": "string",
    "default": "/etc/network/interfaces",
    "description": "Path to the network interfaces configuration file"
  }
}
