# quickroute-gps configuration template
# This template generates the configuration file for quickroute-gps.
# Since the schema provided is empty, this is a minimal placeholder configuration.
# Any future fields should be added here with their defaults and comments.

# No configuration fields were defined in the schema for package: quickroute-gps.
# If this application requires a default configuration, add fields to the schema
# and uncomment the appropriate Jinja2 directives below.

# Example:
# # The port quickroute-gps listens on
# # {{ port | default(8080) }}

# Example:
# # Path to the GPS data source
# # data_path = {{ data_path | default('/var/lib/quickroute-gps/data') }}

# Placeholder for now to satisfy template requirement.
version = "1.0"