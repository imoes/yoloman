{
  "WNHOME": {
    "type": "string",
    "default": "/usr/local/WordNet-3.0",
    "description": "Base directory for WordNet. Corresponds to the WNHOME environment variable."
  },
  "WNSEARCHDIR": {
    "type": "string",
    "default": "/usr/local/WordNet-3.0/dict",
    "description": "Directory in which the WordNet database has been installed. Corresponds to the WNSEARCHDIR environment variable."
  }
}
