{
  "basename": {
    "type": "string",
    "default": "",
    "description": "Basename for output files (basename.dict, basename.index); required"
  },
  "format": {
    "type": "string",
    "default": "",
    "description": "Input format: one of 'c5','t','e','f','h','j','p','i','I'",
    "enum": [
      "t",
      "f",
      "h"
    ]
  },
  "database_short": {
    "type": "string",
    "default": "",
    "description": "Database short name (optionally with version/date); overrides 00-database-short header"
  },
  "database_url": {
    "type": "string",
    "default": "",
    "description": "Source URL of the raw database; overrides 00-database-url header"
  },
  "locale": {
    "type": "string",
    "default": "C",
    "description": "Locale used for sorting (e.g., 'C.UTF-8','en_US.UTF-8')"
  },
  "allchars": {
    "type": "bool",
    "default": false,
    "description": "Include all characters in index/search (default: only alphanums + space)"
  },
  "case_sensitive": {
    "type": "bool",
    "default": false,
    "description": "Make search case-sensitive"
  },
  "headword_separator": {
    "type": "string",
    "default": "",
    "description": "Separator used to split multiple headwords (e.g., '%%%')"
  },
  "columns": {
    "type": "number",
    "default": 72,
    "description": "Line-wrap width for definitions (0 or negative disables wrapping)"
  },
  "default_strategy": {
    "type": "string",
    "default": "",
    "description": "Default search strategy (e.g., 'prefix','suffix','exact')"
  },
  "mime_header": {
    "type": "string",
    "default": "",
    "description": "MIME header to prepend to definitions on OPTION MIME"
  },
  "without_headword": {
    "type": "bool",
    "default": false,
    "description": "Do not include headwords in .dict file"
  },
  "without_header": {
    "type": "bool",
    "default": false,
    "description": "Do not copy header to DB info entry"
  },
  "without_url": {
    "type": "bool",
    "default": false,
    "description": "Do not copy URL to DB info entry"
  },
  "without_time": {
    "type": "bool",
    "default": false,
    "description": "Do not copy creation time to DB info entry"
  },
  "without_ver": {
    "type": "bool",
    "default": false,
    "description": "Suppress dictfmt version entry"
  },
  "without_info": {
    "type": "bool",
    "default": false,
    "description": "Suppress DB info entry entirely"
  },
  "break_headwords": {
    "type": "bool",
    "default": false,
    "description": "Write multiple headwords on separate lines"
  },
  "index_keep_orig": {
    "type": "bool",
    "default": false,
    "description": "Preserve original headword in 4th index column (for UTF-8)"
  },
  "index_data_separator": {
    "type": "string",
    "default": "\\034",
    "description": "Separator between index and data columns in .index file (octal string)"
  },
  "utf8": {
    "type": "bool",
    "default": false,
    "description": "Generate UTF-8 database (see --utf8 option)"
  }
}
