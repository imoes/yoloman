{
  "directory": {
    "type": "string",
    "default": ".",
    "description": "Default directory to scan for renaming operations. Overridden by CLI argument if provided."
  },
  "show_preview": {
    "type": "bool",
    "default": true,
    "description": "Whether to show the preview before applying changes."
  },
  "encoding": {
    "type": "string",
    "default": "UTF-8",
    "description": "Encoding for filenames."
  },
  "recursive": {
    "type": "bool",
    "default": false,
    "description": "Whether to process subdirectories recursively."
  },
  "operations": {
    "type": "list",
    "default": [],
    "description": "List of rename operations to apply (in order). Each must be an object with 'type' field and operation-specific fields."
  },
  "preserve_timestamps": {
    "type": "bool",
    "default": true,
    "description": "Whether to preserve file timestamps after renaming."
  },
  "log_file": {
    "type": "string",
    "default": "~/.gprename_log",
    "description": "Log file path."
  }
}
