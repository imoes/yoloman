{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether the TNEF parser plugin should be loaded at startup"
  },
  "plugin_path": {
    "type": "string",
    "default": "/usr/lib/claws-mail/plugins/tnef_parse.so",
    "description": "Absolute path to the plugin shared library file (tnef_parse.so)"
  }
}
