{
  "service_name": {
    "type": "string",
    "default": "designate-central",
    "description": "Name of the service/daemon (e.g., 'designate-central')"
  },
  "project_name": {
    "type": "string",
    "default": "designate",
    "description": "OpenStack project name (used for paths, user, config, etc.)"
  },
  "service_description": {
    "type": "string",
    "default": "Frontend Designate API server",
    "description": "Short description used in init header and log messages"
  },
  "UWSGI_PORT": {
    "type": "number",
    "default": 9001,
    "description": "Port for uWSGI to bind to (if used)"
  },
  "UWSGI_INI_PATH": {
    "type": "string",
    "default": "/etc/designate/designate-central.ini",
    "description": "Path to uWSGI configuration file"
  },
  "UWSGI_INI_APP": {
    "type": "string",
    "default": "/etc/designate/designate-central.wsgi",
    "description": "Path to uWSGI app entrypoint (used for existence check)"
  },
  "SYSTEM_USER": {
    "type": "string",
    "default": "designate",
    "description": "System user to run the service as"
  },
  "SYSTEM_GROUP": {
    "type": "string",
    "default": "designate",
    "description": "System group to run the service as"
  },
  "CONFIG_FILE": {
    "type": "string",
    "default": "/etc/designate/designate.conf",
    "description": "Path to the main configuration file"
  },
  "LOGFILE": {
    "type": "string",
    "default": "/var/log/designate/designate-central.log",
    "description": "Path to the log file"
  },
  "PIDFILE": {
    "type": "string",
    "default": "/var/run/designate/designate-central.pid",
    "description": "Path to the PID file"
  }
}
