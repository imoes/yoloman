{
  "binaries": {
    "type": "list",
    "default": [],
    "description": "List of absolute paths to binaries whose rpath/runpath will be managed by chrpath. Each path must be a valid executable with an existing RPATH or RUNPATH entry."
  }
}
