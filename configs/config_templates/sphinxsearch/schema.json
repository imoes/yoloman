{
  "indexer_user": {
    "type": "string",
    "default": "sphinxsearch",
    "description": "The user account under which the indexer cron job runs."
  },
  "sphinxsearch_config_dir": {
    "type": "string",
    "default": "/etc/default/sphinxsearch",
    "description": "The default configuration directory or file sourced by the cron job."
  },
  "indexer_binary": {
    "type": "string",
    "default": "/usr/bin/indexer",
    "description": "Path to the sphinxsearch indexer executable."
  }
}
