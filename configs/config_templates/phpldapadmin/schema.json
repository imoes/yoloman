{
  "ldap_server_hostname": {
    "type": "string",
    "default": "ldap.example.com",
    "description": "Hostname used in VirtualHost block and log filenames (e.g., ldap.example.com)"
  },
  "admin_email": {
    "type": "string",
    "default": "root@example.com",
    "description": "Administrator email address for the VirtualHost"
  }
}
