{
  "url": {
    "type": "string",
    "default": "example.com",
    "description": "The domain or URL to analyze for robots.txt entries."
  },
  "show_only_http_200": {
    "type": "bool",
    "default": false,
    "description": "If true, filter results to show only resources returning HTTP 200."
  },
  "search_bing": {
    "type": "bool",
    "default": false,
    "description": "If true, cross-reference with Bing indexed Disallows."
  },
  "input_file": {
    "type": "string",
    "default": "",
    "description": "Path to a file containing a list of domains to scan. Overrides url."
  }
}
