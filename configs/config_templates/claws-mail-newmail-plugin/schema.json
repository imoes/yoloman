{
  "username": {
    "type": "string",
    "default": "",
    "description": "User login name, used to derive default log file path (e.g., /home/username/Mail/NewLog). Must be a valid Unix username."
  },
  "log_file": {
    "type": "string",
    "default": "",
    "description": "Absolute path to the log file where new mail header summaries are written. If empty, defaults to ~/Mail/NewLog using username."
  }
}
