{
  "shellinabox_daemon_start": {
    "type": "number",
    "default": 1,
    "description": "Whether shellinaboxd should start automatically (1 for yes, 0 for no)."
  },
  "shellinabox_port": {
    "type": "number",
    "default": 4200,
    "description": "TCP port that shellinaboxd's webserver listens on."
  },
  "shellinabox_args": {
    "type": "string",
    "default": "--no-beep",
    "description": "Optional arguments for shellinaboxd, e.g. extra service definitions."
  }
}
