{
  "DAEMON": {
    "type": "string",
    "default": "/usr/sbin/rpc.bootparamd",
    "description": "Path to the bootparamd daemon executable."
  },
  "NAME": {
    "type": "string",
    "default": "bootparamd",
    "description": "Service name (used for PID files, log messages, and init.d symlink name)."
  },
  "DESC": {
    "type": "string",
    "default": "boot parameter server",
    "description": "Human-readable description of the service."
  }
}
