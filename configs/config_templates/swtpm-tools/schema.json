{
  "statedir": {
    "type": "string",
    "default": "/var/lib/swtpm-localca",
    "description": "Directory where TPM state and certificates are stored."
  },
  "signingkey": {
    "type": "string",
    "default": "/var/lib/swtpm-localca/signkey.pem",
    "description": "Path to the private key used for signing certificates."
  },
  "issuercert": {
    "type": "string",
    "default": "/var/lib/swtpm-localca/issuercert.pem",
    "description": "Path to the issuer certificate."
  },
  "certserial": {
    "type": "string",
    "default": "/var/lib/swtpm-localca/certserial",
    "description": "Path to the file storing the certificate serial number."
  }
}
