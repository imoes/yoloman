{
  "upscable": {
    "type": "string",
    "default": "usb",
    "description": "Type of cable connecting the UPS to the computer.",
    "enum": [
      "simple",
      "smart",
      "ether",
      "usb",
      "940-0119A",
      "940-0127A",
      "940-0128A",
      "940-0020B",
      "940-0020C",
      "940-0023A",
      "940-0024B",
      "940-0024C",
      "940-1524C",
      "940-0024G",
      "940-0095A",
      "940-0095B",
      "940-0095C",
      "940-0625A",
      "M-04-02-2000"
    ]
  },
  "ups_type": {
    "type": "string",
    "default": "usb",
    "description": "UPS communication type: USB, SERIAL, or NET (networked UPS)",
    "enum": [
      "apcsmart",
      "usb",
      "net",
      "snmp",
      "netsnmp",
      "dumb",
      "pcnet",
      "modbus"
    ]
  },
  "device_path": {
    "type": "string",
    "default": "/dev/ttyS0",
    "description": "Device path (e.g., /dev/ttyS0) for SERIAL; empty for USB autodetection; host:port for NET"
  },
  "device_options": {
    "type": "string",
    "default": "",
    "description": "Additional device-specific options (e.g., baud rate for serial, like '-b 9600'). Appended to DEVICE line."
  },
  "timeout": {
    "type": "number",
    "default": 0,
    "description": "Maximum time (seconds) UPS will power connected devices before forced shutdown"
  },
  "battery_level_percent": {
    "type": "number",
    "default": 5,
    "description": "Minimum battery charge (%) that triggers forced shutdown if runtime estimate unavailable"
  },
  "min_runtime_minutes": {
    "type": "number",
    "default": 3,
    "description": "Minimum remaining runtime (minutes) before forced shutdown"
  },
  "annoy_delay_minutes": {
    "type": "number",
    "default": 60,
    "description": "Minutes to wait after power failure before warning users (ANNOYDELAY)"
  },
  "nis_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable Network Information Server (NETSERVER in config)"
  },
  "nis_port": {
    "type": "number",
    "default": 3551,
    "description": "Port for NIS daemon (NISPORT in config)"
  },
  "nis_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address to bind NIS daemon to (NISIP in config)"
  }
}
