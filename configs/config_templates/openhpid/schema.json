{
  "openhpi_user": {
    "type": "string",
    "default": "openhpi",
    "description": "System user account under which the openhpid daemon will run (for security hardening)."
  },
  "openhpi_group": {
    "type": "string",
    "default": "openhpi",
    "description": "System group for the openhpid daemon process."
  },
  "openhpi_pidfile": {
    "type": "string",
    "default": "/var/run/openhpid.pid",
    "description": "Full path to the PID file where the daemon writes its process ID."
  },
  "openhpi_loglevel": {
    "type": "string",
    "default": "INFO",
    "description": "Verbosity of logging. Set to DEBUG for detailed diagnostics, ERROR or higher for minimal output.",
    "enum": [
      "DEBUG",
      "INFO"
    ]
  },
  "openhpi_logfile": {
    "type": "string",
    "default": "/var/log/openhpid.log",
    "description": "Path to the primary log file. Use '-' to log to stderr (e.g., when using systemd journal)."
  },
  "openhpi_log_rotation_count": {
    "type": "number",
    "default": 5,
    "description": "Number of rotated log files to keep when log rotation is enabled."
  },
  "openhpi_log_rotation_max_size": {
    "type": "number",
    "default": 10485760,
    "description": "Maximum size (in bytes) of a log file before it is rotated."
  },
  "openhpi_transport_plugins": {
    "type": "list",
    "default": [],
    "description": "List of transport plugins to enable (e.g., IPMI, SNMP, Redfish). Each entry configures one plugin with its options."
  },
  "openhpi_auto_load_resources": {
    "type": "bool",
    "default": true,
    "description": "If true, automatically load discovered hardware resources into the HPI inventory."
  },
  "openhpi_scan_interval": {
    "type": "number",
    "default": 300,
    "description": "Interval (in seconds) between automatic scans for new or changed resources. Set to 0 to disable periodic scanning."
  },
  "openhpi_snmp_discovery": {
    "type": "object",
    "default": null,
    "description": "Configuration for SNMP-based auto-discovery of hardware resources."
  },
  "openhpi_ipmi_discovery": {
    "type": "object",
    "default": null,
    "description": "Configuration for IPMI-over-LAN auto-discovery."
  },
  "openhpi_redfish_discovery": {
    "type": "object",
    "default": null,
    "description": "Configuration for Redfish API-based auto-discovery."
  }
}
