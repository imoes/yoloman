{
 "snmp": {
  "description": "SNMP module configuration for ProFTPD",
  "items": {
   "allowed_hosts": {
    "default": [],
    "description": "List of hosts allowed to query SNMP (e.g., SNMP set/get requests); each item is a host spec (hostname, IP, or CIDR)",
    "items": {
     "description": "Allowed host IP, hostname, or CIDR notation",
     "type": "string"
    },
    "type": "list"
   },
   "community": {
    "default": "public",
    "description": "SNMP community string (prefer a secure, non-default value in production)",
    "type": "string"
   },
   "contact": {
    "default": "admin@example.com",
    "description": "Contact information for the server administrator (sysContact MIB)",
    "type": "string"
   },
   "location": {
    "default": "Server Room",
    "description": "Physical location of the FTP server (used in sysLocation MIB)",
    "type": "string"
   },
   "log_level": {
    "default": 2,
    "description": "SNMP daemon log verbosity (0–7; higher = more verbose)",
    "type": "number"
   },
   "pid_file": {
    "default": "/var/run/proftpd-snmp.pid",
    "description": "Path to the SNMP sub-daemon PID file",
    "type": "string"
   },
   "trap_destinations": {
    "default": [],
    "description": "List of SNMP trap receiver hosts",
    "items": {
     "description": "SNMP trap destination host",
     "items": {
      "community": {
       "default": "public",
       "description": "Community string to use when sending traps to this destination (optional; defaults to SNMPCommunity if omitted)",
       "type": "string"
      },
      "host": {
       "default": "",
       "description": "Hostname or IP address of the SNMP manager",
       "type": "string"
      },
      "port": {
       "default": 162,
       "description": "UDP port for trap reception (standard: 162)",
       "type": "number"
      }
     },
     "type": "object"
    },
    "type": "list"
   },
   "trap_events": {
    "default": [],
    "description": "List of events that trigger SNMP traps; common values: login, logout, upload, download, rename, delete",
    "items": {
     "description": "Event name to enable as a trap trigger",
     "enum": [
      "login",
      "logout",
      "upload",
      "download",
      "rename",
      "delete",
      "mkdir",
      "rmdir",
      "chdir",
      "chmod"
     ],
     "type": "string"
    },
    "type": "list"
   }
  },
  "type": "object"
 }
}
