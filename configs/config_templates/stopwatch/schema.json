{
  "decimals": {
    "type": "number",
    "default": 3,
    "description": "Number of decimal places for displaying milliseconds accuracy"
  },
  "format": {
    "type": "string",
    "default": "00:00.000",
    "description": "The time format string to use for display"
  },
  "night_mode": {
    "type": "bool",
    "default": false,
    "description": "Enable dark background theme for low-light environments"
  },
  "color": {
    "type": "string",
    "default": "blue",
    "description": "Color theme for the stopwatch text display"
  },
  "font_size": {
    "type": "string",
    "default": "medium",
    "description": "Base size of the timer font (small, medium, large, or custom)"
  },
  "font_family": {
    "type": "string",
    "default": "monospace",
    "description": "Font family to use when custom font size is selected"
  },
  "auto_stop_at_zero": {
    "type": "bool",
    "default": true,
    "description": "Automatically stop the timer when the count reaches zero"
  },
  "max_laps": {
    "type": "number",
    "default": 100,
    "description": "Maximum number of lap records to keep before overwriting oldest"
  },
  "language": {
    "type": "string",
    "default": "en",
    "description": "ISO language code for the interface translation"
  },
  "preset_timers": {
    "type": "list",
    "default": [
      60,
      300,
      600,
      3600
    ],
    "description": "List of preset durations in seconds for quick timer setup"
  }
}
