{
 "connection_pool_clean_freq": {
  "default": 100,
  "description": "Number of packets after which a connection pool cleaning is performed (-F)",
  "type": "number"
 },
 "display_app_data": {
  "default": false,
  "description": "Display application data traffic (-d)",
  "type": "bool"
 },
 "filter": {
  "default": "",
  "description": "BPF filter expression to select packets (e.g., 'host 10.0.0.1 and port 443'). Empty string captures all.",
  "type": "string"
 },
 "inactive_conn_ttl": {
  "default": 100,
  "description": "TTL for inactive connections in seconds (-t)",
  "type": "number"
 },
 "interface": {
  "default": "lo",
  "description": "Network interface to capture traffic on (e.g., eth0, ens33)",
  "type": "string"
 },
 "key_file": {
  "default": "",
  "description": "Path to the SSL/TLS private key file (OpenSSL PEM format) for decryption. Empty if not decrypting.",
  "type": "string"
 },
 "key_password": {
  "default": "",
  "description": "Password for the SSL key file. Empty if no password or no key file.",
  "type": "string"
 },
 "no_promiscuous": {
  "default": false,
  "description": "Don't put the interface into promiscuous mode (-P)",
  "type": "bool"
 },
 "no_resolve": {
  "default": true,
  "description": "Don't try to resolve host names from IP addresses (-n)",
  "type": "bool"
 },
 "output_json": {
  "default": false,
  "description": "Switch output format to JSON (-j)",
  "type": "bool"
 },
 "parse_asn1": {
  "default": false,
  "description": "Attempt to parse ASN.1 when it appears (-N)",
  "type": "bool"
 },
 "print_absolute_timestamps": {
  "default": false,
  "description": "Print absolute timestamps instead of relative (-e)",
  "type": "bool"
 },
 "print_acks": {
  "default": false,
  "description": "Print bare TCP ACKs (-a)",
  "type": "bool"
 },
 "print_all_fields": {
  "default": false,
  "description": "Print all record fields (-A)",
  "type": "bool"
 },
 "print_full_header": {
  "default": false,
  "description": "Print the full SSL packet header (-H)",
  "type": "bool"
 },
 "print_hex": {
  "default": false,
  "description": "Print each record in hex, as well as decoding it (-x)",
  "type": "bool"
 },
 "print_tcp_headers": {
  "default": false,
  "description": "Print the TCP headers (-T)",
  "type": "bool"
 },
 "quiet": {
  "default": false,
  "description": "Quiet mode, only print summary lines (-q)",
  "type": "bool"
 },
 "read_dump_file": {
  "default": "",
  "description": "Path to a PCAP dump file to read from instead of network (-r)",
  "type": "string"
 },
 "ssl_flags": {
  "description": "SSL flags to print: crypto, d, ht, H. Comma or space separated if multiple. (-S)",
  "enum": [
   "crypto",
   "d",
   "ht",
   "H"
  ],
  "type": "string"
 },
 "ssl_key_log_file": {
  "default": "",
  "description": "Path to the SSLKEYLOGFILE (Mozilla NSS format) for TLS 1.3 decryption. Empty if not used.",
  "type": "string"
 },
 "suppress_printable_hex": {
  "default": false,
  "description": "Suppress printable characters in hex dump (-X)",
  "type": "bool"
 },
 "write_pcap": {
  "default": "",
  "description": "Destination file for decrypted packets in pcap format (-w)",
  "type": "string"
 }
}
