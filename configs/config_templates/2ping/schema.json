{
 "allow_ipv4": {
  "default": true,
  "description": "Whether to allow IPv4 ping requests.",
  "type": "boolean"
 },
 "allow_ipv6": {
  "default": true,
  "description": "Whether to allow IPv6 ping requests.",
  "type": "boolean"
 },
 "allowed_networks": {
  "default": [
   "10.0.0.0/16",
   "172.16.0.0/12"
  ],
  "description": "List of allowed networks in CIDR notation. By default, includes common private Class-B ranges. Each network must be /16 (IPv4) or /64 (IPv6).",
  "type": "list"
 },
 "interface": {
  "default": {
   "ipv4": "0.0.0.0",
   "ipv6": "::"
  },
  "description": "Network interface or IP address to bind to (e.g., '127.0.0.1', 'eth0', '::'). Use '0.0.0.0' or '::' to bind to all interfaces.",
  "type": "string"
 },
 "log_file": {
  "default": "",
  "description": "Path to log file. If empty or undefined, logs go to syslog.",
  "type": "string"
 },
 "log_level": {
  "default": "notice",
  "description": "Log level: debug, info, notice, warning, error, critical, alert, emergency.",
  "type": "string"
 },
 "max_rate": {
  "default": 10,
  "description": "Maximum pings allowed per source IP per second. Helps prevent abuse.",
  "type": "number"
 },
 "port": {
  "default": 402,
  "description": "UDP/TCP port to listen on (default 402).",
  "type": "number"
 },
 "require_symmetric": {
  "default": false,
  "description": "If true, require the source IP of the incoming ping to match the destination IP (useful for routing verification).",
  "type": "boolean"
 },
 "ssl_cert": {
  "default": "",
  "description": "Path to SSL/TLS certificate file (PEM format).",
  "type": "string"
 },
 "ssl_enabled": {
  "default": false,
  "description": "Whether to enable encrypted transport (requires certificate and key).",
  "type": "boolean"
 },
 "ssl_key": {
  "default": "",
  "description": "Path to SSL/TLS private key file (PEM format).",
  "type": "string"
 },
 "timeout": {
  "default": 5,
  "description": "Response timeout in seconds.",
  "type": "number"
 }
}
