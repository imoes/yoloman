{
  "general_colors": {
    "type": "bool",
    "default": true,
    "description": "Enable color output in the status bar."
  },
  "general_interval": {
    "type": "number",
    "default": 5,
    "description": "Refresh interval in seconds."
  },
  "order_0": {
    "type": "string",
    "default": "ipv6",
    "description": "First item in the status bar order."
  },
  "order_1": {
    "type": "string",
    "default": "wireless _first_",
    "description": "Second item in the status bar order."
  },
  "order_2": {
    "type": "string",
    "default": "ethernet _first_",
    "description": "Third item in the status bar order."
  },
  "order_3": {
    "type": "string",
    "default": "battery all",
    "description": "Fourth item in the status bar order."
  },
  "order_4": {
    "type": "string",
    "default": "disk /",
    "description": "Fifth item in the status bar order."
  },
  "order_5": {
    "type": "string",
    "default": "load",
    "description": "Sixth item in the status bar order."
  },
  "order_6": {
    "type": "string",
    "default": "memory",
    "description": "Seventh item in the status bar order."
  },
  "order_7": {
    "type": "string",
    "default": "tztime local",
    "description": "Eighth item in the status bar order."
  },
  "wireless_format_up": {
    "type": "string",
    "default": "W: (%quality at %essid) %ip",
    "description": "Format string for wireless interface when up."
  },
  "wireless_format_down": {
    "type": "string",
    "default": "W: down",
    "description": "Format string for wireless interface when down."
  },
  "ethernet_format_up": {
    "type": "string",
    "default": "E: %ip (%speed)",
    "description": "Format string for ethernet interface when up."
  },
  "ethernet_format_down": {
    "type": "string",
    "default": "E: down",
    "description": "Format string for ethernet interface when down."
  },
  "battery_format": {
    "type": "string",
    "default": "%status %percentage %remaining",
    "description": "Format string for battery status."
  },
  "disk_mountpoint": {
    "type": "string",
    "default": "/",
    "description": "Mountpoint to monitor for disk usage."
  },
  "disk_format": {
    "type": "string",
    "default": "%avail",
    "description": "Format string for disk usage."
  },
  "load_format": {
    "type": "string",
    "default": "%1min",
    "description": "Format string for system load."
  },
  "memory_format": {
    "type": "string",
    "default": "%used | %available",
    "description": "Format string for memory usage."
  },
  "memory_threshold_degraded": {
    "type": "string",
    "default": "1G",
    "description": "Threshold below which memory is considered degraded."
  },
  "memory_format_degraded": {
    "type": "string",
    "default": "MEMORY < %available",
    "description": "Format string for memory when degraded."
  },
  "tztime_format": {
    "type": "string",
    "default": "%Y-%m-%d %H:%M:%S",
    "description": "Format string for time display."
  }
}
