{
  "gtp0_local_ip": {
    "type": "string",
    "default": "10.88.0.1",
    "description": "Local IP address used by the GGSN for GTP-C/GTP-U (must be reachable by SGSNs)."
  },
  "gtp0_remote_ip": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "Remote GTP endpoint (e.g., SGSN) IP; often left as 0.0.0.0 to accept any peer."
  },
  "gtp0_interface": {
    "type": "string",
    "default": "eth0",
    "description": "Network interface for GTP0 traffic (e.g., 'eth0' or 'gtp0')."
  },
  "vty_bind_addr": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address for the VTY/CLI management interface (e.g., for telnet or ssh-based vtysh)."
  },
  "vty_port": {
    "type": "number",
    "default": 4242,
    "description": "TCP port for the VTY/CLI management interface."
  },
  "gsup_local_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Local IP for the GSUP (Generic Subscriber Update Protocol) server interface."
  },
  "gsup_local_port": {
    "type": "number",
    "default": 4222,
    "description": "Local port for GSUP server."
  },
  "gsup_remote_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Remote GSUP peer (e.g., HLR or MSC) IP."
  },
  "gsup_remote_port": {
    "type": "number",
    "default": 4222,
    "description": "Remote GSUP peer port."
  },
  "ip_pools": {
    "type": "list",
    "default": [],
    "description": "List of IP address pools assigned to APNs."
  },
  "apns": {
    "type": "list",
    "default": [],
    "description": "List of APNs served by this GGSN, each linked to an IP pool."
  },
  "gprs_ran": {
    "type": "object",
    "default": null,
    "description": "GPRS Routing Area Identity (RAN) \u2014 optional but recommended for inter-SGSN handover."
  },
  "sgsn_peers": {
    "type": "list",
    "default": [],
    "description": "List of SGSN peers (one per SGSN in the network)."
  },
  "stats_interval": {
    "type": "number",
    "default": 60,
    "description": "Interval (seconds) between GGSN statistics updates."
  }
}
