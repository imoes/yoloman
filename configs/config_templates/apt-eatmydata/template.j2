# Path to the dpkg binary wrapper used by apt-eatmydata.
# This tells APT which dpkg executable to invoke for package operations,
# enabling the "eatmydata" optimization (skipping fsyncs for speed).
Dir::Bin::dpkg "{{ dpkg_path | default('/usr/libexec/apt-eatmydata/dpkg') }}";