{
  "scan_interval": {
    "type": "number",
    "default": 5,
    "description": "Time in seconds between consecutive mDNS PTR queries."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debug logging."
  }
}
