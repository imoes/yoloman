# mdns-scan configuration file
# This file is generated. See values_schema for available options.

# Interval between mDNS scans in seconds.
# Controls how frequently the tool queries _services._dns-sd._udp.local.
# Schema: scan_interval (int, default: 5) - Time in seconds between consecutive mDNS PTR queries.
SCAN_INTERVAL = {{ (scan_interval | default(5)) | int }}

# Debug mode. If true, enables verbose logging to stdout.
# Schema: debug (bool, default: false) - Enable verbose debug logging.
DEBUG = {{ debug | default(false) | bool | ternary('true', 'false') }}