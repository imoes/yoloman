{
  "profile_dir": {
    "type": "string",
    "default": "/home/user/.mozilla/firefox/default",
    "description": "Directory path to the Mozilla Firefox/Thunderbird profile containing the keyrings. Corresponds to the -d option."
  },
  "pinentry_prog": {
    "type": "string",
    "default": "/usr/bin/pinentry",
    "description": "Path to the pinentry program used for authentication. Corresponds to the -p option."
  }
}
