{
  "VERBOSE": {
    "type": "string",
    "default": "yes",
    "description": "Whether to run httpry in verbose mode. Set to 'yes' to enable output from the init script.",
    "enum": [
      "yes",
      "no"
    ]
  },
  "OPTIONS": {
    "type": "string",
    "default": "-o /var/log/httpry.log -i any",
    "description": "Arguments passed to the httpry executable. Includes output file and interface options."
  },
  "EXPRESSION": {
    "type": "string",
    "default": "",
    "description": "BPF-style capture filter string. Leave empty for no filtering."
  }
}
