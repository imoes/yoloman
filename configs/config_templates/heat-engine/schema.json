{
  "project_name": {
    "type": "string",
    "default": "heat",
    "description": "OpenStack project name (e.g., heat, neutron, nova); used for naming, paths, user/group, config file location."
  }
}
