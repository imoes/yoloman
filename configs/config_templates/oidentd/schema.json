{
  "oidentd_limit_connections": {
    "type": "number",
    "default": 10,
    "description": "Maximum number of simultaneous oidentd connections to allow."
  },
  "oidentd_user": {
    "type": "string",
    "default": "oident",
    "description": "User account under which oidentd runs."
  },
  "oidentd_group": {
    "type": "string",
    "default": "oident",
    "description": "Group under which oidentd runs."
  },
  "oidentd_behind_proxy": {
    "type": "string",
    "default": "yes",
    "description": "Whether the host is behind a masquerading router acting as an oidentd proxy. Must be 'yes' or 'no'.",
    "enum": [
      "yes",
      "no"
    ]
  },
  "global_options": {
    "type": "object",
    "default": "",
    "description": "Key-value pairs for global oidentd settings (e.g., 'default_allow yes')."
  },
  "users": {
    "type": "list",
    "default": [],
    "description": "List of users with custom ident response rules."
  },
  "global_rules": {
    "type": "list",
    "default": [],
    "description": "Global rules (evaluated before per-user rules)."
  }
}
