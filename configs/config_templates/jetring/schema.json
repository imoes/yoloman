{
  "source_dir": {
    "type": "string",
    "default": "/etc/keys",
    "description": "The directory containing the jetring changesets and index file."
  },
  "sign_keyring": {
    "type": "string",
    "default": "/etc/gnupg/pubring.kbx",
    "description": "Path to the GPG keyring file used to sign the index file. If not set, signing is disabled."
  },
  "gpg_home": {
    "type": "string",
    "default": "/root/.gnupg",
    "description": "The GPG home directory to use for GPG operations."
  }
}
