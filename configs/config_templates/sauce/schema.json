{
  "log_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "Number of rotated log files to keep for general sauce logs."
  },
  "log_rotation_period": {
    "type": "string",
    "default": "weekly",
    "description": "Frequency of log rotation for general sauce logs (dbreasons, notice, error, reject, fatal).",
    "enum": [
      "daily",
      "weekly"
    ]
  },
  "debug_log_perms": {
    "type": "string",
    "default": "0640",
    "description": "Permission mode for the debug.log file."
  },
  "log_owner": {
    "type": "string",
    "default": "mail",
    "description": "Owner user for sauce log files."
  },
  "log_group": {
    "type": "string",
    "default": "mail",
    "description": "Owner group for sauce log files."
  },
  "debug_log_rotation_period": {
    "type": "string",
    "default": "daily",
    "description": "Frequency of log rotation for debug.log.",
    "enum": [
      "daily",
      "weekly"
    ]
  }
}
