{
  "provides": {
    "type": "string",
    "default": "spacenavd",
    "description": "Name of the service provided."
  },
  "required_start": {
    "type": "string",
    "default": "$remote_fs",
    "description": "LSB dependency: required start services."
  },
  "required_stop": {
    "type": "string",
    "default": "$remote_fs",
    "description": "LSB dependency: required stop services."
  },
  "default_start": {
    "type": "string",
    "default": "2 3 4 5",
    "description": "LSB runlevels to start the service."
  },
  "default_stop": {
    "type": "string",
    "default": "0 1 6",
    "description": "LSB runlevels to stop the service."
  },
  "short_description": {
    "type": "string",
    "default": "3Dconnexion Input Devices Userspace Driver",
    "description": "Short description of the service."
  },
  "description": {
    "type": "string",
    "default": "A free user space driver for 3Dconnexion input devices,\t                 compatible with the proprietary 3dxsrv daemon.",
    "description": "Full description of the service."
  },
  "author_name": {
    "type": "string",
    "default": "Rodolphe PELLOUX-PRAYER",
    "description": "Author's name."
  },
  "author_email": {
    "type": "string",
    "default": "rodolphe@damsy.net",
    "description": "Author's email address."
  },
  "desc": {
    "type": "string",
    "default": "3Dconnexion Input Devices Userspace Driver",
    "description": "Description variable used by the script."
  },
  "name": {
    "type": "string",
    "default": "spacenavd",
    "description": "Service name."
  },
  "daemon_path": {
    "type": "string",
    "default": "/usr/bin/spacenavd",
    "description": "Path to the daemon executable."
  },
  "daemon_args": {
    "type": "string",
    "default": "-v",
    "description": "Default arguments for the daemon."
  },
  "pidfile_path": {
    "type": "string",
    "default": "/run/spnavd.pid",
    "description": "Path to the PID file."
  }
}
