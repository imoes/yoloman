{
  "boot_subpath": {
    "type": "string",
    "default": "boot/efi",
    "description": "Alternative boot subpath to match in lsblk filter"
  },
  "efi_mountpoint": {
    "type": "string",
    "default": "efi",
    "description": "EFI mountpoint name to match in lsblk filter"
  },
  "dtb_source_path": {
    "type": "string",
    "default": "/usr/lib/linux-image-${latest_kernel}",
    "description": "Source path for DTB files to copy to the ESP"
  }
}
