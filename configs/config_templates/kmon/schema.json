{
  "accent_color": {
    "type": "string",
    "default": "white",
    "description": "Set the accent color using hex code or standard color name."
  },
  "color": {
    "type": "string",
    "default": "darkgray",
    "description": "Set the main color using hex code or standard color name."
  },
  "tickrate": {
    "type": "number",
    "default": 250,
    "description": "Set the refresh rate of the terminal in milliseconds."
  },
  "reverse": {
    "type": "bool",
    "default": false,
    "description": "Reverse the order of the kernel module list."
  },
  "unicode": {
    "type": "bool",
    "default": false,
    "description": "Enable Unicode symbols for block titles."
  }
}
