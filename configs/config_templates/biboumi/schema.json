{
  "hostname": {
    "type": "string",
    "default": "biboumi.example.com",
    "description": "The hostname this XMPP-to-IRC gateway will advertise to XMPP clients."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Shared secret password used to authenticate this component to the XMPP server."
  },
  "xmpp_server_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address of the XMPP server to which biboumi connects."
  },
  "port": {
    "type": "number",
    "default": 5347,
    "description": "The port on which the XMPP component listens for incoming connections (typically 5347 for XMPP components)."
  },
  "admin": {
    "type": "string",
    "default": "",
    "description": "XMPP JID (bare or full) of the service administrator. Receives operational notices and errors."
  },
  "realname_customization": {
    "type": "bool",
    "default": true,
    "description": "Allow per-user customization of the realname sent to IRC servers."
  },
  "realname_from_jid": {
    "type": "bool",
    "default": false,
    "description": "Use the JID (instead of hostname) to generate the realname if realname_customization=true."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/biboumi/biboumi.log",
    "description": "Path to the log file for Biboumi\u2019s runtime logs."
  },
  "ca_file": {
    "type": "string",
    "default": "",
    "description": "Absolute path to the CA certificate file (PEM format). Used for TLS verification. Maps to tls_cert in schema."
  },
  "outgoing_bind": {
    "type": "string",
    "default": "",
    "description": "Local IP address to bind to for outgoing IRC connections."
  },
  "log_level": {
    "type": "string",
    "default": "1",
    "description": "Verbosity level: NONE=0, ERROR=1, WARNING=2, INFO=3, DEBUG=4, or TRACE=5. Matches Biboumi's numeric logging levels.\n",
    "enum": [
      "0",
      "1",
      "2",
      "3"
    ]
  }
}
