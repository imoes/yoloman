# /usr/share/doc/lighttpd/magnet.txt.gz
# http://trac.lighttpd.net/trac/wiki/Docs%3AModMagnet

# List of lighttpd modules to load.
# Defaults to mod_magnet if not specified.
server.modules += ( {{ modules | default(['mod_magnet']) | join(', ') }} )