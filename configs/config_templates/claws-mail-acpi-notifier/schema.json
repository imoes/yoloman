{
  "enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable the acpi_notifier plugin (must be true for plugin to load)"
  },
  "newmail_mode": {
    "type": "string",
    "default": "blink",
    "description": "LED behavior when new mail is received ('on', 'blink', or 'off')"
  },
  "user_interaction_mode": {
    "type": "string",
    "default": "blink",
    "description": "LED behavior when user interaction is required ('blink', 'on', or 'off')"
  }
}
