{
  "types": {
    "type": "list",
    "default": [
      "deb",
      "deb-src"
    ],
    "description": "List of repository types (e.g., deb, deb-src)."
  },
  "uris": {
    "type": "list",
    "default": [
      "https://repo.mobian.org/"
    ],
    "description": "List of repository URIs."
  },
  "suites": {
    "type": "list",
    "default": [
      "trixie"
    ],
    "description": "List of repository suites (e.g., trixie, bookworm)."
  },
  "components": {
    "type": "list",
    "default": [
      "main"
    ],
    "description": "List of repository components (e.g., main, contrib, non-free)."
  },
  "signed_by": {
    "type": "string",
    "default": "/usr/share/keyrings/mobian-archive-keyring.gpg",
    "description": "Path to the GPG keyring file used to sign the repository."
  }
}
