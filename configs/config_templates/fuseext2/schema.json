{
  "mountpoint": {
    "type": "string",
    "default": "",
    "description": "Mount point directory (must exist and be empty)"
  },
  "device": {
    "type": "string",
    "default": "",
    "description": "Block device or image file to mount (e.g., /dev/sdX, image.ext2)"
  },
  "options": {
    "type": "list",
    "default": [],
    "description": "List of fuse2fs-compatible options. Recognized by fuse-ext2 wrapper: ro, rw, force. All others forwarded to fuse2fs."
  }
}
