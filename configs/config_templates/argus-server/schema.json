{
  "log_path": {
    "type": "string",
    "default": "argus/argus.out",
    "description": "Path (relative to /var/log) where the argus log file resides"
  },
  "rotate_count": {
    "type": "number",
    "default": 14,
    "description": "Number of rotated log files to keep"
  }
}
