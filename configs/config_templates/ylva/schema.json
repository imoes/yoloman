{
  "config": {
    "type": "object",
    "description": "Ylva runtime configuration options and commands to include in the generated script/config file. Note: ylva relies on environment variables for persistent config like color and default username, and CLI flags for actions like adding entries or showing passwords. This object allows templating a wrapper script or .env file content."
  }
}
