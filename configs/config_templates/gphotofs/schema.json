{
  "mountpoint": {
    "type": "string",
    "default": "/mnt/gphoto",
    "description": "Mount point directory where the camera filesystem will be exposed."
  },
  "additional_options": {
    "type": "list",
    "default": [],
    "description": "Optional FUSE mount options (e.g., allow_other, allow_root, use_ino). Each item is a string option name or 'key=value'."
  }
}
