{
  "color": {
    "type": "bool",
    "default": false,
    "description": "Use ANSI escape codes to colorize output"
  },
  "xml": {
    "type": "bool",
    "default": false,
    "description": "Output conjugation results with XML tags"
  },
  "common": {
    "type": "bool",
    "default": false,
    "description": "Conjugate verbs using most common Brazilian usage instead of normative grammar"
  },
  "verbs": {
    "type": "list",
    "default": [],
    "description": "List of verbs to conjugate in batch mode (used with -t flag); each item is a verb infinitive in UTF-8"
  }
}
