{
  "default_mouse_disabled": {
    "type": "string",
    "default": "false",
    "description": "Global default for whether mouse configs are disabled"
  },
  "default_mouse_resolution_1": {
    "type": "number",
    "default": 1800,
    "description": "Default polling resolution for first profile on DeathAdder Chroma"
  },
  "default_mouse_polling_rate_1": {
    "type": "number",
    "default": 1000,
    "description": "Default polling frequency (Hz) for first profile"
  },
  "default_mouse_led_scrollwheel_color": {
    "type": "string",
    "default": "0000FF",
    "description": "Default hex color for scrollwheel LED on DeathAdder Chroma"
  },
  "default_mouse_resolution_1_da": {
    "type": "number",
    "default": 900,
    "description": "Default polling resolution for first profile on standard DeathAdder"
  },
  "default_mouse_resolution_1_copperhead": {
    "type": "number",
    "default": 800,
    "description": "Default polling resolution for first profile on Copperhead"
  },
  "default_mouse_resolution_2_copperhead": {
    "type": "number",
    "default": 2000,
    "description": "Default polling resolution for second profile on Copperhead"
  },
  "default_mouse_polling_rate_2_copperhead": {
    "type": "number",
    "default": 1000,
    "description": "Default polling frequency for second profile on Copperhead"
  },
  "default_mouse_resolution_1_krait": {
    "type": "number",
    "default": 1600,
    "description": "Default polling resolution for first profile on Krait"
  },
  "default_mouse_resolution_1_lachesis": {
    "type": "number",
    "default": 1000,
    "description": "Default polling resolution for first profile on Lachesis"
  },
  "default_mouse_resolution_1_naga": {
    "type": "number",
    "default": 1000,
    "description": "Default polling resolution for first profile on Naga"
  },
  "default_mouse_resolution_1_boomslang": {
    "type": "number",
    "default": 800,
    "description": "Default polling resolution for first profile on Boomslang"
  },
  "default_mouse_resolution_1_diamondback": {
    "type": "number",
    "default": 16000,
    "description": "Default polling resolution for first profile on Diamondback Chroma"
  },
  "default_mouse_led_basic_color": {
    "type": "string",
    "default": "0000FF",
    "description": "Default hex color for basic LED on Diamondback Chroma"
  }
}
