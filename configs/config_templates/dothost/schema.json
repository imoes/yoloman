{
  "addresses": {
    "type": "list",
    "default": [],
    "description": "List of DNS hostnames or IP addresses to look up (as positional arguments to dothost). Each item is a string representing a hostname or IP address."
  }
}
