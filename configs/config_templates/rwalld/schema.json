{
  "enable": {
    "type": "bool",
    "default": true,
    "description": "Whether to start the rpc.rwalld daemon."
  },
  "ip": {
    "type": "string",
    "default": "",
    "description": "IP address to bind the rpc.rwalld service to."
  }
}
