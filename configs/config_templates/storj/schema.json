{
  "bridge": {
    "type": "string",
    "default": "",
    "description": "The Storj bridge host address (e.g., localhost:14002)."
  },
  "bridge_user": {
    "type": "string",
    "default": "",
    "description": "The username for the Storj bridge account."
  },
  "bridge_pass": {
    "type": "string",
    "default": "",
    "description": "The password for the Storj bridge account."
  },
  "keypass": {
    "type": "string",
    "default": "",
    "description": "The passphrase to unlock the user's encryption keys."
  },
  "encryption_key": {
    "type": "string",
    "default": "",
    "description": "The encryption key used for file transfers."
  },
  "proxy": {
    "type": "string",
    "default": "",
    "description": "SOCKS proxy URL (e.g., socks5://127.0.0.1:1080)."
  },
  "url": {
    "type": "string",
    "default": "",
    "description": "Override the base URL for the Storj API."
  },
  "log_level": {
    "type": "number",
    "default": 0,
    "description": "The log level for the storj command (0=info, 1+=debug)."
  }
}
