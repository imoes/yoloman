{
  "primary_hostname": {
    "type": "string",
    "default": "",
    "description": "Specify your host's canonical name here. This should normally be the fully qualified \"official\" name of your host. If this option is not set, the uname() function is called to obtain the name."
  },
  "local_domains": {
    "type": "string",
    "default": "localhost : localhost.localdomain",
    "description": "List of local domains. Colon-separated list."
  },
  "relay_to_domains": {
    "type": "string",
    "default": "",
    "description": "List of domains for which your host is an incoming relay. Colon-separated list. Leave empty if not doing any relaying."
  },
  "relay_from_hosts": {
    "type": "string",
    "default": "localhost",
    "description": "List of hosts that can use your host as an outgoing relay. Colon-separated list."
  },
  "av_scanner": {
    "type": "string",
    "default": "clamd:/var/run/clamd.exim/clamd.sock",
    "description": "Define the interface to your virus scanner. Example is typical for ClamAV."
  },
  "tls_advertise_hosts": {
    "type": "string",
    "default": "*",
    "description": "Specify hosts for which TLS is advertised. Default is to allow all."
  },
  "tls_certificate": {
    "type": "string",
    "default": "/etc/pki/tls/certs/exim.pem",
    "description": "Specify the location of the Exim server's TLS certificate."
  },
  "tls_privatekey": {
    "type": "string",
    "default": "/etc/pki/tls/private/exim.pem",
    "description": "Specify the location of the Exim server's private key. Must not be encrypted."
  },
  "daemon_smtp_ports": {
    "type": "string",
    "default": "25 : 465 : 587",
    "description": "Ports on which Exim listens for SMTP connections. Colon-separated list."
  },
  "tls_on_connect_ports": {
    "type": "string",
    "default": "465",
    "description": "Ports on which TLS is required from the start (TLS-on-connect)."
  },
  "never_users": {
    "type": "string",
    "default": "root",
    "description": "Colon-separated list of users that deliveries will never be run under."
  },
  "host_lookup": {
    "type": "string",
    "default": "*",
    "description": "Specify networks for which reverse DNS lookup is done. Default is to do it for all incoming IP calls."
  },
  "auth_advertise_hosts": {
    "type": "string",
    "default": "",
    "description": "Specify hosts for which SMTP AUTH is advertised. Empty string means nowhere (Exim's default would be to allow it from anywhere)."
  },
  "dns_dnssec_ok": {
    "type": "number",
    "default": "1",
    "description": "Causes Exim to try to initialize the system resolver library with DNSSEC support."
  },
  "prdr_enable": {
    "type": "bool",
    "default": "true",
    "description": "Enable PRDR feature if compiled with support."
  },
  "log_selector": {
    "type": "string",
    "default": "+smtp_protocol_error +smtp_syntax_error \\",
    "description": "Specify additional logging details. Adjust to suit."
  },
  "ignore_bounce_errors_after": {
    "type": "string",
    "default": "2d",
    "description": "Unfreeze frozen bounce messages after this period, try once more, and ignore failures."
  },
  "timeout_frozen_after": {
    "type": "string",
    "default": "7d",
    "description": "Cancel (remove) frozen messages older than this period."
  },
  "keep_environment": {
    "type": "string",
    "default": "^LDAP",
    "description": "List of environment variables we trust."
  },
  "add_environment": {
    "type": "string",
    "default": "PATH=/usr/bin::/bin",
    "description": "List of environment variables we want to set to a specific value."
  },
  "remote_msa_port": {
    "type": "number",
    "default": "587",
    "description": "Port for the remote_msa transport (message submission port)."
  },
  "local_delivery_group": {
    "type": "string",
    "default": "mail",
    "description": "Group used for local delivery to user mailboxes."
  }
}
