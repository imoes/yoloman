{
  "enable_logtail": {
    "type": "bool",
    "default": true,
    "description": "Enable or disable logtail service/functionality."
  },
  "log_files": {
    "type": "list",
    "default": [],
    "description": "List of log file configurations to monitor."
  },
  "log_files.items": {
    "type": "string",
    "default": "",
    "description": "Flat map fields for log file entries: 'file' (string, path to log), 'offset' (string, path to offset file), 'test_mode' (bool, whether to use -t flag).",
    "enum": [
      "file",
      "offset",
      "test_mode"
    ]
  },
  "warn_on_truncate": {
    "type": "bool",
    "default": true,
    "description": "Enable warnings when the log file size decreases (indicating rotation/truncation)."
  }
}
