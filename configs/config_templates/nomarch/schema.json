{
  "archive_path": {
    "type": "string",
    "default": "/var/lib/nomarch/archives/data.arc",
    "description": "Path to the .arc archive to process"
  },
  "extract_dir": {
    "type": "string",
    "default": "/var/lib/nomarch/output",
    "description": "Directory where archives will be listed or extracted"
  },
  "use_uppercase_filenames": {
    "type": "bool",
    "default": false,
    "description": "Use uppercase filenames; preserve original case from archive (-U)"
  },
  "verbose_output": {
    "type": "bool",
    "default": false,
    "description": "Give verbose output when used with -l (-v)"
  },
  "test_only": {
    "type": "bool",
    "default": false,
    "description": "Test files in archive (check CRCs) (-t)"
  },
  "list_files": {
    "type": "bool",
    "default": false,
    "description": "List files in archive (-l)"
  },
  "match_files": {
    "type": "list",
    "default": [],
    "description": "Optional filenames/wildcards to specify which members to process. Empty list processes all members."
  }
}
