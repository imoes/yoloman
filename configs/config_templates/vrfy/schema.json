{
  "vrfy_localhost": {
    "type": "string",
    "default": "localhost",
    "description": "Nearest host running sendmail for local address verification (VRFY_LOCALHOST)"
  },
  "vrfy_uucp_relay": {
    "type": "string",
    "default": "localhost",
    "description": "Host contacted for .uucp addresses (VRFY_UUCPRELAY)"
  },
  "vrfy_bitnet_relay": {
    "type": "string",
    "default": "localhost",
    "description": "Host contacted for .bitnet/.earn addresses (VRFY_BITNETRELAY)"
  },
  "vrfy_single_relay": {
    "type": "string",
    "default": "localhost",
    "description": "Host contacted for single unqualified hostnames (VRFY_SINGLERELAY)"
  },
  "vrfy_defaults": {
    "type": "list",
    "default": [],
    "description": "List of default command-line options for VRFY_DEFAULTS environment variable. Supported flags: -v, -vv, -vvv, -d, -dd, -ddd, -a, -l, -s, -e, -h, -H, -n, -S <sender>, -c <secs>, -t <secs>, -p <domain>, -T <domain>, -L <level>, -R."
  }
}
