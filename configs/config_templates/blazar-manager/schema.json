{
  "project_name": {
    "type": "string",
    "default": "blazar",
    "description": "Project name for OpenStack service (used for paths, users, etc.)"
  },
  "process_name": {
    "type": "string",
    "default": "blazar-manager",
    "description": "RabbitMQ process name for OpenStack messaging"
  }
}
