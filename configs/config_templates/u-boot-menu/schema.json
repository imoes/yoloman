{
  "u_boot_update": {
    "type": "string",
    "default": "true",
    "description": "Enable or disable u-boot-update processing.",
    "enum": [
      "true",
      "false"
    ]
  },
  "u_boot_alternatives": {
    "type": "list",
    "default": [
      "default",
      "recovery"
    ],
    "description": "List of u-boot alternatives to manage."
  },
  "u_boot_default": {
    "type": "string",
    "default": "l0",
    "description": "The default u-boot alternative to use."
  },
  "u_boot_prompt": {
    "type": "string",
    "default": "1",
    "description": "Whether to show the boot menu prompt.",
    "enum": [
      "0",
      "1"
    ]
  },
  "u_boot_entries": {
    "type": "string",
    "default": "all",
    "description": "Which entries to include in the menu."
  },
  "u_boot_menu_label": {
    "type": "string",
    "default": "Debian GNU/Linux",
    "description": "Label for the main menu entry."
  },
  "u_boot_parameters": {
    "type": "string",
    "default": "ro quiet",
    "description": "Kernel parameters passed to the boot entries."
  },
  "u_boot_root": {
    "type": "string",
    "default": "",
    "description": "Root device specification for the kernel."
  },
  "u_boot_timeout": {
    "type": "string",
    "default": "50",
    "description": "Timeout in seconds before booting the default entry."
  },
  "u_boot_fdt": {
    "type": "string",
    "default": "",
    "description": "Specific Device Tree Blob to use."
  },
  "u_boot_fdt_dir": {
    "type": "string",
    "default": "/usr/lib/linux-image-",
    "description": "Directory containing kernel images and DTBs."
  },
  "u_boot_fdt_overlays": {
    "type": "string",
    "default": "",
    "description": "Specific FDT overlays to apply."
  },
  "u_boot_fdt_overlays_dir": {
    "type": "string",
    "default": "/boot/dtbo/",
    "description": "Directory containing device tree overlays."
  },
  "u_boot_sync_dtbs": {
    "type": "string",
    "default": "false",
    "description": "Whether to synchronize DTBs automatically.",
    "enum": [
      "true",
      "false"
    ]
  }
}
