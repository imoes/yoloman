{
  "device": {
    "type": "string",
    "default": "/dev/ttyS0",
    "description": "The serial port device file to read modem status lines from."
  },
  "no_loop": {
    "type": "bool",
    "default": false,
    "description": "If true, statserial displays status once and exits instead of looping."
  },
  "decimal": {
    "type": "bool",
    "default": false,
    "description": "If true, print modem status as a decimal number."
  },
  "hexa": {
    "type": "bool",
    "default": false,
    "description": "If true, print modem status as a hexadecimal number."
  }
}
