{
  "port": {
    "type": "number",
    "default": 8080,
    "description": "The TCP port number for the ucrpf1host service to listen on."
  },
  "host": {
    "type": "string",
    "default": "localhost",
    "description": "The hostname or IP address of the Panowin F1 3D printer."
  },
  "device": {
    "type": "string",
    "default": "/dev/ttyUSB0",
    "description": "The serial device path for direct printer communication."
  },
  "baud_rate": {
    "type": "number",
    "default": 115200,
    "description": "The baud rate for serial communication with the printer."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable verbose logging output."
  }
}
