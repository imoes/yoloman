{
  "cs_warn": {
    "type": "string",
    "default": "Warning",
    "description": "Warning string pattern to detect in checkpc output"
  },
  "cs_error": {
    "type": "string",
    "default": "lp\\:  Need to fix",
    "description": "Error string pattern to detect in checkpc output"
  },
  "cs_lock_own": {
    "type": "string",
    "default": "Warning - owner/group of ./var/lock. are 0/0, not 7/7",
    "description": "Specific lock file ownership warning to ignore"
  },
  "cs_lock_perm": {
    "type": "string",
    "default": "Warning - permissions of ./var/lock. are 041777, not 042700",
    "description": "Specific lock file permission warning to ignore"
  },
  "cs_miss_lockfile": {
    "type": "string",
    "default": "Warning -   LPD Lockfile .* does not exist!",
    "description": "Specific missing lockfile warning to ignore"
  },
  "cs_rv_match": {
    "type": "string",
    "default": "0",
    "description": "Return value string indicating errors were found (match for grep -v logic)"
  },
  "cs_error_message": {
    "type": "string",
    "default": "cron.daily found errors, run checkpc for details.",
    "description": "Syslog message to log when errors are detected"
  }
}
