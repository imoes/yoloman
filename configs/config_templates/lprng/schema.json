{
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debugging output to stderr (only for troubleshooting)"
  },
  "spool_dir": {
    "type": "string",
    "default": "/var/spool/lpd",
    "description": "Root directory for printer spool files (queues, job logs, status)"
  },
  "lpd_user": {
    "type": "string",
    "default": "lp",
    "description": "System user account under which the lpd daemon runs for security isolation"
  },
  "max_jobs": {
    "type": "number",
    "default": 10,
    "description": "Maximum number of print jobs that may be queued per printer simultaneously"
  },
  "max_job_size": {
    "type": "number",
    "default": 0,
    "description": "Maximum size (in bytes) for a single print job. Set to 0 for unlimited size"
  },
  "remote_printer_connect_timeout": {
    "type": "number",
    "default": 60,
    "description": "Seconds to wait when connecting to a remote printer before timing out"
  },
  "bind_addr": {
    "type": "string",
    "default": "*",
    "description": "IP address for lpd to bind to. Use '*' for all interfaces, or a specific IPv4/IPv6 address"
  },
  "port": {
    "type": "number",
    "default": 515,
    "description": "TCP port on which lpd listens for incoming LPR connections"
  },
  "ipv6": {
    "type": "bool",
    "default": false,
    "description": "Enable support for IPv6 connections to the lpd service"
  },
  "extended": {
    "type": "bool",
    "default": true,
    "description": "Enable LPRng extensions (e.g., job control, status queries) beyond strict LPR protocol"
  },
  "log_job": {
    "type": "bool",
    "default": true,
    "description": "Log print job events (start, completion, errors) to the system syslog"
  },
  "log_level": {
    "type": "number",
    "default": 1,
    "description": "Verbosity level for debug logging (0\u20137); higher values increase log detail"
  },
  "default_printer": {
    "type": "string",
    "default": "",
    "description": "Name of the default printer queue to use when no destination is specified"
  },
  "printers": {
    "type": "list",
    "default": [],
    "description": "List of printers configured on this system; each entry defines a print queue",
    "items": {
      "name": {
        "type": "string",
        "description": "Unique name of the printer queue (used as destination in LPR/LP commands)"
      },
      "comment": {
        "type": "string",
        "default": "",
        "description": "Human-readable description of the printer"
      },
      "device": {
        "type": "string",
        "description": "Device path or URI for the physical printer (e.g., /dev/lp0, socket://host:9100)"
      },
      "backend": {
        "type": "string",
        "default": "",
        "description": "Optional backend command to process jobs before sending to device (e.g., filter, conversion)"
      },
      "filter": {
        "type": "string",
        "default": "",
        "description": "Path to filter script or program to preprocess print data (e.g., postscript conversion)"
      },
      "options": {
        "type": "list",
        "default": [],
        "description": "List of queue-specific options (e.g., 'prefilter=/usr/bin/pstops', 'job_timeout=300')"
      },
      "auth_required": {
        "type": "bool",
        "default": false,
        "description": "Require user authentication for jobs submitted to this queue"
      }
    }
  },
  "access_rules": {
    "type": "list",
    "default": [],
    "description": "List of host/network access control rules for the LPR service",
    "items": {
      "action": {
        "type": "string",
        "enum": [
          "allow",
          "deny"
        ],
        "description": "Whether to allow or deny the specified source"
      },
      "source": {
        "type": "string",
        "description": "Hostname, IP address, or CIDR subnet (e.g., 192.168.1.0/24)"
      }
    }
  }
}
