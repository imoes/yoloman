{
  "backends_type": {
    "type": "string",
    "default": "yaml",
    "description": "The backend type for Hiera ENC (e.g., yaml)."
  },
  "datadir": {
    "type": "string",
    "default": "/etc/puppet/hiera-enc/nodes",
    "description": "The directory where Hiera YAML data files are stored."
  },
  "default_hierarchy_name": {
    "type": "string",
    "default": "\"default\"",
    "description": "The final fallback hierarchy level name in quotes."
  }
}
