{
  "kdc_daemon_args": {
    "type": "string",
    "default": "",
    "description": "Additional arguments to pass to the krb5kdc daemon via DAEMON_ARGS."
  },
  "kdc_pidfile": {
    "type": "string",
    "default": "/var/run/krb5kdc.pid",
    "description": "Path to the PID file used for tracking the KDC process."
  }
}
