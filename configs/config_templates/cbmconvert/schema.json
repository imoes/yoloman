{
 "canonical_names": {
  "default": false,
  "description": "When output_format='raw', use ISO 9660-compliant filenames (adds -N)",
  "type": "bool"
 },
 "cpm_opts": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "cpm_ver": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "description": {
  "default": "",
  "description": "Free-form comment describing the conversion task",
  "type": "string"
 },
 "disk_switch": {
  "default": "on_space",
  "description": "When to switch disk images: 'on_space' (default), 'on_dup', or 'off'",
  "type": "string"
 },
 "dos_opts": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "dos_ver": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "duplicate_handling": {
  "default": "stop",
  "description": "Behavior on duplicate file names: 'skip', 'stop' (default), or 'overwrite'\n      ",
  "enum": [
   "skip",
   "stop"
  ],
  "type": "string"
 },
 "input_files": {
  "default": [],
  "description": "List of input archive/tape/disk/image files to convert",
  "type": "list"
 },
 "input_format": {
  "default": "raw",
  "description": "Input format: one of 'raw', 'pc64', 'arc', 'arkive', 'lynx', 't64', 'c2n', 'dos_4', 'dos_7', 'dos_8', 'cpm_4', 'cpm_7', 'cpm_8'",
  "enum": [
   "raw",
   "pc64",
   "arc",
   "arkive",
   "lynx",
   "t64",
   "c2n"
  ],
  "type": "string"
 },
 "output_format": {
  "default": "raw",
  "description": "Output format: one of 'raw', 'pc64', 'lynx', 'c2n', 'dos_4', 'dos_7', 'dos_8', 'cpm_4', 'cpm_7', 'cpm_8' (with optional suffixes 'd' and 'o')\n      ",
  "enum": [
   "raw",
   "pc64",
   "lynx",
   "c2n"
  ],
  "type": "string"
 },
 "verbose": {
  "default": 1,
  "description": "Verbosity level: 0 (errors only), 1 (warnings+errors), 2 (all messages)",
  "type": "number"
 }
}
