{# ============================================================================= #}
{# Claws Mail Attach-Remover Plugin Configuration Placeholder                   #}
{# ============================================================================= #}
{#
# NOTE: This plugin (att_remover) for Claws Mail does NOT use a dedicated
# configuration file. It is loaded as a compiled shared object (.so) plugin
# and its behavior is configured via the Claws Mail GUI:
#   Main Menu -> Configuration -> Plugins... -> Select "Att Remover" -> Config...
#
# If you need to enforce settings across multiple users, you would typically
# configure the entire Claws Mail profile (prefs.py or config files in ~/.claws-mail)
# rather than this specific plugin.
#
# This template serves as a documentation anchor and ensures role consistency.
# No dynamic configuration is emitted here as there is no target file.
#}
#
{# End of template: no configuration directives are written to disk. #}