{
  "session_name": {
    "type": "string",
    "default": "budgie",
    "description": "Name of the session; used as the 'Name' key in the [budgie Session] group. Can be localized."
  },
  "required_components": {
    "type": "list",
    "default": [],
    "description": "List of required component identifiers (e.g., desktop file names like 'budgie-desktop'); will always be started. Must be semicolon-joined in output."
  }
}
