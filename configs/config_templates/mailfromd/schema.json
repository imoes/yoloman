{
  "run_user": {
    "type": "string",
    "default": "mailfromd",
    "description": "System user to run the daemon as (must exist)."
  },
  "run_group": {
    "type": "string",
    "default": "mailfromd",
    "description": "System group to run the daemon as (must exist)."
  }
}
