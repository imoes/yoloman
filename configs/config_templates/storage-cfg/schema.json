{
  "storages": {
    "type": "list",
    "description": "Proxmox storage definitions: [{type: dir|lvm|lvmthin|zfspool|nfs|cifs|..., id, options: [{key, value}]}]. Each option is one indented `key value` line under the section header `type: id`."
  }
}
