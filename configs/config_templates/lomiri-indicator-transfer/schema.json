{
  "executible_path": {
    "type": "string",
    "default": "/usr/libexec/lomiri-indicator-transfer/lomiri-indicator-transfer-service",
    "description": "Path to the service executable."
  },
  "only_show_in": {
    "type": "string",
    "default": "Unity;GNOME;",
    "description": "Semicolon-separated list of desktop environments where the application should appear."
  },
  "nodisplay": {
    "type": "bool",
    "default": true,
    "description": "Whether the entry should be hidden from menus."
  },
  "startup_notify": {
    "type": "bool",
    "default": false,
    "description": "Whether to show startup notification."
  },
  "terminal": {
    "type": "bool",
    "default": false,
    "description": "Whether the application should be run in a terminal."
  },
  "autostart_condition": {
    "type": "string",
    "default": "GNOME3 unless-session gnome",
    "description": "Conditions under which the application should autostart."
  }
}
