{
  "ssl_cert_client": {
    "type": "string",
    "default": "/etc/mysql/client-cert.pem",
    "description": "Path to the client SSL certificate file."
  },
  "ssl_key_client": {
    "type": "string",
    "default": "/etc/mysql/client-key.pem",
    "description": "Path to the client SSL key file."
  },
  "ssl_verify_server_cert": {
    "type": "bool",
    "default": false,
    "description": "If set to on, the client verifies the server's certificate CN matches the hostname."
  }
}
