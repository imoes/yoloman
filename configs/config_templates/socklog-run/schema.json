{
  "log_port": {
    "type": "number",
    "default": 0,
    "description": "The UDP port socklog listens on for syslog messages. 0 often implies a default or auto-determined behavior depending on the socklog version/configuration."
  },
  "max_queue_size": {
    "type": "number",
    "default": 514,
    "description": "The maximum number of messages to keep in the in-memory queue when the logger is slow or down."
  }
}
