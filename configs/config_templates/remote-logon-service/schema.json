{
  "servers": {
    "type": "string",
    "default": "Session Broker",
    "description": "Comma-separated list of server identifiers to connect to."
  },
  "name": {
    "type": "string",
    "default": "Remote Logon",
    "description": "Human-readable name for the session broker server."
  },
  "uri": {
    "type": "string",
    "default": "http://localhost:8080/uccs/inifile/",
    "description": "The URI endpoint for the Session Broker's ini file service."
  }
}
