{
  "field_delimiter": {
    "type": "string",
    "default": "\\t",
    "description": "The field delimiter character used in the output (default is TAB). Corresponds to the -t option."
  },
  "info2_file": {
    "type": "string",
    "default": "/mnt/windows/Recycled/Info2",
    "description": "The path to the INFO2 file to parse. Corresponds to the <file> argument."
  }
}
