{
 "COMMAND": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "DEVICE": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "LAYOUT": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "TRAY_FLAG": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "command_only": {
  "default": true,
  "description": "If true, outputs only the executable command string. If false, outputs key-value pairs.",
  "type": "bool"
 },
 "device": {
  "default": "/dev/input/js0",
  "description": "Path to the joystick device node to monitor. Corresponds to the --device option.",
  "type": "string"
 },
 "layout_name": {
  "default": "default",
  "description": "The name of the layout file to load. Passed as the positional argument.",
  "type": "string"
 },
 "notray": {
  "default": false,
  "description": "If true, passes the --notray flag to disable the system tray icon.",
  "type": "bool"
 }
}
