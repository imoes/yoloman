{
  "log_file": {
    "type": "string",
    "default": "/var/log/ganesha/ganesha.log",
    "description": "Path to the Ganesha NFS server log file."
  },
  "config_file": {
    "type": "string",
    "default": "/etc/ganesha/ganesha.conf",
    "description": "Path to the main Ganesha NFS server configuration file."
  },
  "log_level": {
    "type": "string",
    "default": "NIV_EVENT",
    "description": "Initial log level for the Ganesha NFS server."
  },
  "epoch_exec_script": {
    "type": "string",
    "default": "/bin/true",
    "description": "Script executed at epoch time for notifications or actions."
  },
  "nodeid_exec_script": {
    "type": "string",
    "default": "",
    "description": "Script used to determine the unique node ID. Leave empty for default behavior."
  }
}
