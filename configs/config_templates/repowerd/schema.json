{
  "log_file_name": {
    "type": "string",
    "default": "repowerd.log",
    "description": "The name of the log file for repowerd."
  },
  "log_rotation_count": {
    "type": "number",
    "default": 4,
    "description": "Number of rotated log files to keep."
  },
  "log_max_size": {
    "type": "string",
    "default": "10M",
    "description": "Maximum size of the log file before rotation."
  },
  "pid_file_path": {
    "type": "string",
    "default": "/var/run/rsyslogd.pid",
    "description": "Path to the rsyslogd PID file, used to signal the service."
  }
}
