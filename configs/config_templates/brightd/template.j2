# Defaults for brightd
# installed at /etc/default/brightd by the maintainer scripts

# Whether brightd should be started on login (must be 'true' to enable)
START_BRIGHTD="{{ START_BRIGHTD | default('true') }}"

# Additional command-line options passed to the brightd daemon
DAEMON_OPTS="{{ DAEMON_OPTS | default('-w 5') }}"