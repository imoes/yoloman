{
  "START_BRIGHTD": {
    "type": "string",
    "default": "true",
    "description": "Whether brightd should be started on login (must be 'true' to enable)"
  },
  "DAEMON_OPTS": {
    "type": "string",
    "default": "-w 5",
    "description": "Additional command-line options passed to the brightd daemon"
  }
}
