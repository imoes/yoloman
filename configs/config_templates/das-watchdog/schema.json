{
  "service_name": {
    "type": "string",
    "default": "das_watchdog",
    "description": "Name of the service (used in script name, init.d entries, and process description)"
  },
  "daemon_name": {
    "type": "string",
    "default": "das_watchdog",
    "description": "Name of the daemon binary (used in DAEMON path and start-stop-daemon calls)"
  },
  "short_description": {
    "type": "string",
    "default": "Watchdog",
    "description": "Short description used in LSB header and startup messages"
  }
}
