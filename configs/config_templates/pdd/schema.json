{
 "mode": {
  "description": "Operational mode: empty for default info, 'timer' for countdown, 'stopwatch' for stopwatch, 'date_diff' or 'time_diff' for calculations.",
  "enum": [
   "timer",
   "stopwatch"
  ],
  "type": "string"
 },
 "operation": {
  "description": "Arithmetic operation: 'add', 'sub', or 'day' (show weekday). Empty for difference.",
  "enum": [
   "add",
   "day"
  ],
  "type": "string"
 },
 "quiet": {
  "default": false,
  "description": "Enable quiet mode to suppress output until interrupted or command execution.",
  "type": "bool"
 },
 "run_command": {
  "default": "",
  "description": "Shell command to run when a countdown timer reaches zero (e.g., 'notify-send pdd expired').",
  "type": "string"
 },
 "second_target": {
  "default": "",
  "description": "Secondary date/time/duration argument for comparisons or arithmetic (e.g., 'today', '1983 Jul 3').",
  "type": "string"
 },
 "target": {
  "default": "",
  "description": "Primary date/time/duration argument (e.g., '2024 Jan 15', '00:05:00', '1:2:3').",
  "type": "string"
 }
}
