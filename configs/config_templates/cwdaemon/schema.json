{
  "device": {
    "type": "string",
    "default": "parport0",
    "description": "Device node for the key interface (serial or parallel port). Must be specified without /dev/ prefix (e.g., ttyS0, parport0). Use 'null' for no port keying."
  },
  "port": {
    "type": "number",
    "default": "6789",
    "description": "UDP port on which cwdaemon listens for client connections."
  },
  "wpm": {
    "type": "number",
    "default": "24",
    "description": "Morse code transmit speed in words per minute (WPM)."
  },
  "ptt_delay": {
    "type": "number",
    "default": "0",
    "description": "PTT (push-to-talk) delay in milliseconds."
  },
  "audio_gain": {
    "type": "number",
    "default": "70",
    "description": "Audio output gain (0\u2013100), only applicable if cwdaemon is built with audio support."
  },
  "weight": {
    "type": "number",
    "default": "0",
    "description": "Dit/dah timing weight (50 = straight key; lower = faster dits, higher = slower dits)."
  }
}
