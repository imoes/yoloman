# deepin-deb-installer is a CLI tool; there is no persistent configuration file.
# The tool accepts positional arguments (file paths) and optional flags.
# Since no config file exists, this template outputs a minimal stub comment only.
# deepin-deb-installer configuration
# This package does not provide a configuration file.
# Use command-line invocation: deepin-deb-installer [FILES...] [OPTIONS...]
# Valid options: -h/--help, -v/--version