{
  "target_address": {
    "type": "string",
    "default": "192.168.1.1",
    "description": "The IP address or hostname of the Windows system to probe."
  },
  "username": {
    "type": "string",
    "default": "",
    "description": "The username to use for authentication. Leave empty for no authentication."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "The password to use for authentication."
  },
  "protocols": {
    "type": "list",
    "default": [
      "445/SMB"
    ],
    "description": "List of protocols to use for the probe. Supported values are '445/SMB' and '139/SMB'."
  }
}
