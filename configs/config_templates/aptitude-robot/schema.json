{
  "config_file": {
    "type": "string",
    "default": "aptitude-robot",
    "description": "Filename (without path) of the shell config file in /etc/default/ to source environment variables from."
  },
  "max_random_delay_env_var": {
    "type": "string",
    "default": "MAX_RANDOM_DELAY_SECONDS",
    "description": "Environment variable name used to pass the maximum random delay (in seconds) to the random-delay script."
  }
}
