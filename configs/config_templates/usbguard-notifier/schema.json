{
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode to provide verbose output."
  },
  "wait": {
    "type": "bool",
    "default": true,
    "description": "Wait until an active IPC connection to usbguard-daemon is established."
  },
  "allowed_entities": {
    "type": "list",
    "default": [],
    "description": "List of usernames or group names (prefixed with @) allowed to connect to the usbguard IPC interface for listening to signals."
  }
}
