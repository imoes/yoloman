{
  "size": {
    "type": "number",
    "default": 10000,
    "description": "Maximum number of lines to keep in memory."
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "If true, only display a top at exit."
  },
  "line_by_line": {
    "type": "number",
    "default": 10,
    "description": "Number of results to print per line in machine-friendly format."
  },
  "interval": {
    "type": "number",
    "default": 1,
    "description": "Interval between graphical updates in seconds."
  }
}
