# =============================================================================
# jstest-gtk Configuration Template
# =============================================================================
#
# This package configures 'jstest-gtk', a graphical tool for testing game
# controllers and joysticks on Linux.
#
# IMPORTANT: jstest-gtk is an interactive, user-space GUI application.
# It does NOT run as a daemon, listen on network ports, or use a static
# configuration file for service management.
#
# Consequently, this template contains NO service directives, no network
# bindings, and no persistent daemon configuration. It is provided as a
# self-documenting placeholder to satisfy the Ansible role infrastructure
# requirements for package-level configuration assets.
#
# Any runtime arguments or GUI preferences are handled by the user's
# desktop environment or command-line invocation, not by a system
# configuration file managed by Ansible.
#
# =============================================================================

# This file serves as a structural marker for the jstest-gtk package.
# No active configuration directives are required for this utility.
#
# Variables defined in the playbook are available but unused, as the
# application does not read a system-wide config file.
# {{ app_name | default('jstest-gtk') }}
# {{ debug_mode | default(false) | tojson }}
# {{ log_level | default('info') }}

# End of jstest-gtk configuration.