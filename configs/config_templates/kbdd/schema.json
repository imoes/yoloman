{
  "nodaemon": {
    "type": "bool",
    "default": false,
    "description": "If true, run kbdd in the foreground instead of detaching as a daemon. Useful for debugging or systemd exec_start."
  },
  "initial_group": {
    "type": "number",
    "default": 0,
    "description": "The keyboard layout group index (0-based) to activate upon startup. Corresponds to the uint32 argument in dbus set_layout(uint32)."
  }
}
