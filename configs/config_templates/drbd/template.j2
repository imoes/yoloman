# drbd.conf — Global configuration for DRBD resource definitions

# Global options affecting overall DRBD behavior
{{ global_options | to_nice_json(indent=2) | replace('"', '') | replace('{', '').replace('}', '') | indent(1) }}

# Include external configuration files (if any) — do NOT remove this include if used in production
include "/etc/drbd.d/*.res";
