{
  "should_start": {
    "type": "string",
    "default": "postgresql mysql keystone",
    "description": "Services that should be started before nova-scheduler"
  },
  "should_stop": {
    "type": "string",
    "default": "postgresql mysql keystone",
    "description": "Services that should be stopped after nova-scheduler"
  },
  "desc": {
    "type": "string",
    "default": "OpenStack Nova Scheduler (nova-scheduler)",
    "description": "Description of the service"
  },
  "project_name": {
    "type": "string",
    "default": "nova",
    "description": "The project name used for variable expansion and paths"
  },
  "systemd_type": {
    "type": "string",
    "default": "notify",
    "description": "Systemd service type"
  },
  "systemd_notify_access": {
    "type": "string",
    "default": "all",
    "description": "Systemd notify access level",
    "enum": [
      "none",
      "all"
    ]
  },
  "os_oslo_messaging_rabbit_processname": {
    "type": "string",
    "default": "nova-scheduler",
    "description": "RabbitMQ process name exported for oslo.messaging"
  },
  "system_user_default": {
    "type": "string",
    "default": "nova",
    "description": "Default system user if SYSTEM_USER is not set"
  },
  "system_group_default": {
    "type": "string",
    "default": "nova",
    "description": "Default system group if SYSTEM_GROUP is not set"
  }
}
