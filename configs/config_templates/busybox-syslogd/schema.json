{
  "syslog_opts": {
    "type": "string",
    "default": "-C128",
    "description": "Additional options passed to the syslogd daemon (e.g., buffer size, log level, remote logging settings)."
  },
  "klog_opts": {
    "type": "string",
    "default": "",
    "description": "Additional options passed to the klogd daemon (e.g., kernel log level, priority filtering).\", \"enum\": [\"-n\", \"-d\", \"-f\", \"-i\", \"-o\", \"-q\", \"-s\", \"-t\", \"-v\"]"
  }
}
