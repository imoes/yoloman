{
  "TFTP_ROOT": {
    "type": "string",
    "default": "/var/lib/tftpboot",
    "description": "TFTP root directory for netboot files"
  },
  "MIRROR_REGEXPS": {
    "type": "string",
    "default": "s=://deb.debian.org/dists/=://deb.debian.org/debian/dists/=",
    "description": "Space-separated sed regex patterns to rewrite di-sources.list URLs for preferred mirror"
  },
  "NONFREE_FW": {
    "type": "string",
    "default": "https://cdimage.debian.org/cdimage/unofficial/non-free/firmware/",
    "description": "Base URL for non-free firmware"
  },
  "DEBIAN_KEYRING": {
    "type": "string",
    "default": "/usr/share/keyrings/debian-archive-keyring.gpg",
    "description": "Path to Debian archive keyring for signature verification"
  },
  "UBUNTU_KEYRING": {
    "type": "string",
    "default": "/usr/share/keyrings/ubuntu-archive-keyring.gpg",
    "description": "Path to Ubuntu archive keyring for signature verification"
  },
  "SQUASHIMG": {
    "type": "string",
    "default": "tftp://\\${pxe_default_server}/${LIVE_DIR}/${ISO_NAME}/live/filesystem.squashfs",
    "description": "URL template to fetch the squashfs image of Debian Live root filesystem"
  }
}
