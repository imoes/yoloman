{
  "_comment_items_structure": {
    "description": "Each item in the 'options' list should be a map with 'name' (string) and 'value' (string/number/bool).",
    "type": "object"
  },
  "items": {
    "default": "",
    "description": "Not used directly, defines structure of list items in schema.",
    "type": "string"
  },
  "options": {
    "default": [],
    "description": "List of configuration options to apply to ncal. Each item is a flat map of option name and value.",
    "type": "list"
  },
  "options.name": {
    "description": "Name of the ncal option (e.g., 'show_julian', 'start_day', 'months_ahead').",
    "enum": [
      "julian",
      "easter",
      "month",
      "country_code",
      "year"
    ],
    "type": "string",
    "enum_open": true
  },
  "options.value": {
    "default": "",
    "description": "Value for the option. For bool options, use 'true'/'false'. For others, use the specific value.",
    "type": "string"
  }
}
