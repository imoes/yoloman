{
  "service_user": {
    "type": "string",
    "default": {
      "value": "a2jmidid",
      "description": "System user account under which the service will run"
    },
    "description": "Unix user for running the service (must exist)"
  },
  "service_group": {
    "type": "string",
    "default": {
      "value": "a2jmidid",
      "description": "System group for the service user"
    },
    "description": "Unix group for the service user (must exist)"
  }
}
