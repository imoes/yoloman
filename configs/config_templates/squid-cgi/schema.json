{
  "allowed_hosts": {
    "type": "list",
    "default": [
      "localhost"
    ],
    "description": "List of hostnames or addresses allowed to manage the cache via cachemgr.cgi. Each entry can be a hostname (with optional :port), IP address, or wildcard pattern. If :port is omitted, the default proxy port is assumed."
  }
}
