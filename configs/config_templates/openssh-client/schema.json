{
  "strict_host_key_checking": {
    "type": "string",
    "default": "auto",
    "description": "How to handle unknown or changed host keys. Use 'auto' (accept new), 'yes' (strict), or 'no' (disable)."
  },
  "log_level": {
    "type": "string",
    "default": "ERROR",
    "description": "Logging verbosity: QUIET, FATAL, ERROR, INFO, VERBOSE, DEBUG, etc. Note: The default ssh_config file does not set LogLevel explicitly, but this variable is available for global override if uncommented/added. In this specific verbatim template, LogLevel is not present, so this variable is kept for schema completeness but not rendered in the template block provided below unless added. However, adhering to 'replace ONLY site-specific literal values' and the file does NOT contain a LogLevel directive in the active section. The commented out lines do not count as active config. Wait, looking at the file again: there is NO LogLevel directive in the Host * block. The instructions say 'Replace ONLY site-specific literal values'. Since LogLevel is not in the file, I cannot replace it. I must only parametrize what is there. The provided schema has 'log_level', but if the directive isn't in the real file, I shouldn't invent it. I will only parametrize directives that actually exist in the <Host *> block. The directives present are: SendEnv, HashKnownHosts, GSSAPIAuthentication. The commented lines are comments. I should NOT uncomment them. I will ONLY replace the values of active directives if they match the schema. Active directives: SendEnv (not in schema), HashKnownHosts (not in schema), GSSAPIAuthentication (not in schema). This is tricky. The schema provided is for 'openssh-client'. The real file has very few active settings. I must strictly follow 'Replace ONLY site-specific literal values'. If a value in the schema corresponds to a literal in the file, I replace it. If not, I don't. Let's look at the commented lines. They are comments. I must keep them verbatim. So I cannot parametrize commented-out values unless I uncomment them, which changes the config structure. I must keep everything byte-for-byte verbatim EXCEPT replacing literals with {{ vars }}. The active lines are:\n    SendEnv LANG LC_* COLORTERM NO_COLOR\n    HashKnownHosts yes\n    GSSAPIAuthentication yes\nNone of these are in the provided values_schema. The provided schema has: strict_host_key_checking, log_level, forward_agent, forward_x11, connect_timeout, password_authentication, preferred_authentications, server_alive_interval, server_alive_count_max, compression, protocol, tcp_keepalive, host_configs. \nConnectTimeout is commented out (# ConnectTimeout 0). \nStrictHostKeyChecking is commented out (# StrictHostKeyChecking ask).\nForwardAgent is commented out.\nForwardX11 is commented out.\nPasswordAuthentication is commented out.\n\nIf I strictly follow 'Keep EVERYTHING ELSE BYTE-FOR-BYTE VERBATIM', I cannot touch comments. Therefore, I cannot parametrize commented-out lines. \nHowever, often in these tasks, if a schema value maps to a commented default, it might be expected to uncomment and parametrize? No, the prompt says 'Do NOT add... anything'.\nLet's re-read carefully: 'Replace ONLY site-specific literal values an admin would change'. If the value is commented out, it's not active. An admin changing it would have to uncomment it. \nBut wait, look at the commented lines. They are examples. \nIs it possible the prompt implies that I should ONLY parametrize lines that are currently active AND in the schema? If so, none of the active lines (SendEnv, HashKnownHosts, GSSAPIAuthentication) are in the schema. \nThis suggests the template might effectively be the same as the input if no active lines match. \nOR, perhaps I am supposed to parametrize the commented values by leaving them commented? e.g. `# StrictHostKeyChecking {{ strict_host_key_checking }}`. This keeps the line structure (commented out) but parametrizes the value. This is a common pattern for 'default' configs. The prompt says 'Replace ONLY site-specific literal values... with {{ variable }}'. It does not explicitly forbid changing the text inside comments if it represents a default value. But 'BYTE-FOR-BYTE VERBATIM' usually applies to the structure. Changing `ask` to `{{ strict_host_key_checking }}` inside a comment is a literal value replacement. \nLet's look at `ConnectTimeout 0`. It is commented. \nIf I replace commented values, I am altering the file content (the literal string '0' becomes '{{ connect_timeout }}'). Is that 'byte-for-byte verbatim'? No, the bytes change. But the prompt says 'Replace ONLY site-specific literal values... with {{ variable }}'. This is the overriding instruction. The 'verbatim' instruction applies to 'Include', 'module load', 'sections', 'comments' (meaning keep the comment markers?), 'directives'. \nActually, usually 'verbatim' means don't restructure. Replacing `ask` with `{{ strict_host_key_checking }}` in a comment is a valid parametrization of the *default* value provided in that comment. \nLet's assume I should parametrize the values in the commented lines if they map to the schema, keeping the `# ` prefix.\n\nActive lines:\nSendEnv LANG LC_* COLORTERM NO_COLOR (Not in schema)\nHashKnownHosts yes (Not in schema)\nGSSAPIAuthentication yes (Not in schema)\n\nCommented lines in schema:\n# ConnectTimeout 0 -> # ConnectTimeout {{ connect_timeout }}\n# StrictHostKeyChecking ask -> # StrictHostKeyChecking {{ strict_host_key_checking }}\n# ForwardAgent no -> # ForwardAgent {{ forward_agent }} (bool needs yes/no conversion? Jinja {{ forward_agent | bool | ternary('yes','no') }} is forbidden. No filters allowed. Only `{{ variable }}`. So I cannot parametrize bools in comments easily without filters if the config expects yes/no and the var is true/false. The schema has bool. The config file uses yes/no. I cannot use filters. I cannot convert. So I should probably NOT parametrize the commented lines if I can't use filters to convert bools to yes/no, OR I assume the user will handle the value format. But the prompt says 'sample_values: the shipped default values'. If the sample value for forward_agent is `false`, and I put `# ForwardAgent false`, it's invalid SSH config. \nHowever, the prompt says 'Use no Jinja beyond {{ variable }}'. This strongly implies I should only parametrize values where the type matches directly (string, number). For bools, if the file uses yes/no, and I can't use filters, I might skip parametrizing commented bools, OR I output the string representation. But 'sample_values' must reproduce the real file. If I render `# ForwardAgent false`, it doesn't reproduce `# ForwardAgent no`. \nTherefore, I will ONLY parametrize lines where the variable type matches the file syntax directly, or where no conversion is needed. \nConnectTimeout is a number. `# ConnectTimeout {{ connect_timeout }}`. Default 10. Sample: `# ConnectTimeout 10`. Wait, real file is `0`. My schema default is `10`. The prompt says 'sample_values: the shipped default values'. So the sample value for connect_timeout should be `0`? No, the schema says `default: 10`. The 'sample_values' in the output should be the values used to render the template. If the schema default is 10, the rendered file will have 10. But the instruction says 'sample_values: the shipped default values, so rendering reproduces the real file'. This is a contradiction if the schema default differs from the file literal. Usually, 'sample_values' means 'values that represent the current state/default'. If the file has 0, the sample value for that variable should be 0. But the schema provided has 10. I must reuse the provided schema. I will use the schema's default for the 'default' key, but for 'sample_values', I should probably use the file's literal value to show how it maps? Or use the schema default? The prompt says 'sample_values: the shipped default values'. This implies I should use the values FROM THE FILE as the sample_values to demonstrate the parametrization. But I must also provide the schema defaults. \nLet's look at the output format. `sample_values` is a dict. It should contain the values used to render the template. If I use the schema default (10), the rendered template will have 10. The prompt says 'so rendering reproduces the real file'. This implies sample_values must be the file's actual literals. But the schema says default 10. I will set sample_values to the file's literals (0, ask, no, etc) for the commented lines, and for active lines... there are no active lines in the schema. \nWait, if I use file literals for sample_values, but the schema default is different, that's fine. The sample_values are just examples. \nHowever, I can only parametrize if I can represent the value. \nLet's parametrize the commented lines that match types:\n# ConnectTimeout 0 -> # ConnectTimeout {{ connect_timeout }}\n# StrictHostKeyChecking ask -> # StrictHostKeyChecking {{ strict_host_key_checking }}\nFor bools like ForwardAgent, I cannot use filters. I will leave them as is or skip. Given the constraint 'No filters', I will skip parametrizing commented bools because I can't convert `true`/`false` to `yes`/`no` safely without filters or complex logic, and the prompt forbids it. \nWhat about active lines? None are in the schema. \nSo the template will only have changed commented lines for ConnectTimeout and StrictHostKeyChecking.\n\nLet's check `preferred_authentications`. It's a list. Not in file.\n`log_level`. Not in file.\n\nSo the parametrized template will look like the original, but with `{{ connect_timeout }}` and `{{ strict_host_key_checking }}` in the commented lines.\n\nSample values: \nconnect_timeout: 0 (from file)\nstrict_host_key_checking: 'ask' (from file)\nforward_agent: false (schema default? or file literal 'no'? I'll use file literal 'no' converted to boolean false for the schema? No, sample_values should be the values passed to the renderer. If I don't parametrize forward_agent, I don't need a sample value for it in the context of rendering *this* template. But the schema requires it. I will provide schema defaults for all.\n\nActually, looking at the strict constraint 'Replace ONLY... values... with {{ variable }}', and 'sample_values... so rendering reproduces the real file'. \nIf I parametrize `# ConnectTimeout 0` to `# ConnectTimeout {{ connect_timeout }}`, and sample_value is 0, it reproduces the file. \n\nFinal decision: Parametrize commented numeric/string directives that match the schema. Leave commented bools alone due to no-filter constraint. Leave active lines alone as they don't match schema. \n\nOne more check: `HashKnownHosts yes`. Not in schema. \n`GSSAPIAuthentication yes`. Not in schema. \n\nTemplate:\nInclude /etc/ssh/ssh_config.d/*.conf\n\nHost *\n#   ForwardAgent no\n#   ForwardX11 no\n#   ForwardX11Trusted yes\n#   PasswordAuthentication yes\n#   HostbasedAuthentication no\n#   GSSAPIAuthentication no\n#   GSSAPIDelegateCredentials no\n#   GSSAPIKeyExchange no\n#   GSSAPITrustDNS no\n#   BatchMode no\n#   CheckHostIP no\n#   AddressFamily any\n#   ConnectTimeout {{ connect_timeout }}\n#   StrictHostKeyChecking {{ strict_host_key_checking }}\n... rest verbatim ...",
    "enum": [
      "ERROR",
      "INFO",
      "DEBUG"
    ]
  }
}
