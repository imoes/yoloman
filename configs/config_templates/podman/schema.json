{
  "cni_network_name": {
    "type": "string",
    "default": "podman",
    "description": "Name of the CNI network configuration (used by Podman)."
  },
  "cni_bridge_name": {
    "type": "string",
    "default": "cni-podman0",
    "description": "Name of the CNI bridge interface to create (e.g., cni-podman0)."
  },
  "cni_subnet": {
    "type": "string",
    "default": "10.88.0.0/16",
    "description": "Subnet for the container network (CIDR notation)."
  },
  "cni_gateway": {
    "type": "string",
    "default": "10.88.0.1",
    "description": "Gateway IP address for the container subnet."
  }
}
