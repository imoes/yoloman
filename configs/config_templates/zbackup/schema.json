{
  "non_encrypted": {
    "type": "bool",
    "default": false,
    "description": "If true, the --non-encrypted flag is passed to zbackup."
  },
  "password_file": {
    "type": "string",
    "default": "~/.my_backup_password",
    "description": "Path to the file containing the password for encryption/decryption."
  },
  "threads": {
    "type": "number",
    "default": 1,
    "description": "Number of threads to limit partial LZMA compression to."
  },
  "repo_path": {
    "type": "string",
    "default": "/my/backup/repo",
    "description": "The directory used to hold the backup repository."
  },
  "backup_path": {
    "type": "string",
    "default": "/my/backup/repo/backups/backup-current",
    "description": "The target file path for the backup."
  }
}
