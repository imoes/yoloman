{
  "args": {
    "type": "list",
    "default": [],
    "description": "List of CLI arguments to pass to `duf`, one per line (e.g., `--json`, `--only local`, `--sort size`); arguments with spaces must be quoted (e.g., `--only-mp '/dev/sd*'`); use raw string quoting as needed for shell compatibility"
  }
}
