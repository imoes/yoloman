{
  "enable_config": {
    "type": "bool",
    "default": false,
    "description": "Enable reading user and project configuration from YAML files. Requires --enable-config."
  },
  "compile_commands_dir": {
    "type": "string",
    "default": "",
    "description": "Path to look for compile_commands.json. Falls back to current/parent directories if invalid."
  },
  "query_driver": {
    "type": "list",
    "default": [],
    "description": "Comma-separated list of globs for whitelisting gcc-compatible drivers to extract system includes. Must be a list of glob strings (e.g., ['/usr/bin/**/clang-*'])."
  },
  "all_scopes_completion": {
    "type": "bool",
    "default": false,
    "description": "If true, code completion includes index symbols from non-visible scopes."
  },
  "background_index": {
    "type": "bool",
    "default": true,
    "description": "If true, index project code in the background and persist on disk."
  },
  "background_index_priority": {
    "type": "string",
    "default": "background",
    "description": "Thread priority for background index. Valid values: 'background', 'low', 'normal'.",
    "enum": [
      "background",
      "low",
      "normal"
    ]
  },
  "clang_tidy": {
    "type": "bool",
    "default": false,
    "description": "If true, enable clang-tidy diagnostics."
  },
  "completion_style": {
    "type": "string",
    "default": "detailed",
    "description": "Granularity of code completion suggestions. Valid values: 'detailed', 'bundled'.",
    "enum": [
      "detailed",
      "bundled"
    ]
  },
  "fallback_style": {
    "type": "string",
    "default": "",
    "description": "clang-format style applied when no .clang-format is found."
  },
  "function_arg_placeholders": {
    "type": "bool",
    "default": true,
    "description": "If true, completions include placeholders for function parameters."
  },
  "header_insertion": {
    "type": "string",
    "default": "never",
    "description": "Controls #include insertion on completion acceptance. Valid values: 'iwyu', 'never'.",
    "enum": [
      "iwyu",
      "never"
    ]
  },
  "header_insertion_decorators": {
    "type": "bool",
    "default": false,
    "description": "If true, prepend circular dot/space to completion label based on include insertion."
  },
  "import_insertions": {
    "type": "bool",
    "default": false,
    "description": "If header insertion is enabled, add #import directives for Objective-C."
  },
  "limit_references": {
    "type": "number",
    "default": 1000,
    "description": "Limit number of references returned. 0 means no limit."
  },
  "limit_results": {
    "type": "number",
    "default": 100,
    "description": "Limit number of results returned. 0 means no limit."
  },
  "project_root": {
    "type": "string",
    "default": "",
    "description": "Path to project root. Requires RemoteIndexAddress to be set."
  },
  "remote_index_address": {
    "type": "string",
    "default": "",
    "description": "Address of the remote index server."
  },
  "rename_file_limit": {
    "type": "number",
    "default": 50,
    "description": "Limit number of files affected by symbol renaming. 0 means no limit."
  },
  "log": {
    "type": "string",
    "default": "error",
    "description": "Verbosity of log messages. Valid values: 'error', 'info', 'verbose'.",
    "enum": [
      "error",
      "info",
      "verbose"
    ]
  },
  "offset_encoding": {
    "type": "string",
    "default": "",
    "description": "Force offsetEncoding ('utf-8', 'utf-16', 'utf-32'). Overrides client negotiation.",
    "enum": [
      "utf-8",
      "utf-16",
      "utf-32"
    ]
  },
  "path_mappings": {
    "type": "list",
    "default": [],
    "description": "List of '<client_path>=<server_path>' pairs to translate client/server paths."
  },
  "pch_storage": {
    "type": "string",
    "default": "memory",
    "description": "Where to store PCHs. Valid values: 'disk', 'memory'.",
    "enum": [
      "disk",
      "memory"
    ]
  },
  "malloc_trim": {
    "type": "bool",
    "default": false,
    "description": "If true, release memory periodically via malloc_trim(3)."
  },
  "workers": {
    "type": "number",
    "default": 0,
    "description": "Number of async workers (including background index). Mirrors -j flag. 0 uses default."
  },
  "check": {
    "type": "string",
    "default": "",
    "description": "Parse one file in isolation (e.g., 'main.cpp'). Enables --check mode."
  }
}
