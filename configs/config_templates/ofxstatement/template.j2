# ofxstatement configuration file
# Global debug mode
[general]
# Enable debug output when running ofxstatement commands
# Defaults to false (production-safe). Set to true for verbose logging.
debug = {{ debug | default(False) | lower }}