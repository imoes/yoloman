{
  "measurement": {
    "type": "string",
    "default": "ethtool",
    "description": "Name of measurement to use in InfluxDB line protocol"
  },
  "flags": {
    "type": "list",
    "default": [],
    "description": "List of flag objects for command-line options; each flag must have 'option', and optionally 'value' (unless 'quiet', which takes no value)"
  }
}
