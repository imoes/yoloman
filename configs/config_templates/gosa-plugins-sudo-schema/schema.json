{
  "sudo_schema_dn": {
    "type": "string",
    "default": "cn=sudo,cn=schema,cn=config",
    "description": "The DN for the sudo schema entry."
  },
  "sudo_schema_cn": {
    "type": "string",
    "default": "sudo",
    "description": "The common name for the sudo schema entry."
  }
}
