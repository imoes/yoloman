{
  "local_enable": {
    "type": "bool",
    "default": true,
    "description": "Allow local (system) users to log in to the FTP server."
  },
  "write_enable": {
    "type": "bool",
    "default": true,
    "description": "Enable write operations (upload, delete, rename, etc.) for authenticated users."
  },
  "anon_root": {
    "type": "string",
    "default": "/srv/ftp",
    "description": "Absolute path to the root directory for anonymous users (chroot jail)."
  },
  "chroot_local_user": {
    "type": "bool",
    "default": true,
    "description": "Restrict local users to their home directories (chroot jail)."
  },
  "chroot_list_enable": {
    "type": "bool",
    "default": false,
    "description": "Enable the chroot user list file (see chroot_list_file). If set, only users *not* in the list will be chrooted (if chroot_local_user=YES)."
  },
  "chroot_list_file": {
    "type": "string",
    "default": "/etc/vsftpd.chroot_list",
    "description": "Path to file listing users to exempt from chroot jail (if chroot_list_enable=YES and chroot_local_user=YES)."
  },
  "listen": {
    "type": "bool",
    "default": true,
    "description": "Run vsftpd as a standalone daemon (not under inetd/xinetd)."
  },
  "listen_port": {
    "type": "number",
    "default": 21,
    "description": "TCP port on which vsftpd listens for FTP control connections."
  },
  "listen_ipv6": {
    "type": "bool",
    "default": false,
    "description": "Listen on IPv6 in addition to IPv4 (requires IPv6 support and listen=YES)."
  },
  "pasv_enable": {
    "type": "bool",
    "default": true,
    "description": "Enable passive mode data connections (required for clients behind NAT/firewall)."
  },
  "pasv_min_port": {
    "type": "number",
    "default": 10090,
    "description": "Lowest port to use for passive mode data connections (firewall must allow inbound TCP to this range)."
  },
  "pasv_max_port": {
    "type": "number",
    "default": 10100,
    "description": "Highest port to use for passive mode data connections (firewall must allow inbound TCP to this range)."
  },
  "max_clients": {
    "type": "number",
    "default": 0,
    "description": "Maximum total number of simultaneous client connections. 0 means unlimited."
  },
  "max_per_ip": {
    "type": "number",
    "default": 0,
    "description": "Maximum number of simultaneous connections per client IP. 0 means unlimited."
  },
  "idle_session_timeout": {
    "type": "number",
    "default": 300,
    "description": "Time (seconds) before idle sessions are disconnected (0 = no timeout)."
  },
  "data_connection_timeout": {
    "type": "number",
    "default": 120,
    "description": "Time (seconds) before idle data connections are closed (0 = no timeout)."
  },
  "ftpd_banner": {
    "type": "string",
    "default": "Welcome to FTP service.",
    "description": "Banner message displayed to users before authentication."
  },
  "userlist_enable": {
    "type": "bool",
    "default": true,
    "description": "Enable the user list file (userlist_file) to control access."
  },
  "userlist_file": {
    "type": "string",
    "default": "/etc/vsftpd.user_list",
    "description": "Path to file listing users allowed/denied access (see userlist_deny)."
  },
  "userlist_deny": {
    "type": "bool",
    "default": true,
    "description": "If true, users in userlist_file are denied login. If false, only users in userlist_file are allowed."
  },
  "ssl_enable": {
    "type": "bool",
    "default": false,
    "description": "Enable SSL/TLS encryption (FTPS). Requires valid certificate and key."
  },
  "ssl_certificate": {
    "type": "string",
    "default": "/etc/ssl/certs/vsftpd.crt",
    "description": "Path to SSL/TLS certificate file (PEM format). Required if ssl_enable=YES."
  },
  "ssl_key": {
    "type": "string",
    "default": "/etc/ssl/private/vsftpd.key",
    "description": "Path to SSL/TLS private key file (PEM format). Required if ssl_enable=YES."
  },
  "ssl_ciphers": {
    "type": "string",
    "default": "HIGH:MEDIUM:!aNULL:!MD5",
    "description": "SSL cipher list (OpenSSL syntax) for secure connections."
  },
  "ssl_require_ssl": {
    "type": "bool",
    "default": false,
    "description": "Require SSL/TLS for all logins/data connections (prevents plain-text fallback)."
  },
  "ssl_tlsv1": {
    "type": "bool",
    "default": true,
    "description": "Allow TLSv1 protocol in SSL connections."
  },
  "ssl_sslv2": {
    "type": "bool",
    "default": false,
    "description": "Allow SSLv2 protocol (insecure, not recommended)."
  },
  "ssl_sslv3": {
    "type": "bool",
    "default": false,
    "description": "Allow SSLv3 protocol (insecure, not recommended)."
  },
  "xferlog_enable": {
    "type": "bool",
    "default": true,
    "description": "Enable logging of file transfers to a dedicated log file."
  },
  "xferlog_std_format": {
    "type": "bool",
    "default": false,
    "description": "Write transfer logs in wu-ftpd style (standard) instead of vsftpd extended format."
  },
  "xferlog_file": {
    "type": "string",
    "default": "/var/log/vsftpd.log",
    "description": "Path to file where transfer logs are written (if xferlog_enable=YES)."
  },
  "anon_upload_enable": {
    "type": "bool",
    "default": false,
    "description": "Allow anonymous users to upload files (requires write_enable=YES and proper permissions)."
  },
  "anon_mkdir_write_enable": {
    "type": "bool",
    "default": false,
    "description": "Allow anonymous users to create directories."
  },
  "anon_other_write_enable": {
    "type": "bool",
    "default": false,
    "description": "Allow anonymous users additional write operations (delete, rename, etc.)."
  },
  "anon_world_readable_only": {
    "type": "bool",
    "default": true,
    "description": "Only allow anonymous users to download files with world-readable permissions."
  },
  "local_umask": {
    "type": "string",
    "default": "022",
    "description": "Umask for local users' uploaded files (octal)."
  },
  "virtual_use_local_privs": {
    "type": "bool",
    "default": false,
    "description": "Virtual users get same privileges as local users (if false, they act like anonymous)."
  },
  "anon_umask": {
    "type": "string",
    "default": "077",
    "description": "Umask for anonymous users' uploaded files (octal)."
  },
  "file_open_mode": {
    "type": "string",
    "default": "0644",
    "description": "Permission mode (octal) for uploaded files (e.g., '0644' = rw-r--r--)."
  },
  "tcp_wrappers": {
    "type": "bool",
    "default": false,
    "description": "Enable TCP wrappers (/etc/hosts.allow/deny) access control."
  },
  "pam_service_name": {
    "type": "string",
    "default": "vsftpd",
    "description": "PAM service name used for authentication (e.g., for virtual users)."
  },
  "hide_ids": {
    "type": "bool",
    "default": false,
    "description": "Hide user/group IDs in directory listings (show 'ftp' instead of real names)."
  },
  "local_root": {
    "type": "string",
    "default": "",
    "description": "Absolute path to root directory for *all* local users (overrides home dirs)."
  },
  "user_config_dir": {
    "type": "string",
    "default": "/etc/vsftpd/user_conf",
    "description": "Directory containing per-user config files (filename = username)."
  },
  "ascii_upload_enable": {
    "type": "bool",
    "default": false,
    "description": "Enable ASCII mode upload (converts line endings)."
  },
  "ascii_download_enable": {
    "type": "bool",
    "default": false,
    "description": "Enable ASCII mode download (converts line endings)."
  },
  "syslog_enable": {
    "type": "bool",
    "default": false,
    "description": "Log to syslog instead of xferlog_file (requires xferlog_enable)."
  },
  "connect_timeout": {
    "type": "number",
    "default": 60,
    "description": "Time (seconds) before data connection attempts time out (for active mode)."
  }
}
