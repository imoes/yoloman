{
  "cgit_url_path": {
    "type": "string",
    "default": "cgit",
    "description": "The URL path prefix for the CGit interface."
  },
  "cgit_cgi_path": {
    "type": "string",
    "default": "/usr/lib/cgit/cgit.cgi",
    "description": "The filesystem path to the CGit CGI script."
  },
  "cgit_css_url_path": {
    "type": "string",
    "default": "cgit-css",
    "description": "The URL path prefix for CGit static assets (CSS, images, etc.)."
  },
  "cgit_css_path": {
    "type": "string",
    "default": "/usr/share/cgit",
    "description": "The filesystem path to the CGit static assets directory."
  }
}
