{
 "aria_group_commit": {
  "default": "none",
  "description": "Aria group commit mode (e.g., 'none', 'interval').",
  "type": "string"
 },
 "aria_log_file_size": {
  "default": "1G",
  "description": "Aria log file size.",
  "type": "string"
 },
 "basedir": {
  "default": "/usr",
  "description": "Base directory where MariaDB binaries and libraries are installed.",
  "type": "string"
 },
 "bind_address": {
  "default": "127.0.0.1",
  "description": "IP address to bind to (e.g., '127.0.0.1', '0.0.0.0').",
  "type": "string"
 },
 "binlog_format": {
  "default": "ROW",
  "description": "Binary logging format (ROW, STATEMENT, or MIXED).",
  "type": "string"
 },
 "binlog_path": {
  "default": "/var/log/mysql/mariadb-bin",
  "description": "Base name/path for binary log files.",
  "type": "string"
 },
 "character_set_server": {
  "default": "utf8mb4",
  "description": "Default character set for new databases and tables.",
  "type": "string"
 },
 "collation_server": {
  "default": "utf8mb4_general_ci",
  "description": "Default collation for new databases and tables.",
  "type": "string"
 },
 "datadir": {
  "default": "/var/lib/mysql",
  "description": "Directory where database files (tables, logs, etc.) are stored.",
  "type": "string"
 },
 "default_authentication_plugin": {
  "default": "mysql_native_password",
  "description": "Default authentication plugin (e.g., 'mysql_native_password', 'sha256_password').",
  "type": "string"
 },
 "default_storage_engine": {
  "default": "InnoDB",
  "description": "Default storage engine for newly created tables (e.g., 'InnoDB', 'Aria').",
  "enum": [
   "InnoDB",
   "Aria",
   "MyISAM",
   "MEMORY",
   "CSV",
   "FEDERATEDX",
   "MyRocks",
   "Spider",
   "CONNECT",
   "DuckDB"
  ],
  "type": "string"
 },
 "enable_binlog": {
  "default": true,
  "description": "Enable binary logging (required for replication and point-in-time recovery).",
  "type": "bool"
 },
 "enable_general_log": {
  "default": false,
  "description": "Enable general query log (not recommended for production due to performance impact).",
  "type": "bool"
 },
 "enable_query_cache": {
  "default": false,
  "description": "Enable query cache (deprecated in MariaDB 10.1+; disable unless needed for legacy apps).",
  "type": "bool"
 },
 "enable_rocksdb": {
  "default": false,
  "description": "Enable RocksDB storage engine plugin.",
  "type": "bool"
 },
 "enable_slow_log": {
  "default": true,
  "description": "Enable logging of slow queries (queries exceeding long_query_time).",
  "type": "bool"
 },
 "enable_ssl": {
  "default": false,
  "description": "Enable SSL/TLS for encrypted client connections.",
  "type": "bool"
 },
 "extra_plugins": {
  "default": [],
  "description": "Additional plugins to load (e.g., 'auth_pam.so').",
  "items": {
   "description": "Plugin filename or module name.",
   "type": "string"
  },
  "type": "list"
 },
 "extra_vars": {
  "description": "Arbitrary additional server variables (e.g., {'wsrep_cluster_address': 'gcomm://', 'loose_wsrep_on': 1}).",
  "items": {
   "description": "Variable name (key) → value mapping.",
   "type": "string|number|bool"
  },
  "type": "object"
 },
 "general_log_file": {
  "default": "/var/log/mysql/mariadb.log",
  "description": "Path to the general query log file.",
  "type": "string"
 },
 "gtid_mode": {
  "default": false,
  "description": "Enable GTID-based replication (requires consistent server IDs and binlog).",
  "type": "bool"
 },
 "gtid_slave_pos": {
  "default": "",
  "description": "GTID position to start replication from (empty = auto from master).",
  "type": "string"
 },
 "innodb_buffer_pool_instances": {
  "default": 1,
  "description": "Number of buffer pool instances (use >1 for large buffer pools, e.g., >1GB).",
  "type": "number"
 },
 "innodb_buffer_pool_size": {
  "default": "256M",
  "description": "Size of InnoDB buffer pool (e.g., '256M', '1G'). Should be ~70–80% of available RAM on dedicated servers.",
  "type": "string"
 },
 "innodb_flush_log_at_trx_commit": {
  "default": 1,
  "description": "Controls durability/safety (1 = safest, 2 = faster, 0 = fastest but risky).",
  "type": "number"
 },
 "innodb_flush_method": {
  "default": "fsync",
  "description": "How InnoDB flushes data and logs to disk (e.g., 'fsync', 'O_DIRECT').",
  "type": "string"
 },
 "innodb_log_file_size": {
  "default": "64M",
  "description": "Size of each InnoDB redo log file (affects recovery time and write throughput).",
  "type": "string"
 },
 "innodb_read_io_threads": {
  "default": 4,
  "description": "Number of background I/O threads for InnoDB reads.",
  "type": "number"
 },
 "innodb_write_io_threads": {
  "default": 4,
  "description": "Number of background I/O threads for InnoDB writes.",
  "type": "number"
 },
 "interactive_timeout": {
  "default": 28800,
  "description": "Number of seconds the server waits for activity on an interactive connection.",
  "type": "number"
 },
 "is_master": {
  "default": false,
  "description": "If true, configure this server as a replication master (binary logs enabled automatically).",
  "type": "bool"
 },
 "is_slave": {
  "default": false,
  "description": "If true, configure this server as a replication slave.",
  "type": "bool"
 },
 "local_infile": {
  "default": false,
  "description": "Allow LOCAL INFILE (security-sensitive — disable unless needed).",
  "type": "bool"
 },
 "lock_wait_timeout": {
  "default": 31536000,
  "description": "Number of seconds to wait for a metadata lock before giving up.",
  "type": "number"
 },
 "log_error_file": {
  "default": "/var/log/mysql/error.log",
  "description": "Path to the error log file.",
  "type": "string"
 },
 "long_query_time": {
  "default": 2,
  "description": "Threshold in seconds for logging slow queries.",
  "type": "number"
 },
 "master_host": {
  "default": "",
  "description": "Hostname or IP of the replication master.",
  "type": "string"
 },
 "master_password": {
  "default": "",
  "description": "Password for the replication user (store securely via secrets manager in production).",
  "type": "string"
 },
 "master_port": {
  "default": 3306,
  "description": "Port of the replication master.",
  "type": "number"
 },
 "master_user": {
  "default": "",
  "description": "Replication user account on master.",
  "type": "string"
 },
 "max_allowed_packet": {
  "default": "64M",
  "description": "Maximum size of one packet or any row (e.g., '64M').",
  "type": "string"
 },
 "max_connections": {
  "default": 151,
  "description": "Maximum number of simultaneous client connections allowed.",
  "type": "number"
 },
 "max_heap_table_size": {
  "default": "64M",
  "description": "Maximum size of user-created in-memory temporary tables.",
  "type": "string"
 },
 "open_files_limit": {
  "default": 8192,
  "description": "Maximum number of open files for the server.",
  "type": "number"
 },
 "port": {
  "default": 3306,
  "description": "TCP port number for remote connections.",
  "type": "number"
 },
 "query_cache_limit": {
  "default": "2M",
  "description": "Maximum size of individual query results that may be cached.",
  "type": "string"
 },
 "query_cache_size": {
  "default": "64M",
  "description": "Size of query cache memory (ignored if query cache is disabled).",
  "type": "string"
 },
 "relay_log_index_path": {
  "default": "/var/log/mysql/relay-bin.index",
  "description": "Path to relay log index file.",
  "type": "string"
 },
 "relay_log_path": {
  "default": "/var/log/mysql/relay-bin",
  "description": "Base name/path for relay log files (slave).",
  "type": "string"
 },
 "replicate_do_db": {
  "default": [],
  "description": "List of database names to replicate (master only).",
  "items": {
   "description": "Database name to include in replication.",
   "type": "string"
  },
  "type": "list"
 },
 "replicate_ignore_db": {
  "default": [],
  "description": "List of database names to exclude from replication (master only).",
  "items": {
   "description": "Database name to exclude from replication.",
   "type": "string"
  },
  "type": "list"
 },
 "server_id": {
  "default": 1,
  "description": "Unique numeric identifier for this server in a replication setup.",
  "type": "number"
 },
 "skip_name_resolve": {
  "default": true,
  "description": "Skip hostname lookups for incoming connections (use IP only for security/performance).",
  "type": "bool"
 },
 "slow_query_log_file": {
  "default": "/var/log/mysql/mariadb-slow.log",
  "description": "Path to the slow query log file.",
  "type": "string"
 },
 "socket": {
  "default": "/run/mysqld/mysqld.sock",
  "description": "Unix socket file path for local connections.",
  "type": "string"
 },
 "ssl_ca_path": {
  "default": "/etc/mysql/ssl/ca.pem",
  "description": "Path to SSL CA certificate.",
  "type": "string"
 },
 "ssl_cert_path": {
  "default": "/etc/mysql/ssl/server-cert.pem",
  "description": "Path to SSL server certificate.",
  "type": "string"
 },
 "ssl_key_path": {
  "default": "/etc/mysql/ssl/server-key.pem",
  "description": "Path to SSL server private key.",
  "type": "string"
 },
 "table_open_cache": {
  "default": 400,
  "description": "Number of table descriptors to cache.",
  "type": "number"
 },
 "thread_cache_size": {
  "default": 16,
  "description": "Number of threads to keep in cache for reuse.",
  "type": "number"
 },
 "tmp_table_size": {
  "default": "64M",
  "description": "Maximum size of internal in-memory temporary tables (e.g., '64M').",
  "type": "string"
 },
 "use_syslog": {
  "default": false,
  "description": "Log messages to system syslog instead of log files.",
  "type": "bool"
 },
 "wait_timeout": {
  "default": 28800,
  "description": "Number of seconds the server waits for activity on an idle connection.",
  "type": "number"
 }
}
