{
  "server_name": {
    "type": "string",
    "default": "ProFTPD server",
    "description": "The name of the FTP server."
  },
  "server_admin": {
    "type": "string",
    "default": "root@localhost",
    "description": "The email address of the server administrator."
  },
  "max_instances": {
    "type": "number",
    "default": 20,
    "description": "Maximum number of child processes to prevent DoS attacks."
  }
}
