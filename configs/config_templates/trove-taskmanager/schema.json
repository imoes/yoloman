{
  "desc": {
    "type": "string",
    "default": "OpenStack Trove DBaaS Task Manager",
    "description": "Description string for the init script"
  },
  "project_name": {
    "type": "string",
    "default": "trove",
    "description": "OpenStack project name used for paths and variable prefixes"
  },
  "service_name": {
    "type": "string",
    "default": "taskmanager",
    "description": "Specific service name appended to project name for process naming"
  },
  "system_user": {
    "type": "string",
    "default": "trove",
    "description": "System user running the daemon"
  },
  "system_group": {
    "type": "string",
    "default": "trove",
    "description": "System group running the daemon"
  }
}
