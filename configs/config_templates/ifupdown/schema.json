{
  "configure_interfaces": {
    "type": "string",
    "default": "yes",
    "description": "Set to 'no' to skip interfaces configuration on boot",
    "enum": [
      "yes",
      "no"
    ]
  },
  "exclude_interfaces": {
    "type": "list",
    "default": [],
    "description": "Don't configure these interfaces. Shell wildcards supported"
  },
  "verbose": {
    "type": "string",
    "default": "no",
    "description": "Set to 'yes' to enable additional verbosity",
    "enum": [
      "yes",
      "no"
    ]
  },
  "wait_online_method": {
    "type": "string",
    "default": "ifup",
    "description": "Method to wait for the network to become online: ifup, route, ping, ping6, none",
    "enum": [
      "ifup",
      "route",
      "ping",
      "ping6",
      "none"
    ]
  },
  "wait_online_iface": {
    "type": "string",
    "default": "",
    "description": "Which interface to wait for"
  },
  "wait_online_address": {
    "type": "string",
    "default": "",
    "description": "Which address to wait for for route, ping and ping6 methods"
  },
  "wait_online_timeout": {
    "type": "number",
    "default": 300,
    "description": "Timeout in seconds for waiting for the network to come online"
  }
}
