{
  "memory_image": {
    "type": "string",
    "default": "/var/lib/rsakeyfind/memory_dump.bin",
    "description": "Path to the memory image file containing the BER-encoded RSA private keys to be located."
  },
  "modulus_file": {
    "type": "string",
    "default": null,
    "description": "Path to a file containing the hex-encoded modulus. If provided, rsakeyfind will locate private and public keys matching this modulus. If not provided, all keys are located."
  }
}
