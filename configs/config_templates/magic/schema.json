{
  "magic_entries": {
    "type": "list",
    "default": [],
    "description": "List of magic pattern entries; each defines an offset, type, optional modifiers, comparison, value, and message"
  }
}
