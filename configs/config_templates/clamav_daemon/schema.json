{
  "debug_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debugging output. Set to false for production."
  },
  "pid_file_path": {
    "type": "string",
    "default": "/run/clamav/clamd.pid",
    "description": "Absolute path where the daemon writes its process ID."
  },
  "socket_path": {
    "type": "string",
    "default": "/run/clamav/clamd.ctl",
    "description": "Unix domain socket path for local client communication."
  },
  "socket_group": {
    "type": "string",
    "default": "clamav",
    "description": "Group ownership of the socket file (e.g., 'clamav' or 'postfix')."
  },
  "socket_mode": {
    "type": "string",
    "default": "660",
    "description": "File permissions for the socket (octal string like '660')."
  },
  "max_clients": {
    "type": "number",
    "default": 20,
    "description": "Maximum number of concurrent client connections (e.g., mail workers)."
  },
  "daemon_user": {
    "type": "string",
    "default": "clamav",
    "description": "System user account under which the daemon runs for security isolation."
  },
  "database_dir": {
    "type": "string",
    "default": "/var/lib/clamav",
    "description": "Directory containing ClamAV signature databases (main.cvd, daily.cvd, bytecode.cvd)."
  },
  "extra_signatures": {
    "type": "list",
    "default": [],
    "description": "Optional additional signature files (e.g., .cvd or .ndb paths).",
    "items": {
      "type": "string"
    }
  },
  "update_log_path": {
    "type": "string",
    "default": "/var/log/clamav/freshclam.log",
    "description": "Log file path for signature updates (used by freshclam)."
  },
  "log_file_path": {
    "type": "string",
    "default": "/var/log/clamav/clamd.log",
    "description": "Log file path for clamd operations."
  },
  "log_rotate": {
    "type": "bool",
    "default": true,
    "description": "Enable log rotation when log file size exceeds MaxSize."
  },
  "log_file_size_mb": {
    "type": "number",
    "default": 10,
    "description": "Maximum log file size (in MB) before rotation."
  },
  "scan_recursive": {
    "type": "bool",
    "default": true,
    "description": "Recursively scan subdirectories."
  },
  "scan_pe": {
    "type": "bool",
    "default": true,
    "description": "Scan Windows Portable Executable (PE) files."
  },
  "scan_ole2": {
    "type": "bool",
    "default": true,
    "description": "Scan OLE2 containers (e.g., Microsoft Office documents)."
  },
  "scan_pdf": {
    "type": "bool",
    "default": true,
    "description": "Scan PDF documents."
  },
  "scan_html": {
    "type": "bool",
    "default": true,
    "description": "Scan HTML content for malicious scripts."
  },
  "scan_mail": {
    "type": "bool",
    "default": true,
    "description": "Scan email messages and attachments."
  },
  "scan_archive": {
    "type": "bool",
    "default": true,
    "description": "Scan inside archive formats (ZIP, RAR, etc.)."
  },
  "archive_max_depth": {
    "type": "number",
    "default": 16,
    "description": "Maximum recursion depth when scanning nested archives."
  },
  "archive_max_files": {
    "type": "number",
    "default": 1000,
    "description": "Maximum number of files to scan inside a single archive."
  },
  "archive_max_file_size_mb": {
    "type": "number",
    "default": 25,
    "description": "Maximum size (in MB) of an individual file inside an archive to scan."
  },
  "idle_timeout": {
    "type": "number",
    "default": 30,
    "description": "Seconds before a client connection is closed due to inactivity."
  },
  "bytecode_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable the ClamAV bytecode interpreter for modern signatures."
  },
  "bytecode_timeout": {
    "type": "number",
    "default": 60,
    "description": "Seconds before a bytecode instruction times out (to prevent hangs)."
  },
  "stream_max_length_mb": {
    "type": "number",
    "default": 25,
    "description": "Maximum total size (in MB) of data streamed to clamd."
  },
  "stream_min_port": {
    "type": "number",
    "default": 1024,
    "description": "Minimum TCP port number for network stream scanning (if enabled)."
  },
  "stream_max_port": {
    "type": "number",
    "default": 2048,
    "description": "Maximum TCP port number for network stream scanning."
  },
  "heuristic_precedence": {
    "type": "bool",
    "default": true,
    "description": "Prefer heuristic detection over exact matches (useful for unknown threats)."
  },
  "max_scan_size_mb": {
    "type": "number",
    "default": 100,
    "description": "Maximum total size (in MB) of data scanned in one operation (e.g., full message)."
  },
  "max_file_size_mb": {
    "type": "number",
    "default": 25,
    "description": "Maximum size of a single file to scan. Larger files are skipped."
  },
  "max_recursion": {
    "type": "number",
    "default": 16,
    "description": "Maximum recursion depth when scanning nested directories."
  },
  "max_files": {
    "type": "number",
    "default": 10000,
    "description": "Maximum number of files to scan in one directory tree."
  },
  "pua_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable detection of Potentially Unwanted Applications (PUA)."
  },
  "pua_categories": {
    "type": "list",
    "default": [],
    "description": "List of PUA categories to scan for. Options: All, Archive, Compression, Email, Exploit, Lang, Packed, PDF, Shellcode.",
    "items": {
      "type": "string",
      "enum": [
        "All",
        "Archive",
        "Compression",
        "Email",
        "Exploit",
        "Lang",
        "Packed",
        "PDF",
        "Shellcode"
      ]
    }
  },
  "exclude_paths": {
    "type": "list",
    "default": [],
    "description": "List of paths (glob or regex patterns) to skip during scanning.",
    "items": {
      "type": "string"
    }
  },
  "include_paths": {
    "type": "list",
    "default": [],
    "description": "List of paths to scan exclusively (if IncludePath support is enabled).",
    "items": {
      "type": "string"
    }
  }
}
