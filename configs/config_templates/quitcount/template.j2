# === quitcount Configuration Template ===
# This template generates the configuration file for the `quitcount` service.
# Since the schema is currently empty, this template produces a minimal valid
# configuration placeholder. It is designed to be extended as schema fields
# are defined.

# Global settings placeholder.
# No fields defined in the current schema.

# Example structure for future expansion:
# [service]
# enabled = {{ quitcount_enabled | default(true) | lower }}
# log_level = {{ quitcount_log_level | default("info") }}
# bind_address = {{ quitcount_bind_address | default("127.0.0.1") }}

# If the service requires a database, it would typically be injected via
# peer injection or variables provided by the parent role.
# db_host = {{ db_host | default("127.0.0.1") }}
# db_port = {{ db_port | default(5432) }}

# End of quitcount configuration.