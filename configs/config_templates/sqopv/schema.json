{
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output for debugging. Corresponds to the --debug flag in sqopv(1)."
  },
  "keyring": {
    "type": "string",
    "default": "/etc/sqopv/keys.gpg",
    "description": "Path to the public keyring file used for verifying OpenPGP signatures."
  },
  "manifest": {
    "type": "string",
    "default": "/etc/sqopv/manifest.txt",
    "description": "Path to a file listing the files or signatures to be verified."
  },
  "output_dir": {
    "type": "string",
    "default": "/var/log/sqopv",
    "description": "Directory where verification logs or results are stored."
  }
}
