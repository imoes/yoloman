{
  "input": {
    "type": "string",
    "default": "",
    "description": "Path to file containing input subdomains"
  },
  "output": {
    "type": "string",
    "default": "",
    "description": "Path to output file for altered subdomains"
  },
  "wordlist": {
    "type": "string",
    "default": "",
    "description": "Path to file containing words to use for altering subdomains"
  },
  "resolve": {
    "type": "bool",
    "default": false,
    "description": "Resolve all altered subdomains via DNS"
  },
  "add_number_suffix": {
    "type": "bool",
    "default": false,
    "description": "Append digits 0\u20139 to every altered subdomain"
  },
  "ignore_existing": {
    "type": "bool",
    "default": false,
    "description": "Skip existing subdomains listed in input file"
  },
  "dnsserver": {
    "type": "string",
    "default": "",
    "description": "IP address of custom DNS resolver to use"
  },
  "save_file": {
    "type": "string",
    "default": "",
    "description": "Path to file to save resolved subdomains"
  },
  "threads": {
    "type": "number",
    "default": 10,
    "description": "Number of threads for concurrent DNS resolution"
  }
}
