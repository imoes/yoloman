{
  "debug_output": {
    "type": "bool",
    "default": false,
    "description": "Enable debug output for bugfixing and diagnostics."
  },
  "dump_small_memory": {
    "type": "bool",
    "default": false,
    "description": "Dump all values from the internal small memory."
  },
  "dump_big_memory": {
    "type": "bool",
    "default": false,
    "description": "Dump all values from the internal big memory (new hardware)."
  },
  "get_status": {
    "type": "bool",
    "default": false,
    "description": "Get status from the weather station."
  },
  "invalid_value_output": {
    "type": "string",
    "default": "i",
    "description": "String used to represent invalid or unreachable sensor values in the colon-separated output."
  }
}
