{
  "file_paths": {
    "type": "list",
    "default": [],
    "description": "List of absolute or relative paths to files or symlinks to be chased by chase(1). Each entry is a single file path string. Empty list means no default targets \u2014 must be provided at runtime or via external mechanism (e.g., CLI args or stdin)."
  }
}
