{
  "pathspecs": {
    "type": "list",
    "default": [],
    "description": "List of paths (directories or files) to be included in the ISO9660 filesystem. Each entry may include graft points when -graft-points is used (e.g., 'dest=src'). Required per genisoimage specification. See pathspec and -graft-points in genisoimage(1)."
  }
}
