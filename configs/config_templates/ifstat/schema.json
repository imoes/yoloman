{
  "ifstat_delay": {
    "type": "number",
    "default": 1,
    "description": "Delay between updates in seconds. Supports decimals (min 0.1)."
  },
  "ifstat_subsequent_delay": {
    "type": "string",
    "default": "",
    "description": "Second delay for subsequent polls, separated by '/'. If empty, uses first delay only."
  },
  "ifstat_count": {
    "type": "number",
    "default": 0,
    "description": "Number of updates before stopping. 0 or empty means unlimited."
  },
  "ifstat_interfaces": {
    "type": "list",
    "default": [],
    "description": "List of interface names to monitor. Comma-separated in CLI."
  },
  "ifstat_monitor_loopback": {
    "type": "bool",
    "default": false,
    "description": "Enable monitoring of loopback interfaces (-l)."
  },
  "ifstat_monitor_all": {
    "type": "bool",
    "default": false,
    "description": "Enable monitoring of all interfaces found, including inactive ones (-a)."
  },
  "ifstat_hide_null": {
    "type": "bool",
    "default": false,
    "description": "Hide interfaces with null counters (-z)."
  },
  "ifstat_no_header": {
    "type": "bool",
    "default": false,
    "description": "Turn off displaying the header periodically (-n)."
  },
  "ifstat_timestamp": {
    "type": "bool",
    "default": false,
    "description": "Add timestamp at beginning of each line (-t)."
  },
  "ifstat_total_bandwidth": {
    "type": "bool",
    "default": false,
    "description": "Report total bandwidth for all monitored interfaces (-T)."
  },
  "ifstat_no_index": {
    "type": "bool",
    "default": false,
    "description": "Disable use of interface indexes (-A)."
  },
  "ifstat_fixed_width": {
    "type": "bool",
    "default": false,
    "description": "Use fixed width columns (-w)."
  },
  "ifstat_wrap_lines": {
    "type": "bool",
    "default": false,
    "description": "Wrap lines larger than terminal width (-W)."
  },
  "ifstat_no_scroll": {
    "type": "bool",
    "default": false,
    "description": "Keep stats on the same line if possible (-S)."
  },
  "ifstat_kbits": {
    "type": "bool",
    "default": false,
    "description": "Report bandwidth in kbits/sec instead of kbytes/sec (-b)."
  },
  "ifstat_quiet": {
    "type": "bool",
    "default": false,
    "description": "Quiet mode, no warnings (-q)."
  },
  "ifstat_driver": {
    "type": "string",
    "default": "proc",
    "description": "Driver to use for gathering stats (e.g., proc, snmp, ifmib).",
    "enum": [
      "proc",
      "ifmib",
      "kstat",
      "ifdata",
      "route",
      "kvm",
      "dlpi",
      "win32",
      "snmp"
    ]
  },
  "ifstat_driver_opts": {
    "type": "string",
    "default": "",
    "description": "Options for the driver (e.g., alternate proc file or SNMP comm@host/nn)."
  },
  "ifstat_snmp_target": {
    "type": "string",
    "default": "",
    "description": "SNMP target configuration [comm@][#]host[/nn]. Overrides driver_opts if driver is snmp."
  }
}
