{
  "user": {
    "type": "string",
    "default": "nginx",
    "description": "System user under which Nginx processes run."
  },
  "worker_processes": {
    "type": "string",
    "default": "auto",
    "description": "Number of worker processes. 'auto' uses the number of CPU cores."
  },
  "error_log": {
    "type": "string",
    "default": "/var/log/nginx/error.log",
    "description": "Path to the error log file."
  },
  "pid": {
    "type": "string",
    "default": "/run/nginx.pid",
    "description": "Path to the PID file."
  },
  "worker_connections": {
    "type": "number",
    "default": "1024",
    "description": "Maximum number of simultaneous connections that can be opened by a worker process."
  },
  "access_log": {
    "type": "string",
    "default": "/var/log/nginx/access.log",
    "description": "Path to the access log file."
  },
  "keepalive_timeout": {
    "type": "number",
    "default": "65",
    "description": "Gives a timeout for keep-alive connections with the client."
  },
  "types_hash_max_size": {
    "type": "number",
    "default": "4096",
    "description": "Maximum size of the types hash tables."
  },
  "listen_port": {
    "type": "number",
    "default": "80",
    "description": "Port for the default server block to listen on."
  },
  "server_name": {
    "type": "string",
    "default": "_",
    "description": "Server name for the default server block. '_' acts as a wildcard/default catch-all."
  },
  "document_root": {
    "type": "string",
    "default": "/usr/share/nginx/html",
    "description": "Document root for the default server block."
  }
}
