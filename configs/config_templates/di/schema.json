{
  "DI_ARGS": {
    "type": "string",
    "default": "-dg -f sbuvpT",
    "description": "Default command-line arguments for di(1), as a single string; overrides DI_ARGS environment variable if specified"
  }
}
