# di configuration (disk information)
# Set default arguments for di(1) via DI_ARGS environment variable.
# Example: DI_ARGS="-dg -f sbuvpT"
# This variable overrides the DI_ARGS environment variable if specified in the shell.
DI_ARGS={{ DI_ARGS | default("-dg -f sbuvpT") }}