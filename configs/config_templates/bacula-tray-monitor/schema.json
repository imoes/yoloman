{
  "debug_level": {
    "type": "number",
    "default": 20,
    "description": "Debug level (0-99); higher values produce more verbose debug output."
  },
  "director_config": {
    "type": "string",
    "default": "/etc/bacula/bacula-dir.conf",
    "description": "Absolute path to the Bacula Director configuration file."
  }
}
