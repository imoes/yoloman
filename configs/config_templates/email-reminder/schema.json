{
  "collect_script": {
    "type": "string",
    "default": "/usr/sbin/collect-reminders",
    "description": "Path to the script that collects email reminders"
  },
  "send_script": {
    "type": "string",
    "default": "/usr/bin/send-reminders",
    "description": "Path to the script that sends email reminders"
  },
  "spool_dir": {
    "type": "string",
    "default": "/var/spool/email-reminder/",
    "description": "Directory containing email reminder spool files"
  },
  "user": {
    "type": "string",
    "default": "email-reminder",
    "description": "System user under which the send script runs"
  }
}
