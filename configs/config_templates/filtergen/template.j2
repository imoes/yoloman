# /etc/filtergen/fgadm.conf

# Packet filter backend to use; one of 'iptables', 'ipchains', 'ipfilter', or 'cisco'.
BACKEND={{ BACKEND | default('iptables') }}

# Additional options to pass to filtergen when run from the initscript.
FGOPTS="{{ FGOPTS | default('') }}"