{
  "BACKEND": {
    "type": "string",
    "default": "iptables",
    "description": "Packet filter backend to use; one of 'iptables', 'ipchains', 'ipfilter', or 'cisco'.",
    "enum": [
      "iptables",
      "ipchains",
      "ipfilter",
      "cisco",
      "ip6tables",
      "iptables-restore",
      "ip6tables-restore"
    ]
  },
  "FGOPTS": {
    "type": "string",
    "default": "",
    "description": "Additional options to pass to filtergen when run from the initscript."
  }
}
