{
  "home_dir": {
    "type": "string",
    "default": "/home/user",
    "description": "The home directory of the user running Tkabber"
  },
  "plugins": {
    "type": "list",
    "default": [],
    "description": "List of plugin names to install from /usr/share/tkabber-plugins. Available plugins include: aniemoticons, attline, battleship, bc, browser, checkers, chess, ctcomp, custom-urls, cyrillize, debug, ejabberd, floatingcontact, floatinglog, georoster, gmail, iconsets, jidlink, latex, mute, openurl, osd, otr, poker, presencecmd, quotelastmsg, quiz, receipts, renju, reversi, singularity, socials, spy, stripes, tclchat, tkabber-khim, traffic, unixkeys, whiteboard."
  }
}
