{
  "l4_clevel": {
    "type": "number",
    "default": 1,
    "description": "Default compression level. Values 1-12. Higher values trade speed for ratio. Recommended 1 for fast, 9 for high."
  },
  "l4_nbwokers": {
    "type": "number",
    "default": 0,
    "description": "Default number of threads for compression. 0 means auto-detect from CPU cores. Max 200."
  }
}
