{
  "avs_network_block": {
    "type": "string",
    "default": "192.168.1.100",
    "description": "IP network block for Authorized Vulnerability Scanning (AVS) traffic to exclude from inspection."
  },
  "exclusion_uri": {
    "type": "string",
    "default": "/index.php",
    "description": "URI prefix to exclude specific parameters from inspection in examples."
  },
  "exclusion_filename": {
    "type": "string",
    "default": "/wp-login.php",
    "description": "Filename suffix to exclude specific parameters from inspection in examples."
  },
  "admin_uri_prefix": {
    "type": "string",
    "default": "/admin",
    "description": "URI prefix for admin area to exclude specific rule ranges in examples."
  }
}
