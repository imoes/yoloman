{
  "rules": {
    "type": "list",
    "default": [],
    "description": "List of filter rules (one per line, as in .filter/filter-rules). Each rule must conform to the documented syntax: `if (expression) then action` or `expression then action`."
  }
}
