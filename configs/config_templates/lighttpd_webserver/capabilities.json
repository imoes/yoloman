{
  "provides": [
    {
      "capability": "web_server",
      "backend": "lighttpd",
      "port_field": "port",
      "default_port": 80,
      "provisionable": {
        "vhosts": "vhosts",
        "static_files_root": "document_root"
      }
    }
  ],
  "requires": [
    {
      "capability": "app_runtime",
      "backends": [
        "php-fpm"
      ],
      "backend_field": "fastcgi_enabled",
      "fields": {
        "socket": "fastcgi_socket",
        "host": "fastcgi_bin_path"
      }
    }
  ],
  "peer_injection": [],
  "confidence": "high"
}
