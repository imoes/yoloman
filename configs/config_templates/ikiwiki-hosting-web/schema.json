{
  "maxload": {
    "type": "number",
    "default": 5,
    "description": "Maximum load average for parallel aggregation jobs."
  },
  "maxjobs": {
    "type": "number",
    "default": 5,
    "description": "Maximum number of parallel aggregation jobs."
  },
  "nice_command": {
    "type": "string",
    "default": "nice -n 19 ionice -c 3",
    "description": "Nice and ionice command string to lower process priority."
  }
}
