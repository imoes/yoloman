{
  "log_level": {
    "type": "string",
    "default": "NOTICE",
    "description": "Verbosity of CTDB logs. 'NOTICE' is the shipped default; use 'DEBUG' only for troubleshooting.",
    "enum": [
      "ERROR",
      "WARNING",
      "NOTICE",
      "INFO",
      "DEBUG"
    ]
  }
}
