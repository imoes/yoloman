{
  "SLON_TOOLS_START_NODES": {
    "type": "string",
    "default": "",
    "description": "Space-separated list of node numbers or names configured in slon_tools.conf that should be handled in the init script."
  }
}
