{
  "mac_vendor_qemu": {
    "type": "string",
    "default": "52:54:00\tQEMU",
    "description": "QEMU virtual Ethernet MAC prefix vendor mapping"
  },
  "mac_vendor_bochs": {
    "type": "string",
    "default": "b0:c4:20\tBochs",
    "description": "Bochs virtual Ethernet MAC prefix vendor mapping"
  },
  "mac_vendor_vrrp": {
    "type": "string",
    "default": "00:00:5e:00:01\tVRRP (last octet is VRID)",
    "description": "VRRP MAC prefix vendor mapping (RFC 2338)"
  },
  "mac_vendor_wlbs": {
    "type": "string",
    "default": "02:bf\tMicrosoft WLBS (last four octets are IP address)",
    "description": "Microsoft WLBS MAC prefix vendor mapping"
  },
  "mac_vendor_hsrp": {
    "type": "string",
    "default": "00:00:0c:07:ac\tHSRP (last octet is group number)",
    "description": "Cisco HSRP MAC prefix vendor mapping"
  },
  "mac_vendor_broadcast": {
    "type": "string",
    "default": "ff:ff:ff:ff:ff:ff\tBroadcast",
    "description": "Ethernet broadcast address vendor mapping"
  }
}
