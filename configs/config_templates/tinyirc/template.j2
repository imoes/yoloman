# ============================================================================
# tinyirc - Configuration Template
# ============================================================================
# This file configures the 'tinyirc' IRC bot/client.
# Each setting is documented below with its purpose and default value.
# ============================================================================

# -----------------------------------------------------------------------------
# IRC Server Connection
# -----------------------------------------------------------------------------
# The IRC server host and port to connect to.
# Format: HOST[:PORT]
# Default: 'irc.freenode.org:6667'
server: {{ server | default('irc.freenode.org:6667') }}

# -----------------------------------------------------------------------------
# Bot Nickname
# -----------------------------------------------------------------------------
# The default nickname to use on the IRC server.
# Default: 'bot'
nickname: {{ nickname | default('bot') }}

# -----------------------------------------------------------------------------
# Dumb Mode (Terminal Interaction)
# -----------------------------------------------------------------------------
# If true, disables the curses interface and writes commands to stdout/stdin.
# Default: false
dumb_mode: {{ dumb_mode | default(false) | lower }}