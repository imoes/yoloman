{
  "server": {
    "type": "string",
    "default": "irc.freenode.org:6667",
    "description": "The IRC server host and port to connect to. Format is HOST[:PORT]."
  },
  "nickname": {
    "type": "string",
    "default": "bot",
    "description": "The default nickname to use on the IRC server."
  },
  "dumb_mode": {
    "type": "bool",
    "default": false,
    "description": "If true, disables the curses interface and writes commands to stdout/stdin."
  }
}
