{
  "SYSTEM_USER": {
    "type": "string",
    "default": "neutron",
    "description": "System user running the Neutron RPC server daemon."
  },
  "SYSTEM_GROUP": {
    "type": "string",
    "default": "neutron",
    "description": "System group running the Neutron RPC server daemon."
  }
}
