{
  "PROVIDES": {
    "type": "string",
    "default": "masakari-engine",
    "description": "Provides: line in init info"
  },
  "SHOULD_START": {
    "type": "string",
    "default": "postgresql mysql keystone rabbitmq-server ntp",
    "description": "Should-Start: line in init info"
  },
  "SHOULD_STOP": {
    "type": "string",
    "default": "postgresql mysql keystone rabbitmq-server ntp",
    "description": "Should-Stop: line in init info"
  },
  "SHORT_DESCRIPTION": {
    "type": "string",
    "default": "Masakari Engine daemon",
    "description": "Short-Description: line in init info"
  },
  "DESCRIPTION": {
    "type": "string",
    "default": "Masakari provides Virtual Machine High Availability (VMHA) service for\n                   OpenStack clouds by automatically recovering the KVM-based Virtual\n                   Machine(VM)s from failure events such as VM process down, provisioning\n                   process down, and nova-compute host failure. It also provides API service for\n                   manage and control the automated rescue mechanism.",
    "description": "Description: line in init info"
  },
  "AUTHOR": {
    "type": "string",
    "default": "Thomas Goirand <zigo@debian.org>",
    "description": "Author comment"
  },
  "DESC": {
    "type": "string",
    "default": "OpenStack Masakari Engine (masakari-engine)",
    "description": "DESC variable"
  },
  "PROJECT_NAME": {
    "type": "string",
    "default": "masakari",
    "description": "Project name variable"
  },
  "AUTHOR_2": {
    "type": "string",
    "default": "Ond\u0159ej Nov\u00fd <novy@ondrej.org>",
    "description": "Second author comment"
  }
}
