{
  "reader": {
    "type": "string",
    "default": "",
    "description": "Name or path of the smart card reader to connect to (e.g., 'ACS CCID Reader 0')"
  },
  "commands": {
    "type": "list",
    "default": [],
    "description": "List of Lua commands to execute on startup (equivalent to -e/--exec options)"
  },
  "show_version": {
    "type": "bool",
    "default": false,
    "description": "If true, print version info and list of detected readers instead of launching the GUI"
  }
}
