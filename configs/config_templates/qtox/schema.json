{
  "qtox_prefix": {
    "type": "string",
    "default": "/usr",
    "description": "The installation prefix for qTox, typically /usr or /usr/local."
  },
  "qtox_additional_rw_dirs": {
    "type": "string",
    "default": "/home",
    "description": "Additional directories where qTox should have read/write access for user data."
  },
  "multiarch": {
    "type": "string",
    "default": "lib/x86_64-linux-gnu",
    "description": "The multiarch triplet directory for shared libraries, e.g., lib/x86_64-linux-gnu."
  }
}
