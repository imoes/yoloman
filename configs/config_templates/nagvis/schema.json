{
  "admins_group": {
    "type": "string",
    "default": "admins",
    "description": "The contact group name that receives full admin access to NagVis."
  },
  "first_level_support_group": {
    "type": "string",
    "default": "first_level_support",
    "description": "The contact group name that receives view-only access to all maps."
  },
  "power_users_group": {
    "type": "string",
    "default": "power_users",
    "description": "The contact group name that receives view and edit access to all maps."
  },
  "oracle_admins_group": {
    "type": "string",
    "default": "oracle_admins",
    "description": "The contact group name that receives view and edit access to a specific map."
  },
  "oracle_map_name": {
    "type": "string",
    "default": "oracle_servers",
    "description": "The specific map name accessible by the oracle_admins group."
  }
}
