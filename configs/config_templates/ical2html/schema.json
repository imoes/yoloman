{
 "duration": {
  "default": "P1M",
  "description": "The duration of the calendar view, in ISO 8601 duration format (e.g., P5W, P60D).",
  "type": "string"
 },
 "exclude_categories": {
  "default": [],
  "description": "List of category names to exclude from the output.",
  "type": "list"
 },
 "exclude_class": {
  "description": "Exclude events with this class (PUBLIC, CONFIDENTIAL, PRIVATE, NONE).",
  "enum": [
   "PUBLIC",
   "CONFIDENTIAL",
   "PRIVATE",
   "NONE"
  ],
  "type": "string"
 },
 "footer_text": {
  "default": "",
  "description": "Text to add at the bottom of the HTML file.",
  "type": "string"
 },
 "html_title": {
  "default": "Calendar",
  "description": "Text to use for the HTML page title.",
  "type": "string"
 },
 "include_categories": {
  "default": [],
  "description": "List of category names to include in the output.",
  "type": "list"
 },
 "include_class": {
  "description": "Filter to only events with this class (PUBLIC, CONFIDENTIAL, PRIVATE, NONE).",
  "enum": [
   "PUBLIC",
   "CONFIDENTIAL",
   "PRIVATE",
   "NONE"
  ],
  "type": "string"
 },
 "include_description": {
  "default": false,
  "description": "Include the event's long description in a <PRE> block.",
  "type": "bool"
 },
 "include_location": {
  "default": false,
  "description": "Include the event's location in a <PRE> block.",
  "type": "bool"
 },
 "input_file": {
  "default": "/etc/ical2html/calendar.ics",
  "description": "Path to the input iCalendar file. Defaults to standard input if empty.",
  "type": "string"
 },
 "mark_today": {
  "default": false,
  "description": "Mark the current day with #today in the HTML file.",
  "type": "bool"
 },
 "monday_first": {
  "default": false,
  "description": "Draw Monday as the first day of the week (Sunday is default).",
  "type": "bool"
 },
 "start_date": {
  "default": "20240101",
  "description": "The start date for the calendar view in yyyymmdd format.",
  "type": "string"
 },
 "timezone": {
  "default": "GMT",
  "description": "Adjust for this timezone, specified as country/city.",
  "type": "string"
 }
}
