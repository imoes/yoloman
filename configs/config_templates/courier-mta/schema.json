{
  "init_provides": {
    "type": "string",
    "default": "courier-mta",
    "description": "Service name provided by this init script"
  },
  "init_required_start": {
    "type": "string",
    "default": "courier-authdaemon courier courierfilter $remote_fs $syslog",
    "description": "Dependencies required to start before this service"
  },
  "init_required_stop": {
    "type": "string",
    "default": "$remote_fs $syslog",
    "description": "Dependencies required to stop after this service"
  },
  "init_default_start": {
    "type": "string",
    "default": "2 3 4 5",
    "description": "Runlevels in which the service should start"
  },
  "init_default_stop": {
    "type": "string",
    "default": "0 1 6",
    "description": "Runlevels in which the service should stop"
  },
  "init_short_description": {
    "type": "string",
    "default": "Courier MTA server",
    "description": "Short description of the service"
  },
  "init_author": {
    "type": "string",
    "default": "Ond\u0159ej Sur\u00fd <ondrej@debian.org>",
    "description": "Author/maintainer of the init script"
  },
  "daemon_path": {
    "type": "string",
    "default": "/usr/sbin/esmtpd",
    "description": "Path to the Courier MTA daemon binary"
  },
  "desc": {
    "type": "string",
    "default": "Courier MTA server",
    "description": "Human-readable description of the daemon"
  }
}
