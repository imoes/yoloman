{
  "output_mode": {
    "type": "string",
    "default": "modified",
    "description": "The comparison mode: 'modified' for changed files, 'list' for all files, or 'none' for standard diff.",
    "enum": [
      "modified",
      "list"
    ]
  },
  "autoskip_autotools": {
    "type": "bool",
    "default": true,
    "description": "Whether to skip files belonging to GNU autotools when in modified mode."
  },
  "run_stats": {
    "type": "bool",
    "default": false,
    "description": "Whether to run diffstat statistics on all modified files."
  },
  "ignore_patterns": {
    "type": "list",
    "default": [
      ".gitignore",
      ".git"
    ],
    "description": "List of file or directory patterns to ignore during comparison."
  }
}
