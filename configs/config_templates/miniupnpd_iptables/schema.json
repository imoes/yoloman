{
  "external_iface": {
    "type": "string",
    "default": "eth0",
    "description": "Network interface used for external (WAN) traffic (e.g., PPPoE, cable, DSL)."
  },
  "internal_ifaces": {
    "type": "list",
    "default": [
      {
        "name": "eth1",
        "cidr": "192.168.1.0/24"
      }
    ],
    "items": {
      "type": "object",
      "fields": {
        "name": {
          "type": "string",
          "default": "eth1",
          "description": "Internal (LAN) interface name (e.g., br0, eth1)."
        },
        "cidr": {
          "type": "string",
          "default": "192.168.1.0/24",
          "description": "IPv4 network address and CIDR prefix (e.g., 192.168.1.0/24). Optional but recommended."
        }
      }
    },
    "description": "List of internal (LAN) interfaces with optional network CIDR. Supports multiple LANs."
  },
  "enable_upnp": {
    "type": "bool",
    "default": true,
    "description": "Enable UPnP Internet Gateway Device (IGD) service."
  },
  "enable_pcp": {
    "type": "bool",
    "default": true,
    "description": "Enable Port Control Protocol (PCP) for advanced NAT traversal."
  },
  "enable_natpmp": {
    "type": "bool",
    "default": false,
    "description": "Enable NAT-PMP (legacy NAT mapping protocol). Disable unless required by legacy clients."
  },
  "ifname": {
    "type": "string",
    "default": "eth0",
    "description": "Exact name of the external interface (must match system interface name). Required for iptables rules."
  },
  "secure_mode": {
    "type": "bool",
    "default": true,
    "description": "Only allow port forwarding rules from internal IPs. Prevents rogue external port mapping attempts."
  },
  "system_update": {
    "type": "bool",
    "default": true,
    "description": "Send UPnP SSDP system-update notifications to notify clients of reboots or config changes."
  },
  "presentation_url": {
    "type": "string",
    "default": "http://192.168.1.1/",
    "description": "Web UI URL advertised via UPnP for gateway management. Set to '' to disable."
  },
  "friendly_name": {
    "type": "string",
    "default": "My Router",
    "description": "Human-readable name shown in UPnP control points (e.g., 'D-Link Router')."
  },
  "uuid": {
    "type": "string",
    "default": "",
    "description": "Unique device identifier (UPnP UUID format: uuid:12345678-1234-1234-1234-123456789abc). Leave empty for auto-generate."
  },
  "serial": {
    "type": "string",
    "default": "12345678",
    "description": "Device serial number for UPnP identification (e.g., hardware serial or MAC-based)."
  },
  "model_name": {
    "type": "string",
    "default": "MiniUPnPd",
    "description": "Model name advertised via UPnP (e.g., 'Linux Router')."
  },
  "model_number": {
    "type": "string",
    "default": "1.0",
    "description": "Model version or revision (e.g., '1.0.0')."
  },
  "lease_file": {
    "type": "string",
    "default": "/var/lib/miniupnpd/leases",
    "description": "Path to persistent lease file for dynamic port mappings. Set to '' to disable persistence."
  },
  "upnp_port_min": {
    "type": "number",
    "default": 0,
    "description": "Minimum external port allowed in port mappings (0 = no limit)."
  },
  "upnp_port_max": {
    "type": "number",
    "default": 0,
    "description": "Maximum external port allowed in port mappings (0 = no limit)."
  },
  "logfile": {
    "type": "string",
    "default": "/var/log/miniupnpd.log",
    "description": "Path to log file (empty = no file logging)."
  },
  "log_verbosity": {
    "type": "number",
    "default": 1,
    "description": "Log verbosity: 1=errors, 2=warnings, 3=info, 4=debug."
  },
  "log_syslog": {
    "type": "bool",
    "default": true,
    "description": "Log to syslog in addition to file."
  },
  "natpmp_port_min": {
    "type": "number",
    "default": 0,
    "description": "Minimum external port for NAT-PMP mappings (if enabled)."
  },
  "natpmp_port_max": {
    "type": "number",
    "default": 0,
    "description": "Maximum external port for NAT-PMP mappings (if enabled)."
  },
  "allow_ports": {
    "type": "list",
    "default": [],
    "description": "Whitelist of allowed port mappings. Each entry: 'port:protocol[:ip[:internal_port]]'. Wildcards (*), e.g., '*:tcp'."
  },
  "deny_ports": {
    "type": "list",
    "default": [],
    "description": "Blacklist of port mappings. Higher precedence than allow list. Same format as allow_ports."
  },
  "incremental_lease_update": {
    "type": "bool",
    "default": true,
    "description": "Write lease file incrementally on changes instead of full rewrite (safer for flash storage)."
  },
  "enable_ipv6": {
    "type": "bool",
    "default": false,
    "description": "Enable IPv6 support (requires IPv6-enabled kernel and compiled miniupnpd)."
  },
  "ssdp_broadcast_interval": {
    "type": "number",
    "default": 30,
    "description": "Interval (seconds) between SSDP multicast broadcasts."
  },
  "ssdp_alive_time": {
    "type": "number",
    "default": 1800,
    "description": "Time (seconds) before SSDP entries expire and require re-broadcast."
  },
  "ssdp_ttl": {
    "type": "number",
    "default": 2,
    "description": "Time-to-live (TTL) for SSDP multicast packets."
  },
  "igd_device_type": {
    "type": "string",
    "default": "urn:schemas-upnp-org:device:InternetGatewayDevice:1",
    "description": "UPnP device type string (IGDv1 or IGDv2). Change only for compatibility."
  },
  "enable_content_filtering": {
    "type": "bool",
    "default": false,
    "description": "Enable UPnP content filtering (not implemented for iptables backend; kept for config compatibility)."
  },
  "start_script": {
    "type": "string",
    "default": "",
    "description": "Optional shell script executed after service starts (e.g., firewall warm-up)."
  },
  "stop_script": {
    "type": "string",
    "default": "",
    "description": "Optional shell script executed before service stops (e.g., cleanup rules)."
  }
}
