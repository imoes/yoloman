{
  "screen_geometry": {
    "type": "string",
    "default": "24x80.132",
    "description": "Screen geometry in form lines x minimum_columns . maximum_columns (e.g., 24x80.132)"
  },
  "replay_commands": {
    "type": "string",
    "default": "",
    "description": "Path to commands file for replaying recorded tests (-c)"
  },
  "font_file": {
    "type": "string",
    "default": "",
    "description": "Path to font file containing DRCS strings (-f)"
  },
  "log_results": {
    "type": "bool",
    "default": false,
    "description": "Enable logging of test results to vttest.log (-l)"
  },
  "use_padding": {
    "type": "bool",
    "default": false,
    "description": "Enable padding for high-speed lines (-p)"
  },
  "show_recent_only": {
    "type": "bool",
    "default": false,
    "description": "Show only the most recent part of continuous responses (-q)"
  },
  "add_delay": {
    "type": "bool",
    "default": false,
    "description": "Add time-delays in panning/scrolling options (-s)"
  },
  "suppress_utf8_switch": {
    "type": "bool",
    "default": false,
    "description": "Suppress switch from UTF-8 mode on startup (-u)"
  },
  "use_8bit_controls": {
    "type": "bool",
    "default": false,
    "description": "Use 8-bit controls instead of 7-bit (-8)"
  }
}
