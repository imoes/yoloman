{
  "mpiexec": {
    "type": "string",
    "default": "no",
    "description": "Path to mpiexec executable or 'no' to disable"
  },
  "mpirun": {
    "type": "string",
    "default": "no",
    "description": "Path to mpirun executable or 'no' to disable"
  },
  "lsf_profile": {
    "type": "string",
    "default": "/etc/profile.lsf",
    "description": "Path to LSF profile file (profile.lsf) defining the shell environment"
  },
  "bsub": {
    "type": "string",
    "default": "/usr/bin/bsub",
    "description": "Path to bsub command for job submission"
  },
  "bqueues": {
    "type": "string",
    "default": "/usr/bin/bqueues",
    "description": "Path to bqueues command for listing queues"
  },
  "bjobs": {
    "type": "string",
    "default": "/usr/bin/bjobs",
    "description": "Path to bjobs command for job status polling"
  },
  "bhist": {
    "type": "string",
    "default": "/usr/bin/bhist",
    "description": "Path to bhist command for job history"
  },
  "bacct": {
    "type": "string",
    "default": "/usr/bin/bacct",
    "description": "Path to bacct command for accounting"
  },
  "bkill": {
    "type": "string",
    "default": "/usr/bin/bkill",
    "description": "Path to bkill command for killing jobs"
  },
  "log_path": {
    "type": "string",
    "default": "no",
    "description": "Path to LSF event log directory or 'no' to disable"
  }
}
