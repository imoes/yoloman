{
  "update_package_lists": {
    "type": "string",
    "default": "1",
    "description": "Frequency (in days) to run apt-get update. 0 disables it."
  },
  "download_upgradeable_packages": {
    "type": "string",
    "default": "1",
    "description": "Frequency (in days) to download upgradeable packages. 0 disables it."
  },
  "autoclean_interval": {
    "type": "string",
    "default": "0",
    "description": "Frequency (in days) to run apt-get autoclean. 0 disables it."
  }
}
