{
  "mgmt_if": {
    "type": "string",
    "default": "tempestbr",
    "description": "Management interface name for the bridge and iptables rules."
  },
  "mgmt_net_cidr": {
    "type": "string",
    "default": "192.168.100.1/24",
    "description": "Management network CIDR used for NAT and forwarding rules."
  },
  "otc_pxe_server_ip": {
    "type": "string",
    "default": "192.168.100.1",
    "description": "IP address assigned to the management bridge interface."
  },
  "otc_pxe_netmask": {
    "type": "string",
    "default": "255.255.255.0",
    "description": "Netmask for the management network interface."
  },
  "pxe_nic_name": {
    "type": "string",
    "default": "tempestnic0",
    "description": "Name of the dummy network interface used for PXE bridging."
  },
  "pxe_vm_virtap_name": {
    "type": "string",
    "default": "tempesttap",
    "description": "Name of the TAP device used for VM network connectivity."
  },
  "pxe_vm_nic_user": {
    "type": "string",
    "default": "root",
    "description": "User allowed to open the TAP device for VM NICs."
  }
}
