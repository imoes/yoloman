{
  "worker_connections": {
    "type": "number",
    "default": 1024,
    "description": "Maximum number of simultaneous connections that can be opened by a worker process."
  },
  "keepalive_timeout": {
    "type": "number",
    "default": 65,
    "description": "Timeout (in seconds) for keep-alive connections between client and server."
  },
  "gzip_comp_level": {
    "type": "number",
    "default": 4,
    "description": "Gzip compression level (1\u20139); higher = more CPU, smaller output."
  },
  "gzip_types": {
    "type": "list",
    "default": [
      "text/plain",
      "text/css",
      "application/json",
      "application/javascript",
      "text/xml",
      "application/xml",
      "application/xml+rss",
      "text/javascript"
    ],
    "description": "MIME types to compress with Gzip; must be compatible with client Accept-Encoding.",
    "items": {
      "type": "string",
      "description": "A MIME type to include in Gzip compression."
    }
  }
}
