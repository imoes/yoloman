{
  "heed_default_display_manager": {
    "type": "string",
    "default": "true",
    "description": "Whether to heed the default display manager setting. Set to 'false' to start x2gothinclient even if it is not the default display manager.",
    "enum": [
      "true",
      "false"
    ]
  }
}
