# Click configuration file for preinstalled Ubuntu Core applications.
# This package manages the local database of snap applications,
# not a network-facing database service.

[Click Database]
# The root directory for preinstalled Ubuntu core applications.
# Default: /usr/share/click/preinstalled
root = {{ click_database_root | default('/usr/share/click/preinstalled') }}