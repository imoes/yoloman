{
  "monochrome": {
    "type": "bool",
    "default": false,
    "description": "Enable monochrome mode, disabling ANSI color escapes."
  },
  "cols": {
    "type": "number",
    "default": 80,
    "description": "Width of the screen in columns."
  },
  "rows": {
    "type": "number",
    "default": 24,
    "description": "Height of the screen in rows."
  },
  "interval": {
    "type": "number",
    "default": 4,
    "description": "Refresh interval in seconds between updates (minimum 1)."
  }
}
