{
  "command": {
    "type": "list",
    "default": [
      "/bin/bash",
      "-c",
      "echo hello"
    ],
    "description": "The command and arguments to launch in the new PID namespace."
  },
  "flags": {
    "type": "list",
    "default": [],
    "description": "List of newpid flags to use. Supported: '-i' (IPC), '-n' (Network), '-u' (UTS). Empty list results in just PID namespace (default behavior)."
  }
}
