{
  "name": {
    "type": "string",
    "default": "opensysusers",
    "description": "The name of the service script variable NAME"
  },
  "desc": {
    "type": "string",
    "default": "standalone sysusers service",
    "description": "The description of the service"
  },
  "daemon": {
    "type": "string",
    "default": "/usr/bin/opensysusers-sysusers",
    "description": "Path to the opensysusers daemon executable"
  },
  "run_dir": {
    "type": "string",
    "default": "/run/opensysusers",
    "description": "Directory used for runtime status files"
  }
}
