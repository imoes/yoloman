{
  "FLAGS": {
    "type": "string",
    "default": "defaults 50",
    "description": "Update-rc.d flags for service registration"
  },
  "PID_FILE": {
    "type": "string",
    "default": "/var/run/snmptrapfmt.pid",
    "description": "Path to the PID file used by the daemon"
  }
}
