{
  "POLICYKIT_AUTH_AGENT_NAME": {
    "type": "string",
    "default": "PolicyKit",
    "description": "The name of the PolicyKit system displayed in the French localization strings."
  },
  "POLICYKIT_AUTH_AGENT_EXEC": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu/ukui-polkit/polkit-ukui-authentication-agent-1",
    "description": "The absolute path to the PolicyKit Authentication Agent executable binary."
  }
}
