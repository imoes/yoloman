{
  "quickchar_exec": {
    "type": "string",
    "default": "quickchar",
    "description": "Command to execute the QuickChar helper process"
  },
  "gsettings_schema": {
    "type": "string",
    "default": "org.ubuntubudgie.quickchar",
    "description": "GSettings schema path for the service configuration"
  },
  "gsettings_key": {
    "type": "string",
    "default": "enable-quickchar",
    "description": "GSettings key to determine autostart condition"
  }
}
