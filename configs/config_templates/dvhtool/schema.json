{
  "device": {
    "type": "string",
    "default": "/dev/sda",
    "description": "Path to the disk device (e.g., /dev/sda) or file image containing a disk volume header."
  },
  "operations": {
    "type": "list",
    "default": [],
    "description": "List of dvhtool actions to perform. Only one action will be executed per invocation (the last one specified). Multiple operations can be defined but only the final one will be applied in a single dvhtool run. See man dvhtool(8) for semantics."
  }
}
