{
  "proxy_enabled": {"type": "string", "default": "yes", "description": "Whether the proxy settings are active (yes/no).", "enum": ["yes", "no"]},
  "http_proxy": {"type": "string", "default": "", "description": "HTTP proxy URL (e.g. http://proxy.example.internal:80)."},
  "https_proxy": {"type": "string", "default": "", "description": "HTTPS proxy URL."},
  "no_proxy": {"type": "string", "default": "localhost, 127.0.0.1", "description": "Comma-separated hosts/domains that bypass the proxy."}
}
