{
  "sieve_after": {
    "default": "",
    "description": "Path to a global Sieve script that runs *after* user scripts (e.g., for archiving or notifications)",
    "type": "string"
  },
  "sieve_before": {
    "default": "",
    "description": "Path to a global Sieve script that runs *before* user scripts (e.g., for spam filtering or logging)",
    "type": "string"
  },
  "sieve_debug": {
    "default": false,
    "description": "Enable verbose debugging of Sieve script execution (only for troubleshooting \u2014 not for production)",
    "type": "bool"
  },
  "sieve_extensions": {
    "default": [
      "fileinto",
      "reject",
      "envelope",
      "vacation",
      "imapflags",
      "notify",
      "include",
      "variables",
      "date",
      "editheader",
      "copy",
      "subaddress",
      " relational",
      "regex"
    ],
    "description": "List of Sieve extensions to enable. Omit extensions your site doesn\u2019t need for security.",
    "items": {
      "description": "Name of a Sieve extension (e.g., 'fileinto' for moving messages, 'vacation' for auto-reply)",
      "type": "string"
    },
    "type": "list"
  },
  "sieve_global": {
    "default": false,
    "description": "If true, enables a global Sieve script for all users (not common, typically avoided)",
    "type": "bool"
  },
  "sieve_global_script": {
    "default": "",
    "description": "Path to the global Sieve script (e.g., /etc/dovecot/sieve/global.sieve). Ignored unless sieve_global is true.",
    "type": "string"
  },
  "sieve_global_script_dir": {
    "default": "/var/lib/dovecot/sieve/global/",
    "description": "Directory where global Sieve scripts are stored (used by some Sieve implementations for compiled scripts).",
    "type": "string"
  },
  "sieve_global_scripts_dir": {
    "default": "",
    "description": "(Deprecated) Legacy path to global Sieve scripts directory. Prefer sieve_global_script_dir in modern Dovecot.",
    "type": "string"
  },
  "sieve_include": {
    "description": "Additional Sieve-related settings (key=value pairs) to include in the plugin block. Use for advanced tuning.",
    "items": {
      "description": "A single Sieve plugin setting in the form 'key = value'. Example: sieve_trace_dir = /var/log/dovecot/sieve-trace/",
      "type": "string"
    },
    "type": "object"
  },
  "sieve_max_personal_scripts": {
    "default": 30,
    "description": "Maximum number of personal Sieve scripts (excluding global ones) a user may store.",
    "type": "number"
  },
  "sieve_max_script_size": {
    "default": 102400,
    "description": "Maximum allowed size (in bytes) of a single Sieve script. Prevents oversized or malicious scripts.",
    "type": "number"
  },
  "sieve_max_scripts": {
    "default": 50,
    "description": "Maximum total number of Sieve scripts (global + personal) allowed per user.",
    "type": "number"
  },
  "sieve_storage": {
    "default": "maildir",
    "description": "Storage backend used for Sieve scripts (usually 'maildir' or 'file'). 'maildir' stores scripts in Maildir++ hierarchy.",
    "enum": [
      "maildir",
      "file"
    ],
    "type": "string",
    "enum_open": true
  },
  "sieve_user_script": {
    "default": "file:~/sieve",
    "description": "Location of per-user Sieve scripts. Common values: file:~/sieve, file:~/.dovecot.sieve, or dir:~/.dovecot.sieve.d/",
    "type": "string"
  },
  "sieve_user_script_dir": {
    "default": "~/.dovecot.sieve.d/",
    "description": "Directory where users can store multiple Sieve scripts (used with script name selection via active file)",
    "type": "string"
  }
}
