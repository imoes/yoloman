{
 "auth": {
  "default": "ssh",
  "description": "Authentication method between server and clients (e.g., 'ssh', 'bsd', 'password').",
  "type": "string"
 },
 "changer": {
  "default": "ch-grup",
  "description": "Changer command or script for auto-changers (e.g., 'ch-grup' for autoloader with changer scripts).",
  "type": "string"
 },
 "changerparams": {
  "default": "",
  "description": "Parameters passed to the changer (e.g., 'ch-grup /dev/sg1').",
  "type": "string"
 },
 "compression_type": {
  "default": "lzo",
  "description": "Compression algorithm (e.g., 'lzo', 'gzip', 'none'). Only used if use_compression=true.",
  "type": "string"
 },
 "defaultcycle": {
  "default": "2 weeks",
  "description": "Full backup cycle duration — defines how often a full backup runs (e.g., '2 weeks', '7 days').",
  "type": "string"
 },
 "devices": {
  "default": [
   {
    "labelname": "Tape-%03d",
    "labelparams": "",
    "labeltype": "amlabel",
    "name": "tape1"
   }
  ],
  "description": "Device-specific labeling configuration (e.g., for tape labeling strategy).",
  "type": "list"
 },
 "devices[].labelname": {
  "description": "Label format (e.g., 'Tape-%03d' for 'Tape-001').",
  "type": "string"
 },
 "devices[].labelparams": {
  "description": "Additional label parameters (e.g., '2000000K' for size limit).",
  "type": "string"
 },
 "devices[].labeltype": {
  "default": "amlabel",
  "description": "Labeling method: 'amlabel' (Amanda auto-label), 'none', or 'none' for manual labeling.",
  "type": "string"
 },
 "devices[].name": {
  "description": "Logical device name (must match 'tapedev' or changer device name).",
  "type": "string"
 },
 "directory": {
  "default": "/var/lib/amanda",
  "description": "Base directory for Amanda state files (index, log, lock, etc.).",
  "type": "string"
 },
 "dumpuser": {
  "default": "backup",
  "description": "System user account that runs Amanda backup processes (must have access to tape device).",
  "type": "string"
 },
 "elog": {
  "default": "/var/log/amanda/amanda-elog",
  "description": "Path to the Amanda error log file.",
  "type": "string"
 },
 "encryption_type": {
  "default": "aes256",
  "description": "Encryption algorithm (e.g., 'aes256', '3des'). Only used if use_encryption=true.",
  "type": "string"
 },
 "extra_options": {
  "description": "Free-form extra configuration directives (e.g., 'dumptimeout 600', 'identtimeout 60'). Keys are option names; values are arguments.",
  "type": "object"
 },
 "hosts": {
  "default": [
   {
    "comment": "Production Web Server",
    "disklist_file": "/etc/amanda/disklists/client1",
    "name": "client1.example.com"
   },
   {
    "comment": "Database Server",
    "disks": [
     {
      "comment": "Root partition",
      "level": "0",
      "mountpoint": "/",
      "name": "/"
     },
     {
      "comment": "MySQL data",
      "level": "0",
      "mountpoint": "/var/lib/mysql",
      "name": "/var/lib/mysql"
     }
    ],
    "name": "client2.example.com"
   }
  ],
  "description": "List of Amanda clients to back up. Each entry may specify an external disklist file or inline disk entries.",
  "type": "list"
 },
 "hosts[].comment": {
  "description": "Optional human-readable description of the host.",
  "type": "string"
 },
 "hosts[].disklist_file": {
  "description": "Path to an external disklist file for this host (if omitted, use inline 'disks').",
  "type": "string"
 },
 "hosts[].disks": {
  "description": "Inline list of disks to back up for this host (used only if disklist_file is absent).",
  "items": {
   "comment": {
    "description": "Optional description of the disk.",
    "type": "string"
   },
   "level": {
    "default": "0",
    "description": "Dump level (e.g., '0' for full, '1' for incremental).",
    "type": "string"
   },
   "mountpoint": {
    "description": "Actual mountpoint or source path (may differ from name in Windows clients).",
    "type": "string"
   },
   "name": {
    "description": "Disk name/path (e.g., '/', '/home', 'C:').",
    "type": "string"
   }
  },
  "type": "list"
 },
 "hosts[].name": {
  "description": "Hostname of the Amanda client.",
  "type": "string"
 },
 "mailfrom": {
  "default": "amanda@example.com",
  "description": "Sender email address for Amanda notifications.",
  "type": "string"
 },
 "mailto": {
  "default": "admin@example.com",
  "description": "Default recipient for all Amanda notifications (alerts, summaries).",
  "type": "string"
 },
 "mailto_failure": {
  "default": "ops-alerts@example.com",
  "description": "Recipient for failure alerts only (leave blank to use 'mailto').",
  "type": "string"
 },
 "mailto_success": {
  "default": "",
  "description": "Recipient for success reports only (leave blank to use 'mailto').",
  "type": "string"
 },
 "maxdumpfile_size": {
  "default": "2G",
  "description": "Maximum size of an individual dump file before splitting.",
  "type": "string"
 },
 "maxdumps": {
  "default": 8,
  "description": "Maximum number of parallel dump processes (concurrent backups).",
  "type": "number"
 },
 "maxdumpsize": {
  "default": "10G",
  "description": "Maximum size of a single dump (before splitting or compression).",
  "type": "string"
 },
 "org": {
  "default": "MyCompany Backup",
  "description": "Organization name — used in email notifications and logs.",
  "type": "string"
 },
 "schedule": {
  "default": {
   "name": "daily",
   "rules": [
    {
     "level": "0",
     "name": "daily-sunday",
     "pattern": "Sun"
    },
    {
     "level": "1",
     "name": "daily-mon-sat",
     "pattern": "Mon-Sat"
    }
   ]
  },
  "description": "Backup schedule definition. Each rule maps a time pattern to a dump level.",
  "type": "object"
 },
 "schedule.name": {
  "description": "Schedule name (for reference only).",
  "type": "string"
 },
 "schedule.rules": {
  "description": "List of schedule rules.",
  "items": {
   "level": {
    "default": "0",
    "description": "Backup level (e.g., '0' for full, '1' for incremental).",
    "type": "string"
   },
   "name": {
    "description": "Rule name (e.g., 'weekend-full').",
    "type": "string"
   },
   "pattern": {
    "description": "Time pattern (e.g., 'Sun', 'Mon-Fri', '00:00-23:59').",
    "type": "string"
   }
  },
  "type": "list"
 },
 "tapecycle": {
  "default": 1,
  "description": "Number of tape cycles before rotation is enforced (minimum number of full backup cycles per tape).",
  "type": "number"
 },
 "tapedev": {
  "default": "/dev/st0",
  "description": "Path to the primary tape device (e.g., /dev/st0 for SCSI tape).",
  "type": "string"
 },
 "tapesize": {
  "default": "10G",
  "description": "Maximum total backup data size per tape (e.g., '10G', '2000000K', or 'auto').",
  "type": "string"
 },
 "tapeutil": {
  "default": "mt-st",
  "description": "Tape utility command for tape control (e.g., 'mt-st', 'mt', or custom script).",
  "type": "string"
 },
 "tlog": {
  "default": "/var/log/amanda/amanda.log",
  "description": "Path to the Amanda transaction log file.",
  "type": "string"
 },
 "tpchanger": {
  "default": "",
  "description": "Alternative changer device for tape-puller changers (e.g., '/dev/sg1'). Leave blank if not used.",
  "type": "string"
 },
 "use_compression": {
  "default": true,
  "description": "Enable compression of backup data before writing to tape.",
  "type": "bool"
 },
 "use_encryption": {
  "default": false,
  "description": "Enable encryption of backup data on tape (requires OpenSSL and compatible device).",
  "type": "bool"
 }
}
