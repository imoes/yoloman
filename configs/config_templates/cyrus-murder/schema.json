{
  "common_auth_include": {
    "type": "string",
    "default": "common-auth",
    "description": "PAM include directive for authentication (e.g., 'common-auth')"
  },
  "common_account_include": {
    "type": "string",
    "default": "common-account",
    "description": "PAM include directive for account management (e.g., 'common-account')"
  }
}
