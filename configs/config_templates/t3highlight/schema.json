{
  "language": {
    "type": "string",
    "default": "auto",
    "description": "Source language for highlighting (see -L option)"
  },
  "style": {
    "type": "string",
    "default": "esc",
    "description": "Output style (see -L option, e.g., esc, html)",
    "enum": [
      "esc",
      "html"
    ]
  },
  "document_type": {
    "type": "string",
    "default": "standalone",
    "description": "Document type for styles with multiple types (see -d option)",
    "enum": [
      "standalone",
      "separate-css",
      "raw"
    ]
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Print verbose warning messages"
  },
  "custom_tags": {
    "type": "list",
    "default": [],
    "description": "List of custom key-value tags for document header/footer (--tag options)"
  },
  "language_file": {
    "type": "string",
    "default": "",
    "description": "Path to a custom language file (--language-file option)"
  }
}
