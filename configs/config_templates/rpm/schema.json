{
  "macros": {
    "type": "list",
    "default": [],
    "description": "List of RPM macros to define via --define. Each item is a flat map with 'name' and 'value'. This corresponds to the --define option and macro configuration path in rpm(8). Use FLAT map structure as required by schema rules for list-of-objects, even though 'items' isn't explicitly named in the top-level schema key, the structure follows the pattern: list of objects with keys matching the flat map fields defined below. Note: To comply with 'items as a FLAT map field-name' rule, we treat 'macros' as a list where each element has fields defined by the 'items' concept implicitly via the example structure. However, strict adherence to 'NO custom filters' and 'standard Jinja' means we iterate. The schema for 'macros' items is described by its content. Let's define the item structure clearly. The prompt says: 'For a list-of-objects, add items as a FLAT map field-name -> {type, default, description}'. So I need to add an 'items' key to the 'macros' schema object? No, 'items' is a field *inside* the schema object for the list variable? No, usually JSON Schema has 'items'. The prompt says: 'add items as a FLAT map field-name -> ...'. This implies the value of the 'items' key in the variable's schema object should be that map? Or that the variable schema has a key 'items' which is the schema for the elements? Yes. So 'macros' schema will have 'type': 'list', 'default': [], 'description': '...', and 'items': { 'name': {...}, 'value': {...} }? Wait, the prompt says 'items as a FLAT map field-name -> {type, default, description}'. This is slightly ambiguous. Does it mean the 'items' key contains a map of field names to their schemas? Yes, that's standard JSON Schema style. Let's do that. So 'macros': { type: list, items: { name: {type: string}, value: {type: string} } }. This fits. Let's verify. 'default' for the list is []. 'items' is the schema for the elements. This is a common interpretation. Let's proceed with this structure for 'macros'.} ,\n    \"httpproxy\": {\n      \"type\": \"string\",\n      \"default\": \"\",\n      \"description\": \"HTTP proxy host for rpm fetches. Corresponds to --httpproxy or %_httpproxy macro.\"\n    },\n    \"httpport\": {\n      \"type\": \"number\",\n      \"default\": 8080,\n      \"description\": \"HTTP proxy port. Corresponds to --httpport or %_httpport macro.\"\n    },\n    \"dbpath\": {\n      \"type\": \"string\",\n      \"default\": \"/var/lib/rpm\",\n      \"description\": \"Database path. Corresponds to --dbpath.\"\n    },\n    \"root\": {\n      \"type\": \"string\",\n      \"default\": \"/\",\n      \"description\": \"Root directory. Corresponds to --root.\"\n    },\n    \"query_format\": {\n      \"type\": \"string\",\n      \"default\": \"%{NAME} %{VERSION}\\n\",\n      \"description\": \"Query format string. Corresponds to --qf/--queryformat.\"\n    },\n    \"include_docs\": {\n      \"type\": \"bool\",\n      \"default\": true,\n      \"description\": \"Include documentation. Corresponds to --includedocs.\"\n    },\n    \"exclude_docs\": {\n      \"type\": \"bool\",\n      \"default\": false,\n      \"description\": \"Exclude documentation. Corresponds to --excludedocs.\"\n    }\n  },\n  "
  }
}
