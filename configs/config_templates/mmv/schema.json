{
  "pcp_tmp_dir": {
    "type": "string",
    "default": "/var/tmp/pcp",
    "description": "The PCP temporary directory where MMV files are generated."
  },
  "service_name": {
    "type": "string",
    "default": "myapp",
    "description": "The name of the application or source generating the metrics."
  },
  "pmda_domain": {
    "type": "number",
    "default": -1,
    "description": "The PMDA domain number. Use -1 for dynamic assignment or a specific integer."
  },
  "cluster_id": {
    "type": "number",
    "default": 0,
    "description": "Cluster identifier hint for metric identification."
  },
  "flags": {
    "type": "number",
    "default": 0,
    "description": "MMV flags (e.g., MMV_FLAG_PROCESS)."
  },
  "metrics": {
    "type": "list",
    "default": [],
    "description": "List of metric definitions to export."
  },
  "indoms": {
    "type": "list",
    "default": [],
    "description": "List of instance domain definitions."
  },
  "instances": {
    "type": "list",
    "default": [],
    "description": "List of instance definitions."
  },
  "labels": {
    "type": "list",
    "default": [],
    "description": "List of label definitions for v3 MMV format."
  }
}
