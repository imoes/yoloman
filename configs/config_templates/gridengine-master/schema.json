{
  "PIDFILE": {
    "type": "string",
    "default": "/var/run/gridengine/qmaster.pid",
    "description": "Path to the PID file for the SGE master daemon"
  },
  "SCRIPTNAME": {
    "type": "string",
    "default": "/etc/init.d/gridengine-master",
    "description": "Path to this init script"
  },
  "SGE_START_MASTERD": {
    "type": "string",
    "default": "yes",
    "description": "Set to 'yes' to enable starting the SGE master daemon automatically",
    "enum": [
      "yes",
      "no"
    ]
  },
  "DAEMON_ARGS": {
    "type": "string",
    "default": "",
    "description": "Additional arguments passed to the sge_qmaster daemon at startup"
  }
}
