{
  "server_ip": {
    "type": "string",
    "default": "10.0.0.2",
    "description": "The IP address the iodined server listens on for tunnel traffic."
  },
  "tunnel_network": {
    "type": "string",
    "default": "10.0.0.1/24",
    "description": "The IP network assigned to clients connecting via the tunnel."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Password required for client authentication."
  },
  "capability_file": {
    "type": "string",
    "default": "/var/lib/iodine/capfile",
    "description": "Path to the capability file used for DNS resolution within the tunnel."
  },
  "pid_file": {
    "type": "string",
    "default": "/run/iodined.pid",
    "description": "Path to the PID file for the iodined daemon."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/iodine.log",
    "description": "Path to the log file for iodined."
  }
}
