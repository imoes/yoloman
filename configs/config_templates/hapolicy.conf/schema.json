{
  "global": {
    "type": "object",
    "default": [],
    "description": "Placeholder global object. hapolicy is a CLI tool and does not read this config file for runtime settings. Settings are passed via command line (e.g., -s, -l, -v) in postfix master.cf."
  }
}
