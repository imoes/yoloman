{
  "home_dir": {
    "type": "string",
    "default": "~/.rnp",
    "description": "Directory where RNP looks for keyrings. Source: --homedir"
  },
  "default_cipher": {
    "type": "string",
    "default": "AES256",
    "description": "Symmetric algorithm used during encryption. Source: --cipher"
  },
  "default_hash": {
    "type": "string",
    "default": "SHA256",
    "description": "Hash algorithm used for signing and key derivation. Source: --hash",
    "enum": [
      "SHA256",
      "SHA512"
    ]
  },
  "armor_output": {
    "type": "bool",
    "default": false,
    "description": "Apply ASCII armoring to output. Source: --armor"
  },
  "compression_algo": {
    "type": "string",
    "default": "zlib",
    "description": "Compression algorithm: zlib, zip, or bzip2. Source: --zip/--bzip2"
  },
  "compression_level": {
    "type": "number",
    "default": 6,
    "description": "Compression level 0-9. Source: -z"
  },
  "aead_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable AEAD encryption. Source: --aead"
  },
  "aead_algo": {
    "type": "string",
    "default": "EAX",
    "description": "AEAD algorithm selection: EAX or OCB. Source: --aead",
    "enum": [
      "EAX",
      "OCB"
    ]
  },
  "recipients": {
    "type": "list",
    "default": [],
    "description": "List of public key identifiers to encrypt to. Source: --recipient"
  },
  "signers": {
    "type": "list",
    "default": [],
    "description": "List of secret key identifiers to sign with. Source: --userid"
  },
  "pass_fd": {
    "type": "number",
    "default": 0,
    "description": "File descriptor to read passwords from. 0 disables. Source: --pass-fd"
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Password string. Discouraged for production; use pass_fd. Source: --password"
  },
  "overwrite": {
    "type": "bool",
    "default": false,
    "description": "Overwrite existing output files without prompt. Source: --overwrite"
  },
  "output_dir": {
    "type": "string",
    "default": ".",
    "description": "Default output directory for processed files."
  }
}
