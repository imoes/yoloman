{
  "warnquota_command_path": {
    "type": "string",
    "default": "/usr/sbin/warnquota",
    "description": "Absolute path to the warnquota executable."
  },
  "default_quota_file_path": {
    "type": "string",
    "default": "/etc/default/quota",
    "description": "Path to the quota default configuration file that defines the run_warnquota variable."
  },
  "fstab_path": {
    "type": "string",
    "default": "/etc/fstab",
    "description": "Path to the fstab file, checked for quota entries."
  },
  "warnquota_command": {
    "type": "string",
    "default": "/usr/sbin/warnquota -ug",
    "description": "Full command to invoke warnquota with options."
  }
}
