{
  "exports": {
    "type": "list",
    "default": [],
    "description": "List of directories to export via NFS, with client specifications and options.",
    "items": {
      "type": "object",
      "fields": {
        "path": {
          "type": "string",
          "default": "",
          "description": "Absolute path of the directory to export."
        },
        "client": {
          "type": "string",
          "default": "",
          "description": "Client(s) allowed to access the export (e.g., '192.168.1.0/24', '*.example.com', or '*')."
        },
        "options": {
          "type": "list",
          "default": [
            "rw",
            "sync",
            "no_subtree_check"
          ],
          "description": "List of NFS export options (e.g., 'ro', 'rw', 'sync', 'async', 'no_root_squash', 'no_subtree_check', 'secure')."
        }
      }
    }
  }
}
