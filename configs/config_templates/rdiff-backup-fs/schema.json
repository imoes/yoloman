{
  "debug_level": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level for debug messages (0-4)."
  },
  "full_memory": {
    "type": "bool",
    "default": false,
    "description": "Store information about all revisions in memory."
  },
  "last_mode": {
    "type": "bool",
    "default": false,
    "description": "Display files from the most recent increment as directories with versions."
  },
  "caching_files": {
    "type": "number",
    "default": 10,
    "description": "Number of files retrieved from archive to cache."
  },
  "revisions_memory": {
    "type": "number",
    "default": 10,
    "description": "Number of revisions stored in memory for on-demand retrieval."
  },
  "temp_dir": {
    "type": "string",
    "default": "/tmp",
    "description": "Directory for temporary files."
  },
  "mount_point": {
    "type": "string",
    "default": "/mnt/rdiff-backup",
    "description": "The local directory where the rdiff-backup archive will be mounted."
  },
  "repository_path": {
    "type": "string",
    "default": "/var/backups/rdiff",
    "description": "Path to the rdiff-backup archive repository."
  }
}
