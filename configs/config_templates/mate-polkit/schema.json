{
  "default_executable": {
    "type": "string",
    "default": "/usr/bin/mate-polkit",
    "description": "The absolute path to the MATE PolicyKit executable wrapper."
  },
  "default_desktop_file": {
    "type": "string",
    "default": "org.mate.PolicyKit.mate-polkit.desktop",
    "description": "The desktop entry file name for the authentication agent."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debug output to standard error."
  },
  "auto_detect_desktop": {
    "type": "bool",
    "default": true,
    "description": "Automatically detect the running desktop environment if possible."
  }
}
