{
  "acme_challenge_path_prefix": {
    "type": "string",
    "default": "/.well-known/acme-challenge/",
    "description": "URL path prefix for ACME HTTP-01 challenge responses. Must match Let's Encrypt requirement."
  },
  "acme_challenge_dir": {
    "type": "string",
    "default": "/run/acme/acme-challenge",
    "description": "Absolute filesystem path where ACME HTTP-01 challenge files are written."
  }
}
