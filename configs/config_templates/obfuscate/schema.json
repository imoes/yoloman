{
  "default_mode": {
    "type": "string",
    "default": "fill",
    "description": "The default censoring method. 'fill' black boxes areas, 'blur' applies a blur overlay.",
    "enum": [
      "fill",
      "blur"
    ]
  },
  "blur_radius": {
    "type": "number",
    "default": 10,
    "description": "The radius of the blur effect when using 'blur' mode."
  },
  "extensions": {
    "type": "list",
    "default": [
      "jpg",
      "png",
      "gif"
    ],
    "description": "List of image file extensions supported by the tool for censoring."
  }
}
