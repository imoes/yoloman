{
  "home_dir": {
    "type": "string",
    "default": "/root",
    "description": "The user's login directory used to compute kickpass workspace path."
  },
  "editor": {
    "type": "string",
    "default": "nano",
    "description": "The preferred utility to edit text files/safe metadata."
  },
  "agent_sock": {
    "type": "string",
    "default": "/tmp/kickpass.sock",
    "description": "Path to the socket used to communicate with kickpass agent."
  },
  "default_password_length": {
    "type": "number",
    "default": 32,
    "description": "Default length for generated random passwords."
  },
  "auto_generate_password": {
    "type": "bool",
    "default": false,
    "description": "Whether to use generated random passwords by default when creating safes."
  },
  "agent_timeout_seconds": {
    "type": "number",
    "default": 3600,
    "description": "Timeout in seconds for the opened safe in the kickpass agent."
  },
  "init_safes": {
    "type": "list",
    "default": [],
    "description": "List of safe names/paths to be initialized in the workspace."
  }
}
