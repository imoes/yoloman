{
  "methods": {
    "type": "string",
    "default": "m_inmail m_passwd m_finger",
    "description": "Space separated list of lookup methods to use in ascending priority order."
  },
  "inmail_db": {
    "type": "string",
    "default": "${XDG_DATA_HOME:-$HOME/.local/share}/lbdb/m_inmail.db",
    "description": "Path to the m_inmail database file."
  },
  "finger_hosts": {
    "type": "string",
    "default": "master.debian.org va.debian.org localhost",
    "description": "Space separated list of hosts to query via finger."
  },
  "mail_domain_name": {
    "type": "string",
    "default": "does-not-exist.org",
    "description": "Override the system's mail domain name."
  },
  "mutt_directory": {
    "type": "string",
    "default": "$HOME/.mutt",
    "description": "Directory containing mutt alias files."
  },
  "muttalias_files": {
    "type": "string",
    "default": ".muttrc .mail_aliases muttrc aliases",
    "description": "Space separated list of filenames in MUTT_DIRECTORY containing mutt aliases."
  },
  "pine_rc": {
    "type": "string",
    "default": "/etc/pine.conf /etc/pine.conf.fixed .pinerc",
    "description": "Space separated list of pine configuration files to inspect for addressbooks."
  },
  "pine_addressbooks": {
    "type": "string",
    "default": "/etc/addressbook .addressbook",
    "description": "Space separated list of pine addressbook files."
  },
  "wanderlust_addresses": {
    "type": "string",
    "default": "$HOME/.addresses",
    "description": "Path to WanderLust address book file."
  },
  "palm_address_database": {
    "type": "string",
    "default": "$HOME/.jpilot/AddressDB.pdb",
    "description": "Path to Palm address database file."
  },
  "gnomecard_files": {
    "type": "string",
    "default": "$HOME/.gnome/GnomeCard.gcrd",
    "description": "Space separated list of GnomeCard database files."
  },
  "emacs_cmd": {
    "type": "string",
    "default": "emacs",
    "description": "Emacs command to use for BBDB module."
  },
  "ldap_nicks": {
    "type": "string",
    "default": "debian bigfoot",
    "description": "Space separated list of LDAP server nicknames to use."
  },
  "abook_files": {
    "type": "string",
    "default": "$HOME/.abook/friends $HOME/.abook/work",
    "description": "Space separated list of abook addressbook files."
  },
  "passwd_ignore_sys": {
    "type": "string",
    "default": "true",
    "description": "Set to 'true' to ignore UIDs outside the 1000-29999 range in /etc/passwd.",
    "enum": [
      "true",
      "false"
    ]
  },
  "vcf_files": {
    "type": "string",
    "default": "$HOME/my.vcf",
    "description": "Space separated list of VCF files to search."
  },
  "modules_path": {
    "type": "string",
    "default": "${prefix}/libexec/lbdb $HOME/.lbdb/modules",
    "description": "Space separated list of directories containing private lbdb modules."
  },
  "sort_output": {
    "type": "string",
    "default": "address",
    "description": "Sorting method for output: 'address', 'name', 'comment', 'reverse_comment', 'false', or 'no'.",
    "enum": [
      "address",
      "name",
      "comment",
      "reverse_comment",
      "false",
      "no"
    ]
  },
  "keep_dupes": {
    "type": "string",
    "default": "no",
    "description": "Whether to keep duplicate mail addresses. 'no' or 'yes'.",
    "enum": [
      "yes",
      "no"
    ]
  },
  "munge_limitcount": {
    "type": "number",
    "default": 3,
    "description": "Minimum number of times an entry must be seen by m_inmail to survive cleanup."
  },
  "munge_limitdate": {
    "type": "string",
    "default": "$(date --date='-1 month' +\"%F %R\")",
    "description": "Date string for m_inmail entry cleanup (older entries are removed unless seen > limitcount times)."
  },
  "mu_personal": {
    "type": "string",
    "default": "true",
    "description": "If 'true' or 'yes', only find addresses from messages where 'my' email was in an address field (m_mu).",
    "enum": [
      "true",
      "false",
      "yes",
      "no"
    ]
  },
  "mu_after": {
    "type": "string",
    "default": "$(date --date='-1 year' +%s)",
    "description": "UNIX timestamp. Only see addresses seen in messages after this time (m_mu)."
  },
  "mu_ignore_missingname": {
    "type": "string",
    "default": "true",
    "description": "If 'true' or 'yes', do not see addresses without a real name (m_mu).",
    "enum": [
      "true",
      "false",
      "yes",
      "no"
    ]
  },
  "mu_home": {
    "type": "string",
    "default": "$HOME/.mu",
    "description": "Override the MU homedir."
  }
}
