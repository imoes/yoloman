{
  "timeout": {
    "type": "number",
    "default": 30,
    "description": "Global network timeout in seconds for fetching stream properties."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging and configuration parsing output."
  },
  "config_path": {
    "type": "string",
    "default": "/etc/quvi/quvirc",
    "description": "The canonical path to the main configuration file."
  },
  "script_dirs": {
    "type": "list",
    "default": [
      "/usr/lib/quvi/scripts",
      "/usr/local/lib/quvi/scripts"
    ],
    "description": "Directories where quvi searches for media parsing scripts (lua scripts). Items is a flat list of strings representing paths."
  }
}
