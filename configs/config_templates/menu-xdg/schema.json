{
  "outputencoding": {
    "type": "string",
    "default": "UTF-8",
    "description": "The character encoding for the generated menu files."
  },
  "outputlanguage": {
    "type": "string",
    "default": "C",
    "description": "The locale/language setting for the generated menu files.",
    "enum": [
      "C",
      "en",
      "de",
      "pt"
    ]
  },
  "genmenu": {
    "type": "string",
    "default": "debian-menu.menu",
    "description": "The name of the generated main menu file."
  },
  "rootprefix": {
    "type": "string",
    "default": "/var/lib/menu-xdg/menus",
    "description": "The root prefix path for system-wide menu storage."
  },
  "userprefix": {
    "type": "string",
    "default": ".config/menus",
    "description": "The relative user home directory path for user-specific menu storage."
  }
}
