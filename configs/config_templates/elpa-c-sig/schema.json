{
  "config_dir": {
    "type": "string",
    "default": "/etc/emacs/site-start.d",
    "description": "Directory where the site-startup configuration file is installed"
  },
  "filename": {
    "type": "string",
    "default": "50c-sig.el",
    "description": "Filename of the site-startup configuration file"
  },
  "install_prefix": {
    "type": "string",
    "default": "/usr/share/emacs/site-lisp",
    "description": "Base installation prefix for site Lisp packages"
  },
  "package_name": {
    "type": "string",
    "default": "c-sig",
    "description": "Name of the package whose elisp files are installed"
  }
}
