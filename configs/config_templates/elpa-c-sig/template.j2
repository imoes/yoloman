;; -*-emacs-lisp---
;;
;; {{ 'Emacs site-startup configuration file for ' ~ (package_name | default('c-sig')) | default('Emacs site-startup configuration file for c-sig') }}
;; Managed by Ansible: do not edit manually.
;;
;; Schema defaults:
;;   config_dir:      '/etc/emacs/site-start.d' (Directory for site-startup files)
;;   filename:        '50c-sig.el'          (Filename of this configuration file)
;;   install_prefix:  '/usr/share/emacs/site-lisp' (Base installation prefix for site Lisp packages)
;;   package_name:    'c-sig'              (Name of the Emacs Lisp package)

;; The {{ package_name | default('c-sig') }} package follows the Debian/GNU Linux 'emacsen' policy and
;; byte-compiles its elisp files for each 'emacs flavor' (emacs19,
;; xemacs19, emacs20, xemacs20...).  The compiled code is then
;; installed in a subdirectory of the respective site-lisp directory.
;; We have to add this to the load-path:
(let ((package-dir (concat "{{ install_prefix | default('/usr/share/emacs/site-lisp') }}/{{ package_name | default('c-sig') }}")))
;; If package-dir does not exist, the {{ package_name | default('c-sig') }} package must have
;; removed but not purged, and we should skip the setup.
  (when (file-directory-p package-dir)
    (if (fboundp 'debian-pkg-add-load-path-item)
        (debian-pkg-add-load-path-item package-dir)
      (setq load-path (cons package-dir load-path)))
    (autoload 'add-signature "{{ package_name | default('c-sig') }}" "{{ package_name | default('c-sig') }}" t)
    (autoload 'delete-signature "{{ package_name | default('c-sig') }}" "{{ package_name | default('c-sig') }}" t)
    (autoload 'insert-signature-eref "{{ package_name | default('c-sig') }}" "{{ package_name | default('c-sig') }}" t)
    (autoload 'insert-signature-automatically "{{ package_name | default('c-sig') }}" "{{ package_name | default('c-sig') }}" t)
    (autoload 'insert-signature-randomly "{{ package_name | default('c-sig') }}" "{{ package_name | default('c-sig') }}" t)))