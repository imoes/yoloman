{
  "rolo_datafile": {
    "type": "string",
    "default": "~/.rolo/contacts.vcf",
    "description": "The path to the vcf file storing contacts. Defaults to ~/.rolo/contacts.vcf if not specified in command line."
  },
  "rolo_editor": {
    "type": "string",
    "default": "/usr/bin/editor",
    "description": "The command to use for editing contact entries. Falls back to EDITOR env var, then /usr/bin/editor."
  },
  "rolo_readonly": {
    "type": "bool",
    "default": false,
    "description": "If true, forces read-only mode, preventing modification of the contact database."
  }
}
