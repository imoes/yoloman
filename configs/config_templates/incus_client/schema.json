{
  "store_path": {
    "type": "string",
    "default": "~/.local/share/incus",
    "description": "Path to the client state directory (stores images, aliases, logs, etc.)."
  },
  "default_remote": {
    "type": "string",
    "default": "local",
    "description": "Default Incus remote to use when none is specified (e.g., `local`, `images`, `incus`).",
    "enum": [
      "local",
      "images",
      "incus"
    ]
  },
  "server_url": {
    "type": "string",
    "default": "",
    "description": "Full server URL (e.g., `https://my.incus.server:8443`) \u2014 only needed for unnamed remotes."
  },
  "debug": {
    "type": "boolean",
    "default": false,
    "description": "Enable verbose debug output for troubleshooting."
  },
  "quiet": {
    "type": "boolean",
    "default": false,
    "description": "Suppress non-error output (e.g., for scripting)."
  },
  "aliases": {
    "type": "object",
    "default": [],
    "description": "Custom command aliases (key = alias name, value = command to run). Note: The config format uses simple lines `alias = cmd`, so this input is expected to be a list of strings in format `name=cmd` or the template should handle an object. Based on the existing template `{{ alias_name, alias_cmd }}`, it expects an object. However, the schema had `items: { type: string }` which is invalid for an object type without properties. Correcting the schema to match the usage (object with string values) is a fix for the schema shape consistency, but the prompt says 'Keep schema shape'. The previous schema had `type: object` but `items` instead of `properties`. I will fix the schema to be valid JSON schema for an object if needed, but the template expects an object. The previous schema was: `\"type\": \"object\", \"default\": {}, ... \"items\": { \"type\": \"string\" }`. This is invalid schema. I will fix it to be valid schema for an object with string values? No, I should preserve the shape. The shape was `type: object`. The `items` part was erroneous. I will remove the `items` from `aliases` schema to make it valid, or just leave it? The instructions say 'Fix a wrong default or type'. The `items` field is not a type or default. It's a schema definition error. I will remove the `items` key from `aliases` and `default_disk_devices` if they are hallucinated schema keys. Actually, looking at `default_disk_devices`, it had a complex schema. The template iterates over it. The schema provided a complex structure. The `aliases` schema was simpler. I will just remove the invalid `items` from `aliases` and `trusted_image_fingerprints` if they are not supported or wrong. But `trusted_image_fingerprints` is a list. `aliases` is an object. The `items` in `aliases` was likely a copy-paste error. I will remove it. Same for `default_disk_devices`? The template uses `default_disk_devices.items()`. The schema defined properties. That seems correct. I will remove the `items` from `aliases` schema as it's not a valid property for a `type: object` in strict JSON schema (should be `additionalProperties` or `properties`). However, the instruction is "
  },
  "removed": {
    "type": "array",
    "default": [
      "networked"
    ],
    "description": "Variables removed from the template because they are not documented in the official Incus client config man page."
  }
}
