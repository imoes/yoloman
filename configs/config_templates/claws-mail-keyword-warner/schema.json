{
  "warn_keywords": {
    "type": "list",
    "default": [],
    "description": "List of keywords (strings) that, if detected in outgoing messages, trigger a warning before sending. Regex metacharacters are supported. Empty list disables warnings."
  }
}
