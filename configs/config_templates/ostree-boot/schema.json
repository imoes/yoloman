{
  "ostree_dracut_modules": {
    "type": "string",
    "default": "ostree systemd ",
    "description": "Space-separated list of dracut modules to add for ostree boot support."
  },
  "ostree_reproducible": {
    "type": "bool",
    "default": true,
    "description": "Whether to enable reproducible build settings."
  }
}
