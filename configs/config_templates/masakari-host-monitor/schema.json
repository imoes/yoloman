{
  "provides": {
    "type": "string",
    "default": "masakari-host-monitor",
    "description": "Provides field for init script metadata"
  },
  "short_description": {
    "type": "string",
    "default": "Masakari Host Monitor daemon",
    "description": "Short description for init script metadata"
  },
  "description": {
    "type": "string",
    "default": "Masakari provides Virtual Machine High Availability (VMHA) service for\n                   OpenStack clouds by automatically recovering the KVM-based Virtual\n                   Machine(VM)s from failure events such as VM process down, provisioning\n                   process down, and nova-compute host failure. It also provides API service for\n                   manage and control the automated rescue mechanism.",
    "description": "Long description for init script metadata"
  },
  "author_email": {
    "type": "string",
    "default": "Thomas Goirand <zigo@debian.org>",
    "description": "Primary author contact info"
  },
  "author_email_2": {
    "type": "string",
    "default": "Ond\u0159ej Nov\u00fd <novy@ondrej.org>",
    "description": "Secondary author contact info"
  },
  "description_long": {
    "type": "string",
    "default": "OpenStack Masakari Host Monitor (masakari-host-monitor)",
    "description": "Description string used in DESC variable"
  },
  "project_name": {
    "type": "string",
    "default": "masakari",
    "description": "Base project name for directory and variable names"
  },
  "name_suffix": {
    "type": "string",
    "default": "host-monitor",
    "description": "Suffix for the service name"
  },
  "daemon_name_suffix": {
    "type": "string",
    "default": "hostmonitor",
    "description": "Suffix for the daemon binary name"
  },
  "NPROC": {
    "type": "number",
    "default": 1,
    "description": "Number of processors for fallback worker calculation (referenced as ${NPROC} in shell arithmetic)"
  }
}
