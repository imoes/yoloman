{
  "localstatedir": {
    "type": "string",
    "default": "/var",
    "description": "Base directory for variable state data (e.g., run, log, lib)"
  },
  "sysconfdir": {
    "type": "string",
    "default": "/etc",
    "description": "Base directory for configuration files"
  }
}
