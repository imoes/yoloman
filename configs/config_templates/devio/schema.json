{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options passed to devio(1); each item must be a valid argument string (e.g., '--help', '-v') per GNU long-option syntax. Only documented options: '--help', '-h', '--version', '-v'."
  }
}
