{
  "cron_hour": {
    "type": "string",
    "default": "4",
    "description": "Hour of the day (0\u201323) when apt-mirror cron job runs"
  },
  "apt_mirror_user": {
    "type": "string",
    "default": "apt-mirror",
    "description": "User account under which apt-mirror runs"
  },
  "cron_log_path": {
    "type": "string",
    "default": "/var/spool/apt-mirror/var/cron.log",
    "description": "Path to the cron job log file"
  }
}
