{
  "SYSDEBUG": {
    "type": "number",
    "default": 0,
    "description": "Set to 1 for debugging output, 0 to disable."
  },
  "SYSLOGOUT_TMPDIR": {
    "type": "string",
    "default": "/var/tmp/syslogout",
    "description": "The temporary directory used for syslogout debugging and user-specific state."
  },
  "SYSLOGOUT_DIR": {
    "type": "string",
    "default": "/etc/syslogout.d",
    "description": "The system-wide directory containing configuration scripts to execute."
  }
}
