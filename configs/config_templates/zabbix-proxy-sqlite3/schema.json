{
  "log_file_name": {
    "type": "string",
    "default": "zabbix_proxy.log",
    "description": "The filename for the Zabbix Proxy log file."
  },
  "log_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "The number of rotated log files to keep."
  }
}
