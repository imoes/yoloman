# Freeboard configuration template.
# This file configures the URL path and static file root for the Freeboard dashboard application.

# URL path alias under which Freeboard is served (e.g., /freeboard, /dashboards).
# This defines the base URL path for the application interface.
Alias {{ freeboard_alias_path | default('/freeboard') }} {{ freeboard_docroot | default('/usr/share/freeboard') }}