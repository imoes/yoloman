{
  "daemon_path": {
    "type": "string",
    "default": "/usr/sbin/imapproxyd",
    "description": "Full path to the imapproxy daemon executable."
  },
  "pidfile_path": {
    "type": "string",
    "default": "/var/run/imapproxy.pid",
    "description": "Full path to the PID file."
  },
  "default_file_path": {
    "type": "string",
    "default": "/etc/default/imapproxy",
    "description": "Path to the default configuration file for init script overrides."
  }
}
