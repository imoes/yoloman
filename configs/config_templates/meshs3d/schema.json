{
  "debug_mode": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode in meshs3d (corresponds to -d flag)"
  },
  "node_hostname": {
    "type": "string",
    "default": "localhost",
    "description": "Hostname or IP address of the OLSR node to visualize (corresponds to -H flag)"
  },
  "s3d_url": {
    "type": "string",
    "default": "",
    "description": "URL of the S3D environment to connect to, skipping the local S3D check (corresponds to --s3d-url flag). Leave empty to use default local behavior."
  }
}
