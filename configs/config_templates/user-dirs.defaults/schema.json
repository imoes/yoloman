{
  "xdg_enable_relative_paths": {
    "type": "bool",
    "default": false,
    "description": "If enabled, directory paths may be stored as relative (e.g. `../Documents` instead of `/home/user/Documents`). Disable for maximum compatibility with older or stricter applications."
  }
}
