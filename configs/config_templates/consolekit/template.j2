# /var/log/ConsoleKit/history
# Logrotate configuration for ConsoleKit session history logs.
# Controls how many historical log files are retained before oldest ones are deleted.

{# rotate_count: Number of rotated log files to keep #}
{# Default: 6 #}
	rotate {{ rotate_count | default(6) }}