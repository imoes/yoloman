{
  "verbose": {
    "type": "bool",
    "default": true,
    "description": "Enable verbose output from kmod utilities."
  },
  "kernel_version": {
    "type": "string",
    "default": "5.15.0",
    "description": "The running kernel version to resolve module paths against."
  },
  "modules_dep_path": {
    "type": "string",
    "default": "/lib/modules/{{ kernel_version }}/modules.dep",
    "description": "Absolute path to the modules.dep file."
  },
  "modprobe_options": {
    "type": "list",
    "default": [],
    "description": "Global command-line options to pass to modprobe."
  },
  "blacklist_modules": {
    "type": "list",
    "default": [],
    "description": "List of module names to blacklist."
  },
  "aliases": {
    "type": "list",
    "default": [],
    "description": "List of alias configurations for modules."
  },
  "alias_name": {
    "type": "string",
    "default": "eth0",
    "description": "Name of the alias (e.g., network interface name)."
  },
  "module_name": {
    "type": "string",
    "default": "e1000",
    "description": "Target module name for the alias."
  }
}
