{
  "slbackup_log_dir": {
    "type": "string",
    "default": "slbackup",
    "description": "Directory path for slbackup log files"
  },
  "slbackup_log_file": {
    "type": "string",
    "default": "slbackup.log",
    "description": "Filename for the main slbackup log"
  },
  "slbackup_log_rotate_count": {
    "type": "number",
    "default": 12,
    "description": "Number of rotated log files to keep for the main slbackup log"
  },
  "slbackup_run_scripts_log_file": {
    "type": "string",
    "default": "run_scripts.log",
    "description": "Filename for the run scripts log"
  },
  "slbackup_run_scripts_log_rotate_count": {
    "type": "number",
    "default": 12,
    "description": "Number of rotated log files to keep for the run scripts log"
  }
}
