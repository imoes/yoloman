{
  "opengl": {
    "type": "bool",
    "default": true,
    "description": "Whether to enable hardware-accelerated OpenGL rendering."
  },
  "check_for_updates": {
    "type": "bool",
    "default": true,
    "description": "Whether Tiled should check for updates on startup."
  },
  "zoom_step": {
    "type": "string",
    "default": "1.25",
    "description": "The factor by which zoom changes with each mouse wheel notch."
  }
}
