{
  "db_path": {
    "type": "string",
    "default": "/var/lib/virtuoso-opensource-7/db",
    "description": "The directory from where the database files are placed, defined by a relative path in the configfile."
  }
}
