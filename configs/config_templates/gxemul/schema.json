{
  "config_sections": {
    "type": "list",
    "default": [],
    "description": "List of configuration sections, each with a name and associated options (one per line, already formatted strings)."
  }
}
