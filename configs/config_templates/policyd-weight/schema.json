{
  "daemon_opts": {
    "type": "string",
    "default": "",
    "description": "Daemon options for policyd-weight (e.g., -D for foreground, -d for debug, -f for config file path).",
    "enum": [
      "-D",
      "-d",
      "-f",
      "-h",
      "-k",
      "-s",
      "-v"
    ]
  }
}
