{
  "ISO_LOCATION": {
    "type": "string",
    "default": "/boot/grml/",
    "description": "Directory path where ISO files are stored and from which rescue entries are generated."
  },
  "GRUB_SUBMENU": {
    "type": "string",
    "default": "Grml Rescue Boot menu",
    "description": "Name of the GRUB submenu under which grml rescue entries will be grouped. Set to empty string to disable submenu."
  },
  "CUSTOM_BOOTOPTIONS": {
    "type": "string",
    "default": "ssh=password lang=de",
    "description": "Space-separated boot options to append to all generated kernel command lines. Leave empty to use defaults."
  }
}
