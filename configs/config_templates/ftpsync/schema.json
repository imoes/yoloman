{
  "sync_archives": {
    "type": "list",
    "default": [],
    "description": "List of archive names to sync. Each item corresponds to an archive defined in ftpsync.conf(5). If empty, 'sync:all' is used."
  },
  "stage_only": {
    "type": "number",
    "default": 0,
    "description": "If 1, sync only in stage 1. If 2, sync only in stage 2. Omit (0) for full sync."
  },
  "multi_hop": {
    "type": "bool",
    "default": false,
    "description": "Enable multi-hop sync (usually in addition to stage 1)."
  },
  "callback": {
    "type": "bool",
    "default": false,
    "description": "Call back when sync completes."
  },
  "trigger_mode": {
    "type": "string",
    "default": "",
    "description": "Override auto-detected trigger mode: either 'ssh' or 'cron'. Use only when automatic detection fails.",
    "enum": [
      "ssh",
      "cron"
    ]
  }
}
