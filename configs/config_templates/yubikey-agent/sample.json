{
  "exec_start": "/usr/bin/yubikey-agent -l $XDG_RUNTIME_DIR/yubikey-agent/yubikey-agent.sock",
  "env_vars": [
    "SSH_AUTH_SOCK=$XDG_RUNTIME_DIR/yubikey-agent/yubikey-agent.sock"
  ],
  "restart_policy": "on-failure"
}
