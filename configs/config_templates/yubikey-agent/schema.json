{
  "exec_start": {
    "type": "string",
    "default": "/usr/bin/yubikey-agent -l $XDG_RUNTIME_DIR/yubikey-agent/yubikey-agent.sock",
    "description": "The full command line to start yubikey-agent, including the socket path."
  },
  "env_vars": {
    "type": "list",
    "default": [
      "SSH_AUTH_SOCK=$XDG_RUNTIME_DIR/yubikey-agent/yubikey-agent.sock"
    ],
    "description": "List of environment variable definitions to pass to the service process, typically SSH_AUTH_SOCK and optionally SSH_AUTH_SOCK pointing to the agent socket."
  },
  "restart_policy": {
    "type": "string",
    "default": "on-failure",
    "description": "The systemd restart policy for the yubikey-agent service (e.g., no, always, on-failure)."
  }
}
