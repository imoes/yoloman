# VOMS Server Configuration Template
# This template configures the VOMS (Virtual Organization Membership Service) server daemon.
# It is intended to be used by an Ansible role to render the main configuration file.

# {{ voms_user | default('voms') }}: The system user account under which the VOMS server runs.
VOMS_USER={{ voms_user | default('voms') }}