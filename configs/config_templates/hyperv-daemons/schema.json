{
  "hv_vss_daemon": {
    "type": "object",
    "description": "Configuration for Hyper-V Volume Shadow Copy Service daemon",
    "items": {
      "enabled": {
        "type": "bool",
        "default": true,
        "description": "Whether to start and enable the hv_vss_daemon"
      },
      "bin_path": {
        "type": "string",
        "default": "/usr/sbin/hv_vss_daemon",
        "description": "Full path to the hv_vss_daemon binary"
      },
      "log_level": {
        "type": "string",
        "default": "info",
        "description": "Logging verbosity level (e.g., error, warning, info, debug)"
      },
      "extra_args": {
        "type": "list",
        "default": [],
        "description": "Additional command-line arguments to pass to the daemon"
      }
    }
  },
  "hv_fcopy_daemon": {
    "type": "object",
    "description": "Configuration for Hyper-V File Copy daemon",
    "items": {
      "enabled": {
        "type": "bool",
        "default": true,
        "description": "Whether to start and enable the hv_fcopy_daemon"
      },
      "bin_path": {
        "type": "string",
        "default": "/usr/sbin/hv_fcopy_daemon",
        "description": "Full path to the hv_fcopy_daemon binary"
      },
      "log_level": {
        "type": "string",
        "default": "info",
        "description": "Logging verbosity level (e.g., error, warning, info, debug)"
      },
      "extra_args": {
        "type": "list",
        "default": [],
        "description": "Additional command-line arguments to pass to the daemon"
      }
    }
  },
  "hv_kvp_daemon": {
    "type": "object",
    "description": "Configuration for Hyper-V Key-Value Pair daemon",
    "items": {
      "enabled": {
        "type": "bool",
        "default": true,
        "description": "Whether to start and enable the hv_kvp_daemon"
      },
      "bin_path": {
        "type": "string",
        "default": "/usr/sbin/hv_kvp_daemon",
        "description": "Full path to the hv_kvp_daemon binary"
      },
      "log_level": {
        "type": "string",
        "default": "info",
        "description": "Logging verbosity level (e.g., error, warning, info, debug)"
      },
      "extra_args": {
        "type": "list",
        "default": [],
        "description": "Additional command-line arguments to pass to the daemon"
      }
    }
  }
}
