{
  "desc": {
    "type": "string",
    "default": "Hyper-V key-value pair (KVP) daemon",
    "description": "Description of the daemon displayed in logs and status messages."
  },
  "name": {
    "type": "string",
    "default": "hv_kvp_daemon",
    "description": "Name of the daemon, used for PID file and process identification."
  },
  "bin_path": {
    "type": "string",
    "default": "/usr/sbin/hv_kvp_daemon",
    "description": "Full path to the hv_kvp_daemon binary."
  },
  "kvp_device_path": {
    "type": "string",
    "default": "/dev/vmbus/hv_kvp",
    "description": "Path to the Hyper-V KVP kernel device."
  },
  "extra_args": {
    "type": "list",
    "default": [
      "--",
      "-n"
    ],
    "description": "Additional command-line arguments to pass to the daemon (e.g., flags)."
  }
}
