{
  "snmp_address": {
    "type": "string",
    "default": "@LOCAL",
    "description": "The address for SNMP traps/notifications. Default @LOCAL binds to the local host."
  },
  "snmp_community": {
    "type": "string",
    "default": "public",
    "description": "The community string used for SNMP read access."
  }
}
