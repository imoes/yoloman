{
 "auth_realm": {
  "default": "Request Tracker",
  "description": "Authentication realm displayed in login prompt.",
  "type": "string"
 },
 "auth_type": {
  "default": "htaccess",
  "description": "Authentication backend: 'htaccess' (htpasswd file), 'ldap', or 'none' (not recommended).",
  "type": "string"
 },
 "cors_enabled": {
  "default": false,
  "description": "Enable Cross-Origin Resource Sharing (CORS) for API or embedded widgets.",
  "type": "bool"
 },
 "cors_origin": {
  "default": "*",
  "description": "Allowed origin for CORS (e.g., 'https://dashboard.example.com'). Use '*' for all origins (not recommended in production).",
  "type": "string"
 },
 "environment_vars": {
  "description": "Additional environment variables to pass to RT (key-value pairs).",
  "items": {
   "type": "string"
  },
  "type": "object"
 },
 "extra_directives": {
  "default": [],
  "description": "List of additional Apache directives (e.g., custom headers, rewrite rules, logging). One per list item.",
  "items": {
   "type": "string"
  },
  "type": "list"
 },
 "fastcgi_max_processes": {
  "default": 10,
  "description": "Maximum number of FastCGI processes (controls concurrency). Adjust based on server capacity.",
  "type": "number"
 },
 "htpasswd_file": {
  "default": "/etc/request-tracker5/htpasswd",
  "description": "Path to htpasswd file for basic authentication (used when auth_type='htaccess').",
  "type": "string"
 },
 "ldap_bind_dn": {
  "default": "cn=readonly,dc=example,dc=com",
  "description": "Bind DN for LDAP queries (used when auth_type='ldap').",
  "type": "string"
 },
 "ldap_bind_password": {
  "default": "",
  "description": "Password for LDAP bind DN (used when auth_type='ldap'). Should be stored securely (e.g., via Apache env vars or file permissions).",
  "sensitive": true,
  "type": "string"
 },
 "ldap_url": {
  "default": "ldap://ldap.example.com/DC=example,DC=com?uid?sub?(objectClass=person)",
  "description": "LDAP URL for user authentication and lookup (used when auth_type='ldap').",
  "type": "string"
 },
 "perl5lib": {
  "default": "/usr/local/lib/request-tracker5/lib:/usr/share/request-tracker5/lib",
  "description": "Perl library path(s) required by RT. Colon-separated list of directories.",
  "type": "string"
 },
 "rt_home": {
  "default": "/usr/share/request-tracker5",
  "description": "RT installation home directory (contains bin/, lib/, etc.).",
  "type": "string"
 },
 "rt_lib": {
  "default": "/usr/share/request-tracker5/lib/RT",
  "description": "Path to RT library directory.",
  "type": "string"
 },
 "server_name": {
  "default": "localhost",
  "description": "Server hostname (used if ssl_redirect_host not specified).",
  "type": "string"
 },
 "ssl_redirect_host": {
  "default": "https://",
  "description": "Base URL for SSL redirect (e.g., 'https://rt.example.com'). Defaults to 'https://' + server_name.",
  "type": "string"
 },
 "use_ssl": {
  "default": true,
  "description": "Enable HTTPS and redirect HTTP traffic to HTTPS.",
  "type": "bool"
 },
 "web_path": {
  "default": "rt",
  "description": "URL path where RT will be accessible (e.g., 'rt' → http://example.com/rt/).",
  "type": "string"
 }
}
