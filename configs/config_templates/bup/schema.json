{
  "bup_dir": {
    "type": "string",
    "default": "~/.bup",
    "description": "Path to the bup repository directory; corresponds to -d/--bup-dir option or BUP_DIR environment variable"
  },
  "git_check_override": {
    "type": "string",
    "default": "",
    "description": "If set to 'true', 'yes', or '1', bypasses git version verification; corresponds to BUP_ASSUME_GIT_VERSION_ISFINE environment variable",
    "enum": [
      "true",
      "yes",
      "1"
    ]
  }
}
