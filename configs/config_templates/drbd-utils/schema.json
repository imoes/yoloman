{
  "drbd_include_path": {
    "type": "string",
    "default": "drbd.d",
    "description": "Path to the directory containing drbd configuration includes (global_common.conf and *.res files)."
  }
}
