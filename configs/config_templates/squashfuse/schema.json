{
  "archive_path": {
    "type": "string",
    "default": "/var/lib/squashfuse/images/rootfs.squashfs",
    "description": "Path to the SquashFS archive file"
  },
  "mountpoint": {
    "type": "string",
    "default": "/mnt/squashfs",
    "description": "Directory where the filesystem will be mounted"
  },
  "options": {
    "type": "list",
    "default": [
      "allow_other"
    ],
    "description": "List of options to pass to squashfuse (e.g., subdir=/usr, offset=1024, debug)"
  },
  "foreground": {
    "type": "bool",
    "default": false,
    "description": "Run in foreground mode (implies debug)"
  }
}
