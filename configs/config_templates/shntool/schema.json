{
  "mode": {
    "type": "string",
    "default": "len",
    "description": "Operating mode: len, fix, hash, pad, join, split, cat, cmp, cue, conv, info, strip, gen, trim",
    "enum": [
      "len",
      "fix",
      "hash",
      "pad",
      "join",
      "split",
      "cat",
      "cmp",
      "cue",
      "conv",
      "info",
      "strip",
      "gen",
      "trim"
    ]
  },
  "output_directory": {
    "type": "string",
    "default": ".",
    "description": "Directory to write output files"
  },
  "filename_prefix": {
    "type": "string",
    "default": "",
    "description": "Prefix string added to base part of output filenames"
  },
  "filename_postfix": {
    "type": "string",
    "default": "",
    "description": "Postfix string added to base part of output filenames"
  },
  "overwrite_policy": {
    "type": "string",
    "default": "ask",
    "description": "How to handle existing files: ask, always, never",
    "enum": [
      "ask",
      "always",
      "never"
    ]
  },
  "progress_indicator": {
    "type": "string",
    "default": "pct",
    "description": "Progress indicator type: pct, dot, spin, face, none",
    "enum": [
      "pct",
      "dot",
      "spin",
      "face",
      "none"
    ]
  },
  "input_decoder": {
    "type": "string",
    "default": "",
    "description": "Input file format decoder and arguments (e.g., 'shn shorten -x %f')"
  },
  "output_encoder": {
    "type": "string",
    "default": "",
    "description": "Output file format extension, encoder and arguments (e.g., 'flac - %f')"
  },
  "file_reorder": {
    "type": "string",
    "default": "natural",
    "description": "Reorder input files: ask, ascii, natural, none",
    "enum": [
      "ask",
      "ascii",
      "natural",
      "none"
    ]
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Suppress non-critical output"
  },
  "suppress_warnings": {
    "type": "bool",
    "default": false,
    "description": "Suppress warnings"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Print debugging information"
  },
  "time_format_high_precision": {
    "type": "bool",
    "default": false,
    "description": "Print times in h:mm:ss.{ff,nnn} format"
  },
  "total_unit": {
    "type": "string",
    "default": "b",
    "description": "Unit for len total size: b, kb, mb, gb, tb",
    "enum": [
      "b",
      "kb",
      "mb",
      "gb",
      "tb"
    ]
  },
  "file_unit": {
    "type": "string",
    "default": "b",
    "description": "Unit for len individual file size: b, kb, mb, gb, tb",
    "enum": [
      "b",
      "kb",
      "mb",
      "gb",
      "tb"
    ]
  },
  "column_names_hidden": {
    "type": "bool",
    "default": false,
    "description": "Do not show column names in len output"
  },
  "totals_line_hidden": {
    "type": "bool",
    "default": false,
    "description": "Do not show totals line in len output"
  },
  "shift_backward": {
    "type": "bool",
    "default": true,
    "description": "Fix mode: Shift track breaks backward to previous sector boundary"
  },
  "shift_forward": {
    "type": "bool",
    "default": false,
    "description": "Fix mode: Shift track breaks forward to next sector boundary"
  },
  "round_sectors": {
    "type": "bool",
    "default": false,
    "description": "Fix mode: Round track breaks to nearest sector boundary"
  },
  "pad_last_file": {
    "type": "bool",
    "default": true,
    "description": "Fix mode: Pad last file with silence"
  },
  "check_only": {
    "type": "bool",
    "default": false,
    "description": "Fix mode: Check if fixing is needed without fixing"
  },
  "process_all": {
    "type": "bool",
    "default": false,
    "description": "Fix mode: Process all files even if unchanged"
  },
  "composite_fingerprint": {
    "type": "bool",
    "default": false,
    "description": "Hash mode: Generate composite fingerprint"
  },
  "md5": {
    "type": "bool",
    "default": true,
    "description": "Hash mode: Generate MD5 fingerprints"
  },
  "sha1": {
    "type": "bool",
    "default": false,
    "description": "Hash mode: Generate SHA1 fingerprints"
  },
  "count_start": {
    "type": "number",
    "default": 1,
    "description": "Split mode: Number to start counting from for output files"
  },
  "count_format": {
    "type": "string",
    "default": "%02d",
    "description": "Split mode: Output file count format string"
  },
  "split_points_file": {
    "type": "string",
    "default": "",
    "description": "Split mode: File containing split points"
  },
  "split_interval": {
    "type": "string",
    "default": "",
    "description": "Split mode: Interval to split file (e.g., '1:00')"
  },
  "cue_name_manipulation": {
    "type": "string",
    "default": "",
    "description": "Split mode: Character manipulation string for CUE filenames"
  },
  "cue_filename_format": {
    "type": "string",
    "default": "",
    "description": "Split mode: Filename format based on CUE fields (e.g., '%p - %t')"
  },
  "lead_in": {
    "type": "string",
    "default": "",
    "description": "Split mode: Lead-in time taken from previous track"
  },
  "lead_out": {
    "type": "string",
    "default": "",
    "description": "Split mode: Lead-out time taken from next track"
  },
  "extract_tracks": {
    "type": "string",
    "default": "",
    "description": "Split mode: Comma-separated list of tracks to extract (e.g., '1-3,5')"
  },
  "read_from_terminal": {
    "type": "bool",
    "default": false,
    "description": "Conv mode: Read WAVE data from the terminal"
  },
  "generate_length": {
    "type": "string",
    "default": "0:00",
    "description": "Gen mode: Length of silence to generate"
  },
  "trim_beginning": {
    "type": "bool",
    "default": false,
    "description": "Trim mode: Only trim silence from beginning"
  },
  "trim_ending": {
    "type": "bool",
    "default": false,
    "description": "Trim mode: Only trim silence from end"
  },
  "keep_extra_chunks": {
    "type": "bool",
    "default": false,
    "description": "Strip mode: Do not strip extra RIFF chunks"
  },
  "keep_non_canonical_headers": {
    "type": "bool",
    "default": false,
    "description": "Strip mode: Do not make WAVE headers canonical"
  },
  "byte_shift_buffer_secs": {
    "type": "number",
    "default": 3,
    "description": "Cmp mode: Seconds of audio for byte-shift buffer"
  },
  "byte_shift_fuzz": {
    "type": "number",
    "default": 8,
    "description": "Cmp mode: Fuzz factor for byte-shift comparison"
  },
  "check_byte_shift": {
    "type": "bool",
    "default": false,
    "description": "Cmp mode: Check for byte-shifted data"
  },
  "list_differences": {
    "type": "bool",
    "default": false,
    "description": "Cmp mode: List differing bytes"
  },
  "output_simple_cue": {
    "type": "bool",
    "default": true,
    "description": "Cue mode: Output simple CUE sheet"
  },
  "output_split_points": {
    "type": "bool",
    "default": false,
    "description": "Cue mode: Output split points in byte-offset format"
  },
  "suppress_extra_chunks": {
    "type": "bool",
    "default": false,
    "description": "Cat mode: Suppress extra RIFF chunks"
  },
  "suppress_data": {
    "type": "bool",
    "default": false,
    "description": "Cat mode: Suppress WAVE data"
  },
  "suppress_header": {
    "type": "bool",
    "default": false,
    "description": "Cat mode: Suppress WAVE header"
  },
  "suppress_null_pad": {
    "type": "bool",
    "default": false,
    "description": "Cat mode: Suppress NULL pad byte"
  }
}
