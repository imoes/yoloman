{
  "name": {
    "type": "string",
    "default": "ifrename",
    "description": "The service name used for logging and usage messages."
  },
  "ifrename_binary": {
    "type": "string",
    "default": "/usr/sbin/ifrename",
    "description": "Path to the ifrename binary executable."
  },
  "iftab_path": {
    "type": "string",
    "default": "/etc/iftab",
    "description": "Path to the iftab configuration file containing interface renaming rules."
  }
}
