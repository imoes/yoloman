{
  "DROPBEAR_PORT": {
    "type": "number",
    "default": 22,
    "description": "The TCP port that Dropbear listens on."
  },
  "DROPBEAR_RECEIVE_WINDOW": {
    "type": "number",
    "default": 65536,
    "description": "Receive window size \u2014 a tradeoff between memory usage and network performance."
  },
  "DROPBEAR_EXTRA_ARGS": {
    "type": "string",
    "default": "",
    "description": "Additional arguments for Dropbear (e.g., banner file, custom host keys)."
  }
}
