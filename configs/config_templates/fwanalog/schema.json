{
  "cron_file": {
    "type": "string",
    "default": "fwanalog",
    "description": "Filename (without path) of the cron.d entry"
  },
  "service_name": {
    "type": "string",
    "default": "fwanalog",
    "description": "Human-readable name of the service"
  },
  "analysis_interval": {
    "type": "string",
    "default": "daily",
    "description": "Frequency of the analysis"
  },
  "log_email_type": {
    "type": "string",
    "default": "log-email",
    "description": "Purpose of the cron job (e.g., log-email, analysis)",
    "enum": [
      "log-email",
      "analysis"
    ],
    "enum_open": true
  },
  "minute": {
    "type": "string",
    "default": "59",
    "description": "Minute of the hour when the cron job runs"
  },
  "hour": {
    "type": "string",
    "default": "23",
    "description": "Hour of the day when the cron job runs"
  },
  "cron_script_path": {
    "type": "string",
    "default": "/usr/lib/fwanalog/cron",
    "description": "Absolute path to the cron script"
  }
}
