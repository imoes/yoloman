{# biff(1) session mail notification control #}
{# Schema: notify_enabled (bool, default=False) #}
{# Enable ('y') or disable ('n') mail notification for the current terminal session per biff(1) #}
{% if notify_enabled | default(False) | bool %}
biff y
{% else %}
biff n
{% endif %}