{
  "resources": {
    "type": "list",
    "default": [],
    "description": "List of resource definitions to embed. Each item must include type, symbol, and path fields matching elfrc requirements. Required for non-interactive use. Empty list results in no resources processed (no output generated unless -o/-h are specified via CLI, but this config only handles the resource list)."
  }
}
