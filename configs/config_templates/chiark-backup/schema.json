{
  "lvm_vg": {
    "type": "string",
    "default": "",
    "description": "Name of the LVM volume group to use for backups"
  },
  "lvm_lv": {
    "type": "string",
    "default": "chiark_backup",
    "description": "Name of the LVM logical volume to use for backups"
  },
  "lvm_lvsize_opts": {
    "type": "string",
    "default": "",
    "description": "Options for specifying the logical volume size"
  },
  "lvm_lvtools_opts": {
    "type": "string",
    "default": "-A n",
    "description": "Options passed to lvm tools (e.g., lvcreate)"
  },
  "lvm_lvcreate_opts": {
    "type": "string",
    "default": "",
    "description": "Additional options for the lvcreate command"
  },
  "lvm_lvcreate_args": {
    "type": "string",
    "default": "",
    "description": "Additional arguments for the lvcreate command"
  }
}
