{
  "uml_switch_start": {
    "type": "string",
    "default": "false",
    "description": "Set to 'true' to enable uml_switch to start with SysV scripts, or 'false' to prevent it."
  },
  "uml_switch_options": {
    "type": "string",
    "default": "",
    "description": "Options to pass to uml_switch, e.g., tap setup configurations."
  },
  "uml_switch_user": {
    "type": "string",
    "default": "uml-net",
    "description": "User account under which uml_switch runs."
  },
  "uml_switch_ctl": {
    "type": "string",
    "default": "/var/run/uml-utilities/uml_switch.ctl",
    "description": "Path to the control socket file for uml_switch."
  }
}
