{
  "XDG_DATA_HOME": {
    "type": "string",
    "default": "$HOME/.local/share",
    "description": "User-specific data directory, typically used by Flatpak for user installations."
  },
  "XDG_DATA_DIRS": {
    "type": "string",
    "default": "/usr/local/share:/usr/share",
    "description": "Fallback system-wide data directories. Flatpak appends its installation export paths to this list."
  }
}
