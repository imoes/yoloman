{
  "default_command": {
    "type": "string",
    "default": "sh",
    "description": "The primary toybox subcommand to execute when the wrapper is invoked without arguments."
  },
  "extra_args": {
    "type": "list",
    "default": [],
    "description": "A list of additional arguments to pass to the toybox binary globally. Common values might include '--no-color' or specific environment settings supported by toybox utilities."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, enables verbose output by running --help or logging before execution."
  }
}
