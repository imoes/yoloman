{
  "display": {
    "type": "string",
    "default": "localhost:0.0",
    "description": "Sets the display where wmsysmon will appear. X11 display name format."
  },
  "geometry": {
    "type": "string",
    "default": "+0+0",
    "description": "Set the position for wmsysmon using X11 geometry specification."
  },
  "update_rate": {
    "type": "number",
    "default": 1000,
    "description": "Sets the update rate for wmsysmon in milliseconds."
  },
  "blinky_lights": {
    "type": "bool",
    "default": false,
    "description": "Set blinky lights on for interrupts visualization."
  }
}
