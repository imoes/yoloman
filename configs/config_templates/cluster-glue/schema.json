{
  "debugfile": {
    "type": "string",
    "default": "/var/log/ha-debug",
    "description": "Path to the debug log file"
  },
  "logfile": {
    "type": "string",
    "default": "/var/log/ha-log",
    "description": "Path to the main log file"
  },
  "logmode": {
    "type": "string",
    "default": "0644",
    "description": "Octal file permissions for log files"
  },
  "logfacility": {
    "type": "string",
    "default": "daemon",
    "description": "Syslog facility to use (e.g., daemon, local0); 'none' disables syslog",
    "enum": [
      "daemon",
      "daemon",
      "syslog",
      "none"
    ]
  },
  "entity": {
    "type": "string",
    "default": "logd",
    "description": "Entity name shown at start of messages for log files"
  },
  "syslogprefix": {
    "type": "string",
    "default": "linux-ha",
    "description": "Prefix added to syslog messages for filtering; empty/disabled by default"
  },
  "useapphbd": {
    "type": "string",
    "default": "no",
    "description": "Whether to register with apphbd; 'yes' or 'no'"
  },
  "sendqlen": {
    "type": "string",
    "default": "256",
    "description": "Send queue length from parent to child process"
  },
  "recvqlen": {
    "type": "string",
    "default": "256",
    "description": "Receive queue length in child process"
  }
}
