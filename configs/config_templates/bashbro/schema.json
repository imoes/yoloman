{
  "port": {
    "type": "number",
    "default": 8080,
    "description": "TCP port for the bashbro HTTP server"
  },
  "jail_root": {
    "type": "string",
    "default": "",
    "description": "Absolute path to jail root directory (empty string means no jail)"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging"
  }
}
