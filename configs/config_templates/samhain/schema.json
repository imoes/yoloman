{
  "prefix": {
    "type": "string",
    "default": "/usr",
    "description": "The installation prefix directory for Samhain binaries and libraries."
  },
  "name": {
    "type": "string",
    "default": "samhain",
    "description": "The name of the service/daemon used for PID files and log messages."
  },
  "database_path": {
    "type": "string",
    "default": "/var/lib/samhain",
    "description": "The directory where the Samhain integrity database is stored."
  }
}
