# Defaults for yaku-ns initscript
# sourced by /etc/init.d/yaku-ns
# installed at /etc/default/yaku-ns by the maintainer scripts
#
# This is a POSIX shell fragment.
#
# Configuration variables for the yaku-ns daemon.
# This file is read by the init script to pass arguments to the daemon.

# Additional options that are passed to the Daemon.
# Controls how the yaku-ns daemon is started with extra CLI flags.
DAEMON_OPTS="{{ daemon_opts | default('') }}"