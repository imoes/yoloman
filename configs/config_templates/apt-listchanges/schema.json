{
  "apt_listchanges_pre_install_cmd": {
    "type": "string",
    "default": "/usr/bin/apt-listchanges --apt || test $? -lt 10",
    "description": "Command to run before installing packages, typically the apt-listchanges invocation."
  },
  "apt_listchanges_version": {
    "type": "string",
    "default": "2",
    "description": "Version of apt-listchanges output format to use."
  },
  "apt_listchanges_info_fd": {
    "type": "string",
    "default": "20",
    "description": "File descriptor number for info output from apt-listchanges."
  },
  "apt_listchanges_etc_main": {
    "type": "string",
    "default": "listchanges.conf",
    "description": "Filename for the main apt-listchanges configuration file relative to DPkg::Etc."
  },
  "apt_listchanges_etc_parts": {
    "type": "string",
    "default": "listchanges.conf.d",
    "description": "Directory name for additional apt-listchanges configuration snippets relative to DPkg::Etc."
  }
}
