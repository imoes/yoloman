{
  "proftpd_log_file": {
    "type": "string",
    "default": "/var/log/proftpd/proftpd.log",
    "description": "Path to the main ProFTPD log file."
  },
  "proftpd_controls_log_file": {
    "type": "string",
    "default": "/var/log/proftpd/controls.log",
    "description": "Path to the ProFTPD control socket log file."
  },
  "proftpd_log_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "Number of rotated log files to keep."
  },
  "proftpd_log_create_mode": {
    "type": "string",
    "default": "640",
    "description": "Permission mode for new log files."
  },
  "proftpd_log_create_owner": {
    "type": "string",
    "default": "root",
    "description": "Owner for new log files."
  },
  "proftpd_log_create_group": {
    "type": "string",
    "default": "adm",
    "description": "Group for new log files."
  },
  "proftpd_xferlog_file": {
    "type": "string",
    "default": "/var/log/proftpd/xferlog",
    "description": "Path to the FTP transfer log file."
  },
  "proftpd_xferreport_file": {
    "type": "string",
    "default": "/var/log/proftpd/xferreport",
    "description": "Path to the FTP transfer report file."
  },
  "proftpd_xferlog_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "Number of rotated xferlog files to keep."
  },
  "proftpd_xferlog_create_mode": {
    "type": "string",
    "default": "640",
    "description": "Permission mode for new xferlog files."
  },
  "proftpd_xferlog_create_owner": {
    "type": "string",
    "default": "root",
    "description": "Owner for new xferlog files."
  },
  "proftpd_xferlog_create_group": {
    "type": "string",
    "default": "adm",
    "description": "Group for new xferlog files."
  },
  "proftpd_xferstats_interval": {
    "type": "number",
    "default": 2,
    "description": "Interval for ftpstats (hours)."
  }
}
