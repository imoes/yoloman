{
  "http_proxy": {"type": "string", "default": "", "description": "HTTP proxy URL for APT downloads (e.g. http://proxy.example.com:80). Empty = no proxy."},
  "https_proxy": {"type": "string", "default": "", "description": "HTTPS proxy URL for APT. Empty = no proxy."},
  "no_proxy": {"type": "list", "default": [], "description": "Hosts/domains fetched DIRECT, bypassing the proxy.", "items": {"type": "string", "default": ""}}
}
