{
 "enable_misc": {
  "default": false,
  "description": "Enable indirect mounting under /misc using /etc/auto.misc (commonly used for CDROM, floppies, etc.)",
  "type": "bool"
 },
 "mounts": {
  "default": [],
  "description": "List of direct mount entries. Each entry defines where to mount, which map to use, and optional mount options.",
  "type": "list"
 },
 "options": {
  "description": "Additional command-line options passed to automount (e.g., '--verbose')",
  "enum": [
   "--timeout",
   "--pid-file",
   "--force",
   "--start",
   "--stop",
   "--signal",
   "--oknodo",
   "--quiet"
  ],
  "type": "string"
 }
}
