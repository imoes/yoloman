{
  "uwsgi_port": {
    "type": "number",
    "default": 8778,
    "description": "The port number the uwsgi placement-api service binds to."
  },
  "system_user": {
    "type": "string",
    "default": "placement",
    "description": "The system user under which the placement-api service runs."
  },
  "system_group": {
    "type": "string",
    "default": "placement",
    "description": "The system group under which the placement-api service runs."
  }
}
