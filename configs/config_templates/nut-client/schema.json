{
  "pid_dir": {
    "type": "string",
    "default": "/run/nut",
    "description": "Directory for storing PID files."
  },
  "upsmon_path": {
    "type": "string",
    "default": "/sbin/upsmon",
    "description": "Path to the upsmon binary."
  }
}
