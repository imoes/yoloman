{
  "client_name": {
    "type": "string",
    "default": "zita-mu1",
    "description": "Name to use as the Jack client identifier. Maps to the -name option."
  },
  "jack_server": {
    "type": "string",
    "default": "system",
    "description": "The name of the Jack server to connect to. Maps to the -s option."
  },
  "geometry": {
    "type": "string",
    "default": "800x600+0+0",
    "description": "Window position and size in the format 'WxH+X+Y'. Maps to the -g option."
  }
}
