{
  "debug_level": {
    "type": "number",
    "default": 0,
    "description": "Verbosity of debug output (0-9). 0 is quiet, 9 is verbose."
  },
  "debug_flags": {
    "type": "string",
    "default": "none",
    "description": "Comma-separated list of debug categories (agent, messenger, imcontext, scim).",
    "enum": [
      "all",
      "none",
      "agent",
      "messenger",
      "imcontext",
      "scim"
    ]
  },
  "standalone": {
    "type": "bool",
    "default": true,
    "description": "If true, prevents the agent from daemonizing itself."
  },
  "noexit": {
    "type": "bool",
    "default": false,
    "description": "If true, prevents the agent from exiting when there are no clients."
  }
}
