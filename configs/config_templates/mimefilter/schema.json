{
  "listreq_email": {
    "type": "string",
    "default": "admin@example.com",
    "description": "The email address for the list administrator, used in the $::from header."
  },
  "description_title": {
    "type": "string",
    "default": "AUTOMATIC WARNING",
    "description": "The title/description used for MIME part and message replacements when they are stripped."
  }
}
