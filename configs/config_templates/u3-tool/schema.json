{
  "device": {
    "type": "string",
    "default": "/dev/sdb",
    "description": "Path to the U3 USB flash disk device file"
  },
  "cd_image": {
    "type": "string",
    "default": null,
    "description": "Path to the CD image file to load onto the device"
  },
  "cd_size": {
    "type": "number",
    "default": null,
    "description": "Size of the CD partition in bytes for repartitioning"
  },
  "action": {
    "type": "string",
    "default": "info",
    "description": "The primary action to perform on the device (info, dump, enable, disable, reset, unlock, load, repartition)",
    "enum": [
      "info",
      "dump",
      "enable",
      "disable",
      "reset",
      "unlock",
      "load",
      "repartition"
    ]
  },
  "password": {
    "type": "string",
    "default": null,
    "description": "Current password for the device, required for security operations"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output during execution"
  }
}
