{
  "zip_compression_level": {
    "type": "number",
    "default": 6,
    "description": "Compression level from 0 (store) to 9 (best compression). Default is 6."
  },
  "zip_output_path": {
    "type": "string",
    "default": "archive.zip",
    "description": "Path to the resulting zip archive file."
  },
  "zip_compression_method": {
    "type": "string",
    "default": "deflate",
    "description": "Compression method: 'deflate' (default), 'store', or 'bzip2'.",
    "enum": [
      "deflate",
      "store",
      "bzip2"
    ]
  },
  "zip_verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose mode for detailed output during archiving."
  },
  "zip_sources": {
    "type": "list",
    "default": [],
    "description": "List of file or directory paths to include in the archive."
  }
}
