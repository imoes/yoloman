{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose mode to show progress and paging statistics."
  },
  "target_mode": {
    "type": "string",
    "default": "none",
    "description": "Target selection mode: 'all' to page in all processes, 'pid' to page in a specific process, 'none' to disable automatic execution."
  },
  "target_pid": {
    "type": "number",
    "default": 0,
    "description": "Process ID to target for paging in. Required if target_mode is 'pid'."
  }
}
