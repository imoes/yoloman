{# Access Control List for MIT Kerberos Admin Server (kadmind) #}
{# Format: principal permissions [target_principal] [instance] #}
{# Each line grants specific permissions to a principal for administration #}
{% for entry in acl_entries %}
{% if entry.comment %}
{{ entry.comment }}
{% endif %}
{{ entry.principal }}	{{ entry.permissions }}{% if entry.target_principal %} {{ entry.target_principal }}{% endif %}{% if entry.instance %} {{ entry.instance }}{% endif %}
{% endfor %}