{
  "default_term_type": {
    "type": "string",
    "default": "vt100",
    "description": "The terminal type string to send via the TERMTYPE option (e.g., vt100, ansi).",
    "enum": [
      "vt100",
      "vt52",
      "ansi",
      "vtnt"
    ]
  },
  "hosts": {
    "type": "list",
    "default": [],
    "description": "A list of host configurations to apply to the telnetrc file."
  }
}
