{
  "control_server_port": {
    "type": "number",
    "default": 0,
    "description": "Port number to run the GUI control server. Set to 0 to disable."
  },
  "peer_listen_port": {
    "type": "number",
    "default": 0,
    "description": "Port number to listen for incoming BitTorrent peer connections. Set to 0 to disable."
  },
  "seed_mode": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable seed-mode (keep running and seed after download completes)."
  },
  "trace_file": {
    "type": "string",
    "default": "",
    "description": "Path to the file where trace execution output should be written."
  }
}
