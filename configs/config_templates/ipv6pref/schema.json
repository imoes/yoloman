{
  "preferred_address_type": {
    "type": "string",
    "default": "public",
    "description": "The IPv6 address preference to use. 'public' corresponds to v6pub (stable public address), 'temporary' corresponds to v6tmp (privacy extension temporary address).",
    "enum": [
      "public",
      "temporary"
    ]
  }
}
