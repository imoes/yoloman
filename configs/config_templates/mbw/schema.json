{
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "If true, suppresses informational messages (-q)."
  },
  "suppress_average": {
    "type": "bool",
    "default": false,
    "description": "If true, suppresses printing the average of each test (-a)."
  },
  "loops": {
    "type": "number",
    "default": 100,
    "description": "Number of loops per test (-n)."
  },
  "tests": {
    "type": "list",
    "default": "all",
    "description": "Tests to be run. Use 'all' for all tests, or a list of integers (0 for memcpy, 1 for dumb copy, 2 for memcpy with arbitrary block size) (-t)."
  },
  "block_size": {
    "type": "number",
    "default": null,
    "description": "Block size in bytes for test type 2 (-b)."
  },
  "array_size_mib": {
    "type": "number",
    "default": 64,
    "description": "Array size in MiB for the benchmark (positional argument)."
  }
}
