{
  "enabled_default": {
    "type": "number",
    "default": 0,
    "description": "Whether pcapdump is enabled by default (0 or 1). Not used on systemd systems."
  },
  "instance_names": {
    "type": "list",
    "default": [
      "eth0"
    ],
    "description": "List of network interfaces for pcapdump instances."
  }
}
