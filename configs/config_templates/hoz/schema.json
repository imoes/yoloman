{
  "input_file": {
    "type": "string",
    "default": "/var/lib/hoz/data.iso",
    "description": "Path to the input file to be split or merged."
  },
  "output_dir": {
    "type": "string",
    "default": "/var/lib/hoz/output",
    "description": "Directory where pieces are stored (for cut) or where the merged file is written (for paste)."
  },
  "cut_size": {
    "type": "string",
    "default": "10M",
    "description": "Size of each piece when cutting, e.g., 10M, 500K."
  },
  "operation": {
    "type": "string",
    "default": "cut",
    "description": "Operation to perform: 'cut' to split, 'paste' to merge.",
    "enum": [
      "cut",
      "paste"
    ]
  }
}
