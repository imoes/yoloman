{
  "devicescan_enabled": {
    "type": "boolean",
    "default": true,
    "description": "If true, use DEVICESCAN; otherwise, explicitly list devices below."
  },
  "devicescan_directives": {
    "type": "list",
    "default": [
      "-d",
      "removable",
      "-n",
      "standby",
      "-m",
      "root",
      "-M",
      "exec",
      "/usr/share/smartmontools/smartd-runner"
    ],
    "description": "List of directives to apply when DEVICESCAN is enabled."
  },
  "monitored_devices": {
    "type": "list",
    "default": [],
    "description": "List of explicitly monitored devices (ignored if devicescan_enabled is true)."
  }
}