{
 "autochanger": {
  "default": null,
  "description": "Autochanger/robotic library definition (rare — only if FD also acts as Storage Daemon).",
  "items": {
   "changer_command": {
    "default": "",
    "description": "Command/script used to control the autochanger mechanism.",
    "type": "string"
   },
   "changer_device": {
    "default": "",
    "description": "Path to the autochanger device (e.g., /dev/sg1).",
    "type": "string"
   },
   "devices": {
    "items": {
     "name": {
      "default": "",
      "description": "Name of the device referenced.",
      "type": "string"
     }
    },
    "type": "list"
   },
   "name": {
    "default": "",
    "description": "Name of the autochanger resource.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "catalog": {
  "default": null,
  "description": "Optional catalog configuration for metadata lookup during restores.",
  "items": {
   "db_address": {
    "default": "localhost",
    "description": "Database host address.",
    "type": "string"
   },
   "db_name": {
    "default": "bacula",
    "description": "Name of the Bacula database.",
    "type": "string"
   },
   "db_password": {
    "default": "",
    "description": "Database password for Bacula user.",
    "type": "string"
   },
   "db_port": {
    "default": 5432,
    "description": "Database port (e.g., 5432 for PostgreSQL).",
    "type": "number"
   },
   "db_user": {
    "default": "bacula",
    "description": "Database user for Bacula access.",
    "type": "string"
   },
   "name": {
    "default": "MyCatalog",
    "description": "Name of the catalog resource.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "directors": {
  "default": [
   {
    "address": "127.0.0.1",
    "fd_port": 9101,
    "name": "Director",
    "password": "director-password",
    "tls_enable": true
   }
  ],
  "description": "List of Directors authorized to connect to this File Daemon. Each entry must match the Director's configuration.",
  "items": {
   "address": {
    "default": "127.0.0.1",
    "description": "IP address or hostname of the Director.",
    "type": "string"
   },
   "fd_port": {
    "default": 9101,
    "description": "Port the Director uses to connect to this File Daemon (typically 9101).",
    "type": "number"
   },
   "name": {
    "default": "Director",
    "description": "Name of the Director (must match Director resource name).",
    "type": "string"
   },
   "password": {
    "default": "director-password",
    "description": "Shared secret password used for Director authentication.",
    "type": "string"
   },
   "tls_ca_cert": {
    "default": "",
    "description": "Path to CA certificate for TLS (optional; leave empty if not using TLS auth).",
    "type": "string"
   },
   "tls_cert": {
    "default": "",
    "description": "Path to this daemon's TLS certificate (optional).",
    "type": "string"
   },
   "tls_enable": {
    "default": true,
    "description": "Whether to require TLS encryption for this Director connection.",
    "type": "bool"
   },
   "tls_key": {
    "default": "",
    "description": "Path to this daemon's TLS private key (optional).",
    "type": "string"
   }
  },
  "type": "list"
 },
 "fd_addresses": {
  "default": [
   "127.0.0.1"
  ],
  "description": "List of addresses the FD binds to. Usually 127.0.0.1 (localhost) for security.",
  "type": "list"
 },
 "fd_name": {
  "default": "FileDaemon",
  "description": "Unique name of this File Daemon — must match what the Director expects.",
  "type": "string"
 },
 "fd_port": {
  "default": 9102,
  "description": "TCP port on which the File Daemon listens for Director connections.",
  "type": "number"
 },
 "log_file": {
  "default": "/var/log/bacula/bacula-fd.log",
  "description": "Path to the File Daemon's main log file.",
  "type": "string"
 },
 "max_concurrent_jobs": {
  "default": 5,
  "description": "Maximum number of backup/restore jobs this daemon can run simultaneously.",
  "type": "number"
 },
 "messages": {
  "default": {
   "Standard": {
    "level": "all, !skipped, !saved",
    "target": "127.0.0.1 9103",
    "type": "daemon"
   }
  },
  "description": "Message destinations (console, mail, append, catalog). Each key is a messages block name.",
  "items": {
   "level": {
    "default": "",
    "description": "Message severity level filter (e.g., 'all, !skipped').",
    "type": "string"
   },
   "target": {
    "default": "",
    "description": "Target for the message (e.g., address:port, file path, or email).",
    "type": "string"
   },
   "type": {
    "description": "Type of message destination.",
    "enum": [
     "daemon",
     "console",
     "mail",
     "append",
     "catalog"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "pid_directory": {
  "default": "/run/bacula",
  "description": "Directory where the FD writes its process ID file.",
  "type": "string"
 },
 "plugins": {
  "default": [],
  "description": "List of plugins to load for enhanced backup functionality (e.g., VSS, LVM, etc.).",
  "items": {
   "name": {
    "default": "",
    "description": "Plugin name (must be unique).",
    "type": "string"
   },
   "options": {
    "default": {},
    "description": "Key-value configuration options for the plugin.",
    "type": "object"
   },
   "path": {
    "default": "",
    "description": "Path to the shared library (.so) for the plugin.",
    "type": "string"
   }
  },
  "type": "list"
 },
 "tls_ca_cert_file": {
  "default": "/etc/bacula/ssl/certs/bacula-ca.crt",
  "description": "Path to CA certificate for TLS verification.",
  "type": "string"
 },
 "tls_cert_file": {
  "default": "/etc/bacula/ssl/certs/bacula-fd.crt",
  "description": "Path to File Daemon's TLS certificate.",
  "type": "string"
 },
 "tls_enabled": {
  "default": true,
  "description": "Enable TLS globally for all FD connections (overrides per-director if true).",
  "type": "bool"
 },
 "tls_key_file": {
  "default": "/etc/bacula/ssl/private/bacula-fd.key",
  "description": "Path to File Daemon's TLS private key.",
  "type": "string"
 },
 "working_directory": {
  "default": "/var/lib/bacula",
  "description": "Working directory for temporary files, spooling, and working storage.",
  "type": "string"
 }
}
