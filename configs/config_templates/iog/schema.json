{
  "cron_schedule": {
    "type": "string",
    "default": "0 *  * * *",
    "description": "Cron schedule for the iog job"
  },
  "cron_user": {
    "type": "string",
    "default": "www-data",
    "description": "User to run the iog cron job as"
  }
}
