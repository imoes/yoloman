{
  "START_DAEMON": {
    "type": "string",
    "default": "no",
    "description": "Set to 'yes' to start fwknopd at boot time.",
    "enum": [
      "yes",
      "no"
    ]
  },
  "DAEMON_ARGS": {
    "type": "string",
    "default": "",
    "description": "Additional arguments to pass to fwknopd on startup (e.g., --override-config path)."
  }
}
