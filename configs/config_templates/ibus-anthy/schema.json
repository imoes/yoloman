{
  "initial_setup_name_suffix": {
    "type": "string",
    "default": "GNOME Initial Setup",
    "description": "Suffix appended to the desktop entry name and script filename for the initial setup wizard."
  },
  "autostart_token": {
    "type": "string",
    "default": "gnome-initial-setup-done",
    "description": "Token used in the AutostartCondition to check if the initial setup has already been performed."
  }
}
