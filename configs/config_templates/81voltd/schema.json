{
  "service_user": {
    "type": "string",
    "default": {
      "template": "81voltd"
    },
    "description": "System user under which the service runs"
  },
  "service_group": {
    "type": "string",
    "default": {
      "template": "81voltd"
    },
    "description": "System group under which the service runs"
  },
  "working_directory": {
    "type": "string",
    "default": {
      "template": "/var/lib/81voltd"
    },
    "description": "Working directory for the service process"
  },
  "executable": {
    "type": "string",
    "default": {
      "template": "/usr/bin/81voltd"
    },
    "description": "Absolute path to the service executable"
  },
  "args": {
    "type": "list",
    "default": {},
    "description": "List of command-line arguments for the executable"
  },
  "environment": {
    "type": "list",
    "default": {},
    "description": "List of environment variables in 'KEY=VALUE' format"
  },
  "restart_policy": {
    "type": "string",
    "default": {
      "template": "on-failure"
    },
    "description": "Restart policy: 'no', 'on-failure', or 'always'"
  },
  "restart_retries": {
    "type": "number",
    "default": {
      "template": 3
    },
    "description": "Number of restart attempts before giving up (only used with 'on-failure' policy)"
  },
  "log_directory": {
    "type": "string",
    "default": {
      "template": "/var/log/81voltd"
    },
    "description": "Directory where service logs are written"
  },
  "log_file_pattern": {
    "type": "string",
    "default": {
      "template": "81voltd.log"
    },
    "description": "Pattern for log filename, e.g., 'service-%Y%m%d.log'"
  },
  "readiness_probe": {
    "type": "string",
    "default": {},
    "description": "Command to check if service is ready, e.g., '/usr/bin/81voltd health-check-ready'"
  },
  "liveness_probe": {
    "type": "string",
    "default": {},
    "description": "Command to check if service is alive, e.g., '/usr/bin/81voltd health-check-alive'"
  },
  "port": {
    "type": "number|list",
    "default": {
      "template": 8181
    },
    "description": "Port number (int) or list of port numbers the service binds to"
  },
  "memory_limit": {
    "type": "string",
    "default": {
      "template": "512M"
    },
    "description": "Memory limit in human-readable form (e.g., '512M', '1G')"
  },
  "cpu_limit": {
    "type": "string",
    "default": {
      "template": "1.0"
    },
    "description": "CPU limit as a float or percentage (e.g., '0.5', '50%')"
  },
  "dependencies": {
    "type": "list",
    "default": {},
    "description": "List of service names this service depends on"
  },
  "mounts": {
    "type": "list",
    "default": {},
    "description": "List of bind mounts, each with source, destination, and optional options (ro/rw,etc.)"
  },
  "health_check_interval": {
    "type": "number",
    "default": {
      "template": 30
    },
    "description": "Interval (in seconds) between health checks"
  }
}
