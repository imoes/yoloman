{
  "state_directory": {
    "type": "string",
    "default": "/var/lib/iwd",
    "description": "Directory for persistent state (e.g., known networks, credentials)."
  },
  "configuration_directory": {
    "type": "string",
    "default": "/etc/iwd",
    "description": "Directory for configuration files (e.g., main.conf, *.netdev, *.network)."
  },
  "daemon_path": {
    "type": "string",
    "default": "/usr/libexec/iwd",
    "description": "Full path to the iwd daemon binary."
  },
  "pidfile_path": {
    "type": "string",
    "default": "/var/run/iwd-sysd2v.pid",
    "description": "Path to the PID file used during service startup."
  },
  "start_args": {
    "type": "string",
    "default": "--background --make-pidfile",
    "description": "Arguments passed to the daemon at startup."
  }
}
