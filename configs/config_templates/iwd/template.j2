#!/bin/sh
# Generated by sysd2v v0.3  --  http://www.trek.eu.org/devel/sysd2v
# kFreeBSD do not accept scripts as interpreters, using #!/bin/sh and sourcing.
if [ true != "$INIT_D_SCRIPT_SOURCED" ] ; then
    set "$0" "$@"; INIT_D_SCRIPT_SOURCED=true . /lib/init/init-d-script
fi
### BEGIN INIT INFO
# Provides:       iwd
# Required-Start: $remote_fs
# Required-Stop:  $remote_fs
# Should-Start:   $network
# Should-Stop:    $network
# Default-Start:  2 3 4 5
# Default-Stop:   0 1 6
# Description:    Wireless service
### END INIT INFO
set -a

# Directory for persistent state (e.g., known networks, credentials).
# Defaults to: /var/lib/iwd
STATE_DIRECTORY={{ state_directory | default('/var/lib/iwd') }}

# Directory for configuration files (e.g., main.conf, *.netdev, *.network).
# Defaults to: /etc/iwd
CONFIGURATION_DIRECTORY={{ configuration_directory | default('/etc/iwd') }}

set +a

# Full path to the iwd daemon binary.
# Defaults to: /usr/libexec/iwd
DAEMON={{ daemon_path | default('/usr/libexec/iwd') }}

# Path to the PID file used during service startup.
# Defaults to: /var/run/iwd-sysd2v.pid
PIDFILE={{ pidfile_path | default('/var/run/iwd-sysd2v.pid') }}

# Arguments passed to the daemon at startup.
# Defaults to: --background --make-pidfile
START_ARGS="{{ start_args | default('--background --make-pidfile') }}"

do_start_cmd_override ()
{
  install -d -m 0700 "$STATE_DIRECTORY" || return 2
  install -d "$CONFIGURATION_DIRECTORY" || return 2
  do_start_cmd || return $?
}