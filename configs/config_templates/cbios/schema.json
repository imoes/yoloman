{
  "cbios_variant": {
    "type": "string",
    "default": "MSX1",
    "description": "MSX hardware variant: MSX1, MSX2, or MSX2+",
    "enum": [
      "MSX1",
      "MSX2",
      "MSX2+"
    ]
  },
  "cbios_locale": {
    "type": "string",
    "default": "INTL",
    "description": "BIOS localization: INTL (International), JP (Japanese), or BR (Brazilian)"
  },
  "cbios_bios_path": {
    "type": "string",
    "default": "",
    "description": "Optional explicit path to BIOS ROM file; if omitted, defaults to package-installed ROM"
  }
}
