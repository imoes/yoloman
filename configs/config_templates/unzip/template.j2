# Default environment options for unzip
# un_zip_opts: List of command-line options for unzip. Options like -qq (quiet), -a (text conversion), -n (never overwrite) are common. See man page for valid options.
export UNZIP="{{ un_zip_opts | default(['-qq']) | join(' ') }}"