{
  "un_zip_opts": {
    "type": "list",
    "default": [
      "-qq"
    ],
    "description": "List of command-line options for unzip. Options like -qq (quiet), -a (text conversion), -n (never overwrite) are common. See man page for valid flags."
  }
}
