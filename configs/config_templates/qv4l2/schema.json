{
  "video_device": {
    "type": "string",
    "default": "/dev/video0",
    "description": "The video device node to use for capture or output. If a number, /dev/video<num> is used."
  },
  "vbi_device": {
    "type": "string",
    "default": "/dev/vbi0",
    "description": "The VBI device node to use."
  },
  "radio_device": {
    "type": "string",
    "default": "/dev/radio0",
    "description": "The radio device node to use."
  },
  "sdr_device": {
    "type": "string",
    "default": "/dev/swradio0",
    "description": "The SDR device node to use."
  },
  "raw_mode": {
    "type": "bool",
    "default": false,
    "description": "If true, opens the device in raw mode without using libv4l2 wrapper functions."
  }
}
