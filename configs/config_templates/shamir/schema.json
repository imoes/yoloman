{
  "shares": {
    "type": "number",
    "default": 5,
    "description": "Total number of shares to generate for the secret."
  },
  "threshold": {
    "type": "number",
    "default": 3,
    "description": "Minimum number of shares required to reconstruct the secret."
  },
  "format": {
    "type": "string",
    "default": "bip39",
    "description": "The word list format to use for mnemonic codes (e.g., bip39, custom)."
  },
  "language": {
    "type": "string",
    "default": "english",
    "description": "The language for the mnemonic word list."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Flag to enable verbose logging/output."
  }
}
