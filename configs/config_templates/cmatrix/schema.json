{
  "cli_args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments to pass to cmatrix. Each item is a bare string argument (e.g., ['-a', '-u', '2', '-C', 'red']). Order matters. Must match documented options exactly."
  }
}
