{
  "server_host": {
    "type": "string",
    "default": "localhost",
    "description": "The hostname or IP address of the ruptime server to report to."
  },
  "server_port": {
    "type": "number",
    "default": 4242,
    "description": "The port number of the ruptime server."
  },
  "hostname": {
    "type": "string",
    "default": "",
    "description": "The FQDN hostname to report. If empty, the system's local hostname is typically used."
  },
  "key": {
    "type": "string",
    "default": "changeme",
    "description": "The symmetric key for encrypting/decrypting ruptime data."
  }
}
