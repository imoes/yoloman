# /etc/protocols — Internet protocol assignments (IANA registered)
# Each line defines a protocol number, name, and optionally aliases and comments.
# Format: number name aliases # comment
{% for proto in protocols %}{{ proto.number }} {{ proto.name }} {{ proto.aliases | join(' ') }} # {{ proto.comment }}{% endfor %}