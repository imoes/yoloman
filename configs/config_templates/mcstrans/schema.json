{
  "desc": {
    "type": "string",
    "default": "SELinux Context Translation System",
    "description": "Description string for the daemon service."
  },
  "name": {
    "type": "string",
    "default": "mcstransd",
    "description": "Name of the daemon (used for PID file, process name, etc)."
  },
  "daemon_path": {
    "type": "string",
    "default": "/sbin/mcstransd",
    "description": "Absolute path to the mcstransd executable."
  },
  "daemon_args": {
    "type": "string",
    "default": "",
    "description": "Additional arguments passed to the daemon."
  },
  "script_path": {
    "type": "string",
    "default": "/etc/init.d/mcstrans",
    "description": "Path to the init script itself."
  }
}
