{
  "report_mode": {
    "type": "string",
    "default": "mc",
    "description": "Reporting mode: mc (MediaConch report), mi (MediaInfo report), or mt (MediaTrace report)"
  },
  "output_format": {
    "type": "string",
    "default": "text",
    "description": "Output format: text, xml, maxml, or html",
    "enum": [
      "text",
      "xml",
      "maxml",
      "html"
    ]
  },
  "policy_file": {
    "type": "string",
    "default": "",
    "description": "Path to the policy file (XSL or Schematron) to apply"
  },
  "display_file": {
    "type": "string",
    "default": "",
    "description": "Path to the display transformation file (XSL) to apply"
  },
  "input_files": {
    "type": "list",
    "default": [],
    "description": "List of media files to check/conformance check against"
  }
}
