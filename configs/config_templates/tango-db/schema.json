{
  "tango_port": {
    "type": "string",
    "default": "10000",
    "description": "The port number for the Tango database server."
  },
  "tango_host": {
    "type": "string",
    "default": "localhost",
    "description": "The hostname or IP address for the Tango database server."
  },
  "tango_daemon_user": {
    "type": "string",
    "default": "tango",
    "description": "The system user to run the Tango database daemon as."
  },
  "tango_daemon_group": {
    "type": "string",
    "default": "tango",
    "description": "The system group to run the Tango database daemon as."
  }
}
