{
  "config_file": {
    "type": "string",
    "default": "/etc/rtpengine/rtpengine.conf",
    "description": "Path to the rtpengine configuration file."
  },
  "pidfile": {
    "type": "string",
    "default": "/run/rtpengine/rtpengine-daemon.pid",
    "description": "Path to the PID file for the rtpengine daemon."
  },
  "manage_iptables": {
    "type": "bool",
    "default": true,
    "description": "Whether rtpengine should manage iptables rules automatically."
  },
  "set_user": {
    "type": "string",
    "default": "rtpengine",
    "description": "The user under which the rtpengine process should run."
  },
  "set_mask": {
    "type": "number",
    "default": 0,
    "description": "The umask value for the rtpengine process."
  }
}
