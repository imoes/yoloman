{
  "http_endpoint": {
    "type": "string",
    "default": ":3903",
    "description": "HTTP address and port to serve the metrics UI and health endpoint (e.g., `\":3903\"`)."
  },
  "host": {
    "type": "string",
    "default": "::",
    "description": "Address to bind to (e.g., \"::\" for all interfaces, \"127.0.0.1\" for local only)."
  },
  "extra_args": {
    "type": "string",
    "default": "",
    "description": "Extra command-line arguments to pass to the mtail server. Use this for advanced options not explicitly parametrized above."
  }
}
