{
  "retries": {
    "type": "number",
    "default": 60,
    "description": "How often srp_daemon should rescan the fabric in seconds."
  },
  "log_path": {
    "type": "string",
    "default": "/var/log/srp_daemon.log",
    "description": "Path where srp_daemon should log."
  },
  "ports": {
    "type": "string",
    "default": "NONE",
    "description": "Ports srp_daemon should be started on. Format is CA:port. Can be ALL, NONE, or a space-separated list of CA:port pairs.",
    "enum": [
      "NONE",
      "ALL"
    ]
  }
}
