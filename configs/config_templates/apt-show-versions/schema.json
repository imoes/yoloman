{
  "ex4systemd_timer": {
    "type": "string",
    "default": "$1",
    "description": "Shell positional parameter (typically $1) passed to the script; used to detect invocation by systemd timer vs cron."
  }
}
