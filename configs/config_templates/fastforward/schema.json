{
  "cdb_path": {
    "type": "string",
    "default": "/var/qmail/alias/.qmail-forward.cdb",
    "description": "Path to the fastforward CDB database file (typically created by setforward)"
  },
  "mode": {
    "type": "string",
    "default": "bounce_missing",
    "description": "Behavior when no forwarding rule is found. Values: 'pass_through', 'no_deliver', 'bounce_missing'"
  },
  "recipient_mode": {
    "type": "string",
    "default": "recipient",
    "description": "How the recipient address is determined. Values: 'default_host', 'recipient'"
  }
}
