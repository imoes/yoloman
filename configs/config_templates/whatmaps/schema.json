{
  "enable_restart": {
    "type": "string",
    "default": "0",
    "description": "Set to 1 or true to enable service restarts on security updates"
  },
  "distro_id": {
    "type": "string",
    "default": "${distro_id}",
    "description": "The distribution identifier variable"
  },
  "distro_codename": {
    "type": "string",
    "default": "${distro_codename}",
    "description": "The distribution codename variable"
  }
}
