{
  "logfile": {
    "type": "string",
    "default": "/var/log/railcontrol.log",
    "description": "Path to the log file where railcontrol writes its logs."
  },
  "daemonize": {
    "type": "bool",
    "default": true,
    "description": "Whether to run railcontrol as a daemon (background process)."
  },
  "silent": {
    "type": "bool",
    "default": true,
    "description": "Whether to suppress console output. Implied by daemonize."
  }
}
