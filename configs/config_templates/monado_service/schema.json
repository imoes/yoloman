{
  "runtime_path": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu/monado",
    "description": "Directory containing Monado XR runtime modules (e.g., 'openxr-monado-provider.so')."
  },
  "runtimes": {
    "type": "list",
    "default": [
      "openxr-monado-provider"
    ],
    "description": "List of XR runtime modules to load (by basename, without .so).",
    "items": {
      "type": "string",
      "description": "Runtime module name."
    }
  },
  "max_sessions": {
    "type": "number",
    "default": 3,
    "description": "Maximum number of concurrent XR sessions (clients) allowed."
  },
  "enable_composition": {
    "type": "bool",
    "default": true,
    "description": "Enable GPU-accelerated composition for rendered output (required for VR headsets)."
  },
  "composition_resolution": {
    "type": "string",
    "default": "auto",
    "description": "Composition resolution: either 'auto' (derived from headset resolution) or 'WxH' (e.g., '2160x2160')."
  },
  "composition_scale": {
    "type": "number",
    "default": 1.0,
    "description": "Composition scaling factor (0.1\u20131.0). Lower values improve performance at slight quality cost."
  },
  "enable_vulkan_composition": {
    "type": "bool",
    "default": true,
    "description": "Use Vulkan-based composition backend (requires compatible GPU/driver)."
  },
  "gpu_devices": {
    "type": "list",
    "default": [
      {
        "path": "/dev/dri/renderD128",
        "description": "Default discrete GPU"
      },
      {
        "path": "/dev/dri/card0",
        "description": "Integrated GPU fallback"
      }
    ],
    "description": "List of GPU device paths to use for composition/rendering (ordered by preference).",
    "items": {
      "type": "object",
      "fields": {
        "path": {
          "type": "string",
          "default": "/dev/dri/renderD128",
          "description": "Device node path (e.g., /dev/dri/renderD128)."
        },
        "description": {
          "type": "string",
          "default": "",
          "description": "Optional human-readable description (shown in config UI)."
        }
      }
    }
  },
  "log_level": {
    "type": "string",
    "default": "warn",
    "description": "Logging verbosity: none, error, warn, info, debug, trace.",
    "allowed_values": [
      "none",
      "error",
      "warn",
      "info",
      "debug",
      "trace"
    ]
  },
  "syslog": {
    "type": "bool",
    "default": true,
    "description": "Send log messages to system syslog (recommended for production services)."
  },
  "stderr": {
    "type": "bool",
    "default": false,
    "description": "Output logs to stderr (useful when running manually in foreground for debugging)."
  },
  "raw_input": {
    "type": "bool",
    "default": true,
    "description": "Enable raw input device handling (e.g., for HTC Vive controllers). Disable only if using alternative input drivers."
  },
  "hmd_event_device": {
    "type": "string",
    "default": "",
    "description": "Path to HMD input event device (e.g., /dev/input/event5). Leave empty to auto-detect."
  }
}
