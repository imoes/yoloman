{
  "icon_path": {
    "type": "string",
    "default": "menu",
    "description": "The subdirectory within the prefix where icon files are stored."
  },
  "root_prefix": {
    "type": "string",
    "default": "/etc/X11/fvwm/wm-icons",
    "description": "The root directory prefix for wm-icons configuration and data."
  },
  "user_prefix": {
    "type": "string",
    "default": "/.fvwm/wm-icons",
    "description": "The user home directory prefix for wm-icons configuration and data."
  },
  "treewalk": {
    "type": "string",
    "default": "c(m)",
    "description": "The treewalk parameter used by the menu generation logic.",
    "enum": [
      "c(m)",
      "c",
      "m"
    ]
  },
  "genmenu": {
    "type": "string",
    "default": "menudefs.hook",
    "description": "The hook file name used for generating the menu."
  },
  "root_section": {
    "type": "string",
    "default": "/DebianWmIcons",
    "description": "The root section name for the generated FVWM menu."
  }
}
