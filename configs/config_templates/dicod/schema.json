{
  "dicod_disabled": {
    "type": "bool",
    "default": false,
    "description": "Whether to disable the dicod init script (set to true to disable)"
  },
  "daemon_opts": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options passed to the dicod daemon"
  }
}
