{
 "fields": {
  "default": "",
  "description": "The fields to print for xymondboard. Empty string uses xymon defaults.",
  "type": "string"
 },
 "query_type": {
  "description": "The default query to execute. One of: clientlog, xymondboard, hostinfo, xymondlog, ghostlist, config, ping, version.",
  "enum": [
   "clientlog",
   "xymondboard",
   "hostinfo",
   "xymondlog",
   "ghostlist",
   "ghost",
   "config",
   "ping",
   "version"
  ],
  "type": "string"
 },
 "sections": {
  "default": [],
  "description": "Section(s) to print for clientlog. Multiple sections are comma separated in the final config.",
  "type": "list"
 },
 "test_name": {
  "default": "info",
  "description": "The test to use for selecting hosts or printing with xymondlog.",
  "type": "string"
 },
 "xymon_cmd": {
  "default": "xymon",
  "description": "Path and command name for the `xymon` binary.",
  "type": "string"
 },
 "xymon_srv": {
  "default": "127.0.0.1:1984",
  "description": "Where to find the Xymon server: IP:PORT or HOSTNAME:PORT.",
  "type": "string"
 }
}
