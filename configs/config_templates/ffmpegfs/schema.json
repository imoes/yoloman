{
  "desttype": {
    "type": "string",
    "default": "mp4",
    "description": "Destination format: AIFF, ALAC, BMP, FLAC, HLS, JPG, MOV, MP3, MP4, MKV, OGG, Opus, PNG, ProRes, TS, WAV, WebM"
  },
  "audiocodec": {
    "type": "string",
    "default": "",
    "description": "Audio codec to use. Valid values depend on desttype (e.g., AAC, MP3 for MP4)",
    "enum": [
      "AAC",
      "MP3",
      "OPUS",
      "VORBIS",
      "AC3"
    ]
  },
  "videocodec": {
    "type": "string",
    "default": "",
    "description": "Video codec to use. Valid values depend on desttype (e.g., H264, H265 for MP4)",
    "enum": [
      "H264",
      "H265",
      "MPEG1",
      "MPEG2",
      "VP9",
      "VP8",
      "AV1"
    ]
  },
  "autocopy": {
    "type": "string",
    "default": "OFF",
    "description": "Auto copy option: OFF, MATCH, MATCHLIMIT, STRICT, STRICTLIMIT",
    "enum": [
      "OFF",
      "MATCH",
      "MATCHLIMIT",
      "STRICT",
      "STRICTLIMIT"
    ]
  },
  "recodesame": {
    "type": "string",
    "default": "NO",
    "description": "Recode to same format: NO, YES",
    "enum": [
      "NO",
      "YES"
    ]
  },
  "profile": {
    "type": "string",
    "default": "NONE",
    "description": "Output profile (MP4 only): NONE, FF, EDGE, IE, CHROME, SAFARI, OPERA, MAXTHON",
    "enum": [
      "NONE",
      "FF",
      "EDGE",
      "IE",
      "CHROME",
      "SAFARI",
      "OPERA",
      "MAXTHON"
    ]
  },
  "level": {
    "type": "string",
    "default": "HQ",
    "description": "Output level (MP4 only): PROXY, LT, STANDARD, HQ",
    "enum": [
      "PROXY",
      "LT",
      "STANDARD",
      "HQ"
    ]
  },
  "include_extensions": {
    "type": "list",
    "default": [],
    "description": "List of file extensions to encode (supports wildcards)"
  },
  "hide_extensions": {
    "type": "list",
    "default": [],
    "description": "List of file extensions to exclude from output (supports wildcards)"
  },
  "audiobitrate": {
    "type": "string",
    "default": "128K",
    "description": "Audio bitrate (e.g., 128K, 256Kbps)"
  },
  "audiosamplerate": {
    "type": "string",
    "default": "44100",
    "description": "Audio sample rate in Hz (e.g., 44100, 48KHz)"
  },
  "audiochannels": {
    "type": "number",
    "default": 2,
    "description": "Number of audio channels (e.g., 1, 2, 6). Set to 0 to keep the number of channels."
  },
  "audiosamplefmt": {
    "type": "string",
    "default": "0",
    "description": "Audio sample format: 0, 8, 16, 32, 64, F16, F32, F64",
    "enum": [
      "0",
      "8",
      "16",
      "32",
      "64",
      "F16",
      "F32",
      "F64"
    ]
  },
  "videobitrate": {
    "type": "string",
    "default": "2M",
    "description": "Video bitrate (e.g., 2M, 1500Kbps)"
  },
  "videoheight": {
    "type": "number",
    "default": 0,
    "description": "Target video height in pixels (0 = keep source)"
  },
  "videowidth": {
    "type": "number",
    "default": 0,
    "description": "Target video width in pixels (0 = keep source)"
  },
  "deinterlace": {
    "type": "bool",
    "default": false,
    "description": "Enable deinterlacing"
  },
  "segment_duration": {
    "type": "string",
    "default": "10.0",
    "description": "HLS segment duration in seconds (float)"
  },
  "min_seek_time_diff": {
    "type": "string",
    "default": "30",
    "description": "HLS minimum seek time difference in seconds (float)"
  },
  "hwaccel_enc": {
    "type": "string",
    "default": "NONE",
    "description": "Hardware encoding API: NONE, VAAPI, OMX",
    "enum": [
      "NONE",
      "VAAPI",
      "OMX"
    ]
  },
  "hwaccel_dec_blocked": {
    "type": "list",
    "default": [],
    "description": "List of codecs (and optionally profiles) to block for hardware decoding (e.g., ['H264', 'VP9:0'])"
  },
  "hwaccel_enc_device": {
    "type": "string",
    "default": "",
    "description": "Hardware encoding device path (e.g., /dev/dri/renderD128)"
  },
  "hwaccel_dec": {
    "type": "string",
    "default": "NONE",
    "description": "Hardware decoding API: NONE, VAAPI, MMAL",
    "enum": [
      "NONE",
      "VAAPI",
      "MMAL"
    ]
  },
  "hwaccel_dec_device": {
    "type": "string",
    "default": "",
    "description": "Hardware decoding device path"
  },
  "noalbumarts": {
    "type": "bool",
    "default": false,
    "description": "Disable copying album art into output"
  },
  "enablescript": {
    "type": "bool",
    "default": false,
    "description": "Enable virtual script (index.php) generation"
  },
  "scriptfile": {
    "type": "string",
    "default": "index.php",
    "description": "Name of virtual script"
  },
  "scriptsource": {
    "type": "string",
    "default": "scripts/videotag.php",
    "description": "Path to source script file"
  },
  "expiry_time": {
    "type": "string",
    "default": "1w",
    "description": "Cache entry expiry time (e.g., 1w, 7d)"
  },
  "max_inactive_suspend": {
    "type": "string",
    "default": "15s",
    "description": "Time to continue transcoding after client disconnect before suspending"
  },
  "max_inactive_abort": {
    "type": "string",
    "default": "30s",
    "description": "Time to continue transcoding after client disconnect before aborting"
  },
  "prebuffer_time": {
    "type": "string",
    "default": "0",
    "description": "Prebuffer duration to allow smooth playback (e.g., 5s). Set to 0 to disable."
  },
  "prebuffer_size": {
    "type": "string",
    "default": "100K",
    "description": "Prebuffer size in bytes (e.g., 100K). Set to 0 to disable."
  },
  "max_cache_size": {
    "type": "string",
    "default": "",
    "description": "Maximum cache size (e.g., 10G). Empty/unlimited."
  },
  "min_diskspace": {
    "type": "string",
    "default": "0",
    "description": "Minimum required disk space for cache (e.g., 1G). 0 = no minimum."
  },
  "cachepath": {
    "type": "string",
    "default": "",
    "description": "Cache directory path"
  },
  "disable_cache": {
    "type": "bool",
    "default": false,
    "description": "Disable caching completely"
  },
  "cache_maintenance": {
    "type": "string",
    "default": "1h",
    "description": "Cache maintenance interval (e.g., 1h)"
  },
  "prune_cache": {
    "type": "bool",
    "default": false,
    "description": "Prune cache on startup"
  },
  "clear_cache": {
    "type": "bool",
    "default": false,
    "description": "Clear cache on startup"
  },
  "max_threads": {
    "type": "number",
    "default": 0,
    "description": "Maximum transcoding threads (0 = unlimited). Default is 16 times number of CPU cores."
  },
  "decoding_errors": {
    "type": "bool",
    "default": false,
    "description": "Abort on decoding errors. Default is to ignore errors."
  },
  "min_dvd_chapter_duration": {
    "type": "string",
    "default": "1s",
    "description": "Minimum DVD chapter duration to transcode (seconds). Set to 0 to disable."
  },
  "win_smb_fix": {
    "type": "bool",
    "default": true,
    "description": "Enable Windows SMB workaround. Defaults to on."
  },
  "log_maxlevel": {
    "type": "string",
    "default": "INFO",
    "description": "Maximum log level: ERROR, WARNING, INFO, DEBUG, TRACE",
    "enum": [
      "ERROR",
      "WARNING",
      "INFO",
      "DEBUG",
      "TRACE"
    ]
  },
  "log_stderr": {
    "type": "bool",
    "default": false,
    "description": "Output log to stderr"
  },
  "log_syslog": {
    "type": "bool",
    "default": false,
    "description": "Output log to syslog"
  },
  "logfile": {
    "type": "string",
    "default": "",
    "description": "Path to log file. Empty by default."
  }
}
