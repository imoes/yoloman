{
  "user": {
    "type": "string",
    "default": "_gvm",
    "description": "System user account under which the greenbone-feed-sync service runs."
  },
  "group": {
    "type": "string",
    "default": "_gvm",
    "description": "System group associated with the greenbone-feed-sync service user."
  }
}
