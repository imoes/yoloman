{
  "zabbix_agent2_run_dir": {
    "type": "string",
    "default": "/run/zabbix",
    "description": "Directory for Zabbix Agent 2 runtime files, including PID file."
  },
  "zabbix_agent2_stop_retry": {
    "type": "string",
    "default": "TERM/30/KILL/5",
    "description": "Retry strategy for stopping the daemon (signal/delay/signal/delay).",
    "enum": [
      "TERM/30/KILL/5",
      "KILL/5"
    ]
  },
  "zabbix_agent2_user": {
    "type": "string",
    "default": "zabbix",
    "description": "User account under which the Zabbix Agent 2 process runs."
  },
  "zabbix_agent2_group": {
    "type": "string",
    "default": "zabbix",
    "description": "Group account under which the Zabbix Agent 2 process runs."
  }
}
