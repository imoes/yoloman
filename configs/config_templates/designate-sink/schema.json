{
  "SERVICE_NAME": {
    "type": "string",
    "default": "designate-sink",
    "description": "Service name as provided in init info header"
  },
  "SERVICE_DESCRIPTION": {
    "type": "string",
    "default": "OpenStack Designate DNSaaS Sink (designate-sink)",
    "description": "Human-readable description of the service"
  },
  "PROJECT_NAME": {
    "type": "string",
    "default": "designate",
    "description": "OpenStack project name (used in paths, user, group, config paths)"
  },
  "SERVICE_SUFFIX": {
    "type": "string",
    "default": "sink",
    "description": "Suffix appended to project name to form service name (e.g., -sink)"
  }
}
