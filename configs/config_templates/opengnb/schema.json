{
  "network.public_index_nodes": {
    "type": "list",
    "default": [
      [
        "101.32.178.3",
        9001
      ],
      [
        "45.78.14.231",
        9001
      ]
    ],
    "description": "List of public index nodes, each item is [host: str, port: int]. Used only when no custom index node is configured."
  },
  "node": {
    "type": "object",
    "description": "Node identity \u2014 unique identifier and cryptographic private key"
  },
  "network": {
    "type": "object",
    "description": "Overlay network identity and addressing"
  },
  "discovery": {
    "type": "object",
    "description": "How the node discovers other peers in the mesh"
  }
}
