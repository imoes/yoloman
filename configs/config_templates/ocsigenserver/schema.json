{
  "pidfile": {
    "type": "string",
    "default": "/run/ocsigenserver/ocsigenserver.pid",
    "description": "Path to the PID file for the server process."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/ocsigenserver/ocsigenserver.log",
    "description": "Path to the main log file."
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Logging verbosity level (e.g., debug, info, warning, error)."
  },
  "http_host": {
    "type": "string",
    "default": "*",
    "description": "IP address or hostname the HTTP server binds to (e.g., '0.0.0.0' or '*')."
  },
  "http_port": {
    "type": "number",
    "default": 8080,
    "description": "TCP port for HTTP traffic."
  },
  "http_ssl_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable HTTPS support (requires valid certificate and key)."
  },
  "https_host": {
    "type": "string",
    "default": "*",
    "description": "IP address or hostname the HTTPS server binds to."
  },
  "https_port": {
    "type": "number",
    "default": 8443,
    "description": "TCP port for HTTPS traffic."
  },
  "ssl_cert": {
    "type": "string",
    "default": "/etc/ocsigenserver/server.crt",
    "description": "Path to the SSL/TLS certificate file (PEM format)."
  },
  "ssl_key": {
    "type": "string",
    "default": "/etc/ocsigenserver/server.key",
    "description": "Path to the SSL/TLS private key file (PEM format)."
  },
  "start_servers": {
    "type": "number",
    "default": 4,
    "description": "Number of worker processes to start on startup."
  },
  "min_spare_servers": {
    "type": "number",
    "default": 2,
    "description": "Minimum number of idle worker processes to maintain."
  },
  "max_spare_servers": {
    "type": "number",
    "default": 8,
    "description": "Maximum number of idle worker processes allowed."
  },
  "max_requests": {
    "type": "number",
    "default": 0,
    "description": "Maximum requests per worker before restart (0 = unlimited)."
  },
  "module_paths": {
    "type": "list",
    "default": [
      "/usr/lib/ocsigenserver/modules"
    ],
    "description": "List of directories to search for dynamically loadable modules. If empty, uses default path /usr/lib/ocsigenserver/modules. To disable module loading entirely, use <empty> modulepath tag or omit it; however, in this template we default to the standard path. Setting to empty list effectively disables loading custom modules if the server checks for presence, but typically one path is required. If the user wants NO modules, they should remove the tag. For this schema, we keep the default path but allow overriding."
  },
  "enabled_extensions": {
    "type": "list",
    "default": [
      {
        "module": "mod_cgi",
        "config": {}
      },
      {
        "module": "mod_staticfile",
        "config": {}
      }
    ],
    "description": "List of extensions to enable, each with a module name and optional configuration parameters. Note: 'mod_ocsigen' is not a standard module name in modern Ocsigen Server; standard modules are mod_staticfile, mod_cgi, mod_cgi_perl, mod_python, mod_lua, mod_proxy, etc. Removed 'mod_ocsigen' as it is hallucinated/not documented in standard configs."
  },
  "sites": {
    "type": "list",
    "default": [
      {
        "host": "localhost",
        "port": 8080,
        "document_root": "/var/www/ocsigen",
        "cgi_enabled": false,
        "cgi_aliases": [],
        "static_dir": "/var/www/ocsigen/static",
        "static_url": "/static",
        "ocsigen_extensions": []
      }
    ],
    "description": "List of virtual sites (host/port combos) with their own document roots and settings."
  }
}
