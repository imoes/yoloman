{
  "dir": {
    "type": "string",
    "default": "/var/www",
    "description": "Directory to put files into"
  },
  "runas": {
    "type": "string",
    "default": "www-data",
    "description": "User to run the service as"
  },
  "options": {
    "type": "string",
    "default": "eth0 1.5",
    "description": "Interface and bandwidth limit (e.g., 'eth0 1.5' for 1.5 Mbps)"
  }
}
