{
  "enabled": true,
  "listen_address": "0.0.0.0",
  "listen_port": 8080,
  "storage_dir": "/var/lib/powa/snapshots",
  "worker_count": 3,
  "max_snapshots": 500,
  "targets": [
    {
      "name": "main_db",
      "conninfo": "host=pg1.example.com port=5432 dbname=app user=powa password='secret' sslmode=require",
      "snapshot_every": 600,
      "retention_days": 14,
      "enabled": true
    },
    {
      "name": "analytics_db",
      "conninfo": "host=pg2.example.com port=5432 dbname=analytics user=powa password='_anon' sslmode=prefer",
      "snapshot_every": 900,
      "retention_days": 30,
      "enabled": true
    }
  ],
  "tls": {
    "enabled": true,
    "cert_file": "/etc/powa/tls/cert.pem",
    "key_file": "/etc/powa/tls/key.pem",
    "ca_file": "/etc/powa/tls/ca.pem",
    "min_version": "1.2"
  },
  "log_level": "info"
}
