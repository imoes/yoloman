{
  "dns_bridge_interface": {
    "type": "string",
    "default": "lxdbr0",
    "description": "Name of the LXD-managed bridge interface to exclude from dnsmasq binding (typically the first LXD bridge)."
  }
}
