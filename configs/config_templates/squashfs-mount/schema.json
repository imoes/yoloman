{
  "mount_entries": {
    "type": "list",
    "default": [
      {
        "image": "/var/lib/squashfs/root.sqsh",
        "mountpoint": "/mnt/squashfs"
      }
    ],
    "description": "List of objects defining the squashfs image path and the target mount point for each mount operation. Each object must have 'image' and 'mountpoint' keys."
  },
  "command": {
    "type": "string",
    "default": "/bin/sh",
    "description": "The command to execute inside the mount namespace after the squashfs images are mounted. Defaults to /bin/sh if not provided or empty."
  }
}
