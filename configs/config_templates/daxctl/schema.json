{
  "sysconfdir": {
    "type": "string",
    "default": "/etc",
    "description": "Base directory for system configuration files"
  },
  "subsection_name": {
    "type": "string",
    "default": "unique_identifier_foo",
    "description": "Unique identifier for the reconfigure-device subsection; must be unique across all config files"
  },
  "nvdimm_uuid": {
    "type": "string",
    "default": "ed93e918-e165-49d8-921d-383d7b9660c5",
    "description": "UUID of the NVDIMM device to reconfigure"
  },
  "mode": {
    "type": "string",
    "default": "system-ram",
    "description": "Target mode for the NVDIMM namespace (e.g., 'devdax', 'system-ram')\n    \"enum\": [\n      \"devdax\",\n      \"system-ram\"\n    ]\n    }\n  },\n  \"online\": {\n    \"type\": \"boolean\",\n    \"default\": true,\n    \"description\": \"Whether to bring the device online after reconfiguration\"\n  },\n  \"movable\": {\n    \"type\": \"boolean\",\n    \"default\": false,\n    \"description\": \"Whether the memory should be marked as movable (relevant for system-ram mode)\"\n  }\n}",
    "enum": [
      "devdax",
      "system-ram"
    ]
  }
}
