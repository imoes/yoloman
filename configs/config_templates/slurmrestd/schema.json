{
  "listen_addresses": {
    "type": "list",
    "default": [],
    "description": "List of host:port pairs or unix:/path/to/socket strings to listen on. Example: [\"unix:/var/run/slurm/restd.sock\", \"0.0.0.0:6820\"]"
  },
  "auth_plugins": {
    "type": "list",
    "default": [
      "rest_auth/local"
    ],
    "description": "Comma-delimited list of authentication plugins to load. Example: [\"rest_auth/local\", \"rest_auth/jwt\"]"
  },
  "data_parser_plugins": {
    "type": "list",
    "default": [
      "v0.0.42"
    ],
    "description": "Comma-delimited list of data_parser plugins to load. Example: [\"v0.0.42\", \"latest\"]"
  },
  "openapi_plugins": {
    "type": "list",
    "default": [
      "slurmctld",
      "slurmdbd"
    ],
    "description": "Comma-delimited list of OpenAPI plugins to load. Example: [\"slurmctld\", \"slurmdbd\"]"
  },
  "max_connections": {
    "type": "number",
    "default": 124,
    "description": "Maximum number of connections to process at any one time."
  },
  "thread_count": {
    "type": "number",
    "default": 20,
    "description": "Number of threads to use to process client connections."
  },
  "user_id": {
    "type": "string",
    "default": "",
    "description": "User ID to change to before processing client request. Leave empty to keep default user."
  },
  "group_id": {
    "type": "string",
    "default": "",
    "description": "Group ID to change to before processing client request. Leave empty to keep default group."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose operation (-v flag)."
  },
  "debug_flags": {
    "type": "string",
    "default": "",
    "description": "Debug flags for slurmrestd. Overrides SLURM_DEBUG_FLAGS. Example: 'partition,jobstep'",
    "enum": [
      "auth",
      "core",
      "events",
      "health",
      "listen",
      "plugin",
      "resource",
      "rest",
      "script",
      "shutdown",
      "spank",
      "state",
      "tasks",
      "trace"
    ]
  },
  "security": {
    "type": "list",
    "default": [],
    "description": "Security functionality controls. Values: become_user, disable_unshare_files, disable_unshare_sysv, disable_user_check. Example: [\"disable_user_check\"]"
  },
  "slurm_conf": {
    "type": "string",
    "default": "/etc/slurm/slurm.conf",
    "description": "Path to the Slurm configuration file."
  }
}
