{
 "ARGS": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "address": {
  "default": "",
  "description": "IP address (for Ethernet) or device path (for USB/SPI/SERIAL). Required for non-PCI devices.",
  "type": "string"
 },
 "bitfile_path": {
  "default": "",
  "description": "Path to the FPGA configuration bitfile (.bin) to write. If empty, performs info/read commands.",
  "type": "string"
 },
 "device_name": {
  "default": "",
  "description": "Name of the Mesa FPGA card (e.g., 7i90, 7i77). If empty, detects based on interface.",
  "type": "string"
 },
 "enable_verify": {
  "default": false,
  "description": "If true, verifies the flash memory against the bitfile after writing.",
  "type": "bool"
 },
 "interface_type": {
  "default": "AUTO",
  "description": "Interface type for connection: ETHER, SPI, USB, SERIAL, EPP, or AUTO.",
  "enum": [
   "ETHER",
   "SPI",
   "USB",
   "SERIAL",
   "EPP",
   "AUTO"
  ],
  "type": "string"
 },
 "set_ip_address": {
  "default": "",
  "description": "IP address to set in EEPROM memory (e.g., 192.168.1.100). Only for Ethernet boards.",
  "type": "string"
 },
 "use_fallback": {
  "default": false,
  "description": "If true, uses the fallback area of the FLASH memory.",
  "type": "bool"
 },
 "verbose_mode": {
  "default": false,
  "description": "If true, prints detailed information during execution.",
  "type": "bool"
 }
}
