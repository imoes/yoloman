{
  "AMULED_USER": {
    "type": "string",
    "default": "",
    "description": "User account under which the amule-daemon service should run. Must be non-empty for the init.d script to start the service."
  },
  "AMULED_HOME": {
    "type": "string",
    "default": "",
    "description": "Alternative home directory for the amule daemon. The daemon will use $AMULED_HOME/.aMule as its data directory."
  }
}
