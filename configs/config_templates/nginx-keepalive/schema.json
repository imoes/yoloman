{
 "access_log_path": {
  "default": "/var/log/nginx/access.log",
  "description": "Path to the access log file.",
  "type": "string"
 },
 "error_log_path": {
  "default": "/var/log/nginx/error.log",
  "description": "Path to the error log file.",
  "type": "string"
 },
 "keepalive_requests": {
  "default": 10000,
  "description": "Maximum number of requests per keepalive connection before it is closed. Higher values reduce connection churn.",
  "type": "number"
 },
 "keepalive_timeout": {
  "default": 65,
  "description": "Time (in seconds) that the server keeps connections open for keepalive reuse. Reduce for high-churn workloads.",
  "type": "number"
 },
 "servers": {
  "default": [],
  "description": "List of server blocks (virtual hosts) to define.",
  "items": {
   "listen": {
    "default": "80",
    "description": "Listen address and port. Can include IPv6 (e.g., '[::]:80'), SSL (e.g., '443 ssl').",
    "type": "string"
   },
   "locations": {
    "default": [],
    "description": "Location blocks for this server.",
    "items": {
     "path": {
      "default": "/",
      "description": "Location path (e.g., '/api' or '= /health').",
      "type": "string"
     },
     "proxy_connect_timeout": {
      "default": 60,
      "description": "Timeout (seconds) for connecting to the upstream server.",
      "type": "number"
     },
     "proxy_pass": {
      "default": "",
      "description": "Upstream group name to proxy requests to (e.g., 'backend'); leave empty to disable proxy.",
      "type": "string"
     },
     "proxy_read_timeout": {
      "default": 60,
      "description": "Timeout (seconds) for reading response from the upstream server.",
      "type": "number"
     },
     "static_root": {
      "default": "",
      "description": "Document root for static files; leave empty to disable static serving.",
      "type": "string"
     }
    },
    "type": "list"
   },
   "name": {
    "default": "localhost",
    "description": "Server name(s), e.g., 'example.com' or wildcard '*.example.com'.",
    "type": "string"
   },
   "ssl": {
    "default": false,
    "description": "Whether to enable TLS for this server.",
    "type": "bool"
   },
   "ssl_cert_path": {
    "default": "/etc/nginx/ssl/server.crt",
    "description": "Path to SSL certificate file (required if SSL enabled).",
    "type": "string"
   },
   "ssl_key_path": {
    "default": "/etc/nginx/ssl/server.key",
    "description": "Path to SSL private key file (required if SSL enabled).",
    "type": "string"
   }
  },
  "type": "list"
 },
 "upstreams": {
  "default": [],
  "description": "List of upstream groups for load balancing. Each defines a group of backend servers with optional keepalive tuning.",
  "items": {
   "keepalive_connections": {
    "default": 32,
    "description": "Number of idle keepalive connections to hold per worker for this upstream group.",
    "type": "number"
   },
   "name": {
    "default": "backend",
    "description": "Name of the upstream group (used in proxy_pass).",
    "type": "string"
   },
   "servers": {
    "default": [],
    "description": "List of backend servers in this upstream group.",
    "items": {
     "address": {
      "default": "127.0.0.1",
      "description": "IP address or hostname of the backend server.",
      "type": "string"
     },
     "backup": {
      "default": false,
      "description": "If true, this server is a backup and only used when all primary servers are down.",
      "type": "bool"
     },
     "down": {
      "default": false,
      "description": "If true, this server is permanently marked as unavailable.",
      "type": "bool"
     },
     "port": {
      "default": 8080,
      "description": "Port on which the backend server listens.",
      "type": "number"
     }
    },
    "type": "list"
   }
  },
  "type": "list"
 },
 "worker_connections": {
  "default": 1024,
  "description": "Maximum number of simultaneous connections that each worker process can handle.",
  "type": "number"
 },
 "worker_processes": {
  "default": "auto",
  "description": "Number of worker processes; 'auto' sets it equal to the number of CPU cores.",
  "type": "string"
 }
}
