{
  "zm_cgi_bin_path": {
    "type": "string",
    "default": "/usr/lib/zoneminder/cgi-bin",
    "description": "The path to ZoneMinder's CGI binaries."
  },
  "zm_cache_path": {
    "type": "string",
    "default": "/var/cache/zoneminder",
    "description": "The path to ZoneMinder's cache directory."
  },
  "zm_www_path": {
    "type": "string",
    "default": "/usr/share/zoneminder/www",
    "description": "The path to ZoneMinder's web application files."
  }
}
