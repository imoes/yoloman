{
 "encoding": {
  "default": "hex",
  "description": "Output encoding: hex, octal, decimal, base32, c32, w32, z32, base64, i64, p64, x64, u64, g64, a85, z85",
  "enum": [
   "hex",
   "octal",
   "decimal",
   "base32",
   "base64",
   "p64"
  ],
  "type": "string"
 },
 "hash_type": {
  "default": "md5",
  "description": "Hash algorithm: md5, sha1, sha256, sha512, whirl, whirlpool, jh224, jh256, jh384, jh512",
  "enum": [
   "md5",
   "sha1",
   "sha256",
   "sha512",
   "whirl",
   "whirlpool",
   "jh256"
  ],
  "type": "string"
 },
 "hide_text": {
  "default": false,
  "description": "Hide typed input in CGI mode",
  "type": "bool"
 },
 "hook_scripts": {
  "default": [],
  "description": "List of hookscript paths to run on failures (CHECK) or matches (LOCATE/DUPS)",
  "type": "list"
 },
 "line_ending": {
  "default": "none",
  "description": "Line ending to append to stdin input in CGI mode: none, lf, crlf, cr",
  "enum": [
   "none",
   "lf",
   "crlf",
   "cr"
  ],
  "type": "string"
 },
 "memcached_servers": {
  "default": [],
  "description": "List of memcached server addresses",
  "type": "list"
 },
 "no_options": {
  "default": false,
  "description": "Disable user-editable options in CGI mode",
  "type": "bool"
 },
 "output_length": {
  "default": 0,
  "description": "Truncate output hash to this many characters (0 = no truncation)",
  "type": "number"
 },
 "segment_char": {
  "description": "Character to separate output segments",
  "enum": [
   "+",
   " ",
   "-",
   ".",
   "/",
   ":"
  ],
  "type": "string"
 },
 "segment_length": {
  "default": 0,
  "description": "Break output into segments of this many characters (0 = no segmentation)",
  "type": "number"
 },
 "show_text": {
  "default": false,
  "description": "Show typed input in CGI mode (overrides hide_text)",
  "type": "bool"
 },
 "xattr_types": {
  "default": [],
  "description": "Extended attribute types to use: 'user' or 'trusted'",
  "type": "list"
 }
}
