{
  "pidfile_path": {
    "type": "string",
    "default": "/var/run/ngircd/ngircd.pid",
    "description": "Full path to the PID file."
  },
  "verbose": {
    "type": "bool",
    "default": true,
    "description": "Enable verbose logging in init script."
  }
}
