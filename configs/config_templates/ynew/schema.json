{
  "command_editor": {
    "type": "string",
    "default": "vim",
    "description": "The text editor launched by ynew to write a new command script if the command does not exist."
  },
  "shell": {
    "type": "string",
    "default": "bash",
    "description": "The shell interpreter used to execute the command script in the yakuake tab."
  },
  "home_dir": {
    "type": "string",
    "default": "~/.ynew",
    "description": "The directory where ynew stores the individual command scripts."
  }
}
