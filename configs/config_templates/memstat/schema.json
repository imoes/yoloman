{
  "scan_directories": {
    "type": "list",
    "default": [
      "/bin",
      "/usr/bin",
      "/sbin",
      "/usr/sbin",
      "/lib",
      "/usr/lib",
      "/dev"
    ],
    "description": "List of directories to scan recursively for executables and shared objects. These should include all major bin, lib, and dev directories in the system."
  }
}
