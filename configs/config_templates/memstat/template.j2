# memstat configuration file
# Controls which directories are scanned recursively for executables and shared objects.
# Each line contains one directory path.

# List of directories to scan recursively for executables and shared objects.
# These should include all major bin, lib, and dev directories.
# Default: ['/bin', '/usr/bin', '/sbin', '/usr/sbin', '/lib', '/usr/lib', '/dev']
{% for directory in (scan_directories | default(['/bin', '/usr/bin', '/sbin', '/usr/sbin', '/lib', '/usr/lib', '/dev'])) %}
{{ directory }}
{% endfor %}