{
  "target": {
    "type": "object",
    "description": "Target connection details. Note: In gonja, we flatten this or access fields directly if the schema allows nested objects. However, the prompt restricts 'default' to bare scalars or arrays. To comply strictly, we must flatten the schema or use a list of objects for multiple targets. Given httping is typically a single command, we flatten the target into the root or use a single object if the engine supports nested defaults. The prompt says 'default MUST be a BARE scalar ... or array'. It does NOT forbid nested objects in the *schema definition* structure (properties), only in the *default value*. So we can have a nested schema for 'target' with nested properties, as long as the default is not an object. But wait, 'default' is a field in the var definition. If var 'target' is type object, its default must be an object? The prompt says: 'For a list-of-objects, add items as a FLAT map...'. It implies complex types might be tricky. Let's flatten everything to root level to be safe and compliant with 'default' being bare scalar/array where possible, or use lists for multi-value. Actually, the prompt says 'default' is a bare scalar or array. It doesn't explicitly forbid 'object' type, but 'type' is EXACTLY one of string|number|bool|list. So 'object' is NOT allowed. Therefore, we must flatten all settings to the root level."
  }
}
