{
  "start": {
    "type": "string",
    "default": "0",
    "description": "Starting value (0% progress). Supports metric/binary suffixes (k,m,g,t,ki,mi,gi,ti) or 'initial' to use first value from progress command.",
    "enum": [
      "initial",
      "0",
      "1",
      "2",
      "3",
      "6",
      "9"
    ]
  },
  "interval": {
    "type": "number",
    "default": 1,
    "description": "Seconds between invocations of the progress command. Cannot be used with 'cont' option."
  },
  "down": {
    "type": "bool",
    "default": false,
    "description": "Set to true if progress value decreases during execution (e.g., file deletion). If set, default start is 'initial'."
  },
  "width": {
    "type": "number",
    "default": 80,
    "description": "Width (columns) of the eta output. Defaults to terminal width if not specified and stdout is a TTY."
  },
  "cont": {
    "type": "bool",
    "default": false,
    "description": "Set to true to read progress continuously from command\u2019s stdout (e.g., with tail -f). Cannot be used with 'interval'."
  }
}
