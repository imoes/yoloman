{
  "USER": {
    "type": "string",
    "default": "jabber",
    "description": "The user account to run the MUC service as."
  },
  "GROUP": {
    "type": "string",
    "default": "jabber",
    "description": "The group account to run the MUC service as."
  },
  "MUC_RUN": {
    "type": "string",
    "default": "1",
    "description": "Set to 1 to enable running the MUC service, 0 to disable.",
    "enum": [
      "0",
      "1"
    ]
  }
}
