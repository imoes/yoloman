{
  "max_devices": {
    "type": "number",
    "default": 1,
    "description": "Maximum number of fingerprint devices that can be enrolled per user at the same time. Set to 0 for unlimited."
  },
  "data_store": {
    "type": "string",
    "default": "",
    "description": "Absolute or relative path for storing fingerprint templates. If empty, uses the default XDG-based location."
  },
  "drivers": {
    "type": "list",
    "default": [],
    "description": "Ordered list of fingerprint drivers to use (e.g., 'libfprint-2-tod1-goodix', 'libfprint-2-tod1-vfs0090'). Leave empty to let fprintd auto-select the best available driver.\n      \"items\": {\n        \"type\": \"string\"\n      }\n    }"
  },
  "removed": {
    "type": "list",
    "default": [],
    "description": "Removed variables that were not documented in the provided man page or official documentation: `log_level`, `timeout`."
  }
}
