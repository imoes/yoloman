{
  "config": {
    "type": "object",
    "default": null,
    "description": "Root configuration object. Note: 'default' cannot be an object in this schema format, so we define fields individually below. In a real Jinja2 implementation for this specific constraint, we would likely flatten the schema or use a list of key-value pairs. However, to adhere strictly to 'NO nested objects' in default and 'items as flat map', we will define the top-level keys directly in the schema if possible, or use a list of settings. \nGiven the complexity, a better approach for 'Class-B' reuse is to define a list of 'settings' where each setting is an object? No, the schema says 'items as a FLAT map field-name'. \nLet's simplify: We will use a list of key-value pairs for the configuration file content, OR we can define the schema fields at the top level. \nActually, the prompt asks for `values_schema` var -> {type, default...}. \nSince the template uses `{{ config.path }}`, `config` is a variable. But `default` cannot be a nested object. \nSolution: Define `config_path`, `config_mirror` etc. as top-level variables in the schema? \nOr, define `config` as type `list` where each item is a map? No, Jinja2 needs dot notation access usually. \nLet's look at the constraint: "
  },
  "default": {
    "type": "object",
    "default": null,
    "description": "Root configuration object..."
  }
}
