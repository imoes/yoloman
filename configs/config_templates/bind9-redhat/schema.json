{
  "bind_listen_on_ipv4": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IPv4 address for BIND to listen on port 53."
  },
  "bind_listen_on_ipv6": {
    "type": "string",
    "default": "::1",
    "description": "IPv6 address for BIND to listen on port 53."
  },
  "bind_directory": {
    "type": "string",
    "default": "/var/named",
    "description": "Directory where BIND stores its zone files and data."
  },
  "bind_dump_file": {
    "type": "string",
    "default": "/var/named/data/cache_dump.db",
    "description": "Path to the cache dump database file."
  },
  "bind_statistics_file": {
    "type": "string",
    "default": "/var/named/data/named_stats.txt",
    "description": "Path to the statistics file."
  },
  "bind_memstatistics_file": {
    "type": "string",
    "default": "/var/named/data/named_mem_stats.txt",
    "description": "Path to the memory statistics file."
  },
  "bind_secroots_file": {
    "type": "string",
    "default": "/var/named/data/named.secroots",
    "description": "Path to the secure roots file."
  },
  "bind_recursing_file": {
    "type": "string",
    "default": "/var/named/data/named.recursing",
    "description": "Path to the recursing clients file."
  },
  "bind_allow_query": {
    "type": "string",
    "default": "localhost",
    "description": "ACL or value specifying who can query the DNS server."
  },
  "bind_recursion": {
    "type": "bool",
    "default": "yes",
    "description": "Enable or disable DNS recursion."
  },
  "bind_dnssec_validation": {
    "type": "bool",
    "default": "yes",
    "description": "Enable or disable DNSSEC validation."
  },
  "bind_managed_keys_directory": {
    "type": "string",
    "default": "/var/named/dynamic",
    "description": "Directory for managed DNSSEC keys."
  },
  "bind_geoip_directory": {
    "type": "string",
    "default": "/usr/share/GeoIP",
    "description": "Directory for GeoIP database files."
  },
  "bind_pid_file": {
    "type": "string",
    "default": "/run/named/named.pid",
    "description": "Path to the PID file for the BIND process."
  },
  "bind_session_keyfile": {
    "type": "string",
    "default": "/run/named/session.key",
    "description": "Path to the TSIG session key file."
  }
}
