{
  "name": {
    "type": "string",
    "default": "ircd-hybrid",
    "description": "The name of the daemon process."
  },
  "desc": {
    "type": "string",
    "default": "Hybrid 8 IRC Server",
    "description": "A human-readable description of the service."
  },
  "run_user": {
    "type": "string",
    "default": "irc",
    "description": "The system user to run the daemon as."
  },
  "run_group": {
    "type": "string",
    "default": "irc",
    "description": "The system group to run the daemon as."
  }
}
