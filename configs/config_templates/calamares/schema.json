{
  "branding": {
    "type": "string",
    "default": "calamares",
    "description": "Branding directory name (under /usr/share/calamares/branding/)."
  },
  "config_dir": {
    "type": "string",
    "default": "/etc/calamares",
    "description": "Configuration directory path."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debug output."
  },
  "modules": {
    "type": "list",
    "default": [],
    "description": "List of module names (without .conf extension) to load."
  },
  "module_paths": {
    "type": "list",
    "default": [],
    "description": "List of additional directories to search for modules."
  },
  "settings": {
    "type": "object",
    "default": null,
    "description": "Top-level settings dictionary. Each key-value pair is rendered as 'key: value' under 'settings:'. Only scalar values allowed."
  }
}
