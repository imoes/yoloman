{
  "INTERFACES": {
    "type": "string",
    "default": "eth0 eth1",
    "description": "Space-separated list of interfaces the DHCP relay should listen on."
  },
  "SERVERS": {
    "type": "string",
    "default": "192.168.1.1",
    "description": "Space-separated list of DHCP server IP addresses to forward requests to."
  },
  "OPTIONS": {
    "type": "string",
    "default": "",
    "description": "Additional options passed to the dhcrelay daemon."
  }
}
