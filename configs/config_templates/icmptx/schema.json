{
  "role": {
    "type": "string",
    "default": "client",
    "description": "The operational mode of the icmptx instance. Either 'server' or 'client'.",
    "enum": [
      "server",
      "client"
    ]
  },
  "target": {
    "type": "string",
    "default": "192.0.2.1",
    "description": "The host name or IP address of the server (for clients) or the local interface/address to bind to (for servers, as per synopsis 'server' arg)."
  }
}
