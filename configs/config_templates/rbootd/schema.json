{
  "listen_interface": {
    "type": "string",
    "default": "eth0",
    "description": "Network interface (e.g., 'eth0') or '0.0.0.0' for all interfaces to listen on for PXE/DHCP boot traffic."
  }
}
