; Budgie Desktop Environment: Default Applications Configuration
;
; This file maps MIME types or URI schemes to .desktop files.
; Each entry associates a specific file type (e.g., PDF, MP3) with the application
; that should handle it by default.
;
; Structure: [Default Applications]
;            mime/type=application.desktop

[Default Applications]

; Default application for CSV files
application/csv={{ application_csv | default('libreoffice-calc.desktop') }}

; Default application for Excel files (legacy alias)
application/excel={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for MS Excel files
application/msexcel={{ application_vnd_ms_excel | default('libreoffice-calc.desktop') }}

; Default application for MS Word files
application/msword={{ application_msword | default('libreoffice-writer.desktop') }}

; Default application for OGG files
application/ogg={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for OXPS files
application/oxps={{ application_oxps | default('org.gnome.Evince.desktop') }}

; Default application for PDF files
application/pdf={{ application_pdf | default('org.gnome.Evince.desktop') }}

; Default application for PostScript files
application/postscript={{ application_postscript | default('org.gnome.Evince.desktop') }}

; Default application for RTF files
application/rtf={{ application_rtf | default('libreoffice-writer.desktop') }}

; Default application for Tab-Separated Values files
application/tab-separated-values={{ application_csv | default('libreoffice-calc.desktop') }}

; Default application for .deb packages
application/vnd.debian.binary-package={{ application_vnd_debian_binary_package | default('gnome-software-local-file.desktop') }}

; Default application for MS Cabinet archives
application/vnd.ms-cab-compressed={{ application_vnd_ms_cab_compressed | default('gnome-software-local-file.desktop') }}

; Default application for Lotus 1-2-3 files
application/vnd.lotus-1-2-3={{ application_lotus_1_2_3 | default('libreoffice-calc.desktop') }}

; Default application for MS Excel files
application/vnd.ms-excel={{ application_vnd_ms_excel | default('libreoffice-calc.desktop') }}

; Default application for MS Word files
application/vnd.ms-word={{ application_vnd_ms_word | default('libreoffice-writer.desktop') }}

; Default application for MS XPS documents
application/vnd.ms-xpsdocument={{ application_vnd_ms_xpsdocument | default('org.gnome.Evince.desktop') }}

; Default application for RealMedia files
application/vnd.rn-realmedia={{ application_vnd_rn_realmedia | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Sun XML Base files
application/vnd.sun.xml.base={{ application_vnd_sun_xml_base | default('libreoffice-base.desktop') }}

; Default application for Sun XML Calc files
application/vnd.sun.xml.calc={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for Sun XML Calc templates
application/vnd.sun.xml.calc.template={{ application_vnd_sun_xml_calc_template | default('libreoffice-calc.desktop') }}

; Default application for Sun XML Draw files
application/vnd.sun.xml.draw={{ application_vnd_sun_xml_draw | default('libreoffice-draw.desktop') }}

; Default application for Sun XML Draw templates
application/vnd.sun.xml.draw.template={{ application_vnd_sun_xml_draw_template | default('libreoffice-draw.desktop') }}

; Default application for Sun XML Math files
application/vnd.sun.xml.math={{ application_vnd_sun_xml_math | default('libreoffice-math.desktop') }}

; Default application for Sun XML Writer files
application/vnd.sun.xml.writer={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for Sun XML Writer templates
application/vnd.sun.xml.writer.template={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for Sun XML Writer global files
application/vnd.sun.xml.writer.global={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for OASIS OpenDocument Database files
application/vnd.oasis.opendocument.database={{ application_vnd_sun_xml_base | default('libreoffice-base.desktop') }}

; Default application for OASIS OpenDocument Formula files
application/vnd.oasis.opendocument.formula={{ application_vnd_sun_xml_math | default('libreoffice-math.desktop') }}

; Default application for OASIS OpenDocument Graphics files
application/vnd.oasis.opendocument.graphics={{ application_vnd_sun_xml_draw | default('libreoffice-draw.desktop') }}

; Default application for OASIS OpenDocument Graphics templates
application/vnd.oasis.opendocument.graphics-template={{ application_vnd_sun_xml_draw_template | default('libreoffice-draw.desktop') }}

; Default application for OASIS OpenDocument Presentation files
application/vnd.oasis.opendocument.presentation={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for OASIS OpenDocument Presentation templates
application/vnd.oasis.opendocument.presentation-template={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for OASIS OpenDocument Spreadsheet files
application/vnd.oasis.opendocument.spreadsheet={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for OASIS OpenDocument Spreadsheet templates
application/vnd.oasis.opendocument.spreadsheet-template={{ application_vnd_sun_xml_calc_template | default('libreoffice-calc.desktop') }}

; Default application for OASIS OpenDocument Text files
application/vnd.oasis.opendocument.text={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for OASIS OpenDocument Text templates
application/vnd.oasis.opendocument.text-template={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for OASIS OpenDocument Text Web files
application/vnd.oasis.opendocument.text-web={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for OASIS OpenDocument Text Master files
application/vnd.oasis.opendocument.text-master={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for Sun XML Impress (Presentation) files
application/vnd.sun.xml.impress={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for Sun XML Impress templates
application/vnd.sun.xml.impress.template={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for StarDivision Calc files
application/vnd.stardivision.calc={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for StarDivision Draw files
application/vnd.stardivision.draw={{ application_vnd_sun_xml_draw | default('libreoffice-draw.desktop') }}

; Default application for StarDivision Impress files
application/vnd.stardivision.impress={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for StarDivision Math files
application/vnd.stardivision.math={{ application_vnd_sun_xml_math | default('libreoffice-math.desktop') }}

; Default application for StarDivision Writer files
application/vnd.stardivision.writer={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for MS Powerpoint files
application/mspowerpoint={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for MS PowerPoint files
application/vnd.ms-powerpoint={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for WordPerfect files
application/vnd.wordperfect={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for WordPerfect files
application/wordperfect={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for WordprocessingML Document files
application/vnd.openxmlformats-officedocument.wordprocessingml.document={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for WordprocessingML Template files
application/vnd.openxmlformats-officedocument.wordprocessingml.template={{ application_vnd_sun_xml_writer | default('libreoffice-writer.desktop') }}

; Default application for SpreadsheetML Sheet files
application/vnd.openxmlformats-officedocument.spreadsheetml.sheet={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for SpreadsheetML Template files
application/vnd.openxmlformats-officedocument.spreadsheetml.template={{ application_vnd_sun_xml_calc_template | default('libreoffice-calc.desktop') }}

; Default application for PresentationML Slide files
application/vnd.openxmlformats-officedocument.presentationml.slide={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for PresentationML Slideshow files
application/vnd.openxmlformats-officedocument.presentationml.slideshow={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for PresentationML Presentation files
application/vnd.openxmlformats-officedocument.presentationml.presentation={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for PresentationML Template files
application/vnd.openxmlformats-officedocument.presentationml.template={{ application_impress | default('libreoffice-impress.desktop') }}

; Default application for Lotus 1-2-3 files (alias)
application/x-123={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for AbiWord files
application/x-abiword={{ application_abisword | default('abiword.desktop') }}

; Default application for Applix Spreadsheet files
application/x-applix-spreadsheet={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for AR archives
application/x-ar={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for ARJ archives
application/x-arj={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Audacity project files
application/x-audacity-project={{ application_audacity | default('audacity.desktop') }}

; Default application for DVI files
application/x-bzdvi={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Bzip2-compressed Tar archives
application/x-bzip-compressed-tar={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Bzip2 archives
application/x-bzip={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Bzip2-compressed PDF files
application/x-bzpdf={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Bzip2-compressed PostScript files
application/x-bzpostscript={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Comic Book Archive (CBR) files
application/x-cbr={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Comic Book Archive (CBT) files
application/x-cbt={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Comic Book Archive (CBZ) files
application/x-cbz={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Compressed Tar archives
application/x-compressed-tar={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Compress archives
application/x-compress={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for CAB archives
application/x-cab={{ application_vnd_ms_cab_compressed | default('gnome-software-local-file.desktop') }}

; Default application for MS CAB compressed files
application/x-ms-cab-compressed={{ application_vnd_ms_cab_compressed | default('gnome-software-local-file.desktop') }}

; Default application for Debian packages
application/x-deb={{ application_vnd_debian_binary_package | default('gnome-software-local-file.desktop') }}

; Default application for Debian packages
application/x-debian-package={{ application_vnd_debian_binary_package | default('gnome-software-local-file.desktop') }}

; Default application for DOS MS Excel files
application/x-dos_ms_excel={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for DVI files
application/x-dvi={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for EAR archives
application/x-ear={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Excel files
application/x-excel={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for M4A media files
application/x-extension-m4a={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MP4 media files
application/x-extension-mp4={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLAC audio files
application/x-flac={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for Glade project files
application/x-glade={{ application_glade | default('glade.desktop') }}

; Default application for Gnumeric spreadsheets
application/x-gnumeric={{ application_gnumeric | default('gnumeric.desktop') }}

; Default application for Tar archives
application/x-gtar={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Gzip-compressed DVI files
application/x-gzdvi={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Gzip archives
application/x-gzip={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Gzip-compressed PDF files
application/x-gzpdf={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Gzip-compressed PostScript files
application/x-gzpostscript={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Web content (HTML)
application/xhtml+xml={{ application_firefox | default('firefox-esr.desktop') }}

; Default application for Java archives
application/x-jar={{ application_openjdk | default('openjdk-7-java.desktop') }}

; Default application for Java archives
application/x-java-archive={{ application_openjdk | default('openjdk-7-java.desktop') }}

; Default application for Java Web Start files
application/x-java-jnlp-file={{ application_icedtea | default('icedtea-netx-javaws.desktop') }}

; Default application for LHA archives
application/x-lha={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for LHZ archives
application/x-lhz={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for XLS files
application/xls={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for LZOP archives
application/x-lzop={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Matroska media files
application/x-matroska={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MPS (Media Player Score?) files
application/x-mps={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for MS Excel files
application/x-ms-excel={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for MS Excel files
application/x-msexcel={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for OGG audio files
application/x-ogg={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for Oleo spreadsheet files
application/x-oleo={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for Perl source code
application/x-perl={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for Planperfect files
application/x-planperfect={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for Quattro Pro files
application/x-quattropro={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for RAR archives
application/x-rar-compressed={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for RAR archives
application/x-rar={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for RPM packages
application/x-rpm={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Scilab Calc files
application/x-sc={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for Flash media files
application/x-shockwave-flash={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for SYLK spreadsheet files
application/x-sylk={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for Tar archives
application/x-tar={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for WAR archives
application/x-war={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for xBase database files
application/x-xbase={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for XLS files
application/x-xls={{ application_vnd_sun_xml_calc | default('libreoffice-calc.desktop') }}

; Default application for XZ-compressed PDF files
application/x-xzpdf={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for ZIP archives
application/x-zip-compressed={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for ZIP archives
application/x-zip={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for Zoo archives
application/x-zoo={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for ZIP archives
application/zip={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for 3GPP audio files
audio/3gpp={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for AC3 audio files
audio/ac3={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for AMR audio files
audio/AMR={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for AMR-WB audio files
audio/AMR-WB={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Basic audio files
audio/basic={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLAC audio files
audio/flac={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for MIDI audio files
audio/midi={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MP4 audio files
audio/mp4={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MP3 audio files
audio/mpeg={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for M3U playlist files
audio/mpegurl={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for OGG audio files
audio/ogg={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for SID audio files
audio/prs.sid={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for RealAudio files
audio/vnd.rn-realaudio={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for APE audio files
audio/x-ape={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLAC audio files
audio/x-flac={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for GSM audio files
audio/x-gsm={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for IT audio files
audio/x-it={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for M4A audio files
audio/x-m4a={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Matroska audio files
audio/x-matroska={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MOD audio files
audio/x-mod={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MP3 audio files
audio/x-mp3={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for MP3 audio files
audio/x-mpeg={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for M3U playlist files
audio/x-mpegurl={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for ASF audio files
audio/x-ms-asf={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for ASX audio files
audio/x-ms-asx={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WAX audio files
audio/x-ms-wax={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WMA audio files
audio/x-ms-wma={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Musepack audio files
audio/x-musepack={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for AIFF audio files
audio/x-pn-aiff={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for AU audio files
audio/x-pn-au={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for RealAudio files
audio/x-pn-realaudio={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for RealAudio plugin files
audio/x-pn-realaudio-plugin={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WAV audio files
audio/x-pn-wav={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for ACM audio files
audio/x-pn-windows-acm={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for RealAudio files
audio/x-realaudio={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for RealAudio files
audio/x-real-audio={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for SBC audio files
audio/x-sbc={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for PLS playlist files
audio/x-scpls={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for Speex audio files
audio/x-speex={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for TTA audio files
audio/x-tta={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WAV audio files
audio/x-wav={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WavPack audio files
audio/x-wavpack={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Vorbis audio files
audio/x-vorbis={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for Vorbis+OGG audio files
audio/x-vorbis+ogg={{ application_ogg | default('rhythmbox.desktop') }}

; Default application for XM audio files
audio/x-xm={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for BMP image files
image/bmp={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for GIF image files
image/gif={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for JPEG image files
image/jpeg={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for JPG image files
image/jpg={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PJPEG image files
image/pjpeg={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PNG image files
image/png={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for SVG image files
image/svg+xml={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for TIFF image files
image/tiff={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for DjVu image files
image/vnd.djvu={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for RealPix image files
image/vnd.rn-realpix={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for BMP image files
image/x-bmp={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for Bzip2-compressed EPS image files
image/x-bzeps={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for EPS image files
image/x-eps={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for Grayscale image files
image/x-gray={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for Gzip-compressed EPS image files
image/x-gzeps={{ application_evince | default('org.gnome.Evince.desktop') }}

; Default application for ICB image files
image/x-icb={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for ICO image files
image/x-ico={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PNG image files
image/x-png={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PNM image files
image/x-portable-anymap={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PBM image files
image/x-portable-bitmap={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PGM image files
image/x-portable-graymap={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PPM image files
image/x-portable-pixmap={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PSD image files
image/x-psd={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for XBM image files
image/x-xbitmap={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for XPM image files
image/x-xpixmap={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for directories
inode/directory={{ application_nemo | default('nemo.desktop') }}

; Default application for Ultravox audio files
misc/ultravox={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for multipart/zip archives
multipart/x-zip={{ application_file_roller | default('org.gnome.FileRoller.desktop') }}

; Default application for AbiWord text files
text/abiword={{ application_abisword | default('abiword.desktop') }}

; Default application for Calendar files
text/calendar={{ application_evolution | default('org.gnome.Evolution.desktop') }}

; Default application for Comma-Separated Values files
text/comma-separated-values={{ application_csv | default('libreoffice-calc.desktop') }}

; Default application for CSV files
text/csv={{ application_csv | default('libreoffice-calc.desktop') }}

; Default application for HTML files
text/html={{ application_firefox | default('firefox-esr.desktop') }}

; Default application for Plain text files
text/plain={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for Rich Text Format files
text/richtext={{ application_abisword | default('abiword.desktop') }}

; Default application for RTF files
text/rtf={{ application_rtf | default('libreoffice-writer.desktop') }}

; Default application for Spreadsheet files
text/spreadsheet={{ application_csv | default('libreoffice-calc.desktop') }}

; Default application for Tab-Separated Values files
text/tab-separated-values={{ application_csv | default('libreoffice-calc.desktop') }}

; Default application for Comma-Separated Values files
text/x-comma-separated-values={{ application_csv | default('libreoffice-calc.desktop') }}

; Default application for C Header files
text/x-chdr={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for C Source files
text/x-csrc={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application forDTD files
text/x-dtd={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for Java source files
text/x-java={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for MathML files
text/mathml={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for Python source files
text/x-python={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for SQL files
text/x-sql={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for XML files
text/xml={{ application_gedit | default('org.gnome.gedit.desktop') }}

; Default application for 3GPP video files
video/3gpp={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for DV video files
video/dv={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLI video files
video/fli={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLV video files
video/flv={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for M2TS video files
video/mp2t={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MP4 video files
video/mp4={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MP4V-ES video files
video/mp4v-es={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MPEG video files
video/mpeg={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for AVI video files
video/msvideo={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for OGG video files
video/ogg={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for QuickTime video files
video/quicktime={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Vivo video files
video/vivo={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for DivX video files
video/vnd.divx={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for RealVideo files
video/vnd.rn-realvideo={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Vivo video files
video/vnd.vivo={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WebM video files
video/webm={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for ANIM video files
video/x-anim={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for AVI video files
video/x-avi={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLC video files
video/x-flc={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLI video files
video/x-fli={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLIC video files
video/x-flic={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for FLV video files
video/x-flv={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for M4V video files
video/x-m4v={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Matroska video files
video/x-matroska={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for MPEG video files
video/x-mpeg={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for ASF video files
video/x-ms-asf={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for ASX video files
video/x-ms-asx={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for AVI video files
video/x-msvideo={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WM video files
video/x-ms-wm={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WMV video files
video/x-ms-wmv={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WMX video files
video/x-ms-wmx={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for WVX video files
video/x-ms-wvx={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for NSV video files
video/x-nsv={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for OGM+OGG video files
video/x-ogm+ogg={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Theora+OGG video files
video/x-theora+ogg={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Totem stream files
video/x-totem-stream={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Video DVD content
x-content/video-dvd={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Video CD content
x-content/video-vcd={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Super VCD content
x-content/video-svcd={{ application_celluloid | default('io.github.celluloid_player.Celluloid.desktop') }}

; Default application for Audio CDDA content
x-content/audio-cdda={{ application_rhythmbox_device | default('rhythmbox-device.desktop') }}

; Default application for Audio DVD content
x-content/audio-dvd={{ application_rhythmbox_device | default('rhythmbox-device.desktop') }}

; Default application for Audio Player content
x-content/audio-player={{ application_rhythmbox_device | default('rhythmbox-device.desktop') }}

; Default application for DCF Image content
x-content/image-dcf={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for PictureCD content
x-content/image-picturecd={{ application_gthumb | default('org.gnome.gThumb.desktop') }}

; Default application for Windows XLS association
zz-application/zz-winassoc-xls={{ application_csv | default('libreoffice-calc.desktop') }}

; Default application for APT URL scheme handlers
x-scheme-handler/apt={{ application_apturl | default('apturl.desktop') }}

; Default application for HTTP URL scheme handlers
x-scheme-handler/http={{ application_firefox | default('firefox-esr.desktop') }}

; Default application for HTTPS URL scheme handlers
x-scheme-handler/https={{ application_firefox | default('firefox-esr.desktop') }}

; Default application for Mailto URL scheme handlers
x-scheme-handler/mailto={{ application_thunderbird | default('thunderbird.desktop') }}