{
  "interface": {
    "type": "string",
    "default": "eth0",
    "description": "Interface to listen to multicast DHCPv6 traffic"
  },
  "sqlite_volatile_path": {
    "type": "string",
    "default": "/var/lib/dhcpy6d/volatile.sqlite",
    "description": "Path to the SQLite database for volatile (lease/LLIP-MAC mapping) data"
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/dhcpy6d.log",
    "description": "Path to the log file"
  }
}
