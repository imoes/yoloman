{
  "host": {
    "type": "string",
    "default": "localhost",
    "description": "The hostname or IP address of the IBM host to connect to."
  },
  "port": {
    "type": "number",
    "default": 23,
    "description": "The TCP port number on the host for the 3270 data stream connection."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "If true, enables debugging information and tracing of data going between tcl3270 and s3270."
  }
}
