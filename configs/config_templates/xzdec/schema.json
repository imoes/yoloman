{
  "binary_path": {
    "type": "string",
    "default": "/usr/bin/xzdec",
    "description": "Path to the xzdec executable binary."
  },
  "strict_mode": {
    "type": "bool",
    "default": false,
    "description": "If true, enables double-quiet mode (-qq) to suppress errors."
  },
  "input_files": {
    "type": "list",
    "default": [],
    "description": "List of file paths to decompress. xzdec decompresses to stdout by default."
  },
  "output_file": {
    "type": "string",
    "default": "",
    "description": "Optional path to redirect decompressed output to a file. If empty, output goes to stdout."
  }
}
