{
  "UNLOCK_SESSIONS": {
    "type": "string",
    "default": "false",
    "description": "Whether to automatically unlock user sessions after resume from suspend"
  },
  "KEEP_INITRAMFS": {
    "type": "string",
    "default": "false",
    "description": "Whether to keep unpacked initramfs in RAM to accelerate suspension"
  }
}
