{
  "desc": {
    "type": "string",
    "default": "OpenStack Neutron DHCP agent (neutron-dhcp-agent)",
    "description": "Description string for the init script"
  },
  "project_name": {
    "type": "string",
    "default": "neutron",
    "description": "The OpenStack project name, used for paths, names, and variables"
  }
}
