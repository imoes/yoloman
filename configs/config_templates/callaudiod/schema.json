{
  "log_level": {
    "type": "number",
    "default": 2,
    "description": "Verbosity of logging: 0 (quiet) to 4 (debug). Higher values increase log detail."
  },
  "pid_file": {
    "type": "string",
    "default": "/run/callaudiod/callaudiod.pid",
    "description": "Path to the PID file used by the daemon for process management."
  },
  "sample_rate": {
    "type": "number",
    "default": 48000,
    "description": "Default audio sample rate (Hz) applied to routes unless overridden."
  },
  "echo_cancellation": {
    "type": "bool",
    "default": true,
    "description": "Globally enable echo cancellation for all routes supporting it."
  },
  "routes": {
    "type": "list",
    "default": [],
    "description": "List of named audio routing rules mapping inputs to outputs."
  },
  "default_policy": {
    "type": "string",
    "default": "normal",
    "description": "Name of the routing policy to activate at startup."
  },
  "policies": {
    "type": "object",
    "default": null,
    "description": "Named sets of route names to apply as presets."
  },
  "usb_overrides": {
    "type": "list",
    "default": [],
    "description": "Override rules for specific USB audio devices based on vendor/product ID."
  },
  "bt_sco.codec": {
    "type": "string",
    "default": "msbc",
    "description": "Preferred SCO codec: 'cvsd' (legacy) or 'msbc' (wideband, preferred).",
    "enum": [
      "cvsd",
      "msbc"
    ]
  },
  "bt_sco.max_connections": {
    "type": "number",
    "default": 1,
    "description": "Maximum simultaneous Bluetooth SCO connections allowed."
  }
}
