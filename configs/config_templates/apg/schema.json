{
  "apg_min_length": {
    "type": "number",
    "default": 8,
    "description": "Minimum password length"
  },
  "apg_max_length": {
    "type": "number",
    "default": 14,
    "description": "Maximum password length"
  },
  "apg_char_classes": {
    "type": "string",
    "default": "SNCL",
    "description": "Character set specification (S=special, N=numeric, C=consonant, L=vowel)"
  },
  "apg_char_classes_without_special": {
    "type": "string",
    "default": "NCL",
    "description": "Character set without special characters"
  },
  "apg_random_min_length": {
    "type": "number",
    "default": 20,
    "description": "Minimum length for random password generation"
  },
  "apg_random_max_length": {
    "type": "number",
    "default": 20,
    "description": "Maximum length for random password generation"
  }
}
