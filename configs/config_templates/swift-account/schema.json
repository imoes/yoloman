{
  "swift_account_name": {
    "type": "string",
    "default": "account",
    "description": "The base name for the Swift account service components (e.g., account-server)."
  },
  "swift_account_server_uwsgi_port": {
    "type": "number",
    "default": 6202,
    "description": "The port number on which the Swift account server uwsgi instance listens."
  },
  "swift_project_name": {
    "type": "string",
    "default": "swift",
    "description": "The OpenStack project name used for directory structures and variable prefixes."
  }
}
