{
  "config": {
    "type": "object",
    "default": "See sample_values",
    "description": "Top-level configuration object for ontospy settings"
  },
  "config.verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, enables verbose/debug mode as per --verbose flag"
  },
  "config.command": {
    "type": "string",
    "default": "scan",
    "description": "The subcommand to execute: scan, gendocs, lib, shell, ser, utils",
    "enum": [
      "scan",
      "gendocs",
      "lib",
      "shell",
      "ser",
      "utils"
    ]
  },
  "config.source": {
    "type": "string",
    "default": "/etc/ontospy/default.rdf",
    "description": "Path or URL to the RDF source to inspect"
  },
  "config.format": {
    "type": "string",
    "default": "html",
    "description": "Output format for serialization or documentation generation",
    "enum": [
      "html",
      "markdown"
    ]
  },
  "config.output_dir": {
    "type": "string",
    "default": "/var/lib/ontospy/docs",
    "description": "Directory to write output files for gendocs/ser commands"
  },
  "config.extra_args": {
    "type": "list",
    "default": [],
    "description": "Additional arguments to pass to the ontospy command"
  },
  "config.extra_args.items": {
    "type": "string",
    "default": "",
    "description": "A single extra argument string"
  }
}
