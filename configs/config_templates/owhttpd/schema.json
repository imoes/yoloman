{
  "name": {
    "type": "string",
    "default": "owhttpd",
    "description": "The service name used for the daemon binary, PID file, and log messages."
  },
  "pid_dir": {
    "type": "string",
    "default": "/var/run/owfs",
    "description": "Directory where the PID file is stored."
  }
}
