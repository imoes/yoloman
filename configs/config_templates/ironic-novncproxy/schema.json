{
  "UWSGI_PORT": {
    "type": "number",
    "default": 6080,
    "description": "The TCP port on which the NoVNC proxy listens."
  },
  "UWSGI_INI_PATH": {
    "type": "string",
    "default": "/etc/ironic/novncproxy.ini",
    "description": "Path to the uWSGI configuration file."
  },
  "UWSGI_INI_APP": {
    "type": "string",
    "default": "/etc/ironic/novncproxy.ini",
    "description": "Path to the uWSGI application file (same as ini path in this context)."
  },
  "SYSTEM_USER": {
    "type": "string",
    "default": "ironic",
    "description": "The system user account running the service."
  },
  "SYSTEM_GROUP": {
    "type": "string",
    "default": "ironic",
    "description": "The system group account running the service."
  }
}
