{
  "host": {
    "type": "string",
    "default": "localhost",
    "description": "Host or IP under test"
  },
  "port": {
    "type": "number",
    "default": 8080,
    "description": "TCP port under test"
  },
  "timeout": {
    "type": "number",
    "default": 0,
    "description": "Timeout in seconds, zero for no timeout"
  },
  "strict": {
    "type": "bool",
    "default": false,
    "description": "Only execute subcommand if the test succeeds"
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Don't output any status messages"
  },
  "command": {
    "type": "list",
    "default": [],
    "description": "Command and args to execute after the test finishes. Items should be strings (e.g., ['python', 'app.py']). Empty list means just wait/check availability without executing a subcommand."
  }
}
