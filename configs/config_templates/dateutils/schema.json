{
  "enable_aliases": {
    "type": "bool",
    "default": false,
    "description": "Whether to emit shell alias definitions."
  },
  "aliases": {
    "type": "list",
    "default": [],
    "description": "List of shell aliases for common dateutils commands. Each item must specify tool and optionally options."
  }
}
