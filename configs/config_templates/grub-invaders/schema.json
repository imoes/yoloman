{
  "grub_invaders_image_path": {
    "type": "string",
    "default": "/boot/invaders.exec",
    "description": "Path to the GRUB Invaders multiboot image, relative to root filesystem (typically /boot/invaders.exec)"
  }
}
