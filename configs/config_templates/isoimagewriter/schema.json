{
  "lang": {
    "type": "string",
    "default": "en",
    "description": "The language code for the application interface (e.g., en, de, fr). Matches the --lang CLI option."
  },
  "dir": {
    "type": "string",
    "default": "/tmp/isoimagewriter",
    "description": "The directory path used by the application for temporary files or data. Matches the --dir CLI option."
  }
}
