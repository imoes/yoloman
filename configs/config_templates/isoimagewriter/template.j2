# Configuration for isoimagewriter
# The language code for the application interface (e.g., en, de, fr). Matches the --lang CLI option.
--lang {{ lang | default('en') }}

# The directory path used by the application for temporary files or data. Matches the --dir CLI option.
--dir {{ dir | default('/tmp/isoimagewriter') }}