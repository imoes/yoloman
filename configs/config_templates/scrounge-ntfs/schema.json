{
 "cluster_size": {
  "default": "",
  "description": "The cluster size in sectors. If empty, defaults to 8.",
  "type": "string"
 },
 "cmd": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "disk": {
  "default": "/dev/sda",
  "description": "The raw device used to access the disk which contains the NTFS partition to rescue files from.",
  "type": "string"
 },
 "end": {
  "default": 0,
  "description": "The end of the NTFS partition in sectors.",
  "type": "number"
 },
 "mft_offset": {
  "default": "",
  "description": "The location of the MFT from the beginning of the partition in sectors. If empty, no directory information can be used.",
  "type": "string"
 },
 "output_dir": {
  "default": "/mnt/recovery",
  "description": "Directory to put rescued files in.",
  "type": "string"
 },
 "start": {
  "default": 0,
  "description": "The beginning of the NTFS partition in sectors.",
  "type": "number"
 }
}
