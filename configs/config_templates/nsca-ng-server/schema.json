{
  "command_file": {
    "type": "string",
    "default": "/var/run/icinga2/cmd/icinga2.cmd",
    "description": "Path to the Icinga command file."
  },
  "listen_address": {
    "type": "string",
    "default": "localhost:5668",
    "description": "Address and port to listen on."
  },
  "run_user": {
    "type": "string",
    "default": "nagios",
    "description": "System user to run the daemon as."
  },
  "pid_file": {
    "type": "string",
    "default": "/var/run/nsca-ng/nsca-ng.pid",
    "description": "Path to the PID file."
  },
  "default_auth_password": {
    "type": "string",
    "default": "change-me",
    "description": "Default password for the wildcard authorize section."
  }
}
