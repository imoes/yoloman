{
  "name": {
    "type": "string",
    "default": "nsca-ng",
    "description": "The base name of the daemon service."
  },
  "desc": {
    "type": "string",
    "default": "monitoring command acceptor",
    "description": "Description of the service displayed in logs and status messages."
  },
  "pid_dir": {
    "type": "string",
    "default": "/var/run/nsca-ng/",
    "description": "Directory where the PID file is stored."
  },
  "cfg_file": {
    "type": "string",
    "default": "/etc/nsca-ng/nsca-ng.cfg",
    "description": "Path to the main configuration file."
  }
}
