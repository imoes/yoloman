{
  "interface": {
    "type": "string",
    "default": "eth0",
    "description": "Network interface to listen on for ARP requests."
  },
  "network": {
    "type": "string",
    "default": "unconfigured",
    "description": "IPv4 network (CIDR or IP range) for which farpd will answer ARP requests; empty string means all addresses on the interface."
  }
}
