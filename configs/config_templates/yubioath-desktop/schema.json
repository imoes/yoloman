{
  "log_level": {
    "type": "string",
    "default": "INFO",
    "description": "The verbosity level for application logs. Must be one of DEBUG, INFO, WARNING, ERROR, CRITICAL.",
    "enum": [
      "DEBUG",
      "INFO",
      "WARNING",
      "ERROR",
      "CRITICAL"
    ]
  },
  "log_file": {
    "type": "string",
    "default": "",
    "description": "Path to the file where logs should be written. If empty, logs are printed to standard output."
  },
  "ui_themes": {
    "type": "list",
    "default": [
      "dark",
      "light"
    ],
    "description": "List of available UI themes for the desktop application."
  },
  "auto_start_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether the application should start automatically upon login."
  }
}
