{
  "rarpd_opts": {
    "type": "string",
    "default": "-e -v",
    "description": "Command-line options passed to rarpd via the OPTS variable in the init script. Common flags: -e (skip image checking), -v (verbose), -b <dir> (set boot directory), <interface> (limit to specific interface)."
  }
}
