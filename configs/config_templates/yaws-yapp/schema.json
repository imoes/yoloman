{
  "ebin_dir": {
    "type": "string",
    "default": "/usr/lib/yapp/ebin",
    "description": "Path to the directory where yapp beam code is placed."
  },
  "runmod": {
    "type": "string",
    "default": "yapp",
    "description": "Module to start yapp at startup."
  },
  "port": {
    "type": "number",
    "default": 8080,
    "description": "Port for the yapp virtual server."
  },
  "listen": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to listen on for the yapp virtual server."
  },
  "docroot": {
    "type": "string",
    "default": "/usr/share/yaws-yapp/www",
    "description": "Document root for the yapp virtual server."
  },
  "dir_listings": {
    "type": "bool",
    "default": true,
    "description": "Enable directory listings for the yapp virtual server."
  },
  "arg_rewrite_mod": {
    "type": "string",
    "default": "yapp",
    "description": "Module for argument rewriting in the yapp virtual server."
  },
  "yapp_server_id": {
    "type": "string",
    "default": "yappexample",
    "description": "Server ID for the yapp instance."
  },
  "bootstrap_yapps": {
    "type": "string",
    "default": "yapp",
    "description": "Yapps to bootstrap when the server starts."
  }
}
