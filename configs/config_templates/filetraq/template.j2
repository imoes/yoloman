# filetraq cron job configuration
# This template generates a crontab entry for the filetraq utility.
# filetraq monitors filesystem changes and logs them.

# Interval in minutes between filetraq executions
# Default: 5
*/{{ interval_minutes | default(5) }} * * * * root [ -x /usr/sbin/filetraq ] && /usr/sbin/filetraq