{
  "identity": {
    "type": "string",
    "default": "web-checker",
    "description": "Identity string for the client. Defaults to hostname if not set."
  },
  "password": {
    "type": "string",
    "default": "8a5UMsMzZhu6sSPkSmSaqC3HjMGCLwdt",
    "description": "Password used for encryption/authentication."
  },
  "server": {
    "type": "string",
    "default": "monitoring.example.com",
    "description": "Hostname or IP address of the NSCA server."
  },
  "tls_ciphers": {
    "type": "string",
    "default": "PSK-AES256-CBC-SHA",
    "description": "TLS cipher suites allowed for the connection."
  },
  "delay": {
    "type": "number",
    "default": 2,
    "description": "Delay in seconds before sending data."
  },
  "port": {
    "type": "number",
    "default": 5668,
    "description": "Port number of the NSCA server."
  },
  "timeout": {
    "type": "number",
    "default": 10,
    "description": "Timeout in seconds for the connection attempt."
  }
}
