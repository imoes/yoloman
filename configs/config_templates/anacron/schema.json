{
  "shell": {
    "type": "string",
    "default": "/bin/sh",
    "description": "Shell used to execute cron commands"
  },
  "path": {
    "type": "string",
    "default": "/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin",
    "description": "PATH environment variable for cron jobs"
  },
  "minute": {
    "type": "string",
    "default": "30",
    "description": "Minute when the job runs"
  },
  "hour_start": {
    "type": "string",
    "default": "7",
    "description": "Starting hour of the hourly range"
  },
  "hour_end": {
    "type": "string",
    "default": "23",
    "description": "Ending hour of the hourly range"
  }
}
