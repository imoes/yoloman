{
  "enable_ipmievd": {
    "type": "bool",
    "default": true,
    "description": "Whether the ipmievd daemon should be started on boot (maps to ENABLED variable in init script)."
  },
  "ipmievd_options": {
    "type": "string",
    "default": "open daemon",
    "description": "Options passed to ipmievd (e.g., 'open daemon' or 'sel daemon')."
  },
  "reqmodules": {
    "type": "string",
    "default": "",
    "description": "Space-separated list of kernel modules to load before starting ipmievd (maps to REQMODULES variable)."
  }
}
