{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options to pass to glBSP. Each element must be a valid glBSP option (e.g., '-h', '-q', '-f'). Order matters; duplicates allowed if semantically meaningful."
  }
}
