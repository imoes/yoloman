{
  "category": {
    "type": "string",
    "default": "neutral",
    "description": "Joke category. Valid options: neutral, adult, chuck, all.",
    "enum": [
      "neutral",
      "adult",
      "chuck",
      "all"
    ]
  },
  "language": {
    "type": "string",
    "default": "en",
    "description": "Joke language. Valid options: en, de, es.",
    "enum": [
      "en",
      "de",
      "es"
    ]
  }
}
