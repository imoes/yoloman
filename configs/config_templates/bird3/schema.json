{
  "bird_run_user": {
    "type": "string",
    "default": "bird",
    "description": "System user under which BIRD runs."
  },
  "bird_run_group": {
    "type": "string",
    "default": "bird",
    "description": "System group under which BIRD runs."
  },
  "bird_args": {
    "type": "string",
    "default": "",
    "description": "Additional command-line arguments passed to the BIRD daemon. Leave empty to use defaults."
  }
}
