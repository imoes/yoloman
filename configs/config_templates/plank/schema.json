{
  "impl": {
    "type": "string",
    "default": "launchpad",
    "description": "The implementation backend for Plank."
  },
  "project": {
    "type": "string",
    "default": "plank",
    "description": "The project name associated with Plank."
  },
  "bug_pattern_base": {
    "type": "string",
    "default": "None",
    "description": "Base URL or pattern for bug reporting. Set to a string URL or None."
  }
}
