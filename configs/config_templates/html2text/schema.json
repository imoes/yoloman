{
 "ascii": {
  "default": false,
  "description": "Whether to force ASCII output.",
  "type": "bool"
 },
 "from_encoding": {
  "description": "Encoding of the input file. Empty string means auto-detect.",
  "enum": [
   "UTF-8",
   "ISO-8859-1",
   "ASCII//TRANSLIT",
   "ASCII"
  ],
  "type": "string"
 },
 "images": {
  "default": false,
  "description": "Whether to render images with alt text.",
  "type": "bool"
 },
 "indent": {
  "default": 0,
  "description": "Number of spaces to use for indentation of list items and paragraphs.",
  "type": "number"
 },
 "links": {
  "default": false,
  "description": "Whether to render links as numbered references.",
  "type": "bool"
 },
 "nimages": {
  "default": false,
  "description": "Whether to ignore images entirely (alias for images=false).",
  "type": "bool"
 },
 "paragraphs": {
  "default": 1,
  "description": "Number of blank lines between paragraphs.",
  "type": "number"
 },
 "rules": {
  "default": true,
  "description": "Whether to render horizontal rules.",
  "type": "bool"
 },
 "tables": {
  "default": true,
  "description": "Whether to render tables.",
  "type": "bool"
 },
 "to_encoding": {
  "default": "UTF-8",
  "description": "Encoding of the output file. Empty string means UTF-8.",
  "enum": [
   "UTF-8",
   "ASCII//TRANSLIT",
   "ASCII"
  ],
  "type": "string"
 },
 "width": {
  "default": 79,
  "description": "Width of the output text line in characters.",
  "type": "number"
 }
}
