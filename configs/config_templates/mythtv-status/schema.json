{
  "cron_schedule": {
    "type": "string",
    "default": "*/10 *",
    "description": "The time schedule for the cron job (e.g., every 10 minutes)."
  },
  "cron_user": {
    "type": "string",
    "default": "root",
    "description": "The user under which the cron job executes."
  },
  "cron_command_path": {
    "type": "string",
    "default": "/usr/sbin/mythtv-update-motd",
    "description": "The path to the mythtv-update-motd executable."
  }
}
