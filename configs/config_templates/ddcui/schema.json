{
  "ddcui": {
    "type": "object",
    "description": "ddcui command-line options configuration block. Only set fields needed to override defaults or enable features. All keys are optional unless noted otherwise."
  },
  "ddcui.feature_set": {
    "type": "string",
    "default": "MCCS",
    "description": "Initial feature set to show. Valid values: MCCS, MANUFACTURER, COLOR, CUSTOM",
    "enum": [
      "MCCS",
      "MANUFACTURER",
      "COLOR"
    ]
  },
  "ddcui.custom_features": {
    "type": "list",
    "default": [],
    "description": "List of feature codes (hex strings) for CUSTOM feature set. Codes: 2-char hex, optionally prefixed with 'x' or suffixed with 'h'. E.g. '10', 'x12', '14h'. Ignored unless feature_set is CUSTOM."
  },
  "ddcui.only_capabilities": {
    "type": "bool",
    "default": false,
    "description": "Restrict features to those in the capabilities string. Applies when feature_set is MCCS, COLOR, or MANUFACTURER."
  },
  "ddcui.all_capabilities": {
    "type": "bool",
    "default": false,
    "description": "Always include features found in the capabilities string. Applies when feature_set is MCCS."
  },
  "ddcui.show_unsupported": {
    "type": "bool",
    "default": false,
    "description": "Display unsupported features in the feature set."
  },
  "ddcui.nc_values_source": {
    "type": "string",
    "default": "",
    "description": "Source of values shown in the drop-down for Non-Continuous features. Valid: MCCS, CAPABILITIES, BOTH",
    "enum": [
      "MCCS",
      "CAPABILITIES",
      "BOTH"
    ]
  },
  "ddcui.use_latest_nc_values": {
    "type": "bool",
    "default": false,
    "description": "Always use values from latest MCCS version."
  },
  "ddcui.model": {
    "type": "string",
    "default": "",
    "description": "Model name as specified in the monitor's EDID and shown on the toolbar."
  },
  "ddcui.style": {
    "type": "string",
    "default": "",
    "description": "Qt style name (e.g., 'Fusion', 'Windows'). List styles via --styles."
  },
  "ddcui.ddc_errors_to_terminal": {
    "type": "bool",
    "default": false,
    "description": "Report DDC protocol and data errors to the terminal."
  },
  "ddcui.trace_groups": {
    "type": "list",
    "default": [],
    "description": "List of trace group names to trace. May be specified more than once."
  },
  "ddcui.trace_files": {
    "type": "list",
    "default": [],
    "description": "List of filenames (simple-file-name) to trace. May be specified more than once."
  },
  "ddcui.trace_functions": {
    "type": "list",
    "default": [],
    "description": "List of function names to trace. May be specified more than once."
  },
  "ddcui.timestamp": {
    "type": "bool",
    "default": false,
    "description": "Prefix trace messages with timestamp."
  },
  "ddcui.thread_id": {
    "type": "bool",
    "default": false,
    "description": "Prefix trace messages with thread id."
  }
}
