{
  "IF_METRIC_ENV_VAR": {
    "type": "string",
    "default": "${IF_METRIC}",
    "description": "Environment variable name holding the metric value"
  },
  "IF_METRIC_VALUE": {
    "type": "string",
    "default": "${IF_METRIC}",
    "description": "The actual metric value passed to ifmetric"
  }
}
