{
  "maxconn": {
    "type": "number",
    "default": 2000,
    "description": "Maximum number of concurrent connections for the global section"
  },
  "user": {
    "type": "string",
    "default": "haproxy",
    "description": "User under which HAProxy runs"
  },
  "group": {
    "type": "string",
    "default": "haproxy",
    "description": "Group under which HAProxy runs"
  },
  "log_level": {
    "type": "string",
    "default": "global",
    "description": "Logging level for defaults section"
  },
  "mode": {
    "type": "string",
    "default": "http",
    "description": "Operating mode (tcp or http)",
    "enum": [
      "http",
      "tcp",
      "health"
    ]
  },
  "timeout_connect": {
    "type": "string",
    "default": "5000ms",
    "description": "Maximum time to wait for a connection attempt to a server"
  },
  "timeout_client": {
    "type": "string",
    "default": "50000ms",
    "description": "Maximum inactivity time on the client side"
  },
  "timeout_server": {
    "type": "string",
    "default": "50000ms",
    "description": "Maximum inactivity time on the server side"
  },
  "http_port": {
    "type": "number",
    "default": 80,
    "description": "Port for the frontend HTTP listener"
  },
  "backend_name": {
    "type": "string",
    "default": "web_servers",
    "description": "Name of the backend section"
  },
  "balance_algorithm": {
    "type": "string",
    "default": "roundrobin",
    "description": "Load balancing algorithm for the backend"
  },
  "backend_servers": {
    "type": "list",
    "default": [],
    "description": "List of backend servers; each item has 'name', 'address', 'port', and optional 'check'"
  }
}
