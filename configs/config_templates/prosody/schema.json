{
  "virtual_host_name": {
    "type": "string",
    "default": "localhost",
    "description": "The primary hostname/domain for the Prosody server virtual host."
  },
  "user": {
    "type": "string",
    "default": "prosody",
    "description": "System user account under which Prosody runs."
  }
}
