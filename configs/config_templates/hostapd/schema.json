{
  "daemon_conf": {
    "type": "string",
    "default": "",
    "description": "Absolute path to hostapd configuration file (e.g., '/etc/hostapd/hostapd.conf')"
  },
  "daemon_opts": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options for hostapd daemon (excluding -B and -P, which are auto-configured)"
  }
}
