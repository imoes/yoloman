{
  "audit_rules": {
    "type": "list",
    "default": [],
    "description": "List of generated SELinux policy rules (TE allow/dontaudit statements) produced by audit2allow. Each entry is a single rule string."
  }
}
