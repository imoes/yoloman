{
  "audit_rules": {
    "type": "list",
    "default": [],
    "description": "List of raw audit rules to include in /etc/audit/audit.rules. Each entry is a single rule string as accepted by auditctl.",
    "items": {
      "type": "string",
      "description": "A single audit rule in auditctl syntax (e.g., '-a always,exit -F arch=b64 -S execve')."
    }
  }
}
