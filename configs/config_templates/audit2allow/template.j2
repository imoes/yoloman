# Audit rules for auditd (generated by audit2allow workflow)
# Use auditctl to load these rules at boot
# Rules are applied in order; order can matter for exclusions
# Format: -a <list>,<action> -F <field>=<value> ...
# See auditctl(8) for full syntax
{% for rule in audit_rules %}
{{ rule | default('# Invalid rule') }}
{% endfor %}