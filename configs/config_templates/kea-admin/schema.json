{
  "command": {
    "type": "string",
    "default": "db-init",
    "description": "The kea-admin command to execute: db-init, db-version, db-upgrade, lease-dump, lease-upload, or stats-recount.",
    "enum": [
      "db-init",
      "db-version",
      "db-upgrade",
      "lease-dump",
      "lease-upload",
      "stats-recount"
    ]
  },
  "backend": {
    "type": "string",
    "default": "pgsql",
    "description": "The database backend type: memfile, mysql, or pgsql.",
    "enum": [
      "memfile",
      "mysql",
      "pgsql"
    ]
  },
  "host": {
    "type": "string",
    "default": "localhost",
    "description": "Hostname when connecting to the database."
  },
  "port": {
    "type": "number",
    "default": 5432,
    "description": "Port when connecting to the database. Defaults to client default if not specified."
  },
  "username": {
    "type": "string",
    "default": "kea_admin",
    "description": "Username when connecting to the database."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Password when connecting to the database. If empty, KEA_ADMIN_DB_PASSWORD env var is used or prompt occurs."
  },
  "database_name": {
    "type": "string",
    "default": "kea",
    "description": "Name of the database to connect to."
  },
  "script_directory": {
    "type": "string",
    "default": "/usr/share/kea/scripts",
    "description": "Override scripts directory used during upgrades/initialization."
  },
  "output_file": {
    "type": "string",
    "default": "/var/lib/kea/leases.csv",
    "description": "File to which lease data will be dumped (required for lease-dump)."
  },
  "input_file": {
    "type": "string",
    "default": "/var/lib/kea/leases_import.csv",
    "description": "CSV file with leases to be uploaded (required for lease-upload)."
  },
  "ip_version": {
    "type": "string",
    "default": "4",
    "description": "IP version flag for lease dump/upload: 4 or 6.",
    "enum": [
      "4",
      "6"
    ]
  },
  "extra_args": {
    "type": "list",
    "default": [],
    "description": "Extra arguments to pass to the database command tool (e.g., --ssl). Items are strings."
  },
  "items": {
    "type": "string",
    "default": "",
    "description": "Placeholder for list items in extra_args"
  },
  "yes": {
    "type": "bool",
    "default": false,
    "description": "Assume yes on overwriting temporary files."
  }
}
