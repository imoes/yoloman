{
  "dillo_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable the Claws Mail Dillo plugin to render HTML messages"
  },
  "dillo_binary": {
    "type": "string",
    "default": "/usr/bin/dillo",
    "description": "Absolute path to the Dillo browser executable"
  },
  "fetch_remote_urls": {
    "type": "bool",
    "default": false,
    "description": "Allow Dillo to fetch remote URLs (e.g., images, scripts); disabled by default for security"
  },
  "dillo_args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments to pass to Dillo on startup"
  }
}
