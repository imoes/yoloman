{
  "stderr": {
    "type": "bool",
    "default": false,
    "description": "Log to standard error instead of syslog"
  },
  "cgroup": {
    "type": "string",
    "default": "",
    "description": "Filter events for specific cgroup path (e.g., /sys/fs/cgroup/mygroup)"
  },
  "libdir": {
    "type": "string",
    "default": "",
    "description": "Extra plugin directory (overrides default /usr/local/lib64/bpftune)"
  },
  "learning_rate": {
    "type": "number",
    "default": 4,
    "description": "Learning rate level: 0=\u00b11.0625%, 1=\u00b13.125%, 2=\u00b16.25%, 3=\u00b112.5%, 4=\u00b125%"
  },
  "rollback": {
    "type": "bool",
    "default": false,
    "description": "Roll back sysctl settings on exit"
  },
  "port": {
    "type": "number",
    "default": 0,
    "description": "TCP port to listen on for queries (0 = use ephemeral port)"
  }
}
