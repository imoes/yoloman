{
  "name": {
    "type": "string",
    "default": "moosefs-chunkserver",
    "description": "Name of the service, used for PID file location and /etc/default file."
  },
  "cfgfile": {
    "type": "string",
    "default": "/etc/mfs/mfschunkserver.cfg",
    "description": "Path to the MooseFS chunkserver configuration file."
  },
  "retry": {
    "type": "string",
    "default": "TERM/60/KILL/5",
    "description": "Stop retry strategy for start-stop-daemon."
  }
}
