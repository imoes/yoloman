{
  "server_num": {
    "type": "number",
    "default": 99,
    "description": "The X server display number. Xvfb will listen on :N. Must be a non-negative integer."
  },
  "extra_args": {
    "type": "string",
    "default": "-screen 0 1280x1024x24 -shmem",
    "description": "Additional command-line arguments passed to Xvfb. Supports -screen, -pixdepths, -fbdir, -shmem, -linebias, -blackpixel, -whitepixel."
  }
}
