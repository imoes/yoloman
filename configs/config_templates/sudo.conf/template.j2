# Path definitions for sudoers, logs, and temporary files
# Set the directory where sudoers file(s) are stored
# Set the directory where sudo logs are written
# Set the path to the sudoers.tmp file used during visudo
# Set the path to the sudo log file (if logging is enabled)
# Set the path to the sudo time stamp directory (for timeout tracking)
# Enable or disable verbose logging
# Enable or disable authentication via tty (e.g., require password even for local users)
# Set the maximum number of authentication attempts before aborting
# Set the timeout (in minutes) after which sudo requires re-authentication
# Set the list of plugins to load (path and options)
# Set the list of env_keep variables (preserved across sudo)
{% for line in lines %}{{ line }}
{% endfor %}