{
  "ikey": {
    "type": "string",
    "default": "",
    "description": "Duo integration key for the service."
  },
  "skey": {
    "type": "string",
    "default": "",
    "description": "Duo secret key for the service."
  },
  "host": {
    "type": "string",
    "default": "",
    "description": "Duo API host (e.g., api-xxxxx.duosecurity.com)."
  },
  "failmode": {
    "type": "string",
    "default": "safe",
    "description": "Failover behavior: 'safe' allows login on error, 'secure' denies it.",
    "enum": [
      "safe",
      "secure"
    ]
  }
}
