{
  "lyskom_server_cron_schedule": {
    "type": "string",
    "default": "0,10,20,30,40,50 * * * *",
    "description": "Cron schedule for the lyskom-server update job."
  },
  "lyskom_server_cron_user": {
    "type": "string",
    "default": "root",
    "description": "User to run the cron job as."
  },
  "lyskom_server_cron_command_path": {
    "type": "string",
    "default": "/usr/sbin/updateLysKOM",
    "description": "Path to the updateLysKOM script."
  }
}
