{
  "log_file": {
    "type": "string",
    "default": "/var/log/parser3/parser3.log",
    "description": "Path to the parser3 error log file. If not set, defaults to parser3.log in the script directory."
  },
  "verbose_errors": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose error reporting to stderr if log file writing fails."
  },
  "debug_mode": {
    "type": "number",
    "default": 0,
    "description": "Enable debug mode (0=disabled, 1=enabled)."
  },
  "cgi_timeout": {
    "type": "number",
    "default": 30,
    "description": "Maximum execution time for CGI scripts in seconds."
  },
  "allowed_mime_types": {
    "type": "list",
    "default": [
      "text/html"
    ],
    "description": "List of allowed MIME types for CGI output."
  }
}
