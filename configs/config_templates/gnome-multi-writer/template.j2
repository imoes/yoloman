# GNOME Multi Writer CLI configuration
# This file defines command-line options for gnome-multi-writer invocation.
# It is consumed by wrapper scripts or launchers.
# Currently, only the verbose/debug output option is supported.

{% set verbose = verbose | default(False) %}
# verbose: Enable verbose output (debugging information)
{% if verbose %}
--verbose
{% endif %}