{
  "black": {
    "type": "bool",
    "default": false,
    "description": "Style HTML output to use a black background with white text."
  },
  "css_file": {
    "type": "string",
    "default": "",
    "description": "Path to CSS file to link in HTML header."
  },
  "input_file": {
    "type": "string",
    "default": "",
    "description": "Input file instead of stdin."
  },
  "iso_encoding": {
    "type": "number",
    "default": 1,
    "description": "ISO-8859-X encoding (X: 1\u201316) instead of UTF-8."
  },
  "line_fix": {
    "type": "bool",
    "default": false,
    "description": "Fix inputs using control sequences to change cursor position."
  },
  "lang": {
    "type": "string",
    "default": "en",
    "description": "ISO-639-1 language code for HTML lang attribute.",
    "enum": [
      "aa",
      "ab",
      "am",
      "an",
      "as",
      "de",
      "en",
      "es",
      "fr",
      "ie",
      "io",
      "is",
      "it",
      "or",
      "so",
      "to",
      "zu"
    ]
  },
  "no_header": {
    "type": "bool",
    "default": false,
    "description": "Do not enclose HTML in <html>, <head>, and <body> tags."
  },
  "pink": {
    "type": "bool",
    "default": false,
    "description": "Style HTML output to use a pink background."
  },
  "ignore_cr": {
    "type": "bool",
    "default": false,
    "description": "Ignore carriage-returns (\\r)."
  },
  "stylesheet": {
    "type": "bool",
    "default": false,
    "description": "Use external stylesheet instead of inline styles."
  },
  "title": {
    "type": "string",
    "default": "stdin",
    "description": "HTML document title."
  },
  "word_wrap": {
    "type": "bool",
    "default": false,
    "description": "Wrap long lines in HTML output (CSS3)."
  },
  "no_xml": {
    "type": "bool",
    "default": false,
    "description": "Use <!DOCTYPE html> instead of XML declaration."
  },
  "style": {
    "type": "string",
    "default": "",
    "description": "Inline <body> style string."
  }
}
