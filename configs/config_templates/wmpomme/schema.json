{
  "display": {
    "type": "string",
    "default": ":0",
    "description": "The X server display to connect to, corresponding to the -display argument."
  },
  "refresh_interval": {
    "type": "number",
    "default": null,
    "description": "The update time interval in seconds, corresponding to the -r argument. Null to omit."
  }
}
