{
  "slurm_path": {
    "type": "string",
    "default": "/usr",
    "description": "Root of the slurm installation"
  },
  "srun": {
    "type": "string",
    "default": "/usr/bin/srun",
    "description": "Path to the srun tool to run parallel jobs in a slurm cluster"
  },
  "sbatch": {
    "type": "string",
    "default": "/usr/bin/sbatch",
    "description": "Path to the sbatch tool to submit jobs to a slurm cluster"
  },
  "salloc": {
    "type": "string",
    "default": "/usr/bin/salloc",
    "description": "Path to the salloc tool to create a slurm job allocation"
  },
  "scancel": {
    "type": "string",
    "default": "/usr/bin/scancel",
    "description": "Path to the scancel tool to cancel a slurm job"
  },
  "scontrol": {
    "type": "string",
    "default": "/usr/bin/scontrol",
    "description": "Path to the scontrol tool to view the status of a slurm job"
  },
  "openmpi_path": {
    "type": "string",
    "default": "",
    "description": "Path to the openmpi implementation if available"
  },
  "mpich2_path": {
    "type": "string",
    "default": "",
    "description": "Path to the mpich2 implementation if available"
  }
}
