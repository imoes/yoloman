{
  "name": {
    "type": "string",
    "default": "ibus-mozc GNOME Initial Setup",
    "description": "The display name for the desktop entry."
  },
  "exec": {
    "type": "string",
    "default": "/usr/share/ibus-mozc/ibus-mozc-gnome-initial-setup.sh",
    "description": "The command to execute for the desktop entry."
  },
  "only_show_in": {
    "type": "string",
    "default": "GNOME;",
    "description": "Semicolon-separated list of desktop environments where this application should be shown."
  },
  "no_display": {
    "type": "bool",
    "default": true,
    "description": "If true, the application will not be displayed in the application menu."
  },
  "autostart_condition": {
    "type": "string",
    "default": "unless-exists ibus-mozc-gnome-initial-setup-done",
    "description": "Condition for autostarting the application."
  }
}
