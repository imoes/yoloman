{
  "hostname": {
    "type": "string",
    "default": "",
    "description": "Optional externally visible hostname to appear in error messages and links (unquoted; set empty to omit)"
  },
  "NO_CRON_RUN": {
    "type": "string",
    "default": "",
    "description": "If non-empty in /etc/default/apt-cacher-ng, disables this script's execution"
  }
}
