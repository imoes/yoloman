{
  "data_dir": {
    "type": "string",
    "default": "~/.local/share/zoxide",
    "description": "Path for zoxide data files (_ZO_DATA_DIR)"
  },
  "echo": {
    "type": "bool",
    "default": false,
    "description": "Print the matched directory before navigating to it (_ZO_ECHO)"
  },
  "exclude_dirs": {
    "type": "list",
    "default": [
      "/tmp",
      "/dev"
    ],
    "description": "List of directory globs to be excluded from the database (_ZO_EXCLUDE_DIRS)"
  },
  "max_age": {
    "type": "number",
    "default": 180,
    "description": "Maximum total age in days after which entries start getting deleted (_ZO_MAXAGE)"
  },
  "resolve_symlinks": {
    "type": "bool",
    "default": false,
    "description": "Resolve symlinks when storing paths (_ZO_RESOLVE_SYMLINKS)"
  },
  "fzf_opts": {
    "type": "list",
    "default": [],
    "description": "Custom flags to pass to fzf (_ZO_FZF_OPTS)"
  }
}
