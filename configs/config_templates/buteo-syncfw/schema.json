{
  "service_name": {
    "type": "string",
    "default": "com.meego.msyncd",
    "description": "D-Bus service name for the synchronizer daemon"
  },
  "interface_name": {
    "type": "string",
    "default": "com.meego.msyncd",
    "description": "D-Bus interface name used by the synchronizer daemon"
  },
  "path": {
    "type": "string",
    "default": "/synchronizer",
    "description": "D-Bus object path for the synchronizer daemon"
  }
}
