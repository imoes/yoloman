{
  "geometry": {
    "type": "string",
    "default": "100x140",
    "description": "Initial window geometry widthxheight."
  },
  "count": {
    "type": "number",
    "default": 4,
    "description": "Number of update intervals to keep in history."
  },
  "sec": {
    "type": "number",
    "default": 20,
    "description": "Duration of each update interval in seconds."
  },
  "wsec": {
    "type": "number",
    "default": 1080,
    "description": "Total seconds of the time window displayed."
  },
  "tmin": {
    "type": "number",
    "default": 10,
    "description": "Minimum temperature value for the Y-axis scale (Celsius)."
  },
  "tmax": {
    "type": "number",
    "default": 50,
    "description": "Maximum temperature value for the Y-axis scale (Celsius)."
  },
  "vmin": {
    "type": "number",
    "default": 1.8,
    "description": "Minimum voltage value for the Y-axis scale (Volts)."
  },
  "vmax": {
    "type": "number",
    "default": 2.2,
    "description": "Maximum voltage value for the Y-axis scale (Volts)."
  },
  "tick": {
    "type": "number",
    "default": 3,
    "description": "Number of ticks on the vertical axis."
  },
  "fahren": {
    "type": "bool",
    "default": false,
    "description": "Display temperatures in Fahrenheit if True, else Celsius."
  },
  "method": {
    "type": "string",
    "default": "V",
    "description": "Hardware access method: V (via), S (sys), I (isa), A (auto).",
    "enum": [
      "V",
      "S",
      "I",
      "A"
    ]
  },
  "probe_chip": {
    "type": "string",
    "default": "",
    "description": "Specific chip to probe (e.g., lm85, it87). Leave empty to auto-detect.",
    "enum": [
      "winbond",
      "wl784",
      "via686",
      "it87",
      "gl52",
      "lm85",
      "lm80",
      "lm90",
      "lm75"
    ]
  },
  "tyan_tiger_mp": {
    "type": "bool",
    "default": false,
    "description": "Enable support for Tyan Tiger MP motherboards."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug output."
  },
  "color_mb": {
    "type": "string",
    "default": "blue",
    "description": "Color for Motherboard Temperature 1."
  },
  "color_cpu": {
    "type": "string",
    "default": "red",
    "description": "Color for CPU Temperature 2."
  },
  "color_cs": {
    "type": "string",
    "default": "cyan",
    "description": "Color for Chip Temperature 3."
  },
  "color_vc": {
    "type": "string",
    "default": "green",
    "description": "Color for VCore Voltage."
  },
  "comment_mb": {
    "type": "string",
    "default": "MB",
    "description": "Label for Motherboard Temperature. Use 'NOTEMP' to hide."
  },
  "comment_cpu": {
    "type": "string",
    "default": "CPU",
    "description": "Label for CPU Temperature. Use 'NOTEMP' to hide."
  },
  "comment_cs": {
    "type": "string",
    "default": "chip",
    "description": "Label for Chip Temperature. Use 'NOTEMP' to hide."
  },
  "comment_vc": {
    "type": "string",
    "default": "Vc",
    "description": "Label for VCore Voltage."
  }
}
