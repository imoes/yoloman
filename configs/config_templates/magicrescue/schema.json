{
  "output_directory": {
    "type": "string",
    "default": "/tmp/magicrescue_output",
    "description": "The directory where recovered files will be saved. Must have sufficient free space and not be on the source device."
  },
  "blocksize": {
    "type": "number",
    "default": 512,
    "description": "The block size to consider for file starts. Usually set to the hardware sector size (e.g., 512) for best performance."
  },
  "recipes": {
    "type": "list",
    "default": [
      "jpeg-jfif"
    ],
    "description": "A list of recipe names or paths to use for identifying file types. Examples: jpeg-jfif, png, gif."
  },
  "input_file_list": {
    "type": "string",
    "default": "",
    "description": "Path to a file containing additional device names to scan, one per line. Empty string means use command line devices only."
  },
  "machine_readable_mode": {
    "type": "string",
    "default": "",
    "description": "The output mode for machine-readable stdout. Options are 'i', 'o', or 'io'. Empty string disables this mode.",
    "enum": [
      "i",
      "o",
      "io"
    ]
  },
  "resume_offset": {
    "type": "string",
    "default": "=",
    "description": "Offset to resume scanning from. Can be absolute (=), relative (+), or from EOF (-). Supports hex (0x) and sign.",
    "enum": [
      "=",
      "+",
      "-"
    ]
  },
  "devices": {
    "type": "list",
    "default": [
      "/dev/sda1"
    ],
    "description": "A list of block devices or files to scan for recoverable files."
  }
}
