{
  "name": {
    "type": "string",
    "default": "rspregistrar",
    "description": "The base name of the daemon and service."
  },
  "daemon_path": {
    "type": "string",
    "default": "/usr/bin/rspregistrar",
    "description": "The full path to the rsplib-registrar executable."
  },
  "log_level": {
    "type": "number",
    "default": 3,
    "description": "The logging level for the daemon."
  },
  "log_color": {
    "type": "string",
    "default": "off",
    "description": "Whether to enable colored log output.",
    "enum": [
      "on",
      "off"
    ]
  }
}
