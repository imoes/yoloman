{
  "exec_path": {
    "type": "string",
    "default": "/usr/libexec/notify-osd",
    "description": "Path to the notify-osd executable."
  },
  "terminal": {
    "type": "bool",
    "default": false,
    "description": "Whether to run the application in a terminal."
  },
  "type": {
    "type": "string",
    "default": "Application",
    "description": "Type of the desktop entry."
  },
  "notshowin": {
    "type": "list",
    "default": [
      "KDE"
    ],
    "description": "Desktop environments where this entry should not be shown."
  },
  "autostart_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether GNOME autostart is enabled for this entry."
  }
}
