{
  "path_default": {
    "type": "string",
    "default": "$PATH:~/bin",
    "description": "Default PATH value appended to the daemon's environment."
  },
  "pidfiles_default": {
    "type": "string",
    "default": "~/.pidfiles",
    "description": "Default directory where pidfiles are stored for daemons using the wildcard '*'."
  },
  "command_default": {
    "type": "string",
    "default": "/usr/bin/command",
    "description": "Default command path to execute when defining a named daemon rule."
  }
}
