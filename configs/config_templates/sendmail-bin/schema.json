{
  "prefix": {
    "type": "string",
    "default": "/usr",
    "description": "The installation prefix directory for Sendmail."
  },
  "exec_prefix": {
    "type": "string",
    "default": "/usr",
    "description": "The installation execution prefix directory."
  },
  "bindir": {
    "type": "string",
    "default": "/usr/bin",
    "description": "The directory for user-executable binaries."
  },
  "sbindir": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "The directory for system administrator binaries."
  },
  "libexecdir": {
    "type": "string",
    "default": "/usr/libexec/sendmail",
    "description": "The directory for non-user-invokable libraries and programs."
  },
  "datadir": {
    "type": "string",
    "default": "/usr/share",
    "description": "The read-only architecture-independent data directory."
  },
  "sysconfdir": {
    "type": "string",
    "default": "/etc",
    "description": "The read-only single-machine data directory for configuration files."
  },
  "sharedstatedir": {
    "type": "string",
    "default": "/usr/com",
    "description": "The directory for modifiable architecture-independent data."
  },
  "localstatedir": {
    "type": "string",
    "default": "/var",
    "description": "The directory for modifiable single-machine data (logs, spool, etc.)."
  },
  "libdir": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu",
    "description": "The directory for object code libraries."
  },
  "includedir": {
    "type": "string",
    "default": "/usr/include",
    "description": "The directory for C header files."
  },
  "infodir": {
    "type": "string",
    "default": "/usr/share/info",
    "description": "The directory for Info documentation."
  },
  "mandir": {
    "type": "string",
    "default": "/usr/share/man",
    "description": "The directory for man page documentation."
  },
  "docdir": {
    "type": "string",
    "default": "/usr/share/doc",
    "description": "The directory for package documentation."
  },
  "copies": {
    "type": "number",
    "default": 2,
    "description": "Number of backup copies to retain for sendmail configuration files."
  }
}
