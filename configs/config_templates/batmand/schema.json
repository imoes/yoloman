{
  "interface1": {
    "type": "string",
    "default": "eth1",
    "description": "First network interface for batmand to bind to"
  },
  "interface2": {
    "type": "string",
    "default": "eth2",
    "description": "Second network interface for batmand to bind to"
  }
}
