{
  "config": {
    "type": "list",
    "default": [],
    "description": "A list of configuration parameters to apply. Each item is a flat map of parameter name to value. Parameter names must match lsd CLI flags or TOML keys where applicable, but here we simplify to a list of key-value pairs for flexibility within Jinja2 constraints. Actually, per schema rules, I will define a single complex object `config` with specific fields."
  }
}
