{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose diagnostic output during execution."
  },
  "reffile": {
    "type": "string",
    "default": "",
    "description": "Path to a reference file to obtain ownership and access mode from."
  },
  "sources": {
    "type": "list",
    "default": [],
    "description": "List of source files or glob patterns to install/mimic into the destination."
  },
  "destination": {
    "type": "string",
    "default": "/usr/local/bin",
    "description": "The destination file path or directory where source files will be placed."
  }
}
