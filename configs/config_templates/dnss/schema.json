{
  "mode_flags": {
    "type": "string",
    "default": "--enable_dns_to_https",
    "description": "Flags controlling the runtime mode (DNS-over-HTTPS, DNS-over-gRPC, or gRPC-to-DNS)."
  },
  "monitoring_flag": {
    "type": "string",
    "default": "--monitoring_listen_addr=127.0.0.1:9981",
    "description": "Monitoring listen address; set to empty string to disable monitoring."
  }
}
