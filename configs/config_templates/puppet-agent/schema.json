{
 "daemon_opts": {
  "description": "Additional options passed to the Puppet daemon at startup.",
  "enum": [
   "--certname",
   "--daemonize",
   "--no-daemonize",
   "--debug",
   "--detailed-exitcodes",
   "--digest",
   "--disable",
   "--enable",
   "--evaltrace",
   "--fingerprint",
   "--help",
   "--job-id",
   "--logdest",
   "--masterport",
   "--noop",
   "--onetime",
   "--serverport",
   "--sourceaddress",
   "--tags",
   "--test",
   "--trace",
   "--verbose",
   "--version",
   "--waitforcert",
   "--write_catalog_summary",
   "--skip_tags"
  ],
  "type": "string"
 }
}
