{
  "idle_logfile": {
    "type": "string",
    "default": "",
    "description": "The filename for debug output. Prepend '+' to append. Leave empty for stderr."
  },
  "idle_debug": {
    "type": "string",
    "default": "all",
    "description": "Debug output type, e.g., 'all' for full debug."
  }
}
