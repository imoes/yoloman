{
  "compute_driver": {
    "type": "string",
    "default": "libvirt.LibvirtDriver",
    "description": "The compute driver to use for managing instances."
  },
  "virt_type": {
    "type": "string",
    "default": "qemu",
    "description": "The virtualization type used by libvirt (e.g., qemu, kvm)."
  }
}
