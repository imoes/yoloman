{
  "description": {
    "type": "string",
    "default": "SiriDB Server Configuration",
    "description": "A descriptive title for this configuration instance."
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "The initial log level for the server. Valid values: debug, info, warning, error, critical.",
    "enum": [
      "debug",
      "info",
      "warning",
      "error",
      "critical"
    ]
  },
  "log_colorized": {
    "type": "bool",
    "default": true,
    "description": "Whether to enable colorized logging output."
  }
}
