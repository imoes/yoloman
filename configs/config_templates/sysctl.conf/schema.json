{
  "sysctl_params": {
    "type": "list",
    "default": [],
    "description": "List of sysctl parameters to configure. Each entry specifies a kernel parameter and its value.",
    "items": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "default": "",
          "description": "The sysctl parameter name (e.g., net.ipv4.ip_forward)."
        },
        "value": {
          "type": "string",
          "default": "",
          "description": "The value to assign (as string; e.g., '1', '0', '32768')."
        },
        "description": {
          "type": "string",
          "default": "",
          "description": "A human-readable explanation of what the setting does."
        }
      },
      "required": [
        "key",
        "value",
        "description"
      ]
    }
  }
}
