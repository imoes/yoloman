{
  "enable": {
    "type": "string",
    "default": "no",
    "description": "Whether to enable the sxid service. Set to 'yes' to start.",
    "enum": [
      "no",
      "yes"
    ]
  },
  "sxid_opts": {
    "type": "string",
    "default": "${SXID_OPTS}",
    "description": "Options passed to the sxid binary."
  }
}
