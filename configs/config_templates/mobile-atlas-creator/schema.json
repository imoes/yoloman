{
  "values_schema": {
    "type": "object",
    "description": "Configuration parameters for mobile-atlas-creator derived from documented file locations."
  },
  "cache_dir": {
    "type": "string",
    "default": "/var/cache/mobac",
    "description": "Directory for per-user cache. Documented in FILES section as ${HOME}/.mobac. Parametrized for deployment override."
  },
  "config_dir": {
    "type": "string",
    "default": "/etc/mobac",
    "description": "Directory for per-user configuration files. Documented in FILES section as ${HOME}/.config/mobac. Parametrized for system-wide deployment override."
  }
}
