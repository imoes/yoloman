{
  "service_name": {
    "type": "string",
    "default": "courier-webadmin",
    "description": "Name of the provided service (used in the INIT INFO section)"
  },
  "pidfile_path": {
    "type": "string",
    "default": "/run/courier/webadmin.pid",
    "description": "Full path to the PID file for the daemon"
  }
}
