{
  "PORT": {
    "type": "string",
    "default": "",
    "description": "Port or device path passed to radioclkd. Usually a serial device (e.g., /dev/ttyUSB0) or empty if configured via /etc/radioclk.conf."
  }
}
