{
  "source_dir": {
    "type": "string",
    "default": "",
    "description": "Absolute path to the source directory to be mirrored (required)."
  },
  "mount_point": {
    "type": "string",
    "default": "",
    "description": "Absolute path to the mount point where the mirrored filesystem will appear (required)."
  },
  "options": {
    "type": "list",
    "default": [],
    "description": "List of bindfs options (comma-separated in fstab but stored here as list for template rendering). Each element must be a valid bindfs option without leading '-o'."
  }
}
