{
  "ca_cert": {
    "default": "",
    "description": "Path to the CA certificate PEM file or 'none'. Empty string implies default CA verification.",
    "type": "string"
  },
  "certificate": {
    "default": "",
    "description": "Path to the certificate PEM file for SSL/TLS. Empty string disables PKI.",
    "type": "string"
  },
  "detach": {
    "default": true,
    "description": "Run the daemon in the background (detach).",
    "type": "bool"
  },
  "ic_nb_db": {
    "default": "unix:/ovn_ic_nb_db.sock",
    "description": "OVSDB connection string for the OVN Interconnection Northbound Database.",
    "type": "string"
  },
  "ic_sb_db": {
    "default": "unix:/ovn_ic_sb_db.sock",
    "description": "OVSDB connection string for the OVN Interconnection Southbound Database.",
    "type": "string"
  },
  "log_file": {
    "default": "/var/log/ovn/ovn-ic.log",
    "description": "Path to the log file. Empty string disables file logging.",
    "type": "string"
  },
  "log_spec": {
    "description": "Verbose log specification (e.g., 'dbg', 'emissary:console:dbg'). Empty string uses defaults.",
    "enum": [
      "dbg",
      "info",
      "warn",
      "err",
      "off",
      "emer",
      "any"
    ],
    "type": "string",
    "enum_open": true
  },
  "monitor": {
    "default": false,
    "description": "Start a monitor process to restart the daemon on fatal signals.",
    "type": "bool"
  },
  "no_chdir": {
    "default": false,
    "description": "Do not change the working directory to root when detaching.",
    "type": "bool"
  },
  "no_self_confinement": {
    "default": false,
    "description": "Disable self-imposed file access confinement.",
    "type": "bool"
  },
  "overwrite_pidfile": {
    "default": false,
    "description": "Overwrite an existing locked PID file.",
    "type": "bool"
  },
  "ovnnb_db": {
    "default": "unix:/ovnnb_db.sock",
    "description": "OVSDB connection string for the OVN Northbound Database.",
    "type": "string"
  },
  "ovnsb_db": {
    "default": "unix:/ovnsb_db.sock",
    "description": "OVSDB connection string for the OVN Southbound Database.",
    "type": "string"
  },
  "pidfile": {
    "default": "/run/ovn/ovn-ic.pid",
    "description": "Path to the PID file. If empty, no PID file is created.",
    "type": "string"
  },
  "private_key": {
    "default": "",
    "description": "Path to the private key PEM file for SSL/TLS. Empty string disables PKI.",
    "type": "string"
  },
  "syslog_method": {
    "default": "libc",
    "description": "Method for syslog: libc, unix:file, udp:ip:port, or null.",
    "enum": [
      "libc",
      "unix:file",
      "udp:ip:port",
      "null"
    ],
    "type": "string"
  },
  "syslog_target": {
    "default": "",
    "description": "UDP host:port for syslog messages. Empty string disables this.",
    "type": "string"
  },
  "unixctl": {
    "default": "/run/ovn/ovn-ic.ctl",
    "description": "Path to the control socket for runtime management commands.",
    "type": "string"
  },
  "user_group": {
    "default": "",
    "description": "Run as specified user:group. Empty string means no user switching.",
    "type": "string"
  }
}
