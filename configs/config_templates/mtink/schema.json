{
  "noAutodetect": {
    "type": "bool",
    "default": false,
    "description": "Disable automatic detection of the printer."
  },
  "device": {
    "type": "string",
    "default": "/dev/usb/lp0",
    "description": "The device file path for the printer."
  },
  "model": {
    "type": "string",
    "default": "",
    "description": "The printer model identifier. Empty string lets the program guess if autodetect is enabled."
  }
}
