{
  "config_filename": {
    "type": "string",
    "default": "/var/cache/debconf/config.dat",
    "description": "Path to the config database file."
  },
  "passwords_filename": {
    "type": "string",
    "default": "/var/cache/debconf/passwords.dat",
    "description": "Path to the passwords database file."
  },
  "templates_filename": {
    "type": "string",
    "default": "/var/cache/debconf/templates.dat",
    "description": "Path to the templates database file."
  }
}
