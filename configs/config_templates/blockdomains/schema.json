{
  "mode": {
    "type": "string",
    "default": "block",
    "description": "Operational mode: 'block' to deny matching traffic, 'monitor' to log only without blocking."
  },
  "rules_file": {
    "type": "string",
    "default": "/etc/blockdomains/blocked/*.acl",
    "description": "Absolute path to the directory containing domain blocking rules (one rule per line)."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/blockdomains.log",
    "description": "Path to the log file. Leave empty to log to stderr instead."
  }
}
