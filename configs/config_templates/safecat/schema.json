{
  "safecat_tempdir": {
    "type": "string",
    "default": "/var/lib/safecat/tmp",
    "description": "The temporary directory for writing data before linking to the destination."
  },
  "safecat_destdir": {
    "type": "string",
    "default": "/var/lib/safecat/new",
    "description": "The destination directory where complete files are linked into."
  }
}
