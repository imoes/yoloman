{
  "preprocessor": {
    "type": "string",
    "default": "/usr/bin/cpp",
    "description": "Path to the C preprocessor executable used by trawld."
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "If true, enables debug mode."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, enables verbose mode."
  },
  "load_file": {
    "type": "string",
    "default": "/etc/trawl/resources",
    "description": "Path to a resource file to load on startup."
  },
  "nocpp": {
    "type": "bool",
    "default": false,
    "description": "If true, disables the use of the C preprocessor."
  }
}
