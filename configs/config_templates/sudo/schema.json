{
  "sudo_groups": {
    "type": "list",
    "default": [
      {
        "name": "wheel",
        "full_access": true,
        "commands": []
      }
    ],
    "description": "List of sudo-enabled groups; each group defines access scope and commands",
    "items": {
      "name": {
        "type": "string",
        "default": "",
        "description": "Name of the group (without % prefix in template; added automatically)"
      },
      "full_access": {
        "type": "bool",
        "default": true,
        "description": "If true, group members may run any command; if false, restrict to listed commands"
      },
      "commands": {
        "type": "list",
        "default": [],
        "description": "List of full paths to commands allowed for this group (only used if full_access=false)"
      }
    }
  },
  "include_sudoers_d": {
    "type": "bool",
    "default": true,
    "description": "If true, include additional sudo configurations from /etc/sudoers.d (recommended for modularity)"
  },
  "enable_passwd_prompt": {
    "type": "bool",
    "default": true,
    "description": "If true, users must enter their password when invoking sudo; disable only in secure, ephemeral, or containerized environments"
  },
  "enable_logging": {
    "type": "bool",
    "default": false,
    "description": "If true, log all sudo commands and session I/O to /var/log/sudo.log (use for audit compliance)"
  }
}
