{
  "server_root": {
    "type": "string",
    "default": "/var/www/html",
    "description": "Default root directory for web content"
  },
  "server_name": {
    "type": "string",
    "default": "example.com",
    "description": "Primary server name"
  },
  "ssl_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable SSL/TLS configuration"
  },
  "cert_directory": {
    "type": "string",
    "default": "/etc/ssl/certs",
    "description": "Directory containing SSL certificates"
  },
  "listen_port": {
    "type": "number",
    "default": 80,
    "description": "Port to listen on"
  },
  "included_paths": {
    "type": "list",
    "default": [],
    "description": "List of files to include during preprocessing"
  },
  "servers": {
    "type": "list",
    "default": [],
    "description": "List of server configurations to generate vhost blocks for. Each server must have 'name' and optional 'aliases' and 'root' fields. 'root' defaults to server_root if not specified. 'aliases' is a list of strings, joined with ' @' in the template."
  }
}
