{
  "database": {
    "type": "string",
    "default": "~/.duc.db",
    "description": "Path to the Duc database file"
  },
  "apparent_size": {
    "type": "bool",
    "default": false,
    "description": "Use apparent size instead of actual size (global toggle)"
  },
  "bytes": {
    "type": "bool",
    "default": false,
    "description": "Show exact byte counts instead of human-readable sizes (global toggle)"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode (increased verbosity)"
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Suppress warnings (quiet mode)"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Increase verbosity"
  },
  "ls_recursive": {
    "type": "bool",
    "default": false,
    "description": "Recursively list subdirectories in 'duc ls'"
  },
  "ls_classify": {
    "type": "bool",
    "default": false,
    "description": "Append file type indicator (*/) to entries in 'duc ls'"
  },
  "ls_color": {
    "type": "bool",
    "default": false,
    "description": "Use colorized output in 'duc ls'"
  },
  "ls_name_sort": {
    "type": "bool",
    "default": false,
    "description": "Sort output by name instead of size in 'duc ls'"
  },
  "ls_levels": {
    "type": "number",
    "default": 4,
    "description": "Number of depth levels to traverse in 'duc ls'"
  },
  "ls_full_path": {
    "type": "bool",
    "default": false,
    "description": "Show full paths instead of tree view in 'duc ls'"
  },
  "ls_dirs_only": {
    "type": "bool",
    "default": false,
    "description": "List only directories, skip individual files in 'duc ls'"
  },
  "ls_ascii": {
    "type": "bool",
    "default": false,
    "description": "Use ASCII instead of UTF-8 for tree characters in 'duc ls'"
  },
  "ls_graph": {
    "type": "bool",
    "default": false,
    "description": "Draw relative-size graph bars in 'duc ls'"
  },
  "ls_count": {
    "type": "bool",
    "default": false,
    "description": "Show number of files instead of file size in 'duc ls'"
  },
  "ui_no_color": {
    "type": "bool",
    "default": false,
    "description": "Disable colors on terminal output in 'duc ui'"
  },
  "ui_name_sort": {
    "type": "bool",
    "default": false,
    "description": "Sort by name instead of size in 'duc ui'"
  },
  "graph_format": {
    "type": "string",
    "default": "png",
    "description": "Output format for 'duc graph' (png|svg|pdf|html)",
    "enum": [
      "png",
      "svg",
      "pdf",
      "html"
    ]
  },
  "graph_size": {
    "type": "number",
    "default": 800,
    "description": "Image size in pixels for 'duc graph'"
  },
  "graph_dpi": {
    "type": "number",
    "default": 96.0,
    "description": "Output DPI for 'duc graph'"
  },
  "graph_levels": {
    "type": "number",
    "default": 4,
    "description": "Max depth of rings to draw in 'duc graph'"
  },
  "graph_fuzz": {
    "type": "number",
    "default": 0.7,
    "description": "Fuzz factor for smoother curves in 'duc graph'"
  },
  "graph_ring_gap": {
    "type": "number",
    "default": 0,
    "description": "Gap between rings in pixels for 'duc graph'"
  },
  "graph_palette": {
    "type": "string",
    "default": "size",
    "description": "Palette to use for 'duc graph' (size|rainbow|greyscale|monochrome|classic)",
    "enum": [
      "size",
      "rainbow",
      "greyscale",
      "monochrome",
      "classic"
    ]
  },
  "graph_gradient": {
    "type": "bool",
    "default": false,
    "description": "Draw with color gradient in 'duc graph'"
  },
  "graph_count": {
    "type": "bool",
    "default": false,
    "description": "Show number of files instead of file size in 'duc graph'"
  },
  "cgi_css_url": {
    "type": "string",
    "default": "",
    "description": "URL of external CSS stylesheet for 'duc cgi'"
  },
  "cgi_header": {
    "type": "string",
    "default": "",
    "description": "HTML file to include as header for 'duc cgi'"
  },
  "cgi_footer": {
    "type": "string",
    "default": "",
    "description": "HTML file to include as footer for 'duc cgi'"
  },
  "cgi_format": {
    "type": "string",
    "default": "png",
    "description": "Output format for 'duc cgi' graph (png|svg|pdf|html)",
    "enum": [
      "png",
      "svg",
      "pdf",
      "html"
    ]
  },
  "cgi_size": {
    "type": "number",
    "default": 800,
    "description": "Image size in pixels for 'duc cgi'"
  },
  "cgi_dpi": {
    "type": "number",
    "default": 96.0,
    "description": "Output DPI for 'duc cgi'"
  },
  "cgi_levels": {
    "type": "number",
    "default": 4,
    "description": "Max depth of rings to draw in 'duc cgi'"
  },
  "cgi_fuzz": {
    "type": "number",
    "default": 0.7,
    "description": "Fuzz factor for smoother curves in 'duc cgi'"
  },
  "cgi_ring_gap": {
    "type": "number",
    "default": 0,
    "description": "Gap between rings in pixels for 'duc cgi'"
  },
  "cgi_palette": {
    "type": "string",
    "default": "size",
    "description": "Palette to use for 'duc cgi' (size|rainbow|greyscale|monochrome|classic)",
    "enum": [
      "size",
      "rainbow",
      "greyscale",
      "monochrome",
      "classic"
    ]
  },
  "cgi_gradient": {
    "type": "bool",
    "default": false,
    "description": "Draw with color gradient in 'duc cgi'"
  },
  "cgi_count": {
    "type": "bool",
    "default": false,
    "description": "Show number of files instead of file size in 'duc cgi'"
  },
  "cgi_list": {
    "type": "bool",
    "default": false,
    "description": "Generate file listing table in 'duc cgi'"
  },
  "cgi_tooltip": {
    "type": "bool",
    "default": false,
    "description": "Enable on-hover tooltips in 'duc cgi'"
  },
  "index_fs_exclude": {
    "type": "string",
    "default": "ext4",
    "description": "Comma-separated list of file system types to exclude during indexing",
    "enum": [
      "ext3",
      "ext4",
      "dosfs"
    ]
  },
  "index_fs_include": {
    "type": "string",
    "default": "ext4",
    "description": "Comma-separated list of file system types to include during indexing",
    "enum": [
      "ext3",
      "ext4",
      "dosfs"
    ]
  },
  "index_uid": {
    "type": "number",
    "default": 0,
    "description": "Limit indexing to files owned by this UID (numeric)"
  },
  "index_username": {
    "type": "string",
    "default": "",
    "description": "Limit indexing to files owned by this username"
  },
  "index_exclude": {
    "type": "string",
    "default": "",
    "description": "Glob pattern to exclude files during indexing"
  },
  "index_progress": {
    "type": "bool",
    "default": false,
    "description": "Show progress during indexing"
  },
  "index_one_file_system": {
    "type": "bool",
    "default": false,
    "description": "Skip directories on different file systems during indexing"
  },
  "index_max_depth": {
    "type": "number",
    "default": 0,
    "description": "Limit directory depth recorded in database (0 = unlimited)"
  },
  "index_hide_file_names": {
    "type": "bool",
    "default": false,
    "description": "Hide file names (preserve only directory names) in index"
  },
  "index_force": {
    "type": "bool",
    "default": false,
    "description": "Force overwrite of corrupted database"
  },
  "index_uncompressed": {
    "type": "bool",
    "default": false,
    "description": "Do not compress database (for debugging or legacy backends)"
  },
  "index_check_hard_links": {
    "type": "bool",
    "default": false,
    "description": "Count hard links only once during indexing"
  },
  "index_dry_run": {
    "type": "bool",
    "default": false,
    "description": "Do not update database, only crawl (dry run)"
  }
}
