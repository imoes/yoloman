{
  "services": {
    "type": "list",
    "default": [],
    "description": "List of RPC services with their port and protocol assignments. Each entry defines a service name, port, protocol, and optional aliases.",
    "items": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "default": "",
          "description": "Service name (e.g., 'nfs', 'mountd', 'ypbind')"
        },
        "port": {
          "type": "number",
          "default": 0,
          "description": "TCP/UDP port number assigned to this RPC service"
        },
        "protocol": {
          "type": "string",
          "default": "tcp",
          "description": "Transport protocol (usually 'tcp' or 'udp')"
        },
        "aliases": {
          "type": "list",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Optional alternative names for the service (used for human readability in comments)"
        }
      },
      "required": [
        "name",
        "port",
        "protocol"
      ]
    }
  }
}
