{
  "warp": {
    "type": "bool",
    "default": false,
    "description": "Enable warp mode for sleepenh to adjust resulting timestamps"
  },
  "default_interval": {
    "type": "number",
    "default": 1.0,
    "description": "Default time to sleep in seconds if not specified in individual tasks"
  },
  "tasks": {
    "type": "list",
    "default": [],
    "description": "List of periodic tasks to run with sleepenh timing control. For list-of-objects, see items schema below for structure of each object in the list. (Not used directly in type field, but defines the structure of elements in the 'tasks' list via the items field below.)"
  }
}
