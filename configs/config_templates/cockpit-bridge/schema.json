{
  "args": {
    "type": "list",
    "default": [],
    "description": "Optional additional command-line arguments passed to cockpit-bridge. Each item is rendered as a comment above the ExecStart line for documentation only (not executed)."
  },
  "bin_path": {
    "type": "string",
    "default": "/usr/lib/cockpit/cockpit-bridge",
    "description": "Path to the cockpit-bridge binary."
  },
  "interact": {
    "type": "string",
    "default": "",
    "description": "If non-empty, specify raw cockpit1 protocol boundary (used for debugging/testing)."
  },
  "packages": {
    "type": "bool",
    "default": false,
    "description": "If true, list available Cockpit packages and exit."
  },
  "help": {
    "type": "bool",
    "default": false,
    "description": "If true, show help options."
  },
  "version": {
    "type": "bool",
    "default": false,
    "description": "If true, show Cockpit version information."
  }
}
