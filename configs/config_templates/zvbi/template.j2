# zvbi (Video BSI - Video Blind Scanning/Insertion) daemon configuration
# Generated by Ansible Jinja2 template

# Whether to start the zvbi daemon (zvbid) on system startup.
# If enabled, the init script will launch the daemon automatically.
start_daemon={{ start_daemon | default(true) | lower }}