{
  "invisible": {
    "type": "bool",
    "default": false,
    "description": "Whether to start modem-manager-gui in invisible mode without showing the main window."
  },
  "mmmodule": {
    "type": "string",
    "default": "mm",
    "description": "The modem management module backend to use (e.g., Modem Manager, oFono, or Wader)."
  },
  "cmmodule": {
    "type": "string",
    "default": "NetworkManager",
    "description": "The connection management module backend to use (e.g., NetworkManager, ConnMan)."
  }
}
