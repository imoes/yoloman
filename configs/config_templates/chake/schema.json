{
  "nodes": {
    "type": "list",
    "default": [],
    "description": "List of nodes to manage. Each item is a dict with keys: 'url' (string, required), 'config_manager' (string, optional, default 'itamae'), 'recipes' (list of strings, optional), 'encrypted_files' (list of strings, optional)."
  }
}
