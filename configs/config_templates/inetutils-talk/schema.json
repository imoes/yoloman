{
  "talkd_port": {
    "type": "number",
    "default": 517,
    "description": "The TCP/UDP port number on which the talk daemon listens for connection requests."
  },
  "enable_talkd": {
    "type": "bool",
    "default": false,
    "description": "Whether to start the talk daemon (talkd) as a background service."
  },
  "talkd_address": {
    "type": "string",
    "default": "",
    "description": "The IP address or hostname to bind the talk daemon to. Empty string binds to all interfaces."
  }
}
