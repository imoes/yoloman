{
  "output_file": {
    "type": "string",
    "default": "./output.pcap",
    "description": "Path to the output pcap file. If omitted or empty, defaults to stdout (not recommended for automation)."
  },
  "input_files": {
    "type": "list",
    "default": [],
    "description": "List of input pcap file paths to slice or merge. Must contain at least one file."
  },
  "start_time": {
    "type": "string",
    "default": "",
    "description": "Start time for extraction. Formats: Unix timestamp (float), relative (+N), or ymdhmsu (e.g., 2023y01m01d00h00m00s). Empty uses default (earliest packet)."
  },
  "end_time": {
    "type": "string",
    "default": "",
    "description": "End time for extraction. Formats: Unix timestamp (float), relative (+N) from start, or ymdhmsu. Empty uses default (10 years after start)."
  },
  "keep_duplicates": {
    "type": "bool",
    "default": false,
    "description": "If true, do not discard duplicate packets when merging multiple files (-D)."
  },
  "relative_merge": {
    "type": "bool",
    "default": false,
    "description": "If true, merge files based on relative time offsets rather than absolute timestamps (-l)."
  },
  "session_types": {
    "type": "string",
    "default": "",
    "description": "Comma-separated list of session types to track: tcp, sip, h323. Empty disables session tracking (-s).",
    "enum": [
      "tcp",
      "sip",
      "h323"
    ]
  },
  "session_expire_seconds": {
    "type": "number",
    "default": 0,
    "description": "Seconds of inactivity after which an identified session expires. Only effective with session tracking (-e)."
  },
  "verbosity": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level: 0 (silent), 1 (show session openings/closings), 2 (also show subsessions) (-v)."
  }
}
