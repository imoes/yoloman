{
  "interface": {
    "type": "string",
    "default": "eth0",
    "description": "Network interface to use for listening and injection (must be in UP state)"
  },
  "images": {
    "type": "list",
    "default": [],
    "description": "List of paths to firmware image files; ap51-flash selects the first matching firmware for each detected device"
  }
}
