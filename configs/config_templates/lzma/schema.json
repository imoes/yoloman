{
  "xz_defaults": {
    "type": "string",
    "default": "--memlimit=150MiB",
    "description": "Space-separated list of options parsed from XZ_DEFAULTS environment variable. Used as system-wide or user-specific defaults. Commonly sets memory limits or presets."
  },
  "xz_opt": {
    "type": "string",
    "default": "-7e",
    "description": "Space-separated list of options parsed from XZ_OPT environment variable. Passed to xz when invoked by scripts or tools like tar."
  }
}
