{
  "metar_url": {
    "type": "string",
    "default": "http://weather.noaa.gov/pub/data/observations/metar/stations/",
    "description": "Base URL for METAR files. The station ID and .TXT extension are appended automatically."
  },
  "stations": {
    "type": "list",
    "default": [
      "KJFK",
      "EHAM"
    ],
    "description": "List of ICAO station identifiers for which to retrieve weather reports."
  },
  "decode": {
    "type": "bool",
    "default": false,
    "description": "If true, pass the -d flag to decode reports into human-readable format."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, pass the -v flag to be verbose during retrieval."
  }
}
