{
  "report_dir": {
    "type": "string",
    "default": "/var/lib/pgcluu/reports",
    "description": "Directory where HTML reports will be generated."
  },
  "mail_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable email notifications for alerts (e.g., long-running queries, lock waits)."
  },
  "mail_to": {
    "type": "string",
    "default": "dba@example.com",
    "description": "Comma-separated list of recipient email addresses."
  },
  "mail_smtp": {
    "type": "string",
    "default": "localhost",
    "description": "SMTP server hostname or IP for sending emails."
  },
  "mail_from": {
    "type": "string",
    "default": "pgcluu@{{ hostname | default('localhost') }}",
    "description": "Sender email address for notifications."
  }
}
