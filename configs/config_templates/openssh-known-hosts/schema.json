{
  "cron_schedule": {
    "type": "string",
    "default": "0 *",
    "description": "Cron schedule expression for when the known_hosts update should run."
  },
  "cron_user": {
    "type": "string",
    "default": "root",
    "description": "User account under which the cron job executes."
  }
}
