{
 "backend_items": {
  "description": "A single backend configuration object.",
  "type": "object"
 },
 "backends": {
  "default": [],
  "description": "List of backend servers.",
  "type": "list"
 },
 "ether_addr": {
  "default": "",
  "description": "Physical (MAC) address of the backend (required for DSR mode).",
  "type": "string"
 },
 "family": {
  "default": "ipv4",
  "description": "Family of the virtual service: ipv4, ipv6, or dual (ipv4 by default).",
  "enum": [
   "ipv4",
   "ipv6",
   "dual"
  ],
  "type": "string"
 },
 "global_settings": {
  "source": "inferred-from-template",
  "type": "object"
 },
 "iface": {
  "default": "",
  "description": "Input interface (only required for DSR mode).",
  "type": "string"
 },
 "ip_addr": {
  "default": "",
  "description": "IP address for the backend (required, except for DSR).",
  "type": "string"
 },
 "items": {
  "description": "A single farm (service) configuration object matching the nftlb JSON format.",
  "type": "object"
 },
 "mode": {
  "default": "snat",
  "description": "Topology mode: snat, dnat, or dsr (required).",
  "enum": [
   "snat",
   "dnat",
   "dsr"
  ],
  "type": "string"
 },
 "name": {
  "default": "",
  "description": "Name of the backend (required by nftlb).",
  "type": "string"
 },
 "oface": {
  "default": "",
  "description": "Output interface (only required for DSR mode).",
  "type": "string"
 },
 "priority": {
  "default": 1,
  "description": "Priority for the backend (1 by default).",
  "type": "number"
 },
 "proto": {
  "default": "tcp",
  "description": "Protocol: tcp, udp, sctp, or all (tcp by default).",
  "enum": [
   "tcp",
   "udp",
   "sctp",
   "all"
  ],
  "type": "string"
 },
 "scheduler": {
  "default": "rr",
  "description": "Scheduler: weight, rr, hash, symhash (round robin by default).",
  "enum": [
   "weight",
   "rr",
   "hash",
   "symhash"
  ],
  "type": "string"
 },
 "services": {
  "default": [],
  "description": "List of load-balanced services (farms) to configure. Each service defines a virtual IP, protocol, and backend pool per nftlb(8) documentation.",
  "type": "list"
 },
 "state": {
  "default": "up",
  "description": "State: up, down, or off (up by default).",
  "enum": [
   "up",
   "down",
   "off"
  ],
  "type": "string"
 },
 "virtual_addr": {
  "default": "",
  "description": "IP address for the virtual service (required).",
  "type": "string"
 },
 "virtual_ports": {
  "default": "80",
  "description": "Port list (comma-separated or hyphen-ranged, e.g., 80 or 80-88).",
  "type": "string"
 },
 "weight": {
  "default": 1,
  "description": "Weight of the backend (1 by default).",
  "type": "number"
 }
}
