{
  "device": {
    "type": "string",
    "default": "/dev/cdrom",
    "description": "CD/DVD device path, e.g., /dev/sr0 or /dev/cdrom"
  },
  "info_only": {
    "type": "bool",
    "default": false,
    "description": "If true, print CD/DVD info and exit; otherwise run test if other flags enabled"
  },
  "timing_test": {
    "type": "bool",
    "default": false,
    "description": "If true, perform timing test (not valid for audio CDs)"
  },
  "gnuplot": {
    "type": "bool",
    "default": false,
    "description": "If true, write raw data to plot file for gnuplot(1)"
  },
  "plot_file": {
    "type": "string",
    "default": "./cdck-plot.dat",
    "description": "Path to the gnuplot data output file"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output during operation"
  }
}
