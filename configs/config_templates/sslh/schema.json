{
  "sslh_user": {
    "type": "string",
    "default": "sslh",
    "description": "The system user under which sslh runs."
  },
  "sslh_listen_address": {
    "type": "string",
    "default": "<change-me>",
    "description": "The IP address on which sslh listens."
  },
  "sslh_listen_port": {
    "type": "number",
    "default": 443,
    "description": "The port on which sslh listens."
  },
  "ssh_host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "The host address of the SSH backend."
  },
  "ssh_port": {
    "type": "number",
    "default": 22,
    "description": "The port of the SSH backend."
  },
  "ssl_host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "The host address of the SSL backend."
  },
  "ssl_port": {
    "type": "number",
    "default": 443,
    "description": "The port of the SSL backend."
  }
}
