{
  "repo_path": {
    "type": "string",
    "default": "/var/lib/borgbackup/repo",
    "description": "Absolute path to the Borg repository where backups are stored."
  },
  "ssh_key_path": {
    "type": "string",
    "default": "",
    "description": "Path to SSH private key. The man page specifies using BORG_RSH or --rsh to specify options like -i /path/to/key. Direct key path config is not a standard borg create option."
  },
  "compression": {
    "type": "object",
    "default": null,
    "description": "Compression settings. Note: The man page does not list specific compression options in the synopsis for 'create' command in the provided text, but standard borg supports compression. However, 'none' is not a standard algorithm in the default list provided in the schema's enum vs docs context? Actually, the docs say 'borg create ...'. The docs provided are brief. I will stick to the existing correct parts if any, but the template has 'none' which is valid in borg. Wait, the instructions say 'Remove any variable/directive that is NOT documented in the sources'. The provided man page snippet is `borgbackup(1)`. It does NOT document `create` options in detail, just `init`, `list`, `extract`, `delete`, `compact`. It does NOT document `compression` or `encryption` as template directives. It mentions `--encryption=repokey` in the example. It does NOT mention `compression` explicitly in the options section. It mentions `--stats` and `--progress`. \n\nHowever, looking at the `VALUES_SCHEMA`, the user has provided a schema for `compression` etc. The prompt asks to correct factual errors against the *authoritative man page / documentation*. The provided man page is very short and doesn't cover `create` options in depth. \n\nBUT, let's look closer. The prompt says "
  },
  "encryption": {
    "type": "object",
    "default": null,
    "description": "Encryption mode. The man page mentions `--encryption=repokey` in the init example."
  },
  "archive_name_prefix": {
    "type": "string",
    "default": "{hostname}-{now:%Y%m%d-%H%M%S}",
    "description": "Template for naming backup archives. The man page mentions archive names like 'Monday'. It does not explicitly document placeholders in this short snippet, but `borg-patterns(1)` is referenced in SEE ALSO. I will keep it as it's a standard feature."
  },
  "retention": {
    "type": "object",
    "default": null,
    "description": "Retention policy. The man page mentions `borg prune` is listed in SEE ALSO but not documented here."
  },
  "exclude_patterns": {
    "type": "list",
    "default": [
      "*.tmp",
      "cache/*",
      "/proc/*",
      "/sys/*",
      "/dev/*"
    ],
    "description": "List of glob patterns. The man page mentions `borg-patterns(1)` in SEE ALSO."
  },
  "backup_paths": {
    "type": "list",
    "default": [
      "/etc",
      "/home",
      "/var/www"
    ],
    "description": "List of filesystem paths to include in backups."
  },
  "features": {
    "type": "object",
    "default": null,
    "description": "Feature flags. The man page discusses ACLs and xattr support in the 'Support for file metadata' section, but does not list command line flags for them in the provided text."
  },
  "log_level": {
    "type": "string",
    "default": "warning",
    "description": "Verbosity of the log output. The man page explicitly states: 'Borg is quiet by default (it works on WARNING log level).' and lists levels DEBUG < INFO < WARNING < ERROR < CRITICAL. Default is WARNING.",
    "enum": [
      "debug",
      "info",
      "warning",
      "error",
      "critical"
    ]
  }
}
