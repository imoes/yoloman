{
  "data_path": {
    "type": "string",
    "default": "/usr/sbin/gm-backup_data",
    "description": "Path to the GNUmed database data backup script."
  },
  "database_path": {
    "type": "string",
    "default": "/usr/sbin/gm-backup",
    "description": "Path to the GNUmed database backup script."
  },
  "zipsign_path": {
    "type": "string",
    "default": "/usr/sbin/gm-zip+sign_backups",
    "description": "Path to the GNUmed backup compression and signing script."
  }
}
