{
  "data_dir": {
    "default": "/var/lib/ddupdate",
    "description": "Directory for runtime data (e.g., last IP cache).",
    "type": "string"
  },
  "debug": {
    "default": false,
    "description": "Enable verbose debug logging to stderr.",
    "type": "bool"
  },
  "enable_ipv6": {
    "default": true,
    "description": "Enable IPv6 detection and updates if available.",
    "type": "bool"
  },
  "global_options": {
    "description": "Global plugin configuration (e.g., default credentials, API tokens). Used when per-service options are absent.",
    "type": "object"
  },
  "hosts": {
    "default": [],
    "description": "List of hostname entries to update.",
    "items": {
      "address_source": {
        "default": "default",
        "description": "Method to determine IP: 'default', 'web', 'ifconfig', 'local'.",
        "type": "string"
      },
      "address_type": {
        "default": "ipv4",
        "description": "Address type to use: 'ipv4', 'ipv6', or 'dual'.",
        "type": "string"
      },
      "hostname": {
        "default": "",
        "description": "Fully qualified domain name (FQDN) to update.",
        "type": "string"
      },
      "plugin": {
        "default": "",
        "description": "Override plugin name (if different from service's plugin). Optional.",
        "type": "string"
      },
      "provider": {
        "default": "",
        "description": "Service provider name (must match one defined in 'services').",
        "type": "string"
      }
    },
    "type": "list"
  },
  "log_file": {
    "default": "",
    "description": "Path to log file (if empty, logs to stderr only).",
    "type": "string"
  },
  "log_level": {
    "default": "info",
    "description": "Minimum log level: 'debug', 'info', 'warning', 'error', or 'critical'.",
    "enum": [
      "debug",
      "info",
      "warning",
      "error",
      "critical"
    ],
    "type": "string"
  },
  "network_timeout": {
    "default": 30,
    "description": "Network request timeout in seconds.",
    "type": "number"
  },
  "plugin_dir": {
    "default": "/usr/lib/ddupdate",
    "description": "Directory where plugin modules (.py or .so) are stored.",
    "type": "string"
  },
  "policy": {
    "default": "first_ok",
    "description": "Policy for handling multiple services (e.g., 'first_ok' = stop after first successful update).",
    "type": "string"
  },
  "services": {
    "default": [],
    "description": "List of named services to enable (each service has a plugin and optional custom options).",
    "items": {
      "name": {
        "default": "",
        "description": "Unique identifier for the service (e.g., 'my-dyndns').",
        "type": "string"
      },
      "options": {
        "default": {},
        "description": "Plugin-specific key-value options (e.g., {'api_key': 'abc123', 'zone': 'example.com'}).",
        "type": "object"
      },
      "plugin": {
        "default": "",
        "description": "Name of the plugin to use (e.g., 'dnsimple', 'cloudflare').",
        "type": "string"
      }
    },
    "type": "list"
  }
}
