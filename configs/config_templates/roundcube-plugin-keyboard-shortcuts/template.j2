<?php

// Array of additional keyboard shortcut definitions for Roundcube.
// Each entry typically maps a key combination to an action string.
// Default is an empty JSON array, meaning no extra shortcuts are registered.
$rcmail_config['keyboard_shortcuts_extras'] = {{ keyboard_shortcuts_extras | default('[]') | tojson }};

?>