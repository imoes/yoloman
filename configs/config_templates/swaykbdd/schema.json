{
  "options": {
    "type": "list",
    "default": [
      "--default=en,de",
      "--timeout=50"
    ],
    "description": "List of command-line arguments for swaykbdd. Each item is a full argument string (e.g., '--default=layout', '--timeout=ms'). Use an empty list to start with defaults (no extra args). The '--default' argument specifies layout indices. The '--timeout' argument sets the debounce delay in milliseconds."
  }
}
