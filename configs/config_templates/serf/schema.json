{
  "desc": {
    "type": "string",
    "default": "Serf Agent",
    "description": "Description of the service used in log messages."
  },
  "name": {
    "type": "string",
    "default": "serf",
    "description": "Service name, used for PID file and script naming."
  },
  "user": {
    "type": "string",
    "default": "_serf",
    "description": "User to run the daemon as."
  },
  "group": {
    "type": "string",
    "default": "_serf",
    "description": "Group to run the daemon as."
  },
  "daemon": {
    "type": "string",
    "default": "/usr/bin/serf",
    "description": "Path to the serf binary."
  },
  "config_dir": {
    "type": "string",
    "default": "/etc/serf",
    "description": "Path to the serf configuration directory."
  }
}
