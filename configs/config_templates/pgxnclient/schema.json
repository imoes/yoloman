{
  "pgxn_index": {
    "type": "string",
    "default": "https://api.pgxn.org/",
    "description": "The base URL for the PGXN API index."
  },
  "user_agent": {
    "type": "string",
    "default": "pgxnclient/1.3.2",
    "description": "The User-Agent header string to use for HTTP requests."
  },
  "http_timeout": {
    "type": "number",
    "default": 30,
    "description": "The maximum time in seconds to wait for an HTTP request to complete."
  },
  "cache_dir": {
    "type": "string",
    "default": "/tmp/pgxnclient-cache",
    "description": "The directory path used for caching downloaded extension metadata."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, enables verbose logging output to stderr."
  }
}
