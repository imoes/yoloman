{
  "project_name": {
    "type": "string",
    "default": "watcher",
    "description": "The base name of the OpenStack project."
  },
  "messaging_processname": {
    "type": "string",
    "default": "watcher-applier",
    "description": "The process name used for RabbitMQ messaging."
  },
  "system_user": {
    "type": "string",
    "default": "watcher",
    "description": "The system user under which the service runs."
  },
  "system_group": {
    "type": "string",
    "default": "watcher",
    "description": "The system group under which the service runs."
  },
  "config_file": {
    "type": "string",
    "default": "watcher.conf",
    "description": "The configuration filename relative to /etc/watcher/."
  }
}
