{
  "modules": {
    "type": "list",
    "default": [],
    "description": "List of kernel modules to load at boot for cups-filters functionality (e.g., 'usblp', 'usbhid'). Each entry specifies a module name to be passed to 'modprobe'.",
    "items": {
      "type": "object",
      "fields": {
        "name": {
          "type": "string",
          "default": "",
          "description": "Name of the kernel module (e.g., 'usblp' for USB printer support). Must match the module filename (without '.ko')."
        }
      }
    }
  }
}
