{
  "receiver_device": {
    "type": "string",
    "default": "/dev/hidraw0",
    "description": "The path to the hidraw device node for the Logitech Unifying receiver."
  },
  "pairing_timeout": {
    "type": "number",
    "default": 30,
    "description": "The duration in seconds the receiver stays open for pairing."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output."
  },
  "udev_rules_path": {
    "type": "string",
    "default": "/etc/udev/rules.d/99-logitech-unifying.rules",
    "description": "Path to the udev rules file for device permissions."
  },
  "auto_devices": {
    "type": "list",
    "default": [],
    "description": "List of devices to configure automatically. Each item is a map with device_id and name."
  },
  "auto_devices_items": {
    "type": "object",
    "description": "Fields for each item in auto_devices list."
  },
  "auto_devices_items_device_id": {
    "type": "number",
    "default": 0,
    "description": "The device index or ID."
  },
  "auto_devices_items_name": {
    "type": "string",
    "default": "Unnamed Device",
    "description": "The friendly name for the device."
  }
}
