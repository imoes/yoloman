{
  "devicescan_directives": {
    "type": "list",
    "default": [],
    "description": "List of directives to apply when using DEVICESCAN (e.g., ['-H', '-m root@example.com'])"
  },
  "default_directives": {
    "type": "list",
    "default": [],
    "description": "Directives set via DEFAULT block \u2014 applied to subsequent devices until next DEFAULT"
  },
  "devices": {
    "type": "list",
    "default": [],
    "description": "List of devices to monitor, each with a name and list of directives"
  }
}