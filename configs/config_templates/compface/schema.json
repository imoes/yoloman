{
  "format": {
    "type": "string",
    "default": "compressed",
    "description": "Output format: 'compressed' for compface's native format, or 'xbm' for X11 Bitmap output (only valid for uncompface)",
    "enum": [
      "compressed",
      "xbm"
    ]
  },
  "file_pairs": {
    "type": "list",
    "default": [],
    "description": "List of input-output file pairs to process. Each entry may omit either field (defaults to '-' for stdin/stdout). Empty list means process stdin to stdout once (no extra pairs)."
  }
}
