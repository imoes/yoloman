{
  "name": {
    "type": "string",
    "default": "nova-api",
    "description": "Short name of the service for the DESC string"
  },
  "project_name": {
    "type": "string",
    "default": "nova",
    "description": "The OpenStack project name, used for paths and variable prefixes"
  },
  "uwsgi_port": {
    "type": "number",
    "default": 8774,
    "description": "The port number the UWSGI server listens on"
  },
  "uwsgi_ini_path": {
    "type": "string",
    "default": "/etc/nova/nova-api-uwsgi.ini",
    "description": "Path to the UWSGI configuration file"
  }
}
