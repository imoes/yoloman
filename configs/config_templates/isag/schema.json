{
  "datafiles_path": {
    "type": "string",
    "default": "/var/log/sysstat",
    "description": "The pathname where are located the daily data files."
  },
  "gr_height": {
    "type": "number",
    "default": 400,
    "description": "Specify the height of the chart area."
  },
  "gr_width": {
    "type": "number",
    "default": 720,
    "description": "Specify the width of the chart area."
  },
  "y_limits": {
    "type": "list",
    "default": [],
    "description": "Y-axis limits for each graph type. Each item specifies the graph name and min/max values."
  }
}
