{
 "algorithm": {
  "default": "base64",
  "description": "Encoding algorithm: 'base16', 'base32', 'base32hex', 'base64', 'base64url', 'base64pem', or 'base64mime'",
  "enum": [
   "base16",
   "base32",
   "base32hex",
   "base64",
   "base64url",
   "base64pem",
   "base64mime"
  ],
  "type": "string"
 },
 "case_output": {
  "description": "Case for alphabetic output characters: 'upper', 'lower', or empty for default",
  "enum": [
   "upper",
   "lower"
  ],
  "type": "string"
 },
 "decode_behavior": {
  "default": "",
  "description": "Decoding robustness: 'strict', 'ignore_all_space', 'ignore_garbage', or empty for default",
  "type": "string"
 },
 "input_file": {
  "default": "",
  "description": "Path to input file; empty string means stdin",
  "type": "string"
 },
 "mode": {
  "default": "encode",
  "description": "Operation mode: 'encode' or 'decode'",
  "enum": [
   "encode",
   "decode"
  ],
  "type": "string"
 },
 "output_file": {
  "default": "",
  "description": "Path to output file; empty string means stdout",
  "type": "string"
 },
 "text_line_endings": {
  "default": false,
  "description": "Convert native text line breaks ↔ CRLF (applies only to base64* modes)",
  "type": "bool"
 },
 "wrap_width": {
  "default": null,
  "description": "Line wrap width in characters; 0 disables all line breaks; null means use algorithm-specific default",
  "type": "number"
 }
}
