{
  "provides": {
    "type": "string",
    "default": "mysqmail-postfix-logger",
    "description": "The name of the service provided by this init script."
  },
  "short_description": {
    "type": "string",
    "default": "A MySQL traffic logger for the Postfix SMTP",
    "description": "Short description used in the LSB header."
  },
  "description": {
    "type": "string",
    "default": "This is a (very small) daemon will log all SMTP traffic\n                    to SQL, splitting the information by domains and\n                    users for the from and to field, so it's then very\n                    easy to count all the SMTP traffic for a given\n                    domain name.",
    "description": "Full description used in the LSB header."
  },
  "desc": {
    "type": "string",
    "default": "MySQMail postfix logger",
    "description": "The descriptive name of the daemon used in log messages."
  },
  "name": {
    "type": "string",
    "default": "mysqmail-postfix-logger",
    "description": "The base name of the daemon, executable, and PID file."
  },
  "conf_file_base": {
    "type": "string",
    "default": "mysqmail",
    "description": "The base name of the configuration file (without extension)."
  }
}
