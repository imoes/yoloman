{
  "start_firehol": {
    "type": "string",
    "default": "NO",
    "description": "Control whether FireHOL starts automatically at boot: 'NO' (default), 'AUTO' (managed externally), or 'YES'. If set to 'AUTO', the WAIT_FOR_IFACE list is emptied. If set to 'YES', FireHOL starts at boot. 'NO' is the default disabled state.",
    "enum": [
      "NO",
      "AUTO",
      "YES"
    ]
  }
}
