{
  "mnt": {
    "type": "string",
    "default": "/mnt",
    "description": "Mount point for the floppy disk."
  },
  "floppy": {
    "type": "string",
    "default": "/dev/fd0",
    "description": "Device file for the floppy disk."
  },
  "boot_only_with_floppy": {
    "type": "string",
    "default": "no",
    "description": "If 'yes', script exits with error if floppy mount fails; otherwise continues.\n      \n    ",
    "enum": [
      "yes",
      "no"
    ]
  },
  "fstypes": {
    "type": "string",
    "default": "ext3,ext2,reiserfs,iso9660,vfat,auto",
    "description": "Comma-separated list of filesystem types to try when mounting the floppy."
  }
}
