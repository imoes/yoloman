# Configuration for the Icinga Web 2 PDF Export module.
# This module generates PDF reports using a headless browser.

[chrome]
# Path to the Chromium or Chrome binary used for PDF generation.
binary = "{{ chrome_binary | default('/usr/bin/chromium') }}"