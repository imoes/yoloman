{
  "config_file": {
    "type": "string",
    "default": "/etc/ziproxy/ziproxy.conf",
    "description": "Path to the main ziproxy configuration file"
  },
  "user": {
    "type": "string",
    "default": "ziproxy",
    "description": "System user to run the ziproxy daemon as"
  },
  "group": {
    "type": "string",
    "default": "ziproxy",
    "description": "System group to run the ziproxy daemon as"
  }
}
