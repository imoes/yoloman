{
  "http_port": {
    "type": "number",
    "default": "",
    "description": "The HTTP port Caddy listens on for the default site."
  },
  "address": {
    "type": "string",
    "default": "http://",
    "description": "The address Caddy binds to. Can be a domain, IP, or http:// for local testing."
  },
  "document_root": {
    "type": "string",
    "default": "/usr/share/caddy",
    "description": "The directory where site files are stored."
  },
  "caddyfile_dir": {
    "type": "string",
    "default": "Caddyfile.d/*.caddyfile",
    "description": "Glob pattern for importing additional Caddyfile configurations."
  }
}
