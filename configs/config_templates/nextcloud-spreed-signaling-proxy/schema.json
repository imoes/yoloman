{
  "http_listen": {
    "type": "string",
    "default": "127.0.0.1:9090",
    "description": "IP and port to listen on for HTTP requests."
  },
  "app_debug": {
    "type": "bool",
    "default": false,
    "description": "Set to true to install pprof debug handlers."
  },
  "app_country": {
    "type": "string",
    "default": "DE",
    "description": "ISO 3166 country this proxy is located at."
  },
  "app_tokentype": {
    "type": "string",
    "default": "static",
    "description": "Type of token configuration for signaling servers allowed to connect.",
    "enum": [
      "static",
      "etcd"
    ]
  },
  "tokens_server1": {
    "type": "string",
    "default": "pubkey1.pem",
    "description": "Public key for the first signaling server token."
  },
  "tokens_server2": {
    "type": "string",
    "default": "pubkey2.pem",
    "description": "Public key for the second signaling server token."
  },
  "etcd_keyformat": {
    "type": "string",
    "default": "/signaling/proxy/tokens/%s/public-key",
    "description": "Format of key name to retrieve the public key from etcd."
  },
  "mcu_type": {
    "type": "string",
    "default": "janus",
    "description": "The type of the MCU to use."
  },
  "mcu_url": {
    "type": "string",
    "default": "ws://localhost:8188/",
    "description": "The URL to the websocket endpoint of the MCU server."
  },
  "mcu_maxstreambitrate": {
    "type": "number",
    "default": 1048576,
    "description": "The maximum bitrate per publishing stream (in bits per second)."
  },
  "mcu_maxscreenbitrate": {
    "type": "number",
    "default": 2097152,
    "description": "The maximum bitrate per screensharing stream (in bits per second)."
  },
  "stats_allowed_ips": {
    "type": "string",
    "default": "",
    "description": "Comma-separated list of IP addresses that are allowed to access the stats endpoint."
  },
  "etcd_endpoints": {
    "type": "string",
    "default": "127.0.0.1:2379,127.0.0.1:22379,127.0.0.1:32379",
    "description": "Comma-separated list of static etcd endpoints to connect to."
  },
  "etcd_discoverysrv": {
    "type": "string",
    "default": "example.com",
    "description": "DNS SRV domain to perform endpoint discovery."
  },
  "etcd_discoveryservice": {
    "type": "string",
    "default": "foo",
    "description": "Service name for DNS SRV discovery."
  },
  "etcd_clientkey": {
    "type": "string",
    "default": "/path/to/etcd-client.key",
    "description": "Path to private key for TLS authentication with etcd."
  },
  "etcd_clientcert": {
    "type": "string",
    "default": "/path/to/etcd-client.crt",
    "description": "Path to client certificate for TLS authentication with etcd."
  },
  "etcd_cacert": {
    "type": "string",
    "default": "/path/to/etcd-ca.crt",
    "description": "Path to CA certificate for TLS authentication with etcd."
  }
}
