{
  "run_user": {
    "type": "string",
    "default": "bird",
    "description": "User under which BIRD daemon runs."
  },
  "run_group": {
    "type": "string",
    "default": "bird",
    "description": "Group under which BIRD daemon runs."
  },
  "extra_args": {
    "type": "string",
    "default": "",
    "description": "Additional arguments passed to the BIRD daemon via BIRD_ARGS (uncommented)."
  }
}
