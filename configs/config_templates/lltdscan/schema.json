{
  "interface": {
    "type": "string",
    "default": "eth0",
    "description": "Network interface to send LLTD broadcasts on."
  },
  "timeout": {
    "type": "number",
    "default": 5000,
    "description": "Timeout in milliseconds to wait for responses."
  },
  "utf8": {
    "type": "bool",
    "default": false,
    "description": "Enable UTF-8 display for machine names."
  },
  "verbose": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level. 0 for quiet, 1 for standard, 2 for debug."
  },
  "target_mac": {
    "type": "string",
    "default": "",
    "description": "Specific MAC address to scan for. Leave empty to scan all."
  }
}
