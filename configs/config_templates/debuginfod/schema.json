{
  "base_cache_dir": {
    "type": "string",
    "default": "",
    "description": "Location of the cache where downloaded files are kept ($DEBUGINFOD_CACHE_PATH)."
  },
  "tmp_dir": {
    "type": "string",
    "default": "/tmp",
    "description": "File system to be used for temporary files ($TMPDIR)."
  },
  "listen_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address the server binds to. Both IPv4 and IPv6 sockets are opened if possible."
  },
  "listen_port": {
    "type": "number",
    "default": 8002,
    "description": "TCP port number on which debuginfod should listen."
  },
  "worker_count": {
    "type": "number",
    "default": 1,
    "description": "Concurrency limit for the scanning queue threads. The default is related to the number of processors; minimum is 1."
  },
  "max_connections": {
    "type": "number",
    "default": 0,
    "description": "Size of the pool of threads serving webapi queries (0 for automatic). Minimum 2 if specified."
  },
  "cache_max_age": {
    "type": "number",
    "default": 300,
    "description": "Rescan time for the file and archive directories in seconds."
  },
  "max_cache_size": {
    "type": "number",
    "default": 86400,
    "description": "Groom time for the index database in seconds."
  },
  "db_path": {
    "type": "string",
    "default": "$HOME/.debuginfod.sqlite",
    "description": "Path of the sqlite database used to store the index."
  },
  "metadata_maxtime": {
    "type": "number",
    "default": 5,
    "description": "Limit on the runtime of metadata webapi queries in seconds. Set 0 to disable."
  },
  "fdcache_mbs": {
    "type": "number",
    "default": 0,
    "description": "Cache limit in megabytes for recently extracted files from archives."
  },
  "fdcache_prefetch": {
    "type": "number",
    "default": 0,
    "description": "Number of other files from an archive to prefetch into the cache."
  },
  "fdcache_mintmp": {
    "type": "number",
    "default": 25,
    "description": "Disk space threshold percentage for emergency flushing of the caches."
  },
  "forwarded_ttl_limit": {
    "type": "number",
    "default": 8,
    "description": "Limit of X-Forwarded-For hops."
  },
  "scan_checkpoint": {
    "type": "number",
    "default": 256,
    "description": "Run a synchronized SQLITE WAL checkpoint operation after every NUM completed archive or file scans. 0 to disable."
  },
  "log_level": {
    "type": "number",
    "default": 0,
    "description": "Verbosity of logging. 0 is default. Increase to add details."
  },
  "upstream_sources": {
    "type": "list",
    "default": [],
    "description": "List of URL prefixes for trusted debuginfod instances ($DEBUGINFOD_URLS)."
  },
  "passive": {
    "type": "bool",
    "default": false,
    "description": "Set the server to passive mode, where it only services webapi requests."
  }
}
