{
  "adminer_alias_path": {
    "type": "string",
    "default": "/etc/adminer",
    "description": "Filesystem path where Adminer is aliased (must match Alias directive). Typically /etc/adminer or /usr/share/adminer."
  }
}
