{
  "ignore": {
    "type": "list",
    "default": [],
    "description": "List of newline-separated filename patterns to ignore (in addition to the default ignore list)."
  },
  "ignore-bad-ideas": {
    "type": "list",
    "default": [],
    "description": "List of newline-separated filename patterns to ignore specifically for generated files (e.g., build artifacts)."
  }
}
