{
  "sources": {
    "type": "list",
    "default": [
      "proc"
    ],
    "description": "List of sources kacpimon should connect to. Valid values are 'proc' (/proc/acpi/event), 'netlink', and 'input' (/dev/input/event*)."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose/debugging output to stderr."
  },
  "output_format": {
    "type": "string",
    "default": "text",
    "description": "Format for the event output. Either 'text' or 'json'."
  }
}
