{
  "resolv_conf": {
    "type": "string",
    "default": "/etc/resolv.conf",
    "description": "Path to the resolv.conf file managed by resolvconf."
  },
  "name_servers": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Local name server IP address. Leave commented if not using a local DNS server."
  }
}
