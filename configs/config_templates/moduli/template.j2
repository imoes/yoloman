# /etc/ssh/moduli — Diffie-Hellman group parameters for SSH key exchange
# Each line: bits type generator modulus (hex) optional comment
{% for group in moduli_groups %}{{ group.bits }} {{ group.type }} {{ group.generator }} {{ group.modulus }}{% if group.comment %} # {{ group.comment }}{% endif %}{% endfor %}