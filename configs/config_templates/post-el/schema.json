{
  "post_insert_auto_mode_default": {
    "type": "bool",
    "default": true,
    "description": "Whether to automatically insert post-mode into auto-mode-alist on load."
  },
  "site_lisp_path": {
    "type": "string",
    "default": "site-lisp",
    "description": "The relative path under /usr/share/ where post-el is installed (e.g., site-lisp)."
  }
}
