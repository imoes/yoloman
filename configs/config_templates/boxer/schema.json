{
  "suite": {
    "type": "string",
    "default": "stretch",
    "description": "Debian suite (e.g., stretch, buster, bullseye, bookworm, trixie)",
    "enum": [
      "stretch",
      "buster",
      "trixie",
      "unstable"
    ],
    "enum_open": true
  },
  "data_dir": {
    "type": "string",
    "default": "examples",
    "description": "Path to Boxer data directory (e.g., where part definitions live)"
  }
}
