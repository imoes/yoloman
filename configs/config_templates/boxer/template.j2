{# Boxer deployment manifest #}
{# Suite: Debian suite (e.g., stretch, buster, bullseye, bookworm, trixie) #}
suite: {{ suite | default('stretch') }}

{# Data Dir: Path to Boxer data directory (e.g., where part definitions live) #}
data_dir: {{ data_dir | default('examples') }}