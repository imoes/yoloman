{
  "arch": {
    "type": "string",
    "default": "amd64",
    "description": "Target architecture for the udebs (e.g., amd64, i386, arm64)"
  },
  "kernel_package": {
    "type": "string",
    "default": "linux",
    "description": "Source package name for the kernel tree"
  },
  "output_dir": {
    "type": "string",
    "default": "/tmp/udebs",
    "description": "Directory where the generated .udeb files will be stored"
  },
  "kernels": {
    "type": "list",
    "default": [],
    "description": "List of kernel variants to split into udebs. Each item must conform to the items schema below."
  },
  "debug_level": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level of the kernel-wedge output (0-2)"
  },
  "parallel_build": {
    "type": "bool",
    "default": true,
    "description": "Whether to build kernel modules in parallel"
  }
}
