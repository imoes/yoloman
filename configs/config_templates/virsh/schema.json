{
  "hostip": {
    "type": "string",
    "default": "",
    "description": "Hostname or IP address of the KVM/libvirt hypervisor hosting the VM."
  },
  "login": {
    "type": "string",
    "default": "root",
    "description": "Username used to authenticate to the hypervisor (SSH or libvirt URI)."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Password for the login user. For security, prefer using 'password_file'."
  },
  "password_file": {
    "type": "string",
    "default": "",
    "description": "Absolute path to a file containing the password (e.g., /etc/fence_agents/credentials). Recommended for production."
  },
  "identity_file": {
    "type": "string",
    "default": "",
    "description": "Path to SSH private key for key-based authentication. Overrides password if set."
  },
  "port": {
    "type": "number",
    "default": 22,
    "description": "SSH port on the hypervisor."
  },
  "plug": {
    "type": "string",
    "default": "",
    "description": "Name of the VM (guest domain) to fence. Required for multi-VM setups or non-default naming."
  },
  "action_delay": {
    "type": "number",
    "default": 0,
    "description": "Seconds to wait after sending a fence command before checking status or retrying."
  },
  "retry": {
    "type": "number",
    "default": 3,
    "description": "Maximum number of attempts to execute the fence action (e.g., off/on)."
  },
  "retry_timeout": {
    "type": "number",
    "default": 0,
    "description": "Maximum total time (in seconds) to wait for a fence action to complete. Overrides retry count when reached."
  },
  "tls": {
    "type": "boolean",
    "default": false,
    "description": "Enable TLS for the libvirt connection (requires libvirtd configured with TLS)."
  },
  "tls_cert_dir": {
    "type": "string",
    "default": "",
    "description": "Path to directory containing TLS certificates (client.key, client.crt, ca.crt). Enables TLS automatically if set."
  },
  "tls_hostname": {
    "type": "string",
    "default": "",
    "description": "Expected hostname in TLS certificate for verification (e.g., if hypervisor is accessed by FQDN)."
  },
  "ca_file": {
    "type": "string",
    "default": "",
    "description": "Path to CA certificate bundle used to verify the hypervisor's TLS certificate."
  },
  "use_uri": {
    "type": "boolean",
    "default": false,
    "description": "Use direct libvirt URI instead of SSH transport. Requires valid libvirt URI."
  },
  "uri": {
    "type": "string",
    "default": "",
    "description": "Libvirt connection URI (e.g., 'qemu+ssh://user@host/system'). Only used if use_uri=1."
  },
  "options": {
    "type": "string",
    "default": "",
    "description": "Extra options to pass to fence_virsh (comma-separated). Avoid unless absolutely necessary."
  }
}
