# ==============================================================================
# congruity Configuration Template
# ==============================================================================
#
# Package: congruity
# Description: Logitech Harmony remote programming GUI configuration placeholder.
#
# NOTE: This service (congruity) is a CLI-driven tool that operates solely on
# external .EZHex/.EZUp/.EZTut files provided by the Harmony website. It does
# not ship a standard configuration file, nor does it support runtime
# configuration parameters.
#
# This template is provided as a schema placeholder for Ansible automation
# purposes. Any variables defined here are strictly for structural completeness
# and have no effect on the runtime behavior of congruity.
#
# See congruity(1) man page for details.
# ==============================================================================

# --- Placeholder Schema Fields ---
# These fields exist only to satisfy the role's schema definition.
# They are not processed by the congruity application.

# dummy: Placeholder field only; congruity has no configurable parameters.
# Default: False
# Type: boolean
{% set _dummy = dummy | default(false) %}
# dummy = {{ _dummy }}