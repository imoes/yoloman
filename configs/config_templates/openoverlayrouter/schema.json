{
  "debug": {
    "type": "number",
    "default": 0,
    "description": "Debug level [0..3]. Higher values produce more verbose logs."
  },
  "map_request_retries": {
    "type": "number",
    "default": 2,
    "description": "Additional Map-Requests to send per map cache miss."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/oor.log",
    "description": "Log file path when running in daemon mode."
  },
  "operating_mode": {
    "type": "string",
    "default": "xTR",
    "description": "LISP device operating mode: 'xTR', 'RTR', 'MN', or 'MS'. Enum: xTR, RTR, MN, MS",
    "enum": [
      "xTR",
      "RTR",
      "MN",
      "MS"
    ]
  },
  "control_iface": {
    "type": "string",
    "default": "",
    "description": "Interface for control messages (MS mode). Required for MS operating mode."
  },
  "ms_eid_prefix": {
    "type": "string",
    "default": "",
    "description": "Accepted EID prefix for Map Server registration (MS mode). e.g., '10.0.0.0/24'."
  },
  "ms_key": {
    "type": "string",
    "default": "",
    "description": "Password to authenticate Map-Registers received by the Map Server (MS mode)."
  },
  "ms_accept_more_specifics": {
    "type": "bool",
    "default": true,
    "description": "Accept more specific prefixes with same authentication information for MS lisp-site."
  },
  "ms_static_eid_prefix": {
    "type": "string",
    "default": "",
    "description": "EID prefix for static registered site in Map Server (MS mode)."
  },
  "ms_static_rloc_address": {
    "type": "string",
    "default": "",
    "description": "RLOC address (IPv4/IPv6/ELP) for MS static registered site."
  },
  "ms_static_rloc_priority": {
    "type": "number",
    "default": 0,
    "description": "Priority for the RLOC of the MS static registered site [0-255]."
  },
  "ms_static_rloc_weight": {
    "type": "number",
    "default": 0,
    "description": "Weight for the RLOC of the MS static registered site [0-255]."
  },
  "encapsulation": {
    "type": "string",
    "default": "LISP",
    "description": "Encapsulation type for data plane: 'LISP' or 'VXLAN-GPE'.",
    "enum": [
      "LISP",
      "VXLAN-GPE"
    ]
  },
  "rloc_probe_interval": {
    "type": "number",
    "default": 30,
    "description": "Interval in seconds for periodic RLOC probes."
  },
  "rloc_probe_retries": {
    "type": "number",
    "default": 2,
    "description": "RLOC probe retries before setting locator down [0..5]."
  },
  "rloc_probe_retries_interval": {
    "type": "number",
    "default": 5,
    "description": "Interval in seconds for RLOC probe retries [1..rloc-probe-interval]."
  },
  "map_resolver_address": {
    "type": "string",
    "default": "",
    "description": "IPv4 or IPv6 address of the Map-Resolver."
  },
  "static_map_eid_prefix": {
    "type": "string",
    "default": "",
    "description": "EID prefix for static map cache entry."
  },
  "static_map_rloc_address": {
    "type": "string",
    "default": "",
    "description": "RLOC address for static map cache entry."
  },
  "static_map_rloc_priority": {
    "type": "number",
    "default": 0,
    "description": "Priority for RLOC in static map cache [0-255]."
  },
  "static_map_rloc_weight": {
    "type": "number",
    "default": 0,
    "description": "Weight for RLOC in static map cache [0-255]."
  },
  "rtr_iface_name": {
    "type": "string",
    "default": "",
    "description": "Interface name for RTR outgoing traffic (RTR mode)."
  },
  "rtr_iface_ip_version": {
    "type": "number",
    "default": 4,
    "description": "IP version for RTR interface: 4 or 6."
  },
  "rtr_iface_priority": {
    "type": "number",
    "default": 0,
    "description": "Priority for RTR interface RLOC [0-255]."
  },
  "rtr_iface_weight": {
    "type": "number",
    "default": 0,
    "description": "Weight for RTR interface RLOC [0-255]."
  },
  "nat_traversal_support": {
    "type": "string",
    "default": "off",
    "description": "NAT traversal support: 'on' or 'off'. Required for xTR/MN if behind NAT.",
    "enum": [
      "on",
      "off"
    ]
  },
  "map_server_address": {
    "type": "string",
    "default": "",
    "description": "IPv4 or IPv6 address of the Map-Server."
  },
  "map_server_key": {
    "type": "string",
    "default": "",
    "description": "Password to authenticate with the Map-Server."
  },
  "map_server_proxy_reply": {
    "type": "string",
    "default": "off",
    "description": "Configure map-server to Map-Reply on behalf of the xTR: 'on' or 'off'.",
    "enum": [
      "on",
      "off"
    ]
  },
  "proxy_etr_address": {
    "type": "string",
    "default": "",
    "description": "IPv4 or IPv6 address of the Proxy-ETR."
  },
  "proxy_etr_priority": {
    "type": "number",
    "default": 0,
    "description": "Priority for Proxy-ETR [0-255]."
  },
  "proxy_etr_weight": {
    "type": "number",
    "default": 0,
    "description": "Weight for Proxy-ETR [0-255]."
  },
  "database_mapping_eid_prefix": {
    "type": "string",
    "default": "",
    "description": "EID prefix (ip-prefix/mask) for the node's database mapping."
  },
  "database_mapping_rloc_address": {
    "type": "string",
    "default": "",
    "description": "RLOC address for the node's database mapping."
  },
  "database_mapping_rloc_priority": {
    "type": "number",
    "default": 0,
    "description": "Priority for RLOC in database mapping [0-255]."
  },
  "database_mapping_rloc_weight": {
    "type": "number",
    "default": 0,
    "description": "Weight for RLOC in database mapping [0-255]."
  },
  "database_mapping_rloc_iface": {
    "type": "string",
    "default": "",
    "description": "Interface name associated with RLOC in database mapping."
  },
  "database_mapping_rloc_iface_ip_version": {
    "type": "number",
    "default": 4,
    "description": "IP version for RLOC interface in database mapping: 4 or 6."
  },
  "database_mapping_rloc_iface_priority": {
    "type": "number",
    "default": 0,
    "description": "Priority for RLOC interface in database mapping [0-255]."
  },
  "database_mapping_rloc_iface_weight": {
    "type": "number",
    "default": 0,
    "description": "Weight for RLOC interface in database mapping [0-255]."
  },
  "elp_name": {
    "type": "string",
    "default": "",
    "description": "Name to define the Explicit Locator Path (ELP)."
  },
  "elp_node1_address": {
    "type": "string",
    "default": "",
    "description": "RLOC address of the first ELP node."
  },
  "elp_node1_strict": {
    "type": "bool",
    "default": false,
    "description": "Strict routing for first ELP node (not yet implemented, use false)."
  },
  "elp_node1_probe": {
    "type": "bool",
    "default": false,
    "description": "Probe setting for first ELP node (not yet implemented, use false)."
  },
  "elp_node1_lookup": {
    "type": "bool",
    "default": false,
    "description": "Lookup setting for first ELP node (not yet implemented, use false)."
  },
  "elp_node2_address": {
    "type": "string",
    "default": "",
    "description": "RLOC address of the second ELP node."
  },
  "elp_node2_strict": {
    "type": "bool",
    "default": false,
    "description": "Strict routing for second ELP node (not yet implemented, use false)."
  },
  "elp_node2_probe": {
    "type": "bool",
    "default": false,
    "description": "Probe setting for second ELP node (not yet implemented, use false)."
  },
  "elp_node2_lookup": {
    "type": "bool",
    "default": false,
    "description": "Lookup setting for second ELP node (not yet implemented, use false)."
  }
}
