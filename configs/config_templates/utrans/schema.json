{
  "backend_list": {
    "type": "list",
    "default": [
      "openrc",
      "xinetd",
      "cron"
    ],
    "description": "Comma-separated list of backends to load. Available: openrc (service), xinetd (socket), cron (timer), inetd (socket), lsb (service). Prefix with '=' to exclude defaults."
  },
  "force_mode": {
    "type": "string",
    "default": "none",
    "description": "Force mode for translation. Valid types: 'overwrite' (allow overwriting existing output files) or 'none'."
  }
}
