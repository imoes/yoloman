{
  "log_file": {
    "type": "string",
    "default": "/var/log/sendmail.log",
    "description": "Path to the log file for the sendmail emulator. Used by nullmailer-inject/smtpd."
  },
  "my_hostname": {
    "type": "string",
    "default": "localhost",
    "description": "Fully qualified domain name (FQDN) of this host \u2014 used in SMTP banner and envelope."
  },
  "log_level": {
    "type": "number",
    "default": 9,
    "description": "Logging verbosity: 0 (minimal) to 14 (debug). Higher values increase log detail."
  },
  "queue_interval": {
    "type": "number",
    "default": 900,
    "description": "Seconds between automatic queue runs (0 = disable automatic queue runs; rely on cron or manual `nullmailer-queue` or `sendmail -q`)"
  },
  "max_message_size": {
    "type": "number",
    "default": 10485760,
    "description": "Maximum accepted message size in bytes (0 = no limit). Recommended: 10 MB (10485760 bytes)."
  }
}
