{
  "plugin_path": {
    "type": "string",
    "default": "~/.claws-mail/plugins",
    "description": "Path to Claws Mail plugins directory; pgpmime.so is typically installed in this path."
  },
  "enable_plugin": {
    "type": "bool",
    "default": false,
    "description": "If true, add '[plugins] pgpmime=1' to explicitly enable the pgpmime plugin."
  },
  "gpg_conf": {
    "type": "string",
    "default": "~/.gnupg/gpg.conf",
    "description": "Path to the GnuPG configuration file used by pgpmime for signature verification and decryption."
  }
}
