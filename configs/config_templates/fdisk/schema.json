{
  "device": {
    "type": "string",
    "default": "/dev/sda",
    "description": "Target block device to partition (e.g., /dev/sda). Must be a real block device path."
  },
  "fdisk_cmdline_options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options to pass to fdisk. Each item is a single argument string (e.g., '-c=dos', '-u=sectors'). Options must be documented in fdisk(8). Important options include --lock for exclusive locking."
  }
}
