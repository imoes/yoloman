{
 "PURGE_OPTS": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "purge_action_bits": {
  "default": 0,
  "description": "Action bits: 0 (print only), 1 (send PURGE), 2 (unlink weird files).",
  "type": "number"
 },
 "purge_debug_level": {
  "default": 0,
  "description": "Debug level for purge output.",
  "type": "number"
 },
 "purge_expression_file": {
  "default": null,
  "description": "Path to a file containing regular expressions. If null, uses inline expressions or none.",
  "type": "string"
 },
 "purge_expressions": {
  "default": [],
  "description": "List of regular expressions to search for/purge. Use single-item lists for -E or multiple for repeated -E flags.",
  "type": "list"
 },
 "purge_host": {
  "default": "localhost:3128",
  "description": "Host and port of the Squid cache to connect to.",
  "type": "string"
 },
 "purge_keep_headers": {
  "default": false,
  "description": "If true and rescue mode is active, keeps HTTP headers in rescued files.",
  "type": "bool"
 },
 "purge_rescue_dir": {
  "default": null,
  "description": "Directory to copy rescued cache objects to. If null, copy-out mode is disabled.",
  "type": "string"
 },
 "purge_sequential_mode": {
  "default": false,
  "description": "If true, disables parallel execution (-n flag) to reduce system load.",
  "type": "bool"
 },
 "purge_squid_conf_path": {
  "default": "/etc/squid/squid.conf",
  "description": "Path to the squid.conf file used to discover cache directories.",
  "type": "string"
 },
 "purge_verbose": {
  "default": false,
  "description": "If true, enables verbose output.",
  "type": "bool"
 }
}
