{
  "xymon_document_root": {
    "type": "string",
    "default": "/var/lib/xymon/www",
    "description": "The DocumentRoot when Xymon webfiles are served as the root URL."
  },
  "xymon_web_root": {
    "type": "string",
    "default": "/var/lib/xymon/www",
    "description": "The filesystem path for the Xymon web interface files."
  },
  "xymon_cgi_root": {
    "type": "string",
    "default": "/usr/lib/xymon/cgi-bin/",
    "description": "The filesystem path for the standard Xymon CGI scripts."
  },
  "xymon_seccgi_root": {
    "type": "string",
    "default": "/usr/lib/xymon/cgi-secure/",
    "description": "The filesystem path for the secure/admin Xymon CGI scripts."
  },
  "xymon_auth_passwd_file": {
    "type": "string",
    "default": "/etc/xymon/xymonpasswd",
    "description": "The path to the htpasswd file for secure CGI access authentication."
  },
  "xymon_auth_group_file": {
    "type": "string",
    "default": "/etc/xymon/xymongroups",
    "description": "The path to the group file for restricting admin access by group membership."
  }
}
