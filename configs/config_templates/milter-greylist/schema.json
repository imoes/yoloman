{
  "pid_file": {
    "type": "string",
    "default": "/var/run/milter-greylist.pid",
    "description": "Path to the PID file for the milter-greylist daemon."
  },
  "socket_path": {
    "type": "string",
    "default": "/var/run/milter-greylist/greylist.sock",
    "description": "Path to the Unix socket used by milter-greylist."
  },
  "run_user": {
    "type": "string",
    "default": "root",
    "description": "User under which the milter-greylist daemon should run."
  },
  "options": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options passed to the milter-greylist daemon."
  }
}
