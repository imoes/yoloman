{
  "daemion_path": {
    "type": "string",
    "default": "/usr/lib/xymon/client/bin/xymonlaunch",
    "description": "Path to the xymonlaunch daemon executable."
  },
  "pidfile_path": {
    "type": "string",
    "default": "/var/run/xymon/clientlaunch.pid",
    "description": "Path to the PID file for the xymon-client service."
  },
  "xymon_client_home": {
    "type": "string",
    "default": "/usr/lib/xymon/client",
    "description": "Home directory for the xymon client installation."
  },
  "client_log_path": {
    "type": "string",
    "default": "/var/log/xymon/clientlaunch.log",
    "description": "Path to the log file for the xymon client launch process."
  }
}
