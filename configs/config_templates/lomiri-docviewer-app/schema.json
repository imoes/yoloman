{
  "start_file": {
    "type": "string",
    "default": "",
    "description": "Path to the document file to open automatically. If empty, the application will prompt the user to select a file."
  }
}
