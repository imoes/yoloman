{
  "zones": {
    "type": "list",
    "default": [
      "America/New_York",
      "Europe/London",
      "Asia/Tokyo"
    ],
    "description": "List of time zones in TZ variable format (e.g., America/New_York). Each zone appears on its own line in the config file."
  },
  "format": {
    "type": "string",
    "default": "%A %Y-%m-%d %H:%M:%S %Z",
    "description": "Time display format string compatible with the date command."
  }
}
