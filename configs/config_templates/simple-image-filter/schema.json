{
  "input_dir": {
    "type": "string",
    "default": "/var/lib/simple-image-filter/in",
    "description": "The directory where input images are placed for processing."
  },
  "output_dir": {
    "type": "string",
    "default": "/var/lib/simple-image-filter/out",
    "description": "The directory where processed output images are saved."
  },
  "quality": {
    "type": "number",
    "default": 85,
    "description": "The compression quality level for output images, typically between 0 and 100."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable verbose logging mode."
  }
}
