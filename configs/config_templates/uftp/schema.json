{
  "name": {
    "type": "string",
    "default": "uftpd",
    "description": "The name of the uftpd daemon process."
  },
  "daemon_name": {
    "type": "string",
    "default": "uftpd",
    "description": "The basename of the uftpd binary executable."
  },
  "pidfile": {
    "type": "string",
    "default": "/var/run/uftpd.pid",
    "description": "The path to the PID file for the uftpd daemon."
  },
  "scriptname": {
    "type": "string",
    "default": "/etc/init.d/ufto",
    "description": "The path to this init script itself."
  }
}
