{
  "host": {
    "type": "string",
    "default": "http://http.us.debian.org/debian",
    "description": "The base URL of the jigit update site."
  },
  "tmpdir": {
    "type": "string",
    "default": "/tmp",
    "description": "Directory where jigit stores temporary files for download and ISO construction."
  }
}
