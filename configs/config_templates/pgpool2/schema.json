{
  "syslog_facility": {
    "type": "string",
    "default": "local0",
    "description": "Syslog facility for pgpool logs (e.g., local0, daemon). It must be one of the available syslog facilities.",
    "enum": [
      "syslog",
      "local0"
    ],
    "enum_open": true
  },
  "log_debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debugging messages in the log."
  }
}
