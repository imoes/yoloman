{
  "config_file_path": {
    "type": "string",
    "default": "/etc/globus/globus-gram-job-manager.conf",
    "description": "Path to the main Globus GRAM job manager configuration file"
  },
  "job_manager_type": {
    "type": "string",
    "default": "pbs",
    "description": "Type of job manager backend (e.g., pbs, slurm, etc.)"
  },
  "seg_module": {
    "type": "string",
    "default": "pbs",
    "description": "Name of the SEG (Simple Execution Gateway) module to load"
  }
}
