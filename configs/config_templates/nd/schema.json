{
  "http_proxy": {
    "type": "string",
    "default": "",
    "description": "The URL of the HTTP proxy to use for WebDAV requests. If empty, no proxy is used."
  },
  "lock_owner": {
    "type": "string",
    "default": "nd-user",
    "description": "The default owner name to use for file locks if the -o option is not explicitly provided. Mirrors the USER environment variable behavior."
  }
}
