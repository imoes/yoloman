{
  "source_file": {
    "type": "string",
    "default": "rules",
    "description": "The input file containing iptables commands. If omitted, defaults to 'rules'."
  },
  "dest_file": {
    "type": "string",
    "default": "",
    "description": "The output file for the converted format. If empty, output goes to stdout."
  },
  "sloppy": {
    "type": "bool",
    "default": false,
    "description": "If true, indicates that '-N UserChain' is not required prior to mentioning any UserChain input or append."
  }
}
