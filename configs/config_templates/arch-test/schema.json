{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether arch-test configuration is active."
  },
  "test_all": {
    "type": "bool",
    "default": false,
    "description": "If true, run arch-test without arguments to enumerate runnable architectures."
  },
  "test_single_arch": {
    "type": "string",
    "default": "",
    "description": "Architecture name (e.g., 'amd64', 'armhf') to test individually. Empty string means disabled."
  },
  "chroot_path": {
    "type": "string",
    "default": "",
    "description": "Chroot path for testing architectures inside a chroot environment (requires root). Ignored unless test_single_arch is set."
  }
}
