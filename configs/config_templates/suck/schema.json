{
  "log_directory": {
    "type": "string",
    "default": "log",
    "description": "The subdirectory name within /var/log for suck log files."
  },
  "log_rotate_count": {
    "type": "number",
    "default": 2,
    "description": "Number of rotated log files to keep."
  },
  "log_user": {
    "type": "string",
    "default": "news",
    "description": "User to switch to for log rotation."
  },
  "log_group": {
    "type": "string",
    "default": "news",
    "description": "Group to switch to for log rotation."
  }
}
