{
  "renderd": {
    "type": "object",
    "default": null,
    "description": "Global settings for the renderd daemon, including threading, timeouts, and Mapnik XML paths. Also used for init script socket path extraction (only the socket name base is used for PIDSOCKDIR)."
  }
}
