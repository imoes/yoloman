{
  "cron_schedule": {
    "type": "string",
    "default": "*/2\t* * * *",
    "description": "Cron schedule for running ceph-info"
  },
  "cron_user": {
    "type": "string",
    "default": "root",
    "description": "User to run the ceph-info command as"
  }
}
