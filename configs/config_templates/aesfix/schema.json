{
  "schedule_file": {
    "type": "string",
    "default": "/etc/aesfix/key_schedule.hex",
    "description": "Path to the hex-encoded AES key schedule file to be corrected. Must exist and be readable by the aesfix process."
  }
}
