{# aesfix configuration file for Debian trixie (aesfix 1.0.1-8) #}
{# Only the input file is configurable; no runtime options exist. #}

# Path to the hex-encoded AES key schedule file to be corrected.
# Must exist and be readable by the aesfix process.
{{ schedule_file | default('/etc/aesfix/key_schedule.hex') }}