{
  "config_entries": {
    "type": "list",
    "default": [],
    "description": "List of configuration flags and optional values, each represented as a flat object with 'key' and 'value' fields. Only keys 'configfile', 'lang', 'langfile', and 'locale' are supported per gpsprune(1) options."
  }
}
