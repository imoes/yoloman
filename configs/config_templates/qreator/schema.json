{
  "location_address": {
    "type": "string",
    "default": "",
    "description": "Address string for -l/--location parameter."
  },
  "phone_number": {
    "type": "string",
    "default": "",
    "description": "Phone number string for -p/--phone parameter."
  },
  "text_content": {
    "type": "string",
    "default": "",
    "description": "Plain text content for -t/--text parameter."
  },
  "url_address": {
    "type": "string",
    "default": "",
    "description": "URL string for -u/--url parameter."
  },
  "businesscard_vcf": {
    "type": "string",
    "default": "",
    "description": "VCard (VCF) formatted string for -b/--businesscard parameter."
  },
  "wifi_ssid": {
    "type": "string",
    "default": "",
    "description": "WiFi SSID for -w/--wifi parameter."
  },
  "wifi_security": {
    "type": "string",
    "default": "nopass",
    "description": "WiFi security type (WEP, WPA, WPA2) for -w/--wifi parameter."
  },
  "wifi_password": {
    "type": "string",
    "default": "",
    "description": "WiFi password for -w/--wifi parameter."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose/debug output (-v/--verbose)."
  }
}
