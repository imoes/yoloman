{
  "policies": {
    "type": "list",
    "default": [],
    "description": "A list of timeout policy configurations. Each policy defines a named set of timeouts for specific protocol states, used with iptables CT target. See nfct(8) EXAMPLE section for syntax mapping: `nfct add timeout <name>` followed by `<proto> <state1> <val1> <state2> <val2> ...`."
  }
}
