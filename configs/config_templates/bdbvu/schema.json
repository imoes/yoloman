{
  "filename": {
    "type": "string",
    "default": "",
    "description": "Path to the Berkeley DB file to open. Note: bdbvu does not support a configuration file; this value would typically be passed via command line (bdbvu <filename>). This schema entry is retained for compatibility but is not applied by a config parser."
  }
}
