# bdbvu configuration
# No configuration file is supported; bdbvu is a Qt GUI tool that accepts
# a filename as a command-line argument.
# This template is retained for schema consistency but has no functional effect
# as the tool does not read a config file.

# Path to the Berkeley DB file to open. Note: bdbvu does not support a configuration file;
# this value would typically be passed via command line.
# {{ filename | default('') }}