{
  "device": {
    "type": "string",
    "default": "/sys/class/backlight/intel_backlight",
    "description": "The sysfs path to the backlight controller device."
  },
  "verbosity": {
    "type": "number",
    "default": 0,
    "description": "Log verbosity level (0-3)."
  },
  "min_brightness": {
    "type": "number",
    "default": 10,
    "description": "Minimum brightness value to prevent complete blackness."
  }
}
