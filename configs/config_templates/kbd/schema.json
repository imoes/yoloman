{
  "device": {
    "default": "",
    "description": "Optional input device path (e.g., '/dev/input/event0') to apply layout only to a specific keyboard. Leave blank to configure all keyboards system-wide.",
    "type": "string"
  },
  "layout": {
    "default": "us",
    "description": "Primary keyboard layout (e.g., 'us', 'de', 'fr', 'ru'). Use comma-separated values for multilingual setups (e.g., 'us,ru').",
    "enum": [
      "as",
      "be",
      "by",
      "in",
      "is",
      "ma",
      "mk",
      "no",
      "si",
      "sk",
      "us"
    ],
    "type": "string",
    "enum_open": true
  },
  "model": {
    "default": "pc105",
    "description": "Physical keyboard model. 'pc105' is standard for most PCs. Other common values include 'pc104', 'pc102', 'sun5'.",
    "type": "string"
  },
  "options": {
    "default": "",
    "description": "XKB options to customize behavior (e.g., switching layout with Alt+Shift, swapping Caps Lock and Ctrl). Leave empty for defaults.",
    "type": "string"
  },
  "rules": {
    "default": "base",
    "description": "XKB ruleset. 'base' works for most distributions; 'evdev' is used on some systems for better hardware support.",
    "type": "string"
  },
  "variant": {
    "description": "Optional layout variant(s) corresponding to each layout (e.g., 'dvorak', 'nodeadkeys'). Leave empty if using default variant(s).",
    "enum": [
      "grp:menu_toggle"
    ],
    "type": "string"
  }
}
