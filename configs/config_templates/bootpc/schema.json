{
  "bootpc_args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments passed directly to bootpc(8); each item is a complete argument string (e.g., '--server 192.168.1.1', '--dev eth0'). Must match documented options only."
  }
}
