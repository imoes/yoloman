{
  "slurm_conf": {
    "type": "string",
    "default": "/etc/slurm/slurm.conf",
    "description": "The location of the Slurm configuration file, set via the SLURM_CONF environment variable."
  }
}
