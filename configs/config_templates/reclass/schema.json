{
  "cache": {
    "default": {
      "enabled": false,
      "path": "/var/cache/reclass",
      "ttl": 3600
    },
    "description": "Optional caching layer to speed up repeated queries.",
    "items": {
      "enabled": {
        "default": false,
        "description": "Enable caching.",
        "type": "bool"
      },
      "path": {
        "default": "/var/cache/reclass",
        "description": "Directory to store cache files.",
        "type": "string"
      },
      "ttl": {
        "default": 3600,
        "description": "Cache validity time in seconds (0 = never expire).",
        "type": "number"
      }
    },
    "type": "object"
  },
  "default_node_class": {
    "default": "",
    "description": "Default class name to apply to nodes without explicit classes (e.g., '_defaults').",
    "type": "string"
  },
  "diagnostics": {
    "default": {
      "show_missing_nodes": false
    },
    "description": "Toggles diagnostic features (e.g., for troubleshooting node resolution).",
    "items": {
      "show_missing_nodes": {
        "default": false,
        "description": "Print warnings for nodes not found in the inventory.",
        "type": "bool"
      }
    },
    "type": "object"
  },
  "external_data": {
    "description": "Key-value pairs for static external data (e.g., cloud provider metadata, secrets).",
    "items": {
      "key": {
        "default": "",
        "description": "External data provider name.",
        "type": "string"
      },
      "val": {
        "default": "",
        "description": "JSON-encoded external data payload.",
        "type": "string"
      }
    },
    "type": "object"
  },
  "interpolation": {
    "default": {
      "enabled": true
    },
    "description": "Controls whether reclass performs variable interpolation (e.g., ${var} \u2192 value) in values.",
    "items": {
      "enabled": {
        "default": true,
        "description": "Enable or disable interpolation of values.",
        "type": "bool"
      }
    },
    "type": "object"
  },
  "logging": {
    "default": {
      "file": "",
      "level": "WARNING"
    },
    "description": "Logging verbosity and destination.",
    "items": {
      "file": {
        "default": "",
        "description": "Path to log file (empty = stderr only).",
        "type": "string"
      },
      "level": {
        "default": "WARNING",
        "description": "Minimum log level (DEBUG, INFO, WARNING, ERROR, CRITICAL).",
        "type": "string"
      }
    },
    "type": "object"
  },
  "max_inheritance_depth": {
    "default": 20,
    "description": "Maximum class inheritance depth to prevent infinite loops (lower = safer).",
    "type": "number"
  },
  "merge_strategy": {
    "default": {
      "dicts": "deep",
      "lists": "append"
    },
    "description": "Strategy to combine dicts and lists during class merging.",
    "items": {
      "dicts": {
        "default": "deep",
        "description": "Dict merging: 'deep' (recursive merge) or 'overwrite'.",
        "type": "string"
      },
      "lists": {
        "default": "append",
        "description": "List merging: 'append' (union) or 'overwrite'.",
        "type": "string"
      }
    },
    "type": "object"
  },
  "metadata": {
    "description": "Configuration for optional external metadata endpoint (e.g., for dynamic node class injection).",
    "type": "object"
  },
  "name": {
    "default": "reclass",
    "description": "Logical name for this reclass instance; used for logging and isolation.",
    "type": "string"
  },
  "naming": {
    "default": {
      "class_name_pattern": "^[a-zA-Z0-9._-]+$",
      "node_name_pattern": "^[a-zA-Z0-9._-]+$"
    },
    "description": "Regex patterns to validate node and class names.",
    "items": {
      "class_name_pattern": {
        "default": "^[a-zA-Z0-9._-]+$",
        "description": "Regex to validate class names.",
        "type": "string"
      },
      "node_name_pattern": {
        "default": "^[a-zA-Z0-9._-]+$",
        "description": "Regex to validate node names.",
        "type": "string"
      }
    },
    "type": "object"
  },
  "node_name_transform": {
    "default": {
      "type": "none"
    },
    "description": "Optional normalization for incoming node names (e.g., strip domain name).",
    "items": {
      "type": {
        "default": "none",
        "description": "Transform type: 'none', 'lowercase', 'strip-domain'.",
        "type": "string"
      }
    },
    "type": "object"
  },
  "output": {
    "default": {
      "allowed_types": [
        "yaml",
        "json"
      ],
      "format": "yaml"
    },
    "description": "Output formatting preferences for reclass\u2019s exported inventory.",
    "items": {
      "allowed_types": {
        "default": [
          "yaml",
          "json"
        ],
        "description": "List of allowed output formats (used by integrations like Salt or Ansible).",
        "items": {
          "default": "",
          "description": "Permitted output format.",
          "type": "string"
        },
        "type": "list"
      },
      "format": {
        "default": "yaml",
        "description": "Default output format (yaml or json).",
        "type": "string"
      }
    },
    "type": "object"
  },
  "path_prefix": {
    "default": "",
    "description": "Path prefix to prepend to all file-based paths (multi-tenant setups).",
    "type": "string"
  },
  "sources": {
    "default": [
      {
        "path": "/etc/reclass/nodes"
      },
      {
        "path": "/etc/reclass/classes"
      }
    ],
    "description": "List of source directories containing node definitions (YAML files ending in .yml/.yaml) and class definitions.",
    "items": {
      "path": {
        "default": "",
        "description": "Absolute path to a source directory (e.g., nodes or classes).",
        "type": "string"
      }
    },
    "type": "list"
  },
  "storage": {
    "default": {
      "path": "/var/lib/reclass"
    },
    "description": "Base directory where reclass stores its cache and generated inventory data.",
    "items": {
      "path": {
        "default": "/var/lib/reclass",
        "description": "Absolute path to the storage directory.",
        "type": "string"
      }
    },
    "type": "object"
  },
  "timezone": {
    "default": "UTC",
    "description": "Timezone used for timestamps (e.g., 'Europe/Berlin', 'America/New_York').",
    "type": "string"
  },
  "uri_encoding": {
    "default": "percent",
    "description": "Encoding method for URIs in external references: 'percent' or 'none'.",
    "type": "string",
    "enum": [
      "percent",
      "none"
    ]
  },
  "yaml": {
    "default": {
      "width": 80
    },
    "description": "YAML rendering options.",
    "items": {
      "width": {
        "default": 80,
        "description": "Line width for YAML output (0 = unlimited).",
        "type": "number"
      }
    },
    "type": "object"
  }
}
