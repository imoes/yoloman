{
  "allow_combining": {
    "type": "bool",
    "default": true,
    "description": "If false, disallows combining characters (equivalent to -C)"
  },
  "allow_zero_width": {
    "type": "bool",
    "default": true,
    "description": "If false, disallows zero-width characters (equivalent to -Z)"
  },
  "input_file": {
    "type": "string",
    "default": "stdin",
    "description": "Path to the input file containing LaTeX strings; if omitted or set to 'stdin', reads from standard input"
  }
}
