{
  "features": {
    "type": "number",
    "default": 128,
    "description": "Number of significant hashes to retain (-f)"
  },
  "shingle_size": {
    "type": "number",
    "default": 8,
    "description": "Size of shingles in bytes (-s)"
  },
  "operation": {
    "type": "string",
    "default": "compute",
    "description": "Operation mode: 'compute', 'batch', 'match', or 'compare'",
    "enum": [
      "compute",
      "batch",
      "match",
      "compare"
    ]
  },
  "input_file": {
    "type": "string",
    "default": "/dev/stdin",
    "description": "Input file to hash (used in compute mode)"
  },
  "files": {
    "type": "list",
    "default": [],
    "description": "List of files to process (used in batch or match mode). Items are strings representing file paths."
  },
  "hash_file1": {
    "type": "string",
    "default": "/tmp/hash1.sim",
    "description": "Path to first hash file (used in compare mode)"
  },
  "hash_file2": {
    "type": "string",
    "default": "/tmp/hash2.sim",
    "description": "Path to second hash file (used in compare mode)"
  }
}
