{
  "input_file": {
    "type": "string",
    "default": "/var/lib/readstat/input.sas7bdat",
    "description": "Path to the input data set file (SAS, SPSS, Stata, etc.)."
  },
  "output_file": {
    "type": "string",
    "default": "/var/lib/readstat/output.csv",
    "description": "Path to the output file. Format is determined by extension."
  },
  "force_overwrite": {
    "type": "bool",
    "default": false,
    "description": "If true, overwrite existing output files without prompting."
  },
  "metadata_file": {
    "type": "string",
    "default": "sps",
    "description": "Path to a separate metadata file (json, dct, sps, etc.). Leave empty if not used.",
    "enum": [
      "sas7bcat",
      "json",
      "dct",
      "sps"
    ]
  }
}
