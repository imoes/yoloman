{
 "base_cache_dir": {
  "default": "/var/cache/debuginfod",
  "description": "Root directory for storing cached debug information files (object files, debug sections, etc.)",
  "type": "string"
 },
 "bind_address": {
  "default": "127.0.0.1",
  "description": "IP address to bind the server to (e.g., '127.0.0.1' for localhost only, '' for all interfaces)",
  "type": "string"
 },
 "cacert_path": {
  "default": "",
  "description": "Path to custom CA certificate bundle for TLS; empty uses system trust store",
  "type": "string"
 },
 "cache_size_limit": {
  "default": 10737418240,
  "description": "Maximum total size (in bytes) of the debuginfo cache; 0 means unlimited",
  "type": "number"
 },
 "cache_ttl": {
  "default": 604800,
  "description": "Time (in seconds) after which cached debuginfo is considered stale and evicted; 0 means never expire",
  "type": "number"
 },
 "executable_cache_dir": {
  "default": "/var/lib/debuginfod",
  "description": "Directory where downloaded debuginfo packages are extracted and stored as executables for linking",
  "type": "string"
 },
 "log_file": {
  "default": "",
  "description": "Path to write log output; empty means write to stderr",
  "type": "string"
 },
 "max_downloads_per_client": {
  "default": 8,
  "description": "Maximum number of concurrent downloads allowed per client IP",
  "type": "number"
 },
 "max_upstream_connections": {
  "default": 64,
  "description": "Maximum number of simultaneous connections to upstream debuginfod servers",
  "type": "number"
 },
 "max_workers": {
  "default": 16,
  "description": "Maximum number of scanner threads to handle incoming requests",
  "type": "number"
 },
 "metrics_enabled": {
  "default": true,
  "description": "Expose a Prometheus-compatible metrics endpoint at /metrics",
  "type": "bool"
 },
 "port": {
  "default": 8002,
  "description": "TCP port on which the debuginfod HTTP server listens",
  "type": "number"
 },
 "proxy_url": {
  "default": "",
  "description": "HTTP(S) proxy URL for outgoing requests (e.g., 'http://proxy.example.com:3128'); empty = direct",
  "type": "string"
 },
 "prune_cache": {
  "default": true,
  "description": "Automatically prune old and unused debuginfo entries from the cache",
  "type": "bool"
 },
 "tls_verify": {
  "default": true,
  "description": "Enforce strict TLS certificate verification for upstream connections",
  "type": "bool"
 },
 "upstream_auth_users": {
  "default": [],
  "description": "List of credentials for authenticating to upstream debuginfod servers using HTTP Basic Auth",
  "type": "list"
 },
 "upstream_debuginfods": {
  "default": [],
  "description": "List of HTTP(S) URLs pointing to upstream debuginfod servers to query for debuginfo",
  "type": "list"
 },
 "verbose": {
  "default": false,
  "description": "Enable verbose (debug-level) logging",
  "type": "bool"
 }
}
