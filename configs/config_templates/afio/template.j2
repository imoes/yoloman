# =============================================================================
# afio Configuration
# =============================================================================
# This file contains command-line directives passed to the afio archiver utility.
# Each directive corresponds to a single line in the configuration.
#
# Managed by Ansible. Do not edit manually.
# =============================================================================

# --- afio Directives ---
# List of afio command-line directives (as strings) to be written as-is in the
# configuration file. Each item corresponds to one line in the output.
# Default: [] (empty list)
{% for directive in ((config | default({})).directives | default([])) %}
# Directive {{ loop.index }}: {{ directive }}
{{ directive }}
{% endfor %}