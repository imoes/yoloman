{
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug mode; logs to /tmp/ceni.log"
  },
  "file": {
    "type": "string",
    "default": "/etc/network/interfaces",
    "description": "Alternative interfaces file path"
  },
  "iface": {
    "type": "string",
    "default": "",
    "description": "Interface name to configure directly"
  },
  "no_act": {
    "type": "bool",
    "default": false,
    "description": "Run ifup/ifdown in simulation mode (with -n)"
  },
  "interfaces": {
    "type": "list",
    "default": [],
    "description": "List of network interface definitions (one per interface block in /etc/network/interfaces). Each interface must specify 'name'. Other fields are optional and reflect standard ifupdown settings. Ceni is interactive, but this list may be populated from prior manual or UI-driven configuration. Not all fields (e.g., 'hwaddress') apply to every interface type. See interfaces(5)."
  }
}
