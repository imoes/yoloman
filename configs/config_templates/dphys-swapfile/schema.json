{
  "CONF_SWAPFILE": {
    "type": "string",
    "default": "/var/swap",
    "description": "Path where the swapfile is located."
  },
  "CONF_SWAPSIZE": {
    "type": "number",
    "default": null,
    "description": "Absolute swapfile size in megabytes. If empty/null, computed dynamically."
  },
  "CONF_SWAPFACTOR": {
    "type": "number",
    "default": 2,
    "description": "Multiplier for RAM size when computing swap size dynamically."
  },
  "CONF_MAXSWAP": {
    "type": "number",
    "default": 2048,
    "description": "Maximum allowed swap size in megabytes (computed or absolute)."
  }
}
