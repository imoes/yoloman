# /etc/locale.alias — locale name alias mapping file
# Maps legacy or alternative locale names to canonical locale identifiers
{% for alias, canonical in aliases | dict2items %}# {{ alias | replace('_', ' ') | title }} alias -> {{ canonical | replace('_', ' ') | title }}
{{ alias }} {{ canonical }}
{% endfor %}