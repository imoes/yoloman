# Enable automatic systemd service startup of coturn.
# Set to 'true' or '1' to start the turnserver daemon on boot, 'false' or '0' to disable it.
TURNSERVER_ENABLED={{ turnserver_enabled|default(false)|bool|int }}