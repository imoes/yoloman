{
  "dest_dir": {
    "type": "string",
    "default": "/var/backups/vbackup",
    "description": "Global destination directory for backup archives."
  },
  "log_level": {
    "type": "number",
    "default": 8,
    "description": "Message level: 1-19. 8 is standard informational."
  },
  "modules": {
    "type": "list",
    "default": [],
    "description": "List of module configurations to be included in the backup strategy. Each item is a flat map with 'priority', 'name', 'type', and module-specific fields."
  }
}
