{
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debugging output"
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/ovn/ovn-bgp-agent.log",
    "description": "Path to the agent log file"
  },
  "log_format": {
    "type": "string",
    "default": "%(asctime)s %(levelname)s %(name)s %(message)s",
    "description": "Logging format string"
  },
  "log_date_format": {
    "type": "string",
    "default": "%Y-%m-%d %H:%M:%S",
    "description": "Date/time format used in log entries"
  },
  "log_max_size": {
    "type": "number",
    "default": 100,
    "description": "Maximum size (in MiB) per log file before rotation"
  },
  "log_backup_count": {
    "type": "number",
    "default": 5,
    "description": "Number of rotated log files to retain"
  },
  "ovsdb_socket": {
    "type": "string",
    "default": "unix:/run/openvswitch/db.sock",
    "description": "Path to the OVSDB management socket"
  },
  "bgp_speaker_type": {
    "type": "string",
    "default": "go-bgp",
    "description": "BGP implementation to use; choose from 'go-bgp', 'frr', 'bird'",
    "enum": [
      "go-bgp",
      "frr",
      "bird"
    ]
  },
  "bgp_as": {
    "type": "number",
    "default": 64512,
    "description": "Local autonomous system number for BGP"
  },
  "bgp_router_id": {
    "type": "string",
    "default": "auto",
    "description": "BGP router ID (IPv4 address); set to 'auto' to auto-detect"
  },
  "bgp_graceful_restart": {
    "type": "bool",
    "default": true,
    "description": "Enable BGP graceful restart support (RFC 4724)"
  },
  "bgp_hold_time": {
    "type": "number",
    "default": 90,
    "description": "BGP hold timer in seconds (must be >= 3)"
  },
  "bgp_keepalive_interval": {
    "type": "number",
    "default": 30,
    "description": "BGP keepalive interval in seconds (recommended: 1/3 hold_time)"
  },
  "route_advertise_delay": {
    "type": "number",
    "default": 10,
    "description": "Seconds to wait before advertising routes after BGP session establishment"
  },
  "advertise_node_local_ip": {
    "type": "bool",
    "default": true,
    "description": "Advertise local node tunnel IPs (e.g., geneve/vxlan IPs) to BGP peers"
  },
  "advertise_external_ip": {
    "type": "bool",
    "default": true,
    "description": "Advertise external/subnet gateway IPs to BGP peers"
  },
  "bgp_neighbors": {
    "type": "list",
    "default": [],
    "description": "List of BGP peers (neighbors) to connect to"
  },
  "ovn_nb_db": {
    "type": "string",
    "default": "tcp:127.0.0.1:6641",
    "description": "OVN Northbound database connection string"
  },
  "ovn_sb_db": {
    "type": "string",
    "default": "tcp:127.0.0.1:6642",
    "description": "OVN Southbound database connection string"
  },
  "ssl_ca_file": {
    "type": "string",
    "default": "/etc/ovn/ovn-bgp-agent-ca.crt",
    "description": "Path to CA certificate for secure DB connections (TLS)"
  },
  "ssl_cert_file": {
    "type": "string",
    "default": "/etc/ovn/ovn-bgp-agent.crt",
    "description": "Path to client certificate for DB TLS"
  },
  "ssl_key_file": {
    "type": "string",
    "default": "/etc/ovn/ovn-bgp-agent.key",
    "description": "Path to client private key for DB TLS"
  },
  "ovs_bridge": {
    "type": "string",
    "default": "br-ex",
    "description": "Name of the OVS bridge to use for external port IP discovery"
  },
  "removed": {
    "type": "string",
    "description": "Removed hallucinated directives: sr_iov_enabled, sr_iov_pci_addresses, snmp_enabled, snmp_listen, snmp_community, metrics_port, healthcheck_enabled. These are not part of the standard ovn-bgp-agent configuration."
  }
}
