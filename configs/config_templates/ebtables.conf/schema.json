{
  "rules": {
    "type": "list",
    "default": [],
    "description": "List of ebtables rules to apply (e.g., to filter or bridge traffic). Each rule should be a complete command string including table, chain, and match/action options.",
    "items": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "A full ebtables command line, including options. Example: '-A INPUT -s 00:11:22:33:44:55 -j DROP'."
        }
      },
      "required": [
        "command"
      ]
    }
  }
}
