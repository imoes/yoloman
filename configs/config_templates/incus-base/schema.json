{
  "use_syslog": {
    "type": "string",
    "default": "no",
    "description": "Set to yes to make incusd use syslog instead of directly writing log to /var/log/incus/incus.log",
    "enum": [
      "yes",
      "no"
    ]
  }
}
