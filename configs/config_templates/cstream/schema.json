{
  "block_size": {
    "type": "number",
    "default": 8192,
    "description": "Block size (bytes) used for read/write operations."
  },
  "buffer_size": {
    "type": "number",
    "default": null,
    "description": "Buffer size (bytes) before writing; must be >= block_size; if null, defaults to block_size."
  },
  "concurrent_mode": {
    "type": "number",
    "default": 0,
    "description": "Concurrent operation mode: 0=disabled, 1=read buffers, 2=write buffers, 3=both."
  },
  "input_file": {
    "type": "string",
    "default": "",
    "description": "Input file path; empty string = stdin; 'hostname:port' = TCP connect; ':port' = TCP listen."
  },
  "output_file": {
    "type": "string",
    "default": "",
    "description": "Output file path; '-' = discard; 'hostname:port' = TCP connect; ':port' = TCP listen."
  },
  "input_type_flags": {
    "type": "string",
    "default": "",
    "description": "Input file type flags: 'f'=FIFO, 'F'=fsync, 'a'=audio device, 't'=fd3 copy, 'N'=no TCP.",
    "enum": [
      "f",
      "F",
      "a",
      "t",
      "N"
    ]
  },
  "output_type_flags": {
    "type": "string",
    "default": "",
    "description": "Output file type flags: 'f'=FIFO, 'F'=fsync, 'a'=audio device, 't'=fd3 copy, 'N'=no TCP, 'D'=O_DIRECT, 'S'=O_SYNC.",
    "enum": [
      "f",
      "F",
      "a",
      "t",
      "N",
      "S"
    ]
  },
  "line_count": {
    "type": "bool",
    "default": false,
    "description": "Include line count in statistics."
  },
  "data_limit": {
    "type": "string",
    "default": "",
    "description": "Data limit with optional suffix: k=1024, m=1024\u00b2, g=1024\u00b3.",
    "enum": [
      "k",
      "m",
      "g"
    ]
  },
  "pid_file": {
    "type": "string",
    "default": "",
    "description": "Path to write process ID (parent PID if concurrent mode > 0)."
  },
  "throughput_limit": {
    "type": "number",
    "default": null,
    "description": "Throughput limit in bytes/second; positive for long-term average, negative for per-call upper bound."
  },
  "report_interval": {
    "type": "number",
    "default": 0,
    "description": "Interval (seconds) to report throughput; 0 = disable."
  },
  "verbose_level": {
    "type": "number",
    "default": 0,
    "description": "Verbose level: 0=none, 1=summary, 2=per-pair rate, 3=read/write separate, 4=per-IO."
  },
  "disable_sighup_doubletap": {
    "type": "bool",
    "default": false,
    "description": "If true, disable the SIGHUP double-tap safety (cstream kills itself on single SIGHUP after 5s wait)."
  }
}
