{
  "cmd": {
    "type": "string",
    "default": "/sbin/fsck /dev/sda1",
    "description": "The command to execute and log, including arguments."
  },
  "logfile": {
    "type": "string",
    "default": "/var/log/fsck.log",
    "description": "The path to the logfile where the output will be saved."
  },
  "append": {
    "type": "bool",
    "default": false,
    "description": "If true, append output to the logfile instead of replacing it (-a)."
  },
  "skip_control_chars": {
    "type": "bool",
    "default": false,
    "description": "If true, skip text bracketed by control-A and control-B (-s)."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, make logsave more verbose in its output to the user (-v)."
  }
}
