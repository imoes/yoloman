{
  "input_files": {
    "type": "list",
    "default": [],
    "description": "List of input .fc files to process (generated by filecompare). Must contain at least one file."
  },
  "orientation": {
    "type": "string",
    "default": "vertical",
    "description": "Image orientation: 'vertical' or 'horizontal'."
  },
  "direction": {
    "type": "string",
    "default": "down",
    "description": "Data flow direction: 'down', 'up', 'left', or 'right'. Default 'down' (right for horizontal orientation).",
    "enum": [
      "down",
      "up",
      "right"
    ]
  },
  "width": {
    "type": "number",
    "default": 100,
    "description": "Output image width in pixels. Must be a positive integer."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose mode."
  }
}
