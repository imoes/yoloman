{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Activate verbose output."
  },
  "output_file": {
    "type": "string",
    "default": "",
    "description": "Output filename. If empty, output goes to stdout."
  },
  "arch": {
    "type": "string",
    "default": "X64",
    "description": "Target architecture (X86, INTEL, IA64, INTEL64, X64).",
    "enum": [
      "X86",
      "INTEL",
      "IA64",
      "INTEL64",
      "X64"
    ]
  },
  "includedir": {
    "type": "list",
    "default": [],
    "description": "List of user include directories (-I). Use items to define directory strings if needed, but here it is a flat list of strings."
  },
  "wxidir": {
    "type": "list",
    "default": [],
    "description": "List of WiX system include directories (--wxidir)."
  },
  "define": {
    "type": "list",
    "default": [],
    "description": "List of define variables in 'KEY=VALUE' format (-D)."
  },
  "descriptor": {
    "type": "string",
    "default": "package.wxs",
    "description": "The MSI descriptor file to build."
  }
}
