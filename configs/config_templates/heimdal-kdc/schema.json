{
  "kdc_enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether to start the Heimdal KDC daemon."
  },
  "kdc_config_file": {
    "type": "string",
    "default": "/etc/heimdal-kdc/kdc.conf",
    "description": "Path to the KDC configuration file."
  },
  "kpasswdd_enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether to start the Heimdal kpasswdd (password change) daemon."
  },
  "kpasswdd_params": {
    "type": "string",
    "default": "",
    "description": "Additional command-line parameters for kpasswdd."
  },
  "master_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether this host is the KDC master (used for iprop replication)."
  },
  "slave_enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether this host is a KDC slave (for iprop replication)."
  },
  "slave_params": {
    "type": "string",
    "default": "",
    "description": "Additional parameters for the KDC slave (e.g., master hostname for iprop)."
  }
}
