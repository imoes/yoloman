{
  "interface": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "Network interface address for the msmtpd daemon to bind to."
  },
  "port": {
    "type": "number",
    "default": 10025,
    "description": "Port number for the msmtpd daemon to listen on."
  }
}
