{
  "allow_all": {
    "type": "string",
    "default": "#allow all # allow all",
    "description": "Rule to allow all USB devices. Default is commented out to deny by default."
  },
  "allow_hubs": {
    "type": "string",
    "default": "#allow bDeviceClass==09 bInterfaceClass==09 # allow hubs",
    "description": "Rule to allow USB hubs based on device and interface class codes."
  },
  "deny_hids_if_many": {
    "type": "string",
    "default": "#deny bInterfaceClass==03 devcount > 2 #deny HIDs if more than 2 are connected",
    "description": "Rule to deny Human Interface Devices (HIDs) if more than 2 are connected."
  }
}
