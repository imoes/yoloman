{
  "allow_all": "#allow all # allow all",
  "allow_hubs": "#allow bDeviceClass==09 bInterfaceClass==09 # allow hubs",
  "deny_hids_if_many": "#deny bInterfaceClass==03 devcount > 2 #deny HIDs if more than 2 are connected"
}
