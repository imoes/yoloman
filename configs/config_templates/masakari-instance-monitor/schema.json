{
  "provides": {
    "type": "string",
    "default": "masakari-instance-monitor",
    "description": "The name of the service provided by this init script."
  },
  "should_start_services": {
    "type": "string",
    "default": "postgresql mysql keystone rabbitmq-server ntp chrony",
    "description": "Space-separated list of services that should be started before this one."
  },
  "should_stop_services": {
    "type": "string",
    "default": "postgresql mysql keystone rabbitmq-server ntp chrony",
    "description": "Space-separated list of services that should be stopped after this one."
  },
  "short_description": {
    "type": "string",
    "default": "Masakari Instance Monitor daemon",
    "description": "Short description for the init script metadata."
  },
  "long_description": {
    "type": "string",
    "default": "Masakari provides Virtual Machine High Availability (VMHA) service for\n                   OpenStack clouds by automatically recovering the KVM-based Virtual\n                   Machine(VM)s from failure events such as VM process down, provisioning\n                   process down, and nova-compute host failure. It also provides API service for\n                   manage and control the automated rescue mechanism.",
    "description": "Long description for the init script metadata."
  },
  "author_name": {
    "type": "string",
    "default": "Thomas Goirand",
    "description": "Primary author name."
  },
  "author_email": {
    "type": "string",
    "default": "zigo@debian.org",
    "description": "Primary author email."
  },
  "desc": {
    "type": "string",
    "default": "OpenStack Masakari Instance Monitor (masakari-instancemonitor)",
    "description": "Description of the daemon used in log messages."
  },
  "project_name": {
    "type": "string",
    "default": "masakari",
    "description": "The OpenStack project name, used for naming conventions."
  },
  "author_name_2": {
    "type": "string",
    "default": "Ond\u0159ej Nov\u00fd",
    "description": "Secondary author name."
  },
  "author_email_2": {
    "type": "string",
    "default": "novy@ondrej.org",
    "description": "Secondary author email."
  },
  "system_user": {
    "type": "string",
    "default": "masakari",
    "description": "System user running the daemon."
  },
  "system_group": {
    "type": "string",
    "default": "masakari",
    "description": "System group running the daemon."
  },
  "config_file_name": {
    "type": "string",
    "default": "masakari.conf",
    "description": "Name of the main configuration file relative to /etc/project_name/."
  }
}
