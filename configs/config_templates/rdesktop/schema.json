{
  "server_address": {
    "type": "string",
    "default": "192.168.1.100",
    "description": "The hostname or IP address and optional port of the RDP server."
  },
  "username": {
    "type": "string",
    "default": "admin",
    "description": "Username for authentication on the server."
  },
  "domain": {
    "type": "string",
    "default": "",
    "description": "Domain for authentication. Empty string to omit."
  },
  "startup_shell": {
    "type": "string",
    "default": "",
    "description": "Startup shell/application to start instead of Explorer. Empty to use default desktop."
  },
  "working_directory": {
    "type": "string",
    "default": "",
    "description": "Initial working directory for the user session."
  },
  "client_hostname": {
    "type": "string",
    "default": "",
    "description": "Client hostname to send to the server. Empty to use default system hostname."
  },
  "keyboard_map": {
    "type": "string",
    "default": "en-us",
    "description": "Keyboard layout keymap (e.g., en-us, de, fr)."
  },
  "desktop_geometry": {
    "type": "string",
    "default": "1024x768",
    "description": "Desktop geometry in WxH[@DPI][+X[+Y]] format or 'workarea' or percentage."
  },
  "color_depth": {
    "type": "number",
    "default": 32,
    "description": "Color depth in bits per pixel (8, 15, 16, 24, or 32)."
  },
  "experience_level": {
    "type": "string",
    "default": "b",
    "description": "Network experience level: m[odem], b[roadband], l[an].",
    "enum": [
      "modem",
      "b",
      "l"
    ]
  },
  "tls_version": {
    "type": "string",
    "default": "",
    "description": "TLS version: 1.0, 1.1, or 1.2. Empty to allow all.",
    "enum": [
      "1.0",
      "1.1",
      "1.2"
    ]
  },
  "window_title": {
    "type": "string",
    "default": "Remote Desktop",
    "description": "Window title for the RDP client window."
  },
  "fullscreen_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable fullscreen mode."
  },
  "compression_enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable compression of the RDP datastream."
  },
  "bitmap_caching_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable caching of bitmaps to disk."
  },
  "numlock_sync_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable numlock synchronization between Xserver and remote session."
  },
  "hide_decorations_enabled": {
    "type": "bool",
    "default": false,
    "description": "Hide window manager decorations."
  },
  "colormap_private_enabled": {
    "type": "bool",
    "default": false,
    "description": "Use private colourmap for better color accuracy."
  },
  "remote_control_disabled": {
    "type": "bool",
    "default": false,
    "description": "Disable use of remote control features."
  },
  "local_cursor_enabled": {
    "type": "bool",
    "default": false,
    "description": "Use local X cursor inherited from window manager."
  },
  "mouse_motion_disabled": {
    "type": "bool",
    "default": false,
    "description": "Do not send mouse motion events."
  },
  "force_bitmaps_enabled": {
    "type": "bool",
    "default": false,
    "description": "Force server to send screen updates as bitmaps."
  },
  "x_backing_store_enabled": {
    "type": "bool",
    "default": false,
    "description": "Use BackingStore of the Xserver."
  },
  "single_app_mode_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable single application mode."
  },
  "single_app_button_size": {
    "type": "string",
    "default": "standard",
    "description": "Button size for single application mode in pixels or 'standard'."
  },
  "seamlessrdp_shell": {
    "type": "string",
    "default": "",
    "description": "Path to seamless rdp shell executable to enable SeamlessRDP."
  },
  "redirections": {
    "type": "list",
    "default": [],
    "description": "List of device redirection strings to enable. Format: -r compliant strings like 'clipboard:PRIMARYCLIPBOARD' or 'disk:share=path'. Items are flat strings representing the -r argument value."
  },
  "sc_csp_name": {
    "type": "string",
    "default": "",
    "description": "CredSSP Smartcard CSP Name."
  },
  "sc_container_name": {
    "type": "string",
    "default": "",
    "description": "CredSSP Smartcard Container Name."
  },
  "sc_reader_name": {
    "type": "string",
    "default": "",
    "description": "CredSSP Smartcard Reader Name."
  },
  "sc_card_name": {
    "type": "string",
    "default": "",
    "description": "CredSSP Smartcard Card Name."
  },
  "disable_encryption_enabled": {
    "type": "bool",
    "default": false,
    "description": "Disable encryption entirely."
  },
  "disable_client_encryption_enabled": {
    "type": "bool",
    "default": false,
    "description": "Disable encryption from client to server."
  },
  "embed_window_id": {
    "type": "string",
    "default": "",
    "description": "Window ID to embed rdesktop into."
  },
  "rdp_version_4_enabled": {
    "type": "bool",
    "default": false,
    "description": "Use RDP version 4."
  },
  "console_mode_enabled": {
    "type": "bool",
    "default": false,
    "description": "Attach to the console of the server."
  },
  "smartcard_pin_enabled": {
    "type": "bool",
    "default": false,
    "description": "Use password as smartcard pin."
  },
  "wm_keybindings_preserved": {
    "type": "bool",
    "default": false,
    "description": "Do not override window manager key bindings."
  }
}
