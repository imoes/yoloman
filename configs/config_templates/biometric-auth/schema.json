{
  "provides": {
    "type": "string",
    "default": "biometric-authentication",
    "description": "Service name provided in the LSB header"
  },
  "service_name": {
    "type": "string",
    "default": "biometric-authentication",
    "description": "Base name of the service used in paths and PID file"
  },
  "daemon_name": {
    "type": "string",
    "default": "biometric-authenticationd",
    "description": "Name of the daemon executable"
  },
  "short_description": {
    "type": "string",
    "default": "Biometric Authentication Service",
    "description": "Short description used in LSB header and startup messages"
  },
  "short_description_lower": {
    "type": "string",
    "default": "biometric authentication service",
    "description": "Lowercase version of short description for log messages"
  },
  "description": {
    "type": "string",
    "default": "The service layer of the biometric identification authentication framework.\\n  The service layer uses the DBus bus to provide the upper application with\\n  operation interfaces such as feature enroll, feature verify, feature identify,\\n  feature search, feature delete, etc. Meanwhile, it also provides notification\\n  of device status changes event and notification of USB device hotplug event.",
    "description": "Full description block from LSB header"
  }
}
