{
  "input_files": {
    "type": "list",
    "default": [],
    "description": "List of Debian control data files to process (e.g., /var/lib/dpkg/available, Packages, Sources, .dsc, .changes). If empty or omitted, stdin is used."
  },
  "output_format": {
    "type": "string",
    "default": "xml",
    "description": "Output format: 'xml' for XML (default), 'json' for JSON.",
    "enum": [
      "xml",
      "json"
    ]
  }
}
