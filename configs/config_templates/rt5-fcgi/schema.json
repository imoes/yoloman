{
  "enabled": {
    "type": "number",
    "default": 0,
    "description": "Whether the RT5 FCGI service is enabled to start on boot."
  },
  "workers": {
    "type": "number",
    "default": 2,
    "description": "Number of RT worker processes to spawn."
  }
}
