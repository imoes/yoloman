{
  "lightweight": {
    "type": "bool",
    "default": false,
    "description": "If true, runs in lightweight mode to preserve file content integrity at the cost of less thorough metadata removal."
  }
}
