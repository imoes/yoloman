{
  "config_file": {
    "type": "string",
    "default": "/etc/dns2tcpd.conf",
    "description": "Path to the dns2tcp server configuration file."
  },
  "daemon_path": {
    "type": "string",
    "default": "/usr/bin/dns2tcpd",
    "description": "Path to the dns2tcp daemon executable."
  },
  "run_as_user": {
    "type": "string",
    "default": "nobody",
    "description": "User account under which the dns2tcp server process should run."
  }
}
