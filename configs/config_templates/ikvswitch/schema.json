{
  "MY_IP": {
    "type": "string",
    "default": "10.2.34.71",
    "description": "IP address of the host."
  },
  "HOST_VIRTUAL_SUBNET_START_PREFIX": {
    "type": "string",
    "default": "192.168",
    "description": "First part of the virtual subnet prefix."
  },
  "HOST_VIRTUAL_SUBNET_END_PREFIX": {
    "type": "number",
    "default": 96,
    "description": "Second part of the virtual subnet prefix, used with start prefix for /19 subnet calculation."
  },
  "HOST_VIRTUAL_SUBNET_NET2_SUFFIX": {
    "type": "number",
    "default": 97,
    "description": "Suffix for HOST_VIRTUAL_SUBNET_NET2 IP."
  },
  "HOST_VIRTUAL_SUBNET_NET3_SUFFIX": {
    "type": "number",
    "default": 98,
    "description": "Suffix for HOST_VIRTUAL_SUBNET_NET3 IP."
  },
  "MAC_PREFIX": {
    "type": "string",
    "default": "00:22:22:dd:ee",
    "description": "MAC address prefix of the NICs."
  },
  "NUM_U": {
    "type": "number",
    "default": 23,
    "description": "How many servers per rack ikvswitch should handle."
  },
  "AS_START": {
    "type": "number",
    "default": 65500,
    "description": "Start of the AS numbering for all switches."
  },
  "LEAF_TO_VM_VLAN_NUM": {
    "type": "number",
    "default": 10,
    "description": "VLAN number to add to ensX interfaces in leaf switches."
  },
  "DHCP_RELAY_DESTINATION": {
    "type": "string",
    "default": "192.168.100.2",
    "description": "IP to which DHCP relay forwards requests."
  },
  "DHCPD_SUBNET_1_SUFFIX": {
    "type": "number",
    "default": 110,
    "description": "Suffix for DHCPD_SUBNET_1 IP."
  },
  "DHCPD_SUBNET_2_SUFFIX": {
    "type": "number",
    "default": 111,
    "description": "Suffix for DHCPD_SUBNET_2 IP."
  },
  "DHCPD_SUBNET_3_SUFFIX": {
    "type": "number",
    "default": 112,
    "description": "Suffix for DHCPD_SUBNET_3 IP."
  },
  "VLAN_SUBNETS_NUM": {
    "type": "number",
    "default": 5,
    "description": "Number of VLAN subnets."
  },
  "VLAN_SBUNETS_START_IP": {
    "type": "number",
    "default": 113,
    "description": "Start IP suffix for VLAN subnets."
  },
  "VLAN_START": {
    "type": "number",
    "default": 11,
    "description": "Start VLAN number."
  },
  "HOST_DUMMYNET_IFNAME_PREFIX": {
    "type": "string",
    "default": "ikhn",
    "description": "Prefix for host dummynet NIC interface name."
  },
  "HOST_BRIDGE_NAME_PREFIX": {
    "type": "string",
    "default": "ikhb",
    "description": "Prefix for host bridge name."
  },
  "HOST_VM_TAP_IFNAME_PREFIX": {
    "type": "string",
    "default": "ikht",
    "description": "Prefix for host VM TAP interface name."
  },
  "INTERNET_TO_SPINE_TAP_IFNAME_PREFIX": {
    "type": "string",
    "default": "ikin",
    "description": "Prefix for internet to spine TAP interface name."
  },
  "INTERNET_TO_SPINE_BRIDGE_PREFIX": {
    "type": "string",
    "default": "ikib",
    "description": "Prefix for internet to spine bridge name."
  },
  "SPINE_TO_INTERNET_TAP_PREFIX": {
    "type": "string",
    "default": "ikit",
    "description": "Prefix for spine to internet TAP interface name."
  },
  "SPINE_VM_TAP_IFNAME_PREFIX": {
    "type": "string",
    "default": "ikst",
    "description": "Prefix for spine VM TAP interface name."
  },
  "SPINE_BRIDGE_PREFIX": {
    "type": "string",
    "default": "iksb",
    "description": "Prefix for spine bridge name."
  },
  "LEAF_TO_SPINE_TAP_PREFIX": {
    "type": "string",
    "default": "iklt",
    "description": "Prefix for leaf to spine TAP interface name."
  },
  "LEAF_TO_SERVERS_TAP_PREFIX": {
    "type": "string",
    "default": "ikls",
    "description": "Prefix for leaf to servers TAP interface name."
  },
  "LEAF_TO_SERVER_BRIDGE_PREFIX": {
    "type": "string",
    "default": "ikeb",
    "description": "Prefix for leaf to server bridge name."
  },
  "HOST_TAP_NAME_PREFIX": {
    "type": "string",
    "default": "ikvm",
    "description": "Prefix for host TAP bridge name."
  },
  "HOST_IPMI_BRIDGE_NAME": {
    "type": "string",
    "default": "bripmi",
    "description": "Name of the IPMI bridge."
  },
  "HOST_IPMI_IP": {
    "type": "string",
    "default": "192.168.200.1",
    "description": "IP address of the IPMI bridge."
  },
  "VM_TAP_NIC": {
    "type": "string",
    "default": "ikipmi",
    "description": "Name of the VM TAP NIC for IPMI."
  },
  "DEBIAN_RELEASE": {
    "type": "string",
    "default": "trixie",
    "description": "Name of the Debian release to use."
  },
  "DEBIAN_MIRROR": {
    "type": "string",
    "default": "http://deb.debian.org/debian",
    "description": "Debian mirror to use for debootstrap."
  },
  "DEBIAN_SECURITY_MIRROR": {
    "type": "string",
    "default": "http://security.debian.org/debian-security",
    "description": "Debian security mirror to use for debootstrap."
  },
  "VM_ROOT_PASS": {
    "type": "string",
    "default": "changeme",
    "description": "Root password to set inside VMs."
  },
  "GUEST_MAC_ADDRESS_PREFIX": {
    "type": "string",
    "default": "08:00:27:06:AC:",
    "description": "Prefix of MAC addresses inside switch VMs."
  }
}
