{
  "search_registries": {
    "type": "list",
    "default": [
      "registry.fedoraproject.org",
      "registry.access.redhat.com",
      "docker.io"
    ],
    "description": "List of registries to search when pulling unqualified images (e.g., 'nginx' without a prefix)."
  },
  "registries": {
    "type": "list",
    "default": [],
    "description": "Per-registry configuration blocks. Each entry defines settings (mirrors, TLS) for a specific registry."
  }
}
