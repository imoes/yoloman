# ==============================================================================
# Package: gdisk
# Description: GPT fdisk — Interactive GPT partitioning tool
# ==============================================================================
# NOTE: gdisk is an interactive CLI tool and does NOT use a static configuration file.
# This template serves as a self-documenting placeholder to indicate that
# no configuration directives exist for this package.
#
# For non-interactive scripting and automation, please use sgdisk(8) instead.
# See: gdisk(8), sgdisk(8)
# ==============================================================================
# gdisk(8) — GPT fdisk — Interactive GPT partitioning tool
# It operates interactively via CLI; no static config file is used.
# For scripting, use sgdisk(8) instead, which supports non-interactive invocation.
# See: gdisk(8), sgdisk(8), manpages gdisk, gptfdisk.