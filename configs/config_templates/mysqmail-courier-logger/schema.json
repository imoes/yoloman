{
  "provides": {
    "type": "string",
    "default": "mysqmail-courier-logger",
    "description": "Provides line for LSB header"
  },
  "required_start": {
    "type": "string",
    "default": "$remote_fs $syslog",
    "description": "Required-Start line for LSB header"
  },
  "required_stop": {
    "type": "string",
    "default": "$remote_fs $syslog",
    "description": "Required-Stop line for LSB header"
  },
  "should_start": {
    "type": "string",
    "default": "mysql",
    "description": "Should-Start line for LSB header"
  },
  "should_stop": {
    "type": "string",
    "default": "mysql",
    "description": "Should-Stop line for LSB header"
  },
  "default_start": {
    "type": "string",
    "default": "2 3 4 5",
    "description": "Default-Start line for LSB header"
  },
  "default_stop": {
    "type": "string",
    "default": "0 1 6",
    "description": "Default-Stop line for LSB header"
  },
  "short_description": {
    "type": "string",
    "default": "MySQL accouting for courier-imap and courier-pop",
    "description": "Short-Description line for LSB header"
  },
  "description": {
    "type": "string",
    "default": "This is a (ver small) traffic logger for the C courier-imap,\n                    courier-imaps, courier-pop and courier-pops daemons to SQL,\n                    splitting the information by domains and users, so it's then very\n                    easy to count all the pop and imap traffic for a given\n                    domain name.",
    "description": "Description line for LSB header"
  },
  "desc": {
    "type": "string",
    "default": "MySQMail courier logger",
    "description": "DESC variable value"
  },
  "name": {
    "type": "string",
    "default": "mysqmail-courier-logger",
    "description": "NAME variable value"
  },
  "path_conf_file": {
    "type": "string",
    "default": "/etc/mysqmail.conf",
    "description": "PATH_CONF_FILE variable value"
  }
}
