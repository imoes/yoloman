{
  "dshell": {
    "type": "string",
    "default": "/bin/bash",
    "description": "Login shell for new users"
  },
  "skel": {
    "type": "string",
    "default": "/etc/skel",
    "description": "Directory containing skeleton files for new home directories"
  },
  "first_system_uid": {
    "type": "number",
    "default": 100,
    "description": "First UID reserved for system users"
  },
  "last_system_uid": {
    "type": "number",
    "default": 999,
    "description": "Last UID reserved for system users"
  },
  "first_system_gid": {
    "type": "number",
    "default": 100,
    "description": "First GID reserved for system groups"
  },
  "last_system_gid": {
    "type": "number",
    "default": 999,
    "description": "Last GID reserved for system groups"
  },
  "first_uid": {
    "type": "number",
    "default": 1000,
    "description": "First UID for regular (non-system) users"
  },
  "last_uid": {
    "type": "number",
    "default": 59999,
    "description": "Last UID for regular (non-system) users"
  },
  "first_gid": {
    "type": "number",
    "default": 1000,
    "description": "First GID for regular (non-system) groups"
  },
  "last_gid": {
    "type": "number",
    "default": 59999,
    "description": "Last GID for regular (non-system) groups"
  },
  "usergroups": {
    "type": "string",
    "default": "yes",
    "description": "Whether each non-system user gets their own group"
  },
  "dir_mode": {
    "type": "string",
    "default": "0700",
    "description": "Permission mode for regular user home directories"
  },
  "sys_dir_mode": {
    "type": "string",
    "default": "0755",
    "description": "Permission mode for system user home directories"
  },
  "name_regex": {
    "type": "string",
    "default": "^[a-zA-Z][a-zA-Z0-9_-]*\\$?$",
    "description": "Regex for validating regular username"
  },
  "sys_name_regex": {
    "type": "string",
    "default": "^[a-zA-Z_][a-zA-Z0-9_-]*\\$?$",
    "description": "Regex for validating system username"
  },
  "skel_ignore_regex": {
    "type": "string",
    "default": "\\\\\\.(dpkg\\\\|ucf\\\\)-(old\\\\|new\\\\|dist\\\\|save)\\$",
    "description": "Regex for files in SKEL to exclude from copying"
  },
  "extra_groups": {
    "type": "string",
    "default": "users",
    "description": "Additional groups to add new non-system users to"
  }
}
