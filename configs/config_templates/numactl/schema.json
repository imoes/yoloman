{
 "EXECUTABLE": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "NUMACTL_CMD": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "balancing": {
  "default": false,
  "description": "Enable kernel NUMA balancing. Maps to --balancing.",
  "type": "bool"
 },
 "cpunodebind": {
  "default": "",
  "description": "CPU node binding (e.g., '0', '0,1'). Maps to --cpunodebind.",
  "type": "string"
 },
 "exec_args": {
  "default": [],
  "description": "List of command-line arguments to pass to the executable.",
  "type": "list"
 },
 "executable": {
  "default": "/usr/bin/myapp",
  "description": "The path to the executable to be run under NUMA policy.",
  "type": "string"
 },
 "huge_pages": {
  "default": false,
  "description": "Use huge pages for SYSV shared memory. Maps to --huge.",
  "type": "bool"
 },
 "interleave_nodes": {
  "default": "",
  "description": "Nodes for interleave policy. Maps to --interleave.",
  "type": "string"
 },
 "membind": {
  "default": "",
  "description": "Memory binding nodes (e.g., '0', 'all'). Maps to --membind.",
  "type": "string"
 },
 "physcpubind": {
  "default": "",
  "description": "Physical CPU binding (e.g., '0-3', 'all', '+0-4'). Maps to --physcpubind.",
  "type": "string"
 },
 "preferred_node": {
  "default": "",
  "description": "Preferred node for memory allocation. Maps to --preferred.",
  "type": "string"
 },
 "separator_cmd": {
  "default": false,
  "description": "Insert '--' before command to separate numactl options. Maps to --.",
  "type": "bool"
 },
 "service_name": {
  "default": "myservice",
  "description": "The name of the service for which this configuration is generated.",
  "type": "string"
 },
 "shm_file": {
  "default": "",
  "description": "Path to tmpfs/hugetlbfs file for persistent policy. Maps to --file.",
  "type": "string"
 },
 "shm_id": {
  "default": "",
  "description": "Numeric shared memory ID. Maps to --shmid.",
  "type": "string"
 },
 "shm_keyfile": {
  "default": "",
  "description": "Key file for shared memory ID generation via ftok. Maps to --shm.",
  "type": "string"
 },
 "shm_length": {
  "default": "",
  "description": "Length of shared memory region to apply policy to. Maps to --length.",
  "type": "string"
 },
 "shm_mode": {
  "default": "",
  "description": "Mode for creating shared memory segment. Maps to --shmmode.",
  "type": "string"
 },
 "shm_offset": {
  "default": "",
  "description": "Offset into shared memory segment. Maps to --offset.",
  "type": "string"
 },
 "strict": {
  "default": false,
  "description": "Error on conflicting page policy. Maps to --strict.",
  "type": "bool"
 },
 "touch": {
  "default": false,
  "description": "Touch pages to enforce policy early. Maps to --touch.",
  "type": "bool"
 },
 "use_all": {
  "default": false,
  "description": "Unset default cpuset awareness. Maps to --all.",
  "type": "bool"
 },
 "weighted_interleave_nodes": {
  "default": "",
  "description": "Nodes for weighted interleave policy. Maps to --weighted-interleave.",
  "type": "string"
 }
}
