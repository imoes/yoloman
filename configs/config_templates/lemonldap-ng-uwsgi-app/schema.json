{
  "plugins": {
    "type": "string",
    "default": "psgi",
    "description": "UWSGI plugins to load"
  },
  "socket": {
    "type": "string",
    "default": "127.0.0.1:5000",
    "description": "Socket address for the UWSGI server"
  },
  "psgi": {
    "type": "string",
    "default": "/usr/share/lemonldap-ng/llng-server/llng-server.psgi",
    "description": "Path to the PSGI application file"
  },
  "perl_no_die_catch": {
    "type": "bool",
    "default": true,
    "description": "Whether to catch Perl die exceptions"
  },
  "master": {
    "type": "bool",
    "default": true,
    "description": "Whether to run in master mode"
  },
  "uid": {
    "type": "string",
    "default": "www-data",
    "description": "User ID to run the UWSGI worker as"
  },
  "gid": {
    "type": "string",
    "default": "www-data",
    "description": "Group ID to run the UWSGI worker as"
  }
}
