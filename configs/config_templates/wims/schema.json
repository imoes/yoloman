{
  "wims_url_prefix": {
    "type": "string",
    "default": "wims",
    "description": "The URL path prefix for accessing WIMS (e.g., 'wims' results in /wims/...)"
  },
  "wims_data_root": {
    "type": "string",
    "default": "/var/lib/wims",
    "description": "The base directory where WIMS data and public_html reside."
  }
}
