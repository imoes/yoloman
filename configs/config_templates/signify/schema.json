{
  "sig_width": {
    "type": "number",
    "default": 79,
    "description": "The desired output width of the generated signature in characters."
  },
  "email": {
    "type": "string",
    "default": "user@example.com",
    "description": "The user's email address, substituted as $email."
  },
  "name": {
    "type": "string",
    "default": "John Doe",
    "description": "The user's name, substituted as $name."
  },
  "weight_professional": {
    "type": "number",
    "default": 3,
    "description": "Weight for the professional closing alternate. Higher value increases likelihood of selection."
  },
  "weight_casual": {
    "type": "number",
    "default": 2,
    "description": "Weight for the casual closing alternate. Higher value increases likelihood of selection."
  },
  "weight_formal": {
    "type": "number",
    "default": 1,
    "description": "Weight for the formal closing alternate. Higher value increases likelihood of selection."
  },
  "weight_footer": {
    "type": "number",
    "default": 0.1,
    "description": "Weight for the footer/credits line. Kept low to appear occasionally."
  },
  "website": {
    "type": "string",
    "default": "http://www.example.com",
    "description": "The website URL to display in the footer."
  }
}
