{
  "health_error_for": {
    "type": "string",
    "default": "5m",
    "description": "Duration for which cluster must be HEALTH_ERROR before alerting."
  },
  "health_warning_for": {
    "type": "string",
    "default": "15m",
    "description": "Duration for which cluster must be HEALTH_WARN before alerting."
  },
  "mon_down_quorum_risk_for": {
    "type": "string",
    "default": "30s",
    "description": "Duration before alerting on monitor quorum at risk."
  },
  "mon_down_for": {
    "type": "string",
    "default": "30s",
    "description": "Duration before alerting on monitor down (quorum intact)."
  },
  "mon_disk_critical_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on monitor disk critically low."
  },
  "mon_disk_low_for": {
    "type": "string",
    "default": "5m",
    "description": "Duration before alerting on monitor disk space low."
  },
  "mon_clock_skew_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on monitor clock skew."
  },
  "osd_down_high_threshold": {
    "type": "number",
    "default": 10,
    "description": "Percentage threshold for alerting on high OSD down rate."
  },
  "osd_host_down_for": {
    "type": "string",
    "default": "5m",
    "description": "Duration before alerting on OSD host down."
  },
  "osd_down_for_duration": {
    "type": "string",
    "default": "5m",
    "description": "Duration before alerting on OSD marked down."
  },
  "osd_near_full_for": {
    "type": "string",
    "default": "5m",
    "description": "Duration before alerting on OSD near full."
  },
  "osd_full_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on OSD full."
  },
  "osd_backfill_full_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on OSD backfill full."
  },
  "osd_too_many_repairs_for": {
    "type": "string",
    "default": "30s",
    "description": "Duration before alerting on too many OSD repairs."
  },
  "osd_timeout_public_network_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on slow public network heartbeats."
  },
  "osd_timeout_cluster_network_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on slow cluster network heartbeats."
  },
  "osd_disk_size_mismatch_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on OSD disk size mismatch."
  },
  "device_failure_predicted_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on predicted device failure."
  },
  "device_failure_prediction_too_high_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on excessive predicted device failures."
  },
  "device_failure_relocation_incomplete_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on incomplete device failure relocation."
  },
  "osd_flapping_window": {
    "type": "string",
    "default": "5m",
    "description": "Time window to detect OSD flapping."
  },
  "osd_read_errors_for": {
    "type": "string",
    "default": "30s",
    "description": "Duration before alerting on OSD read errors."
  },
  "pg_imbalance_for": {
    "type": "string",
    "default": "5m",
    "description": "Duration before alerting on PG imbalance."
  },
  "mds_filesystem_damaged_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on CephFS damage."
  },
  "mds_filesystem_offline_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on CephFS offline."
  },
  "mds_filesystem_degraded_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on CephFS degraded."
  },
  "mds_ranks_low_for": {
    "type": "string",
    "default": "1m",
    "description": "Duration before alerting on Ceph MDS ranks low."
  }
}
