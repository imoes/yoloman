{
  "nice_value": {
    "type": "number",
    "default": 0,
    "description": "Nice value for the BackupPC daemon (e.g., 0, 10, 19). Higher means lower priority."
  },
  "ionice_setting": {
    "type": "string",
    "default": "",
    "description": "IONICE setting for the BackupPC daemon (e.g., 'idle', 'best-effort:6'). Empty string disables IONICE."
  }
}
