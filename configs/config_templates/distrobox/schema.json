{
  "sections": {
    "type": "list",
    "default": [],
    "description": "List of container configurations, each with a unique name (section header in manifest). Each item is a flat map of distrobox.ini directives for that container entry. See items for allowed keys and types."
  }
}
