{
  "enable_zfs": {
    "type": "string",
    "default": "yes",
    "description": "Enable ZFS support (yes/no)",
    "enum": [
      "yes",
      "no"
    ]
  },
  "restart_on_upgrade": {
    "type": "string",
    "default": "no",
    "description": "Try to restart zfs-fuse when upgrading (yes/no)",
    "enum": [
      "yes",
      "no"
    ]
  }
}
