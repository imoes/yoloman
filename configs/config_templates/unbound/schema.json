{
  "interfaces": {
    "type": "list",
    "default": [
      "127.0.0.1"
    ],
    "description": "List of IP addresses to bind the DNS resolver interface(s). Use 0.0.0.0 for all IPv4, :: for all IPv6."
  },
  "server_port": {
    "type": "number",
    "default": 53,
    "description": "Local port on which the resolver listens for DNS queries."
  },
  "do_ip4": {
    "type": "bool",
    "default": true,
    "description": "Enable IPv4 support for DNS queries and responses."
  },
  "do_ip6": {
    "type": "bool",
    "default": false,
    "description": "Enable IPv6 support for DNS queries and responses."
  },
  "do_udp": {
    "type": "bool",
    "default": true,
    "description": "Enable UDP transport for DNS queries and responses."
  },
  "do_tcp": {
    "type": "bool",
    "default": true,
    "description": "Enable TCP transport for DNS queries (required for large DNSSEC responses)."
  },
  "tcp_upstream": {
    "type": "number",
    "default": 10,
    "description": "Maximum number of TCP connections allowed per upstream server."
  },
  "tcp_downstream": {
    "type": "number",
    "default": 100,
    "description": "Maximum number of TCP connections allowed per local interface."
  },
  "num_threads": {
    "type": "number",
    "default": 0,
    "description": "Number of worker threads. Set to 0 to auto-detect CPU count."
  },
  "edns_buffer_size": {
    "type": "number",
    "default": 1232,
    "description": "EDNS buffer size in bytes (must be \u2265 512 and \u2264 4096 for best compatibility)."
  },
  "msg_cache_size": {
    "type": "string",
    "default": "4mb",
    "description": "Size of the message cache. Supports suffixes like 'kb', 'mb', 'gb'."
  },
  "msg_cache_slabs": {
    "type": "number",
    "default": 1,
    "description": "Number of slabs in the message cache (power of two recommended for performance)."
  },
  "key_cache_size": {
    "type": "string",
    "default": "4mb",
    "description": "Size of the DNSSEC key cache."
  },
  "key_cache_slabs": {
    "type": "number",
    "default": 1,
    "description": "Number of slabs in the key cache."
  },
  "rrset_cache_size": {
    "type": "string",
    "default": "8mb",
    "description": "Size of the RRSET (resource record set) cache."
  },
  "rrset_cache_slabs": {
    "type": "number",
    "default": 1,
    "description": "Number of slabs in the RRSET cache."
  },
  "infra_cache_ttl": {
    "type": "number",
    "default": 900,
    "description": "TTL for infrastructure cache entries (e.g., RTT info, hints), in seconds."
  },
  "infra_cache_slabs": {
    "type": "number",
    "default": 1,
    "description": "Number of slabs in the infrastructure cache."
  },
  "log_queries": {
    "type": "bool",
    "default": false,
    "description": "Log every incoming query (debugging use only; can generate large logs)."
  },
  "log_replies": {
    "type": "bool",
    "default": false,
    "description": "Log every outgoing reply (debugging use only)."
  },
  "log_tag": {
    "type": "bool",
    "default": true,
    "description": "Prefix log lines with thread number and time in ISO 8601 format."
  },
  "log_time_ascii": {
    "type": "bool",
    "default": true,
    "description": "Use human-readable ASCII timestamp in logs (instead of Unix epoch)."
  },
  "access_control": {
    "type": "list",
    "default": [
      "127.0.0.0/8 allow",
      "::1/128 allow",
      "0.0.0.0/0 deny"
    ],
    "description": "Access control list entries in format: `<subnet> <action>` (e.g., '192.168.1.0/24 allow_sld')."
  },
  "forward_upstream": {
    "type": "list",
    "default": [],
    "description": "List of forwarding zones. Each zone forwards queries for a domain suffix to upstream resolvers."
  }
}
