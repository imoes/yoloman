{
  "sbin_path": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Path to the globus-job-manager binary"
  },
  "config_path": {
    "type": "string",
    "default": "/etc/globus/globus-gram-job-manager.conf",
    "description": "Path to the main globus-gram-job-manager configuration file"
  },
  "job_manager_type": {
    "type": "string",
    "default": "fork",
    "description": "Type of job manager (e.g., fork, gridftp)"
  },
  "seg_module": {
    "type": "string",
    "default": "fork",
    "description": "SEG (Simple Execution Gateway) module to use"
  }
}
