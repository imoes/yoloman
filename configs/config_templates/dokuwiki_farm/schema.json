{
  "farm_dir": {
    "type": "string",
    "default": {
      "type": "string",
      "default": "/var/lib/dokuwiki/farm",
      "description": "Absolute path to the directory that contains every animal's data subdirectory"
    },
    "description": "Absolute path to the directory that contains every animal's data subdirectory"
  },
  "farm_base_domain": {
    "type": "string",
    "default": {
      "type": "string",
      "default": "wiki.example.com",
      "description": "Root domain of the farm; animals appear as subdomains or path segments below this"
    },
    "description": "Root domain of the farm; animals appear as subdomains or path segments below this"
  }
}
