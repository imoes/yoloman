{
  "document_root": {
    "type": "string",
    "default": "/var/lib/wims-lti",
    "description": "The document root directory for the WIMS LTI application."
  },
  "lti_user": {
    "type": "string",
    "default": "lti",
    "description": "The system user running the WSGI daemon process."
  },
  "lti_group": {
    "type": "string",
    "default": "lti",
    "description": "The system group running the WSGI daemon process."
  },
  "server_name": {
    "type": "string",
    "default": "SERVERNAME",
    "description": "The server hostname or domain name for the VirtualHost."
  }
}
