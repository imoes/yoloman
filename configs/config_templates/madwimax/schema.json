{
  "dhclient_pid_dir": {
    "type": "string",
    "default": "/var/run",
    "description": "Directory where dhclient writes its PID file"
  },
  "dhclient_lease_dir": {
    "type": "string",
    "default": "/var/lib/dhcp",
    "description": "Directory where dhclient stores lease information"
  },
  "udhcpc_pid_dir": {
    "type": "string",
    "default": "/var/run",
    "description": "Directory where udhcpc writes its PID file"
  }
}
