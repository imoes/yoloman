{
  "input_dir": {
    "type": "string",
    "default": "/var/lib/mscompress/incoming",
    "description": "Directory containing source files to be compressed by mscompress."
  },
  "output_dir": {
    "type": "string",
    "default": "/var/lib/mscompress/compressed",
    "description": "Directory where compressed files (with .u extension) will be stored."
  },
  "compression_level": {
    "type": "number",
    "default": 5,
    "description": "The LZ77 compression efficiency level. mscompress typically uses a default, but wrappers may expose this."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "If true, enables verbose output from the compression process."
  }
}
