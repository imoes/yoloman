{
  "whereami_path": {
    "type": "string",
    "default": "/usr/sbin/whereami",
    "description": "Path to the whereami binary."
  },
  "config_path": {
    "type": "string",
    "default": "/etc/whereami/apm.conf",
    "description": "Path to the whereami configuration file."
  }
}
