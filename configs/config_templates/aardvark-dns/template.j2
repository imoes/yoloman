# /etc/aardvark-dns/forward.conf
# Forwarding configuration for aardvark-dns
# Each line specifies a domain and one or more upstream DNS servers
{% for forward in forwards %}
# Forward DNS queries for {{ forward.domain }} to these servers
{% if forward.policy == 