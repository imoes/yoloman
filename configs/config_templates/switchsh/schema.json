{
  "command": {
    "type": "string",
    "default": "/usr/bin/env bash -l",
    "description": "The primary command to execute. switchsh bind-mounts bash as /bin/sh for this command."
  },
  "arguments": {
    "type": "list",
    "default": [],
    "description": "List of arguments to pass to the command. This allows using bashisms within the context of the command execution."
  }
}
