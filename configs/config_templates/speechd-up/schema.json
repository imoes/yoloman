{
  "LOGDIR": {
    "type": "string",
    "default": "/var/log/speechd-up",
    "description": "Log directory to use"
  },
  "DAEMON_OPTS": {
    "type": "string",
    "default": "-l1",
    "description": "Additional options given to the server"
  },
  "DIETIME": {
    "type": "number",
    "default": 10,
    "description": "Time to wait for the server to die, in seconds"
  },
  "STARTTIME": {
    "type": "number",
    "default": 2,
    "description": "Time to wait for the server to start, in seconds"
  }
}
