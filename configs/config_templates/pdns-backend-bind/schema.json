{
  "zone_directory": {
    "type": "string",
    "default": "/var/lib/powerdns",
    "description": "Base directory for PowerDNS zone files and configuration."
  },
  "bind_master_ip": {
    "type": "string",
    "default": "192.0.2.53",
    "description": "IP address of the bind master for slave zones."
  }
}
