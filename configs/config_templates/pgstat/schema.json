{
 "count": {
  "default": 0,
  "description": "Number of iterations to display (0 means infinite). (corresponds to 'count' argument in CLI).",
  "type": "number"
 },
 "database": {
  "default": "postgres",
  "description": "Database to connect to (corresponds to -d).",
  "type": "string"
 },
 "delay": {
  "default": 1,
  "description": "Delay in seconds between updates (corresponds to 'delay' argument in CLI).",
  "type": "number"
 },
 "filter": {
  "default": "",
  "description": "Include only this object (only works for database, table, tableio, index, function, statement statistics, replication slots, and slru). (corresponds to -f).",
  "type": "string"
 },
 "filter_objects": {
  "default": [],
  "description": "List of filter objects to include if using -f.",
  "type": "list"
 },
 "host": {
  "default": "localhost",
  "description": "Database server host or socket directory (corresponds to -h).",
  "type": "string"
 },
 "human_readable": {
  "default": false,
  "description": "Display human-readable values (corresponds to -H).",
  "type": "bool"
 },
 "no_header": {
  "default": false,
  "description": "Do not redisplay header (corresponds to -n).",
  "type": "bool"
 },
 "port": {
  "default": 5432,
  "description": "Database server port number (corresponds to -p).",
  "type": "number"
 },
 "stat": {
  "default": "bgwriter",
  "description": "Stats to collect (e.g., bgwriter, database, table, tableio, index, function, statement, replication slots, slru, wal, etc.). (corresponds to -s).",
  "enum": [
   "archiver",
   "bgwriter",
   "buffercache",
   "checkpointer",
   "connection",
   "database",
   "table",
   "tableio",
   "index",
   "function",
   "statement",
   "slru",
   "xlog",
   "deadlive",
   "repslots",
   "tempfile",
   "waitevent",
   "wal",
   "progress_analyze",
   "progress_basebackup",
   "progress_cluster",
   "progress_copy",
   "progress_createindex",
   "progress_vacuum",
   "pbpools",
   "pbstats"
  ],
  "type": "string"
 },
 "substat": {
  "description": "Part of stats to display (only works for database and statement). (corresponds to -S).",
  "enum": [
   "database",
   "statement"
  ],
  "type": "string"
 },
 "user": {
  "default": "postgres",
  "description": "Connect as specified database user (corresponds to -U).",
  "type": "string"
 },
 "verbose": {
  "default": false,
  "description": "Verbose output (corresponds to -v).",
  "type": "bool"
 }
}
