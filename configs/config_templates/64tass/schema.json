{
  "options": {
    "type": "object",
    "description": "64tass command-line options grouped under a single top-level object for clarity (as no native config file format exists); all values map directly to documented CLI flags or their equivalents."
  }
}
