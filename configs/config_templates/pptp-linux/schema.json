{
  "require_mppe_128": {
    "type": "string",
    "default": "#require-mppe-128",
    "description": "MPPE 128-bit encryption requirement from the PPP project fork. Uncomment to enable."
  },
  "mppe_required": {
    "type": "string",
    "default": "#mppe required,stateless",
    "description": "MPPE required encryption setting from the Alphacron fork. Uncomment to enable."
  }
}
