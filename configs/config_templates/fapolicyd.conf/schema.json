{
  "debug_enabled": {
    "type": "boolean",
    "default": {
      "value": 0
    },
    "description": "This option is either a 0 to mean send policy decisions to the kernel for enforcement. Or it can be a 1 to mean always allow the access even if policy would block it. Use only for policy testing and debug."
  },
  "nice_val": {
    "type": "number",
    "default": {
      "value": 10
    },
    "description": "This option gives fapolicyd a scheduler boost. The number can be from 0 to 20."
  },
  "q_size": {
    "type": "number",
    "default": {
      "value": 800
    },
    "description": "This option is used to control how big of an internal queue that fapolicyd will use."
  },
  "uid": {
    "type": "string",
    "default": {
      "value": "0"
    },
    "description": "This can be a number or an account name which fapolicyd should switch to during startup."
  },
  "gid": {
    "type": "string",
    "default": {
      "value": "0"
    },
    "description": "This can be a number or a group name which fapolicyd should switch to during startup."
  },
  "do_stat_report": {
    "type": "boolean",
    "default": {
      "value": 1
    },
    "description": "This option controls whether fapolicyd should create a usage statistics report on shutdown."
  },
  "detailed_report": {
    "type": "boolean",
    "default": {
      "value": 1
    },
    "description": "This option controls whether fapolicyd should add subject and object information to the usage statistics report."
  },
  "db_max_size": {
    "type": "number",
    "default": {
      "value": 50
    },
    "description": "This option controls how many megabytes to allow the trust database to grow to."
  },
  "subj_cache_size": {
    "type": "number",
    "default": {
      "value": 1549
    },
    "description": "This option controls how many entries the subject cache holds."
  },
  "obj_cache_size": {
    "type": "number",
    "default": {
      "value": 8191
    },
    "description": "This option controls how many entries the object cache holds."
  },
  "watch_fs": {
    "type": "list",
    "default": {
      "value": [
        "ext4",
        "xfs",
        "tmpfs"
      ]
    },
    "description": "This is a comma separated list of file systems that should be watched."
  },
  "trust": {
    "type": "list",
    "default": {
      "value": [
        "rpmdb",
        "file"
      ]
    },
    "description": "This is a comma separated list of trust back-ends."
  },
  "integrity": {
    "type": "string",
    "default": {
      "value": "none"
    },
    "description": "This option tells fapolicyd which integrity strategy it should use."
  },
  "syslog_format": {
    "type": "string",
    "default": {
      "value": "rule,dec,perm,auid,pid,exe,:,path,ftype,trust"
    },
    "description": "This option controls how the output from the access decision is formatted."
  },
  "rpm_sha256_only": {
    "type": "boolean",
    "default": {
      "value": 0
    },
    "description": "The option set to 1 forces the daemon to work only with SHA256 hashes."
  },
  "allow_filesystem_mark": {
    "type": "boolean",
    "default": {
      "value": 0
    },
    "description": "When this option is set to 1, it allows fapolicyd to monitor file access events on bind mounted or overlayed file systems."
  },
  "report_interval": {
    "type": "number",
    "default": {
      "value": 0
    },
    "description": "This option specifies a reporting interval, measured in seconds, which fapolicyd uses to schedule a recurring dump of internal performance statistics."
  },
  "permissive": {
    "type": "boolean",
    "default": {
      "value": 0
    },
    "description": "This option is either a 0 to mean send policy decisions to the kernel for enforcement. Or it can be a 1 to mean always allow the access even if policy would block it."
  }
}
