{
  "mediawiki_docroot": {
    "type": "string",
    "default": "/var/lib/mediawiki",
    "description": "Document root directory where MediaWiki is installed."
  },
  "mediawiki_alias": {
    "type": "string",
    "default": "/mediawiki",
    "description": "URL alias under which MediaWiki is served."
  }
}
