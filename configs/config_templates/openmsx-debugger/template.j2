# OpenMSX Debugger (openmsx-debugger) configuration
#
# NOTE: openmsx-debugger is a command-line utility / library component for the
# OpenMSX emulator. It does NOT run as a background service and does NOT
# accept network connections. It has NO persistent configuration file.
#
# Any "configuration" is typically passed via command-line arguments or
# environment variables at runtime. This template therefore produces a
# minimal placeholder to satisfy Ansible/template requirements, but no
# actual functional directives are generated.

# No service configuration is applicable for openmsx-debugger.