# =============================================================================
# canna-shion Configuration Template
# =============================================================================
# This template generates the configuration file for the canna-shion package.
# It defines module identifiers for various subsystems (basho, keisan, pub,
# scien, sup) used by both CBD (likely Binary/Config Data) and CLD (likely
# Channel/Link Data) components.
#
# All fields use the schema defaults provided in the package definition.
# =============================================================================

# ------------------------------------------------------------------------------
# basho_mwd
# Description: Path or identifier for basho.mwd (typically a module or config file reference)
# ------------------------------------------------------------------------------
basho.cbd({{ basho_mwd | default('basho.mwd') }})            -basho---

# ------------------------------------------------------------------------------
# keisan_mwd
# Description: Path or identifier for keisan.mwd
# ------------------------------------------------------------------------------
keisan.cbd({{ keisan_mwd | default('keisan.mwd') }})          -keisan---

# ------------------------------------------------------------------------------
# pub_mwd
# Description: Path or identifier for pub.mwd
# ------------------------------------------------------------------------------
pub.cbd({{ pub_mwd | default('pub.mwd') }})                -pub---

# ------------------------------------------------------------------------------
# scien_mwd
# Description: Path or identifier for scien.mwd
# ------------------------------------------------------------------------------
scien.cbd({{ scien_mwd | default('scien.mwd') }})            -scien---

# ------------------------------------------------------------------------------
# sup_mwd
# Description: Path or identifier for sup.mwd
# ------------------------------------------------------------------------------
sup.cbd({{ sup_mwd | default('sup.mwd') }})                -sup---

# =============================================================================
# CLD Section
# =============================================================================
# The CLD entries mirror the CBD entries, likely pointing to the same or
# related module identifiers for channel/link data operations.

# ------------------------------------------------------------------------------
# basho_mwd (CLD)
# Description: Path or identifier for basho.mwd
# ------------------------------------------------------------------------------
basho.cld({{ basho_mwd | default('basho.mwd') }})            -basho---

# ------------------------------------------------------------------------------
# keisan_mwd (CLD)
# Description: Path or identifier for keisan.mwd
# ------------------------------------------------------------------------------
keisan.cld({{ keisan_mwd | default('keisan.mwd') }})          -keisan---

# ------------------------------------------------------------------------------
# pub_mwd (CLD)
# Description: Path or identifier for pub.mwd
# ------------------------------------------------------------------------------
pub.cld({{ pub_mwd | default('pub.mwd') }})                -pub---

# ------------------------------------------------------------------------------
# scien_mwd (CLD)
# Description: Path or identifier for scien.mwd
# ------------------------------------------------------------------------------
scien.cld({{ scien_mwd | default('scien.mwd') }})            -scien---

# ------------------------------------------------------------------------------
# sup_mwd (CLD)
# Description: Path or identifier for sup.mwd
# ------------------------------------------------------------------------------
sup.cld({{ sup_mwd | default('sup.mwd') }})                -sup---