{
 "binary": {
  "default": "/usr/bin/sipcalc",
  "description": "Path to the sipcalc executable",
  "type": "string"
 },
 "command": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "resolve": {
  "default": false,
  "description": "If true, enable DNS name resolution (-d) for addresses.",
  "type": "bool"
 },
 "subnets": {
  "default": 0,
  "description": "Number of extra subnets to display. 0 means all possible subnets.",
  "type": "number"
 },
 "targets": {
  "default": [
   "192.168.1.0/24"
  ],
  "description": "List of IP addresses, CIDR notations, or interface names to calculate for. Each entry is processed by sipcalc.",
  "type": "list"
 }
}
