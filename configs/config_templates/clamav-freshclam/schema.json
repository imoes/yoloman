{
  "freshclam_pidfile": {
    "type": "string",
    "default": "/run/clamav/freshclam.pid",
    "description": "Path to the freshclam PID file"
  },
  "freshclam_database_directory": {
    "type": "string",
    "default": "/var/run/clamav",
    "description": "Directory for freshclam's runtime database files (e.g., lock file)"
  },
  "freshclam_logfile": {
    "type": "string",
    "default": "/var/log/clamav/freshclam.log",
    "description": "Path to the freshclam log file"
  },
  "clamav_database_directory": {
    "type": "string",
    "default": "/var/lib/clamav",
    "description": "Directory where ClamAV virus database files are stored"
  },
  "clamav_user": {
    "type": "string",
    "default": "clamav",
    "description": "User account under which ClamAV processes run"
  }
}
