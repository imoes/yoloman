{
  "vt": {
    "type": "number",
    "default": 7,
    "description": "The VT to run the greeter on. Can be \"next\", \"current\" or a number designating the VT."
  },
  "command": {
    "type": "string",
    "default": "\"/usr/sbin/agreety --cmd '${SHELL:-/bin/sh}'\"",
    "description": "The default session command, also known as the greeter."
  },
  "user": {
    "type": "string",
    "default": "_greetd",
    "description": "The user to run the command as."
  }
}
