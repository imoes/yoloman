{
  "admin_email": {
    "type": "string",
    "default": "webmaster@localhost",
    "description": "Administrator email address for server notices"
  },
  "bind_address": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address to bind the HTTP server to (e.g., '0.0.0.0' for all interfaces)"
  },
  "http_port": {
    "type": "number",
    "default": 8080,
    "description": "TCP port for HTTP(S) traffic"
  },
  "server_name": {
    "type": "string",
    "default": "paste.example.com",
    "description": "Primary server hostname/domain"
  }
}
