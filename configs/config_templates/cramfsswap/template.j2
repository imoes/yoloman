# cramfsswap configuration (Debian trixie, cramfsswap 1.4.4)
# Swap endianness of a cram filesystem
# Usage: cramfsswap <infile> <outfile>

# Source: cramfsswap(1) manpage — Debian trixie
# Official synopsis: cramfsswap <infile> <outfile>

# Path to input cramfs image file. Required for actual operation.
{% if source_path | default('') %}# Source image path: {{ source_path }}
{% endif %}

# Path to output cramfs image file (with swapped endianness). Required for actual operation.
{% if target_path | default('') %}# Target image path: {{ target_path }}
{% endif %}