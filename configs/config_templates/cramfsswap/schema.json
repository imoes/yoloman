{
  "source_path": {
    "type": "string",
    "default": "",
    "description": "Path to input cramfs image file. Required for actual operation."
  },
  "target_path": {
    "type": "string",
    "default": "",
    "description": "Path to output cramfs image file (with swapped endianness). Required for actual operation."
  }
}
