{
  "base_format": {
    "default": "deb",
    "description": "Base package format to read from (rpm|deb|tgz|slp|pkg). alien reads the input package type from its filename extension, but this can be used to force interpretation.",
    "enum": [
      "rpm",
      "deb",
      "tgz",
      "slp",
      "pkg"
    ],
    "type": "string"
  },
  "bump_version": {
    "default": 0,
    "description": "Increment the converted package version by this number instead of the default +1 (requires keep_version=false).",
    "type": "number"
  },
  "description": {
    "default": "",
    "description": "Package description (only effective when converting from tgz, which lacks descriptions).",
    "type": "string"
  },
  "email": {
    "default": "",
    "description": "Email address to embed in generated Debian packages. Overrides environment variable EMAIL.",
    "type": "string"
  },
  "fixperms": {
    "default": false,
    "description": "Sanitize file owners and permissions during deb generation. May break some packages; defaults to off.",
    "type": "bool"
  },
  "generate_only": {
    "default": false,
    "description": "Generate a temporary build directory but do not build the package. Useful for manual modification before building.",
    "type": "bool"
  },
  "input_files": {
    "default": [],
    "description": "List of input package files to convert. Required. Supported extensions: .rpm, .deb, .tgz, .slp, .pkg.",
    "type": "list"
  },
  "install": {
    "default": false,
    "description": "Whether to automatically install each generated package after conversion and remove the input package file.",
    "type": "bool"
  },
  "keep_version": {
    "default": false,
    "description": "Do not increment the minor version number when converting packages. By default, alien adds 1 to the minor version.",
    "type": "bool"
  },
  "no_patch": {
    "default": false,
    "description": "Disable all patching when building a deb package.",
    "type": "bool"
  },
  "patch_file": {
    "default": "",
    "description": "Explicit patch file path to use during deb generation, overriding automatic lookup in /var/lib/alien. Has no effect unless generating a deb.",
    "type": "string"
  },
  "scripts": {
    "default": false,
    "description": "Whether to convert package maintainer scripts (preinst, postinst, prerm, postrm). Note: review scripts manually before using.",
    "type": "bool"
  },
  "single_directory": {
    "default": false,
    "description": "Generate a single directory without the .orig subdirectory (useful when disk space is constrained and building a deb).",
    "type": "bool"
  },
  "target_arch": {
    "description": "Force the architecture string for the generated package (e.g., 'amd64', 'i386', 'all').",
    "enum": [
      "all",
      "source",
      "any"
    ],
    "type": "string",
    "enum_open": true
  },
  "target_formats": {
    "default": [
      "deb"
    ],
    "description": "List of output formats to generate. Valid values: deb, rpm, tgz, slp, pkg. If omitted but install is true, defaults to ['deb'].",
    "type": "list"
  },
  "test_package": {
    "default": false,
    "description": "Run lintian (if installed) to test generated deb packages. Currently deb-only.",
    "type": "bool"
  },
  "verbose": {
    "default": false,
    "description": "Enable verbose mode: print commands as they are executed.",
    "type": "bool"
  },
  "version": {
    "default": "",
    "description": "Package version (only effective when converting from tgz, which may lack version info).",
    "type": "string"
  },
  "very_verbose": {
    "default": false,
    "description": "Very verbose mode: also print output of each executed command.",
    "type": "bool"
  }
}
