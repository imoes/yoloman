{
  "loglevel": {
    "type": "string",
    "default": "INFO",
    "description": "Logging level (e.g., DEBUG, INFO, WARNING, ERROR). Corresponds to CLI option -L/--log.",
    "enum": [
      "INFO",
      "ERROR"
    ],
    "enum_open": true
  },
  "logfile": {
    "type": "string",
    "default": "",
    "description": "Path to log file. If empty, logs to stderr. Corresponds to CLI option -l/--logfile."
  }
}
