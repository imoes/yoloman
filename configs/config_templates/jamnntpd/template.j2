# Defaults for jamnntpd initscript
# sourced by /etc/init.d/jamnntpd
# installed at /etc/default/jamnntpd by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
# Defaults to an empty string if not provided in the Ansible variables.
DAEMON_ARGS="{{ daemon_args | default('') }}"