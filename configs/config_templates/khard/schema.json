{
  "default_addressbook": {
    "type": "string",
    "default": "Local",
    "description": "The default address book to use for new entries if not specified."
  },
  "default_sort_key": {
    "type": "string",
    "default": "formatted_name",
    "description": "The field used to sort contacts by default."
  },
  "default_sort_reverse": {
    "type": "bool",
    "default": false,
    "description": "Whether to sort contacts in reverse order by default."
  },
  "addressbooks": {
    "type": "list",
    "default": [
      {
        "name": "Local",
        "path": "~/.khard/addressbooks",
        "type": "files",
        "primary": true,
        "default": true,
        "search_template": "",
        "edit_template": ""
      }
    ],
    "description": "List of address book definitions. Items is a map of fields: name, path, type, primary, default, search_template, edit_template."
  },
  "contact_defaults_addressbook": {
    "type": "string",
    "default": "Local",
    "description": "Default addressbook for new contacts."
  }
}
