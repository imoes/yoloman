{
  "verbose_level": {
    "type": "number",
    "default": 0,
    "description": "Level of verbosity (--verbose). 0 for normal, 1 for verbose, 2 for more verbose."
  },
  "non_interactive": {
    "type": "bool",
    "default": false,
    "description": "Whether to use --non-interactive flag to prevent user prompts."
  },
  "extract_outdir": {
    "type": "string",
    "default": ".",
    "description": "Default output directory for extract command (--outdir)."
  },
  "alias_list": {
    "type": "string",
    "default": "pl",
    "description": "Shell alias string for patool list."
  },
  "alias_extract": {
    "type": "string",
    "default": "px",
    "description": "Shell alias string for patool extract."
  },
  "alias_create": {
    "type": "string",
    "default": "pc",
    "description": "Shell alias string for patool create."
  },
  "alias_diff": {
    "type": "string",
    "default": "pd",
    "description": "Shell alias string for patool diff."
  }
}
