{
  "uuidd_user": {
    "type": "string",
    "default": "uuidd",
    "description": "System user account under which the uuidd daemon runs."
  },
  "uuidd_group": {
    "type": "string",
    "default": "uuidd",
    "description": "System group account under which the uuidd daemon runs."
  },
  "uuidd_runstatedir": {
    "type": "string",
    "default": "/run/uuidd",
    "description": "Directory for runtime state files (pidfile, sockets)."
  },
  "uuidd_localstatedir": {
    "type": "string",
    "default": "/var/lib/libuuid",
    "description": "Directory for persistent state data generated by the uuid daemon."
  }
}
