{
  "idle_exemptions_ppp": {
    "type": "number",
    "default": -1,
    "description": "Idle timeout for ppp-.* users (in minutes). -1 means exempt."
  },
  "idle_pty": {
    "type": "number",
    "default": 30,
    "description": "Idle timeout for pty.* sessions (in minutes)."
  },
  "grace_pty": {
    "type": "number",
    "default": 30,
    "description": "Grace period before termination for pty.* sessions (in seconds)."
  },
  "idle_games": {
    "type": "number",
    "default": 10,
    "description": "Idle timeout for users in group=games (in minutes)."
  },
  "grace_games": {
    "type": "number",
    "default": 60,
    "description": "Grace period for group=games (in seconds)."
  },
  "idle_lynx": {
    "type": "number",
    "default": 10,
    "description": "Idle timeout for group=lynx.* users (in minutes)."
  },
  "grace_lynx": {
    "type": "number",
    "default": 60,
    "description": "Grace period for group=lynx.* users (in seconds)."
  },
  "idle_root": {
    "type": "number",
    "default": -1,
    "description": "Idle timeout for root user (in minutes). -1 means exempt."
  },
  "idle_student": {
    "type": "number",
    "default": 15,
    "description": "Idle timeout for group=student (in minutes)."
  },
  "grace_student": {
    "type": "number",
    "default": 180,
    "description": "Grace period for group=student (in seconds)."
  },
  "idle_users": {
    "type": "number",
    "default": 60,
    "description": "Idle timeout for group=users (in minutes)."
  },
  "grace_users": {
    "type": "number",
    "default": 120,
    "description": "Grace period for group=users (in seconds)."
  },
  "ban_users": {
    "type": "number",
    "default": 20,
    "description": "Ban duration in minutes for group=users if they extend session limit."
  },
  "idle_guest": {
    "type": "number",
    "default": 60,
    "description": "Idle timeout for guest user (in minutes)."
  },
  "grace_guest": {
    "type": "number",
    "default": 60,
    "description": "Grace period for guest user (in seconds)."
  },
  "ban_guest": {
    "type": "number",
    "default": 5,
    "description": "Ban duration in minutes for guest if they extend session limit."
  }
}
