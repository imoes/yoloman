{
  "strict_mode": {
    "type": "bool",
    "default": true,
    "description": "If true, yapet checks password files for having the same owner as the user running yapet and verifies that the file mode is strictly 0600. If false, these checks are disabled."
  },
  "screen_lock_timeout": {
    "type": "number",
    "default": 600,
    "description": "The timeout in seconds until the screen is locked. Minimum allowed value is 10."
  }
}
