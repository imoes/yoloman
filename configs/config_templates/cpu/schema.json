{
  "config_file_path": {
    "type": "string",
    "default": "",
    "description": "Optional path to the config file for documentation purposes only; not used by cpu(8)"
  },
  "sections": {
    "type": "list",
    "default": [],
    "description": "List of configuration sections, each containing key-value pairs for cpu modules (e.g., [ldap], [passwd], [general]). Each item is a flat object with 'name' and 'settings'."
  }
}
