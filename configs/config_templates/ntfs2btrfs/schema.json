{
  "compress_algo": {
    "type": "string",
    "default": "zstd",
    "description": "Compression algorithm for existing files: zstd, lzo, zlib, or none.",
    "enum": [
      "zstd",
      "lzo",
      "zlib",
      "none"
    ]
  },
  "hash_algo": {
    "type": "string",
    "default": "crc32c",
    "description": "Checksum algorithm: crc32c, xxhash, sha256, or blake2.",
    "enum": [
      "crc32c",
      "xxhash",
      "sha256",
      "blake2"
    ]
  },
  "no_datasum": {
    "type": "bool",
    "default": false,
    "description": "Skip calculating checksums for existing data."
  },
  "device": {
    "type": "string",
    "default": "/dev/sda1",
    "description": "The block device to convert from NTFS to btrfs."
  }
}
