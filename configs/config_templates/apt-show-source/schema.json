{
  "status_file": {
    "type": "string",
    "default": "/var/lib/dpkg/status",
    "description": "Path to dpkg status file (equivalent to -stf/--status-file)"
  },
  "list_dir": {
    "type": "string",
    "default": "/var/lib/apt/lists",
    "description": "Path to APT lists directory (equivalent to -ld/--list-dir)"
  },
  "package": {
    "type": "string",
    "default": "",
    "description": "Package name to query (equivalent to -p/--package)"
  },
  "version_only": {
    "type": "bool",
    "default": false,
    "description": "Print version only when package is specified (equivalent to --version-only)"
  },
  "all": {
    "type": "bool",
    "default": false,
    "description": "List all source-packages (equivalent to -a/--all)"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output (equivalent to -v/--verbose)"
  }
}
