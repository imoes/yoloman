{
  "backend": {
    "type": "string",
    "default": "auto",
    "description": "Backend used by utrans(1). Auto-detect if empty or 'auto'."
  },
  "output_dir": {
    "type": "string",
    "default": "",
    "description": "Directory where generated unit files are saved."
  },
  "sources": {
    "type": "list",
    "default": [],
    "description": "List of source directories to scan for .service and .timer units."
  },
  "dry_run": {
    "type": "bool",
    "default": false,
    "description": "If true, perform a dry run without making changes."
  },
  "update_only": {
    "type": "bool",
    "default": false,
    "description": "If true, only check for new/modified/deleted units."
  }
}
