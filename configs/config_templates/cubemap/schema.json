{
  "PIDFILE": {
    "type": "string",
    "default": "/var/run/cubemap.pid",
    "description": "Path to the PID file used by cubemap"
  },
  "DAEMON_ARGS": {
    "type": "string",
    "default": "/etc/cubemap.config",
    "description": "Command-line arguments passed to the cubemap daemon"
  },
  "VERBOSE": {
    "type": "string",
    "default": "yes",
    "description": "Set to 'no' to suppress startup/shutdown messages",
    "enum": [
      "yes",
      "no"
    ]
  }
}
