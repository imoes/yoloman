{
  "default_output_format": {
    "type": "string",
    "default": "intel-ih",
    "description": "The default EPROM load file format (e.g., intel-ih, motorola-srec, bin). Based on srecord(3) supported formats."
  },
  "verbose_errors": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable verbose error messages when processing files."
  }
}
