{
  "setuid_root": {
    "type": "bool",
    "default": true,
    "description": "Whether to run with setuid root privileges to enable mlock() for secure memory handling"
  },
  "gnupg_key": {
    "type": "string",
    "default": "0xDEADBEEF",
    "description": "The GnuPG key ID, fingerprint, or user ID of the key whose passphrase is being recovered"
  },
  "output_format": {
    "type": "string",
    "default": "text",
    "description": "The format for outputting recovery results (e.g., text, json)"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging to provide detailed execution information"
  }
}
