{
  "debug_files": {
    "type": "list",
    "default": [],
    "description": "List of source files to enable debug output for (e.g., ['vpn/vpn-provider.c']). Empty list disables file-specific debug filtering (use empty string or omit to use syslog)."
  },
  "nodaemon": {
    "type": "bool",
    "default": false,
    "description": "If true, disables daemonization (equivalent to -n flag). Log output goes to controlling terminal and syslog."
  }
}
