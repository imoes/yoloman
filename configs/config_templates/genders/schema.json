{
  "genders_file": {
    "type": "string",
    "default": "/etc/genders",
    "description": "Path to the genders file used by the library."
  },
  "debug_level": {
    "type": "number",
    "default": 0,
    "description": "Debug level; set to 1 to enable debug output."
  },
  "index_attrs": {
    "type": "list",
    "default": [],
    "description": "List of attribute names to pre-index for faster query performance."
  }
}
