{
  "wildcard": {
    "type": "string",
    "default": "#wildcard  ?",
    "description": "The wildcard character used to match any single byte. If changed, all occurrences in headers/footers must also be updated. Default is commented out '?'. Set to '?' to enable."
  },
  "gif_max_size": {
    "type": "number",
    "default": 5000000,
    "description": "Maximum file size for GIF files in bytes."
  },
  "jpg_max_size": {
    "type": "number",
    "default": 5242880,
    "description": "Maximum file size for JPG files in bytes."
  },
  "png_max_size": {
    "type": "number",
    "default": 20000000,
    "description": "Maximum file size for PNG files in bytes."
  },
  "bmp_max_size": {
    "type": "number",
    "default": 100000,
    "description": "Maximum file size for BMP files in bytes."
  },
  "tif_max_size": {
    "type": "number",
    "default": 200000000,
    "description": "Maximum file size for TIFF files in bytes."
  },
  "avi_max_size": {
    "type": "number",
    "default": 50000000,
    "description": "Maximum file size for AVI files in bytes."
  },
  "mov_max_size": {
    "type": "number",
    "default": 10000000,
    "description": "Maximum file size for MOV files in bytes."
  },
  "mpg_max_size": {
    "type": "number",
    "default": 50000000,
    "description": "Maximum file size for MPG files in bytes."
  },
  "fws_max_size": {
    "type": "number",
    "default": 4000000,
    "description": "Maximum file size for SWF (Flash) files in bytes."
  },
  "doc_max_size": {
    "type": "number",
    "default": 10000000,
    "description": "Maximum file size for DOC files in bytes."
  },
  "pst_max_size": {
    "type": "number",
    "default": 500000000,
    "description": "Maximum file size for PST files in bytes."
  },
  "ost_max_size": {
    "type": "number",
    "default": 500000000,
    "description": "Maximum file size for OST files in bytes."
  },
  "dbx_max_size": {
    "type": "number",
    "default": 10000000,
    "description": "Maximum file size for DBX files in bytes."
  },
  "idx_max_size": {
    "type": "number",
    "default": 10000000,
    "description": "Maximum file size for IDX files in bytes."
  },
  "mbx_max_size": {
    "type": "number",
    "default": 10000000,
    "description": "Maximum file size for MBX files in bytes."
  },
  "wpc_max_size": {
    "type": "number",
    "default": 1000000,
    "description": "Maximum file size for WPC files in bytes."
  },
  "pdf_max_size": {
    "type": "number",
    "default": 5000000,
    "description": "Maximum file size for PDF files in bytes."
  },
  "mail_max_size": {
    "type": "number",
    "default": 500000,
    "description": "Maximum file size for AOL Mail files in bytes."
  },
  "pgd_max_size": {
    "type": "number",
    "default": 500000,
    "description": "Maximum file size for PGP Disk files in bytes."
  },
  "pgp_key_max_size": {
    "type": "number",
    "default": 100000,
    "description": "Maximum file size for PGP Key Ring and related files in bytes."
  },
  "rpm_max_size": {
    "type": "number",
    "default": 1000000,
    "description": "Maximum file size for RPM files in bytes."
  },
  "wav_max_size": {
    "type": "number",
    "default": 200000,
    "description": "Maximum file size for WAV files in bytes."
  },
  "ra_max_size": {
    "type": "number",
    "default": 1000000,
    "description": "Maximum file size for RA (Real Audio) files in bytes."
  },
  "dat_nt_max_size": {
    "type": "number",
    "default": 4000000,
    "description": "Maximum file size for Windows NT registry (regf) files in bytes."
  },
  "dat_w95_max_size": {
    "type": "number",
    "default": 4000000,
    "description": "Maximum file size for Windows 95 registry (CREG) files in bytes."
  },
  "zip_max_size": {
    "type": "number",
    "default": 10000000,
    "description": "Maximum file size for ZIP files in bytes."
  },
  "java_max_size": {
    "type": "number",
    "default": 1000000,
    "description": "Maximum file size for Java (class) files in bytes."
  },
  "max_max_size": {
    "type": "number",
    "default": 1000000,
    "description": "Maximum file size for ScanSoft PaperPort Max files in bytes."
  },
  "pins_max_size": {
    "type": "number",
    "default": 8000,
    "description": "Maximum file size for PINs Password Manager files in bytes."
  }
}
