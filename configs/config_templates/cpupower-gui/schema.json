{
  "profile": {
    "type": "string",
    "default": "Balanced",
    "description": "CPU performance profile to apply (e.g., 'Balanced', 'Performance', 'PowerSave')"
  },
  "allcpus_default": {
    "type": "bool",
    "default": false,
    "description": "If true, apply settings to all CPUs by default"
  },
  "tick_marks_enabled": {
    "type": "bool",
    "default": true,
    "description": "If true, show tick marks on the CPU frequency slider"
  },
  "frequency_ticks": {
    "type": "bool",
    "default": true,
    "description": "If true, display frequency tick labels"
  },
  "energy_pref_per_cpu": {
    "type": "bool",
    "default": false,
    "description": "If true, allow per-CPU energy performance preference settings"
  }
}
