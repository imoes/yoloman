{
  "datadir": {
    "type": "string",
    "default": "/var/lib/collectd/rrd/",
    "description": "Path to the directory where RRD files are stored."
  },
  "libdir": {
    "type": "string",
    "default": "/usr/lib/collectd/",
    "description": "Path to the directory where collectd plugins are installed."
  }
}
