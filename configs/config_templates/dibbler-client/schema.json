{
  "name": {
    "type": "string",
    "default": "dibbler-client",
    "description": "Service name used in init script output messages and pidfile path."
  },
  "daemon_opts": {
    "type": "string",
    "default": "run",
    "description": "Options passed to the dibbler-client daemon at startup.",
    "enum": [
      "run",
      "start",
      "stop",
      "status",
      "install",
      "uninstall"
    ]
  }
}
