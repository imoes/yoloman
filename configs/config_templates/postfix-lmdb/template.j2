# Postfix virtual lookup table configuration
# Manages shared library settings for LMDB (Lightweight Directory Access Protocol) lookup tables in Postfix.

# shlib_directory: The directory where Postfix modules (shared libraries) are stored.
# This path must be accessible by the Postfix master process.
{{ shlib_directory | default('/usr/lib/postfix/modules') }}/postfix-lmdb.so:f:root:-:{{ shlib_mode | default('755') }}