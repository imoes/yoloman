{
  "shlib_directory": {
    "type": "string",
    "default": "/usr/lib/postfix/modules",
    "description": "The directory where Postfix modules (shared libraries) are stored."
  },
  "shlib_mode": {
    "type": "string",
    "default": "755",
    "description": "The file permissions for the Postfix module shared library."
  }
}
