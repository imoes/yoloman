{
  "icon_size": {
    "type": "string",
    "default": "128",
    "description": "Pixel dimension of icons (e.g., '128' for 128x128px)"
  },
  "default_enabled": {
    "type": "string",
    "default": "true",
    "description": "Whether to enable DEP-11 HiDPI icon downloads by default (must be string 'true'/'false' for APT config)"
  }
}
