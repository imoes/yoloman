# Path to the file that, if present, prevents hibernation.
# pm-utils checks for this file before attempting to hibernate the system.
touch {{ do_not_hibernate_path | default('/var/run/do-not-hibernate') }}