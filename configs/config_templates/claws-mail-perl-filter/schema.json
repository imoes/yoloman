{
  "permanent": {
    "type": "string",
    "default": "",
    "description": "Persistent scalar value across filtered messages; initialized to empty string."
  },
  "filter_log_verbosity": {
    "type": "number",
    "default": 2,
    "description": "Log verbosity level: 0=none, 1=manual messages only, 2=actions, 3=matches."
  },
  "rules": {
    "type": "list",
    "default": [],
    "description": "List of filter rules to apply to incoming messages. Each rule has a name (optional), condition, and optional actions. If no actions are defined, 'stop' is implied (but not final \u2014 internal filter still runs). Final rules are: dele, move, copy, forward*, redirect, move_to_trash, abort. See manpage for details."
  }
}
