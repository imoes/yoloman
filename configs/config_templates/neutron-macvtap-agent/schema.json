{
  "SYSTEM_USER": {
    "type": "string",
    "default": "neutron",
    "description": "The system user running the neutron-macvtap-agent."
  },
  "SYSTEM_GROUP": {
    "type": "string",
    "default": "neutron",
    "description": "The system group running the neutron-macvtap-agent."
  }
}
