{
  "debug": {
    "type": "number",
    "default": 0,
    "description": "Debug verbosity level (0\u20135). Higher values produce more logging to syslog."
  },
  "run_as_root": {
    "type": "bool",
    "default": false,
    "description": "Run cachefilesd as root. If false, drops privileges to the specified user/group."
  },
  "user": {
    "type": "string",
    "default": "cache",
    "description": "User account to drop privileges to when not running as root."
  },
  "group": {
    "type": "string",
    "default": "",
    "description": "Group to drop privileges to (optional; only used if not run_as_root)."
  },
  "pidfile": {
    "type": "string",
    "default": "/run/cachefilesd.pid",
    "description": "Path to the PID file for the daemon."
  },
  "cache_dir": {
    "type": "string",
    "default": "/var/cache/fscache",
    "description": "Primary cache directory \u2014 must be on an already-mounted filesystem with fscache support."
  },
  "extra_dirs": {
    "type": "list",
    "default": [],
    "description": "List of additional cache directories for tiered or mirrored caching.",
    "items": {
      "type": "string",
      "description": "Path to an additional cache directory."
    }
  },
  "cache_lower": {
    "type": "string",
    "default": "0",
    "description": "Lower bound on cache size (e.g., '1G', '500M', '100K' or '0' for unlimited minimum)."
  },
  "cache_upper": {
    "type": "string",
    "default": "10G",
    "description": "Upper bound on cache size (e.g., '10G', '500M'). Cache will be pruned when exceeded."
  },
  "scan_interval": {
    "type": "number",
    "default": 5,
    "description": "Seconds between automatic cache scans. Set to 0 to disable scanning."
  },
  "tag": {
    "type": "string",
    "default": "",
    "description": "Optional cache tag \u2014 useful when multiple caches are managed by a single host (e.g., for NFS server clustering)."
  }
}
