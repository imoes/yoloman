{
  "aliases": {
    "description": "Map of command alias names to their command strings. e.g. {pr: 'pr view'}",
    "type": "object"
  },
  "auth": {
    "default": [],
    "description": "List of authentication entries for hosts. Each entry provides a token and optional host (defaults to 'github.com'). Must be list of objects with fields: host, token, type (fixed to 'token').",
    "type": "list"
  },
  "editor": {
    "default": "",
    "description": "Default editor to use (e.g., 'vim', 'nano').",
    "type": "string"
  },
  "git_protocol": {
    "default": "",
    "description": "Protocol to use for git operations: 'ssh' or 'https'.",
    "type": "string",
    "enum": [
      "ssh",
      "https"
    ]
  },
  "isatty": {
    "description": "Control whether to force use of TTY for prompts. Valid values: '0' (disabled), '1' (enabled), '' (auto).",
    "enum": [
      "0",
      "1"
    ],
    "type": "string"
  },
  "pager": {
    "default": "",
    "description": "Pager to use for output (e.g., 'less', 'more', 'cat').",
    "type": "string"
  }
}
