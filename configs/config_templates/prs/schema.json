{
  "store_dir": {
    "type": "string",
    "default": "/home/user/.local/share/prs",
    "description": "The root directory for the password store tree."
  },
  "gpg_program": {
    "type": "string",
    "default": "gpg",
    "description": "The GPG executable binary path."
  },
  "recipients": {
    "type": "list",
    "default": [],
    "description": "List of GPG key IDs or email addresses to encrypt new passwords to. Items field describes the list elements."
  },
  "git_remote_url": {
    "type": "string",
    "default": "",
    "description": "The URL of the git remote repository for syncing the password store."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output."
  },
  "no_interact": {
    "type": "bool",
    "default": false,
    "description": "Disable interactive prompts."
  },
  "recipients_item": {
    "type": "string",
    "default": "",
    "description": "A single GPG recipient identifier (Key ID or Email)."
  }
}
