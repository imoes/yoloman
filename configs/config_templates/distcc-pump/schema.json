{
  "DISTCC_POTENTIAL_HOSTS": {
    "type": "list",
    "default": [],
    "description": "The distcc servers that will be queried by lsdistcc in order to produce a value for DISTCC_HOSTS. This value may be unset or null. In such cases, lsdistcc will not be invoked, and distcc will use DISTCC_HOSTS or the distcc hosts configuration file, as described in the \u201cHost Specifications\u201d section of distcc(1)."
  },
  "DISTCC_HOSTS": {
    "type": "list",
    "default": [],
    "description": "This variable is passed through to distcc but only if DISTCC_POTENTIAL_HOSTS is not set. Each entry follows distcc host syntax (e.g., localhost, host/4, 192.168.1.100+10)."
  },
  "DISTCC_MAX_DISCREPANCY": {
    "type": "number",
    "default": 0,
    "description": "see the distcc(1) man page."
  },
  "LSDISTCC_ARGS": {
    "type": "list",
    "default": [],
    "description": "Extra arguments to pass to lsdistcc. See lsdistcc --help for more details."
  },
  "INCLUDE_SERVER_ARGS": {
    "type": "list",
    "default": [],
    "description": "Extra arguments to pass to the include server."
  },
  "DISTCC_LOCATION": {
    "type": "string",
    "default": "",
    "description": "The location of the distcc bin directory, which is normally inferred from the link-resolved dirname of argv[0]. If this location is in fact the bin directory inside an installation (as will be the case when the pump script in the installation executes), then Python executables and distcc itself will be retrieved from the parent directory of the location."
  }
}
