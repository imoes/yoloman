{
  "ocf_functions_dir": {
    "type": "string",
    "default": "/usr/lib/ocf/lib/heartbeat",
    "description": "Directory containing OCF shell functions."
  },
  "rapiddisk_bin": {
    "type": "string",
    "default": "/sbin/rapiddisk",
    "description": "Path to the rapiddisk binary executable."
  },
  "default_mode": {
    "type": "string",
    "default": "wt",
    "description": "Default caching mode (wt for Write-Through, wa for Write-Around) if OCF_RESKEY_mode is not set.",
    "enum": [
      "wt",
      "wa",
      "wb"
    ]
  }
}
