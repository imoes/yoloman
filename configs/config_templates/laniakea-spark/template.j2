# Laniakea-Spark Configuration
#
# This file configures the Laniakea-Spark service.
# It is largely a no-op configuration file because the schema
# contains no service-defining parameters. It serves primarily
# as a placeholder or for future extensibility.
#
# Variables available (none in current schema):
#   - No configurable parameters defined.

# Default NOPASSWD rule for the _lkspark user to execute debspawn.
# This is often required for internal service orchestration or
# user management tasks within the container or chroot environment.
_lkspark ALL=(ALL) NOPASSWD: /usr/bin/debspawn