{
 "bumblebeed_driver": {
  "description": "The driver used by Bumblebee server; empty means auto-detection; valid: 'nvidia', 'nouveau'",
  "enum": [
   "nvidia",
   "nouveau"
  ],
  "type": "string"
 },
 "bumblebeed_keep_unused_x_server": {
  "default": false,
  "description": "Whether to keep the unused Xorg server running",
  "type": "bool"
 },
 "bumblebeed_no_eco_mode_override": {
  "default": false,
  "description": "If true, '-f' option on optirun is ignored",
  "type": "bool"
 },
 "bumblebeed_server_group": {
  "default": "bumblebee",
  "description": "The name of the Bumblebee server group (GID name)",
  "type": "string"
 },
 "bumblebeed_turn_card_off_at_exit": {
  "default": false,
  "description": "Whether to turn the GPU card off at Bumblebee server exit",
  "type": "bool"
 },
 "bumblebeed_virtual_display": {
  "default": ":8",
  "description": "The secondary Xorg server DISPLAY number",
  "type": "string"
 },
 "bumblebeed_xorg_binary": {
  "default": "/usr/lib/xorg/Xorg",
  "description": "Xorg binary to run",
  "type": "string"
 },
 "bumblebeed_xorg_conf_dir": {
  "default": "/etc/bumblebee/xorg.conf.d",
  "description": "Directory with a dummy config file to pass as -configdir to secondary X",
  "type": "string"
 },
 "driver_nouveau_kernel_driver": {
  "default": "nouveau",
  "description": "Kernel driver module name for nouveau",
  "type": "string"
 },
 "driver_nouveau_pm_method": {
  "default": "auto",
  "description": "Power management method for Nouveau",
  "enum": [
   "auto",
   "bbswitch",
   "switcheroo",
   "none"
  ],
  "type": "string"
 },
 "driver_nouveau_xorg_conf_file": {
  "default": "/etc/bumblebee/xorg.conf.nouveau",
  "description": "Xorg config file for Nouveau driver",
  "type": "string"
 },
 "driver_nvidia_always_unload_kernel_driver": {
  "default": false,
  "description": "Whether to always unload kernel drivers even with PMMethod=none",
  "type": "bool"
 },
 "driver_nvidia_kernel_driver": {
  "default": "nvidia",
  "description": "Kernel driver module name for nvidia (defaults to Driver if empty)",
  "type": "string"
 },
 "driver_nvidia_library_path": {
  "default": "/usr/lib/x86_64-linux-gnu/nvidia:/usr/lib/i386-linux-gnu/nvidia:/usr/lib/x86_64-linux-gnu:/usr/lib/i386-linux-gnu",
  "description": "Colon-separated paths to nvidia libraries",
  "type": "string"
 },
 "driver_nvidia_pm_method": {
  "default": "auto",
  "description": "Power management method for NVIDIA; valid: 'auto', 'bbswitch', 'switcheroo', 'none'",
  "enum": [
   "auto",
   "bbswitch",
   "switcheroo",
   "none"
  ],
  "type": "string"
 },
 "driver_nvidia_xorg_conf_file": {
  "default": "/etc/bumblebee/xorg.conf.nvidia",
  "description": "Xorg config file for NVIDIA driver",
  "type": "string"
 },
 "driver_nvidia_xorg_module_path": {
  "default": "/usr/lib/nvidia,/usr/lib/xorg/modules",
  "description": "Comma-separated path to nvidia_drv.so and default Xorg modules",
  "type": "string"
 },
 "optirun_allow_fallback_to_igc": {
  "default": false,
  "description": "Whether to allow program run under optirun even if Bumblebee server or NVIDIA card is unavailable",
  "type": "bool"
 },
 "optirun_bridge": {
  "default": "auto",
  "description": "Acceleration/rendering bridge; valid: 'auto', 'virtualgl', 'primus'",
  "enum": [
   "auto",
   "virtualgl",
   "primus"
  ],
  "type": "string"
 },
 "optirun_primus_library_path": {
  "default": "/usr/lib/x86_64-linux-gnu/primus:/usr/lib/i386-linux-gnu/primus",
  "description": "Colon-separated paths searched for primus libGL.so.1",
  "type": "string"
 },
 "optirun_vgl_transport": {
  "default": "proxy",
  "description": "Method for VirtualGL frame transport; valid: 'proxy', 'jpeg', 'rgb', 'xv', 'yuv'",
  "enum": [
   "proxy",
   "jpeg",
   "rgb",
   "xv",
   "yuv"
  ],
  "type": "string"
 }
}
