# pytrainer configuration file

# Directory where application configuration will be stored.
# Default: ~/.pytrainer
--confdir={{ confdir | default('~/.pytrainer') }}

# Specify where logging should output to.
# Allowed values: 'file', 'console'
# Default: file
--logtype={{ logtype | default('file') }}