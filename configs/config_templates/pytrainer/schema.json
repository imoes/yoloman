{
  "confdir": {
    "type": "string",
    "default": "~/.pytrainer",
    "description": "Directory where application configuration will be stored."
  },
  "logtype": {
    "type": "string",
    "default": "file",
    "description": "Specify where logging should output to. One of 'file' or 'console'.",
    "enum": [
      "file",
      "console"
    ]
  }
}
