{
  "alertmanager_listen_address": {
    "type": "string",
    "default": "0.0.0.0:9093",
    "description": "Address on which Alertmanager listens for incoming alerts and UI traffic."
  },
  "global_smtp_smarthost": {
    "type": "string",
    "default": "",
    "description": "SMTP smarthost used for sending email notifications (e.g., 'smtp.example.com:587')."
  }
}
