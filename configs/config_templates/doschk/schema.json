{
  "paths": {
    "type": "list",
    "default": [],
    "description": "List of file and directory paths to check. This template provides the stdin input for `doschk`, as `doschk` does not use a configuration file. Each item is a path string (directory or file). Directories should be listed explicitly to ensure they are checked for 14-character SYSV uniqueness; files are checked for 8.3 MS-DOS compatibility."
  }
}
