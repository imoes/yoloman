{
  "listen_address": {
    "type": "string",
    "default": ":8000",
    "description": "TCP or UNIX listen address for the restic-rest-server. Example: ':8000' for TCP or 'unix:/run/restic-rest-server' for Unix socket."
  },
  "backup_dir": {
    "type": "string",
    "default": "/srv/backups/",
    "description": "Directory to store backups. The server will not start unless this is defined."
  },
  "htpasswd_file": {
    "type": "string",
    "default": "/etc/restic-rest-server/users.htpasswd",
    "description": "Path to the htpasswd file for basic authentication."
  }
}
