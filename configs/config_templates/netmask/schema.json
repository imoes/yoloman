{
  "addresses": {
    "type": "list",
    "default": [],
    "description": "List of network specifications. Each item is a map with 'address' (string: IP address) and 'mask' (string: netmask in CIDR prefix like '24' or dotted quad like '255.255.255.0'). The template renders these as standard address/mask pairs suitable for firewall or routing rules. See netmask(1) for valid address and mask formats."
  }
}
