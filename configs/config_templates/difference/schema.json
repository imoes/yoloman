{
  "orig": {
    "type": "string",
    "default": "",
    "description": "Original string to compare."
  },
  "edit": {
    "type": "string",
    "default": "",
    "description": "Modified string to compare against orig."
  },
  "split": {
    "type": "string",
    "default": "word",
    "description": "Granularity level: 'char', 'word', or 'line'. Defaults to 'word'.",
    "enum": [
      "char",
      "word",
      "line"
    ]
  },
  "examples": {
    "type": "list",
    "default": [],
    "description": "List of example diff invocations (for documentation/testing). Each item contains optional orig, edit, and split fields."
  }
}
