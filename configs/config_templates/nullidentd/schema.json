{
  "disable": {
    "type": "bool",
    "default": true,
    "description": "Whether the service is disabled (inetd/xinetd style). Set to false to enable."
  },
  "server_args_user": {
    "type": "string",
    "default": "nobody",
    "description": "First argument passed to /usr/sbin/nullidentd. Typically matches the 'user' setting, but kept separate as per real config."
  },
  "user": {
    "type": "string",
    "default": "nobody",
    "description": "User account under which the daemon runs (for inetd/xinetd service definition)."
  }
}
