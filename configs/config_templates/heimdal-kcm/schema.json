{
  "provides": {
    "type": "string",
    "default": "heimdal-kcm",
    "description": "Service name provided by this init script (used for dependency ordering and service management)"
  },
  "kcm_daemon": {
    "type": "string",
    "default": "/usr/sbin/kcm",
    "description": "Full path to the KCM daemon executable"
  },
  "kcm_name": {
    "type": "string",
    "default": "kcm",
    "description": "Short name used for PID file and logging"
  },
  "kcm_desc": {
    "type": "string",
    "default": "Heimdal KCM",
    "description": "Human-readable description of the service"
  },
  "kcm_params": {
    "type": "string",
    "default": "--detach",
    "description": "Command-line arguments passed to the daemon at startup"
  },
  "short_description": {
    "type": "string",
    "default": "Start KCM server",
    "description": "Short description shown in LSB header (replaces 'Start KCM server' in INIT INFO block)"
  }
}
