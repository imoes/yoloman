# Additional startup parameters for the thinkfan daemon
# These arguments are passed to thinkfan when the service starts.
# Common options: -q (quiet), -p /var/run/thinkfan.pid (pid file)
DAEMON_ARGS="{{ (daemon_args | default('-q')) | string }}"