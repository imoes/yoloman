{
  "daemon_args": {
    "type": "string",
    "default": "-q",
    "description": "Arguments passed to the thinkfan daemon at startup (e.g., -q for quiet mode).",
    "enum": [
      "-q",
      "-v"
    ],
    "enum_open": true
  }
}
