{
  "crypttab_entries": {
    "type": "list",
    "default": [],
    "description": "List of encrypted block device entries to configure in crypttab. Each entry defines a mapping for an encrypted volume. Fields correspond to the four columns in crypttab(5): volume-name, encrypted-device, key-file, options."
  }
}
