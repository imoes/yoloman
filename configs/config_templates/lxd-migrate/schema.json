{
  "rsync_args": {
    "type": "list",
    "default": [
      "-avz",
      "--delete",
      "--exclude=/proc/*",
      "--exclude=/sys/*",
      "--exclude=/dev/*"
    ],
    "description": "Extra arguments to pass to rsync during migration. Array of strings."
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Verbose mode."
  },
  "target_remote": {
    "type": "string",
    "default": "local",
    "description": "Target LXD remote."
  },
  "target_name": {
    "type": "string",
    "default": "migrated-host",
    "description": "Target instance name."
  },
  "source_mounts": {
    "type": "list",
    "default": [
      {
        "path": "/",
        "target": "/"
      }
    ],
    "description": "Mount points to transfer. Structure: path (source), target (dest)."
  },
  "network": {
    "type": "object",
    "default": null,
    "description": "Network config for lxc launch equivalent options. Structure: type, address, netmask."
  },
  "items": {
    "type": "object",
    "default": null,
    "description": "Item schema for source_mounts list. Not used in JSON schema directly but defines the structure for the list."
  },
  "start_on_create": {
    "type": "bool",
    "default": true,
    "description": "Start instance after migration."
  }
}
