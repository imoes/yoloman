{
  "zabbix_log_dir": {
    "type": "string",
    "default": "zabbix-server",
    "description": "Directory for Zabbix server logs."
  },
  "zabbix_server_log_file": {
    "type": "string",
    "default": "zabbix_server.log",
    "description": "Name of the Zabbix server log file."
  },
  "zabbix_log_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "Number of rotated log files to keep."
  },
  "zabbix_log_file_permissions": {
    "type": "string",
    "default": "0640",
    "description": "Permissions for the log file."
  },
  "zabbix_log_user": {
    "type": "string",
    "default": "zabbix",
    "description": "User owner of the log file."
  },
  "zabbix_log_group": {
    "type": "string",
    "default": "zabbix",
    "description": "Group owner of the log file."
  }
}
