{
  "keys_directory": {
    "type": "string",
    "default": "/etc/tmate-ssh-server/keys",
    "description": "Path to the directory containing SSH host keys."
  },
  "listen_port": {
    "type": "number",
    "default": 10022,
    "description": "Port number on which the tmate-ssh-server listens for connections."
  }
}
