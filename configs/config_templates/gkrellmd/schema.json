{
  "bind_address": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address to bind the GKrellM server daemon to. Use '0.0.0.0' to listen on all interfaces."
  },
  "bind_port": {
    "type": "number",
    "default": 19150,
    "description": "TCP port on which gkrellmd will listen for client connections."
  },
  "allow_hosts": {
    "type": "list",
    "default": [
      "localhost",
      "127.0.0.1",
      "::ffff:127.0.0.1"
    ],
    "description": "List of hosts allowed to connect. Use '0.0.0.0/0' or '*' for all hosts (not recommended)."
  }
}
