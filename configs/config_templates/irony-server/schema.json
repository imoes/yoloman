{
 "ARGS": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "debug": {
  "default": false,
  "description": "Enable debug mode to write verbose info to the log file.",
  "type": "bool"
 },
 "interactive": {
  "default": false,
  "description": "Run the server in interactive command loop mode.",
  "type": "bool"
 },
 "log_file": {
  "default": "/var/log/irony-server.log",
  "description": "Path to the file where debug information is written. If empty, logs go to standard error.",
  "type": "string"
 }
}
