{
  "shared_preload_libraries": {
    "type": "list",
    "default": [],
    "description": "List of shared libraries to preload at server start. PL/Tcl requires 'pltcl' to be included here. Other libraries may be added as needed.",
    "items": {
      "type": "string",
      "description": "A shared library name, e.g., 'pltcl'."
    }
  },
  "pltcl_conf_path": {
    "type": "string",
    "default": "/etc/postgresql/17/main/pltcl.conf",
    "description": "Path to the optional PL/Tcl runtime configuration file, if used. This file may define pltcl.startup and pltcl.trusted_startup functions."
  }
}
