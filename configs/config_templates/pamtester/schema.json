{
 "operation": {
  "default": "authenticate",
  "description": "The PAM operation to perform. Valid values: authenticate, acct_mgmt, open_session, close_session, chauthtok",
  "enum": [
   "authenticate",
   "acct_mgmt",
   "open_session",
   "close_session",
   "chauthtok"
  ],
  "type": "string"
 },
 "options": {
  "description": "Optional flags to append to the operation in parentheses. Example: PAM_SILENT | PAM_DISALLOW_NULL_AUTHTOK. Leave empty for default behavior.",
  "enum": [
   "PAM_SILENT",
   "PAM_DISALLOW_NULL_AUTHTOK",
   "PAM_ESTABLISH_CRED",
   "PAM_REINITIALIZE_CRED",
   "PAM_REFRESH_CRED",
   "PAM_CHANGE_EXPIRED_AUTHTOK"
  ],
  "type": "string"
 }
}
