# Default settings for adcli
[adcli]
# The Active Directory domain to join
domain = {{ domain }}

# The domain controller hostname (optional; if not set, DNS SRV queries will be used)
# {{ domain_controller_comment if domain_controller_comment else 