{
  "local_delivery_addresses": {
    "type": "list",
    "default": [
      "root",
      "postmaster",
      "user",
      "user@mailhub",
      "user@mailhub.gadget.com",
      "user@gadget.com"
    ],
    "description": "List of addresses to be delivered locally."
  },
  "smtp_forward_to_firewall_addresses": {
    "type": "list",
    "default": [
      "user@firewall",
      "user@firewall.gadget.com"
    ],
    "description": "List of addresses to be forwarded with SMTP to firewall."
  },
  "smtp_forward_to_harker_addresses": {
    "type": "list",
    "default": [
      "user@harker.com",
      "harker!user"
    ],
    "description": "List of addresses to be forwarded with SMTP to harker.com."
  }
}
