{
  "init_provides": {
    "type": "string",
    "default": "boxbackup-server",
    "description": "Init script provides name (used by LSB)"
  },
  "short_description": {
    "type": "string",
    "default": "boxbackup server",
    "description": "Short description string for LSB header"
  },
  "name": {
    "type": "string",
    "default": "bbstored",
    "description": "Service name (used in PID file paths and init script name)"
  },
  "desc": {
    "type": "string",
    "default": "boxbackup-server",
    "description": "Long description string for logging"
  },
  "conf_path": {
    "type": "string",
    "default": "/etc/boxbackup/bbstored.conf",
    "description": "Path to main configuration file"
  },
  "default_pidfile_path": {
    "type": "string",
    "default": "/var/run/bbstored.pid",
    "description": "Fallback PID file path when not found in config"
  }
}
