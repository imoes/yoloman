{
  "base_path": {
    "type": "string",
    "default": "/icingaweb2",
    "description": "URL path prefix (e.g., '/icingaweb2') under which Icinga Web 2 is accessible. Must begin with '/' and no trailing slash."
  },
  "document_root": {
    "type": "string",
    "default": "/usr/share/icingaweb2/public",
    "description": "The filesystem path to the Icinga Web 2 public directory."
  },
  "config_dir": {
    "type": "string",
    "default": "/etc/icingaweb2",
    "description": "The filesystem path to the Icinga Web 2 configuration directory."
  }
}
