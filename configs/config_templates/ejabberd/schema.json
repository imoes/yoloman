{
  "ejabberd_binary_dir": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Directory where ejabberdctl binary resides"
  },
  "ejabberd_user": {
    "type": "string",
    "default": "ejabberd",
    "description": "System user under which ejabberd runs"
  },
  "run_directory": {
    "type": "string",
    "default": "/run/ejabberd",
    "description": "Runtime directory used by ejabberd for pid files and sockets"
  },
  "short_description": {
    "type": "string",
    "default": "Starts ejabberd XMPP server",
    "description": "One-line summary shown in init script descriptions"
  },
  "description_line1": {
    "type": "string",
    "default": "Starts ejabberd XMPP server, an XMPP",
    "description": "First line of multi-line description field"
  },
  "description_line2": {
    "type": "string",
    "default": "compliant server written in Erlang.",
    "description": "Second line of multi-line description field"
  },
  "chkconfig_description": {
    "type": "string",
    "default": "ejabberd XMPP server",
    "description": "Short description used in chkconfig headers"
  }
}
