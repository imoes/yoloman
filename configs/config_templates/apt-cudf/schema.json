{
  "solver_default": {
    "type": "string",
    "default": "mccs-cbc , mccs-lpsolve",
    "description": "Default solver specification for 'solver:' directive; admin may replace with '*' for automatic selection or other solver name(s)"
  }
}
