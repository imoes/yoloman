{
  "rwhod_interface": {
    "type": "string",
    "default": "",
    "description": "Network interface to bind rwhod to. Empty string or unset binds to all interfaces."
  },
  "rwhod_no_broadcast": {
    "type": "bool",
    "default": false,
    "description": "If true, disables broadcasting rwho status on the network."
  },
  "rwho_spool_dir": {
    "type": "string",
    "default": "/var/spool/rwho",
    "description": "Directory where rwhod stores status files for ruptime/rup to read."
  }
}
