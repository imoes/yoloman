{
  "block_size": {
    "type": "number",
    "default": 20,
    "description": "Block size as a multiple of 512 bytes (default 20 for 10kB)."
  },
  "clean_meta": {
    "type": "bool",
    "default": false,
    "description": "Replace shell meta characters in filenames with hyphens."
  },
  "clean_print": {
    "type": "bool",
    "default": false,
    "description": "Replace non-printable ASCII characters in filenames with hyphens."
  },
  "clean_space": {
    "type": "bool",
    "default": false,
    "description": "Replace white space characters in filenames with hyphens."
  },
  "clean": {
    "type": "bool",
    "default": false,
    "description": "Shortcut for clean_meta, clean_print, and clean_space."
  },
  "down_case": {
    "type": "bool",
    "default": false,
    "description": "Force file names to be all lower case."
  },
  "excludes": {
    "type": "list",
    "default": [],
    "description": "List of shell filename patterns to exclude from the archive. Items are strings representing the pattern."
  },
  "extract": {
    "type": "bool",
    "default": false,
    "description": "Extract the contents of the archive."
  },
  "group_name": {
    "type": "string",
    "default": "",
    "description": "Set the group name field in headers. Empty string to omit."
  },
  "group_number": {
    "type": "number",
    "default": 0,
    "description": "Set the group number field in headers. 0 to omit."
  },
  "input_format": {
    "type": "string",
    "default": "tar",
    "description": "Force input format (e.g., 'tar', 'cpio', 'ar'). Empty string to auto-detect.",
    "enum": [
      "tar",
      "ustar",
      "list",
      "directory",
      "cpio",
      "ar"
    ]
  },
  "list": {
    "type": "bool",
    "default": false,
    "description": "List file headers to standard error."
  },
  "mode_clear": {
    "type": "string",
    "default": "",
    "description": "Octal bits to clear in file modes (e.g., '0022')."
  },
  "mode_set": {
    "type": "string",
    "default": "",
    "description": "Octal bits to set in file modes (e.g., '0755')."
  },
  "no_directories": {
    "type": "bool",
    "default": false,
    "description": "Suppress directories from output."
  },
  "now": {
    "type": "bool",
    "default": false,
    "description": "Set last-modified time to current time for all files."
  },
  "old_type": {
    "type": "bool",
    "default": false,
    "description": "Keep old normal file type indicators unaltered."
  },
  "output_format": {
    "type": "string",
    "default": "",
    "description": "Force output format (e.g., 'tar', 'cpio', 'ar'). Empty string to match input.",
    "enum": [
      "ar",
      "posix",
      "cpio",
      "ustar",
      "v7"
    ]
  },
  "prefix": {
    "type": "string",
    "default": "",
    "description": "Prefix to add to filenames. Empty string to omit."
  },
  "progress": {
    "type": "bool",
    "default": false,
    "description": "Show progress indicator (requires -ifmt=list)."
  },
  "remove_prefixes": {
    "type": "list",
    "default": [],
    "description": "List of directory prefixes to remove from filenames. Items are strings."
  },
  "up_case": {
    "type": "bool",
    "default": false,
    "description": "Force file names to be all upper case."
  },
  "user_name": {
    "type": "string",
    "default": "",
    "description": "Set the user name field in headers. Empty string to omit."
  },
  "user_number": {
    "type": "number",
    "default": 0,
    "description": "Set the user number field in headers. 0 to omit."
  },
  "infile": {
    "type": "string",
    "default": "",
    "description": "Input file path. Use '-' for stdin."
  },
  "outfile": {
    "type": "string",
    "default": "",
    "description": "Output file path. Use '-' for stdout."
  }
}
