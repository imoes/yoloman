{
  "extensions_update_enabled": {
    "type": "bool",
    "default": true,
    "description": "If true, enables extension updates (overrides Firefox default behavior)."
  },
  "disable_openh264": {
    "type": "bool",
    "default": false,
    "description": "If true, disables the OpenH264 codec (used for WebRTC H.264 video)."
  },
  "disable_enhanced_newtab": {
    "type": "bool",
    "default": false,
    "description": "If true, disables enhanced (suggested) content on about:newtab (classic view only)."
  },
  "disable_healthreport_upload": {
    "type": "bool",
    "default": false,
    "description": "If true, disables uploading of health reports (crash data, usage metrics)."
  },
  "disable_urlbar_search_suggestions": {
    "type": "bool",
    "default": false,
    "description": "If true, disables search suggestions in the URL bar (including history/matching)."
  }
}
