# dirdiff configuration
# This file configures the command-line arguments passed to the dirdiff utility.
# Each option is placed on its own line.
# See the dirdiff documentation for available options (e.g., '-a', '--context 3').

{% for opt in options | default([], true) %}# Option: {{ opt }}
{{ opt }}
{% endfor %}