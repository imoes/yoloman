{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options to pass to dirdiff. Each item must be a string matching a documented option (e.g., '-a', '--context 3'). Options must be syntactically valid per dirdiff(1)."
  }
}
