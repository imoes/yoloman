{
  "output_format": {
    "type": "string",
    "default": "",
    "description": "Desired output format: json, xml, html, short, or businfo.",
    "enum": [
      "json",
      "xml",
      "html"
    ]
  },
  "dump_file": {
    "type": "string",
    "default": "",
    "description": "Path to a file to dump the collected hardware information into."
  },
  "classes": {
    "type": "list",
    "default": [],
    "description": "List of hardware classes to report on (e.g., cpu, memory, disk). If empty, all classes are reported unless restricted by other means."
  },
  "disabled_tests": {
    "type": "list",
    "default": [],
    "description": "List of detection tests to disable (e.g., dmi, pci, usb, ide)."
  },
  "enabled_tests": {
    "type": "list",
    "default": [],
    "description": "List of detection tests to explicitly enable."
  },
  "numeric": {
    "type": "bool",
    "default": false,
    "description": "Whether to display numeric IDs for PCI and USB devices."
  },
  "sanitize": {
    "type": "bool",
    "default": false,
    "description": "Whether to remove potentially sensitive information from output."
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Whether to suppress status output."
  },
  "notime": {
    "type": "bool",
    "default": false,
    "description": "Whether to exclude volatile attributes like timestamps."
  }
}
