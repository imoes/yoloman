{
  "PROJECT_NAME": {
    "type": "string",
    "default": "neutron",
    "description": "The OpenStack project name used for paths and user/group names"
  },
  "NAME": {
    "type": "string",
    "default": "networking-bagpipe-bgp-agent",
    "description": "The service name/identifier used for scripts, logs, and pids"
  }
}
