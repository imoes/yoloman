{
  "package_name": {
    "type": "string",
    "default": "tinydns",
    "description": "The name of the package as provided in the init script header."
  },
  "name": {
    "type": "string",
    "default": "tinydns",
    "description": "The service name used for the NAME variable, daemon path, and service directory."
  }
}
