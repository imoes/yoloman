{
  "package_name": {
    "type": "string",
    "default": "ferret-vis",
    "description": "Name of the Debian package (used in messages and fallback logic)"
  },
  "emacs_site_lisp_path": {
    "type": "string",
    "default": "/usr/share/emacs/site-lisp",
    "description": "Path where Emacs site-lisp files are installed"
  }
}
