{
  "boot_time": {
    "type": "string",
    "default": "07:30",
    "description": "Time of day (in HH:MM format) used as cutoff for deciding whether to pre-load."
  },
  "random_sleep_range": {
    "type": "number",
    "default": 200,
    "description": "Upper bound (exclusive) for random sleep interval in seconds."
  },
  "min_sleep": {
    "type": "number",
    "default": 61,
    "description": "Minimum sleep duration (in seconds) before desktop-autoloader runs."
  }
}
