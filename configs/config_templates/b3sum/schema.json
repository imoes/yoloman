{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options/flags to pass to b3sum, each as a single string (e.g., '--raw', '-l', '32')"
  },
  "files": {
    "type": "list",
    "default": [],
    "description": "List of input files to hash or check, or '-' for stdin"
  }
}
