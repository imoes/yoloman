{
  "desktop_file": {
    "type": "string",
    "default": "privacybrowser",
    "description": "The base filename for the desktop entry (used by --desktopfile option)"
  },
  "hidden": {
    "type": "bool",
    "default": false,
    "description": "If true, the application will not appear in the standard application menus"
  }
}
