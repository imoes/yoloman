# Razor is a distributed spam detection system.
# This is the default configuration file for Razor.
# It is typically very minimal as most settings are controlled
# by environment variables or default runtime behavior.

# LogDebugLevel: Razor debug/log level. 0=errors only, 3=verbose debug.
LogDebugLevel={{ LogDebugLevel | default(2) }}