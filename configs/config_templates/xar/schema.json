{
 "action": {
  "default": "c",
  "description": "The action to perform: create (-c), list (-t), or extract (-x).",
  "type": "string"
 },
 "coalesce_heap": {
  "default": false,
  "description": "When multiple files are identical, only store one copy of data (--coalesce-heap).",
  "type": "bool"
 },
 "compression": {
  "default": "gzip",
  "description": "Specifies the compression type: none, gzip, bzip2, lzma.",
  "enum": [
   "none",
   "gzip",
   "bzip2",
   "lzma"
  ],
  "type": "string"
 },
 "compression_level": {
  "default": 6,
  "description": "Compression level integer between 0 and 9 for gzip, bzip2, or lzma.",
  "type": "number"
 },
 "distribution_safe": {
  "default": false,
  "description": "Creates an archive to only contain file properties safe for file distribution (--distribution).",
  "type": "bool"
 },
 "exclude_patterns": {
  "default": [],
  "description": "POSIX regex patterns of files to exclude from adding or extracting. Uses items field for pattern details if needed, but here flat list of strings is sufficient for shell args.",
  "type": "list"
 },
 "ext_dir": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "extract_dir": {
  "default": "/tmp/xar_extract",
  "description": "On extract, xar will chdir to the specified path before extracting.",
  "type": "string"
 },
 "file_cksum": {
  "default": "sha256",
  "description": "Hashing algorithm for file content verification: md5, sha1, sha256, sha512.",
  "enum": [
   "md5",
   "sha1",
   "sha256",
   "sha512"
  ],
  "type": "string"
 },
 "filename": {
  "default": "archive.xar",
  "description": "The filename to use for creation, listing, or extraction.",
  "type": "string"
 },
 "keep_existing": {
  "default": false,
  "description": "Does not overwrite existing files during extraction (-k).",
  "type": "bool"
 },
 "keep_setuid": {
  "default": false,
  "description": "Preserve setuid/setgid bits even if uid/gid differs (--keep-setuid).",
  "type": "bool"
 },
 "link_same": {
  "default": false,
  "description": "When data sections are identical, hardlink them within the archive (--link-same).",
  "type": "bool"
 },
 "no_compress_patterns": {
  "default": [],
  "description": "POSIX regex patterns of files to archive but not compress.",
  "type": "list"
 },
 "prop_exclude": {
  "default": [],
  "description": "List of file properties to exclude from the archive (--prop-exclude).",
  "type": "list"
 },
 "prop_include": {
  "default": [
   "name",
   "type",
   "mode",
   "data"
  ],
  "description": "List of file properties to include in the archive (--prop-include). Only these properties will be included.",
  "type": "list"
 },
 "rsize": {
  "default": 1048576,
  "description": "Size in bytes for the internal libxar read buffer.",
  "type": "number"
 },
 "set_ownership_symbolic": {
  "default": false,
  "description": "On extract, set ownership based on symbolic names (-p).",
  "type": "bool"
 },
 "set_ownership_uid": {
  "default": false,
  "description": "On extract, set ownership based on uid/gid (-P).",
  "type": "bool"
 },
 "stay_local": {
  "default": false,
  "description": "On archival, stay on the local device (-l).",
  "type": "bool"
 },
 "toc_cksum": {
  "default": "sha256",
  "description": "Hashing algorithm for xml header verification: md5, sha1, sha256, sha512.",
  "enum": [
   "md5",
   "sha1",
   "sha256",
   "sha512"
  ],
  "type": "string"
 },
 "verbose": {
  "default": false,
  "description": "Verbose output (-v).",
  "type": "bool"
 }
}
