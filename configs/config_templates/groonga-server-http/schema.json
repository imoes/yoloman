{
  "groonga_server_http_address": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "The IP address the groonga HTTP server binds to."
  },
  "groonga_server_http_port": {
    "type": "number",
    "default": 10043,
    "description": "The port the groonga HTTP server listens on."
  },
  "groonga_server_http_database": {
    "type": "string",
    "default": "/var/lib/groonga/db/db",
    "description": "Path to the Groonga database file."
  },
  "groonga_server_http_log_path": {
    "type": "string",
    "default": "/var/log/groonga/groonga-http.log",
    "description": "Path to the Groonga HTTP server log file."
  },
  "groonga_server_http_pid_path": {
    "type": "string",
    "default": "/run/groonga/groonga-http.pid",
    "description": "Path to the PID file for the Groonga HTTP server process."
  },
  "groonga_server_http_query_log_path": {
    "type": "string",
    "default": "/var/log/groonga/query-http.log",
    "description": "Path to the query log file."
  },
  "groonga_server_http_grn_in_values_too_many_index_match_ratio": {
    "type": "number",
    "default": 0.01,
    "description": "Threshold ratio for forcing sequential search in `in_values()`."
  },
  "groonga_server_http_grn_between_too_many_index_match_ratio": {
    "type": "number",
    "default": 0.01,
    "description": "Threshold ratio for forcing sequential search in `between()`."
  },
  "groonga_server_http_grn_mecab_chunked_tokenize_enabled": {
    "type": "string",
    "default": "no",
    "description": "Set to 'yes' to enable chunked tokenize feature (meCab).\", \n      \"enum\": [\n        \"yes\",\n        \"no\"\n      ]\n    },\n    \"groonga_server_http_grn_mecab_chunk_size_threshold\": {\n      \"type\": \"number\",\n      \"default\": 8192,\n      \"description\": \"Threshold value for chunk size in chunked tokenize feature.\"\n    }\n  },",
    "enum": [
      "yes",
      "no"
    ]
  }
}
