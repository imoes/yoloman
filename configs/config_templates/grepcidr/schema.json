{
  "patterns": {
    "type": "list",
    "default": [],
    "description": "List of IPv4 or IPv6 patterns (CIDR, single IP, or range). One per line in output config file. Blank lines and # comments are ignored by grepcidr at runtime but not by this template."
  }
}
