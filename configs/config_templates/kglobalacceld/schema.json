{
  "exec_path": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu/libexec/kglobalacceld",
    "description": "The full path to the kglobalacceld executable binary."
  },
  "node_display": {
    "type": "bool",
    "default": true,
    "description": "If true, the application will not be shown in application menus."
  },
  "only_show_in": {
    "type": "list",
    "default": [
      "KDE"
    ],
    "description": "A semicolon-separated list of desktop environments where this application should appear."
  },
  "x_systemd_skip": {
    "type": "bool",
    "default": true,
    "description": "If true, systemd will skip this entry."
  }
}
