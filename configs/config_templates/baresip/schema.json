{
  "audio.audec": {
    "description": "Audio decoder module (e.g. opus, g711).",
    "enum": [
      "opus",
      "g722",
      "g726"
    ],
    "type": "string",
    "enum_open": true
  },
  "audio.auenc": {
    "description": "Audio encoder module (e.g. opus, g711).",
    "enum": [
      "opus",
      "g722",
      "g726"
    ],
    "type": "string",
    "enum_open": true
  },
  "audio.auplay": {
    "default": "alsa",
    "description": "Audio player module (e.g. alsa, pulse).",
    "enum": [
      "alsa",
      "coreaudio",
      "wasapi"
    ],
    "type": "string",
    "enum_open": true
  },
  "audio.ausrc": {
    "default": "alsa",
    "description": "Audio source module (e.g. alsa, pulse).",
    "enum": [
      "alsa",
      "coreaudio",
      "wasapi",
      "v4l2"
    ],
    "type": "string",
    "enum_open": true
  },
  "audio.channels": {
    "default": 0,
    "description": "Audio channels (0=auto).",
    "type": "number"
  },
  "audio.encfmt": {
    "default": "s16",
    "description": "Encoding audio format.",
    "enum": [
      "s16",
      "float",
      "s24_3le"
    ],
    "type": "string"
  },
  "audio.playfmt": {
    "default": "s16",
    "description": "Playback audio format.",
    "enum": [
      "s16",
      "float",
      "s24_3le"
    ],
    "type": "string"
  },
  "audio.srate": {
    "default": 0,
    "description": "Audio sample rate (0=auto).",
    "type": "number"
  },
  "audio.srcfmt": {
    "default": "s16",
    "description": "Source audio format (e.g. s16, float).",
    "enum": [
      "s16",
      "float",
      "s24_3le"
    ],
    "type": "string",
    "enum_open": true
  },
  "audiopath": {
    "default": "",
    "description": "Directory containing audio prompts (DTMF tones, ringback, etc.).",
    "type": "string"
  },
  "interface": {
    "default": "",
    "description": "Network interface name to bind to (e.g. eth0). Auto-detects if empty.",
    "type": "string"
  },
  "log_level": {
    "default": 3,
    "description": "Logging verbosity level (0=none, 1=crit, 2=warn, 3=info, 4=debug).",
    "type": "number"
  },
  "net_if.ipv4": {
    "default": "",
    "description": "Force IPv4 (true/false).",
    "type": "string"
  },
  "net_if.ipv6": {
    "default": "",
    "description": "Force IPv6 (true/false).",
    "type": "string"
  },
  "net_if.use": {
    "default": "",
    "description": "Interface to bind for SIP traffic.",
    "type": "string"
  },
  "ua.aor": {
    "default": "",
    "description": "SIP Address-of-Record (e.g. sip:user@example.com).",
    "type": "string"
  },
  "ua.cuser": {
    "default": "",
    "description": "SIP username (cuser) used in authorization.",
    "type": "string"
  },
  "ua.dispname": {
    "default": "",
    "description": "Display name for outbound calls.",
    "type": "string"
  },
  "ua.expires": {
    "default": 3600,
    "description": "Registration expiration time in seconds.",
    "type": "number"
  },
  "ua.regint": {
    "default": 0,
    "description": "Registration interval in seconds (0 disables registration).",
    "type": "number"
  },
  "video.bitrate": {
    "default": 0,
    "description": "Video encoder bitrate in bps.",
    "type": "number"
  },
  "video.disp": {
    "default": "",
    "description": "Video display module (e.g. x11, fb).",
    "type": "string"
  },
  "video.fps": {
    "default": 0.0,
    "description": "Video frames per second.",
    "type": "number"
  },
  "video.size": {
    "default": "",
    "description": "Video resolution (e.g. 640x480).",
    "type": "string"
  },
  "video.src": {
    "default": "v4l2",
    "description": "Video source module (e.g. v4l2).",
    "enum": [
      "v4l2",
      "avcapture",
      "x11"
    ],
    "type": "string",
    "enum_open": true
  }
}
