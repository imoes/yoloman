{
  "declaration_file": {
    "type": "string",
    "default": "/home/user/irpf2024.pdf",
    "description": "Path to the declaration (report) file to be sent to Receita Federal. Corresponds to the -d/--declaration option."
  },
  "output_dir": {
    "type": "string",
    "default": "/home/user/receipts",
    "description": "Path to the directory where the receipt will be saved. Corresponds to the -o/--output-dir option. If empty or not specified, the receipt is saved in the current working directory."
  }
}
