{
  "epicrc_path": {
    "type": "string",
    "default": "~/.epicrc",
    "description": "Path to the epic4 user configuration file (~/.epicrc)"
  },
  "ircrc_path": {
    "type": "string",
    "default": "~/.ircrc",
    "description": "Path to the generic IRC user configuration file (~/.ircrc)"
  },
  "alternatives_path": {
    "type": "string",
    "default": "/etc/alternatives/epic4-default.irc",
    "description": "Path to the alternatives symlink for the default epic4 script"
  }
}
