{
  "force_corefiles": {
    "type": "string",
    "default": "",
    "description": "If set to 'yes', core files will be enabled for OVS processes."
  },
  "ovs_ctl_opts": {
    "type": "string",
    "default": "",
    "description": "Extra options to pass to ovs-ctl, e.g. for wrapping daemons with valgrind."
  }
}
