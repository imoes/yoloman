{
 "allowed_hosts": {
  "default": [
   "127.0.0.1",
   "::1"
  ],
  "description": "List of allowed host IPs or CIDR ranges (regex patterns, escaped with ^ and $) that may connect to this node.",
  "items": {
   "description": "Allowed host pattern (e.g., 192.168.1.0/24, ^10\\.0\\.0\\.[0-9]+$)",
   "type": "string"
  },
  "type": "list"
 },
 "dirdir": {
  "default": "/etc/munin/plugin-conf.d",
  "description": "Directory containing plugin configuration snippets (one per file).",
  "type": "string"
 },
 "group": {
  "default": "root",
  "description": "System group under which the munin-node daemon runs. Typically matches the user group (e.g., 'munin').",
  "type": "string"
 },
 "htmldir": {
  "default": "/var/www/munin",
  "description": "Directory for HTML output (used when serving munin stats directly via webserver).",
  "type": "string"
 },
 "log_level": {
  "default": 1,
  "description": "Verbosity of logging: 0 = minimal, 1 = normal (recommended), higher = verbose (for debugging).",
  "type": "number"
 },
 "logdir": {
  "default": "/var/log/munin",
  "description": "Directory where munin-node logs are stored.",
  "type": "string"
 },
 "max_connections": {
  "default": 30,
  "description": "Maximum number of simultaneous client connections allowed.",
  "type": "number"
 },
 "node_hostname": {
  "default": "",
  "description": "Explicit hostname to report to the munin master. If empty, the FQDN is auto-detected and used.",
  "type": "string"
 },
 "plugin_conf_includes": {
  "default": [
   "/etc/munin/plugin-conf.d"
  ],
  "description": "List of directories to scan for plugin configuration fragments (includedir statements).",
  "items": {
   "description": "Path to plugin configuration directory.",
   "type": "string"
  },
  "type": "list"
 },
 "port": {
  "default": 4949,
  "description": "TCP port on which the munin-node daemon listens for incoming connections.",
  "type": "number"
 },
 "rundir": {
  "default": "/var/run/munin",
  "description": "Directory for runtime files like PID and socket files.",
  "type": "string"
 },
 "strict_host_verification": {
  "default": false,
  "description": "Whether to strictly match client IP against allowed_hosts (true) or allow relaxed matching (false, legacy behavior).",
  "type": "number"
 },
 "timeout": {
  "default": 3600,
  "description": "Connection timeout in seconds before closing idle connections.",
  "type": "number"
 },
 "user": {
  "default": "root",
  "description": "System user account under which the munin-node daemon runs. For security, consider using a non-root user (e.g., 'munin').",
  "type": "string"
 }
}
