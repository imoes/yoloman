{
  "bcb_partition": {
    "type": "string",
    "default": "/dev/block/mmcblk0p30",
    "description": "The path of the misc partition device (BCB partition)."
  },
  "command": {
    "type": "string",
    "default": "dump",
    "description": "The command to execute: read, write, clear, or dump.",
    "enum": [
      "read",
      "write",
      "clear",
      "dump"
    ]
  },
  "field": {
    "type": "string",
    "default": "command",
    "description": "The field to operate on (command, status, recovery, or stage).",
    "enum": [
      "command",
      "status",
      "recovery",
      "stage"
    ]
  },
  "data": {
    "type": "string",
    "default": "",
    "description": "The data to write to the BCB's field. Empty if not writing."
  }
}
