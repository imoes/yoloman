{
  "ch_pattern": {
    "type": "string",
    "default": "chmod|chown|chgrp|chcon|chattr",
    "description": "Regex pattern to detect chmod/chown/chgrp/chcon/chattr system calls"
  },
  "exec_pattern": {
    "type": "string",
    "default": "\\w*(?<!dalvik.)system|exec[a-z][a-z]|fork",
    "description": "Regex pattern to detect process execution attempts"
  },
  "exec_description": {
    "type": "string",
    "default": "Tries to execute a process",
    "description": "Human-readable description for the exec detection"
  },
  "shell_pattern": {
    "type": "string",
    "default": "/system/bin/sh",
    "description": "Regex pattern to detect shell execution"
  },
  "mounts_pattern": {
    "type": "string",
    "default": "/proc/mounts|mount",
    "description": "Regex pattern to detect mount operations"
  },
  "mounts_description": {
    "type": "string",
    "default": "Mounting or list mounts - used in several exploits",
    "description": "Human-readable description for the mounts detection"
  },
  "geteuid_pattern": {
    "type": "string",
    "default": "geteuid",
    "description": "Regex pattern to detect geteuid calls"
  },
  "adb_pattern": {
    "type": "string",
    "default": "sbin/adb",
    "description": "Regex pattern to detect ADB usage"
  },
  "anti_frida_pattern": {
    "type": "string",
    "default": "gum-js-loop|frida-helper|frida-server|frida-agent|re.frida.server|linjector|gdbus|frida-gadget|pool-frida|frida-main-loop|frida_agent_main|pool-spawner|DetectFrida",
    "description": "Regex pattern to detect Frida anti-analysis"
  },
  "anti_frida_description": {
    "type": "string",
    "default": "Anti runtime analysis - Detect frida framework",
    "description": "Human-readable description for Frida detection"
  },
  "pm_install_pattern": {
    "type": "string",
    "default": "pm install",
    "description": "Regex pattern to detect package installation via pm"
  },
  "pm_list_pattern": {
    "type": "string",
    "default": "pm list",
    "description": "Regex pattern to detect package listing via pm"
  },
  "am_broadcast_pattern": {
    "type": "string",
    "default": "am broadcast",
    "description": "Regex pattern to detect broadcast intents via am"
  },
  "am_start_pattern": {
    "type": "string",
    "default": "am start",
    "description": "Regex pattern to detect activity start via am"
  },
  "kill_pattern": {
    "type": "string",
    "default": "kill",
    "description": "Regex pattern to detect process termination"
  },
  "ptrace_pattern": {
    "type": "string",
    "default": "ptrace",
    "description": "Regex pattern to detect ptrace usage"
  },
  "proc_version_pattern": {
    "type": "string",
    "default": "/proc/version",
    "description": "Regex pattern to detect /proc/version access"
  },
  "proc_version_description": {
    "type": "string",
    "default": "Gets description of device",
    "description": "Human-readable description for /proc/version detection"
  },
  "possible_exploit_pattern": {
    "type": "string",
    "default": "root exploit|rootshell|spray|privileges escalated|/proc/kallsyms|0wned",
    "description": "Regex pattern to detect common exploit keywords"
  },
  "ragecage_pattern": {
    "type": "string",
    "default": "CVE-2010-EASY",
    "description": "Regex pattern to detect Rage Against The Cage exploit"
  },
  "ragecage_description": {
    "type": "string",
    "default": "Exploit CVE-2010-EASY rage against the cage spotted",
    "description": "Human-readable description for Rage Against The Cage detection"
  },
  "exploid_pattern": {
    "type": "string",
    "default": "exploid|shakalaca",
    "description": "Regex pattern to detect Exploid/Shakalaca exploit"
  },
  "exploid_description": {
    "type": "string",
    "default": "Exploit CVE-2009-1185 spotted",
    "description": "Human-readable description for Exploid/Shakalaca detection"
  },
  "zerg_pattern": {
    "type": "string",
    "default": "Zerg rush|zerglings|speedlings",
    "description": "Regex pattern to detect Zerg Rush exploit"
  },
  "zerg_description": {
    "type": "string",
    "default": "Exploit CVE-2011-3874 (zergRush) spotted",
    "description": "Human-readable description for Zerg Rush detection"
  },
  "levitator_pattern": {
    "type": "string",
    "default": "clobbering kmem with poisoned pointers",
    "description": "Regex pattern to detect Levitator exploit"
  },
  "levitator_description": {
    "type": "string",
    "default": "Exploit CVE-2011-1350 (levitator) spotted",
    "description": "Human-readable description for Levitator detection"
  },
  "mempodroid_pattern": {
    "type": "string",
    "default": "Mempodipper|Calculating su padding|Opening parent mem",
    "description": "Regex pattern to detect Mempodroid exploit"
  },
  "mempodroid_description": {
    "type": "string",
    "default": "Exploit CVE-2012-0025 (mempodroid) spotted",
    "description": "Human-readable description for Mempodroid detection"
  },
  "towelroot_pattern": {
    "type": "string",
    "default": "towelroot|rootTheShit",
    "description": "Regex pattern to detect Towelroot exploit"
  },
  "supersu_pattern": {
    "type": "string",
    "default": "/system/xbin/daemonsu|/system/xbin/sugote",
    "description": "Regex pattern to detect SuperSU binaries"
  },
  "dalvikvm_pattern": {
    "type": "string",
    "default": "dalvikvm",
    "description": "Regex pattern to detect dalvikvm usage"
  },
  "dalvikvm_description": {
    "type": "string",
    "default": "Possible way to loads Zips containing DEXs",
    "description": "Human-readable description for dalvikvm detection"
  },
  "dexclassloader_pattern": {
    "type": "string",
    "default": "DexClassLoader",
    "description": "Regex pattern to detect DexClassLoader usage"
  },
  "dexclassloader_description": {
    "type": "string",
    "default": "Using DexClassLoader through a native library",
    "description": "Human-readable description for DexClassLoader detection"
  },
  "loadclass_pattern": {
    "type": "string",
    "default": "loadClass",
    "description": "Regex pattern to detect loadClass usage"
  },
  "loadclass_description": {
    "type": "string",
    "default": "Using loadClass through native library",
    "description": "Human-readable description for loadClass detection"
  },
  "url_in_exec_pattern": {
    "type": "string",
    "default": "http://[^ \"\\t]*",
    "description": "Regex pattern to detect URLs in executable context"
  },
  "url_in_exec_description": {
    "type": "string",
    "default": "URL in executable",
    "description": "Human-readable description for URL in exec detection"
  },
  "mtk_su_description": {
    "type": "string",
    "default": "MTK-SU CVE-2020-0069 root on MediaTek",
    "description": "Human-readable description for MTK-SU detection"
  },
  "mtk_su_pattern": {
    "type": "string",
    "default": "Temporary root by diplomatic@XDA|amazing-temp-root-mediatek-armv8",
    "description": "Regex pattern to detect MTK-SU exploit"
  }
}
