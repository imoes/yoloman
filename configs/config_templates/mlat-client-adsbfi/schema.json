{
  "START_CLIENT": {
    "type": "string",
    "default": "no",
    "description": "Whether to start the mlat client (yes/no)"
  },
  "RUN_AS_USER": {
    "type": "string",
    "default": "missing-RUN_AS_USER-setting-in-config",
    "description": "User to run the mlat client daemon as"
  },
  "SERVER_USER": {
    "type": "string",
    "default": "",
    "description": "ADS-B server user identifier"
  },
  "LAT": {
    "type": "number",
    "default": "",
    "description": "Latitude of the receiver station"
  },
  "LON": {
    "type": "number",
    "default": "",
    "description": "Longitude of the receiver station"
  },
  "ALT": {
    "type": "number",
    "default": "",
    "description": "Altitude of the receiver station in meters"
  },
  "INPUT_TYPE": {
    "type": "string",
    "default": "dump1090",
    "description": "Type of input receiver (e.g., dump1090, readsb)",
    "enum": [
      "dump1090",
      "flightaware"
    ]
  },
  "INPUT_HOSTPORT": {
    "type": "string",
    "default": "",
    "description": "Host and port of the input receiver (e.g., 127.0.0.1:30005)"
  },
  "SERVER_HOSTPORT": {
    "type": "string",
    "default": "",
    "description": "Host and port of the MLAT server (e.g., mlat.adsb.fi:31001)"
  },
  "RESULTS": {
    "type": "list",
    "default": [],
    "description": "List of result servers to connect to (e.g., mlat.adsb.fi:31001)"
  },
  "EXTRA_ARGS": {
    "type": "string",
    "default": "",
    "description": "Additional command line arguments for the mlat client"
  },
  "LOGFILE": {
    "type": "string",
    "default": "/var/log/mlat-client-adsbfi.log",
    "description": "Log file path for the daemon output"
  }
}
