{
  "plugin_load_add": {
    "type": "string",
    "default": "provider_bzip2",
    "description": "Name of the plugin to load."
  },
  "provider_bzip2": {
    "type": "string",
    "default": "force_plus_permanent",
    "description": "Loading behavior for the bzip2 provider.",
    "enum": [
      "force_plus_permanent",
      "permanent"
    ]
  }
}
