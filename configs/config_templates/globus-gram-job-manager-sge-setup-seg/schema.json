{
  "sbin_path": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Path to the system binary directory"
  },
  "daemon_binary": {
    "type": "string",
    "default": "globus-job-manager",
    "description": "Name of the Globus job manager daemon binary"
  },
  "config_file_path": {
    "type": "string",
    "default": "/etc/globus/globus-gram-job-manager.conf",
    "description": "Path to the Globus GRAM job manager configuration file"
  },
  "job_manager_type": {
    "type": "string",
    "default": "sge",
    "description": "Type of job manager backend (e.g., sge, pbs, lsf)"
  },
  "seg_module": {
    "type": "string",
    "default": "sge",
    "description": "Name of the SEG (Simple Execution Gateway) module to use"
  }
}
