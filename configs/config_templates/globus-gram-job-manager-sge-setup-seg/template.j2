# Globus GRAM Job Manager SGE Setup Seg Configuration
# Generated from schema for package: globus-gram-job-manager-sge-setup-seg
# This template generates a command line configuration for the setup seg module.

# Path to the system binary directory [default: /usr/sbin]
# Name of the Globus job manager daemon binary [default: globus-job-manager]
# Path to the Globus GRAM job manager configuration file [default: /etc/globus/globus-gram-job-manager.conf]
# Type of job manager backend (e.g., sge, pbs, lsf) [default: sge]
# Name of the SEG (Simple Execution Gateway) module to use [default: sge]

# Command line to execute for the setup seg module.
# Format: <log_target>,<credential_type> <path>/<binary> <binary> -conf <config> -type <type> -seg-module <module>
{{ 'stderr_log,local_cred' }} {{ (sbin_path | default('/usr/sbin')) }}/{{ (daemon_binary | default('globus-job-manager')) }} {{ (daemon_binary | default('globus-job-manager')) }} -conf {{ (config_file_path | default('/etc/globus/globus-gram-job-manager.conf')) }} -type {{ (job_manager_type | default('sge')) }} -seg-module {{ (seg_module | default('sge')) }}