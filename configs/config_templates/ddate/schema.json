{
  "format": {
    "type": "string",
    "default": "%A, %B %d, %Y YOLD",
    "description": "Format string for the Discordian date output. Uses ddate-specific specifiers like %A, %B, %d, %Y, %{%}, %N, %H, etc. See ddate(1) man page for details."
  }
}
