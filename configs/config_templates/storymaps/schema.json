{
  "theme": {
    "type": "string",
    "default": "classic",
    "description": "The default visual theme applied to story cards."
  },
  "app_title": {
    "type": "string",
    "default": "My Story Maps",
    "description": "The title displayed in the application window toolbar."
  },
  "auto_save": {
    "type": "bool",
    "default": true,
    "description": "Whether to automatically save the story map as the user works."
  },
  "stories_dir": {
    "type": "string",
    "default": "/home/user/StoryMaps",
    "description": "The directory path where story maps are automatically saved."
  },
  "language": {
    "type": "string",
    "default": "en",
    "description": "The default language code for the interface and story cards."
  },
  "themes": {
    "type": "list",
    "default": [
      "classic",
      "modern",
      "simple"
    ],
    "description": "List of theme identifiers available for selection."
  }
}
