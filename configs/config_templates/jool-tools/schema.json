{
  "module_name": {
    "type": "string",
    "default": "jool",
    "description": "The name of the kernel module and binary for Jool."
  },
  "config_file": {
    "type": "string",
    "default": "/etc/jool/jool.conf",
    "description": "The path to the Jool configuration file."
  }
}
