{
  "skiplist": {
    "type": "list",
    "default": [],
    "description": "List of packages to exclude completely from dependency graphs (unless --no-skip or --show-all is used). Common examples: libc6, zlib1g. Each item is a string package name."
  },
  "endlist": {
    "type": "list",
    "default": [],
    "description": "List of packages to show in the graph without their dependencies (rendered with diamond shape). Each item is a string package name."
  }
}
