{
  "shell": {
    "type": "string",
    "default": "default",
    "description": "Shell to use for executing benchmarked commands (e.g., bash, zsh, none, default)",
    "enum": [
      "default",
      "none"
    ]
  },
  "warmup": {
    "type": "number",
    "default": 0,
    "description": "Number of warmup runs before the actual benchmark"
  },
  "min_runs": {
    "type": "number",
    "default": 10,
    "description": "Minimum number of runs for each command"
  },
  "max_runs": {
    "type": "number",
    "default": 0,
    "description": "Maximum number of runs for each command. 0 means no limit."
  },
  "runs": {
    "type": "number",
    "default": 0,
    "description": "Exact number of runs for each command. 0 means auto-determine."
  },
  "setup": {
    "type": "string",
    "default": "",
    "description": "Command to execute before each set of timing runs"
  },
  "reference": {
    "type": "string",
    "default": "",
    "description": "Reference command for relative comparison of results"
  },
  "prepare": {
    "type": "string",
    "default": "",
    "description": "Command to execute before each timing run"
  },
  "conclude": {
    "type": "string",
    "default": "",
    "description": "Command to execute after each timing run"
  },
  "cleanup": {
    "type": "string",
    "default": "",
    "description": "Command to execute after completion of all benchmarking runs for each command"
  },
  "ignore_failure": {
    "type": "bool",
    "default": false,
    "description": "Ignore non-zero exit codes of the benchmarked programs"
  },
  "style": {
    "type": "string",
    "default": "auto",
    "description": "Output style type (auto, basic, full, nocolor, color, none)",
    "enum": [
      "auto",
      "basic",
      "full",
      "nocolor",
      "color",
      "none"
    ]
  },
  "sort": {
    "type": "string",
    "default": "auto",
    "description": "Sort order of speed comparison summary (auto, command, mean-time)",
    "enum": [
      "auto",
      "command",
      "mean-time"
    ]
  },
  "time_unit": {
    "type": "string",
    "default": "second",
    "description": "Time unit to be used (microsecond, millisecond, second)",
    "enum": [
      "microsecond",
      "millisecond",
      "second"
    ]
  },
  "export_asciidoc": {
    "type": "string",
    "default": "",
    "description": "File path to export timing summary as AsciiDoc table"
  },
  "export_csv": {
    "type": "string",
    "default": "",
    "description": "File path to export timing summary as CSV"
  },
  "export_json": {
    "type": "string",
    "default": "",
    "description": "File path to export timing summary and individual run timings as JSON"
  },
  "export_markdown": {
    "type": "string",
    "default": "",
    "description": "File path to export timing summary as Markdown table"
  },
  "export_orgmode": {
    "type": "string",
    "default": "",
    "description": "File path to export timing summary as Emacs org-mode table"
  },
  "show_output": {
    "type": "bool",
    "default": false,
    "description": "Print stdout and stderr of the benchmark"
  },
  "output": {
    "type": "string",
    "default": "null",
    "description": "Where output is redirected (null, pipe, inherit, or file path)",
    "enum": [
      "null",
      "pipe",
      "inherit"
    ]
  },
  "input": {
    "type": "string",
    "default": "null",
    "description": "Where input comes from (null or file path)"
  },
  "command_names": {
    "type": "list",
    "default": [],
    "description": "List of meaningful names for each benchmark command (must align with commands order if provided)"
  },
  "commands": {
    "type": "list",
    "default": [],
    "description": "List of benchmark commands to execute"
  },
  "parameter_scan": {
    "type": "list",
    "default": [],
    "description": "List of parameter scan configurations for variable ranges"
  },
  "parameter_list": {
    "type": "list",
    "default": [],
    "description": "List of parameter list configurations for comma-separated values"
  }
}
