{
  "run_kamailio": {
    "type": "string",
    "default": "no",
    "description": "Enable kamailio startup. Accepts 'yes' or 'no' (lowercase).",
    "enum": [
      "yes",
      "no"
    ]
  },
  "user": {
    "type": "string",
    "default": "kamailio",
    "description": "System user to run Kamailio as"
  },
  "group": {
    "type": "string",
    "default": "kamailio",
    "description": "System group to run Kamailio as"
  },
  "shm_memory": {
    "type": "number",
    "default": 64,
    "description": "Amount of shared memory (in MB) allocated for Kamailio"
  },
  "pkg_memory": {
    "type": "number",
    "default": 8,
    "description": "Amount of private memory (in MB) allocated for Kamailio"
  },
  "cfgfile": {
    "type": "string",
    "default": "/etc/kamailio/kamailio.cfg",
    "description": "Path to main Kamailio configuration file"
  },
  "dump_core": {
    "type": "string",
    "default": "no",
    "description": "Enable core file generation on crash. Accepts 'yes' or 'no' (lowercase).",
    "enum": [
      "yes",
      "no"
    ]
  }
}
