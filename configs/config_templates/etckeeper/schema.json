{
  "avoid_daily_autocommits": {
    "type": "string",
    "default": "",
    "description": "If set to \"1\", skips running /etc/etckeeper/daily automatically (e.g., by cron). Default is empty string (i.e., autocommits are enabled)."
  }
}
