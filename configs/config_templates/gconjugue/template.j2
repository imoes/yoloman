{# gconjugue configuration file #}
# gconjugue is a conjugator of Brazilian Portuguese verbs.
# This file defines runtime options for the gconjugue CLI tool.
# No persistent configuration file format is documented for gconjugue.
# Per its upstream behavior, gconjugue operates entirely from command-line arguments.
# Therefore, this template generates a no-op placeholder for compliance.
# (No runtime config file exists; this is a stub to satisfy template requirements.)

# Placeholder directive ensuring the template is not empty.
# This field exists solely to satisfy Ansible/Jinja validation requirements
# and has no functional impact on the gconjugue software.
# Source: schema field 'placeholder' (default: True)
{{ placeholder | default(True) | ternary('# Placeholder enabled', '') }}