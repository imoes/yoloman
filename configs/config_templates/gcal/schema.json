{
  "default_year": {
    "type": "number",
    "default": 0,
    "description": "Default year for calendar output (0 = current year)"
  },
  "default_month": {
    "type": "number",
    "default": 0,
    "description": "Default month for calendar output (0 = current month)"
  },
  "show_holidays": {
    "type": "bool",
    "default": true,
    "description": "Enable or disable built-in holiday list display"
  },
  "holiday_country": {
    "type": "string",
    "default": "DE",
    "description": "ISO 3166-1 alpha-2 country code for holiday list"
  },
  "output_format": {
    "type": "string",
    "default": "standard",
    "description": "Output format: 'standard', 'html', 'ical', 'icalendar'",
    "enum": [
      "standard",
      "html",
      "ical",
      "icalendar"
    ]
  },
  "julian_first": {
    "type": "bool",
    "default": true,
    "description": "Use Julian calendar before Gregorian transition date"
  },
  "gregorian_first": {
    "type": "bool",
    "default": true,
    "description": "Use Gregorian calendar after Julian transition date"
  },
  "week_start": {
    "type": "string",
    "default": "sunday",
    "description": "First day of week: 'monday' or 'sunday'",
    "enum": [
      "monday",
      "sunday"
    ]
  },
  "language": {
    "type": "string",
    "default": "en",
    "description": "Language code (ISO 639-1) for holiday and weekday names"
  },
  "extra_options": {
    "type": "list",
    "default": [],
    "description": "Additional command-line arguments for gcal (e.g., '--no-title')"
  }
}
