# Path to the ppp connection errors log file.
# This block configures logrotate for the PPP connection error log.
/var/log/ppp-connect-errors {
	# Number of rotated log files to keep.
	rotate {{ log_rotate_count | default(4) }}
	weekly
	missingok
	notempty
	compress
	nocreate
}