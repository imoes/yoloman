{
  "prefix": {
    "type": "string",
    "default": "/usr",
    "description": "Installation prefix directory."
  },
  "name": {
    "type": "string",
    "default": "pyspf-milter",
    "description": "Service name."
  },
  "run_dir": {
    "type": "string",
    "default": "/run/pyspf-milter",
    "description": "Directory for the service run/pid files."
  },
  "user": {
    "type": "string",
    "default": "pyspf-milter",
    "description": "User under which the service runs."
  },
  "group": {
    "type": "string",
    "default": "pyspf-milter",
    "description": "Group under which the service runs."
  }
}
