{
  "exec_start": {
    "type": "string",
    "default": "/usr/bin/kactivitymanagerd",
    "description": "Path to the kactivitymanagerd executable."
  },
  "xdg_runtime_dir": {
    "type": "string",
    "default": "/run/user/%U",
    "description": "XDG Base Directory for user runtime files; %U will be expanded by systemd to the user ID."
  },
  "state_path": {
    "type": "string",
    "default": "{{ xdg_runtime_dir }}/kactivitymanagerd",
    "description": "Directory where kactivitymanagerd stores its persistent state; must be writable by the user."
  }
}
