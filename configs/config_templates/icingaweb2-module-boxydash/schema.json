{
  "setting_refresh": {
    "type": "string",
    "default": "10",
    "description": "Refresh interval in seconds for the dashboard view."
  },
  "setting_boxsize": {
    "type": "string",
    "default": "10",
    "description": "Grid box size for the dashboard layout."
  },
  "include_softstate": {
    "type": "string",
    "default": "1",
    "description": "Include soft states (pending) in the dashboard. Set to '0' to exclude.",
    "enum": [
      "0",
      "1"
    ]
  },
  "requires_authentication": {
    "type": "string",
    "default": "1",
    "description": "Require authentication to access the dashboard. Set to '0' to allow anonymous access.",
    "enum": [
      "0",
      "1"
    ]
  },
  "show_legend": {
    "type": "string",
    "default": "1",
    "description": "Show the legend in the dashboard. Set to '0' to hide.",
    "enum": [
      "0",
      "1"
    ]
  },
  "path_prefix": {
    "type": "string",
    "default": "/icingaweb2",
    "description": "The URL prefix where Icinga Web 2 is installed."
  }
}
