{
  "daemon_user": {
    "type": "string",
    "default": "ulog",
    "description": "User under which the ulogd daemon runs"
  },
  "pid_dir": {
    "type": "string",
    "default": "/run/ulog",
    "description": "Directory where the PID file is stored"
  }
}
