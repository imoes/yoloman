{
  "config_file": {
    "type": "string",
    "default": "/etc/diffmon/diffmon.cf",
    "description": "Path (relative or absolute) to the diffmon configuration file"
  },
  "binary": {
    "type": "string",
    "default": "/usr/bin/diffmon",
    "description": "Name/path of the diffmon executable"
  }
}
