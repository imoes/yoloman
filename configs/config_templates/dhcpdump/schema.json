{
  "interface": {
    "type": "string",
    "default": "",
    "description": "Network interface to listen on (e.g., eth0); mutually exclusive with pcapfile"
  },
  "pcapfile": {
    "type": "string",
    "default": "",
    "description": "Path to pcap file to read; mutually exclusive with interface"
  },
  "hostname_filter": {
    "type": "string",
    "default": "",
    "description": "Regular expression to filter Client Hardware Addresses (CHADDR)"
  },
  "full_hex_dump": {
    "type": "bool",
    "default": false,
    "description": "Enable full packet hex dump output"
  }
}
