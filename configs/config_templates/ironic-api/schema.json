{
  "UWSGI_PORT": {
    "type": "string",
    "default": "6385",
    "description": "The port on which the UWSGI server listens for HTTP connections."
  },
  "UWSGI_INI_PATH": {
    "type": "string",
    "default": "/etc/ironic/ironic-api-uwsgi.ini",
    "description": "Path to the UWSGI configuration INI file."
  },
  "UWSGI_INI_APP": {
    "type": "string",
    "default": "/usr/bin/ironic-api-wsgi",
    "description": "Path to the UWSGI application executable."
  }
}
