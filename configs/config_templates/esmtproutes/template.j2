# Route definitions for esmtproutes — map recipient domains to SMTP relay hosts
# Format: domain relay[:port][flags]
{% for route in routes %}{{ route.domain }} {{ route.relay }}{% if route.port %}:{{ route.port }}{% endif %}{% if route.flags %} {{ route.flags | join(',') }}{% endif %}
{% endfor %}