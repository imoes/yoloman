{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable the network indicator service. If disabled, no network status will be shown in the system tray."
  },
  "monitor_interfaces": {
    "type": "list",
    "default": [
      "wlan0",
      "eth0",
      "wwan0"
    ],
    "description": "List of network interfaces to monitor for connection status."
  },
  "show_ethernet": {
    "type": "bool",
    "default": true,
    "description": "Display Ethernet (wired) connection status in the network indicator menu."
  },
  "show_wifi": {
    "type": "bool",
    "default": true,
    "description": "Display Wi-Fi (wireless) connection status in the network indicator menu."
  },
  "show_bluetooth": {
    "type": "bool",
    "default": false,
    "description": "Display Bluetooth network status (e.g., tethering) in the indicator."
  },
  "show_mobile": {
    "type": "bool",
    "default": true,
    "description": "Display cellular/mobile data connection status (e.g., 3G/4G/5G) in the indicator."
  },
  "max_connection_entries": {
    "type": "number",
    "default": 10,
    "description": "Maximum number of saved Wi-Fi connections to display in the indicator menu."
  },
  "show_signal_strength": {
    "type": "bool",
    "default": true,
    "description": "Show signal strength indicator (bars or percentage) next to Wi-Fi/mobile connections."
  },
  "debug_logging": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging to the systemd journal."
  }
}
