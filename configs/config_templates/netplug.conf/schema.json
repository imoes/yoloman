{
  "interfaces": {
    "type": "list",
    "default": [],
    "description": "List of network interfaces to monitor for link state changes",
    "items": {
      "type": "object",
      "fields": {
        "name": {
          "type": "string",
          "default": "",
          "description": "Name of the network interface (e.g., eth0, wlan0)"
        },
        "action_script": {
          "type": "string",
          "default": "",
          "description": "Optional custom script to execute on link up/down events (overrides default)"
        }
      }
    }
  }
}
