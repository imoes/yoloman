{
  "options": {
    "type": "object",
    "description": "Command-line options for connman-gtk (each optional, defaults to false/omitted)"
  },
  "options.tray": {
    "type": "bool",
    "default": false,
    "description": "Launch connman-gtk minimized to system tray (matches --tray)"
  },
  "options.no_icon": {
    "type": "bool",
    "default": false,
    "description": "Disable status icon entirely (matches --no-icon)"
  },
  "options.use_fsid": {
    "type": "bool",
    "default": false,
    "description": "Use filesystem ID as passphrase for private keys when using openconnect (matches --use-fsid)"
  }
}
