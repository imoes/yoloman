{
  "bmf_args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments for the bmf binary; each item is a shell-quoted argument string (e.g., '-p -d /var/lib/bmf'). Must contain at least one argument, commonly '-p' for passthrough mode with header injection."
  }
}
