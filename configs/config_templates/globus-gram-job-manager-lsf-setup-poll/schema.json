{
  "sbin_path": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Path to the globus-job-manager executable."
  },
  "conf_path": {
    "type": "string",
    "default": "/etc/globus",
    "description": "Path to the globus configuration directory."
  }
}
