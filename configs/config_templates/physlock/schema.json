{
  "detach": {
    "type": "bool",
    "default": false,
    "description": "Fork and detach physlock before prompting for authentication."
  },
  "mute": {
    "type": "bool",
    "default": false,
    "description": "Mute kernel messages on console while physlock is running."
  },
  "disable_sysrq": {
    "type": "bool",
    "default": false,
    "description": "Disable SysRq mechanism while physlock is running."
  },
  "prompt_msg": {
    "type": "string",
    "default": "",
    "description": "Message displayed before the password prompt."
  }
}
