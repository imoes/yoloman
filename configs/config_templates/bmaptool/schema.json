{
 "cmd_dest": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "cmd_image": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "commands": {
  "default": [],
  "description": "List of commands to generate. Each item configures one 'copy' or 'create' invocation. Only one command type per run is typical, but multiple commands may be batched for scripting purposes.",
  "type": "list"
 },
 "commands[].bmap": {
  "default": "",
  "description": "Explicit path or URL to the bmap file. If omitted, auto-discovery is attempted.",
  "type": "string"
 },
 "commands[].bmap_sig": {
  "default": "",
  "description": "Path or URL to a detached OpenPGP signature file for the bmap.",
  "type": "string"
 },
 "commands[].dest": {
  "default": "",
  "description": "Destination file or block device path (required for 'copy').",
  "type": "string"
 },
 "commands[].fingerprint": {
  "default": "",
  "description": "Expected GPG fingerprint of the signer (for signature verification).",
  "type": "string"
 },
 "commands[].image": {
  "default": "",
  "description": "Source image file path or URL. Use '-' to read from stdin for 'copy' commands.",
  "type": "string"
 },
 "commands[].keyring": {
  "default": "",
  "description": "Path to a custom GPG keyring file for signature verification.",
  "type": "string"
 },
 "commands[].no_checksum": {
  "default": false,
  "description": "Generate bmap without SHA-256 checksums (not recommended).",
  "type": "bool"
 },
 "commands[].no_sig_verify": {
  "default": false,
  "description": "Skip OpenPGP signature verification of the bmap file (not recommended).",
  "type": "bool"
 },
 "commands[].no_verify": {
  "default": false,
  "description": "Skip SHA-256 checksum verification of data (not recommended).",
  "type": "bool"
 },
 "commands[].nobmap": {
  "default": false,
  "description": "Disable automatic bmap discovery and copy the entire image without bmap.",
  "type": "bool"
 },
 "commands[].output": {
  "default": "",
  "description": "Output file path for the generated bmap (for 'create' command). If omitted, write to stdout.",
  "type": "string"
 },
 "commands[].psplash_pipe": {
  "default": "",
  "description": "Path to a FIFO to which machine-readable progress (psplash format) will be written.",
  "type": "string"
 },
 "commands[].removable_device": {
  "default": false,
  "description": "Only proceed if destination is a removable block device (e.g., SD card or USB).",
  "type": "bool"
 },
 "commands[].type": {
  "default": "copy",
  "description": "Command type: 'copy' or 'create'. Required for each command entry.",
  "enum": [
   "copy",
   "create"
  ],
  "type": "string"
 }
}
