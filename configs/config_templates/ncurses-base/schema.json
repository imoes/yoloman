{
  "etc_terminfo_dir": {
    "type": "string",
    "default": "/etc/terminfo",
    "description": "The path to the system-local terminfo directory searched after user's home terminfo."
  },
  "lib_terminfo_dir": {
    "type": "string",
    "default": "/lib/terminfo",
    "description": "The path to the library-level terminfo directory searched after the etc terminfo directory."
  },
  "usr_share_terminfo_dir": {
    "type": "string",
    "default": "/usr/share/terminfo",
    "description": "The path to the user-level terminfo directory searched last."
  }
}
