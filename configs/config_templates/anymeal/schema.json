{
  "database_path": {
    "type": "string",
    "default": "~/.local/share/anymeal/anymeal.sqlite",
    "description": "Path to the SQLite database file storing recipes."
  },
  "data_directory": {
    "type": "string",
    "default": "~/.local/share/anymeal",
    "description": "Base directory for importing, exporting, and temporary recipe data."
  },
  "ui_theme": {
    "type": "string",
    "default": "system",
    "description": "UI theme: 'light', 'dark', or 'system'."
  },
  "max_recipes": {
    "type": "number",
    "default": 0,
    "description": "Maximum number of recipes to load in memory; 0 means no limit."
  },
  "import_formats": {
    "type": "list",
    "default": [
      "mm",
      "mr",
      "txt"
    ],
    "description": "List of import formats to support (in order of preference)."
  },
  "export_formats": {
    "type": "list",
    "default": [
      "mm",
      "html"
    ],
    "description": "List of export formats to support."
  },
  "print_options": {
    "type": "list",
    "default": [],
    "description": "Print settings (key-value pairs)."
  },
  "search_options": {
    "type": "list",
    "default": [],
    "description": "Search behavior settings (key-value pairs)."
  }
}
