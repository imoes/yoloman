{
 "active_beacon": {
  "default": false,
  "description": "Transmit beacon from collected ESSIDs every 10s.",
  "type": "bool"
 },
 "beacon_params": {
  "default": "",
  "description": "Hex string of TLV IEs (max 50); tags 0/3 ignored/overwritten.",
  "type": "string"
 },
 "bpfc": {
  "default": "",
  "description": "Path to kernel-space BPF file.",
  "type": "string"
 },
 "check_driver": {
  "default": false,
  "description": "Run driver capability checks only.",
  "type": "bool"
 },
 "check_injection": {
  "default": false,
  "description": "Run injection tests only.",
  "type": "bool"
 },
 "client_port": {
  "default": 60123,
  "description": "Status client port (1–65535).",
  "type": "number"
 },
 "disable_ap_attacks": {
  "default": false,
  "description": "Skip AP attacks (PMKID, connected clients).",
  "type": "bool"
 },
 "disable_client_attacks": {
  "default": false,
  "description": "Skip client attacks (ap-less M2).",
  "type": "bool"
 },
 "disable_deauthentication": {
  "default": false,
  "description": "Skip deauthentication/disassociation frames.",
  "type": "bool"
 },
 "do_rcascan": {
  "default": false,
  "description": "Perform radio channel scan on startup.",
  "type": "bool"
 },
 "do_targetscan": {
  "default": "",
  "description": "Target AP MAC to scan (others hidden).",
  "type": "string"
 },
 "dwell_time": {
  "default": 4,
  "description": "Time (seconds) to stay on frequency before hopping.",
  "type": "number"
 },
 "eap_server_cert": {
  "default": "",
  "description": "EAP TLS server certificate (PEM).",
  "type": "string"
 },
 "eap_server_key": {
  "default": "",
  "description": "EAP TLS server private key (PEM).",
  "type": "string"
 },
 "eapol_timeout": {
  "default": 20000,
  "description": "EAPOL timeout in microseconds.",
  "type": "number"
 },
 "eapoleap_timeout": {
  "default": 2500000,
  "description": "EAP EAPOL timeout in microseconds over entire sequence.",
  "type": "number"
 },
 "eapreq": {
  "default": "",
  "description": "EAP request sequence (max 20) as hex string with mode prefix.",
  "type": "string"
 },
 "eapreq_follownak": {
  "default": false,
  "description": "Follow requested Auth Type in Legacy NAK.",
  "type": "bool"
 },
 "eaptlstun": {
  "default": false,
  "description": "Enable TLS tunnel (requires cert/key).",
  "type": "bool"
 },
 "enable_status": {
  "default": 0,
  "description": "Bitmask for real-time status display (bit 0–10).",
  "type": "number"
 },
 "error_max": {
  "default": 100,
  "description": "Terminate after N errors (0 = unlimited).",
  "type": "number"
 },
 "essidlist": {
  "default": "",
  "description": "Path to file with ESSIDs to beacon (max 256 entries).",
  "type": "string"
 },
 "essidlist_wpaent": {
  "default": "",
  "description": "Path to WPA-Enterprise-only ESSIDs file.",
  "type": "string"
 },
 "filter_mode": {
  "default": 0,
  "description": "Filter mode: 0=ignore, 1=protect, 2=target.",
  "type": "number"
 },
 "filterlist_ap": {
  "default": "",
  "description": "Path to AP filter list or single MAC.",
  "type": "string"
 },
 "filterlist_ap_vendor": {
  "default": "",
  "description": "Path to AP OUI (3-byte) filter list.",
  "type": "string"
 },
 "filterlist_client": {
  "default": "",
  "description": "Path to client filter list or single MAC.",
  "type": "string"
 },
 "filterlist_client_vendor": {
  "default": "",
  "description": "Path to client OUI (3-byte) filter list.",
  "type": "string"
 },
 "flood_beacon": {
  "default": false,
  "description": "Transmit beacon on every received beacon (ap-less attack).",
  "type": "bool"
 },
 "force_interface": {
  "default": false,
  "description": "Bypass ioctl checks (dangerous on virtual interfaces).",
  "type": "bool"
 },
 "frames": {
  "default": 3,
  "description": "Bitmask of frame types to capture: 1 (MGT), 2 (EAP/EAPOL) = default 3.",
  "type": "number"
 },
 "frequency": {
  "default": 0,
  "description": "Channel (≤1000) or frequency in MHz (>1000); 0 = auto.",
  "type": "number"
 },
 "gpio_button": {
  "default": 0,
  "description": "Raspberry Pi GPIO pin for button (2–27); 0 = disabled.",
  "type": "number"
 },
 "gpio_statusled": {
  "default": 0,
  "description": "Raspberry Pi GPIO pin for status LED (2–27); 0 = disabled.",
  "type": "number"
 },
 "gpio_statusled_interval": {
  "default": 5,
  "description": "GPIO LED flash interval in seconds.",
  "type": "number"
 },
 "interface": {
  "default": "",
  "description": "Wireless interface name (e.g., wlan0); monitor mode is auto-enabled.",
  "type": "string"
 },
 "nmea_output_file": {
  "default": "",
  "description": "Output NMEA log file path.",
  "type": "string"
 },
 "output_file": {
  "description": "Output file path; '-' for stdout, '+' for stdout with radiotap header.",
  "enum": [
   "-",
   "+"
  ],
  "type": "string"
 },
 "poweroff": {
  "default": false,
  "description": "Power off system on exit.",
  "type": "bool"
 },
 "rcascan_max": {
  "default": 256,
  "description": "Maximum lines shown in RCASCAN.",
  "type": "number"
 },
 "rcascan_order": {
  "default": 0,
  "description": "RCASCAN sort order: 0=PROBERESPONSE, 1=BEACON, 2=CHANNEL.",
  "type": "number"
 },
 "reason_code": {
  "default": 7,
  "description": "Deauthentication/disassociation reason code (e.g., 7 = CLASS3_FRAME).",
  "type": "number"
 },
 "reboot": {
  "default": false,
  "description": "Reboot system on exit.",
  "type": "bool"
 },
 "resume_ap_attacks": {
  "default": 864000,
  "description": "Resume AP attacks after N BEACONs (e.g., ~10 days at 1 BEACON/s).",
  "type": "number"
 },
 "scanlist": {
  "default": 0,
  "description": "Predefined scanlist (0=auto, 1–4=2.4/5/combined).",
  "type": "number"
 },
 "server_port": {
  "default": 60123,
  "description": "Status server port (1–65535).",
  "type": "number"
 },
 "silent": {
  "default": false,
  "description": "Passive mode — no transmission.",
  "type": "bool"
 },
 "status_ip": {
  "default": "224.0.0.255",
  "description": "IP for status server/client (multicast or unicast).",
  "type": "string"
 },
 "stop_ap_attacks": {
  "default": 600,
  "description": "Stop AP attacks after receiving N BEACONs.",
  "type": "number"
 },
 "stop_client_m2_attacks": {
  "default": 0,
  "description": "Stop client M2 attacks after receiving N M2 frames (0 = unlimited).",
  "type": "number"
 },
 "timeout_minutes": {
  "default": 0,
  "description": "Timeout in minutes (0 = disabled).",
  "type": "number"
 },
 "use_gps_device": {
  "default": "",
  "description": "GPS device path (e.g., /dev/ttyUSB0).",
  "type": "string"
 },
 "use_gpsd": {
  "default": false,
  "description": "Use GPSD daemon for GPS data.",
  "type": "bool"
 },
 "weak_candidate": {
  "default": "12345678",
  "description": "Pre-shared key (8–63 chars) for weak PSK alerting (saved to pcapng).",
  "type": "string"
 },
 "wpaent": {
  "default": false,
  "description": "Announce WPA-Enterprise in beacons/probe responses.",
  "type": "bool"
 }
}
