{
  "pidfile_dir": {
    "type": "string",
    "default": "pglistener",
    "description": "Directory name for the PID file (used in /var/run/ and /run/ paths)."
  },
  "name": {
    "type": "string",
    "default": "pglistener",
    "description": "Service/daemon name used for PID file, script name, and default config path."
  },
  "user": {
    "type": "string",
    "default": "pglisten",
    "description": "System user under which the pglistener daemon runs."
  }
}
