{
  "desc": {
    "type": "string",
    "default": "Setting kernel variables",
    "description": "The description string for the init script."
  },
  "daemon": {
    "type": "string",
    "default": "/sbin/sysctl",
    "description": "Path to the sysctl daemon binary."
  },
  "pidfile": {
    "type": "string",
    "default": "none",
    "description": "Path to the PID file, or 'none' if not used."
  },
  "quiet_sysctl": {
    "type": "string",
    "default": "-q",
    "description": "Flags passed to sysctl to control output verbosity. '-q' suppresses output."
  }
}
