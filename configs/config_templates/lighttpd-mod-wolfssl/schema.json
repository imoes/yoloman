{
  "ssl_pemfile": {
    "type": "string",
    "default": "/etc/lighttpd/server.pem",
    "description": "Path to the SSL PEM certificate and key file."
  },
  "ssl_bind_addr": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to bind the SSL server socket."
  },
  "ssl_bind_port": {
    "type": "number",
    "default": 443,
    "description": "Port number to bind the SSL server socket."
  }
}
