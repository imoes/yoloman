{
  "daemon_path": {
    "type": "string",
    "default": "/usr/bin/cxl",
    "description": "Full path to the CXL executable"
  },
  "daemon_args": {
    "type": "string",
    "default": "monitor --daemon",
    "description": "Arguments passed to the CXL daemon command"
  }
}
