{
  "output_dir": {
    "type": "string",
    "default": ".",
    "description": "The directory where archive contents should be extracted. Defaults to current directory if empty or '.'."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Password for encrypted ALZ archives. Leave empty if not required."
  },
  "archive_path": {
    "type": "string",
    "default": "archive.alz",
    "description": "Path to the ALZ archive file to process."
  }
}
