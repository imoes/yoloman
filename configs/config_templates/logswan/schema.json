{
  "input_log_file": {
    "type": "string",
    "default": "/var/log/nginx/access.log",
    "description": "Path to the web server log file to analyze."
  },
  "enable_geoip": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable GeoIP lookups using the -g flag."
  },
  "geoip_db": {
    "type": "string",
    "default": "",
    "description": "Path to the MaxMind GeoIP database file. Required if enable_geoip is true."
  },
  "output_dir": {
    "type": "string",
    "default": "/var/lib/logswan/output",
    "description": "Directory where the resulting JSON analysis files will be saved."
  }
}
