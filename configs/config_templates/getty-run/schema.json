{
  "tty_number": {
    "type": "string",
    "default": "163",
    "description": "The TTY device number (e.g., '163' for hvc0); used to detect if another process is already running."
  },
  "console_device": {
    "type": "string",
    "default": "hvc0",
    "description": "The console device name (e.g., 'hvc0', 'tty1') passed to utmpset."
  }
}
