{
  "accel": {
    "default": "tcg",
    "description": "Accelerator to use (e.g., kvm, tcg). Defaults to TCG for s390x if KVM is not available or not specified.",
    "enum": [
      "kvm",
      "tcg",
      "xen",
      "hvf",
      "nvmm",
      "whpx"
    ],
    "type": "string",
    "enum_open": true
  },
  "acpi_table_data": {
    "default": "",
    "description": "ACPI table data file.",
    "type": "string"
  },
  "acpi_table_sig": {
    "default": "",
    "description": "ACPI table signature.",
    "type": "string"
  },
  "action_panic": {
    "description": "Action on guest panic (none, shutdown, reset, pause, debug).",
    "enum": [
      "none",
      "shutdown",
      "reset",
      "pause",
      "debug"
    ],
    "type": "string"
  },
  "action_reboot": {
    "description": "Action on guest reboot (none, shutdown, pause, debug, reset).",
    "enum": [
      "none",
      "shutdown",
      "pause",
      "debug",
      "reset"
    ],
    "type": "string"
  },
  "action_shutdown": {
    "description": "Action on guest shutdown (none, pause, debug, reset).",
    "enum": [
      "none",
      "pause",
      "debug",
      "reset"
    ],
    "type": "string"
  },
  "add_fds": {
    "default": [],
    "description": "List of file descriptors to add.",
    "type": "list"
  },
  "add_fds.items": {
    "default": "",
    "description": "Placeholder. Structure: fd (number), set (number), opaque (string).",
    "type": "string"
  },
  "audio_backend": {
    "default": "pa",
    "description": "Audio backend driver (e.g., pa for PulseAudio, alsa).",
    "enum": [
      "none",
      "alsa",
      "coreaudio",
      "dsound",
      "oss",
      "pa",
      "pipewire",
      "sdl",
      "sndio",
      "spice",
      "wav"
    ],
    "type": "string",
    "enum_open": true
  },
  "audio_model": {
    "default": "none",
    "description": "Audio device model. 'none' disables audio.",
    "enum": [
      "none",
      "sb16",
      "es1370",
      "ac97",
      "hda",
      "gus",
      "cs4231a"
    ],
    "type": "string"
  },
  "bios_file": {
    "default": "",
    "description": "Path to BIOS file.",
    "type": "string"
  },
  "blockdevs": {
    "default": [],
    "description": "List of advanced block devices.",
    "type": "list"
  },
  "blockdevs.items": {
    "default": "",
    "description": "Placeholder. Structure: driver (string), node_name (string), options (map).",
    "type": "string"
  },
  "boot_order": {
    "default": "c",
    "description": "Boot order string (e.g., cd, nc, cdn).",
    "enum": [
      "a",
      "b",
      "c",
      "d",
      "n"
    ],
    "type": "string",
    "enum_open": true
  },
  "chardevs": {
    "default": [],
    "description": "List of character devices.",
    "type": "list"
  },
  "chardevs.items": {
    "default": "",
    "description": "Placeholder. Structure: backend (string), id (string), options (map).",
    "type": "string"
  },
  "compat_deprecated_input": {
    "description": "Policy for deprecated input (accept, reject, crash).",
    "enum": [
      "accept",
      "reject",
      "crash"
    ],
    "type": "string"
  },
  "compat_unstable_input": {
    "description": "Policy for unstable input (accept, reject, crash).",
    "enum": [
      "accept",
      "reject",
      "crash"
    ],
    "type": "string"
  },
  "cpus": {
    "default": 1,
    "description": "Number of virtual CPUs.",
    "type": "number"
  },
  "daemonize": {
    "default": false,
    "description": "Daemonize the QEMU process after initialization.",
    "type": "bool"
  },
  "debug_flags": {
    "description": "Debug logging flags (e.g., int,cpu,guest_errors).",
    "enum": [
      "int",
      "cpu",
      "restore",
      "exec",
      "fw_cfg",
      "qemu-vnc"
    ],
    "type": "string",
    "enum_open": true
  },
  "debugcon_dev": {
    "default": "",
    "description": "Debug console redirection target.",
    "type": "string"
  },
  "devices": {
    "default": [],
    "description": "List of additional devices.",
    "type": "list"
  },
  "devices.items": {
    "default": "",
    "description": "Placeholder. Structure: driver (string), id (string), properties (map).",
    "type": "string"
  },
  "display_type": {
    "description": "Display type (e.g., sdl, gtk, curses). Used if VNC/SPICE not active.",
    "enum": [
      "sdl",
      "gtk",
      "curses",
      "cocoa",
      "egl-headless",
      "spice-app",
      "dbus",
      "none"
    ],
    "type": "string",
    "enum_open": true
  },
  "drives": {
    "default": [],
    "description": "List of block devices to attach to the VM. Each item is a map with filename, format, cache, index, and description.",
    "type": "list"
  },
  "drives.items": {
    "default": "",
    "description": "Placeholder for items. Actual structure: filename (string), format (string), cache (string), index (string), description (string).",
    "type": "string"
  },
  "dtb": {
    "default": "",
    "description": "Path to Device Tree Binary file.",
    "type": "string"
  },
  "dump_vmstate": {
    "default": "",
    "description": "File to dump VM state to.",
    "type": "string"
  },
  "echr": {
    "default": "",
    "description": "Escape character for monitor switch (numeric ascii value).",
    "type": "string"
  },
  "enable_kvm": {
    "default": false,
    "description": "Enable KVM full virtualization support.",
    "type": "bool"
  },
  "enable_sync_profile": {
    "default": false,
    "description": "Enable synchronization profiling.",
    "type": "bool"
  },
  "fsdevs": {
    "default": [],
    "description": "List of 9p filesystem devices.",
    "type": "list"
  },
  "fsdevs.items": {
    "default": "",
    "description": "Placeholder. Structure: type (string), id (string), mount_tag (string), device_type (string), options (map).",
    "type": "string"
  },
  "fw_cfg_name": {
    "default": "",
    "description": "Name for a firmware configuration entry.",
    "type": "string"
  },
  "fw_cfg_value": {
    "default": "",
    "description": "Value/String for a firmware configuration entry.",
    "type": "string"
  },
  "gdb_dev": {
    "default": "",
    "description": "GDB connection device (e.g., tcp::1234).",
    "type": "string"
  },
  "global_driver": {
    "default": "",
    "description": "Global driver property setting (e.g., ide-hd.physical_block_size=4096).",
    "type": "string"
  },
  "globals": {
    "default": [],
    "description": "List of global driver settings.",
    "type": "list"
  },
  "globals.items": {
    "default": "",
    "description": "A global driver property string.",
    "type": "string"
  },
  "icount_shift": {
    "default": "",
    "description": "Instruction counter shift value.",
    "type": "string"
  },
  "incoming_defer": {
    "default": false,
    "description": "Defer incoming migration.",
    "type": "bool"
  },
  "incoming_exec": {
    "default": "",
    "description": "Incoming migration exec command.",
    "type": "string"
  },
  "incoming_fd": {
    "default": 0,
    "description": "Incoming migration file descriptor.",
    "type": "number"
  },
  "incoming_file": {
    "default": "",
    "description": "Incoming migration file path.",
    "type": "string"
  },
  "incoming_offset": {
    "default": "",
    "description": "Offset for incoming migration file.",
    "type": "string"
  },
  "incoming_tcp": {
    "default": "",
    "description": "Incoming migration TCP host:port.",
    "type": "string"
  },
  "incoming_unix": {
    "default": "",
    "description": "Incoming migration Unix socket path.",
    "type": "string"
  },
  "initrd": {
    "default": "",
    "description": "Path to the initial ramdisk image.",
    "type": "string"
  },
  "iothread_id": {
    "default": "",
    "description": "ID of an IOThread to create.",
    "type": "string"
  },
  "jitdump": {
    "default": false,
    "description": "Generate JIT dump file.",
    "type": "bool"
  },
  "kernel": {
    "default": "",
    "description": "Path to the kernel image to boot directly.",
    "type": "string"
  },
  "kernel_params": {
    "default": "console=hvc0",
    "description": "Kernel command line arguments.",
    "type": "string"
  },
  "keyboard_layout": {
    "default": "en-us",
    "description": "Keyboard layout language.",
    "enum": [
      "ar",
      "da",
      "de",
      "de-ch",
      "en-gb",
      "en-us",
      "es",
      "et",
      "fi",
      "fo",
      "fr",
      "fr-be",
      "fr-ca",
      "fr-ch",
      "hr",
      "hu",
      "is",
      "it",
      "ja",
      "lt",
      "mk",
      "nl",
      "nl-be",
      "no",
      "pl",
      "pt-br",
      "pt",
      "ru",
      "sl",
      "sv",
      "th",
      "tr"
    ],
    "type": "string"
  },
  "l_path": {
    "default": "",
    "description": "Path to BIOS, VGA BIOS, and keymaps directory.",
    "type": "string"
  },
  "loadvm": {
    "default": "",
    "description": "Path to VM state file to load.",
    "type": "string"
  },
  "logfile": {
    "default": "",
    "description": "Log file path. If empty, logs to stderr.",
    "type": "string"
  },
  "mac": {
    "default": "52:54:00:12:34:56",
    "description": "MAC address for the network interface.",
    "type": "string"
  },
  "max_memory": {
    "default": "2G",
    "description": "Maximum amount of memory the guest can reach.",
    "type": "string"
  },
  "mem_slots": {
    "default": 4,
    "description": "Number of hotpluggable memory slots.",
    "type": "number"
  },
  "memory": {
    "default": "1G",
    "description": "Initial guest RAM size (e.g., 1G, 512M).",
    "type": "string"
  },
  "monitor_dev": {
    "default": "vc",
    "description": "Monitor redirection target (e.g., vc, stdio, none).",
    "enum": [
      "vc",
      "stdio",
      "none",
      "/dev/ttyS0",
      "unix:path"
    ],
    "type": "string",
    "enum_open": true
  },
  "msg_guest_name": {
    "description": "Include guest name in messages (on, off).",
    "enum": [
      "on",
      "off"
    ],
    "type": "string"
  },
  "msg_timestamp": {
    "description": "Enable message timestamps (on, off).",
    "enum": [
      "on",
      "off"
    ],
    "type": "string"
  },
  "name": {
    "default": "s390x-vm",
    "description": "Name of the guest.",
    "type": "string"
  },
  "netdevs": {
    "default": [],
    "description": "List of network devices.",
    "type": "list"
  },
  "netdevs.items": {
    "default": "",
    "description": "Placeholder. Structure: type (string), id (string), options (map).",
    "type": "string"
  },
  "nic_model": {
    "default": "virtio",
    "description": "Network card model (e.g., virtio, e1000, rtl8139). For s390x, virtio is recommended.",
    "type": "string"
  },
  "nic_type": {
    "default": "user",
    "description": "Network backend type (user, tap, bridge, socket, etc.).",
    "enum": [
      "user",
      "tap",
      "bridge",
      "socket",
      "l2tpv3",
      "vde",
      "netmap",
      "af-xdp",
      "vhost-user",
      "none"
    ],
    "type": "string"
  },
  "no_reboot": {
    "default": false,
    "description": "Exit instead of rebooting.",
    "type": "bool"
  },
  "no_shutdown": {
    "default": false,
    "description": "Don't exit QEMU on guest shutdown.",
    "type": "bool"
  },
  "no_user_config": {
    "default": false,
    "description": "Don't load user-provided config files.",
    "type": "bool"
  },
  "nodefaults": {
    "default": false,
    "description": "Don't create default devices.",
    "type": "bool"
  },
  "numa_cpu_assigns": {
    "default": [],
    "description": "List of CPU assignments for NUMA.",
    "type": "list"
  },
  "numa_cpu_assigns.items": {
    "default": "",
    "description": "Placeholder. Structure: nodeid (number), socket_id (number), core_id (number), thread_id (number).",
    "type": "string"
  },
  "numanodes": {
    "default": [],
    "description": "List of NUMA nodes.",
    "type": "list"
  },
  "numanodes.items": {
    "default": "",
    "description": "Placeholder. Structure: nodeid (number), memory (string).",
    "type": "string"
  },
  "objects": {
    "default": [],
    "description": "List of generic objects.",
    "type": "list"
  },
  "objects.items": {
    "default": "",
    "description": "Placeholder. Structure: type (string), id (string), properties (map).",
    "type": "string"
  },
  "only_migratable": {
    "default": false,
    "description": "Only allow migratable devices.",
    "type": "bool"
  },
  "option_roms": {
    "default": [],
    "description": "List of option ROM files to load.",
    "type": "list"
  },
  "option_roms.items": {
    "default": "",
    "description": "Path to an option ROM file.",
    "type": "string"
  },
  "overcommit_cpu_pm": {
    "description": "CPU power management overcommit setting (off, on).",
    "enum": [
      "on",
      "off"
    ],
    "type": "string"
  },
  "overcommit_mem_lock": {
    "description": "Memory overcommit lock setting (off, on, on-fault).",
    "enum": [
      "on",
      "off",
      "on-fault"
    ],
    "type": "string"
  },
  "pause_at_start": {
    "default": false,
    "description": "Do not start CPU at startup (pause until 'c' in monitor).",
    "type": "bool"
  },
  "perfmap": {
    "default": false,
    "description": "Generate perf map file.",
    "type": "bool"
  },
  "pflash_file": {
    "default": "",
    "description": "Path to Parallel Flash image.",
    "type": "string"
  },
  "pidfile": {
    "default": "",
    "description": "Path to PID file.",
    "type": "string"
  },
  "plugin_file": {
    "default": "",
    "description": "Path to plugin shared library.",
    "type": "string"
  },
  "preconfig": {
    "default": false,
    "description": "Pause QEMU for interactive configuration before machine creation.",
    "type": "bool"
  },
  "readconfig_file": {
    "default": "",
    "description": "Path to read configuration from.",
    "type": "string"
  },
  "rng_backend": {
    "default": "builtin",
    "description": "RNG backend type (builtin, random, egd).",
    "enum": [
      "builtin",
      "random"
    ],
    "type": "string"
  },
  "rtc_base": {
    "default": "utc",
    "description": "RTC base time (utc, localtime, datetime).",
    "enum": [
      "utc",
      "localtime",
      "datetime"
    ],
    "type": "string"
  },
  "rtc_clock": {
    "default": "host",
    "description": "RTC clock source (host, rt, vm).",
    "enum": [
      "host",
      "rt",
      "vm"
    ],
    "type": "string"
  },
  "run_with_chroot": {
    "default": "",
    "description": "Directory to chroot into.",
    "type": "string"
  },
  "run_with_user": {
    "default": "",
    "description": "User to drop privileges to (username or uid:gid).",
    "type": "string"
  },
  "sandbox": {
    "description": "Seccomp sandbox settings (e.g., on, obsolete=on).",
    "enum": [
      "on",
      "off"
    ],
    "type": "string",
    "enum_open": true
  },
  "seed": {
    "default": 0,
    "description": "Random seed for deterministic behavior.",
    "type": "number"
  },
  "semihosting": {
    "default": false,
    "description": "Enable Semihosting mode.",
    "type": "bool"
  },
  "semihosting_args": {
    "default": [],
    "description": "List of arguments for semihosting.",
    "type": "list"
  },
  "semihosting_args.items": {
    "default": "",
    "description": "A semihosting argument string.",
    "type": "string"
  },
  "serial_dev": {
    "default": "stdio",
    "description": "Serial port redirection target (e.g., stdio, vc, tcp::4444,server=on).",
    "enum": [
      "vc",
      "pty",
      "none",
      "null",
      "stdio",
      "/dev/ttyS0",
      "file:filename",
      "pipe:filename",
      "telnet:host:port",
      "unix:path",
      "braille",
      "msmouse"
    ],
    "type": "string",
    "enum_open": true
  },
  "set_obj": {
    "default": "",
    "description": "Set parameter for an object (e.g., machine.x-foo=bar).",
    "type": "string"
  },
  "sets": {
    "default": [],
    "description": "List of set commands.",
    "type": "list"
  },
  "sets.items": {
    "default": "",
    "description": "A set command string.",
    "type": "string"
  },
  "shim_file": {
    "default": "",
    "description": "Path to shim.efi.",
    "type": "string"
  },
  "smbios_entries": {
    "default": [],
    "description": "List of SMBIOS entries.",
    "type": "list"
  },
  "smbios_entries.items": {
    "default": "",
    "description": "A SMBIOS entry string.",
    "type": "string"
  },
  "snapshot": {
    "default": false,
    "description": "Write to temporary files instead of disk images.",
    "type": "bool"
  },
  "spice_enable": {
    "default": false,
    "description": "Enable SPICE remote desktop protocol.",
    "type": "bool"
  },
  "spice_port": {
    "default": 5900,
    "description": "SPICE TCP port.",
    "type": "number"
  },
  "tpm_device_type": {
    "default": "",
    "description": "TPM device model type (e.g., tpm-tis).",
    "type": "string"
  },
  "tpm_passthrough": {
    "default": false,
    "description": "Enable TPM passthrough to host's TPM.",
    "type": "bool"
  },
  "trace_pattern": {
    "default": "",
    "description": "Tracing pattern to enable.",
    "type": "string"
  },
  "uuid": {
    "default": "",
    "description": "System UUID. If empty, a random one is generated.",
    "type": "string"
  },
  "vnc_display": {
    "default": "",
    "description": "VNC display specification (e.g., :0, 0.0.0.0:5900).",
    "type": "string"
  },
  "watchdog_action": {
    "default": "reset",
    "description": "Action on watchdog timeout (reset, shutdown, poweroff, inject-nmi, pause, debug, none).",
    "enum": [
      "reset",
      "shutdown",
      "poweroff",
      "inject-nmi",
      "pause",
      "debug",
      "none"
    ],
    "type": "string"
  },
  "xen_attach": {
    "default": false,
    "description": "Attach to existing Xen domain.",
    "type": "bool"
  },
  "xen_domid": {
    "default": 0,
    "description": "Xen guest domain ID.",
    "type": "number"
  }
}
