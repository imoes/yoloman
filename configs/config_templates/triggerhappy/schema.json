{
  "daemon_opts": {
    "type": "string",
    "default": "",
    "description": "Additional options that are passed to the Triggerhappy daemon (thd). For example, use '--user root' to run the daemon as root."
  }
}
