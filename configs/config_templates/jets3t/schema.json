{
  "gatekeeperUrl": {
    "type": "string",
    "default": "http://localhost:8080/gatekeeper-0.8.1/GatekeeperServlet",
    "description": "URL to access the JetS3t Gatekeeper servlet implementation."
  },
  "skin_name": {
    "type": "string",
    "default": "html",
    "description": "The named skin implementation/package to use. Leave empty or commented out if not specified."
  },
  "login_username_default": {
    "type": "string",
    "default": "anonymous",
    "description": "Default value for the username login field."
  },
  "login_password_default": {
    "type": "string",
    "default": "",
    "description": "Default value for the password login field."
  }
}
