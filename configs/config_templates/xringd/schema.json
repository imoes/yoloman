{
  "daemon_path": {
    "type": "string",
    "default": "/usr/sbin/xringd",
    "description": "The path to the xringd binary executable."
  },
  "service_name": {
    "type": "string",
    "default": "xringd",
    "description": "The name of the service, used for display and usage hints."
  }
}
