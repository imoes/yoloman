{
  "name": {
    "type": "string",
    "default": "robustirc-bridge",
    "description": "The name of the service/daemon."
  },
  "socks_port": {
    "type": "number",
    "default": 1080,
    "description": "The port number for the SOCKS proxy."
  },
  "listen_address": {
    "type": "string",
    "default": "",
    "description": "The address and port the bridge listens on (e.g., '0.0.0.0:6667' or ':6667'). Empty string means unspecified/default."
  }
}
