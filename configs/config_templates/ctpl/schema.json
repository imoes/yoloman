{
  "input_file": {
    "type": "string",
    "default": "",
    "description": "Input template file path (required positional argument)"
  },
  "output": {
    "type": "string",
    "default": "",
    "description": "Output file path; if empty, output goes to stdout"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose mode (corresponds to -v)"
  },
  "encoding": {
    "type": "string",
    "default": "",
    "description": "Input/output encoding (e.g., UTF-8); if empty, uses system default"
  },
  "env_files": {
    "type": "list",
    "default": [],
    "description": "List of environment file paths (one per -e option)"
  },
  "env_chunks": {
    "type": "list",
    "default": [],
    "description": "List of environment chunk strings (one per -c option)"
  }
}
