{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options/flags to pass to cabextract (e.g., ['-q', '-d', '/tmp/cab', '-e', 'utf8'])). Each element is a bare string matching documented options. Order matters; values are joined with spaces. No validation of option combinations is performed. Only documented flags supported (see cabextract(1))."
  }
}
