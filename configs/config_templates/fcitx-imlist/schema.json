{
  "input_methods": {
    "type": "list",
    "default": [
      "us"
    ],
    "description": "Ordered list of input method names (e.g., 'us', 'jp', 'mozc'). Must be valid fcitx input method names as per fcitx-imlist(1)."
  },
  "default_input_method": {
    "type": "string",
    "default": "",
    "description": "Name of the default input method (must be present in input_methods). If empty, defaults to first item."
  }
}
