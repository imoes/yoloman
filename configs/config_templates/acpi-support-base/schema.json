{
  "policy_funcs_path": {
    "type": "string",
    "default": "/usr/share/acpi-support/policy-funcs",
    "description": "Path to the acpi-support policy functions script to source."
  },
  "powerbtn_script": {
    "type": "string",
    "default": "/etc/acpi/powerbtn.sh",
    "description": "Path to the legacy power button handler script (from acpid)."
  },
  "shutdown_command": {
    "type": "string",
    "default": "/sbin/shutdown -h -P now \"Power button pressed\"",
    "description": "Command to execute when no other handler is present."
  }
}
