{
  "validate_links": {
    "type": "bool",
    "default": true,
    "description": "Enable validation of links for format and response."
  },
  "validate_assets": {
    "type": "bool",
    "default": true,
    "description": "Enable validation of assets for format and response."
  },
  "max_depth": {
    "type": "number",
    "default": 10,
    "description": "Maximum depth to traverse when recursing. Set to null for unlimited depth."
  },
  "recursive": {
    "type": "bool",
    "default": true,
    "description": "Enable recursive validation of all related STAC objects."
  }
}
