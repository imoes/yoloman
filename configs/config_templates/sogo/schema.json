{
  "session_cleanup_nbMinutes": {
    "type": "number",
    "default": 60,
    "description": "Number of minutes of inactivity before a session is dropped."
  },
  "backup_retention_days": {
    "type": "number",
    "default": 31,
    "description": "Number of days worth of backups to keep."
  }
}
