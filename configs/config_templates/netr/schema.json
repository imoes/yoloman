{
  "interface_name": {
    "type": "string",
    "default": "all",
    "description": "Network interface to monitor (e.g., 'eth0', 'wlan0', 'all'). Defaults to all interfaces."
  },
  "refresh_interval": {
    "type": "number",
    "default": 1,
    "description": "Refresh rate in seconds for updating the display."
  },
  "color_output": {
    "type": "bool",
    "default": true,
    "description": "Enable colored output in the terminal for better readability."
  },
  "log_file": {
    "type": "string",
    "default": "",
    "description": "Path to a log file for storing throughput history. Leave empty to disable logging."
  }
}
