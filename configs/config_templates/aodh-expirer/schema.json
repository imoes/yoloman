{
  "SERVICE_NAME": {
    "type": "string",
    "default": "aodh-expirer",
    "description": "Name of the service (as provided in LSB header)"
  },
  "SERVICE_DESC": {
    "type": "string",
    "default": "OpenStack Aodh Expirer (aodh-expirer)",
    "description": "Short description of the service"
  },
  "PROJECT_NAME": {
    "type": "string",
    "default": "aodh",
    "description": "OpenStack project name (used for paths, user, group, config paths, etc.)"
  },
  "SERVICE_DESCRIPTION": {
    "type": "string",
    "default": "aodh expirer",
    "description": "Short description line in LSB header"
  },
  "SERVICE_LONG_DESCRIPTION": {
    "type": "string",
    "default": "aodh listner",
    "description": "Long description line in LSB header (note: likely typo in original 'listner')"
  }
}
