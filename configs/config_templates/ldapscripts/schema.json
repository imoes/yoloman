{
  "ldap_uri": {
    "type": "string",
    "default": "ldap://localhost",
    "description": "LDAP server URI. Use 'ldap://' for unencrypted or 'ldaps://' for SSL/TLS. Include port if non-standard."
  },
  "base_dn": {
    "type": "string",
    "default": "dc=example,dc=com",
    "description": "LDAP base DN (Distinguished Name) for directory root. All LDAP operations will be scoped under this DN."
  },
  "admin_dn": {
    "type": "string",
    "default": "cn=Manager,dc=example,dc=com",
    "description": "DN of the LDAP administrative user with sufficient privileges to add/delete/modify users and groups."
  },
  "admin_password_file": {
    "type": "string",
    "default": "/etc/ldapscripts/ldapscripts.passwd",
    "description": "Path to file containing raw password for admin user (BINDDN). Must be readable only by ldapscripts."
  },
  "default_gid_start": {
    "type": "number",
    "default": 10000,
    "description": "Starting GID number for new group accounts. Aligns with system GID conventions."
  },
  "default_uid_start": {
    "type": "number",
    "default": 10000,
    "description": "Starting UID number for new user accounts. Should match system UID policy (e.g., \u2265 1000 for regular users)."
  },
  "group_objectclass": {
    "type": "string",
    "default": "posixGroup",
    "description": "Space-separated list of LDAP object classes for new groups. Adjust per your schema (e.g., include 'groupOfNames' if using member attributes)."
  },
  "default_shell": {
    "type": "string",
    "default": "/bin/sh",
    "description": "Default login shell assigned to new Unix users."
  },
  "default_home": {
    "type": "string",
    "default": "/home",
    "description": "Base path for user home directories (e.g., /home, /users). Full paths will be constructed as {{ default_home }}/{{ username }}."
  },
  "ldapsearch_bin": {
    "type": "string",
    "default": "/usr/bin/ldapsearch",
    "description": "Full path to the ldapsearch binary."
  },
  "ldapadd_bin": {
    "type": "string",
    "default": "/usr/bin/ldapadd",
    "description": "Full path to the ldapadd binary used to create LDAP entries."
  },
  "ldapdelete_bin": {
    "type": "string",
    "default": "/usr/bin/ldapdelete",
    "description": "Full path to the ldapdelete binary."
  },
  "ldapmodify_bin": {
    "type": "string",
    "default": "/usr/bin/ldapmodify",
    "description": "Full path to the ldapmodify binary."
  },
  "ldappasswd_bin": {
    "type": "string",
    "default": "/usr/bin/ldappasswd",
    "description": "Full path to the ldappasswd binary."
  }
}
