{
  "mymbox_to": {
    "type": "string",
    "default": "",
    "description": "The recipient address or mailbox identifier to check for mail."
  },
  "rclock_date": {
    "type": "string",
    "default": "",
    "description": "The date format or source for the 'rclock' display element."
  }
}
