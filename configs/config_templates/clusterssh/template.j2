# /etc/clusterssh/clusters
# Each non-comment line defines a named cluster: first token is the cluster name,
# remaining tokens are hostnames or references to other cluster names (inheritance).

{% for cluster in clusters %}
# {{ cluster.description }}
{{ cluster.name }} {{ cluster.hosts | join(' ') }}
{% endfor %}
