{
  "service_name": {
    "type": "string",
    "default": "cyborg-api",
    "description": "Service name for the init script (typically matches the package/service name)."
  },
  "service_description": {
    "type": "string",
    "default": "cyborg api",
    "description": "Short description of the service shown in LSB headers."
  },
  "project_description": {
    "type": "string",
    "default": "OpenStack Cyborg API",
    "description": "Human-readable description of the project/service."
  },
  "project_name": {
    "type": "string",
    "default": "cyborg",
    "description": "Project name used for paths, users, and group names."
  },
  "uwsgi_port": {
    "type": "number",
    "default": 8889,
    "description": "Port on which uWSGI will bind for HTTP(S) communication."
  },
  "uwsgi_ini_path": {
    "type": "string",
    "default": "/etc/cyborg/cyborg-api-uwsgi.ini",
    "description": "Path to the uWSGI INI configuration file."
  },
  "uwsgi_ini_app": {
    "type": "string",
    "default": "/usr/bin/cyborg-api",
    "description": "Path to the uWSGI application executable (script or binary)."
  }
}
