{
  "try_exec": {
    "type": "string",
    "default": "lxqt-policykit-agent",
    "description": "Path to the executable to try before launching the application."
  },
  "exec": {
    "type": "string",
    "default": "lxqt-policykit-agent",
    "description": "Command to execute for this desktop entry."
  },
  "only_show_in": {
    "type": "string",
    "default": "LXQt;",
    "description": "Semicolon-separated list of desktop environments where this application should appear."
  },
  "x_lxqt_module": {
    "type": "bool",
    "default": true,
    "description": "Indicator that this is an LXQt module."
  }
}
