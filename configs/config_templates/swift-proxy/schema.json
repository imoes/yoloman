{
  "project_name": {
    "type": "string",
    "default": "swift",
    "description": "The project name used for directory paths and user/group names."
  },
  "uwsgi_port": {
    "type": "number",
    "default": 8080,
    "description": "The port number for the uWSGI service."
  },
  "workers": {
    "type": "number",
    "default": 20,
    "description": "The number of pre-forked worker processes for the proxy-server."
  }
}
