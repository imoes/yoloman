{
  "project_name": {
    "type": "string",
    "default": "swift",
    "description": "The project name used for directory paths and user/group names."
  },
  "uwsgi_port": {
    "type": "number",
    "default": 8080,
    "description": "The port number for the uWSGI service."
  }
}
