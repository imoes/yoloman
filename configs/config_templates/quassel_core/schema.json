{
 "allow_remote": {
  "default": true,
  "description": "Allow clients to connect from remote hosts (set to false for local-only).",
  "type": "bool"
 },
 "auth_backend": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "daemon": {
  "default": true,
  "description": "Whether to run Quassel Core as a background daemon.",
  "type": "bool"
 },
 "db_driver": {
  "default": "SQLite",
  "description": "Database backend to use: SQLite, PostgreSQL, or MySQL.",
  "type": "string"
 },
 "db_host": {
  "default": "localhost",
  "description": "Database server hostname (for PostgreSQL/MySQL).",
  "type": "string"
 },
 "db_name": {
  "default": "quassel",
  "description": "Name of the database to use (PostgreSQL/MySQL).",
  "type": "string"
 },
 "db_path": {
  "default": "/var/lib/quassel/quassel-core.sqlite",
  "description": "Path to the SQLite database file (used only when DbDriver=SQLite).",
  "type": "string"
 },
 "db_port": {
  "default": 5432,
  "description": "Database server port (PostgreSQL default: 5432, MySQL default: 3306).",
  "type": "number"
 },
 "db_user": {
  "default": "quassel",
  "description": "Database user name (PostgreSQL/MySQL).",
  "type": "string"
 },
 "host": {
  "default": "0.0.0.0",
  "description": "IP address or hostname to bind the client listening port to.",
  "type": "string"
 },
 "idle_timeout": {
  "default": 300,
  "description": "Idle connection timeout in seconds (0 = no timeout).",
  "type": "number"
 },
 "log_level": {
  "default": 2,
  "description": "Logging verbosity: 0=Debug, 1=Info, 2=Warning, 3=Error.",
  "type": "number"
 },
 "max_buffer_count": {
  "default": 100,
  "description": "Maximum number of backlog lines to keep per channel/query (0 = unlimited).",
  "type": "number"
 },
 "max_buffered_bytes": {
  "default": 0,
  "description": "Maximum total bytes to store per buffer (0 = unlimited).",
  "type": "number"
 },
 "pid_file": {
  "default": "/run/quassel/quassel-core.pid",
  "description": "Path to the PID file for daemon mode.",
  "type": "string"
 },
 "port": {
  "default": 4242,
  "description": "TCP port for incoming client connections.",
  "type": "number"
 },
 "ssl_cert": {
  "default": "/etc/quassel/quassel-cert.pem",
  "description": "Path to the SSL certificate file (required if UseSSL=true).",
  "type": "string"
 },
 "ssl_key": {
  "default": "/etc/quassel/quassel-key.pem",
  "description": "Path to the SSL private key file (required if UseSSL=true).",
  "type": "string"
 },
 "use_ssl": {
  "default": false,
  "description": "Enable SSL/TLS for client connections (recommended for remote access).",
  "type": "bool"
 }
}
