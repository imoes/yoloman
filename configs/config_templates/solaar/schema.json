{
  "exec_command": {
    "type": "string",
    "default": "solaar --window=hide",
    "description": "The command to execute the Solaar application."
  },
  "icon_name": {
    "type": "string",
    "default": "solaar",
    "description": "The name of the icon file for the application."
  },
  "keywords": {
    "type": "string",
    "default": "logitech;unifying;receiver;mouse;keyboard;",
    "description": "Semicolon-separated keywords for the desktop entry."
  },
  "categories": {
    "type": "string",
    "default": "Utility;GTK;",
    "description": "Semicolon-separated categories for the desktop entry."
  }
}
