{
 "event_handler": {
  "default": [],
  "description": "List of event handlers to register. Each handler runs an external command on cluster events.\nNote: 'user' and 'group' directives are not supported by corosync_notifyd and have been removed per documentation.",
  "type": "list"
 },
 "items": {
  "description": "Configuration for a single event handler.",
  "type": "object"
 },
 "log_file_path": {
  "default": "/var/log/corosync/corosync_notifyd.log",
  "description": "Path to the log file when logging_target is 'file'",
  "type": "string"
 },
 "log_level": {
  "default": "warning",
  "description": "Minimum level of messages to log (debug, info, notice, warning, error, crit, alert, emerg)",
  "type": "string"
 },
 "log_timestamp_format": {
  "default": "iso8601",
  "description": "Format of timestamps in logs: 'none', 'date', 'full', or 'iso8601'",
  "type": "string"
 },
 "logging_target": {
  "default": "syslog",
  "description": "Where to send logs: 'syslog' for system logging, 'file' to write to a dedicated log file",
  "type": "string"
 },
 "properties": {
  "description": "Properties for a single event handler configuration.",
  "type": "object"
 }
}
