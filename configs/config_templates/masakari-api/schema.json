{
  "UWSGI_PORT": {
    "type": "number",
    "default": 15868,
    "description": "The port number on which the Masakari API UWSGI server listens."
  },
  "UWSGI_INI_PATH": {
    "type": "string",
    "default": "/etc/masakari/masakari-api-uwsgi.ini",
    "description": "Path to the UWSGI configuration file for the Masakari API."
  },
  "UWSGI_INI_APP": {
    "type": "string",
    "default": "/usr/bin/masakari-wsgi",
    "description": "Path to the Masakari WSGI application entry point script."
  }
}
