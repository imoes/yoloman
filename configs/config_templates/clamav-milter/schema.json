{
  "User": {
    "type": "string",
    "default": "clamav",
    "description": "User under which the milter runs"
  },
  "PidFile": {
    "type": "string",
    "default": "/var/run/clamav/clamav-milter.pid",
    "description": "Path to PID file"
  },
  "MilterSocket": {
    "type": "string",
    "default": "local:/var/run/clamav/clamav-milter.sock",
    "description": "Socket specification (e.g., 'local:/path/to/socket' or 'inet:port@host')"
  },
  "DataBaseDirectory": {
    "type": "string",
    "default": "/var/run/clamav",
    "description": "Directory where virus database files are stored"
  },
  "Foreground": {
    "type": "string",
    "default": "no",
    "description": "Whether to run in foreground mode (must be 'true'/'yes'/'1' to enable)\nenum: [true, yes, 1, false, no, 0]",
    "enum": [
      "true",
      "yes",
      "1",
      "false",
      "no",
      "0"
    ]
  },
  "Example": {
    "type": "string",
    "default": "",
    "description": "Internal flag indicating example config is still active\nenum: [true, yes, 1, false, no, 0]",
    "enum": [
      "true",
      "yes",
      "1",
      "false",
      "no",
      "0"
    ]
  }
}
