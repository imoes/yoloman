{
  "service_name": {
    "type": "string",
    "default": "squidguard",
    "description": "The name of the service for logrotate identification in comments."
  },
  "log_path": {
    "type": "string",
    "default": "/var/log/squidguard",
    "description": "The directory path where service log files are stored."
  },
  "rotation_frequency": {
    "type": "string",
    "default": "weekly",
    "description": "How often log files should be rotated."
  },
  "log_rotation_count": {
    "type": "number",
    "default": 5,
    "description": "Number of rotated log files to keep."
  },
  "log_permissions": {
    "type": "string",
    "default": "640",
    "description": "Permissions for newly created log files."
  },
  "log_owner": {
    "type": "string",
    "default": "proxy",
    "description": "Owner user for newly created log files."
  },
  "log_group": {
    "type": "string",
    "default": "proxy",
    "description": "Owner group for newly created log files."
  }
}
