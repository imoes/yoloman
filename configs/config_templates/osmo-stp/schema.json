{
  "logging_timestamp": {
    "type": "number",
    "default": 0,
    "description": "Whether to include timestamps in log messages (0 or 1)"
  },
  "logging_level": {
    "type": "string",
    "default": "notice",
    "description": "Default log level for all loggers"
  },
  "m3ua_listen_port": {
    "type": "number",
    "default": 2905,
    "description": "Port number for the M3UA listener"
  }
}
