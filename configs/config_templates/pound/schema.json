{
  "http_listen_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to bind HTTP listeners on. Use 0.0.0.0 to bind all interfaces."
  },
  "http_listen_port": {
    "type": "number",
    "default": 80,
    "description": "Port for HTTP traffic."
  },
  "https_listen_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to bind HTTPS listeners on. Use 0.0.0.0 to bind all interfaces."
  },
  "https_listen_port": {
    "type": "number",
    "default": 443,
    "description": "Port for HTTPS traffic."
  },
  "ssl_cert_path": {
    "type": "string",
    "default": "/etc/pound/ssl/server.pem",
    "description": "Path to the SSL certificate (in PEM format, optionally with private key)."
  },
  "ssl_ciphers": {
    "type": "string",
    "default": "HIGH:MEDIUM:!aNULL:!MD5",
    "description": "SSL cipher list (OpenSSL cipher string format). Leave empty to use defaults."
  },
  "client_header_timeout": {
    "type": "number",
    "default": 60,
    "description": "Maximum time (seconds) to wait for client to send request headers."
  },
  "client_body_timeout": {
    "type": "number",
    "default": 120,
    "description": "Maximum time (seconds) to wait for client to send request body."
  },
  "backend_read_timeout": {
    "type": "number",
    "default": 300,
    "description": "Maximum time (seconds) to wait for a response from backend."
  },
  "max_request_size_mb": {
    "type": "number",
    "default": 10,
    "description": "Maximum allowed size (MB) of an incoming request body."
  },
  "services": {
    "type": "list",
    "default": [],
    "description": "List of backend services to load balance.",
    "items": {
      "name": {
        "type": "string",
        "default": "default",
        "description": "Name/identifier for this service (used for logging and structure)."
      },
      "backends": {
        "type": "list",
        "default": [],
        "items": {
          "address": {
            "type": "string",
            "default": "127.0.0.1",
            "description": "Backend server IP or hostname."
          },
          "port": {
            "type": "number",
            "default": 8080,
            "description": "Backend server port."
          }
        }
      },
      "x_header": {
        "type": "number",
        "default": 0,
        "description": "Whether to add X-Forwarded-For header (1 = yes, 0 = no)."
      },
      "session_persistence": {
        "type": "number",
        "default": 0,
        "description": "Session persistence method: 0=none, 1=client IP, 2=cookie-based."
      },
      "dead_threshold": {
        "type": "number",
        "default": 0,
        "description": "Backend is marked dead after this many consecutive failed health checks (0 = disabled)."
      }
    }
  }
}
