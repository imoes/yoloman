{
  "PORT": {
    "type": "string",
    "default": "/dev/ttyACM0",
    "description": "Serial port device for the logger"
  },
  "DIR": {
    "type": "string",
    "default": "~",
    "description": "Default directory for log files"
  },
  "FNAME": {
    "type": "string",
    "default": "`date +%Y-%m-%d_%H:%M`",
    "description": "Base name pattern for log files; supports shell command substitution"
  }
}
