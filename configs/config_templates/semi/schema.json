{
  "flavor_name_default": {
    "type": "string",
    "default": "emacs",
    "description": "Default Emacs flavor name used when debian-emacs-flavor is not bound. Usually 'emacs' or 'emacs24'."
  }
}
