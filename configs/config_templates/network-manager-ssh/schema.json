{
  "root_user": {
    "type": "string",
    "default": "root",
    "description": "The system user allowed to own and send to the NetworkManager SSH D-Bus service."
  },
  "nm_ssh_service_prefix": {
    "type": "string",
    "default": "org.freedesktop.NetworkManager.ssh",
    "description": "The D-Bus service name prefix for NetworkManager SSH."
  },
  "default_policy_context": {
    "type": "string",
    "default": "default",
    "description": "The D-Bus policy context for default access rules."
  }
}
