{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable the Kerberos 5 connector plugin."
  },
  "realm": {
    "type": "string",
    "default": "EXAMPLE.EDU",
    "description": "Kerberos realm used for authentication (e.g., the institution's realm)."
  },
  "kdc": {
    "type": "string",
    "default": "kdc.example.edu",
    "description": "Hostname or IP address of the KDC server for the realm."
  },
  "admin_server": {
    "type": "string",
    "default": "admin.example.edu",
    "description": "Hostname or IP address of the Kerberos admin server (for password changes, etc.)."
  },
  "default_ttl": {
    "type": "number",
    "default": 3600,
    "description": "Default ticket lifetime in seconds."
  },
  "max_ttl": {
    "type": "number",
    "default": 86400,
    "description": "Maximum renewable ticket lifetime in seconds."
  },
  "encryption_types": {
    "type": "list",
    "default": [
      "aes256-cts-hmac-sha1-96",
      "aes128-cts-hmac-sha1-96"
    ],
    "description": "List of allowed encryption types (cipher suites), ordered by preference."
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Logging verbosity level (debug, info, warning, error)."
  },
  "krb5_conf": {
    "type": "string",
    "default": "/etc/krb5.conf",
    "description": "Path to the system Kerberos configuration file."
  },
  "keytab": {
    "type": "string",
    "default": "",
    "description": "Path to keytab file for service principal authentication. Leave blank if not used."
  },
  "service_principal": {
    "type": "string",
    "default": "HTTP/router@EXAMPLE.EDU",
    "description": "Kerberos service principal (used with keytab if provided)."
  },
  "ldap": {
    "type": "object",
    "default": {
      "enabled": true,
      "uris": [
        "ldap://ldap.example.edu",
        "ldaps://ldap2.example.edu"
      ],
      "base_dn": "dc=example,dc=edu",
      "bind_dn": "cn=admin,dc=example,dc=edu",
      "bind_password": "",
      "user_filter": "(&(objectClass=posixAccount)(krbPrincipalName=%s))",
      "group_filter": "(&(objectClass=posixGroup)(memberUid=%s))",
      "principal_attr": "krbPrincipalName",
      "username_attr": "uid",
      "group_attr": "memberUid"
    },
    "description": "LDAP integration configuration for group mapping.",
    "items": {
      "enabled": {
        "type": "bool",
        "default": true,
        "description": "Enable LDAP-based group/user mapping."
      },
      "uris": {
        "type": "list",
        "default": [
          "ldap://ldap.example.edu"
        ],
        "description": "LDAP server URIs (supports ldap:// or ldaps://)."
      },
      "base_dn": {
        "type": "string",
        "default": "dc=example,dc=edu",
        "description": "Base DN for LDAP searches."
      },
      "bind_dn": {
        "type": "string",
        "default": "cn=admin,dc=example,dc=edu",
        "description": "DN to bind to LDAP (leave empty for anonymous bind)."
      },
      "bind_password": {
        "type": "string",
        "default": "",
        "description": "Password for LDAP bind. Leave blank if anonymous."
      },
      "user_filter": {
        "type": "string",
        "default": "(&(objectClass=posixAccount)(krbPrincipalName=%s))",
        "description": "LDAP filter for user lookups. Use %s as placeholder for Kerberos principal."
      },
      "group_filter": {
        "type": "string",
        "default": "(&(objectClass=posixGroup)(memberUid=%s))",
        "description": "LDAP filter for group lookups. Use %s as placeholder for username."
      },
      "principal_attr": {
        "type": "string",
        "default": "krbPrincipalName",
        "description": "LDAP attribute storing Kerberos principal (e.g., krbPrincipalName)."
      },
      "username_attr": {
        "type": "string",
        "default": "uid",
        "description": "LDAP attribute storing the Unix username (e.g., uid)."
      },
      "group_attr": {
        "type": "string",
        "default": "memberUid",
        "description": "LDAP attribute listing group members (e.g., memberUid or member)."
      }
    }
  },
  "cache": {
    "type": "object",
    "default": {
      "enabled": true,
      "ttl": 600,
      "path": "/var/cache/krb5-connector"
    },
    "description": "Caching configuration for LDAP and Kerberos lookups.",
    "items": {
      "enabled": {
        "type": "bool",
        "default": true,
        "description": "Enable local caching of results."
      },
      "ttl": {
        "type": "number",
        "default": 600,
        "description": "Cache TTL in seconds."
      },
      "path": {
        "type": "string",
        "default": "/var/cache/krb5-connector",
        "description": "Directory path for cache storage."
      }
    }
  },
  "plugins": {
    "type": "object",
    "default": {},
    "description": "Plugin-specific configuration overrides (key-value map). Optional.",
    "items": {
      "type": "string"
    }
  }
}
