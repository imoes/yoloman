{
  "shibboleth_config_path": {
    "type": "string",
    "default": "/etc/shibboleth2.xml",
    "description": "Path to the Shibboleth SP configuration file."
  },
  "shibd_timeout": {
    "type": "number",
    "default": 30,
    "description": "Timeout for the shibd daemon in seconds."
  }
}
