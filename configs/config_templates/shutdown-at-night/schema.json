{
  "shell": {
    "type": "string",
    "default": "/bin/sh",
    "description": "The shell to use for cron jobs."
  },
  "path": {
    "type": "string",
    "default": "/sbin:/bin:/usr/sbin:/usr/bin",
    "description": "The PATH environment variable for cron jobs."
  },
  "shutdown_cron_hours": {
    "type": "string",
    "default": "16,17,18,19,20,21,22,23,0,1,2,3,4,5,6",
    "description": "Comma-separated list of hours when shutdown clients should run."
  },
  "wakeup_cron_minute": {
    "type": "string",
    "default": "6",
    "description": "The minute when wakeup clients should run."
  },
  "wakeup_cron_weekdays": {
    "type": "string",
    "default": "1,2,3,4,5",
    "description": "Comma-separated list of weekdays (0-7, where 0 and 7 are Sunday) when wakeup clients should run."
  }
}
