{
  "runuser": {
    "type": "string",
    "default": "quicktun",
    "description": "User account under which the quicktun daemon runs when precreation is enabled"
  },
  "iface": {
    "type": "string",
    "default": "quicktun.qt0",
    "description": "Name of the tunnel interface (used in daemon name)"
  }
}
