{
  "dbus_busconfig_root_user": {
    "type": "string",
    "default": "root",
    "description": "The user account with elevated policy permissions in the D-Bus configuration"
  },
  "dbus_busconfig_own_service_name": {
    "type": "string",
    "default": "com.lomiri.hfd",
    "description": "The D-Bus service name that the root user is allowed to own"
  },
  "dbus_busconfig_mandatory_context": {
    "type": "string",
    "default": "mandatory",
    "description": "The policy context level (e.g., 'mandatory' or 'default')"
  },
  "dbus_busconfig_send_destination": {
    "type": "string",
    "default": "com.lomiri.hfd",
    "description": "The D-Bus service name that messages may be sent to"
  },
  "dbus_busconfig_receive_interface": {
    "type": "string",
    "default": "com.lomiri.hfd",
    "description": "The D-Bus service interface name that messages may be received from"
  }
}
