{
  "roundcube_clean_db_schedule": {
    "type": "string",
    "default": "0 5 * * *",
    "description": "Cron schedule for database cleaning job."
  },
  "roundcube_clean_db_user": {
    "type": "string",
    "default": "www-data",
    "description": "User account under which the database cleaning job runs."
  },
  "roundcube_bin_path": {
    "type": "string",
    "default": "/usr/share/roundcube",
    "description": "Installation path of Roundcube binaries."
  },
  "roundcube_gc_schedule": {
    "type": "string",
    "default": "5,35 * * * *",
    "description": "Cron schedule for garbage collection job (purging sessions, caches, tempfiles)."
  },
  "roundcube_gc_user": {
    "type": "string",
    "default": "www-data",
    "description": "User account under which the garbage collection job runs."
  }
}
