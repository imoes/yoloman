{
  "command": {
    "type": "string",
    "default": "stdout",
    "description": "The subcommand to execute: stdout, json, prometheus, qemu, prometheus-push",
    "enum": [
      "stdout",
      "json",
      "prometheus",
      "qemu",
      "prometheus-push",
      "help"
    ]
  },
  "verbosity": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level. 0 for normal, increase with -v flags"
  },
  "no_header": {
    "type": "bool",
    "default": false,
    "description": "Do not print the header to standard output"
  },
  "sensor": {
    "type": "string",
    "default": "rapl",
    "description": "The sensor module to use to gather energy consumption metrics"
  },
  "sensor_buffer_per_domain_max_kb": {
    "type": "number",
    "default": 1,
    "description": "Maximum memory size in KB for storing energy consumption of each domain (RAPL only)"
  },
  "sensor_buffer_per_socket_max_kb": {
    "type": "number",
    "default": 1,
    "description": "Maximum memory size in KB for storing energy consumption of each socket (RAPL only)"
  },
  "vm": {
    "type": "bool",
    "default": false,
    "description": "Tell Scaphandre it is running in a virtual machine"
  },
  "prometheus_bind_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "Address to bind the Prometheus HTTP endpoint"
  },
  "prometheus_port": {
    "type": "number",
    "default": 9200,
    "description": "Port to expose the Prometheus HTTP endpoint"
  },
  "pushgateway_url": {
    "type": "string",
    "default": "http://localhost:9091",
    "description": "URL of the Prometheus Push Gateway"
  },
  "pushgateway_job": {
    "type": "string",
    "default": "scaphandre",
    "description": "Job name for pushing metrics to the Push Gateway"
  },
  "json_output_path": {
    "type": "string",
    "default": "",
    "description": "Path to write JSON metrics. Empty or omit for stdout"
  },
  "qemu_metrics_path": {
    "type": "string",
    "default": "/var/run/scaphandre/qemu-metrics",
    "description": "Folder to expose QEMU-KVM VM metrics"
  }
}
