{
  "kernel_modules": {
    "type": "list",
    "default": [
      "dm-crypt",
      "loop"
    ],
    "description": "List of kernel modules required by cryptmount (e.g., `dm-crypt`, `loop`)."
  }
}
