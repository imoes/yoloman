# Kernel modules required by cryptmount at runtime.
# These modules must be loaded before cryptmount can activate encrypted volumes.
# The list is derived from the kernel_modules variable.
{% for module in (kernel_modules | default(['dm-crypt', 'loop'])) %}{{ module }}
{% endfor %}