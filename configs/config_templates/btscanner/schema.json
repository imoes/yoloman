{
  "log_file": {
    "type": "string",
    "default": "/var/log/btscanner.log",
    "description": "Path to the log file"
  },
  "oui_file": {
    "type": "string",
    "default": "/usr/share/btscanner/oui.txt",
    "description": "Path to the OUI database file"
  },
  "store_file": {
    "type": "string",
    "default": "/var/lib/btscanner/store.db",
    "description": "Path to the store file where discovered device details are saved"
  },
  "rangedef_start": {
    "type": "string",
    "default": "",
    "description": "Start MAC address of the scan range"
  },
  "rangedef_end": {
    "type": "string",
    "default": "",
    "description": "End MAC address of the scan range"
  },
  "vulnerabilities": {
    "type": "string",
    "default": "",
    "description": "Comma-separated list of vulnerability checks to perform for the range"
  },
  "name": {
    "type": "bool",
    "default": false,
    "description": "Whether to query and record device names"
  },
  "hci": {
    "type": "bool",
    "default": false,
    "description": "Whether to query HCI information"
  },
  "rssi": {
    "type": "bool",
    "default": false,
    "description": "Whether to query RSSI values"
  },
  "lq": {
    "type": "bool",
    "default": false,
    "description": "Whether to query link quality"
  },
  "txpwr": {
    "type": "bool",
    "default": false,
    "description": "Whether to query transmit power"
  },
  "sdp": {
    "type": "bool",
    "default": false,
    "description": "Whether to perform SDP service discovery"
  }
}
