{
  "AUTH_MODULES": {
    "type": "list",
    "default": [
      "authuserdb",
      "authpam",
      "authldap"
    ],
    "description": "List of authentication modules to load (by basename, in order of preference). Each module corresponds to a .so file in the authdaemon directory.",
    "items": {
      "type": "string",
      "description": "Name of an auth module (e.g., 'authpam', 'authmysql', 'authldap', 'authuserdb')"
    }
  },
  "DEFAULT_AUTH_MODULE": {
    "type": "string",
    "default": "authpam",
    "description": "The default authentication module to use when no specific module is requested by a client."
  },
  "AUTH_LOG_FILE": {
    "type": "string",
    "default": "",
    "description": "Optional path to a dedicated auth log file. Leave empty to log only to syslog."
  }
}
