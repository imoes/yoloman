{
  "AUTH_MODULES": {
    "type": "list",
    "default": [
      "authuserdb",
      "authpam",
      "authldap"
    ],
    "description": "List of authentication modules to load (by basename, in order of preference). Each module corresponds to a .so file in the authdaemon directory."
  }
}
