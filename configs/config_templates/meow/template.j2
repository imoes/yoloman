# Meow configuration file for the `meow` service.
# This template is self-documenting: every directive includes its source schema description.

# How many cats to print (default: 1)
--count {{ count | default(1) }}

# Whether to use the literally flag. Are you literally this cat?
{% if literally | default(False) %}
--literally
{% endif %}