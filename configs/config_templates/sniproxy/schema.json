{
  "daemon_args": {
    "type": "string",
    "default": "-c /etc/sniproxy.conf",
    "description": "Command line arguments passed to the sniproxy daemon, typically specifying the config file path."
  }
}
