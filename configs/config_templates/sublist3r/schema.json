{
  "domain": {
    "type": "string",
    "default": "example.com",
    "description": "Domain name to enumerate its subdomains"
  },
  "bruteforce": {
    "type": "bool",
    "default": false,
    "description": "Enable the subbrute bruteforce module"
  },
  "ports": {
    "type": "list",
    "default": [
      80,
      443
    ],
    "description": "List of TCP ports to scan the found subdomains against"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbosity and display results in realtime"
  },
  "threads": {
    "type": "number",
    "default": 40,
    "description": "Number of threads to use for subbrute bruteforce"
  },
  "engines": {
    "type": "list",
    "default": [],
    "description": "Specify a comma-separated list of search engines (google, yahoo, bing, baidu, ask, netcraft, virustotal, threatcrowd, dnsdumpster, reversedns)"
  },
  "output": {
    "type": "string",
    "default": "",
    "description": "Save the results to this text file"
  },
  "no_color": {
    "type": "bool",
    "default": false,
    "description": "Output without color"
  }
}
