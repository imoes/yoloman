{
  "cluster_name_prefix": {
    "type": "string",
    "default": "client.radosgw.",
    "description": "The prefix used for radosgw instance sections in ceph.conf."
  },
  "default_user": {
    "type": "string",
    "default": "root",
    "description": "The default user to run radosgw as if not specified in ceph.conf."
  }
}
