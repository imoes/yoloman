{
  "options": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options to pass to acpid (as a string to be appended to the acpid command)"
  },
  "modules": {
    "type": "string",
    "default": "battery ac processor button fan thermal video",
    "description": "Space-separated list of kernel modules to load before starting acpid; use \"all\" to load all acpi drivers, or leave empty/commented to load none"
  }
}
