{
  "bind_ip": {
    "type": "string",
    "default": "",
    "description": "IPv4 address to bind to (leave empty to bind to all interfaces)."
  },
  "bind_ipv6": {
    "type": "string",
    "default": "",
    "description": "IPv6 address to bind to (leave empty to disable IPv6)."
  },
  "port": {
    "type": "number",
    "default": 7778,
    "description": "TCP port for plain-text IRC connections."
  },
  "ssl_port": {
    "type": "number",
    "default": null,
    "description": "TCP port for SSL/TLS-encrypted IRC connections."
  },
  "ssl_cert": {
    "type": "string",
    "default": "/usr/share/bip/bip.pem",
    "description": "Path to SSL certificate file."
  },
  "ssl_key": {
    "type": "string",
    "default": "/usr/share/bip/dh.pem",
    "description": "Path to SSL private key file."
  },
  "log_dir": {
    "type": "string",
    "default": "/var/log/bip",
    "description": "Directory where log files (IRC sessions, errors, etc.) are stored."
  },
  "log_level": {
    "type": "number",
    "default": 3,
    "description": "Logging verbosity: 1=error, 5=debug (higher = more verbose)."
  },
  "pid_file": {
    "type": "string",
    "default": "/run/bip/bip.pid",
    "description": "Path to PID file for process management."
  },
  "allow_plaintext": {
    "type": "bool",
    "default": true,
    "description": "Allow plaintext (non-SSL) connections to bip itself (not recommended for production)."
  },
  "users": {
    "type": "list",
    "default": [],
    "description": "List of IRC users (each with their own credentials, network defaults, and settings)."
  }
}
