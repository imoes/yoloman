{
  "version_file": {
    "type": "string",
    "default": "/usr/lib/debian-edu-install/version",
    "description": "Path to the version file used to check if the package is installed."
  },
  "firstboot_marker_file": {
    "type": "string",
    "default": "/etc/debian-edu/xdebian-edu-firstboot",
    "description": "Path to the marker file that prevents re-running the firstboot script."
  },
  "run_at_firstboot_tool": {
    "type": "string",
    "default": "/usr/share/debian-edu-config/tools/run-at-firstboot",
    "description": "Path to the run-at-firstboot tool from debian-edu-config."
  },
  "config_file": {
    "type": "string",
    "default": "/etc/debian-edu/config",
    "description": "Path to the Debian Edu config file containing TESTINSTALL variable."
  },
  "testsuite_runner": {
    "type": "string",
    "default": "/usr/share/debian-edu-install/debian-edu-testsuite",
    "description": "Path to the Debian Edu testsuite runner script."
  }
}
