{
  "chroot_dir": {
    "type": "string",
    "default": "/var/lib/dqcache/root",
    "description": "Base directory for chroot jail where dqcache runs in isolation"
  },
  "listen_addresses": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address(es) dqcache listens on for incoming DNS queries"
  },
  "cache_size_small": {
    "type": "number",
    "default": 10000000,
    "description": "Fixed-size cache in bytes (typically 10000000 = 10MB)"
  }
}
