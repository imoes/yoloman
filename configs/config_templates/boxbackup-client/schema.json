{
  "cron_minute": {
    "type": "string",
    "default": "0",
    "description": "Minute(s) when the sync cron job runs (0-59, * for any)"
  },
  "cron_hour": {
    "type": "string",
    "default": "0",
    "description": "Hour(s) when the sync cron job runs (0-23, * for any)"
  },
  "cron_day_of_month": {
    "type": "string",
    "default": "*",
    "description": "Day(s) of the month when the sync cron job runs (1-31, * for any)"
  },
  "cron_month": {
    "type": "string",
    "default": "*",
    "description": "Month(s) when the sync cron job runs (1-12, * for any)"
  },
  "cron_day_of_week": {
    "type": "string",
    "default": "*",
    "description": "Day(s) of the week when the sync cron job runs (0-7, where 0 and 7 are Sunday, * for any)"
  }
}
