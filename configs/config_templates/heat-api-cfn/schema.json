{
  "heat_api_cfn_service_name": {
    "type": "string",
    "default": "heat-api-cfn",
    "description": "The service name as reported in LSB header (e.g., 'heat-api-cfn')"
  },
  "heat_api_cfn_desc": {
    "type": "string",
    "default": "OpenStack Heat API CFN (heat-api-cfn)",
    "description": "Short human-readable description of the service"
  },
  "project_name": {
    "type": "string",
    "default": "heat",
    "description": "Project/OS service name (e.g., 'heat', 'nova', 'neutron')"
  },
  "heat_api_cfn_suffix": {
    "type": "string",
    "default": "api-cfn",
    "description": "Suffix appended to project_name to form the process name (e.g., 'api-cfn')"
  },
  "heat_api_cfn_uwsgi_port": {
    "type": "string",
    "default": "8000",
    "description": "Port uWSGI binds to for HTTP/HTTPS"
  },
  "heat_api_cfn_uwsgi_ini_path": {
    "type": "string",
    "default": "/etc/heat/heat-api-cfn-uwsgi.ini",
    "description": "Path to uWSGI configuration file"
  },
  "heat_api_cfn_uwsgi_ini_app": {
    "type": "string",
    "default": "/usr/bin/heat-wsgi-api-cfn",
    "description": "Path to uWSGI application script"
  },
  "SYSTEM_USER": {
    "type": "string",
    "default": "heat",
    "description": "System user under which the service runs"
  },
  "SYSTEM_GROUP": {
    "type": "string",
    "default": "heat",
    "description": "System group under which the service runs"
  },
  "CONFIG_FILE": {
    "type": "string",
    "default": "/etc/heat/heat.conf",
    "description": "Path to Heat's main configuration file"
  }
}
