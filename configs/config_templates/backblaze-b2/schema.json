{
  "account_id": {
    "type": "string",
    "default": "",
    "description": "Your Backblaze account ID (12-character hex string)"
  },
  "application_key": {
    "type": "string",
    "default": "",
    "description": "Your Backblaze application key (generated from the Backblaze dashboard)"
  },
  "default_bucket_name": {
    "type": "string",
    "default": "",
    "description": "Optional default bucket name for commands that require a bucket but do not specify one explicitly"
  },
  "proxy_url": {
    "type": "string",
    "default": "",
    "description": "Optional HTTP/HTTPS proxy URL (e.g., 'http://proxy.example.com:8080')"
  },
  "log_level": {
    "type": "string",
    "default": "INFO",
    "description": "Logging verbosity: DEBUG, INFO, WARNING, ERROR, or CRITICAL"
  }
}
