{
  "max_transfer_time": {
    "type": "number",
    "default": 5,
    "description": "Maximum time (in seconds) a transfer is allowed to take in the mock plugin."
  },
  "min_transfer_time": {
    "type": "number",
    "default": 5,
    "description": "Minimum time (in seconds) a transfer is allowed to take in the mock plugin."
  },
  "signals": {
    "type": "number",
    "default": 0,
    "description": "Number of signals to simulate during transfer in the mock plugin."
  }
}
