{
  "default_voice": {
    "type": "string",
    "default": "en",
    "description": "The default voice for the espeakup daemon (e.g., 'en', 'en-us', 'de', etc.). Leave empty for system default.",
    "enum": [
      "en",
      "it",
      "ru",
      "id",
      "be",
      "is"
    ]
  },
  "alsa_card": {
    "type": "string",
    "default": "",
    "description": "ALSA sound card identifier (card number or name). Leave empty to use default card."
  }
}
