{
  "chatty_exec": {
    "type": "string",
    "default": "chatty --gapplication-service",
    "description": "The command to execute for the Chatty daemon service."
  },
  "chatty_icon": {
    "type": "string",
    "default": "sm.puri.Chatty",
    "description": "The icon identifier for the Chatty desktop entry."
  },
  "chatty_comment": {
    "type": "string",
    "default": "SMS/MMS, Matrix and XMPP chat application (daemon mode)",
    "description": "Description comment for the Chatty desktop entry."
  }
}
