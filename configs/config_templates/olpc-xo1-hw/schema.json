{
  "prereqs": {
    "type": "list",
    "default": [],
    "description": "Space-separated list of prerequisite script names that must run before this one."
  },
  "mtd_modules": {
    "type": "list",
    "default": [
      "cafe_nand",
      "redboot",
      "jffs2",
      "ubifs",
      "lzo",
      "zlib",
      "zlib_deflate",
      "deflate"
    ],
    "description": "Space-separated list of MTD-related kernel modules to include."
  },
  "video_drivers": {
    "type": "list",
    "default": [
      "lxfb",
      "gxfb",
      "fbcon"
    ],
    "description": "Space-separated list of video frame buffer drivers to include."
  }
}
