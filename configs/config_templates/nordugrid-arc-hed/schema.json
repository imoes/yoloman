{
  "arc_location": {
    "type": "string",
    "default": "/usr",
    "description": "The base installation directory for ARC components."
  },
  "pid_file": {
    "type": "string",
    "default": "/run/arched.pid",
    "description": "Path to the PID file for the arched daemon."
  }
}
