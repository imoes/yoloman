{
  "sbin_path": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Path to the directory containing the executable binaries"
  },
  "program": {
    "type": "string",
    "default": "globus-job-manager",
    "description": "Name of the job manager executable"
  },
  "config_path": {
    "type": "string",
    "default": "/etc/globus/globus-gram-job-manager.conf",
    "description": "Path to the Globus GRS job manager configuration file"
  }
}
