# Path to the directory containing the executable binaries
# Default: /usr/sbin
# Description: Used to construct the full path to the Globus job manager program.
{{ sbin_path | default('/usr/sbin') }}/globus-job-manager -conf {{ config_path | default('/etc/globus/globus-gram-job-manager.conf') }} -type fork