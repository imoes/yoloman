{
  "init_user": {
    "type": "string",
    "default": "glewlwyd",
    "description": "System user under which the glewlwyd daemon runs."
  },
  "init_prefix": {
    "type": "string",
    "default": "/usr/bin",
    "description": "Installation prefix for glewlwyd binary."
  },
  "init_pidfile": {
    "type": "string",
    "default": "/var/run/glewlwyd.pid",
    "description": "Path to PID file used by init script."
  }
}
