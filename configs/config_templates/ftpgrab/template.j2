# ftpgrab configuration file (ftpgrabrc)
# One host per line: host:username:password:remote_path:local_path
{% for entry in ftpgrab_hosts %}{{ entry.host }}:{{ entry.username | default('anonymous') }}:{{ entry.password | default('') }}:{{ entry.remote_path }}:{{ entry.local_path }}{% endfor %}
