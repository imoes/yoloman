{
  "start_ekeyd": {
    "type": "string",
    "default": "YES",
    "description": "Set to 'YES' to allow ekeyd to start on boot, or 'NO' to prevent it.",
    "enum": [
      "YES",
      "NO"
    ]
  }
}
