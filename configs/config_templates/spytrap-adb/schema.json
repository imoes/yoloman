{
  "verbosity": {
    "type": "number",
    "default": 0,
    "description": "Log verbosity level. 0 is silent, higher values increase detail. Maps to repeated -v flags."
  },
  "start_adb_server": {
    "type": "string",
    "default": "auto",
    "description": "Whether to start the ADB server if needed. Valid values: auto, always, never.",
    "enum": [
      "auto",
      "always",
      "never"
    ]
  }
}
