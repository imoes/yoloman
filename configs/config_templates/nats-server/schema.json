{
  "host": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "The host to bind the NATS server to."
  },
  "port": {
    "type": "number",
    "default": 4222,
    "description": "The port to listen on for client connections."
  }
}
