{
  "args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments to pass to catimg; each item is a bare string argument or flag (e.g., '-c', '-w', '100', 'image.png'). The list must form a valid invocation compatible with catimg(1)."
  }
}
