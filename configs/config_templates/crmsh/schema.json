{
  "interactive": {
    "type": "bool",
    "default": false,
    "description": "Whether to start crmsh in interactive mode by default."
  },
  "editor": {
    "type": "string",
    "default": "vi",
    "description": "Editor used when editing cluster resources or configuration interactively."
  },
  "pager": {
    "type": "string",
    "default": "less",
    "description": "Pager used for multi-page command output (e.g., 'more', 'less')."
  },
  "shell": {
    "type": "string",
    "default": "sh",
    "description": "Default shell invoked by crmsh for command execution (e.g., 'sh', 'bash')."
  },
  "history": {
    "type": "bool",
    "default": true,
    "description": "Whether to persist command history to ~/.crm/shellhistory."
  },
  "timeout": {
    "type": "number",
    "default": 60,
    "description": "Timeout in seconds for interactive operations (e.g., resource operations)."
  },
  "noconfirm": {
    "type": "bool",
    "default": false,
    "description": "If true, skip confirmation prompts for dangerous operations (use with caution)."
  },
  "color": {
    "type": "bool",
    "default": true,
    "description": "Enable colored output in terminal sessions (requires terminal support)."
  },
  "backupdir": {
    "type": "string",
    "default": "/var/lib/pacemaker/backup",
    "description": "Directory where crmsh stores automatic backups of the CIB."
  },
  "plugins": {
    "type": "list",
    "default": [],
    "description": "List of crmsh plugins to load at startup. Each plugin may define its own options.",
    "items": {
      "name": {
        "type": "string",
        "description": "Name of the plugin (e.g., 'ganesha', 'sbd', 'nagios')."
      },
      "options": {
        "type": "object",
        "description": "Key-value configuration for the plugin (only for plugins that support runtime options)."
      }
    }
  },
  "cluster_name": {
    "type": "string",
    "default": "",
    "description": "Default cluster name to use when operating on a single-cluster system. Empty to require explicit specification."
  },
  "crm_cibsh": {
    "type": "string",
    "default": "live",
    "description": "CIB source: 'live' (default, current live config), 'file:/path/to/file.xml' (read-only from file), or 'xml' (raw XML input)."
  },
  "crm_logfile_level": {
    "type": "number",
    "default": 0,
    "description": "Log level for crmsh's internal logging (0\u20137, where 7 is most verbose). 0 disables extra logging."
  }
}
