{
  "logfile": {
    "type": "string",
    "default": "/var/log/cfingerd.log",
    "description": "Path to the cfingerd log file"
  },
  "header_display": {
    "type": "string",
    "default": "/etc/cfingerd/top_finger.txt",
    "description": "Path to the header display file"
  },
  "footer_display": {
    "type": "string",
    "default": "/etc/cfingerd/bottom_finger.txt",
    "description": "Path to the footer display file"
  },
  "no_name_banner": {
    "type": "string",
    "default": "/etc/cfingerd/noname_banner.txt",
    "description": "Path to the no-name banner file"
  },
  "no_user_banner": {
    "type": "string",
    "default": "/etc/cfingerd/nouser_banner.txt",
    "description": "Path to the no-user banner file"
  },
  "rejected_banner": {
    "type": "string",
    "default": "/etc/cfingerd/rejected_banner.txt",
    "description": "Path to the rejected banner file"
  },
  "finger_program": {
    "type": "string",
    "default": "/usr/sbin/userlist",
    "description": "Path to the local finger program"
  },
  "whois_program": {
    "type": "string",
    "default": "/usr/bin/whois",
    "description": "Path to the local whois program"
  }
}
