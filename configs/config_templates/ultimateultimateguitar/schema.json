{
  "transpose": {
    "type": "number",
    "default": 0,
    "description": "Number of semitones to transpose chords. Supports positive (up) or negative (down) integers."
  },
  "initial_url": {
    "type": "string",
    "default": null,
    "description": "URL of a specific song to load immediately. If not provided, the tool enters interactive search mode."
  }
}
