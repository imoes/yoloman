{
  "url": {
    "type": "string",
    "default": "",
    "description": "Initial URL to open; overridden by COG_URL environment variable"
  },
  "background_color": {
    "type": "string",
    "default": "white",
    "description": "Background color as CSS name or #RRGGBBAA hex"
  },
  "scale": {
    "type": "number",
    "default": 1.0,
    "description": "Zoom/scaling factor for Web content (default: 1.0)"
  },
  "device_scale": {
    "type": "number",
    "default": 1.0,
    "description": "Output device scaling factor (default: 1.0, 96 DPI)"
  },
  "doc_viewer": {
    "type": "bool",
    "default": false,
    "description": "Enable document viewer mode (reduces memory, limits network caching)"
  },
  "webprocess_failure": {
    "type": "string",
    "default": "error-page",
    "description": "Action on WebProcess failures: error-page, exit, exit-ok, restart",
    "enum": [
      "error-page",
      "exit",
      "exit-ok",
      "restart"
    ]
  },
  "platform": {
    "type": "string",
    "default": "",
    "description": "Platform plugin name to use (e.g., 'wl', 'x11')"
  },
  "platform_params": {
    "type": "list",
    "default": [],
    "description": "Comma-separated list of platform parameters"
  },
  "dir_handlers": {
    "type": "list",
    "default": [],
    "description": "List of URI scheme handlers for local directories; each entry maps a scheme to a filesystem path"
  },
  "web_extensions_dir": {
    "type": "string",
    "default": "",
    "description": "Path to directory containing Web Extensions"
  },
  "content_filter": {
    "type": "string",
    "default": "",
    "description": "Path to JSON file defining a content filter"
  }
}
