{
  "config_stub": {
    "type": "string",
    "default": "# No configuration file exists \u2014 this package provides only a GUI client.",
    "description": "Placeholder comment per Debian packaging; no config file is used by gyrus."
  }
}
