{
  "desc": {
    "type": "string",
    "default": "yubiserver",
    "description": "Short description of the software"
  },
  "name": {
    "type": "string",
    "default": "yubiserver",
    "description": "Short server's name"
  },
  "daemon": {
    "type": "string",
    "default": "/usr/bin/yubiserver",
    "description": "Path to the server's executable"
  }
}
