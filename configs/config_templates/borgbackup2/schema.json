{
  "archive_name": {
    "default": "daily-{now:%Y-%m-%d-%H%M%S}",
    "description": "Name pattern for the archive. May include strftime directives.",
    "type": "string"
  },
  "backup_sources": {
    "default": [],
    "description": "List of source paths/directories to back up.",
    "type": "list"
  },
  "compression": {
    "default": "lz4",
    "description": "Compression algorithm to use (e.g.,lz4,zstd,none,zlib,auto).",
    "enum": [
      "lz4",
      "zstd",
      "none",
      "zlib"
    ],
    "type": "string",
    "enum_open": true
  },
  "description": {
    "default": "",
    "description": "Optional description for the unit file comment.",
    "type": "string"
  },
  "enabled": {
    "default": true,
    "description": "Whether the borgbackup2 service is enabled.",
    "type": "bool"
  },
  "extra_args": {
    "default": [],
    "description": "Additional arguments passed to `borg create`. Example: ['--exclude-caches', '--one-file-system'].",
    "type": "list"
  },
  "prune_schedule": {
    "default": "",
    "description": "Systemd timer calendar expression (e.g., daily, weekly) for pruning. If empty, no pruning timer is created.",
    "type": "string"
  },
  "repository_encryption": {
    "default": false,
    "description": "If true, use BORG_PASSPHRASE to supply the passphrase (insecure for automation; prefer BORG_PASSCOMMAND).",
    "type": "bool"
  },
  "repository_passphrase": {
    "default": "",
    "description": "Passphrase for encrypted repositories. Only used if repository_encryption is true. WARNING: plaintext in config.",
    "type": "string"
  },
  "repository_url": {
    "description": "Repository URL (local path, ssh://, sftp://, rclone:, or (s3|b2): scheme).",
    "enum": [
      "/path/to/repo",
      "ssh://user@host:port//abs/path/to/repo",
      "ssh://user@host:port/rel/path/to/repo",
      "sftp://user@host:port//abs/path/to/repo",
      "sftp://user@host:port/rel/path/to/repo",
      "rclone:remote:path"
    ],
    "type": "string"
  },
  "syslog_identifier": {
    "default": "borgbackup2",
    "description": "Identifier used in syslog entries.",
    "type": "string"
  }
}
