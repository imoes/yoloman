{
  "plugin_load_add": {
    "type": "string",
    "default": "provider_lz4",
    "description": "The plugin to load for LZ4 compression support."
  },
  "provider_lz4": {
    "type": "string",
    "default": "force_plus_permanent",
    "description": "The loading mode for the LZ4 provider plugin.",
    "enum": [
      "force_plus_permanent",
      "default",
      "skip"
    ]
  }
}
