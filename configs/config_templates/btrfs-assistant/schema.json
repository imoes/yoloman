{
  "snapper_path": {
    "type": "string",
    "default": "/usr/bin/snapper",
    "description": "Absolute path to the snapper binary"
  },
  "bm_config_path": {
    "type": "string",
    "default": "/etc/default/btrfsmaintenance",
    "description": "Absolute path to the btrfsmaintenance configuration file"
  },
  "bm_refresh_script_path": {
    "type": "string",
    "default": "/usr/share/btrfsmaintenance/btrfsmaintenance-refresh-cron.sh",
    "description": "Absolute path to the btrfs maintenance cron refresh script"
  }
}
