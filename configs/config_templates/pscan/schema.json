{
  "source_dir": {
    "type": "string",
    "default": "/src",
    "description": "Directory containing C source code to scan for format string vulnerabilities"
  },
  "output_format": {
    "type": "string",
    "default": "text",
    "description": "Output format for pscan results: text, xml, or json"
  },
  "recursive": {
    "type": "bool",
    "default": true,
    "description": "Whether to recursively scan subdirectories"
  },
  "ignore_patterns": {
    "type": "list",
    "default": [],
    "description": "List of filename patterns to ignore during scanning (e.g., '*.h', 'third_party/*')\n      items: {type: string, default: \"\", description: \"Glob pattern for files to ignore\"}"
  },
  "max_depth": {
    "type": "number",
    "default": 0,
    "description": "Maximum recursion depth (0 for unlimited)"
  },
  "strict_mode": {
    "type": "bool",
    "default": false,
    "description": "Exit with error code 1 if any format string issues are found"
  }
}
