{
  "path_default": {
    "type": "string",
    "default": "$PATH:/sbin:/usr/sbin",
    "description": "Default PATH including sbin directories for service management commands."
  },
  "service_name": {
    "type": "string",
    "default": "lightdm",
    "description": "Name of the display manager service to restart."
  }
}
