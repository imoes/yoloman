{
  "data_source_name": {
    "type": "string",
    "default": "",
    "description": "PostgreSQL connection string. Example: 'postgresql://login:password@hostname:port/' or 'user=prometheus host=/run/postgresql dbname=postgres'"
  },
  "args": {
    "type": "string",
    "default": "",
    "description": "Command-line arguments to pass to the server. Escaping rules apply for backslashes and newlines."
  }
}
