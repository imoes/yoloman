# Kerberos administration ACL file
# Each line grants permissions for a principal to act as an administrator
# Format: principal permissions [database entries]
{% for entry in acl_entries %}{{ entry.principal }} {{ entry.permissions }}{% if entry.entries %} {{ entry.entries | join(', ') }}{% endif %}{% endfor %}