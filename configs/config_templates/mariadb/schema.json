{
  "datadir": {
    "type": "string",
    "default": "/var/lib/mysql",
    "description": "Directory where database files are stored."
  },
  "startup_timeout": {
    "type": "number",
    "default": 30,
    "description": "Maximum number of seconds to wait for the MariaDB server to start up during the 'start' operation."
  }
}
