{
  "SERVICE_NAME": {
    "type": "string",
    "default": "designate-worker",
    "description": "Name of the service (used in process naming, init script name, etc.)"
  },
  "PROJECT_NAME": {
    "type": "string",
    "default": "designate",
    "description": "OpenStack project name; used for system user/group, config paths, etc."
  }
}
