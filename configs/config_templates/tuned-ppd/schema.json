{
  "default": {
    "type": "string",
    "default": "balanced",
    "description": "The default TuneD profile to use."
  },
  "battery_detection": {
    "type": "bool",
    "default": true,
    "description": "Enable battery detection for profile switching."
  },
  "power_saver": {
    "type": "string",
    "default": "powersave",
    "description": "Profile name for power-saving mode."
  },
  "balanced": {
    "type": "string",
    "default": "balanced",
    "description": "Profile name for balanced performance mode."
  },
  "performance": {
    "type": "string",
    "default": "throughput-performance",
    "description": "Profile name for high-performance mode."
  },
  "balanced_battery": {
    "type": "string",
    "default": "balanced-battery",
    "description": "Profile name for balanced mode when on battery."
  }
}
