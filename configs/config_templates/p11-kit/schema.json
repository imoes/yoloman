{
  "remote_modules": {
    "type": "list",
    "default": [],
    "description": "List of remote PKCS#11 module definitions to expose or access via stdin/stdout. See pkcs11.conf(5) remote option syntax."
  },
  "trust_module_path": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu/pkcs11",
    "description": "Default path where PKCS#11 modules are located, used by trust extract and other commands."
  }
}
