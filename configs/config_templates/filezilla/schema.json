{
 "local_path": {
  "default": "",
  "description": "Path to set as local site. Required for -a option.",
  "type": "string"
 },
 "logontype": {
  "description": "Login type, either 'ask' or 'interactive'. Only valid with -l and a URL.",
  "enum": [
   "ask",
   "interactive"
  ],
  "type": "string"
 },
 "show_help": {
  "default": false,
  "description": "If true, displays help (-h).",
  "type": "bool"
 },
 "show_version": {
  "default": false,
  "description": "If true, displays version number (-v).",
  "type": "bool"
 },
 "site_path": {
  "default": "0",
  "description": "Complete site path from Site Manager (e.g., '0/foo/bar/sl\\\\ash'). Required for -c option.",
  "enum": [
   "0",
   "1"
  ],
  "type": "string"
 },
 "start_site_manager": {
  "default": false,
  "description": "If true, starts FileZilla with Site Manager opened (-s).",
  "type": "bool"
 },
 "url": {
  "default": "ftp",
  "description": "FTP URL to connect to (e.g., 'ftp://example.com/path'). Required for URL mode.",
  "enum": [
   "ftp",
   "ftps",
   "sftp"
  ],
  "type": "string"
 }
}
