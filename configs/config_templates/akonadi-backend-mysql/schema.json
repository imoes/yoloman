{
  "akonadi_mysql_innodb_buffer_pool_size": {
    "type": "string",
    "default": "8M",
    "description": "InnoDB buffer pool size \u2014 memory used to cache data and indexes. Adjust based on DB size."
  },
  "akonadi_mysql_max_connections": {
    "type": "string",
    "default": "256",
    "description": "Maximum simultaneous MySQL connections allowed. Increase if clients fail with 'Too many connections'."
  }
}
