# This file installs the postfix-pcre plugin into Postfix's shlib_directory.
# The format is: <library_path>:<type>:<owner>:<group>:<permissions>
# This specific entry installs postfix-pcre.so with root ownership and 755 permissions.

{{ shlib_directory | default('/usr/lib64/postfix') }}/postfix-pcre.so:f:root:-:{{ permissions | default(755) }}