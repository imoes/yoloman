{
  "ssh_service_type": {
    "type": "string",
    "default": "_ssh._tcp",
    "description": "mDNS service type for SSH (e.g., '_ssh._tcp')."
  },
  "ssh_port": {
    "type": "number",
    "default": 22,
    "description": "Port number for SSH service (typically 22)."
  }
}
