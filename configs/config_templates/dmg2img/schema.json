{
  "cmd": {
    "type": "object",
    "description": "Command-line parameters for dmg2img invocation. All fields are optional; only specified options appear in generated config file."
  },
  "cmd.input_dmg": {
    "type": "string",
    "default": "",
    "description": "Path to the input .dmg file (required by dmg2img unless omitted and inferred from output)"
  },
  "cmd.output_img": {
    "type": "string",
    "default": "",
    "description": "Path to the output .img file. If omitted, dmg2img names output <input_file>.img"
  },
  "cmd.silent": {
    "type": "bool",
    "default": false,
    "description": "Enable silent operation (suppress progress messages)"
  },
  "cmd.verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose operation"
  },
  "cmd.extremely_verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable extremely verbose operation"
  },
  "cmd.debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debugging output"
  }
}
