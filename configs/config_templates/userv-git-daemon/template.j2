# Log rotation configuration for git-daemon logs.
# Managed by Ansible role userv-git-daemon.

/var/log/git/userv-git-daemon.log {
	# Number of rotated log files to keep.
	# Default: 7
	rotate {{ rotate_count | default(7) }}
	daily
	missingok
	delaycompress
}