{
  "listeners": {
    "type": "list",
    "default": [],
    "description": "List of listeners to expose (port, optional bind address, protocol). Each defines a listening endpoint."
  },
  "allow_zero_length_clientid": {
    "type": "bool",
    "default": false,
    "description": "Allow clients to connect with an empty client ID (disables automatic client ID assignment)."
  },
  "allow_anonymous": {
    "type": "bool",
    "default": true,
    "description": "If true, permits connections without username/password authentication. Set to false to require auth."
  },
  "password_file": {
    "type": "string",
    "default": "/etc/mosquitto/passwd",
    "description": "Absolute path to the file containing usernames and hashed passwords (use mosquitto_passwd to create)."
  },
  "acl_file": {
    "type": "string",
    "default": "/etc/mosquitto/acl",
    "description": "Absolute path to the file defining access control lists (permissions per user or pattern)."
  },
  "persistence": {
    "type": "bool",
    "default": false,
    "description": "Enable disk persistence of subscriptions, retained messages, and in-flight messages."
  },
  "persistence_location": {
    "type": "string",
    "default": "/var/lib/mosquitto",
    "description": "Directory where persistence files are stored (must exist and be writable by mosquitto user)."
  },
  "max_inflight_messages": {
    "type": "number",
    "default": 20,
    "description": "Maximum number of QoS 1/2 messages in-flight per client (not yet acknowledged)."
  },
  "max_packet_size": {
    "type": "number",
    "default": 268435456,
    "description": "Maximum size (in bytes) of an incoming MQTT packet (set to 0 for no limit; recommended \u2264256MB)."
  },
  "connection_messages": {
    "type": "bool",
    "default": true,
    "description": "Log client connect/disconnect messages to the log."
  },
  "max_connections": {
    "type": "number",
    "default": -1,
    "description": "Maximum number of simultaneous client connections (\u22121 = unlimited)."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/mosquitto/mosquitto.log",
    "description": "Absolute path to the log file (requires proper permissions and directory)."
  },
  "log_type": {
    "type": "list",
    "default": [
      "error",
      "warning",
      "notice",
      "information"
    ],
    "description": "List of log message types to record (options: none, error, warning, notice, information, subscribe, unsubscribe, websockets, audit, debug)."
  },
  "log_timestamp": {
    "type": "bool",
    "default": true,
    "description": "Include ISO 8601 timestamps in log entries."
  },
  "tls_certfile": {
    "type": "string",
    "default": "/etc/mosquitto/certs/server.crt",
    "description": "Absolute path to the server\u2019s TLS certificate (PEM format)."
  },
  "tls_keyfile": {
    "type": "string",
    "default": "/etc/mosquitto/certs/server.key",
    "description": "Absolute path to the server\u2019s TLS private key (PEM format)."
  },
  "tls_ca_file": {
    "type": "string",
    "default": "",
    "description": "Absolute path to the CA certificate file (PEM format) for validating client certificates."
  },
  "tls_version": {
    "type": "string",
    "default": "tlsv1.2",
    "description": "Minimum TLS version to support (e.g., 'tlsv1.2', 'tlsv1.1')."
  }
}
