{
 "backend_options": {
  "description": "Backend-specific key-value options (e.g., for FBv5: 'Gamma=1.0', 'Invert=1').",
  "type": "object"
 },
 "general": {
  "default": {
   "backend": "FBv5",
   "device": "/dev/fb0",
   "flip_y": false,
   "font": "7x13",
   "height": 64,
   "update_interval": 500,
   "width": 128
  },
  "description": "Core LCD4Linux settings: which backend/driver to use, device path, display dimensions, font, update frequency, and orientation.",
  "type": "object"
 },
 "general.backend": {
  "default": "FBv5",
  "description": "LCD driver backend (e.g., FBv5, G15, G19, HD44780, LCD2USB, SerialLCD, USBHID, X11).",
  "type": "string"
 },
 "general.device": {
  "default": "/dev/fb0",
  "description": "Device path (e.g., framebuffer, serial port, USB device).",
  "type": "string"
 },
 "general.flip_y": {
  "default": false,
  "description": "Flip the display vertically (useful for inverted mounting).",
  "type": "bool"
 },
 "general.font": {
  "default": "7x13",
  "description": "Font name for text rendering (must be loadable by the backend).",
  "type": "string"
 },
 "general.height": {
  "default": 64,
  "description": "Display height in pixels (or lines).",
  "type": "number"
 },
 "general.update_interval": {
  "default": 500,
  "description": "Global update interval in milliseconds.",
  "type": "number"
 },
 "general.width": {
  "default": 128,
  "description": "Display width in pixels (or characters for text-only displays).",
  "type": "number"
 },
 "layout": {
  "default": {
   "extra_rows": [],
   "row1": "$date $time",
   "row2": "$uptime",
   "row3": "$load",
   "row4": "$cpu $mem"
  },
  "description": "Layout for each LCD line (rows 1–4). For 4-line displays, rows 5–8 are optional.",
  "type": "object"
 },
 "layout.extra_rows": {
  "default": [],
  "description": "Optional rows 5–8 (list of up to 4 strings). Only used on 8-line displays.",
  "items": {
   "description": "Content for an extra line.",
   "type": "string"
  },
  "type": "list"
 },
 "layout.row1": {
  "default": "$date $time",
  "description": "Content for the first line (supports widgets like $date, $time, $hostname, etc.).",
  "type": "string"
 },
 "layout.row2": {
  "default": "$uptime",
  "description": "Content for the second line.",
  "type": "string"
 },
 "layout.row3": {
  "default": "$load",
  "description": "Content for the third line.",
  "type": "string"
 },
 "layout.row4": {
  "default": "$cpu $mem",
  "description": "Content for the fourth line.",
  "type": "string"
 },
 "widgets": {
  "default": [],
  "description": "List of widget definitions. Each widget provides dynamic data to the display (e.g., CPU, memory, network).",
  "items": {
   "align": {
    "default": "left",
    "description": "Text alignment: left, center, or right.",
    "type": "string"
   },
   "bgcolor": {
    "default": null,
    "description": "Background color in hex or name.",
    "type": "string"
   },
   "color": {
    "default": null,
    "description": "Text/foreground color in hex or name (e.g., #FFFFFF, white).",
    "type": "string"
   },
   "driver": {
    "default": "",
    "description": "Backend driver name (e.g., FBv5, X11) if widget is driver-specific.",
    "type": "string"
   },
   "font": {
    "default": null,
    "description": "Override default font for this widget.",
    "type": "string"
   },
   "format": {
    "default": null,
    "description": "Format string (e.g., '%.1f%%' for numbers).",
    "type": "string"
   },
   "height": {
    "default": null,
    "description": "Height in pixels (optional).",
    "type": "number"
   },
   "name": {
    "description": "Unique widget identifier (used as [Widget.name] section header).",
    "type": "string"
   },
   "oneline": {
    "default": null,
    "description": "Force single-line display (e.g., for scrollable text).",
    "type": "bool"
   },
   "source": {
    "default": null,
    "description": "Data source (e.g., file path, command, or internal variable like 'cpu.usage').",
    "type": "string"
   },
   "threshold": {
    "default": null,
    "description": "Threshold (e.g., for alerting bars/graphs).",
    "type": "number"
   },
   "type": {
    "default": "Text",
    "description": "Widget type (e.g., Text, Bar, Icon, Graph).",
    "type": "string"
   },
   "update_interval": {
    "default": null,
    "description": "Widget-specific update interval (ms), overriding the global one.",
    "type": "number"
   },
   "width": {
    "default": null,
    "description": "Width in pixels/characters (optional, depends on widget type).",
    "type": "number"
   },
   "x": {
    "default": null,
    "description": "Absolute X position in pixels.",
    "type": "number"
   },
   "y": {
    "default": null,
    "description": "Absolute Y position in pixels.",
    "type": "number"
   }
  },
  "type": "list"
 }
}
