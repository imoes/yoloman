{
  "input_file": {
    "type": "string",
    "default": "input.bin",
    "description": "Path to the PKWARE-compressed input file to decompress."
  },
  "output_file": {
    "type": "string",
    "default": "output.bin",
    "description": "Path to write the decompressed output data."
  },
  "input_offset": {
    "type": "number",
    "default": 0,
    "description": "Optional byte offset in input file to start decompression from."
  }
}
