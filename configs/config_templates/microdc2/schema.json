{
  "nickname": {
    "type": "string",
    "default": "MicroDC User",
    "description": "The nickname used to identify the client on Direct Connect hubs."
  },
  "description": {
    "type": "string",
    "default": "Linux Direct Connect Client",
    "description": "A short description of the user/client."
  },
  "realname": {
    "type": "string",
    "default": "",
    "description": "Real name of the user."
  },
  "email": {
    "type": "string",
    "default": "",
    "description": "Email address of the user."
  },
  "slots": {
    "type": "number",
    "default": 1,
    "description": "Number of slots for uploading files."
  },
  "max_download": {
    "type": "number",
    "default": 5,
    "description": "Maximum number of simultaneous downloads."
  },
  "max_upload": {
    "type": "number",
    "default": 5,
    "description": "Maximum number of simultaneous uploads."
  },
  "shared": {
    "type": "string",
    "default": "/home/user/shared",
    "description": "Path to the shared directory."
  },
  "filelist_refresh_interval": {
    "type": "number",
    "default": 60,
    "description": "Refresh interval for local filelist in seconds. 0 for slave mode."
  },
  "startup_commands": {
    "type": "list",
    "default": [],
    "description": "List of commands to run on startup (e.g., connect to hub). Items are flat strings."
  }
}
