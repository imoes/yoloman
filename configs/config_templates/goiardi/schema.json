{
  "web_addr": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address to bind the web server to (e.g., '127.0.0.1', '::')."
  },
  "web_port": {
    "type": "number",
    "default": 4545,
    "description": "Port number for the Chef API server."
  },
  "log_level": {
    "type": "string",
    "default": "warning",
    "description": "Minimum log level to emit. Options are 'debug', 'info', 'warning', 'error', and 'critical'. May be specified on the command line with one or more -V flags.",
    "enum": [
      "debug",
      "info",
      "warning",
      "error",
      "critical"
    ]
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/goiardi/goiardi.log",
    "description": "Path to the log file; leave empty to log to stdout/stderr only."
  },
  "use_ssl": {
    "type": "boolean",
    "default": false,
    "description": "Enable TLS/SSL for secure Chef API communication."
  },
  "mysql_host": {
    "type": "string",
    "default": "localhost",
    "description": "MySQL server hostname or IP address."
  },
  "mysql_port": {
    "type": "number",
    "default": 3306,
    "description": "MySQL server port."
  },
  "mysql_db": {
    "type": "string",
    "default": "goiardi_test",
    "description": "MySQL database name."
  },
  "mysql_user": {
    "type": "string",
    "default": "foo",
    "description": "MySQL username."
  },
  "mysql_password": {
    "type": "string",
    "default": "s3kr1t",
    "description": "MySQL password (leave empty to use socket auth or keyring)."
  },
  "postgresql_host": {
    "type": "string",
    "default": "localhost",
    "description": "PostgreSQL server hostname or IP address."
  },
  "postgresql_port": {
    "type": "number",
    "default": 5432,
    "description": "PostgreSQL server port."
  },
  "postgresql_db": {
    "type": "string",
    "default": "mydb",
    "description": "PostgreSQL database name."
  },
  "postgresql_user": {
    "type": "string",
    "default": "foo",
    "description": "PostgreSQL username."
  },
  "postgresql_password": {
    "type": "string",
    "default": "s3kr1t",
    "description": "PostgreSQL password."
  }
}
