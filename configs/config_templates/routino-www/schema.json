{
  "package_name": {
    "type": "string",
    "default": "routino-www",
    "description": "Name of the package for which the cron job is configured."
  },
  "cleanup_schedule": {
    "type": "string",
    "default": "0 3 * * *",
    "description": "Cron schedule expression for the cleanup job."
  },
  "cleanup_user": {
    "type": "string",
    "default": "root",
    "description": "User under which the cron job runs."
  },
  "cleanup_dir": {
    "type": "string",
    "default": "/var/lib/routino/results",
    "description": "Directory whose files are cleaned up."
  },
  "cleanup_days": {
    "type": "number",
    "default": 7,
    "description": "Number of days ago to find files for deletion."
  }
}
