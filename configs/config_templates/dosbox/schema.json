{
 "autoexec_commands": {
  "default": [],
  "description": "List of commands to execute in [autoexec] section at startup (e.g., 'mount c /home/user/games', 'c:').",
  "type": "list"
 },
 "dos_dosdir": {
  "default": "C",
  "description": "DOS directory for temporary files (e.g., 'C', 'Z').",
  "enum": [
   "C",
   "Z"
  ],
  "type": "string"
 },
 "dos_himem": {
  "default": false,
  "description": "Enable HIMEM.SYS for extended memory.",
  "type": "bool"
 },
 "dos_hma": {
  "default": true,
  "description": "Enable High Memory Area (HMA).",
  "type": "bool"
 },
 "dosbox_language": {
  "description": "Language file to use (e.g., 'english'). Empty string uses default.",
  "enum": [
   "danish",
   "dutch",
   "finnish",
   "french",
   "german",
   "hungarian",
   "italian",
   "korean",
   "norwegian",
   "persian",
   "polish",
   "portuguese",
   "russian",
   "spanish",
   "turkish"
  ],
  "type": "string"
 },
 "dosbox_machine": {
  "default": "svga_s3",
  "description": "Emulated machine type. Valid: hercules, cga, tandy, pcjr, ega, vgaonly, svga_s3, svga_et3000, svga_et4000, svga_paradise, vesa_nolfb, vesa_oldvbe.",
  "enum": [
   "hercules",
   "cga",
   "tandy",
   "pcjr",
   "ega",
   "vgaonly",
   "svga_s3",
   "svga_et3000",
   "svga_et4000",
   "svga_paradise",
   "vesa_nolfb",
   "vesa_oldvbe"
  ],
  "type": "string"
 },
 "dosbox_memsize": {
  "default": 16,
  "description": "Amount of emulated memory in MB.",
  "type": "number"
 },
 "midi_device": {
  "default": "",
  "description": "MIDI device path or identifier (e.g., '/dev/midi0'). Empty string leaves unset.",
  "type": "string"
 },
 "midi_type": {
  "default": "",
  "description": "MIDI device type (e.g., 'mt32', 'cm32l', 'opl2', 'opl3', 'sblast', 'none'). Empty string leaves unset.",
  "type": "string"
 },
 "mixer_blocksize": {
  "default": 1024,
  "description": "Mixer block size in bytes.",
  "type": "number"
 },
 "mixer_nosound": {
  "default": false,
  "description": "Disable sound output (e.g., for headless servers).",
  "type": "bool"
 },
 "mixer_rate": {
  "default": 44100,
  "description": "Mixer sample rate in Hz.",
  "type": "number"
 },
 "mouse_emulate": {
  "default": true,
  "description": "Whether to emulate a mouse for DOS programs.",
  "type": "bool"
 },
 "mouse_focused": {
  "default": true,
  "description": "Whether mouse is captured when window has focus.",
  "type": "bool"
 },
 "renderer_forcescaler": {
  "description": "Forced scaler, even if it might not fit (e.g., 'none', 'xbrz'). Empty string leaves unset.",
  "enum": [
   "none",
   "opengl"
  ],
  "type": "string"
 },
 "renderer_scaler": {
  "description": "Graphical scaler to use (e.g., 'none', 'normal2x', 'advinterp2x'). Empty string leaves unset.",
  "enum": [
   "none",
   "opengl"
  ],
  "type": "string"
 },
 "serial_ports": {
  "default": [],
  "description": "Serial port configurations. Each item maps a port index to its type. Valid types: 'dummy', 'nullmodem', 'direct', 'file', 'real', 'none'. Use only necessary ports to avoid conflicts.",
  "type": "list"
 }
}
