{
  "default_path": {
    "type": "string",
    "default": "/usr/sbin:/sbin:/usr/bin:/bin",
    "description": "Default PATH for the initialization script."
  },
  "run_path": {
    "type": "string",
    "default": "/run",
    "description": "Path where runit creates its control files."
  }
}
