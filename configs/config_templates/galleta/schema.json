{
  "delimiter": {
    "type": "string",
    "default": "\t",
    "description": "Field delimiter to use (single character); overrides default TAB"
  },
  "cookie_file": {
    "type": "string",
    "default": "",
    "description": "Path to the MSIE cookie file to parse"
  }
}
