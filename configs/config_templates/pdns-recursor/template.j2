# This file is managed by Ansible. Do not edit manually.
#
# This template generates the log rotation or parsing configuration for pdns-recursor.
# It matches the standard pdns_recursor log line format and extracts the stats
# message using the `stats_output` pattern.

# Log rotation/parsing configuration for pdns_recursor.
# The `stats_output` variable defines the literal string match pattern for
# the stats logging content. In the context of a log rotation or parsing config,
# this is the literal string match pattern.

^\w{3} [ :0-9]{11} [._[:alnum:]-]+ pdns_recursor\":[0-9]+\": stats: {{ stats_output | default('stats: *') }}