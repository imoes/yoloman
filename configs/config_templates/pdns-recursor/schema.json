{
  "stats_output": {
    "type": "string",
    "default": "stats: *",
    "description": "The log message content for stats logging. In the context of a log rotation or parsing config, this is the literal string match pattern."
  }
}
