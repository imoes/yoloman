{
  "dvd_device": {
    "type": "string",
    "default": "/dev/sr0",
    "description": "The path to the block device of the DVD drive."
  },
  "dvd_region": {
    "type": "string",
    "default": "1",
    "description": "The region code to set the drive to (1-6) or 'ALL'.",
    "enum": [
      "1",
      "2",
      "3",
      "4",
      "5",
      "6"
    ]
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output from the regionset utility."
  },
  "force_change": {
    "type": "bool",
    "default": false,
    "description": "Automatically apply region changes without interactive confirmation."
  }
}
