{
  "BYOBU_BACKEND": {
    "type": "string",
    "default": "tmux",
    "description": "Backend to use; currently supported values are \"screen\" or \"tmux\"",
    "enum": [
      "screen",
      "tmux"
    ]
  }
}
