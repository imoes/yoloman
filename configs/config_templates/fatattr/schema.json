{
  "files_attrs": {
    "type": "list",
    "default": [],
    "description": "List of file-attribute pairs; each item is a map with 'file' (path) and 'attrs' (list of attribute modifiers like '+a', '-h') \u2014 matching fatattr(1) syntax."
  }
}
