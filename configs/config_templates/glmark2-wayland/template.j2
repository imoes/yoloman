{# glmark2-wayland command-line arguments for benchmark execution #}
{# args: List of command-line arguments to pass to glmark2-wayland; each item should be a string representing one complete argument or argument group #}
{% for arg in (args | default([])) %}{{ arg }}
{% endfor %}