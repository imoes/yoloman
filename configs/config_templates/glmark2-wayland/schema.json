{
  "args": {
    "type": "list",
    "default": [],
    "description": "List of command-line arguments to pass to glmark2-wayland; each item should be a string representing one complete argument or argument group (e.g., '-b shading', '--fullscreen') as per documented options."
  }
}
