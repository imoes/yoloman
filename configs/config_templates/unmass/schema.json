{
  "archive_file": {
    "type": "string",
    "default": "/var/lib/unmass/default_archive.lgp",
    "description": "The path to the game archive file to be processed by unmass."
  },
  "output_dir": {
    "type": "string",
    "default": "/var/lib/unmass/output",
    "description": "The directory where extracted files will be placed."
  },
  "files_to_extract": {
    "type": "list",
    "default": [],
    "description": "A list of specific filenames or patterns within the archive to extract. If empty, the whole archive is extracted."
  },
  "verbose": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level for the unmass operation."
  }
}
