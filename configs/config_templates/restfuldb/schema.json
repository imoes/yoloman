{
  "document_root": {
    "type": "string",
    "default": "/usr/share/restfuldb",
    "description": "The document root directory for the restfuldb application."
  },
  "ssl_reneg_buffer_size": {
    "type": "number",
    "default": 13107200,
    "description": "The buffer size for SSL renegotiation to allow large file uploads."
  },
  "auth_user_file": {
    "type": "string",
    "default": "/etc/restfuldb/htpasswd",
    "description": "The path to the htpasswd file used for basic authentication."
  }
}
