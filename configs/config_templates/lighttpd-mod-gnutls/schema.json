{
  "ssl_pemfile": {
    "type": "string",
    "default": "/etc/lighttpd/server.pem",
    "description": "Path to the SSL PEM certificate and key file."
  },
  "ssl_bind_address": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "The IP address to bind the SSL listener to."
  },
  "ssl_bind_port": {
    "type": "number",
    "default": 443,
    "description": "The port number to bind the SSL listener to."
  }
}
