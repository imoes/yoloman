{
  "tprot_display_filter": {
    "type": "string",
    "default": "t-prot -cmekatlS --max-lines=250 --bigq --pgp-short --pgp-move-vrf -Mmutt -L/etc/t-prot/footers -A/etc/t-prot/ads",
    "description": "The display_filter string used to invoke t-prot with its default options. Set to '' to disable TOFU protection by default."
  }
}
