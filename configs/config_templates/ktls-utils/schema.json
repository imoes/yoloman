{
  "loglevel": {
    "type": "number",
    "default": 0,
    "description": "Debug log level for general ktls-utils operations."
  },
  "tls_debug": {
    "type": "number",
    "default": 0,
    "description": "Debug log level for TLS operations."
  },
  "nl_debug": {
    "type": "number",
    "default": 0,
    "description": "Debug log level for Netlink operations."
  },
  "keyrings": {
    "type": "string",
    "default": "",
    "description": "Semicolon-separated list of keyrings to use for authentication."
  },
  "x509_truststore_client": {
    "type": "string",
    "default": "",
    "description": "Path to the X.509 truststore for client authentication."
  },
  "x509_certificate_client": {
    "type": "string",
    "default": "",
    "description": "Path to the X.509 certificate for client authentication."
  },
  "x509_private_key_client": {
    "type": "string",
    "default": "",
    "description": "Path to the private key for client authentication."
  },
  "x509_truststore_server": {
    "type": "string",
    "default": "",
    "description": "Path to the X.509 truststore for server authentication."
  },
  "x509_certificate_server": {
    "type": "string",
    "default": "",
    "description": "Path to the X.509 certificate for server authentication."
  },
  "x509_private_key_server": {
    "type": "string",
    "default": "",
    "description": "Path to the private key for server authentication."
  }
}
