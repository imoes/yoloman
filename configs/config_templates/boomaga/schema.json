{
  "service_type": {
    "type": "string",
    "default": "oneshot",
    "description": "Service type for systemd; boomaga(1) is a one-shot backend"
  },
  "remain_after_exit": {
    "type": "bool",
    "default": false,
    "description": "Whether to keep service marked as active after ExecStart completes"
  },
  "exec_start": {
    "type": "string",
    "default": "/usr/bin/boomaga -v",
    "description": "Command to invoke boomaga; -v enables verbose output for debugging"
  },
  "cups_server": {
    "type": "string",
    "default": "localhost",
    "description": "CUPS server address; boomaga operates locally by default"
  },
  "wanted_by": {
    "type": "string",
    "default": "printers.target",
    "description": "Systemd target that should start this service; boomaga integrates with CUPS printing subsystem"
  }
}
