{
  "font_family": {
    "type": "string",
    "default": "Courier New",
    "description": "The default font family used for plain text and RTF documents."
  },
  "font_size": {
    "type": "number",
    "default": 12,
    "description": "The default font size in points."
  },
  "text_color": {
    "type": "string",
    "default": "black",
    "description": "The default text color."
  },
  "bg_color": {
    "type": "string",
    "default": "white",
    "description": "The default background color."
  },
  "auto_save_interval": {
    "type": "number",
    "default": 300,
    "description": "Auto-save interval in seconds. Set to 0 to disable."
  },
  "valid_extensions": {
    "type": "list",
    "default": [
      ".txt",
      ".rtf"
    ],
    "description": "List of file extensions recognized as valid text documents."
  },
  "word_wrap": {
    "type": "bool",
    "default": true,
    "description": "Whether word wrap is enabled by default."
  },
  "line_endings": {
    "type": "list",
    "default": [
      "LF",
      "CRLF"
    ],
    "description": "List of supported line ending styles."
  }
}
