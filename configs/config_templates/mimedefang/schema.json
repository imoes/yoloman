{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable the mimedefang mail filter."
  },
  "socket_path": {
    "type": "string",
    "default": "/var/spool/sendmail/mimedefang.sock",
    "description": "Path to the Sendmail milter socket."
  },
  "user": {
    "type": "string",
    "default": "defang",
    "description": "Unix user under which mimedefang runs."
  },
  "group": {
    "type": "string",
    "default": "defang",
    "description": "Unix group under which mimedefang runs."
  },
  "num_workers": {
    "type": "number",
    "default": 4,
    "description": "Number of worker processes to spawn for scanning messages."
  },
  "max_message_size": {
    "type": "number",
    "default": 0,
    "description": "Maximum message size in bytes (0 = unlimited)."
  },
  "scan_timeout": {
    "type": "number",
    "default": 300,
    "description": "Maximum time in seconds allowed for scanning a single message."
  },
  "temp_dir": {
    "type": "string",
    "default": "/var/spool/mimedefang",
    "description": "Directory for temporary files created during scanning."
  },
  "perl_lib_paths": {
    "type": "list",
    "default": [
      "/usr/lib/perl5/site_perl"
    ],
    "description": "List of directories containing Perl modules used by mimedefang filters.",
    "items": {
      "type": "string"
    }
  },
  "action_replace_subject_prefix": {
    "type": "string",
    "default": "[SPAM]",
    "description": "Prefix added to subject line when message is tagged as spam."
  },
  "action_quarantine_dir": {
    "type": "string",
    "default": "/var/spool/mimedefang/quarantine",
    "description": "Directory where quarantined messages are stored."
  },
  "action_replace_sender": {
    "type": "string",
    "default": "postmaster@localhost",
    "description": "Sender address used for replacement notifications (e.g., spam rejections)."
  },
  "action_replace_recipients": {
    "type": "string",
    "default": "postmaster@localhost",
    "description": "Recipient address used for replacement notifications."
  },
  "action_quarantine_compress": {
    "type": "bool",
    "default": false,
    "description": "Whether to compress quarantined messages with gzip."
  },
  "log_facility": {
    "type": "string",
    "default": "mail",
    "description": "Syslog facility to use (e.g., mail, local0)."
  },
  "log_level": {
    "type": "number",
    "default": 6,
    "description": "Syslog verbosity level (0\u20137; higher = more verbose)."
  },
  "log_syslog": {
    "type": "bool",
    "default": true,
    "description": "Log messages to syslog."
  },
  "log_stderr": {
    "type": "bool",
    "default": false,
    "description": "Log messages to standard error (useful for debugging)."
  },
  "filters": {
    "type": "list",
    "default": [],
    "description": "List of custom filter definitions to load.",
    "items": {
      "name": {
        "type": "string",
        "description": "Unique name for the filter."
      },
      "type": {
        "type": "string",
        "enum": [
          "message",
          "recipient"
        ],
        "description": "Whether the filter applies to the whole message or individual recipients."
      },
      "path": {
        "type": "string",
        "description": "Path to the Perl filter script or module."
      },
      "args": {
        "type": "list",
        "items": {
          "type": "string"
        },
        "optional": true,
        "description": "Optional space-separated arguments passed to the filter script."
      }
    }
  },
  "whitelist_dir": {
    "type": "string",
    "optional": true,
    "description": "Directory containing whitelisted sender addresses (one per line)."
  },
  "blacklist_dir": {
    "type": "string",
    "optional": true,
    "description": "Directory containing blacklisted sender addresses (one per line)."
  },
  "block_executables": {
    "type": "bool",
    "default": true,
    "description": "Automatically block messages containing executable file types."
  },
  "block_executable_extensions": {
    "type": "list",
    "default": [
      "exe",
      "bat",
      "com",
      "pif",
      "scr",
      "cmd",
      "reg",
      "msi",
      "vbs",
      "js",
      "wsf",
      "jse",
      "ps1"
    ],
    "description": "List of file extensions considered executable and to be blocked.",
    "items": {
      "type": "string"
    }
  },
  "use_spamassassin": {
    "type": "bool",
    "default": false,
    "description": "Enable SpamAssassin integration."
  },
  "spamassassin_socket": {
    "type": "string",
    "optional": true,
    "description": "Path to SpamAssassin's spamd socket (e.g., unix:/var/run/spamd.sock)."
  },
  "spamassassin_user": {
    "type": "string",
    "default": "spamd",
    "description": "Unix user under which spamd runs."
  },
  "spamassassin_timeout": {
    "type": "number",
    "default": 60,
    "description": "Timeout in seconds for SpamAssassin checks."
  }
}
