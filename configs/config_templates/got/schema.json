{
  "remotes": {
    "type": "list",
    "default": [],
    "description": "List of remote repositories configured for got(1). Each remote must specify at least name and repository path. If branch entries are omitted, the remote will use the default fetch behavior based on the repository's HEAD reference. All fields are strings per got.conf(5) syntax."
  }
}
