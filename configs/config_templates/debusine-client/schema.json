{
  "allow_dcut": {
    "type": "bool",
    "default": false,
    "description": "Whether to allow DCUT (Debian Copy-Under Test) builds."
  },
  "codenames": {
    "type": "string",
    "default": "debian",
    "description": "Comma-separated list of Debian codenames the client should consider valid."
  },
  "valid_commands": {
    "type": "list",
    "default": [],
    "description": "List of valid commands for the debusine client."
  }
}
