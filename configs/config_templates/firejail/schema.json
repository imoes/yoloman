{
  "include_allow_bin_sh_local": {
    "type": "string",
    "default": "allow-bin-sh.local",
    "description": "Path (relative or absolute) to the .local include file for persistent customization."
  }
}
