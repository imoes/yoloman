{
  "interface": {
    "type": "string",
    "default": "eth0",
    "description": "Network interface to monitor (e.g., eth0, wlan0)"
  },
  "delay": {
    "type": "number",
    "default": 1,
    "description": "Delay between screen updates in seconds. Must be between 1 and 300."
  },
  "mode": {
    "type": "string",
    "default": "classic",
    "description": "Default graph mode: classic, split, or large.",
    "enum": [
      "classic",
      "split",
      "large"
    ]
  },
  "hostname_in_title": {
    "type": "bool",
    "default": false,
    "description": "Add hostname to the title bar."
  },
  "led": {
    "type": "bool",
    "default": false,
    "description": "Enable TX/RX activity LEDs."
  },
  "graph_style": {
    "type": "string",
    "default": "classic",
    "description": "Select the initial graph view: classic, split, or large.",
    "enum": [
      "classic",
      "split",
      "large"
    ]
  },
  "zero_counters": {
    "type": "bool",
    "default": false,
    "description": "Use virtually zero traffic counters instead of values stored in kernel."
  }
}
