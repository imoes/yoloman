{
  "listen_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address TinyProxy binds to. Use 0.0.0.0 to listen on all interfaces (not recommended for public exposure)."
  },
  "port": {
    "type": "number",
    "default": 8888,
    "description": "TCP port for incoming proxy requests."
  },
  "hostname": {
    "type": "string",
    "default": "",
    "description": "Optional hostname override. If empty, system hostname is used."
  },
  "log_level": {
    "type": "string",
    "default": "Info",
    "description": "Logging verbosity. 'Info' or 'Notice' recommended for production."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/tinyproxy/tinyproxy.log",
    "description": "Path to log file. Use '/dev/stdout' to log to standard output (e.g., systemd)."
  },
  "max_clients": {
    "type": "number",
    "default": 100,
    "description": "Maximum concurrent client connections. Increase for high-traffic deployments."
  },
  "min_spare_servers": {
    "type": "number",
    "default": 1,
    "description": "Minimum idle child processes to keep ready for new requests."
  },
  "max_spare_servers": {
    "type": "number",
    "default": 5,
    "description": "Maximum idle child processes before terminating some."
  },
  "start_servers": {
    "type": "number",
    "default": 3,
    "description": "Number of child processes to spawn at startup."
  },
  "timeout": {
    "type": "number",
    "default": 600,
    "description": "Connection and request timeout in seconds. 600 = 10 minutes."
  },
  "reverse_proxy": {
    "type": "boolean",
    "default": false,
    "description": "Enable reverse proxy mode (not typical for general-purpose proxy servers)."
  },
  "transparent": {
    "type": "boolean",
    "default": false,
    "description": "Run in transparent mode \u2014 does not add Via header. Use only for local network transparency."
  },
  "access_rules": {
    "type": "list",
    "default": [
      {
        "type": "allow",
        "source": "127.0.0.1"
      },
      {
        "type": "deny",
        "source": "all"
      }
    ],
    "description": "List of IP access control rules. Order matters: first match wins.",
    "items": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "allow",
            "deny"
          ],
          "description": "Rule action."
        },
        "source": {
          "type": "string",
          "description": "IP address, CIDR range, or 'all'."
        }
      },
      "required": [
        "type",
        "source"
      ]
    }
  },
  "auth_enabled": {
    "type": "boolean",
    "default": false,
    "description": "Enable basic HTTP authentication. If true, TinyProxy will require username/password in requests."
  },
  "upstream_proxies": {
    "type": "list",
    "default": [],
    "description": "Chain proxy configuration. Useful if upstream network requires a proxy.",
    "items": {
      "type": "object",
      "properties": {
        "protocol": {
          "type": "string",
          "enum": [
            "http",
            "socks4",
            "socks5"
          ],
          "description": "Upstream proxy protocol."
        },
        "host": {
          "type": "string",
          "description": "Upstream proxy hostname or IP."
        },
        "port": {
          "type": "number",
          "description": "Port of upstream proxy."
        }
      },
      "required": [
        "protocol",
        "host",
        "port"
      ]
    }
  },
  "no_proxy": {
    "type": "list",
    "default": [
      "localhost",
      "127.0.0.1"
    ],
    "description": "Domains or IPs to bypass the proxy and connect directly to.",
    "items": {
      "type": "string",
      "description": "Domain, hostname, or IP address."
    }
  },
  "user_agent": {
    "type": "string",
    "default": "",
    "description": "Custom User-Agent header. Empty uses TinyProxy default (e.g., 'TinyProxy/1.10.0')."
  },
  "hide_info_header": {
    "type": "boolean",
    "default": true,
    "description": "If true, hide X-TinyProxy HTTP header to reduce fingerprinting."
  },
  "filter_ports": {
    "type": "boolean",
    "default": true,
    "description": "Block connections to ports commonly used by malicious services (e.g., 25, 119, 135, 445, etc.)."
  },
  "filtered_port_range": {
    "type": "string",
    "default": "1-65535",
    "description": "Port range to block if filter_ports=true. TinyProxy has built-in defaults; this is rarely changed."
  },
  "ssl_support": {
    "type": "boolean",
    "default": false,
    "description": "Enable HTTPS proxy support. Requires valid SSL certificate and key."
  },
  "ssl_port": {
    "type": "number",
    "default": 8889,
    "description": "Port to listen on for HTTPS proxy requests (if ssl_support=true)."
  },
  "ssl_cert": {
    "type": "string",
    "default": "/etc/tinyproxy/cert.pem",
    "description": "Path to SSL certificate file (PEM format) for HTTPS proxy."
  }
}
