{
  "package_name": {
    "type": "string",
    "default": "systraq",
    "description": "The name of the package/service for cron job comments and command references."
  },
  "system_user": {
    "type": "string",
    "default": "debian-systraq",
    "description": "The system user account under which the cron jobs should run."
  },
  "config_dir": {
    "type": "string",
    "default": "/etc/systraq",
    "description": "The directory where the systraq configuration files are located."
  },
  "data_dir": {
    "type": "string",
    "default": "/var/lib/systraq",
    "description": "The directory where systraq stores its runtime data and backups."
  }
}
