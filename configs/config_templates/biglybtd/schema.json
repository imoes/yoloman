{
  "jwt_gui_users": {
    "type": "string",
    "default": "put_username_here",
    "description": "Comma-separated list of usernames allowed to show the JWT UI (e.g., 'alice,bob')."
  },
  "runas_user": {
    "type": "string",
    "default": "biglybt",
    "description": "System user account the BiglyBT daemon runs as (must match the systemd service configuration)."
  }
}
