{
  "bz2_compressor": {
    "type": "string",
    "default": "lbzip2 -n2",
    "description": "Compressor command and options for bz2 format"
  },
  "gz_compressor": {
    "type": "string",
    "default": "pigz -p2",
    "description": "Compressor command and options for gz format"
  },
  "lz_compressor": {
    "type": "string",
    "default": "plzip -n2",
    "description": "Compressor command and options for lz format"
  },
  "xz_compressor": {
    "type": "string",
    "default": "pixz -p2",
    "description": "Compressor command and options for xz format"
  },
  "zst_compressor": {
    "type": "string",
    "default": "zstd -T2",
    "description": "Compressor command and options for zst format"
  }
}
