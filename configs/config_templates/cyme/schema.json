{
  "output_lsusb": {
    "type": "bool",
    "default": false,
    "description": "Enable lsusb-compatible output (corresponds to -l/--lsusb)"
  },
  "output_tree": {
    "type": "bool",
    "default": false,
    "description": "Dump device hierarchy as a tree (corresponds to -t/--tree)"
  },
  "output_blocks": {
    "type": "list",
    "default": [],
    "description": "Blocks to display for each device (e.g., 'name', 'vendor-id', 'product-id'); corresponds to multiple -b/--blocks options"
  },
  "output_bus_blocks": {
    "type": "list",
    "default": [],
    "description": "Blocks to display for each bus; corresponds to --bus-blocks"
  },
  "output_config_blocks": {
    "type": "list",
    "default": [],
    "description": "Blocks to display for each configuration; corresponds to --config-blocks"
  },
  "output_interface_blocks": {
    "type": "list",
    "default": [],
    "description": "Blocks to display for each interface; corresponds to --interface-blocks"
  },
  "output_endpoint_blocks": {
    "type": "list",
    "default": [],
    "description": "Blocks to display for each endpoint; corresponds to --endpoint-blocks"
  },
  "output_headings": {
    "type": "bool",
    "default": false,
    "description": "Show block headings (corresponds to --headings)"
  },
  "output_no_padding": {
    "type": "bool",
    "default": false,
    "description": "Disable padding for aligned blocks (corresponds to --no-padding)"
  },
  "output_sort_devices": {
    "type": "string",
    "default": "device-number",
    "description": "Sort devices by (device-number|branch-position|no-sort); corresponds to --sort-devices\nPossible values:\ndevice-number: Sort by bus device number\nbranch-position: Sort by position in parent branch\nno-sort: No sorting; whatever order it was parsed",
    "enum": [
      "device-number",
      "branch-position",
      "no-sort"
    ]
  },
  "output_sort_buses": {
    "type": "bool",
    "default": false,
    "description": "Sort devices by bus number. If using any sort-devices other than no-sort, this happens automatically (corresponds to --sort-buses)"
  },
  "output_group_devices": {
    "type": "string",
    "default": "no-group",
    "description": "Group devices by value when listing (corresponds to --group-devices)\nPossible values:\nno-group: No grouping\nbus: Group into buses with bus info as heading - like a flat tree",
    "enum": [
      "no-group",
      "bus"
    ]
  },
  "output_hide_buses": {
    "type": "bool",
    "default": false,
    "description": "Hide empty buses when printing tree; those with no devices (corresponds to --hide-buses)"
  },
  "output_hide_hubs": {
    "type": "bool",
    "default": false,
    "description": "Hide empty hubs when printing tree; those with no devices. When listing will hide hubs regardless of whether empty or not (corresponds to --hide-hubs)"
  },
  "output_list_root_hubs": {
    "type": "bool",
    "default": false,
    "description": "Show root hubs when listing; Linux only (corresponds to --list-root-hubs)"
  },
  "filter_name": {
    "type": "string",
    "default": "",
    "description": "Filter on string contained in name (corresponds to --filter-name)"
  },
  "filter_serial": {
    "type": "string",
    "default": "",
    "description": "Filter on string contained in serial (corresponds to --filter-serial)"
  },
  "filter_class": {
    "type": "string",
    "default": "",
    "description": "Filter on USB class code (corresponds to --filter-class)\nPossible values:\nuse-interface-descriptor: Device class is unspecified, interface descriptors are used to determine needed drivers\naudio: Speaker, microphone, sound card, MIDI\ncdc-communications: The modern serial interface; appears as a UART/RS232 port on most systems\nhid: Human Interface Device; game controllers, keyboards, mice etc. Also commonly used as a device data interface rather then creating something from scratch\nphysical: Force feedback joystick\nimage: Still imaging device; scanners, cameras\nprinter: Laser printer, inkjet printer, CNC machine\nmass-storage: Mass storage devices (MSD): USB flash drive, memory card reader, digital audio player, digital camera, external drive\nhub: High speed USB hub\ncdc-data: Used together with class 02h (Communications and CDC Control) above\nsmart-card: USB smart card reader\ncontent-security: Fingerprint reader\nvideo: Webcam\npersonal-healthcare: Pulse monitor (watch)\naudio-video: Webcam, TV\nbillboard: Describes USB-C alternate modes supported by device\nusb-type-c-bridge: An interface to expose and configure the USB Type-C capabilities of Connectors on USB Hubs or Alternate Mode Adapters\nbdp: This base class is defined for devices that conform to the \u201cVESA USB BDP Device Specification\u201d found at the VESA website. This specification defines the usable set of SubClass and Protocol values. Values outside of this defined spec are reserved. These class codes can only be used in Interface Descriptors\nmctp: This base class is defined for devices that conform to the \u201cMCTP over USB\u201d found at the DMTF website as DSP0283. This specification defines the usable set of SubClass and Protocol values. Values outside of this defined spec are reserved. These class codes can only be used in Interface Descriptors\ni3c-device: An interface to expose and configure I3C function within a USB device to allow interaction between host software and the I3C device, to drive transaction on the I3C bus to/from target devices\ndiagnostic: Trace and debugging equipment\nwireless-controller: Wireless controllers: Bluetooth adaptors, Microsoft RNDIS\nmiscellaneous: This base class is defined for miscellaneous device definitions. Some matching SubClass and Protocols are defined on the USB-IF website\napplication-specific-interface: This base class is defined for devices that conform to several class specifications found on the USB-IF website\nvendor-specific-class: This base class is defined for vendors to use as they please",
    "enum": [
      "use-interface-descriptor",
      "audio",
      "cdc-communications",
      "hid",
      "physical",
      "image",
      "printer",
      "mass-storage",
      "hub",
      "cdc-data",
      "smart-card",
      "content-security",
      "video",
      "personal-healthcare",
      "audio-video",
      "billboard",
      "usb-type-c-bridge",
      "bdp",
      "mctp",
      "i3c-device",
      "diagnostic",
      "wireless-controller",
      "miscellaneous",
      "application-specific-interface",
      "vendor-specific-class"
    ]
  },
  "show": {
    "type": "string",
    "default": "",
    "description": "Show only devices with specified device and/or bus numbers (in decimal) in format [[bus]:][devnum] (corresponds to -s/--show)"
  },
  "vidpid": {
    "type": "string",
    "default": "",
    "description": "Show only devices with the specified vendor and product ID numbers (in hexadecimal) in format VID:[PID] (corresponds to -d/--vidpid)"
  },
  "device": {
    "type": "string",
    "default": "",
    "description": "Selects which device lsusb will examine - supplied as Linux /dev/bus/usb/BBB/DDD style path (corresponds to -D/--device)"
  },
  "output_verbose": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level (repeat provides count): 1 prints device configurations; 2 prints interfaces; 3 prints interface endpoints; 4 prints everything and more blocks (corresponds to -v/--verbose)"
  },
  "output_decimal": {
    "type": "bool",
    "default": false,
    "description": "Show base16 values as base10 decimal instead (corresponds to --decimal)"
  },
  "output_color": {
    "type": "string",
    "default": "always",
    "description": "Output coloring mode (corresponds to --color)\nPossible values:\nauto: Show colours if the output goes to an interactive console\nalways: Always apply colouring to the output\nnever: Never apply colouring to the output",
    "enum": [
      "auto",
      "always",
      "never"
    ]
  },
  "output_encoding": {
    "type": "string",
    "default": "glyphs",
    "description": "Output character encoding (corresponds to --encoding)\nPossible values:\nglyphs: Use UTF-8 private use area characters such as those used by NerdFont to show glyph icons\nutf8: Use only standard UTF-8 characters for the output; no private use area glyph icons\nascii: Use only ASCII characters for the output; 0x00 - 0x7F (127 chars)",
    "enum": [
      "glyphs",
      "utf8",
      "ascii"
    ]
  },
  "output_icon": {
    "type": "string",
    "default": "auto",
    "description": "When to print icon blocks (corresponds to --icon)\nPossible values:\nauto: Show icon blocks if the [`Encoding`] supports icons matched in the [`icon::IconTheme`]\nalways: Always print icon blocks if included in configured blocks\nnever: Never print icon blocks",
    "enum": [
      "auto",
      "always",
      "never"
    ]
  },
  "output_more": {
    "type": "bool",
    "default": false,
    "description": "Print more blocks by default at each verbosity (corresponds to -m/--more)"
  },
  "mask_serials": {
    "type": "string",
    "default": "hide",
    "description": "Mask serial numbers with '*' or random chars (corresponds to --mask-serials)\nPossible values:\nhide: Hide with '*' char\nscramble: Mask by randomising existing chars\nreplace: Mask by replacing length with random chars",
    "enum": [
      "hide",
      "scramble",
      "replace"
    ]
  },
  "output_force_libusb": {
    "type": "bool",
    "default": false,
    "description": "Force pure libusb profiler on macOS rather than combining system_profiler output. Has no effect on other platforms or when using nusb (corresponds to -F/--force-libusb)"
  },
  "output_system_profiler": {
    "type": "bool",
    "default": false,
    "description": "Use the system_profiler command on macOS to get USB data. If not using nusb this is the default for macOS, merging with libusb data for verbose output. nusb uses IOKit directly so does not use system_profiler by default (corresponds to --system-profiler)"
  },
  "output_json_output": {
    "type": "bool",
    "default": false,
    "description": "Output as json format after sorting, filters and tree settings are applied; without -tree will be flattened dump of devices (corresponds to --json)"
  },
  "output_json_input": {
    "type": "string",
    "default": "",
    "description": "Read from json output rather than profiling system (corresponds to --from-json)"
  },
  "config_file": {
    "type": "string",
    "default": "/etc/cyme.toml",
    "description": "Path to user config file to use for custom icons, colours and default settings (corresponds to -c/--config)"
  },
  "output_debug": {
    "type": "number",
    "default": 0,
    "description": "Turn debugging information on. Alternatively can use RUST_LOG env: INFO, DEBUG, TRACE (corresponds to -z/--debug)"
  }
}
