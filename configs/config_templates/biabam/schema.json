{
  "subject": {
    "type": "string",
    "default": "",
    "description": "Mail subject line (optional)"
  },
  "recipients": {
    "type": "list",
    "default": [],
    "description": "List of email recipients (comma-separated in rendered config)"
  },
  "attachments": {
    "type": "list",
    "default": [],
    "description": "List of attachment filenames (comma-separated in rendered config)"
  },
  "body": {
    "type": "string",
    "default": "",
    "description": "Optional inline body text (if omitted, stdin is used)"
  }
}
