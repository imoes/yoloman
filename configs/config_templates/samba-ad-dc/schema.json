{
  "SAMBAOPTIONS": {
    "type": "string",
    "default": "",
    "description": "Extra command-line options passed to the samba daemon (e.g., from /etc/default/samba). Keep empty unless specific tuning is required."
  }
}
