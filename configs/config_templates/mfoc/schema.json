{
  "default_keys": {
    "type": "list",
    "default": [
      "000000000000",
      "a0a1a2a3a4a5",
      "b0b1b2b3b4b5"
    ],
    "description": "Comma-separated list of 16-character hex keys to try initially in addition to the default keys."
  },
  "output_file": {
    "type": "string",
    "default": "/var/lib/mfoc/card_dump.bin",
    "description": "File path where the cracked card contents and keys will be dumped."
  },
  "probe_limit": {
    "type": "number",
    "default": 20,
    "description": "Number of times to probe each sector (corresponds to -P option)."
  },
  "tolerance": {
    "type": "number",
    "default": 20,
    "description": "Half the range for a possible distance tolerance (corresponds to -T option)."
  }
}
