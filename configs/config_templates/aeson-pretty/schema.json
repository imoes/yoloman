{
  "indent": {
    "type": "number",
    "default": 4,
    "description": "Number of spaces per nesting-level (affects pretty-print formatting); must be \u2265 0"
  },
  "sort": {
    "type": "bool",
    "default": false,
    "description": "Whether to sort object keys alphabetically before output"
  },
  "compact": {
    "type": "bool",
    "default": false,
    "description": "Whether to produce compact JSON output (removes whitespace)"
  }
}
