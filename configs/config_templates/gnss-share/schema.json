{
  "socket": {
    "type": "string",
    "default": "/var/run/gnss-share.sock",
    "description": "Path to the Unix domain socket used to send NMEA location data."
  },
  "group": {
    "type": "string",
    "default": "geoclue",
    "description": "Group to set as owner for the socket."
  },
  "device_driver": {
    "type": "string",
    "default": "stm",
    "description": "GPS device driver to use. Supported values: stm, stm_serial, mm.",
    "enum": [
      "stm",
      "stm_serial",
      "mm"
    ]
  },
  "device_path": {
    "type": "string",
    "default": "/dev/gnss0",
    "description": "Path to GPS device to use."
  },
  "device_baud_rate": {
    "type": "number",
    "default": 9600,
    "description": "Baud rate for GPS serial device."
  },
  "agps_directory": {
    "type": "string",
    "default": "/var/cache/gnss-share",
    "description": "Directory to load/store almanac and ephemeris data."
  }
}
