{
  "exec_command": {
    "type": "string",
    "default": "clipit",
    "description": "Command to execute the ClipIt clipboard manager"
  },
  "terminal": {
    "type": "bool",
    "default": false,
    "description": "Whether to run in a terminal window"
  },
  "only_show_in": {
    "type": "string",
    "default": "GNOME;XFCE;LXDE;Unity;",
    "description": "Desktop environments in which this entry should be shown"
  },
  "autostart_enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether ClipIt should start automatically on desktop session launch"
  }
}
