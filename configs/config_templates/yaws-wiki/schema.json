{
  "ebin_dir": {
    "type": "string",
    "default": "/usr/lib/yaws-wiki/ebin",
    "description": "Path to the directory where wiki beam code is placed."
  },
  "port": {
    "type": "number",
    "default": 8080,
    "description": "Port number for the yaws wiki virtual server."
  },
  "listen": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "IP address to listen on for the yaws wiki virtual server."
  },
  "docroot": {
    "type": "string",
    "default": "/var/lib/yaws-wiki/www",
    "description": "Document root directory for the yaws wiki virtual server."
  }
}
