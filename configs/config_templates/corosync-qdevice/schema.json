{
  "COROSYNC_QDEVICE_OPTIONS": {
    "type": "string",
    "default": "foreground",
    "description": "Options passed to the corosync-qdevice command at startup",
    "enum": [
      "debug",
      "foreground",
      "help"
    ]
  }
}
