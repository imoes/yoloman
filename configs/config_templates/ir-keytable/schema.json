{
  "ir_keytable_sysfs_device": {
    "type": "string",
    "default": "rc0",
    "description": "The sysfs device name for the IR receiver (e.g., rc0, rc1) used in the -s argument example."
  },
  "ir_keytable_keymaps_dir": {
    "type": "string",
    "default": "/etc/rc_keymaps",
    "description": "The default directory where IR keymap files are stored if not specified in the config."
  }
}
