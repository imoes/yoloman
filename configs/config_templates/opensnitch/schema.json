{
  "pid_dir": {
    "type": "string",
    "default": "/var/run/opensnitchd",
    "description": "Directory where the daemon's PID file is stored"
  },
  "daemon.rules_path": {
    "type": "string",
    "default": "/etc/opensnitchd/rules",
    "description": "Path to the rules directory used by opensnitchd"
  }
}
