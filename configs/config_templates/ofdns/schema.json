{
  "class": {
    "type": "string",
    "default": "IN",
    "description": "The DNS class to query (defaults to IN)"
  },
  "server": {
    "type": "string",
    "default": "",
    "description": "The server to query (IP address or hostname)"
  },
  "types": {
    "type": "list",
    "default": [
      "AAAA",
      "A"
    ],
    "description": "The record type(s) to query"
  },
  "use_tcp": {
    "type": "bool",
    "default": false,
    "description": "Force using TCP for the query"
  },
  "domains": {
    "type": "list",
    "default": [],
    "description": "List of domain names to query"
  }
}
