{
  "lockfile_path": {
    "type": "string",
    "default": "/var/run/htdig.cron",
    "description": "Path to the lockfile used by the cron script to prevent concurrent indexing runs."
  },
  "default_config_file": {
    "type": "string",
    "default": "/etc/default/htdig",
    "description": "Path to the default configuration file containing the `run_rundig` flag."
  }
}
