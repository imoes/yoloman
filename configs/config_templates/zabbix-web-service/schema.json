{
  "run_directory": {
    "type": "string",
    "default": "/run/zabbix",
    "description": "Directory for PID and runtime files."
  },
  "daemon_retry_policy": {
    "type": "string",
    "default": "TERM/30/KILL/5",
    "description": "Signal and timeout policy for stopping the daemon (e.g., SIGNAL/SECONDS/SIGNAL/SECONDS).",
    "enum": [
      "TERM/30/KILL/5",
      "TERM",
      "TERM/30",
      "KILL",
      "KILL/5"
    ]
  },
  "daemon_user": {
    "type": "string",
    "default": "zabbix",
    "description": "User to run the zabbix-web-service daemon as."
  },
  "daemon_group": {
    "type": "string",
    "default": "zabbix",
    "description": "Group to run the zabbix-web-service daemon as."
  }
}
