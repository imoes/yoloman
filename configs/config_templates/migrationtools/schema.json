{
 "DEFAULT_BASE": {
  "default": "dc=padl,dc=com",
  "description": "Default LDAP base DN.",
  "type": "string"
 },
 "DEFAULT_MAIL_DOMAIN": {
  "default": "padl.com",
  "description": "Default DNS mail domain. Usually commented out in the file but parametrized here for potential uncommenting.",
  "type": "string"
 },
 "DEFAULT_MAIL_HOST": {
  "default": "mail.padl.com",
  "description": "Default mail host. Usually commented out in the file but parametrized here for potential uncommenting.",
  "type": "string"
 },
 "EXTENDED_SCHEMA": {
  "default": 0,
  "description": "Turn on extended schema support (e.g., for person objects).",
  "type": "number"
 },
 "IGNORE_GID_ABOVE": {
  "default": 9999,
  "description": "Ignore system groups with GID above this value.",
  "type": "number"
 },
 "IGNORE_GID_BELOW": {
  "default": 100,
  "description": "Ignore system groups with GID below this value.",
  "type": "number"
 },
 "IGNORE_UID_ABOVE": {
  "default": 9999,
  "description": "Ignore system users with UID above this value.",
  "type": "number"
 },
 "IGNORE_UID_BELOW": {
  "default": 1000,
  "description": "Ignore system users with UID below this value.",
  "type": "number"
 },
 "NAMING_CONTEXT_ALIASES": {
  "default": "ou=Aliases",
  "description": "LDAP naming context for aliases when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_FSTAB": {
  "default": "ou=Mounts",
  "description": "LDAP naming context for fstab when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_GROUP": {
  "default": "ou=Group",
  "description": "LDAP naming context for group when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_HOSTS": {
  "default": "ou=Hosts",
  "description": "LDAP naming context for hosts when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_NETGROUP": {
  "default": "ou=Netgroup",
  "description": "LDAP naming context for netgroup when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_NETGROUP_BYHOST": {
  "default": "nisMapName=netgroup.byhost",
  "description": "LDAP naming context for netgroup_byhost when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_NETGROUP_BYUSER": {
  "default": "nisMapName=netgroup.byuser",
  "description": "LDAP naming context for netgroup_byuser when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_NETWORKS": {
  "default": "ou=Networks",
  "description": "LDAP naming context for networks when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_PASSWD": {
  "default": "ou=People",
  "description": "LDAP naming context for passwd when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_PROTOCOLS": {
  "default": "ou=Protocols",
  "description": "LDAP naming context for protocols when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_RPC": {
  "default": "ou=Rpc",
  "description": "LDAP naming context for rpc when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NAMING_CONTEXT_SERVICES": {
  "default": "ou=Services",
  "description": "LDAP naming context for services when NETINFOBRIDGE is false.",
  "type": "string"
 },
 "NETINFOBRIDGE": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "USE_UTF8": {
  "default": 1,
  "description": "Use UTF8 encoding. Comment out if LDAP server does not support UTF8.",
  "type": "number"
 }
}
