{
  "target_time": {
    "type": "string",
    "default": "0900",
    "description": "The time of day in HHMM format (24-hour clock) to trigger the reminder."
  },
  "use_relative_time": {
    "type": "bool",
    "default": false,
    "description": "If true, the target_time is interpreted as minutes from the current time (preceded by '+') rather than a clock time."
  },
  "reminder_message": {
    "type": "string",
    "default": "Time to leave",
    "description": "Custom message to display at the reminder time. Standard leave ignores this and prints a fixed string, but included for wrapper flexibility."
  }
}
