{
  "log_file": {
    "type": "string",
    "default": "/var/log/super.log",
    "description": "The path to the log file to rotate."
  },
  "rotate_count": {
    "type": "number",
    "default": 4,
    "description": "Number of rotated log files to keep."
  },
  "permissions": {
    "type": "string",
    "default": "0640",
    "description": "Octal permissions for newly created log files."
  },
  "owner": {
    "type": "string",
    "default": "root",
    "description": "Owner of newly created log files."
  },
  "group": {
    "type": "string",
    "default": "adm",
    "description": "Group of newly created log files."
  }
}
