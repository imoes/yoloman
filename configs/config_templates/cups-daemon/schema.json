{
  "log_rotate_count": {
    "type": "number",
    "default": 7,
    "description": "Number of rotated log files to keep."
  },
  "service_name": {
    "type": "string",
    "default": "cups",
    "description": "Name of the service to restart after log rotation.",
    "enum": [
      "cups",
      "cups-browsed",
      "cups-lpd"
    ]
  }
}
