{
  "provides": {
    "type": "string",
    "default": "trove-api",
    "description": "The 'Provides' field for the init script metadata."
  },
  "should_start": {
    "type": "string",
    "default": "postgresql mysql keystone rabbitmq-server ntp",
    "description": "The 'Should-Start' field for the init script metadata."
  },
  "should_stop": {
    "type": "string",
    "default": "postgresql mysql keystone rabbitmq-server ntp",
    "description": "The 'Should-Stop' field for the init script metadata."
  },
  "short_description": {
    "type": "string",
    "default": "Trove API",
    "description": "The 'Short-Description' field for the init script metadata."
  },
  "description": {
    "type": "string",
    "default": "Frontend Trove API server",
    "description": "The 'Description' field for the init script metadata."
  },
  "desc": {
    "type": "string",
    "default": "OpenStack Trove DBaaS API",
    "description": "The DESC variable used by the init script."
  },
  "project_name": {
    "type": "string",
    "default": "trove",
    "description": "The OpenStack project name, used for paths and service naming."
  },
  "rabbit_processname": {
    "type": "string",
    "default": "trove-api",
    "description": "The process name used for RabbitMQ messaging."
  },
  "system_user": {
    "type": "string",
    "default": "trove",
    "description": "The system user under which the service runs."
  },
  "system_group": {
    "type": "string",
    "default": "trove",
    "description": "The system group under which the service runs."
  },
  "uwsgi_bind_ip": {
    "type": "string",
    "default": "",
    "description": "The default IP address to bind uWSGI to if not configured via environment or external file."
  },
  "uwsgi_processes_default": {
    "type": "number",
    "default": 0,
    "description": "Default fallback value for uWSGI processes calculation placeholder. In the original script this is calculated dynamically, but the placeholder logic here needs a default if the variable isn't set."
  }
}
