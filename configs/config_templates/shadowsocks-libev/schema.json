{
  "user": {
    "type": "string",
    "default": "nobody",
    "description": "System user account under which shadowsocks-libev runs"
  },
  "group": {
    "type": "string",
    "default": "nogroup",
    "description": "System group under which shadowsocks-libev runs"
  },
  "maxfd": {
    "type": "number",
    "default": 32768,
    "description": "Maximum number of open file descriptors (ulimit -n); increase for high-concurrency deployments"
  }
}
