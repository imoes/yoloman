{
  "buildmethod": {
    "type": "string",
    "default": "sbuild",
    "description": "Select build method from sbuild, xdeb, pdebuild (pdebuild-cross)",
    "enum": [
      "sbuild",
      "xdeb",
      "pdebuild",
      "pdebuild-cross"
    ]
  },
  "base": {
    "type": "string",
    "default": "/srv/buildd",
    "description": "Base directory for buildd operations"
  },
  "arch": {
    "type": "string",
    "default": "armhf",
    "description": "Architecture to build for"
  },
  "cleanbuild": {
    "type": "bool",
    "default": true,
    "description": "If set, each build is done in a clean chroot for repeatable builds"
  },
  "distro": {
    "type": "string",
    "default": "testing",
    "description": "Distro release for clean builds, must match schroot config"
  },
  "chrootname": {
    "type": "string",
    "default": "testing-amd64-sbuild",
    "description": "Name of the chroot used for clean builds"
  }
}
