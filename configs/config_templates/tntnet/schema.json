{
  "daemon_opts": {
    "type": "string",
    "default": "",
    "description": "Additional command-line options passed to the tntnet daemon."
  },
  "no_port_check": {
    "type": "bool",
    "default": false,
    "description": "Set to true to disable checking port 80 availability on startup."
  }
}
