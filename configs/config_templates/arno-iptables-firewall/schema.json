{
  "NAME": {
    "type": "string",
    "default": "arno-iptables-firewall",
    "description": "Service name used in log messages and as process identifier"
  },
  "VERBOSE": {
    "type": "number",
    "default": 0,
    "description": "Enable verbose output (1 to enable, 0 to disable)"
  }
}
