{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Whether xdg-user-dirs-update is enabled on user login."
  },
  "filename_encoding": {
    "type": "string",
    "default": "UTF-8",
    "description": "The filename encoding to use. Can be an explicit encoding or 'locale'.",
    "enum": [
      "UTF-8",
      "locale"
    ]
  }
}
