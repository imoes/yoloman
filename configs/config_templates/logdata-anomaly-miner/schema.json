{
  "log_resource_path": {
    "type": "string",
    "default": "/tmp/syslog",
    "description": "The path to the log file to read from."
  },
  "aminer_user": {
    "type": "string",
    "default": "aminer",
    "description": "The user under which the aminer process runs."
  },
  "aminer_group": {
    "type": "string",
    "default": "aminer",
    "description": "The group under which the aminer process runs."
  },
  "learn_mode": {
    "type": "bool",
    "default": true,
    "description": "Whether to enable learn mode for anomaly detection."
  }
}
