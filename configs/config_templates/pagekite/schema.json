{
  "runas": {
    "type": "string",
    "default": "daemon:daemon",
    "description": "User and group under which the PageKite daemon runs, formatted as user:group."
  },
  "pidfile": {
    "type": "string",
    "default": "/var/run/pagekite.pid",
    "description": "Path to write the PID file when running in daemon mode."
  },
  "logfile": {
    "type": "string",
    "default": "/var/log/pagekite/pagekite.log",
    "description": "Path to the log file for PageKite."
  }
}
