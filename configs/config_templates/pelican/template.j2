# Pelican Static Site Generator Configuration
# Auto-generated from pelican(1) and official docs.
# This file is a valid Python module (pelicanconf.py).

# Path where to find the content files (articles, pages).
# Corresponds to positional argument [path] or standard convention.
CONTENT_DIR = "{{ content_dir | default('content') }}"

# Where to output the generated files.
# Corresponds to -o / --output.
OUTPUT_PATH = "{{ output_path | default('output') }}"

# Theme name or path.
# Corresponds to -t / --theme-path.
# Defaults to built-in 'notmyidea' if not specified.
THEME = "{{ theme | default('notmyidea') }}"

# Name of the website (SITE_NAME).
SITE_NAME = "{{ site_name | default('My Pelican Blog') }}"

# URL of the website (SITEURL).
SITE_URL = "{{ site_url | default('http://example.com') }}"

# Description of the website (SITEDESCRIPTION).
SITE_DESCRIPTION = "{{ site_description | default('A blog powered by Pelican.') }}"

# Default author name (DEFAULT_AUTHOR).
DEFAULT_AUTHOR = "{{ default_author | default('Admin') }}"

# Date format string for articles (DATE_FORMAT).
ARTICLE_DATE_FORMAT = "{{ article_date_format | default('%B %d, %Y') }}"

# Timezone for date processing (TIMEZONE).
TIMEZONE = "{{ timezone | default('UTC') }}"

# Use relative URLs in output.
# Corresponds to --relative-urls flag.
# In Python boolean literals are True/False (capitalized).
USE_RELATIVE_URLS = {{ use_relative_urls | default(false) | lower }}

# Directory to store cache files.
# Corresponds to --cache-path.
CACHE_PATH = "{{ cache_path | default('cache') }}"

# Log level (DEBUG, INFO, WARNING, ERROR).
# Corresponds to --logs-dedup-min-level.
LOG_LEVEL = "{{ log_level | default('WARNING') }}"

# Log message handler (plain or rich).
# Corresponds to --log-handler.
LOG_HANDLER = "{{ log_handler | default('rich') }}"

# Show categories in sidebar (DISPLAY_CATEGORIES_ON_SIDEBAR).
DISPLAY_CATEGORIES_ON_SIDEBAR = {{ display_categories_on_sidebar | default(true) | lower }}

# Show tags in sidebar (DISPLAY_TAGS_ON_SIDEBAR).
DISPLAY_TAGS_ON_SIDEBAR = {{ display_tags_on_sidebar | default(true) | lower }}

# Show archive in sidebar (DISPLAY_ARCHIVE_ON_SIDEBAR).
DISPLAY_ARCHIVE_ON_SIDEBAR = {{ display_archive_on_sidebar | default(false) | lower }}