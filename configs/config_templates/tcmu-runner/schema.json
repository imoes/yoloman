{
  "user": {
    "type": "string",
    "default": "root",
    "description": "The user allowed to own the D-Bus service."
  },
  "service_name": {
    "type": "string",
    "default": "org.kernel.TCMUService1",
    "description": "The D-Bus service name for TCMU."
  },
  "handler_prefix": {
    "type": "string",
    "default": "org.kernel.TCMUService1.HandlerManager1",
    "description": "The D-Bus prefix for handler manager services."
  }
}
