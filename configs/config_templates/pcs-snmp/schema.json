{
  "pcs_snmp_agent_debug": {
    "type": "bool",
    "default": false,
    "description": "Enable logging of debugging messages for the pcs snmp agent."
  },
  "pcs_snmp_agent_update_interval": {
    "type": "number",
    "default": 30,
    "description": "Data update interval in seconds for the pcs snmp agent."
  }
}
