{
  "freeipmi_path": {
    "type": "string",
    "default": "/usr/sbin",
    "description": "Path to FreeIPMI executables."
  },
  "log_level": {
    "type": "string",
    "default": "info",
    "description": "Only log messages with the given severity or above. One of: [debug, info, warn, error]",
    "enum": [
      "debug",
      "info",
      "warn",
      "error"
    ]
  },
  "log_format": {
    "type": "string",
    "default": "logfmt",
    "description": "Output format of log messages. One of: [logfmt, json]",
    "enum": [
      "logfmt",
      "json"
    ]
  },
  "web_listen_address": {
    "type": "string",
    "default": ":9290",
    "description": "Address to listen on for web interface and telemetry."
  }
}
