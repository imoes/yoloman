{
  "search_expression": {
    "type": "string",
    "default": "",
    "description": "Default regex or substring expression for searching alternatives"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging output"
  },
  "editor_command": {
    "type": "string",
    "default": "nano",
    "description": "Command to launch when editing alternative links"
  },
  "quit_key": {
    "type": "string",
    "default": "q",
    "description": "Key binding to quit the rover interface"
  }
}
