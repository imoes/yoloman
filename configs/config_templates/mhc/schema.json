{
  "pkg_name": {
    "type": "string",
    "default": "mhc",
    "description": "The package name string used in paths and autoloads."
  },
  "default_emacs_flavor": {
    "type": "string",
    "default": "emacs",
    "description": "The default Emacs flavor name if debian-emacs-flavor is not bound."
  }
}
