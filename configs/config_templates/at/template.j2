# /etc/at.deny: List of usernames NOT permitted to use the at command.
# If this file is empty, all users may use at unless they are in /etc/at.allow.
# If this file does not exist, only users in /etc/at.allow may use at.
#
# System users that typically should not run interactive batch jobs
# or scheduled at tasks are listed here.
alias
backup
bin
# The web server system user (e.g., www-data) is included to prevent
# the web server process from scheduling at jobs directly.
{{ system_users_www_data | default('www-data') }}
daemon
ftp
games
gnats
guest
irc
lp
mail
man
nobody
operator
proxy
qmaild
qmaill
qmailp
qmailq
qmailr
qmails
sync
sys
{{ system_users_www_data | default('www-data') }}