{
  "wsgi_script_alias_path": {
    "type": "string",
    "default": "/pywps",
    "description": "The URL path prefix for the WSGI application alias."
  },
  "wsgi_script_path": {
    "type": "string",
    "default": "/usr/share/pywps/wps.py",
    "description": "The absolute file system path to the WSGI script entry point."
  },
  "wsgi_directory_path": {
    "type": "string",
    "default": "/usr/share/pywps",
    "description": "The file system path to the directory containing the WSGI script."
  }
}
