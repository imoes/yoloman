{
  "email": {
    "type": "string",
    "default": "user@example.com",
    "description": "Mega.nz account email address for authentication."
  },
  "password": {
    "type": "string",
    "default": "",
    "description": "Mega.nz account password for authentication."
  },
  "session_cache_timeout": {
    "type": "number",
    "default": 10,
    "description": "Session cache timeout in minutes. Megatools refreshes session cache after this period of inactivity."
  },
  "user_agent": {
    "type": "string",
    "default": "Megatools/1.11.5",
    "description": "HTTP User-Agent string sent to Mega.nz servers."
  }
}
