{
  "enable_recording_directory_customization": {
    "type": "bool",
    "default": false,
    "description": "Whether to customize the recording output directory (default: user's Desktop)"
  },
  "recording_directory": {
    "type": "string",
    "default": "/home/user/Desktop",
    "description": "Absolute path to directory where recordings are saved (only used if enable_recording_directory_customization is true)"
  },
  "user": {
    "type": "string",
    "default": "deepin",
    "description": "Username whose Desktop directory is used as fallback when enable_recording_directory_customization is true"
  }
}
