{
  "mls_translations": {
    "type": "list",
    "default": [
      "s0=SystemLow",
      "s0-s0:c0.c1023=SystemLow-SystemHigh",
      "s0:c0.c1023=SystemHigh"
    ],
    "description": "List of Multi-Level Security (MLS) translation rules for SELinux. Each entry maps an MLS range (e.g., 's0:c0.c1023') to a human-readable label (e.g., 'SystemHigh'). Comments and blank lines are preserved as-is in the template via verbatim inclusion."
  }
}
