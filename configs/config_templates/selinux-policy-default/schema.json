{
  "file_contexts": {
    "type": "list",
    "default": [],
    "description": "List of file context rules mapping filesystem paths to SELinux security contexts.",
    "items": {
      "type": "object",
      "properties": {
        "path_spec": {
          "type": "string",
          "default": "",
          "description": "Path specification (may include glob wildcards) to match files or directories."
        },
        "context": {
          "type": "string",
          "default": "system_u:object_r:default_t:s0",
          "description": "SELinux security context in user:role:type[:level] format."
        },
        "comment": {
          "type": "string",
          "default": "",
          "description": "Optional one-line description of the rule (included as a comment in the config)."
        }
      },
      "required": [
        "path_spec",
        "context"
      ],
      "additionalProperties": false
    }
  }
}
