{
  "daemon_path": {
    "type": "string",
    "default": "timelord",
    "description": "Path to the timelord daemon executable."
  },
  "pidfile": {
    "type": "string",
    "default": "",
    "description": "Path to the PID file for the timelord daemon."
  }
}
