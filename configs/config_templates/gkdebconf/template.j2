{# ============================================================================= #}
{# gkdebconf configuration template                                                #}
{# ============================================================================= #}
{# gkdebconf is a graphical frontend to debconf, used for configuring Debian         #}
{# packages via a GUI (GTK) instead of a terminal UI (dialog).                      #}
{#                                                                                    #}
{# IMPORTANT: gkdebconf itself does NOT have a configuration file.                  #}
{# Configuration for debconf backends (file, tee, noninteractive, etc.) is handled  #}
{# by /etc/debconf.conf or /etc/debconf/priorities.conf.                             #}
{#                                                                                    #}
{# This template is intentionally a no-op placeholder to satisfy infrastructure      #}
{# requirements for a Jinja2 template associated with this role/package. It renders  #}
{# to empty output.                                                                  #}
{# ============================================================================= #}
{% if false %}
{# This block is never rendered; template satisfies requirement for a canonical Jinja2 template #}
{% endif %}