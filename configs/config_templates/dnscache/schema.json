{
  "service_name": {
    "type": "string",
    "default": "dnscache",
    "description": "Name of the service (used in init script, directory paths, and process naming)"
  },
  "service_description": {
    "type": "string",
    "default": "DNS cache - tool for finding addresses of Internet hosts",
    "description": "Short description of the service"
  },
  "service_binary": {
    "type": "string",
    "default": "dnscache",
    "description": "Name of the main executable binary (without path)"
  }
}
