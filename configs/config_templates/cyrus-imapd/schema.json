{
  "common_auth": {
    "type": "string",
    "default": "common-auth",
    "description": "The common PAM authentication stack to include for Cyrus IMAP."
  },
  "common_account": {
    "type": "string",
    "default": "common-account",
    "description": "The common PAM account management stack to include for Cyrus IMAP."
  }
}
