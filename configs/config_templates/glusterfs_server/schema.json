{
  "allowlist_rpc": {
    "type": "list",
    "default": [
      "*"
    ],
    "description": "List of IP addresses or hostnames allowed to connect to the GlusterD RPC service. Use * for any (not recommended in production)."
  },
  "log_level": {
    "type": "string",
    "default": "INFO",
    "description": "Logging verbosity level: DEBUG, INFO, WARNING, ERROR, CRITICAL, or NONE."
  },
  "log_file": {
    "type": "string",
    "default": "/var/log/glusterfs/glusterd.log",
    "description": "Path to the glusterd log file."
  },
  "working_directory": {
    "type": "string",
    "default": "/var/lib/glusterd",
    "description": "Base working directory for GlusterFS daemon state (must exist and be writable)."
  },
  "ssl_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable SSL/TLS for internal GlusterFS cluster communication. Requires certificate setup."
  },
  "transport_type": {
    "type": "string",
    "default": "tcp",
    "description": "Transport type(s) for intra-cluster communication: 'tcp', 'rdma', or 'tcp,rdma'.",
    "enum": [
      "tcp",
      "rdma"
    ]
  },
  "quorum_type": {
    "type": "string",
    "default": "none",
    "description": "Quorum policy for cluster consensus: 'none', 'majority', or 'all'. Set to 'majority' for production redundancy."
  },
  "extra_options": {
    "type": "object",
    "default": {},
    "description": "Dictionary of additional custom GlusterD options (key \u2192 value). Used for advanced tuning not exposed by other fields."
  }
}
