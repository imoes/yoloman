{
  "server_root": {
    "type": "string",
    "default": "http://localhost:8080/uccs/inifile/",
    "description": "The root URL of the remote logon broker server, typically ending with the UCCS inifile endpoint path."
  },
  "userid": {
    "type": "string",
    "default": "user",
    "description": "The username used to retrieve session information from the broker server."
  }
}
