# Netlink socket receive buffer size for RDMA subsystem
# Default: 419430400 (400MB)
# This value controls the receive buffer size for Netlink sockets
# used by the RDMA (Remote Direct Memory Access) subsystem.
# Ensure this matches the expected buffer size for your RDMA workload.
nl_sock_rbuf_size = {{ nl_sock_rbuf_size | default(419430400) }}