# IPv6 packet filtering rules persistence — ip6tables-restore(8)
# Automatically generated — do not edit manually; edit config and regenerate
#
# Each rule is a complete ip6tables command line (or iptables-restore compatible
# table/chains/lines) that will be piped to ip6tables-restore(8).
#
# Schema field: rules (list of strings)
# Description: List of IPv6 firewall rules to persist. Each entry represents one
#              ip6tables command line, optionally with a comment for documentation.

{% set rules_list = rules | default([]) %}

{% for rule in rules_list %}
# Rule: {{ loop.index }}
{{ rule }}
{% endfor %}