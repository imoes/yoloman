{
  "title": {
    "type": "string",
    "default": "",
    "description": "The string to set as the window title. If empty, title is not explicitly forced via -t flag unless combined with other logic."
  },
  "icon_name": {
    "type": "string",
    "default": "",
    "description": "The string to set as the icon name. If empty, icon is not explicitly forced via -i flag."
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "If true, suppresses xtitle's default reporting of settings performed."
  }
}
