# dnsdist configuration file, an example can be found in /usr/share/doc/dnsdist/examples

# security_poll_suffix: Disables security status polling via DNS when set to empty string
# If empty, the security polling feature is effectively disabled.
setSecurityPollSuffix("{{ security_poll_suffix | default('') }}")