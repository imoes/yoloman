{
  "input_codes": {
    "type": "list",
    "default": [
      "4006381333931"
    ],
    "description": "List of EAN/PLU/UPC codes (strings) to validate. UP C codes must be prefixed with '0' (i.e., 13 digits). PLU codes are 4-digit; EAN codes are 13-digit. Each item is a string containing only digits, length 4 or 13 (UPC must be padded to 13)."
  }
}
