{# eancheck configuration #}

{# List of EAN/PLU/UPC codes to validate. #}
{# Defaults to ['4006381333931'] if input_codes is undefined. #}
{# Renders one code per line. #}
{% for code in (input_codes | default(['4006381333931'])) %}
{{ code }}
{% endfor %}