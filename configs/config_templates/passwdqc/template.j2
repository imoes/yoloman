min={{ min_length }}
max={{ max_length }}
passphrase={{ min_passphrase_length }}
match={{ min_match_length }}
similar={{ similar }}
random={{ min_random_length }}
enforce={{ enforce_for }}
retry={{ retry_limit }}
# The below are just examples, by default none of these are used
#wordlist={{ wordlist_path }}
#denylist={{ denylist_path }}
#filter={{ filter_path }}