{
  "browser": {
    "type": "string",
    "default": "sensible-browser",
    "description": "The web browser executable to use when opening surfraw results."
  },
  "escape_url_args": {
    "type": "bool",
    "default": true,
    "description": "Whether to apply URL escaping to search arguments."
  },
  "quote": {
    "type": "bool",
    "default": false,
    "description": "Whether to bracket arguments with quotes for exact matching."
  }
}
