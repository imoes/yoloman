{
  "global_settings": {
    "type": "string",
    "default": "",
    "description": "Raw configuration content for the global [main] section of entangle.conf (e.g., log level, capture directory, etc.). Must conform to entangle.conf(5) syntax."
  },
  "notifications": {
    "type": "list",
    "default": [],
    "description": "List of notification endpoints (e.g., email, webhook, Slack) for system alerts. Each item has a type and endpoint."
  }
}
