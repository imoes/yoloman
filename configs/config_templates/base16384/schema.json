{
  "options": {
    "type": "list",
    "default": [],
    "description": "List of command-line options to pass to base16384. Valid values: -e, -d, -t, -n, -c, -C. Omitting -e or -d defaults to encoding (-e)."
  },
  "inputfile": {
    "type": "string",
    "default": "-",
    "description": "Input file path. Use '-' to read from stdin."
  },
  "outputfile": {
    "type": "string",
    "default": "-",
    "description": "Output file path. Use '-' to write to stdout."
  }
}
