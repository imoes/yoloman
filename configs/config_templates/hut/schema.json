{
  "instance_name": {
    "type": "string",
    "default": "sr.ht",
    "description": "The name identifier for this instance."
  },
  "access_token": {
    "type": "string",
    "default": "",
    "description": "The OAuth2 access token string."
  },
  "access_token_cmd": {
    "type": "string",
    "default": "",
    "description": "A command string to retrieve the token dynamically (e.g., 'pass token')."
  },
  "meta_origin": {
    "type": "string",
    "default": "https://meta.sr.ht",
    "description": "The origin URL for the meta.sr.ht service."
  },
  "services": {
    "type": "list",
    "default": [],
    "description": "A list of service configuration maps (e.g., meta, git, todo). Only includes services if explicitly provided to keep the template clean."
  }
}
