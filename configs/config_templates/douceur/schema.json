{
  "command": {
    "type": "string",
    "default": "parse",
    "description": "CLI subcommand: 'parse' (for CSS parsing) or 'inline' (for HTML inlining). Matches douceur(1) synopsis.",
    "enum": [
      "parse",
      "inline"
    ]
  },
  "input_file": {
    "type": "string",
    "default": "parse",
    "description": "Path to input file: CSS for 'parse', HTML for 'inline'. Required.",
    "enum": [
      "parse",
      "inline"
    ]
  },
  "output_file": {
    "type": "string",
    "default": "",
    "description": "Optional path to write output. If absent, output is printed to stdout."
  }
}
