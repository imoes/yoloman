{
  "dn": {
    "type": "string",
    "default": "cn=dhcp,cn=schema,cn=config",
    "description": "The Distinguished Name for the schema configuration entry."
  },
  "cn": {
    "type": "string",
    "default": "dhcp",
    "description": "The common name for the schema configuration entry."
  }
}
