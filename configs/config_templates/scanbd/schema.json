{
  "desc": {
    "type": "string",
    "default": "SANE scanner button daemon",
    "description": "Description of the daemon shown in logs and status output"
  },
  "name": {
    "type": "string",
    "default": "scanbd",
    "description": "Name of the service/daemon"
  },
  "scanbd_conf": {
    "type": "string",
    "default": "/etc/scanbd/scanbd.conf",
    "description": "Path to the main scanbd configuration file"
  },
  "default_conf": {
    "type": "string",
    "default": "/etc/default/scanbd",
    "description": "Path to the default configuration variable file"
  },
  "sane_net_conf": {
    "type": "string",
    "default": "/etc/sane.d/net.conf",
    "description": "Path to the SANE network configuration file"
  },
  "sane_dll_conf": {
    "type": "string",
    "default": "/etc/sane.d/dll.conf",
    "description": "Path to the SANE dynamic loader configuration file"
  },
  "scanbd_dll_conf": {
    "type": "string",
    "default": "/etc/scanbd/dll.conf",
    "description": "Path to the scanbd-specific SANE dll configuration file"
  },
  "saned_default_conf": {
    "type": "string",
    "default": "/etc/default/saned",
    "description": "Path to the saned default configuration file"
  },
  "sane_config_dir": {
    "type": "string",
    "default": "/etc/scanbd",
    "description": "Directory for SANE configuration when running scanbd"
  }
}
