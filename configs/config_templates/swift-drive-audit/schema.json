{
  "schedule_interval": {
    "type": "string",
    "default": "5",
    "description": "The interval in minutes at which the drive audit script runs (e.g., 5 for every 5 minutes)."
  },
  "config_path": {
    "type": "string",
    "default": "/etc/swift/drive-audit.conf",
    "description": "Path to the Swift drive audit configuration file."
  }
}
