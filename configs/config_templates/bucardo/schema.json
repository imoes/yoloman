{
  "name": {
    "type": "string",
    "default": "bucardo",
    "description": "Service name"
  },
  "daemon_path": {
    "type": "string",
    "default": "/usr/bin/bucardo",
    "description": "Full path to the bucardo daemon executable"
  },
  "pidfile_path": {
    "type": "string",
    "default": "/var/run/bucardo/bucardo.mcp.pid",
    "description": "Full path to the PID file"
  },
  "log_dir": {
    "type": "string",
    "default": "/var/log/bucardo",
    "description": "Working directory for the service (typically where logs are written)"
  },
  "run_user": {
    "type": "string",
    "default": "bucardo",
    "description": "System user under which the bucardo service runs"
  }
}
