{# Configuration for apkinfo(1) — display information about an Android APK file #}
# apkinfo(1) configuration
# Command-line options are passed directly via invocation; this file documents defaults.
# Silent mode suppresses debug/warning logs (equivalent to '-s' flag)
silent: {{ silent | default(false) | lower }}