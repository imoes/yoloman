{
  "enable_logging": {
    "type": "bool",
    "default": false,
    "description": "Enable logging to the pt_state directory."
  },
  "log_level": {
    "type": "string",
    "default": "WARN",
    "description": "Maximum log severity level (ERROR, WARN, INFO, DEBUG).",
    "enum": [
      "ERROR",
      "WARN",
      "INFO",
      "DEBUG"
    ]
  },
  "unsafe_logging": {
    "type": "bool",
    "default": false,
    "description": "Disable IP address scrubber when logging."
  },
  "obfs4_dist_bias": {
    "type": "bool",
    "default": false,
    "description": "Use biased probability distributions for obfs4 obfuscation."
  },
  "torrc_directives": {
    "type": "list",
    "default": [],
    "description": "List of Tor configuration directives related to obfs4proxy transport plugins and bridges."
  }
}
