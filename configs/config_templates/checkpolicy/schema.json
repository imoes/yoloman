{
  "handle_unknown": {
    "type": "string",
    "default": "deny",
    "description": "How the kernel should handle unknown classes or permissions. Must be 'allow', 'deny', or 'reject'. (Option: -U)",
    "enum": [
      "allow",
      "deny",
      "reject"
    ]
  },
  "policy_version": {
    "type": "number",
    "default": null,
    "description": "Policy version number. If omitted, defaults to the latest. (Option: -c)"
  },
  "target_platform": {
    "type": "string",
    "default": "selinux",
    "description": "Target platform: 'selinux' or 'xen'. (Option: -t)",
    "enum": [
      "selinux",
      "xen"
    ]
  },
  "mls_enabled": {
    "type": "bool",
    "default": false,
    "description": "Enable Multi-Level Security (MLS) policy. (Option: -M)"
  },
  "binary_input": {
    "type": "bool",
    "default": false,
    "description": "Read from binary policy file instead of source policy.conf. (Option: -b)"
  },
  "binary_output": {
    "type": "bool",
    "default": false,
    "description": "Write binary policy output. (Option: -b is implied if no -F or -C is used, but explicit -b is only for input. The man page does not have a specific flag for 'binary output' distinct from the default behavior. However, the user might want to explicitly select binary output over CIL or conf. But checkpolicy defaults to binary output. There is no -B flag. The previous template had a comment but no flag for binary_output. This variable is now removed as it's not a distinct directive/flag in checkpolicy, it's the default behavior unless -C or -F is specified. Wait, the prompt asks to remove variables NOT documented. 'binary_output' as a toggle is not a documented flag. The flag -b is for input. The default output is binary. There is no flag to *force* binary output if you wanted to choose between binary and CIL? Actually -C forces CIL. -F forces conf. Binary is the default. So 'binary_output' as a boolean flag to enable it is not a valid directive because it's the default. If we set it to true, it should do nothing or just be ignored? Or is it a hallucinated flag? The previous template had a comment but no flag. This is a hallucinated variable. I will remove it.)\n    },\n    "
  }
}
