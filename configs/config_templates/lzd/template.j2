# lzd configuration template
#
# This package is a command-line tool with no persistent configuration file.
# Settings are passed via command-line flags only.
# No config file exists for lzd in standard deployments.

{% if note is defined and note is string and note != '' %}
# Note: {{ note }}
{% else %}
# Note: lzd is a command-line utility without a persistent configuration file.
{% endif %}