{
  "archive_prefix": {
    "type": "string",
    "default": "all",
    "description": "Prefix for backup filenames (used as VOLNAME); e.g., 'myhost-backup' \u2192 'myhost-backup-YYYYMMDD.tar.gz'"
  },
  "backup_dir": {
    "type": "string",
    "default": "/disk2/backup",
    "description": "Absolute path to the directory where backup archives will be stored"
  },
  "max_full_backups": {
    "type": "number",
    "default": 2,
    "description": "Maximum number of full backups to retain; older full backups will be deleted"
  },
  "include_dirs": {
    "type": "list",
    "default": [
      "/etc",
      "/root",
      "/home",
      "/var/mail",
      "/usr/local"
    ],
    "description": "List of directories to include in every backup"
  },
  "exclude_patterns": {
    "type": "list",
    "default": [
      "*.nobackup*",
      "*.o"
    ],
    "description": "List of patterns (fnmatch-style) of files/directories to exclude"
  }
}
