{
  "printer_name": {
    "type": "string",
    "default": "",
    "description": "Name of the printer to operate on (used with -P). The default is the system default printer."
  },
  "raw_device": {
    "type": "string",
    "default": "",
    "description": "Name of the device to write to directly (used with -r). Required for ink levels, identify, or head alignment without model detection."
  },
  "model": {
    "type": "string",
    "default": "",
    "description": "Precise printer model for head alignment (used with -m)."
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Suppress the banner output (used with -q)."
  }
}
