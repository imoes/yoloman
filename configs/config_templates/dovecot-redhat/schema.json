{
 "auth_proxy_self": {
  "default": "",
  "description": "With proxy_maybe=yes if proxy destination matches any of these IPs, don't do proxying.",
  "type": "string"
 },
 "base_dir": {
  "default": "",
  "description": "Base directory where to store runtime data.",
  "type": "string"
 },
 "doveadm_socket_path": {
  "default": "",
  "description": "UNIX socket or host:port used for connecting to doveadm server",
  "type": "string"
 },
 "doveadm_worker_count": {
  "description": "If non-zero, run mail commands via this many connections to doveadm server.",
  "type": "number"
 },
 "import_environment": {
  "default": "",
  "description": "Space separated list of environment variables that are preserved on Dovecot startup.",
  "type": "string"
 },
 "instance_name": {
  "default": "",
  "description": "Name of this instance.",
  "type": "string"
 },
 "listen": {
  "default": "",
  "description": "A comma separated list of IPs or hosts where to listen in for connections.",
  "type": "string"
 },
 "login_access_sockets": {
  "default": "",
  "description": "Space separated list of login access check sockets.",
  "type": "string"
 },
 "login_greeting": {
  "default": "",
  "description": "Greeting message for clients.",
  "type": "string"
 },
 "login_trusted_networks": {
  "default": "",
  "description": "Space separated list of trusted network ranges.",
  "type": "string"
 },
 "protocols": {
  "default": "",
  "description": "Protocols we want to be serving.",
  "type": "string"
 },
 "shutdown_clients": {
  "description": "Should all processes be killed when Dovecot master process shuts down.",
  "type": "bool"
 },
 "verbose_proctitle": {
  "description": "Show more verbose process titles (in ps).",
  "type": "bool"
 }
}
