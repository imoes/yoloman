{
  "mime_mappings": {
    "type": "list",
    "default": [],
    "description": "List of file extension to MIME type mappings. Each item defines one or more file extensions (space-separated) and their associated MIME type.",
    "items": {
      "type": "object",
      "description": "A single MIME type mapping",
      "properties": {
        "extensions": {
          "type": "list",
          "default": [],
          "description": "List of file extensions (without leading dots, e.g., ['txt', 'text']) to associate with this MIME type.",
          "items": {
            "type": "string",
            "description": "A single file extension (e.g., 'html', 'css')"
          }
        },
        "mime_type": {
          "type": "string",
          "default": "",
          "description": "The canonical MIME type string (e.g., 'text/html', 'application/json')"
        }
      },
      "required": [
        "extensions",
        "mime_type"
      ]
    }
  }
}
