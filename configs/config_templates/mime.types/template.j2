# /etc/mime.types — maps file extensions to MIME types
# Auto-managed by media-types package; site-specific overrides may be added below.

# Mapping from file extensions (space-separated) to MIME type
{% for mapping in mime_mappings %}
{{ mapping.extensions | join(' ') }}	{{ mapping.mime_type }}
{% endfor %}
