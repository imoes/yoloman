{
  "send_through_destinations": {
    "type": "string",
    "default": "    host1:2003\n    host2:2003",
    "description": "Whitespace-indented list of host:port destinations for the 'send-through' cluster (forward replication)"
  },
  "send_to_any_one_destinations": {
    "type": "string",
    "default": " host1:2003 host2:2003",
    "description": "Space-separated list of host:port destinations for the 'send-to-any-one' cluster (any-of replication)"
  }
}
