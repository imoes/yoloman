{
  "table": {
    "type": "string",
    "default": "filter",
    "description": "The name of the nftables table used for UPnP filtering."
  },
  "nat_table": {
    "type": "string",
    "default": "filter",
    "description": "The name of the nftables table used for UPnP NAT."
  },
  "chain": {
    "type": "string",
    "default": "miniupnpd",
    "description": "The name of the forward chain for UPnP rules."
  },
  "prerouting_chain": {
    "type": "string",
    "default": "prerouting_miniupnpd",
    "description": "The name of the prerouting chain for UPnP NAT rules."
  },
  "postrouting_chain": {
    "type": "string",
    "default": "postrouting_miniupnpd",
    "description": "The name of the postrouting (MASQUERADE) chain for UPnP NAT rules."
  }
}
