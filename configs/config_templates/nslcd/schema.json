{
  "ldap_uris": {
    "type": "list",
    "default": [
      "ldap://ldap.example.com"
    ],
    "description": "List of LDAP server URIs to connect to. Supports ldap://, ldaps://, and ldap+dns:// schemes."
  },
  "ldap_base": {
    "type": "string",
    "default": "dc=example,dc=com",
    "description": "Default base DN for LDAP searches (e.g., users, groups)."
  },
  "ldap_version": {
    "type": "number",
    "default": 3,
    "description": "LDAP protocol version (typically 3)."
  },
  "ldap_bind_dn": {
    "type": "string",
    "default": "",
    "description": "DN to bind as for authenticated searches. Leave empty for anonymous bind."
  },
  "ldap_bind_pw": {
    "type": "string",
    "default": "",
    "description": "Password for the bind DN. Only used if ldap_bind_dn is set."
  },
  "ldap_scope": {
    "type": "string",
    "default": "sub",
    "description": "Default search scope: one, sub, or base."
  },
  "tls_cacertdir": {
    "type": "string",
    "default": "",
    "description": "Directory containing CA certificates for TLS verification (e.g., /etc/ssl/certs)."
  },
  "tls_cacertfile": {
    "type": "string",
    "default": "",
    "description": "File containing CA certificate(s) for TLS verification."
  },
  "tls_reqcert": {
    "type": "string",
    "default": "demand",
    "description": "LDAP server certificate verification level: 'never', 'allow', 'try', 'demand', 'hard'."
  },
  "timelimit": {
    "type": "number",
    "default": 30,
    "description": "Time limit (in seconds) for the entire LDAP operation (server-side and client-side)."
  },
  "scope_search_timeout": {
    "type": "number",
    "default": 10,
    "description": "Time limit (in seconds) per individual LDAP search request."
  },
  "reconnect_retry": {
    "type": "string",
    "default": "3 30 120 600",
    "description": "Retry intervals (in seconds) on connection failure: initial, max, backoff factor, and max total."
  },
  "nss_map_passwd": {
    "type": "string",
    "default": "uid uidNumber cn gecos homeDirectory loginShell",
    "description": "NSS passwd map: which LDAP attributes to map to passwd fields."
  },
  "nss_map_group": {
    "type": "string",
    "default": "cn memberUid gidNumber",
    "description": "NSS group map: which LDAP attributes to map to group fields."
  },
  "nss_map_shadow": {
    "type": "string",
    "default": "uid userPassword shadowLastChange shadowMax shadowWarning",
    "description": "NSS shadow map: attributes for shadow entries (if enabled)."
  },
  "user_filter": {
    "type": "string",
    "default": "objectClass=posixAccount",
    "description": "Additional LDAP filter for passwd lookups (inside parentheses)."
  },
  "group_filter": {
    "type": "string",
    "default": "objectClass=posixGroup",
    "description": "Additional LDAP filter for group lookups."
  },
  "shadow_filter": {
    "type": "string",
    "default": "objectClass=shadowAccount",
    "description": "Additional LDAP filter for shadow lookups."
  },
  "pam_login_attribute": {
    "type": "string",
    "default": "uid",
    "description": "Attribute used to map PAM login names to LDAP entries."
  },
  "pam_filter": {
    "type": "string",
    "default": "",
    "description": "Additional PAM search filter (e.g., to restrict auth to specific users)."
  },
  "nss_initgroups_ignoreusers": {
    "type": "list",
    "default": [
      "root",
      "nobody"
    ],
    "description": "List of users for which initgroups() lookups should be skipped."
  },
  "nss_min_uid": {
    "type": "number",
    "default": 1000,
    "description": "Minimum UID to include in NSS (prevents system accounts from being overwritten)."
  },
  "nss_max_uid": {
    "type": "number",
    "default": 0,
    "description": "Maximum UID to include (0 = unlimited). Useful to exclude special ranges."
  },
  "nslcd_ssl": {
    "type": "string",
    "default": "off",
    "description": "SSL/TLS mode: 'off', 'start_tls', or 'on' (ldaps)."
  },
  "sasl_mech": {
    "type": "string",
    "default": "",
    "description": "SASL mechanism (e.g., GSSAPI) if SASL auth is enabled."
  },
  "sasl_realm": {
    "type": "string",
    "default": "",
    "description": "SASL realm (e.g., EXAMPLE.COM) for mechanisms like GSSAPI."
  },
  "pam_authc_search_base": {
    "type": "string",
    "default": "",
    "description": "Base DN for PAM authentication searches (if different from ldap_base)."
  }
}
