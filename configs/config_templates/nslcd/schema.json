{
  "k5start_start": {
    "type": "string",
    "default": "yes",
    "description": "Whether to start k5start. Set to 'yes' to force starting, any other value disables it."
  },
  "k5start_bin": {
    "type": "string",
    "default": "/usr/bin/k5start",
    "description": "Path to the k5start binary."
  },
  "k5start_keytab": {
    "type": "string",
    "default": "/etc/krb5.keytab",
    "description": "Path to the Kerberos keytab file for k5start."
  },
  "k5start_ccrefresh": {
    "type": "number",
    "default": 60,
    "description": "Interval in seconds for k5start to refresh the ticket cache."
  },
  "k5start_principal": {
    "type": "string",
    "default": "host/$(hostname -f)",
    "description": "Kerberos principal for k5start. Supports shell expansion like $(hostname -f)."
  }
}
