{
  "runtar_gnutar_path": {
    "type": "string",
    "default": "/bin/tar",
    "description": "Primary path to gnutar used by runtar in amanda-client"
  },
  "runtar_gnutar_path_alt": {
    "type": "string",
    "default": "/usr/bin/tar",
    "description": "Alternative path to gnutar used by runtar (fallback)"
  },
  "amgtar_gnutar_path": {
    "type": "string",
    "default": "/bin/tar",
    "description": "Primary path to gnutar used by amgtar in amanda-client"
  },
  "amgtar_gnutar_path_alt": {
    "type": "string",
    "default": "/usr/bin/tar",
    "description": "Alternative path to gnutar used by amgtar (fallback)"
  },
  "tcp_port_range": {
    "type": "string",
    "default": "512,1024",
    "description": "TCP port range for Amanda communication (lower,upper)"
  },
  "udp_port_range": {
    "type": "string",
    "default": "840,860",
    "description": "UDP port range for Amanda communication (lower,upper)"
  }
}
