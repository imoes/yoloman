{
  "input": {
    "type": "string",
    "default": "-",
    "description": "Input to analyze: a hash string, path to a file, or directory (use '-' for stdin)"
  },
  "extended": {
    "type": "bool",
    "default": false,
    "description": "Enable extended mode to list all possible hash algorithms including salted passwords"
  },
  "mode": {
    "type": "bool",
    "default": false,
    "description": "Include corresponding hashcat mode in output"
  },
  "john": {
    "type": "bool",
    "default": false,
    "description": "Include corresponding JohnTheRipper format in output"
  },
  "outfile": {
    "type": "string",
    "default": "",
    "description": "Path to output file (empty string means stdout)"
  }
}
