# Balboa Backend Configuration
#
# This template generates the configuration file for the Balboa backend process.
# It primarily controls the arguments passed to the backend executable.

# Arguments passed to the Balboa backend process (e.g., port, RocksDB path, etc.)
# Default: Empty string (no arguments)
BALBOA_BACKEND_ARGS={{ balboa_backend_args | default('') }}