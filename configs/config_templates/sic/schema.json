{
  "host": {
    "type": "string",
    "default": "irc6.oftc.net",
    "description": "Overrides the default host."
  },
  "port": {
    "type": "number",
    "default": 6667,
    "description": "Overrides the default port."
  },
  "nick": {
    "type": "string",
    "default": "",
    "description": "Override the default nick. If empty, sic uses the current system user."
  },
  "keyword": {
    "type": "string",
    "default": "",
    "description": "Specifies the keyword to authenticate your nick on the host."
  }
}
