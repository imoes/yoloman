{
  "rom_path": {
    "type": "string",
    "default": "/usr/share/openmsx",
    "description": "The directory containing MSX ROMs and cart images."
  },
  "audio_device": {
    "type": "string",
    "default": "pulse",
    "description": "The audio backend to use for sound output."
  },
  "volume": {
    "type": "number",
    "default": 80,
    "description": "Master volume percentage."
  },
  "windowed": {
    "type": "bool",
    "default": false,
    "description": "Whether to run in a window rather than full screen."
  },
  "display": {
    "type": "string",
    "default": ":0",
    "description": "The X11 display to render graphical output to."
  }
}
