{
 "gopher_enabled": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "gopher_port": {
  "default": 70,
  "description": "TCP port for Gopher protocol (standard is 70)",
  "type": "number"
 },
 "gopherroot": {
  "default": "/var/gopher",
  "description": "Top-level directory for Gopher content (filesystem path)",
  "type": "string"
 },
 "http_enabled": {
  "default": true,
  "description": "Enable HTTP/HTML interface (useful for browsers)",
  "type": "bool"
 },
 "loglevel": {
  "default": "INFO",
  "description": "Logging verbosity: DEBUG, INFO, WARNING, ERROR, or CRITICAL",
  "type": "string"
 },
 "pygopherd_detach": {
  "default": false,
  "description": "Set to true for the server to detach (run in background)",
  "type": "bool"
 },
 "pygopherd_pidfile": {
  "default": "/var/run/pygopherd/pygopherd.pid",
  "description": "Path to PID file for the PyGopherd daemon",
  "type": "string"
 },
 "tls_cert": {
  "default": "/etc/ssl/certs/pygopherd.crt",
  "description": "Path to PEM-encoded TLS certificate file",
  "type": "string"
 },
 "tls_enabled": {
  "default": false,
  "description": "Enable TLS for encrypted HTTP (HTTPS) and WAP (WAPS)",
  "type": "bool"
 },
 "tls_key": {
  "default": "/etc/ssl/private/pygopherd.key",
  "description": "Path to PEM-encoded TLS private key file",
  "type": "string"
 },
 "wap_enabled": {
  "default": false,
  "description": "Enable WAP interface for mobile devices (requires libwap)",
  "type": "bool"
 }
}
