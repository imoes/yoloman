{
  "ifhp_version": {
    "type": "string",
    "default": "3.5.20",
    "description": "Version number of the ifhp software"
  },
  "ifhp_statusfile_max": {
    "type": "number",
    "default": 8,
    "description": "Status file limits in K bytes"
  },
  "ifhp_statusfile_min": {
    "type": "number",
    "default": 2,
    "description": "Status file limits in K bytes"
  },
  "ifhp_snmp_program": {
    "type": "string",
    "default": "/usr/lib/ifhp/snmp_printer_status -h \"%s{snmp_dev}\" -m \"%s{snmp_model}\" -f \"%s{snmp_program_cfg}\"",
    "description": "SNMP monitoring program path and arguments"
  },
  "ifhp_snmp_program_cfg": {
    "type": "string",
    "default": "/usr/lib/ifhp/snmp_printer_status.conf",
    "description": "SNMP monitoring program configuration file path"
  },
  "ifhp_snmp_model": {
    "type": "string",
    "default": "default",
    "description": "SNMP model to use for status OID lookup"
  },
  "ifhp_snmp_status_fields": {
    "type": "string",
    "default": "status printerstatus",
    "description": "SNMP status fields to check"
  },
  "ifhp_snmp_sync_status": {
    "type": "string",
    "default": "ready power_save_mode",
    "description": "SNMP sync status values"
  },
  "ifhp_snmp_end_status": {
    "type": "string",
    "default": "ready idle",
    "description": "SNMP end of job status values"
  },
  "ifhp_snmp_wait_after_close": {
    "type": "number",
    "default": 2,
    "description": "SNMP wait after close timeout in seconds"
  },
  "ifhp_status_ignore": {
    "type": "string",
    "default": "name job page pages",
    "description": "Status keys to ignore"
  },
  "ifhp_sync_interval": {
    "type": "number",
    "default": 20,
    "description": "Interval in seconds for sync requests"
  },
  "ifhp_pagecount_interval": {
    "type": "number",
    "default": 1,
    "description": "Interval in seconds for pagecount polling"
  },
  "ifhp_pagecount_timeout": {
    "type": "number",
    "default": 0,
    "description": "Timeout for pagecount in seconds (0=no timeout)"
  },
  "ifhp_waitend_ctrl_t_interval": {
    "type": "number",
    "default": 3,
    "description": "Interval in seconds for sending Ctrl-T for status"
  },
  "ifhp_end_status": {
    "type": "string",
    "default": "busy idle",
    "description": "Values indicating end of job status"
  },
  "ifhp_waitend_interval": {
    "type": "number",
    "default": 120,
    "description": "Interval in seconds to prompt for reprompt status"
  },
  "ifhp_waitend_timeout": {
    "type": "number",
    "default": 0,
    "description": "Timeout for waitend response in seconds (0=no timeout)"
  },
  "ifhp_progress_pc": {
    "type": "number",
    "default": 25,
    "description": "Progress report interval in percentage"
  },
  "ifhp_progress_k": {
    "type": "number",
    "default": 1000,
    "description": "Progress report interval in Kbytes"
  },
  "ifhp_papersize_default": {
    "type": "string",
    "default": "letter",
    "description": "Default paper size if none specified",
    "enum": [
      "a0",
      "a1",
      "a2",
      "a3",
      "a4",
      "a5",
      "a6",
      "a7",
      "a8",
      "a9",
      "a10",
      "b0",
      "b1",
      "b2",
      "b3",
      "b4",
      "b5",
      "archA",
      "archB",
      "archC",
      "archD",
      "archE",
      "flsa",
      "flse",
      "halfletter",
      "note",
      "letter",
      "legal",
      "11x17",
      "ledger",
      "tabloid"
    ]
  },
  "ifhp_pjl_ready_msg": {
    "type": "string",
    "default": "%s{n} %s{J}:%s{P}",
    "description": "Console message during job"
  },
  "ifhp_pjl_done_msg": {
    "type": "string",
    "default": "Done: %s{n} %s{J}:%s{P}",
    "description": "Console message when job is done"
  },
  "ifhp_pjl_display_size": {
    "type": "number",
    "default": 20,
    "description": "Maximum characters on console display for messages"
  },
  "ifhp_pjl_fontdir": {
    "type": "string",
    "default": "/usr/libexec/filters/fonts/pjl",
    "description": "Directory for PJL font downloads"
  },
  "ifhp_setup": {
    "type": "string",
    "default": "test",
    "description": "Default value for testing or insurance setup file"
  },
  "ifhp_pjl_alert_handler": {
    "type": "string",
    "default": "",
    "description": "Program to write error to operator"
  },
  "ifhp_ps_fontdir": {
    "type": "string",
    "default": "/usr/libexec/filters/fonts/ps",
    "description": "Directory for PostScript font downloads"
  }
}
