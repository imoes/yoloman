{
  "btl_base_warn_component_unused": {
    "type": "number",
    "default": 0,
    "description": "Disable runtime MPI parameter checking for unused BTL components. Set to 0 to silence warnings."
  },
  "mtl": {
    "type": "string",
    "default": "^ofi",
    "description": "MCA Transport Layer (MTL) module selector. Regex pattern excluding modules."
  },
  "btl": {
    "type": "string",
    "default": "^uct,openib,ofi",
    "description": "Byte Transport Layer (BTL) module selector. Regex pattern excluding modules."
  },
  "pml": {
    "type": "string",
    "default": "^ucx",
    "description": "Point-to-Point Layer (PML) module selector. Regex pattern excluding modules."
  },
  "osc": {
    "type": "string",
    "default": "^ucx,pt2pt",
    "description": "One-Sided Communication (OSC) module selector. Regex pattern excluding modules."
  }
}
