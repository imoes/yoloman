[server]
# The plugin name to load for Snappy compression support.
plugin_load_add={{ plugin_load_add | default('provider_snappy') }}
# The loading mode for the Snappy provider plugin.
provider_snappy={{ provider_snappy | default('force_plus_permanent') }}