{
  "plugin_load_add": {
    "type": "string",
    "default": "provider_snappy",
    "description": "The plugin name to load for Snappy compression support."
  },
  "provider_snappy": {
    "type": "string",
    "default": "force_plus_permanent",
    "description": "The loading mode for the Snappy provider plugin.",
    "enum": [
      "default",
      "force_plus_permanent"
    ]
  }
}
