{
  "cron_interval_minutes": {
    "type": "number",
    "default": 5,
    "description": "The interval in minutes for flushing the DMA mail transfer agent's queue."
  },
  "cron_user": {
    "type": "string",
    "default": "root",
    "description": "The user under which the DMA flush command runs."
  }
}
