{
  "mysql_user": {
    "type": "string",
    "default": "kea_admin",
    "description": "Username for the MySQL database used for DHCP lease storage and control."
  },
  "mysql_password": {
    "type": "string",
    "default": "",
    "description": "Password for the MySQL database. Leave blank only if using peer authentication."
  },
  "mysql_host": {
    "type": "string",
    "default": "localhost",
    "description": "Hostname or IP address of the MySQL server."
  },
  "mysql_database": {
    "type": "string",
    "default": "kea",
    "description": "Name of the MySQL database used by Kea DHCP."
  },
  "mysql_port": {
    "type": "number",
    "default": 3306,
    "description": "Port number of the MySQL server."
  },
  "control_socket_path": {
    "type": "string",
    "default": "/run/kea/dhcp4-ctrl.sock",
    "description": "Unix socket path for Kea DHCP control commands (e.g., for 'kea-dhcp4-ctrl-agent')."
  },
  "dhcp_queue_size": {
    "type": "number",
    "default": 64,
    "description": "Size of the DHCP packet processing queue. Increase for high-throughput deployments."
  },
  "lease_cmds_lib_path": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu/kea/hooks/libdhcp_lease_cmds.so",
    "description": "Path to the lease commands hook library (required for API-based lease management)."
  },
  "ddns_lib_path": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu/kea/hooks/libdhcp_ddns.so",
    "description": "Path to the DDNS hook library. Only used if dynamic DNS updates are enabled."
  },
  "enable_ddns": {
    "type": "bool",
    "default": false,
    "description": "Enable dynamic DNS (forward and reverse) updates for assigned addresses."
  },
  "ddns_timeout": {
    "type": "number",
    "default": 1000,
    "description": "Timeout (in milliseconds) for DNS updates."
  },
  "ddns_ttl": {
    "type": "number",
    "default": 3600,
    "description": "Default TTL (seconds) for DNS records created via DDNS."
  },
  "ddns_server_ip": {
    "type": "string",
    "default": "127.0.0.1",
    "description": "IP address of the DNS server to receive DDNS updates (must support TSIG)."
  },
  "ddns_server_port": {
    "type": "number",
    "default": 53,
    "description": "Port of the DNS server for DDNS updates."
  },
  "ddns_qualifying_suffix": {
    "type": "string",
    "default": "example.com",
    "description": "Domain suffix to append to hostnames for FQDNs when DDNS is enabled."
  },
  "interfaces": {
    "type": "list",
    "default": [
      "eth0"
    ],
    "description": "List of network interfaces (by name) on which Kea DHCP will listen and respond to requests.",
    "items": {
      "type": "string",
      "description": "Network interface name (e.g., 'eth0', 'enp0s31f6')."
    }
  },
  "lease_lfc_interval": {
    "type": "number",
    "default": 3600,
    "description": "Interval (seconds) between cleanup of expired leases from the database."
  },
  "renew_time": {
    "type": "number",
    "default": 300,
    "description": "Time (seconds) after lease issuance when client may attempt renewal."
  },
  "rebind_time": {
    "type": "number",
    "default": 500,
    "description": "Time (seconds) after lease issuance when client must attempt rebinding if renewal fails."
  },
  "valid_lifetime": {
    "type": "number",
    "default": 3600,
    "description": "Default validity lifetime (seconds) for IPv4 addresses assigned by this server."
  },
  "log_output": {
    "type": "string",
    "default": "stdout",
    "description": "Where to send log messages. Can be a file path (e.g., '/var/log/kea-dhcp4.log') or special values like 'stdout', 'stderr'."
  },
  "log_severity": {
    "type": "string",
    "default": "INFO",
    "description": "Minimum log severity (DEBUG, INFO, WARN, ERROR, FATAL). Use INFO or DEBUG for troubleshooting."
  },
  "log_debuglevel": {
    "type": "number",
    "default": 0,
    "description": "Debug verbosity level (0\u201399). Higher values produce more logs. Only effective for DEBUG severity."
  },
  "subnets": {
    "type": "list",
    "default": [],
    "description": "List of IPv4 subnets managed by this Kea server. Each defines a network, IP range, and client options.",
    "items": {
      "type": "object",
      "description": "A single IPv4 subnet configuration.",
      "fields": {
        "id": {
          "type": "number",
          "default": 1,
          "description": "Numeric subnet identifier (must be unique per subnet)."
        },
        "network": {
          "type": "string",
          "default": "192.168.10.0",
          "description": "IPv4 network address (e.g., '192.168.10.0')."
        },
        "prefix": {
          "type": "number",
          "default": 24,
          "description": "Subnet mask prefix length (e.g., 24 for 255.255.255.0)."
        },
        "gateway": {
          "type": "string",
          "default": "192.168.10.1",
          "description": "Default gateway/router IP address for clients in this subnet."
        },
        "dns_servers": {
          "type": "list",
          "default": [
            "192.168.10.1"
          ],
          "description": "List of DNS server IPs to supply via DHCP option 6.",
          "items": {
            "type": "string",
            "description": "IPv4 DNS server address."
          }
        },
        "domain_name": {
          "type": "string",
          "default": "lan.example.com",
          "description": "Default domain name to assign via DHCP option 15."
        },
        "pools": {
          "type": "list",
          "default": [
            {
              "start": "192.168.10.100",
              "end": "192.168.10.200"
            }
          ],
          "description": "IP address ranges for dynamic allocation.",
          "items": {
            "type": "object",
            "description": "A contiguous pool of IPv4 addresses.",
            "fields": {
              "start": {
                "type": "string",
                "default": "192.168.10.100",
                "description": "First IP address in the pool."
              },
              "end": {
                "type": "string",
                "default": "192.168.10.200",
                "description": "Last IP address in the pool."
              }
            }
          }
        },
        "reservations": {
          "type": "list",
          "default": [],
          "description": "Static DHCP reservations (by MAC address).",
          "items": {
            "type": "object",
            "description": "A fixed lease assignment for a known client.",
            "fields": {
              "mac": {
                "type": "string",
                "default": "aa:bb:cc:dd:ee:ff",
                "description": "Client hardware (MAC) address in lowercase colon-separated hex."
              },
              "ip": {
                "type": "string",
                "default": "192.168.10.10",
                "description": "Static IP address to assign to this client."
              },
              "hostname": {
                "type": "string",
                "default": "printer1",
                "description": "Hostname to use for DDNS (if enabled) and client identification."
              }
            }
          }
        }
      }
    }
  }
}
