{
  "enabled": {
    "type": "bool",
    "default": false,
    "description": "Whether to load the address_keeper plugin on Claws Mail startup. Note: The plugin must be loaded via the GUI \"Configuration\" -> \"Plugins\" -> \"Load plugin...\" -> select address_keeper.so."
  }
}
