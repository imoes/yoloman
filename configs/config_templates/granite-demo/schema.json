{
  "show_window": {
    "type": "bool",
    "default": true,
    "description": "Whether the demo application window should be visible on startup."
  },
  "window_width": {
    "type": "number",
    "default": 800,
    "description": "Initial window width in pixels. Must be a positive integer."
  },
  "window_height": {
    "type": "number",
    "default": 600,
    "description": "Initial window height in pixels. Must be a positive integer."
  },
  "debug_mode": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging output for debugging."
  },
  "demo_items": {
    "type": "list",
    "default": [
      "Label",
      "Button",
      "Toggle",
      "ComboBox"
    ],
    "description": "List of Granite widget types to demonstrate. Each entry corresponds to a documented widget demo name."
  }
}
