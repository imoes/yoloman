{
  "IONICE_DEFAULT": {
    "type": "string",
    "default": "",
    "description": "Default value for the IONICE variable if ionice is not available or fails the check."
  },
  "IONICE_CMD": {
    "type": "string",
    "default": "/usr/bin/ionice -c3",
    "description": "The full ionice command string to use if ionice is available and supports -c3."
  },
  "NOCACHE_CMD": {
    "type": "string",
    "default": "",
    "description": "The full nocache command string to use if nocache is available. Usually empty if not desired or not installed."
  }
}
