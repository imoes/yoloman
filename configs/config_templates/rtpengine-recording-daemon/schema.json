{
  "nfs_host": {
    "type": "string",
    "default": "192.168.1.1",
    "description": "The IP address or hostname of the NFS server."
  },
  "nfs_remote_path": {
    "type": "string",
    "default": "/var/recordings",
    "description": "The path on the NFS server to mount."
  },
  "nfs_local_mount": {
    "type": "string",
    "default": "/var/lib/rtpengine-recording",
    "description": "The local directory where the NFS share is mounted. Must match output-dir if used."
  },
  "nfs_options": {
    "type": "string",
    "default": "hard,intr,tcp",
    "description": "Mount options for the NFS share."
  }
}
