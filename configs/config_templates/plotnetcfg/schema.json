{
  "format": {
    "type": "string",
    "default": "dot",
    "description": "Output format for the network diagram. Common values: dot, json, svg. Use 'plotnetcfg --list-formats' for available options."
  },
  "ovs_db": {
    "type": "string",
    "default": "/var/run/openvswitch/db.sock",
    "description": "Path to the UNIX socket used to communicate with the Open vSwitch database server. Only used if Open vSwitch interfaces are detected."
  }
}
