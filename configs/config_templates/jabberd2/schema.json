{
  "router_run": {
    "type": "bool",
    "default": true,
    "description": "Enable running the jabberd2 router component."
  },
  "sm_run": {
    "type": "bool",
    "default": true,
    "description": "Enable running the jabberd2 session manager (sm) component."
  },
  "s2s_run": {
    "type": "bool",
    "default": true,
    "description": "Enable running the jabberd2 server-to-server (s2s) component."
  },
  "c2s_run": {
    "type": "bool",
    "default": true,
    "description": "Enable running the jabberd2 client-to-server (c2s) component."
  }
}
