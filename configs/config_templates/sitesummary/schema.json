{
  "site_path": {
    "type": "string",
    "default": "sitesummary",
    "description": "The URL path segment for accessing the sitesummary listing page."
  },
  "www_directory": {
    "type": "string",
    "default": "/var/lib/sitesummary/www",
    "description": "The filesystem path to the web root for sitesummary data."
  }
}
