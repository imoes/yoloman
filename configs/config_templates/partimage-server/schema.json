{
  "randfile": {
    "type": "string",
    "default": "/etc/partimaged/partimaged.rand",
    "description": "Path to the RANDFILE for SSL key generation."
  },
  "default_bits": {
    "type": "number",
    "default": 4096,
    "description": "Number of bits in the generated SSL key."
  },
  "encrypt_key": {
    "type": "bool",
    "default": true,
    "description": "Whether to encrypt the generated SSL key."
  },
  "country": {
    "type": "string",
    "default": "US",
    "description": "Country code (C) for the SSL certificate."
  },
  "state": {
    "type": "string",
    "default": "NY",
    "description": "State or province (ST) for the SSL certificate."
  },
  "locality": {
    "type": "string",
    "default": "New York",
    "description": "Locality or city (L) for the SSL certificate."
  },
  "organization": {
    "type": "string",
    "default": "Partimage Server",
    "description": "Organization name (O) for the SSL certificate."
  },
  "organizational_unit": {
    "type": "string",
    "default": "Automatically-generated Partimage SSL key",
    "description": "Organizational Unit (OU) for the SSL certificate."
  },
  "common_name": {
    "type": "string",
    "default": "localhost",
    "description": "Common Name (CN) for the SSL certificate."
  },
  "email_address": {
    "type": "string",
    "default": "postmaster@example.com",
    "description": "Email address (emailAddress) for the SSL certificate."
  }
}
