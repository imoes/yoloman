{
  "options": {
    "type": "object",
    "description": "Mapping of aircrack-ng command-line options (keys are flag names, values are values or booleans where applicable). Only documented options are included. Use bare scalar or list of scalars where appropriate; omit keys for options not used."
  },
  "options.a_mode": {
    "type": "string",
    "default": "wep",
    "description": "Attack mode: 'wep' (1) or 'wpa' (2)",
    "enum": [
      "wep",
      "wpa"
    ]
  },
  "options.essid": {
    "type": "string",
    "default": "",
    "description": "Target network ESSID (SSID); required for cloaked SSIDs or WPA"
  },
  "options.bssid": {
    "type": "string",
    "default": "",
    "description": "Target access point MAC address"
  },
  "options.nbcpu": {
    "type": "number",
    "default": 0,
    "description": "Number of CPUs to use for cracking; 0 means auto-detect (use all)"
  },
  "options.quiet": {
    "type": "bool",
    "default": false,
    "description": "Suppress status output"
  },
  "options.combine_macs": {
    "type": "list",
    "default": [],
    "description": "List of AP MAC addresses to merge into a virtual AP"
  },
  "options.key_output_file": {
    "type": "string",
    "default": "",
    "description": "Path to write the recovered key"
  },
  "options.wep_search_alphanum": {
    "type": "bool",
    "default": false,
    "description": "Search alphanumeric characters only"
  },
  "options.wep_search_bcd": {
    "type": "bool",
    "default": false,
    "description": "Search binary coded decimal characters only"
  },
  "options.wep_search_fritz": {
    "type": "bool",
    "default": false,
    "description": "Search numeric key for Fritz!Box"
  },
  "options.wep_mask": {
    "type": "string",
    "default": "",
    "description": "WEP key mask (e.g., 'A1:XX:CF')"
  },
  "options.wep_mac_filter": {
    "type": "string",
    "default": "",
    "description": "MAC filter for IVs (e.g., 'ff:ff:ff:ff:ff:ff' or a specific MAC)"
  },
  "options.wep_key_length": {
    "type": "number",
    "default": 128,
    "description": "WEP key length in bits (64, 128, \u2026)"
  },
  "options.wep_key_index": {
    "type": "number",
    "default": 0,
    "description": "WEP key index (1\u20134); 0 means ignore"
  },
  "options.wep_fudge": {
    "type": "number",
    "default": 2,
    "description": "Bruteforce fudge factor"
  },
  "options.wep_disable_korek": {
    "type": "list",
    "default": [],
    "description": "List of KoreK attack indices (1\u201317) to disable"
  },
  "options.wep_disable_bruteforce_last": {
    "type": "bool",
    "default": false,
    "description": "Disable last keybytes bruteforce (equivalent to -x0)"
  },
  "options.wep_enable_bruteforce_last_byte": {
    "type": "bool",
    "default": false,
    "description": "Enable last keybyte bruteforcing (equivalent to -x1)"
  },
  "options.wep_enable_bruteforce_last_two_bytes": {
    "type": "bool",
    "default": false,
    "description": "Enable last two keybytes bruteforcing (equivalent to -x2)"
  },
  "options.wep_disable_bruteforce_multithread": {
    "type": "bool",
    "default": false,
    "description": "Disable bruteforce multithreading (SMP only)"
  },
  "options.wep_show_ascii_key": {
    "type": "bool",
    "default": false,
    "description": "Show ASCII version of the key"
  },
  "options.wep_one_shot_ptw": {
    "type": "bool",
    "default": false,
    "description": "Run only one PTW try"
  },
  "options.wep_visual_inspection": {
    "type": "bool",
    "default": false,
    "description": "Enable visual inspection mode (KoreK-only)"
  },
  "options.wep_max_ivs": {
    "type": "number",
    "default": 0,
    "description": "Maximum number of IVs to use"
  },
  "options.wep_ptw_debug": {
    "type": "number",
    "default": 0,
    "description": "PTW debug level (1 = disable klein, 2 = PTW debug)"
  },
  "options.wordlist": {
    "type": "list",
    "default": [],
    "description": "List of dictionary files; use 'h:' prefix and ':' separators for hex keys"
  },
  "options.new_session_file": {
    "type": "string",
    "default": "",
    "description": "Session save file for -N"
  },
  "options.restore_session_file": {
    "type": "string",
    "default": "",
    "description": "Session save file to restore with -R"
  },
  "options.ewsa_output_file": {
    "type": "string",
    "default": "",
    "description": "Output file for Elcomsoft Wireless Security Auditor project"
  },
  "options.hashcat_hccapx_output_file": {
    "type": "string",
    "default": "",
    "description": "Output HCCAPX file for Hashcat v3.6+"
  },
  "options.hashcat_hccap_output_file": {
    "type": "string",
    "default": "",
    "description": "Output HCCAP file for Hashcat"
  },
  "options.wpa_speed_test": {
    "type": "bool",
    "default": false,
    "description": "Enable WPA cracking speed test"
  },
  "options.wpa_speed_test_duration": {
    "type": "number",
    "default": 0,
    "description": "Duration (seconds) of the WPA speed test"
  },
  "options.airolib_db": {
    "type": "string",
    "default": "",
    "description": "Path to airolib-ng database (cannot be used with -w)"
  },
  "options.simd": {
    "type": "string",
    "default": "generic",
    "description": "Force SIMD optimization: generic, sse2, avx, avx2, avx512, neon, asimd, altivec, power8",
    "enum": [
      "generic",
      "sse2",
      "avx",
      "avx2",
      "avx512",
      "neon",
      "asimd",
      "altivec",
      "power8"
    ]
  },
  "options.cpu_detect": {
    "type": "bool",
    "default": false,
    "description": "Print CPU detection info (implies -u)"
  },
  "input_files": {
    "type": "list",
    "default": [],
    "description": "Input capture/hash files (positional arguments: .cap, .pcap, .ivs, .hccapx)"
  }
}
