# Carburetor configuration
# This file configures the Carburetor anonymous browsing service.
# It is used by the application to set runtime options.

# Application mode (unused in practice — only relevant for --gapplication-service)
# No user-facing configuration file exists in the current upstream implementation.
# See: carburetor(1) man page (Debian trixie), section 5
app_mode = {{ app_mode | default('') }}