{
  "prompt": {
    "type": "string",
    "default": "> ",
    "description": "Input prompt string displayed to the left of the search field."
  },
  "lines": {
    "type": "number",
    "default": 10,
    "description": "Number of lines of items to display in the menu. Must be a positive integer."
  },
  "show_scores": {
    "type": "bool",
    "default": false,
    "description": "Whether to show the match score for each item."
  },
  "show_matches": {
    "type": "string",
    "default": "",
    "description": "Non-interactive mode: print matches for this query instead of entering interactive mode."
  },
  "query": {
    "type": "string",
    "default": "",
    "description": "Initial search query to pre-fill when starting fzy."
  }
}
