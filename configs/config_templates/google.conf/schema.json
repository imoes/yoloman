{
  "user_runtime": {
    "type": "string",
    "default": "0",
    "description": "User runtime directory ID (typically the UID of the lomiri user session)"
  },
  "accounts_cache_path": {
    "type": "string",
    "default": "/home/lomiri/.cache/lomiri-online-accounts",
    "description": "Path to the cache directory for Google account data"
  },
  "accounts_data_path": {
    "type": "string",
    "default": "/home/lomiri/.local/share/lomiri-online-accounts",
    "description": "Path to the persistent data directory for Google account credentials and metadata"
  },
  "provider_name": {
    "type": "string",
    "default": "",
    "description": "Optional custom provider name override (leave empty to use default)"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debug logging for troubleshooting"
  },
  "workers": {
    "type": "list",
    "default": [],
    "description": "List of worker configurations for token refresh handling",
    "items": {
      "provider": {
        "type": "string",
        "default": "google",
        "description": "Provider name this worker handles"
      },
      "min_interval": {
        "type": "number",
        "default": 900,
        "description": "Minimum interval (seconds) between refresh attempts"
      },
      "max_interval": {
        "type": "number",
        "default": 3600,
        "description": "Maximum interval (seconds) between refresh attempts"
      },
      "batch_size": {
        "type": "number",
        "default": 10,
        "description": "Max number of accounts refreshed per batch"
      },
      "enabled": {
        "type": "bool",
        "default": true,
        "description": "Whether this worker is active"
      }
    }
  }
}
