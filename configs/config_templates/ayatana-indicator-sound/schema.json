{
  "exec_path": {
    "type": "string",
    "default": "/usr/libexec/ayatana-indicator-sound/ayatana-indicator-sound-service",
    "description": "Path to the ayatana-indicator-sound-service executable"
  },
  "only_show_in": {
    "type": "string",
    "default": "MATE;XFCE;",
    "description": "Desktop environments where the service should be shown (semicolon-separated)"
  },
  "no_display": {
    "type": "bool",
    "default": true,
    "description": "Whether to hide the entry from menus"
  },
  "startup_notify": {
    "type": "bool",
    "default": false,
    "description": "Whether to send startup notification"
  },
  "terminal": {
    "type": "bool",
    "default": false,
    "description": "Whether the service runs in a terminal"
  }
}
