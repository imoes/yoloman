{
  "citadel_client_encrypt": {
    "type": "string",
    "default": "default",
    "description": "Whether to force SSL/TLS encryption: yes, no, or default",
    "enum": [
      "yes",
      "no",
      "default"
    ]
  },
  "citadel_client_ansi_color": {
    "type": "string",
    "default": "user",
    "description": "ANSI color usage: on, off, auto, or user",
    "enum": [
      "on",
      "off",
      "auto",
      "user"
    ]
  },
  "citadel_client_use_background": {
    "type": "string",
    "default": "off",
    "description": "Whether to use terminal background colors: on or off",
    "enum": [
      "on",
      "off"
    ]
  },
  "citadel_client_status_line": {
    "type": "string",
    "default": "off",
    "description": "Whether to show status line: on or off",
    "enum": [
      "on",
      "off"
    ]
  },
  "citadel_client_prompt_control": {
    "type": "string",
    "default": "user",
    "description": "Prompt control behavior: on, off, or user",
    "enum": [
      "on",
      "off",
      "user"
    ]
  },
  "citadel_client_display_message_numbers": {
    "type": "string",
    "default": "0",
    "description": "Whether to display message numbers: 1 or 0",
    "enum": [
      "1",
      "0"
    ]
  },
  "citadel_client_force_mail_prompts": {
    "type": "string",
    "default": "1",
    "description": "Whether to force prompts for mail rooms: 1 or 0",
    "enum": [
      "1",
      "0"
    ]
  },
  "citadel_client_use_floors": {
    "type": "string",
    "default": "DEFAULT",
    "description": "Whether to use floors: YES, NO, or DEFAULT",
    "enum": [
      "YES",
      "NO",
      "DEFAULT"
    ]
  },
  "citadel_client_beep": {
    "type": "string",
    "default": "1",
    "description": "Whether to beep on instant messages: 1 or 0",
    "enum": [
      "1",
      "0"
    ]
  },
  "citadel_client_allow_attachments": {
    "type": "string",
    "default": "0",
    "description": "Whether to allow file attachments: 1 or 0",
    "enum": [
      "1",
      "0"
    ]
  },
  "citadel_client_idle_threshold": {
    "type": "string",
    "default": "900",
    "description": "Idle timeout in seconds before marking user idle"
  },
  "citadel_client_remember_passwords": {
    "type": "string",
    "default": "0",
    "description": "Whether to remember passwords: 1 or 0",
    "enum": [
      "1",
      "0"
    ]
  },
  "citadel_client_opencmd": {
    "type": "string",
    "default": "xdg-open \"%s\"",
    "description": "Command used to open attachments via 'O' key"
  }
}
