{
  "icon": {
    "type": "string",
    "default": "preferences-system-power-management",
    "description": "The icon name for the Power Management desktop entry."
  },
  "exec_path": {
    "type": "string",
    "default": "/usr/lib/x86_64-linux-gnu/libexec/org_kde_powerdevil",
    "description": "The full path to the powerdevil executable."
  },
  "only_show_in": {
    "type": "string",
    "default": "KDE;",
    "description": "Semicolon-separated list of desktop environments where this application should appear."
  },
  "autostart_phase": {
    "type": "string",
    "default": "0",
    "description": "The autostart phase for the KDE application."
  }
}
