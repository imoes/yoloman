{
  "destination": {
    "type": "string",
    "default": "",
    "description": "Target location for file downloads (global destination directory)"
  },
  "directory": {
    "type": "string",
    "default": "",
    "description": "Exact location for file downloads (overrides destination per extraction)"
  },
  "filename": {
    "type": "string",
    "default": "",
    "description": "Filename format string for downloaded files"
  },
  "retries": {
    "type": "number",
    "default": 4,
    "description": "Maximum number of retries for failed HTTP requests (-1 for infinite)"
  },
  "sleep": {
    "type": "string",
    "default": "",
    "description": "Seconds to wait before each download (e.g., '2.0-3.5' or '1.5')"
  },
  "sleep_request": {
    "type": "string",
    "default": "",
    "description": "Seconds to wait between HTTP requests during extraction"
  },
  "sleep_extractor": {
    "type": "string",
    "default": "",
    "description": "Seconds to wait before starting extraction for an input URL"
  },
  "limit_rate": {
    "type": "string",
    "default": "",
    "description": "Maximum download rate (e.g., '500k' or '2.5M')"
  },
  "no_part": {
    "type": "bool",
    "default": false,
    "description": "Disable use of .part files during downloads"
  },
  "no_skip": {
    "type": "bool",
    "default": false,
    "description": "Disable skipping downloads; overwrite existing files"
  },
  "no_mtime": {
    "type": "bool",
    "default": false,
    "description": "Do not set file modification times from Last-Modified headers"
  },
  "no_download": {
    "type": "bool",
    "default": false,
    "description": "Simulate only: do not download any files"
  },
  "download_archive": {
    "type": "string",
    "default": "",
    "description": "Path to archive file for tracking already downloaded/skipped files"
  },
  "range": {
    "type": "string",
    "default": "",
    "description": "Index range(s) specifying which files to download (e.g., '5', '8-20')"
  },
  "chapter_range": {
    "type": "string",
    "default": "",
    "description": "Chapter/index range for manga and delegated content"
  },
  "filter": {
    "type": "string",
    "default": "",
    "description": "Python expression controlling which files to download"
  },
  "chapter_filter": {
    "type": "string",
    "default": "",
    "description": "Python expression filtering chapters"
  },
  "filesize_min": {
    "type": "string",
    "default": "",
    "description": "Minimum file size to download (e.g., '500k')"
  },
  "filesize_max": {
    "type": "string",
    "default": "",
    "description": "Maximum file size to download (e.g., '2.5M')"
  },
  "http_timeout": {
    "type": "number",
    "default": 30.0,
    "description": "Timeout for HTTP connections in seconds"
  },
  "proxy": {
    "type": "string",
    "default": "",
    "description": "HTTP(S) proxy URL to use"
  },
  "source_address": {
    "type": "string",
    "default": "",
    "description": "Client-side IP address to bind to"
  },
  "force_ipv4": {
    "type": "bool",
    "default": false,
    "description": "Force all connections to use IPv4"
  },
  "force_ipv6": {
    "type": "bool",
    "default": false,
    "description": "Force all connections to use IPv6"
  },
  "no_check_certificate": {
    "type": "bool",
    "default": false,
    "description": "Disable HTTPS certificate validation"
  },
  "netrc": {
    "type": "bool",
    "default": false,
    "description": "Enable .netrc authentication"
  },
  "cookies": {
    "type": "string",
    "default": "",
    "description": "Path to file containing additional cookies"
  },
  "cookies_from_browser": {
    "type": "string",
    "default": "",
    "description": "Browser to load cookies from, optionally with DOMAIN, KEYRING, PROFILE, CONTAINER"
  },
  "write_metadata": {
    "type": "bool",
    "default": false,
    "description": "Write metadata to separate JSON files"
  },
  "write_info_json": {
    "type": "bool",
    "default": false,
    "description": "Write gallery metadata to info.json"
  },
  "write_tags": {
    "type": "bool",
    "default": false,
    "description": "Write image tags to separate text files"
  },
  "zip": {
    "type": "bool",
    "default": false,
    "description": "Store downloaded files in ZIP archive"
  },
  "cbz": {
    "type": "bool",
    "default": false,
    "description": "Store downloaded files in CBZ archive"
  },
  "mtime": {
    "type": "string",
    "default": "",
    "description": "Source metadata field to set file modification time (e.g., 'date')"
  },
  "postprocessors": {
    "type": "list",
    "default": [],
    "description": "List of postprocessor names/objects (e.g., ['ffmpeg', 'exiftool'])"
  },
  "exec": {
    "type": "string",
    "default": "",
    "description": "Command to execute per downloaded file (supports {} placeholders)"
  },
  "exec_after": {
    "type": "string",
    "default": "",
    "description": "Command to execute after all downloads complete"
  },
  "ugoira": {
    "type": "string",
    "default": "webm",
    "description": "Ugoira conversion format (e.g., 'webm', 'mp4')",
    "enum": [
      "webm",
      "mp4",
      "gif",
      "vp8",
      "vp9",
      "vp9-lossless",
      "copy",
      "zip"
    ]
  },
  "rename": {
    "type": "string",
    "default": "",
    "description": "Rename previously downloaded files from FORMAT to current format"
  },
  "rename_to": {
    "type": "string",
    "default": "",
    "description": "Rename previously downloaded files from current format to FORMAT"
  },
  "abort": {
    "type": "number",
    "default": 0,
    "description": "Stop after N consecutive skipped downloads"
  },
  "terminate": {
    "type": "number",
    "default": 0,
    "description": "Stop extractor and parent runs after N consecutive skipped downloads"
  },
  "user_agent": {
    "type": "string",
    "default": "",
    "description": "User-Agent header to use in HTTP requests"
  },
  "browser": {
    "type": "string",
    "default": "",
    "description": "Browser profile for extractor (e.g., 'firefox')"
  },
  "print": {
    "type": "string",
    "default": "prepare",
    "description": "Print EVENT:FORMAT during download (default event is 'prepare')",
    "enum": [
      "prepare",
      "download",
      "metadata"
    ]
  },
  "print_to_file": {
    "type": "string",
    "default": "",
    "description": "Append EVENT:FORMAT to file during download"
  },
  "error_file": {
    "type": "string",
    "default": "",
    "description": "File to record input URLs that returned an error"
  },
  "write_log": {
    "type": "string",
    "default": "",
    "description": "Write logging output to this file"
  },
  "write_unsupported": {
    "type": "string",
    "default": "",
    "description": "File to record unsupported URLs"
  },
  "extractors": {
    "type": "list",
    "default": [],
    "description": "List of per-extractor configurations with name, username, password, and options fields"
  },
  "extra_options": {
    "type": "object",
    "default": null,
    "description": "Arbitrary key-value settings for advanced or extractor-specific options"
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose output"
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Enable quiet mode"
  },
  "no_colors": {
    "type": "bool",
    "default": false,
    "description": "Disable ANSI color codes in output"
  },
  "extractors.name": {
    "type": "string",
    "default": "",
    "description": "Extractor name (e.g., 'pixiv', 'reddit')"
  },
  "extractors.username": {
    "type": "string",
    "default": "",
    "description": "Username for this extractor"
  },
  "extractors.password": {
    "type": "string",
    "default": "",
    "description": "Password for this extractor"
  },
  "extractors.options": {
    "type": "object",
    "default": null,
    "description": "Extractor-specific options"
  }
}
