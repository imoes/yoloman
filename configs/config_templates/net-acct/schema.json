{
  "pidfile": {
    "type": "string",
    "default": "/var/run/nacctd.pid",
    "description": "Path to the PID file for the nacctd daemon."
  },
  "conffile": {
    "type": "string",
    "default": "/etc/naccttab",
    "description": "Path to the nacctd configuration file."
  }
}
