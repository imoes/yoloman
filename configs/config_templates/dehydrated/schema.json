{
  "config_d": {
    "type": "string",
    "default": "/etc/dehydrated/conf.d",
    "description": "Directory containing additional configuration files (conf.d style)"
  },
  "base_dir": {
    "type": "string",
    "default": "/var/lib/dehydrated",
    "description": "Base directory for dehydrated state and challenge files"
  },
  "domains_txt": {
    "type": "string",
    "default": "/etc/dehydrated/domains.txt",
    "description": "Path to file listing domains and their aliases"
  }
}
