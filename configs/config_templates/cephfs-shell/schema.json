{
  "prompt": {
    "type": "string",
    "default": "CephFS:~/>>>",
    "description": "Prompt string for the interactive shell"
  },
  "continuation_prompt": {
    "type": "string",
    "default": ">",
    "description": "Continuation prompt used for multi-line input"
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Suppress command echo in batch mode"
  },
  "timing": {
    "type": "bool",
    "default": false,
    "description": "Display elapsed time after command execution"
  },
  "colors": {
    "type": "bool",
    "default": true,
    "description": "Enable colored output"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable debug output"
  },
  "abbrev": {
    "type": "bool",
    "default": false,
    "description": "Enable command abbreviation"
  },
  "autorun_on_edit": {
    "type": "bool",
    "default": false,
    "description": "Automatically run edited script after edit command"
  },
  "echo": {
    "type": "bool",
    "default": false,
    "description": "Echo commands in batch mode"
  },
  "editor": {
    "type": "string",
    "default": "vim",
    "description": "Default text editor to use with 'edit' command"
  },
  "feedback_to_output": {
    "type": "bool",
    "default": false,
    "description": "Include command output in feedback"
  },
  "locals_in_py": {
    "type": "bool",
    "default": true,
    "description": "Allow local variables in 'py' and 'pyscript' commands"
  }
}
