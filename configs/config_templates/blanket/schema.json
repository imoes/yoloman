{
  "settings": {
    "type": "list",
    "default": [],
    "description": "blanket(1) does not use a configuration file. This list is retained only for compatibility with deployment tooling that may generate wrapper scripts passing arguments. No official config file format exists."
  }
}
