{
  "settings": [
    {
      "key": "volume",
      "value": "0.8",
      "description": "Initial volume factor (0.0\u20131.0); overrides system default. Not documented in manpage, but blanket uses volume controls internally."
    },
    {
      "key": "mixer",
      "value": "master",
      "description": "Audio mixer control name (e.g., 'master', 'PCM'). Common convention; blanket uses PulseAudio/ALSA mixer names."
    }
  ]
}
