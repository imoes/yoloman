{
  "collection_files": {
    "type": "list",
    "default": [],
    "description": "List of paths to emulecollection files to process. Each item is a string representing a filesystem path to an .emc file. Default is an empty list, meaning no files are processed unless specified. Paths may be absolute or relative to the working directory. Required for meaningful operation."
  }
}
