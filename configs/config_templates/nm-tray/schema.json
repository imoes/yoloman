{
  "name": {
    "type": "string",
    "default": "nm-tray",
    "description": "Display name for the application entry"
  },
  "try_exec": {
    "type": "string",
    "default": "nm-tray",
    "description": "Executable path for TryExec directive"
  },
  "exec": {
    "type": "string",
    "default": "nm-tray",
    "description": "Executable path for Exec directive"
  },
  "not_show_in": {
    "type": "string",
    "default": "KDE;GNOME;",
    "description": "Semicolon-separated list of desktop environments where this entry should not appear"
  },
  "lxqt_need_tray": {
    "type": "bool",
    "default": true,
    "description": "Whether the LXQt desktop environment requires this entry to be in the system tray"
  }
}
