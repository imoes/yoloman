{
  "bus": {
    "type": "string",
    "default": "session",
    "description": "D-Bus bus to connect to (typically 'system' or 'session')\n      enum: ['system', 'session']",
    "enum": [
      "system",
      "session"
    ],
    "enum_open": true
  }
}
