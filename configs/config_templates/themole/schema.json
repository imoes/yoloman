{
  "url": {
    "type": "string",
    "default": "http://example.com/page?id=1",
    "description": "The target URL including parameters to be scanned."
  },
  "needle": {
    "type": "string",
    "default": "Success",
    "description": "A string that appears in the response when the injection is successful and disappears when it fails."
  },
  "threads": {
    "type": "number",
    "default": 4,
    "description": "The max number of concurrent requests that the mole will be making."
  }
}
