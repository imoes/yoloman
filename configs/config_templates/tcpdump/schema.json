{
  "profile_name": {
    "type": "string",
    "default": "tcpdump",
    "description": "The name of the AppArmor profile."
  },
  "binary_path": {
    "type": "string",
    "default": "/usr/bin/tcpdump",
    "description": "The full path to the tcpdump binary."
  },
  "binary_path_file": {
    "type": "string",
    "default": "usr.bin.tcpdump",
    "description": "The filename portion of the binary path, used for the local include path."
  }
}
