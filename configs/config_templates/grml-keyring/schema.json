{
  "vendor_name": {
    "type": "string",
    "default": "Grml",
    "description": "Name of the vendor or distribution."
  },
  "vendor_url": {
    "type": "string",
    "default": "http://grml.org/",
    "description": "URL of the vendor's website."
  },
  "vendor_description": {
    "type": "string",
    "default": "Debian based Linux Live system for system administrators",
    "description": "Short description of the vendor's product."
  },
  "bugs_email": {
    "type": "string",
    "default": "mailto:bugs@grml.org",
    "description": "Email address for bug reports in mailto: URI format."
  }
}
