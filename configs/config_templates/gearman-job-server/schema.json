{
  "gearman_listen": {
    "type": "string",
    "default": "--listen=localhost",
    "description": "Listen address and port options for gearmand. Typically --listen=<host>[:<port>]"
  },
  "gearman_daemon": {
    "type": "string",
    "default": "--daemon",
    "description": "Whether to run gearmand as a daemon. Set to empty string to disable daemon mode."
  },
  "gearman_log_file": {
    "type": "string",
    "default": "/var/log/gearman-job-server/gearmand.log",
    "description": "Path to the gearmand log file."
  }
}
