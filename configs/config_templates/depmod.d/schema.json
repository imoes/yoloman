{
  "search_dirs": {
    "type": "list",
    "default": [],
    "description": "Additional directories to search for kernel modules, beyond the default /lib/modules and /usr/lib/modules. Paths must exist and contain a valid modules.dep file.",
    "items": {
      "type": "string",
      "description": "Absolute path to a directory containing kernel modules (e.g., /opt/kernel-modules)"
    }
  },
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose logging for depmod operations. Only needed for troubleshooting; normally left disabled."
  }
}
