{
  "interface": {
    "type": "string",
    "default": "eth0",
    "description": "Network interface to listen on (e.g., eth0, ppp0)"
  },
  "filter": {
    "type": "string",
    "default": "",
    "description": "BPF capture filter string (e.g., 'port 80', 'tcp')"
  },
  "write_mode": {
    "type": "string",
    "default": "R",
    "description": "Write mode for stream reconstruction: R (raw), P (printable), U (unprintable as hex), H (hex-spaced)"
  },
  "display_mode": {
    "type": "string",
    "default": "P",
    "description": "Display mode for payload: P (printable), R (raw), U (unprintable as hex), H (hex-spaced)"
  },
  "max_connections": {
    "type": "number",
    "default": 0,
    "description": "Max connections to track. 0 means unlimited."
  },
  "max_packets": {
    "type": "number",
    "default": 0,
    "description": "Max packets to sniff before exiting. 0 means unlimited."
  },
  "max_closed_connections": {
    "type": "number",
    "default": 0,
    "description": "Exit after this many connections are CLOSED. 0 means unlimited."
  },
  "conn_timeout": {
    "type": "number",
    "default": 600,
    "description": "Connection inactivity timeout in seconds"
  },
  "verbose": {
    "type": "number",
    "default": 0,
    "description": "Verbosity level 0-5. 0 suppresses extra messages."
  },
  "use_colors": {
    "type": "bool",
    "default": false,
    "description": "Use terminal colors"
  },
  "resolve_hosts": {
    "type": "bool",
    "default": false,
    "description": "Display host names instead of IP addresses"
  },
  "no_promiscuous": {
    "type": "bool",
    "default": false,
    "description": "Don't put interface in promiscuous mode"
  },
  "create_dirs": {
    "type": "bool",
    "default": false,
    "description": "Create directories to store sniffed sessions"
  },
  "dirs_per_batch": {
    "type": "number",
    "default": 10,
    "description": "Number of sessions per directory before creating a new one"
  },
  "filenaming_scheme": {
    "type": "number",
    "default": 1,
    "description": "Filenaming system: 1 (client_server.side) or 2 (connectionnum_client_server.side)"
  },
  "use_separator": {
    "type": "bool",
    "default": false,
    "description": "Add a separator for displayed payloads"
  },
  "timestamp_format": {
    "type": "string",
    "default": "",
    "description": "Timestamp format: 't' for h:m:s:us, 'td' for day-month-year, '' for none"
  },
  "pipe_mode": {
    "type": "string",
    "default": "",
    "description": "Pipe mode: 'client', 'server', or '' for normal operation",
    "enum": [
      "client",
      "server"
    ]
  }
}
