{
 "COROSYNC_QNETD_OPTIONS": {
  "description": "Options passed to corosync-qnetd command",
  "enum": [
   "-4",
   "-6",
   "-d",
   "-f",
   "-h",
   "-v"
  ],
  "type": "string"
 },
 "COROSYNC_QNETD_RUNAS": {
  "default": "",
  "description": "User under which qnetd daemon should be running (empty means default user)",
  "type": "string"
 }
}
