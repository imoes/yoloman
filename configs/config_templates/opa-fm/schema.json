{
  "config_dir": {
    "type": "string",
    "default": "/etc",
    "description": "Configuration directory base path"
  },
  "max_instance": {
    "type": "number",
    "default": 7,
    "description": "Largest instance number, for when config file bad"
  },
  "ifs_fm_base": {
    "type": "string",
    "default": "/usr/lib/opa-fm",
    "description": "Base directory for OPA Fabric Manager binaries and runtime"
  }
}
