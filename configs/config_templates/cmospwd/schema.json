{
  "dump_cmos": {
    "type": "bool",
    "default": false,
    "description": "If true, dump CMOS memory to stdout (uses -d option)."
  },
  "kill_cmos": {
    "type": "bool",
    "default": false,
    "description": "If true, clear/kill CMOS password (uses -k option)."
  },
  "force_mode": {
    "type": "bool",
    "default": false,
    "description": "If true, enable force mode to bypass safety checks (uses -R option)."
  },
  "keyboard_layout": {
    "type": "string",
    "default": "",
    "description": "Keyboard layout: 'french_azerty' or 'german_qwertz'. Empty string for default US QWERTY."
  },
  "cmos_backup_action": {
    "type": "string",
    "default": "",
    "description": "Action to perform with CMOS backup file: 'write', 'load', 'restore', or 'set'. Empty string disables all backup-related operations.",
    "enum": [
      "write",
      "load",
      "restore"
    ]
  },
  "cmos_backup_file": {
    "type": "string",
    "default": "",
    "description": "Path to CMOS backup file used with -w/-l/-r/-s options. Must be set when cmos_backup_action is non-empty."
  },
  "execute_module_list": {
    "type": "list",
    "default": [],
    "description": "List of CMOS module numbers (integers) to execute (e.g., [3,6,7]). Each element corresponds to -m<module> option."
  }
}
