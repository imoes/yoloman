{
  "project_name": {
    "type": "string",
    "default": "neutron",
    "description": "The OpenStack project name used for paths and naming conventions."
  },
  "system_user": {
    "type": "string",
    "default": "neutron",
    "description": "The system user under which the service runs."
  },
  "system_group": {
    "type": "string",
    "default": "neutron",
    "description": "The system group under which the service runs."
  }
}
