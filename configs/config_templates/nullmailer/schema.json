{
  "admin_addr": {
    "type": "string",
    "default": "postmaster@localhost",
    "description": "The email address used as the sender for bounce messages and other system-generated emails (e.g., 'From: postmaster@example.com'). Must be a valid email address."
  }
}
