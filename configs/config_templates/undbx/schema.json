{
  "dbx_source": {
    "type": "string",
    "default": "/var/lib/undbx/inbox.dbx",
    "description": "Path to the source .dbx file or directory containing Outlook Express database files."
  },
  "output_dir": {
    "type": "string",
    "default": "/var/lib/undbx/output",
    "description": "Path to the directory where extracted .eml files will be stored. If empty, uses current directory."
  },
  "recover_mode": {
    "type": "bool",
    "default": false,
    "description": "If true, enables --recover mode to extract emails from corrupted files."
  }
}
