{
  "cron_minute": {
    "type": "string",
    "default": "18",
    "description": "Minute of the hour when the sa-sync cron job runs (0-59)"
  },
  "cron_hour": {
    "type": "string",
    "default": "*/3",
    "description": "Hour(s) when the sa-sync cron job runs (e.g., */3 for every 3 hours)"
  },
  "clean_cron_minute": {
    "type": "string",
    "default": "24",
    "description": "Minute of the hour when the sa-clean cron job runs (0-59)"
  },
  "clean_cron_hour": {
    "type": "string",
    "default": "1",
    "description": "Hour of the day when the sa-clean cron job runs (0-23)"
  }
}
