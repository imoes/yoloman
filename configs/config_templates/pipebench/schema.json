{
  "error_exit": {
    "type": "bool",
    "default": false,
    "description": "Exit immediately if an error occurs, breaking the pipe."
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Only show summary statistics."
  },
  "silent": {
    "type": "bool",
    "default": false,
    "description": "Don't show running speed or summary stats."
  },
  "no_summary": {
    "type": "bool",
    "default": false,
    "description": "Don't show the final summary."
  },
  "buffer_size": {
    "type": "number",
    "default": 65536,
    "description": "Buffer size in bytes for I/O operations."
  },
  "raw_speed": {
    "type": "bool",
    "default": false,
    "description": "Show only raw speed, no fancy stats or summary."
  },
  "status_file": {
    "type": "string",
    "default": "/dev/null",
    "description": "Path to write status updates to instead of stderr."
  },
  "decimal_units": {
    "type": "bool",
    "default": false,
    "description": "Use 1kB = 1000B instead of the default 1024B."
  },
  "no_units": {
    "type": "bool",
    "default": false,
    "description": "Don't convert to units (kilo, Mega, etc...), show raw bytes."
  }
}
