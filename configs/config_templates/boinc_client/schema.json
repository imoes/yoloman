{
  "project_auth_files": {
    "type": "list",
    "default": [],
    "description": "List of absolute paths to project authentication files for autorun projects.",
    "items": {
      "type": "string",
      "description": "Absolute path to an .auth_xfer file."
    }
  },
  "use_http_proxy": {
    "type": "bool",
    "default": false,
    "description": "Enable HTTP proxy for all BOINC network traffic."
  },
  "http_proxy_host": {
    "type": "string",
    "default": "",
    "description": "Hostname or IP address of HTTP proxy server."
  },
  "http_proxy_port": {
    "type": "number",
    "default": 8080,
    "description": "Port number of HTTP proxy server."
  },
  "http_proxy_username": {
    "type": "string",
    "default": "",
    "description": "Username for authenticated HTTP proxy (optional)."
  },
  "http_proxy_password": {
    "type": "string",
    "default": "",
    "description": "Password for authenticated HTTP proxy (optional)."
  },
  "max_memory_percent": {
    "type": "number",
    "default": 90,
    "description": "Maximum percentage of total system memory to use for computation (0\u2013100)."
  },
  "max_disk_percent": {
    "type": "number",
    "default": 20,
    "description": "Maximum percentage of total disk space to use for BOINC data (0\u2013100)."
  },
  "max_disk_used": {
    "type": "number",
    "default": "",
    "description": "Maximum disk space to use for BOINC (in GB). Overrides max_disk_percent if set."
  },
  "max_memory_in_use": {
    "type": "number",
    "default": "",
    "description": "Maximum memory to use for BOINC (in MB). Overrides max_memory_percent if set."
  },
  "cpu_time_limit": {
    "type": "number",
    "default": 0,
    "description": "Maximum CPU time to use per day (in minutes). 0 = unlimited."
  },
  "suspend_cpu_usage": {
    "type": "number",
    "default": 90,
    "description": "Suspend computation if system CPU usage exceeds this percentage (0\u2013100)."
  },
  "network_suspend_days": {
    "type": "number",
    "default": 7,
    "description": "Suspend network activity after this many days without work (0 = never)."
  },
  "run_profiler": {
    "type": "bool",
    "default": false,
    "description": "Run the built-in profiler on startup (for benchmarking only)."
  },
  "max_ncpus": {
    "type": "number",
    "default": 0,
    "description": "Maximum number of CPU cores to use. 0 = auto-detect."
  },
  "gpu_usage_limit": {
    "type": "number",
    "default": 100,
    "description": "Maximum percentage of GPU capacity to use (0\u2013100)."
  },
  "gpu_only_when_idle": {
    "type": "bool",
    "default": true,
    "description": "Only use GPU when the system appears to be idle (detects mouse/keyboard activity)."
  },
  "sleep_interval": {
    "type": "number",
    "default": 30,
    "description": "Polling interval (seconds) when tasks are running."
  },
  "max_sleep_interval": {
    "type": "number",
    "default": 300,
    "description": "Maximum polling interval (seconds) when idle."
  },
  "start_delay": {
    "type": "number",
    "default": 30,
    "description": "Delay (seconds) before starting first computation job."
  },
  "dont_verify_images": {
    "type": "bool",
    "default": false,
    "description": "Skip image verification (dangerous \u2014 only for development/testing)."
  },
  "use_all_gpus": {
    "type": "bool",
    "default": true,
    "description": "Use all detected GPUs (if available)."
  },
  "no_gpus": {
    "type": "bool",
    "default": false,
    "description": "Disable GPU use entirely, even if GPUs are detected."
  },
  "allow_remote_gui_rpc": {
    "type": "bool",
    "default": false,
    "description": "Allow remote GUI clients to control this BOINC instance (e.g., BOINC Manager)."
  },
  "gui_rpc_port": {
    "type": "number",
    "default": 0,
    "description": "TCP port for GUI RPC interface. 0 = auto-assign. Used only if allow_remote_gui_rpc is enabled."
  },
  "gui_rpc_password": {
    "type": "string",
    "default": "",
    "description": "Password for GUI RPC authentication (required if allow_remote_gui_rpc is enabled)."
  },
  "auth_domain_name": {
    "type": "string",
    "default": "",
    "description": "Authentication domain name for multi-host setups (e.g., cluster environments)."
  },
  "core_client_only": {
    "type": "bool",
    "default": false,
    "description": "Run only the background client (no graphics/console UI)."
  },
  "checkpoint_freq": {
    "type": "number",
    "default": 600,
    "description": "Frequency (seconds of CPU time) at which task checkpoints are written."
  },
  "max_file_xfers": {
    "type": "number",
    "default": 5,
    "description": "Maximum number of concurrent file downloads/uploads."
  },
  "max_file_xfers_per_project": {
    "type": "number",
    "default": 2,
    "description": "Maximum number of concurrent file transfers per project."
  },
  "min_file_xfers": {
    "type": "number",
    "default": 1,
    "description": "Minimum number of concurrent file transfers to maintain."
  },
  "file_xfer_min_priority": {
    "type": "number",
    "default": 0,
    "description": "Minimum priority (0 = highest) for file transfers."
  },
  "scheduler_poll_min_interval": {
    "type": "number",
    "default": 300,
    "description": "Minimum time (seconds) between scheduler polls (min 60)."
  },
  "log_flags": {
    "type": "list",
    "default": [],
    "description": "Enable logging for specific subsystems (e.g., 'http', 'rpc', 'task').",
    "items": {
      "type": "string",
      "enum": [
        "http",
        "rpc",
        "task",
        "file_xfer",
        "sched",
        "coproc",
        "debug"
      ],
      "description": "Log flag name. Choose from: http, rpc, task, file_xfer, sched, coproc, debug."
    }
  },
  "debug_flags": {
    "type": "list",
    "default": [],
    "description": "Enable verbose debug output for specific modules (e.g., 'http', 'rpc').",
    "items": {
      "type": "string",
      "description": "Debug flag name (e.g., 'http', 'rpc', 'sched')."
    }
  },
  "default_project_preferences": {
    "type": "object",
    "default": {},
    "description": "Default resource and scheduling preferences applied when attaching to new projects.",
    "items": {
      "run_on_batteries": {
        "type": "bool",
        "description": "Allow computation on battery power.",
        "default": false
      },
      "run_if_user_active": {
        "type": "bool",
        "description": "Compute only if the user is idle (no mouse/keyboard activity).",
        "default": true
      },
      "max_jobs_run": {
        "type": "number",
        "description": "Maximum number of jobs to run simultaneously per project.",
        "default": 0
      },
      "dont_suspend_natively": {
        "type": "bool",
        "description": "Use only BOINC-level suspension (not OS-level).",
        "default": false
      },
      "network_usage_limit": {
        "type": "number",
        "description": "Max MB per day for network transfers (0 = unlimited).",
        "default": 0
      },
      "day_of_week": {
        "type": "number",
        "description": "Day of week to start/stop (0 = Sunday, 6 = Saturday).",
        "default": 0
      },
      "start_time": {
        "type": "string",
        "description": "Start time in HH:MM format (24-hour).",
        "default": "00:00"
      },
      "end_time": {
        "type": "string",
        "description": "End time in HH:MM format (24-hour).",
        "default": "23:59"
      },
      "start_day_of_week": {
        "type": "number",
        "description": "Day to begin a weekly schedule window.",
        "default": 0
      },
      "end_day_of_week": {
        "type": "number",
        "description": "Day to end a weekly schedule window.",
        "default": 6
      }
    }
  },
  "coproc_config": {
    "type": "list",
    "default": [],
    "description": "Explicit configuration for coprocessors (e.g., NVIDIA/AMD GPUs) if auto-detection fails or custom limits are needed.",
    "items": {
      "type": "object",
      "description": "Define a coprocessor type and how many to use.",
      "items": {
        "type": {
          "type": "string",
          "description": "Coprocessor type (e.g., 'nvidia', 'cuda', 'amd', 'opencl').",
          "default": "nvidia"
        },
        "count": {
          "type": "number",
          "description": "Number of instances to use (0 = disable).",
          "default": 1
        }
      }
    }
  }
}
