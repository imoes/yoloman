{
  "debug": {
    "type": "number",
    "default": "0",
    "description": "Set to 1 to enable debugging output."
  },
  "verbosity": {
    "type": "number",
    "default": "0",
    "description": "Set to 1 to enable verbose output."
  },
  "conf_source_dir": {
    "type": "string",
    "default": "/some/path/",
    "description": "The source directory where historical md5sums are looked for."
  },
  "conf_force_conffold": {
    "type": "string",
    "default": "YES",
    "description": "Force retaining the old configuration file. Set to YES to enable."
  },
  "conf_force_conffnew": {
    "type": "string",
    "default": "YES",
    "description": "Force overriding with the new configuration file. Set to YES to enable."
  }
}
