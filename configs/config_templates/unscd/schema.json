{
  "name": {
    "type": "string",
    "default": "unscd",
    "description": "The name of the daemon/service as referenced in the init script."
  },
  "desc": {
    "type": "string",
    "default": "Micro Name Service Cache Daemon",
    "description": "The human-readable description of the daemon."
  },
  "daemon": {
    "type": "string",
    "default": "/usr/sbin/nscd",
    "description": "The path to the actual daemon executable binary."
  },
  "pidfile": {
    "type": "string",
    "default": "/var/run/nscd/nscd.pid",
    "description": "The file path where the daemon writes its PID."
  }
}
