{
  "PROJECT_NAME": {
    "type": "string",
    "default": "neutron",
    "description": "The OpenStack project name, used for constructing service names, paths, and user/group identities."
  },
  "SYSTEM_USER": {
    "type": "string",
    "default": "neutron",
    "description": "The system user account under which the service runs."
  },
  "SYSTEM_GROUP": {
    "type": "string",
    "default": "neutron",
    "description": "The system group under which the service runs."
  },
  "CONFIG_FILE": {
    "type": "string",
    "default": "/etc/neutron/neutron.conf",
    "description": "The default configuration file path for the neutron project."
  }
}
