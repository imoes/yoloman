{
  "primary_type": {
    "type": "string",
    "default": "text/plain",
    "description": "The default MIME type used for unspecified content."
  },
  "categories": {
    "type": "list",
    "default": [],
    "description": "List of media categories (e.g., Communication, Arts, Computing) to support."
  },
  "debug_mode": {
    "type": "bool",
    "default": false,
    "description": "Enable debug logging when true."
  },
  "log_path": {
    "type": "string",
    "default": "/var/log/media-types.log",
    "description": "Path to the application log file."
  }
}
