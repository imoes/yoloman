{
  "base_name": {
    "type": "string",
    "default": "docker-registry",
    "description": "Base name of the service/daemon; used for PID file, log file, init script name, and process identification."
  },
  "exec_user": {
    "type": "string",
    "default": "docker-registry",
    "description": "System user account under which the registry service will run (for security hardening)."
  }
}
