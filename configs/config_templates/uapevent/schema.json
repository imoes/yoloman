{
  "enabled_version_display": {
    "type": "bool",
    "default": false,
    "description": "If true, the '-v' flag is active, causing uapevent to print its version and exit."
  },
  "enabled_help_display": {
    "type": "bool",
    "default": false,
    "description": "If true, the '-h' flag is active, causing uapevent to print a summary of options and exit."
  }
}
