{
 "cfg": {
  "source": "inferred-from-template",
  "type": "object"
 },
 "resources": {
  "default": [],
  "description": "List of DRBD resources to configure. Each resource defines replicated storage between two or more nodes.",
  "items": {
   "config": {
    "default": {},
    "description": "Global configuration for this resource.",
    "fields": {
     "device": {
      "default": "/dev/drbd0",
      "description": "Block device path exposed to the OS (e.g., /dev/drbd0). Must match on all nodes.",
      "type": "string"
     },
     "disk": {
      "default": "",
      "description": "Underlying physical disk or partition to use (e.g., /dev/sdb1) — leave empty if overridden per host.",
      "type": "string"
     },
     "meta_disk": {
      "default": "internal",
      "description": "Where to store metadata: 'internal' (default, stored in the same disk), or a disk device/path (e.g., 'device /dev/sda2').",
      "type": "string"
     },
     "on_no_quorum": {
      "default": "",
      "description": "Action when quorum is lost (e.g., 'freeze', 'panic', 'io-error'). Optional.",
      "type": "string"
     },
     "on_suspendsync_fail": {
      "default": "",
      "description": "Action when suspended synchronization fails (e.g., 'io-error', 'panic'). Optional.",
      "type": "string"
     },
     "protocol": {
      "default": "C",
      "description": "DRBD protocol to use: 'A' (asynchronous), 'B' (semi-synchronous), or 'C' (synchronous, default).",
      "type": "string"
     },
     "syncer_rate": {
      "default": "10M",
      "description": "Maximum synchronization rate for resync operations (e.g., '10M' = 10 MiB/s). Accepts suffixes: K, M, G.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "hosts": {
    "default": [],
    "description": "List of host nodes participating in this resource (typically 2 for HA).",
    "items": {
     "address": {
      "default": "",
      "description": "IP address and optional port for inter-node communication (e.g., '10.0.0.1:7789').",
      "type": "string"
     },
     "disk_path": {
      "default": "",
      "description": "Local block device path for this node (e.g., /dev/sdb1) — overrides resource-level disk setting.",
      "type": "string"
     },
     "hostname": {
      "default": "",
      "description": "Hostname or FQDN of the node (must match output of 'uname -n' or DNS).",
      "type": "string"
     },
     "meta_disk_path": {
      "default": "",
      "description": "Path for metadata storage on this node (e.g., 'device /dev/sda3' or 'internal').",
      "type": "string"
     },
     "node_id": {
      "default": null,
      "description": "Numeric ID for node identification in the cluster (0, 1, ...). Required when >2 nodes.",
      "type": "number"
     }
    },
    "type": "list"
   },
   "name": {
    "default": "",
    "description": "Name of the DRBD resource (e.g., 'r0', 'data'). Must be unique across all resources.",
    "type": "string"
   }
  },
  "type": "list"
 }
}
