{
  "database_path": {
    "type": "string",
    "default": "",
    "description": "Absolute path to the encrypted TOTP/HOTP database file. Overrides the default (~/.local/share/cotp/cotp.db). Required only if non-default location is used."
  }
}
