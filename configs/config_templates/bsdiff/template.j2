# ==============================================================================
# BSDiff Configuration Template
# ==============================================================================
# NOTE: bsdiff is a pure command-line utility for generating binary diffs.
# It does NOT run as a daemon and does NOT read a configuration file at runtime.
# This template is intentionally minimal and serves as documentation to confirm
# that no service configuration is required for this package.
#
# Invocation syntax:
#   bsdiff <old_file> <new_file> <patch_file>
#
# For usage in automation (e.g., Ansible), simply invoke the binary directly
# without referencing any config file.
# ==============================================================================

# No directives to configure. This file is empty by design.