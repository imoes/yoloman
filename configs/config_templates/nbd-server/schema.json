{
  "name": {
    "type": "string",
    "default": "nbd-server",
    "description": "The name of the service/daemon."
  },
  "description": {
    "type": "string",
    "default": "Network Block Device server",
    "description": "A short description of the service."
  },
  "pidfile": {
    "type": "string",
    "default": "/var/run/nbd-server.pid",
    "description": "The path to the PID file for the daemon."
  }
}
