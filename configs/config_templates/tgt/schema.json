{
  "conf_dir": {
    "type": "string",
    "default": "/etc/tgt/conf.d",
    "description": "Directory where individual configuration snippets are placed"
  },
  "include_pattern": {
    "type": "string",
    "default": "/etc/tgt/conf.d/*.conf",
    "description": "Glob pattern for including configuration files"
  }
}
