# git-quick-stats configuration
# Environment variables to control git-quick-stats behavior.
# This file is auto-generated by Ansible. Do not edit manually.

{# Iterate over the list of environment variable definitions provided in the schema.
   Each item defines a variable name, value, and description. #}
{% for var in env_vars | default([]) %}
{# Description of the environment variable from the schema. #}
# {{ var.description | default('No description provided') }}
{# Export the variable. Use quote filter to safely handle values with spaces or special chars.
   Default to empty string if value is not provided. #}
export {{ var.name | default('') }}={{ var.value | default('') | quote }}
{% endfor %}