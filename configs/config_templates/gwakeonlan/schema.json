{
  "verbose": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose mode to show all error and information messages"
  },
  "quiet": {
    "type": "bool",
    "default": false,
    "description": "Enable quiet mode to hide all error and information messages (takes precedence over verbose)"
  }
}
