{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable the shortcut viewer service. When false, no ExecStart override is generated."
  },
  "pos": {
    "type": "string",
    "default": "",
    "description": "Comma-separated x,y coordinates for window centering, e.g., '100,50'. Must match the -p/--pos argument format."
  },
  "bypass": {
    "type": "bool",
    "default": false,
    "description": "Pass the --bypass flag to ignore window manager hints."
  }
}
