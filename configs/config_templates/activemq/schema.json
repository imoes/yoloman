{
  "activemq_user": {
    "type": "string",
    "default": "activemq",
    "description": "System user account under which the ActiveMQ process runs."
  },
  "dietime": {
    "type": "number",
    "default": 2,
    "description": "Time (in seconds) to wait for the ActiveMQ server to shut down gracefully before forcing termination."
  }
}
