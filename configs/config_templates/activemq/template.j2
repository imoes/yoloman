# System user account under which the ActiveMQ process runs.
# Default: activemq
ACTIVEMQ_USER="{{ activemq_user | default('activemq') }}"

# Time (in seconds) to wait for the ActiveMQ server to shut down gracefully
# before forcing termination.
# Default: 2
DIETIME={{ dietime | default(2) }}