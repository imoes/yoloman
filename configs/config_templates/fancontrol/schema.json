{
  "DAEMON": {
    "type": "string",
    "default": "/usr/sbin/fancontrol",
    "description": "Path to the fancontrol daemon executable"
  },
  "PIDFILE": {
    "type": "string",
    "default": "/var/run/fancontrol.pid",
    "description": "Path to the PID file used by fancontrol"
  },
  "CONF": {
    "type": "string",
    "default": "/etc/fancontrol",
    "description": "Path to the fancontrol configuration file"
  }
}
