{
  "user_allow_other": {
    "type": "bool",
    "default": false,
    "description": "Whether to enable non-root users to use the 'allow_other' mount option. When true, the line 'user_allow_other' is uncommented."
  },
  "mount_max": {
    "type": "number",
    "default": 1000,
    "description": "Maximum number of FUSE mounts allowed."
  }
}
