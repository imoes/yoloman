{
  "consoles": {
    "type": "list",
    "default": [],
    "description": "List of consoles to be managed by conman. Each entry defines a serial console connection.",
    "items": {
      "name": {
        "type": "string",
        "default": "",
        "description": "Unique name for the console (used as identifier in conman commands)."
      },
      "host": {
        "type": "string",
        "default": "",
        "description": "Hostname or IP address of the remote device (console server, embedded device, etc.)."
      },
      "port": {
        "type": "number",
        "default": 23,
        "description": "Port number used for the connection (e.g., 23 for telnet, 22 for SSH)."
      },
      "protocol": {
        "type": "string",
        "default": "telnet",
        "description": "Protocol to use for the connection (e.g., telnet, ssh, rsh, direct)."
      },
      "username": {
        "type": "string",
        "default": "",
        "description": "Username for authentication (if required by protocol)."
      },
      "password": {
        "type": "string",
        "default": "",
        "description": "Password for authentication (if required by protocol)."
      },
      "baud": {
        "type": "number",
        "default": null,
        "description": "Baud rate for serial connections (e.g., 9600, 115200); omit if not applicable."
      },
      "args": {
        "type": "string",
        "default": "",
        "description": "Additional arguments to pass to the protocol command (e.g., '-e ^\\' for rsh)."
      },
      "log": {
        "type": "bool",
        "default": false,
        "description": "Enable logging of console session data to files in /var/log/conman/."
      }
    }
  },
  "defaults": {
    "type": "object",
    "default": {},
    "description": "Global default key-value settings (e.g., timeout, rshpath, sshpath) applied to all consoles unless overridden."
  }
}
