# ==============================================================================
# mailtextbody configuration
#
# This template configures the behavior of an email processing component
# to specify which MIME content type should be extracted from incoming
# email messages.
# ==============================================================================

# MIME type to extract from the email message (e.g., text/plain, text/html)
# Default: text/plain
mimetype: {{ mimetype | default('text/plain') }}