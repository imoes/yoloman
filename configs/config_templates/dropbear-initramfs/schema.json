{
  "DROPBEAR_OPTIONS": {
    "type": "string",
    "default": "",
    "description": "Command line options to pass to dropbear(8)"
  },
  "IFDOWN": {
    "type": "string",
    "default": "*",
    "description": "Network interface pattern to bring down after initramfs; use 'none' to keep all interfaces up",
    "enum": [
      "*",
      "none"
    ]
  },
  "DROPBEAR_SHUTDOWN_TIMEOUT": {
    "type": "number",
    "default": 60,
    "description": "Maximum seconds to wait for network/dropbear setup at init-bottom before shutting down"
  }
}
