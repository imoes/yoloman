{
  "log_file_path": {
    "type": "string",
    "default": "/var/log/compute-tools/container.log",
    "description": "Path to the container log file"
  },
  "log_rotate_count": {
    "type": "number",
    "default": 12,
    "description": "Number of rotated log files to keep"
  }
}
