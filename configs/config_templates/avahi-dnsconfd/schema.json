{
  "AVAILABILITY_METHOD": {
    "type": "string",
    "default": "auto",
    "description": "Method to determine how to modify resolv.conf: 'netconfig', 'modify_resolvconf', 'resolvconf', or 'fallback'. Auto-detects by checking for executables in order."
  }
}
