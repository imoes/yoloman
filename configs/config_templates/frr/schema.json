{
 "bgp": {
  "default": null,
  "description": "BGP routing configuration (if not enabled, no BGP section is generated)",
  "type": "object"
 },
 "bgp_as": {
  "source": "inferred-from-template",
  "type": "string"
 },
 "hostname": {
  "default": "router",
  "description": "Router hostname used for identification",
  "type": "string"
 },
 "ipv4_unicast": {
  "default": true,
  "description": "Enable IPv4 unicast routing in global address-family",
  "type": "bool"
 },
 "ipv6_unicast": {
  "default": false,
  "description": "Enable IPv6 unicast routing in global address-family",
  "type": "bool"
 },
 "logging.files.path": {
  "default": "/var/log/frr/frr.log",
  "description": "Path to FRR log file if file logging is desired",
  "type": "string"
 },
 "logging.level": {
  "default": "info",
  "description": "Logging level: debug, info, notice, warning, error, crit, alert, emerg\nenum: [\"debug\", \"info\", \"notice\", \"warning\", \"error\", \"crit\", \"alert\", \"emerg\"]",
  "enum": [
   "info",
   "warning",
   "error",
   "alert"
  ],
  "type": "string"
 },
 "router_id": {
  "default": "",
  "description": "Global router-id (IPv4 format, e.g. 192.0.2.1); optional",
  "type": "string"
 }
}
