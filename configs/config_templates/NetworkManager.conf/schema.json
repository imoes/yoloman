{
  "plugins": {
    "type": "string",
    "default": "ifupdown,keyfile",
    "description": "Comma-separated list of plugins to load. Common values: 'ifupdown,keyfile', 'keyfile', etc."
  },
  "ifupdown_managed": {
    "type": "bool",
    "default": false,
    "description": "If true, ifupdown interfaces are managed by NetworkManager. Default is false (unmanaged by NM)."
  }
}
