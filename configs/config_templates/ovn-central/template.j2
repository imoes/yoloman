# OVN Central Controller Configuration
# This template configures OVN (Open Virtual Network) central components.
# It generates a POSIX shell fragment to be sourced by the OVN service startup scripts.

# OVN_CTL_OPTS: Extra command-line options to pass to ovs-ctl when starting OVN services.
# This is, for example, a suitable place to specify --ovn-northd-wrapper=valgrind.
# Default is empty string.
# {{ ovn_ctl_opts | default('') }}