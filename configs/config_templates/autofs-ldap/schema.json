{
  "usetls": {
    "type": "bool",
    "default": false,
    "description": "Whether to use TLS for LDAP connections"
  },
  "tlsrequired": {
    "type": "bool",
    "default": false,
    "description": "Whether TLS is required (abort if TLS cannot be established)"
  },
  "authrequired": {
    "type": "bool",
    "default": false,
    "description": "Whether SASL authentication is required"
  }
}
