# ptask configuration file
# This file controls the runtime behavior of the ptask package.

# Debug level: 0 (none) to 3 (verbose).
# Controls the verbosity of logging and debugging output.
# Valid range is 0-3.
--debug={{ debug_level | default(0) }}