{
 "DBUS_SESSION_BUS_ADDRESS": {
  "default": "",
  "description": "The DBus session bus address. If empty, the script auto-detects it based on XDG_RUNTIME_DIR.",
  "type": "string"
 },
 "NULL": {
  "default": "",
  "description": "Legacy empty variable placeholder for environment variable expansion safety.",
  "type": "string"
 },
 "XDG_RUNTIME_DIR": {
  "default": "/run/user/`id -u`",
  "description": "The XDG runtime directory path, typically /run/user/<uid>.",
  "type": "string"
 },
 "null_part": {
  "source": "inferred-from-template",
  "type": "string"
 }
}
