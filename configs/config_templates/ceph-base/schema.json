{
  "service_user": {
    "type": "string",
    "default": "ceph",
    "description": "The user account under which Ceph daemons run, used for sudo permissions to collect device health metrics."
  }
}
