{
  "clusters": {
    "type": "list",
    "default": [],
    "description": "List of cluster definitions, each containing a name and list of nodes."
  },
  "default_cluster": {
    "type": "list",
    "default": [],
    "description": "List of nodes in the default cluster. Note: In values_schema this is represented as a list of node strings for simplicity, but in practice you'd structure this as a map. For strict Jinja2 usage without complex nesting in schema, we'll adjust."
  }
}
