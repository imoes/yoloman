{
  "domain": {
    "type": "string",
    "default": "example.com",
    "description": "Domain name to fetch related URLs for."
  },
  "list": {
    "type": "string",
    "default": "",
    "description": "File containing a list of domain names. Leave empty to use domain."
  },
  "stream": {
    "type": "bool",
    "default": false,
    "description": "Stream URLs on the terminal."
  },
  "proxy": {
    "type": "string",
    "default": "",
    "description": "Proxy address for web requests. Leave empty to use direct connection."
  },
  "placeholder": {
    "type": "string",
    "default": "FUZZ",
    "description": "Placeholder for parameter values."
  }
}
