{
  "debug": {
    "type": "bool",
    "default": false,
    "description": "Enable verbose debug logging for troubleshooting."
  },
  "log_level": {
    "type": "string",
    "default": "INFO",
    "description": "Logging verbosity level (e.g., DEBUG, INFO, WARNING, ERROR, CRITICAL)."
  },
  "log_file": {
    "type": "string",
    "default": "-",
    "description": "Path to log file; '-' for stdout/stderr; 'syslog' for syslog."
  },
  "keystone_auth_url": {
    "type": "string",
    "default": "http://127.0.0.1:5000/v3",
    "description": "Keystone authentication service endpoint (must include /v3)."
  },
  "project_domain_name": {
    "type": "string",
    "default": "Default",
    "description": "Keystone project domain name for the placement service user."
  },
  "project_name": {
    "type": "string",
    "default": "service",
    "description": "Keystone project (tenant) name where the placement service user resides."
  },
  "user_domain_name": {
    "type": "string",
    "default": "Default",
    "description": "Keystone user domain name for the placement service user."
  },
  "service_username": {
    "type": "string",
    "default": "placement",
    "description": "Username of the Keystone service account for placement."
  },
  "service_password": {
    "type": "string",
    "default": "",
    "description": "Password for the placement service user. *Required if using Keystone auth.*"
  },
  "api_endpoint": {
    "type": "string",
    "default": "http://127.0.0.1:8778",
    "description": "Public/internal endpoint URL where this placement API instance is reachable by other OpenStack services."
  },
  "use_database": {
    "type": "bool",
    "default": true,
    "description": "Enable database storage for inventory data (required for most deployments)."
  },
  "database_connection": {
    "type": "string",
    "default": "mysql+pymysql://placement:placement@127.0.0.1/placement?charset=utf8",
    "description": "Database connection string. Uses SQLAlchemy URI format."
  },
  "lock_path": {
    "type": "string",
    "default": "/var/lib/placement/lock",
    "description": "Directory for file-based locking (multi-process deployments)."
  },
  "policy_file": {
    "type": "string",
    "default": "/etc/placement/policy.json",
    "description": "Path to the policy definition JSON file."
  },
  "use_keystone_authtoken": {
    "type": "bool",
    "default": false,
    "description": "Enable Keystone auth token middleware (typically used when placement is behind a proxy or with token-based middleware)."
  },
  "www_authenticate_uri": {
    "type": "string",
    "default": "http://127.0.0.1:5000/",
    "description": "Public Keystone endpoint used for WWW-Authenticate header (required by middleware)."
  }
}
