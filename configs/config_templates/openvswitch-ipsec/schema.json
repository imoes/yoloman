{
  "daemon_path": {
    "type": "string",
    "default": "/usr/share/openvswitch/scripts/ovs-monitor-ipsec",
    "description": "Full path to the ovs-monitor-ipsec daemon script."
  },
  "name": {
    "type": "string",
    "default": "ovs-monitor-ipsec",
    "description": "Short name of the service/daemon."
  },
  "logdir": {
    "type": "string",
    "default": "/var/log/openvswitch",
    "description": "Directory where log files are stored."
  },
  "datadir": {
    "type": "string",
    "default": "/usr/share/openvswitch",
    "description": "Data directory for Open vSwitch scripts."
  },
  "dodtime": {
    "type": "number",
    "default": 10,
    "description": "Time in seconds to wait for the server to die during stop/restart."
  }
}
