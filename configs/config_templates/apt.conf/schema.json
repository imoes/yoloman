{
  "appstream_native_architecture": {
    "type": "string",
    "default": "amd64",
    "description": "Native architecture used in DEP-11 metadata paths (e.g., 'amd64', 'arm64')"
  },
  "appstream_component": {
    "type": "string",
    "default": "main",
    "description": "Component name (e.g., 'main', 'contrib', 'non-free')"
  },
  "appstream_release": {
    "type": "string",
    "default": "bookworm",
    "description": "Release codename (e.g., 'bookworm', 'bullseye')"
  }
}
