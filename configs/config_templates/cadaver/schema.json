{
  "proxy_host": {
    "type": "string",
    "default": "",
    "description": "Hostname of WebDAV proxy server (optional)"
  },
  "proxy_port": {
    "type": "number",
    "default": 80,
    "description": "Port number for proxy server (defaults to 80)"
  },
  "tolerant": {
    "type": "bool",
    "default": false,
    "description": "Enable tolerant mode (allow cd/open into non-WebDAV collections)"
  },
  "default_remote_path": {
    "type": "string",
    "default": "/",
    "description": "Default remote path to change to after opening connection"
  },
  "startup_commands": {
    "type": "list",
    "default": [],
    "description": "List of cadaver commands to run at startup after opening session"
  }
}
