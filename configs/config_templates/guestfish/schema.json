{
  "read_only": {
    "type": "bool",
    "default": false,
    "description": "If true, make guestfish, guestmount, virt-rescue operate in read-only mode by default (equivalent to --ro flag). If false (default), operate in read-write mode."
  }
}
