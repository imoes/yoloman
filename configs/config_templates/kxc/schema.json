{
  "client_key": {
    "type": "string",
    "default": "/etc/kxc/client.key",
    "description": "Path to the file containing the client private key in PAM format"
  },
  "client_cert": {
    "type": "string",
    "default": "/etc/kxc/client.crt",
    "description": "Path to the file containing the client certificate in PAM format"
  },
  "server_cert": {
    "type": "string",
    "default": "/etc/kxc/server.crt",
    "description": "Path to the file containing the valid server certificate in PAM format"
  },
  "server_url": {
    "type": "string",
    "default": "kxd://localhost/host1/key1",
    "description": "URL to the kxd server including host and key identifier"
  }
}
