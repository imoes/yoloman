{
  "project_name": {
    "type": "string",
    "default": "swift",
    "description": "The project name used to prefix variables and paths."
  },
  "system_user": {
    "type": "string",
    "default": "swift",
    "description": "The system user running the Swift service."
  },
  "system_group": {
    "type": "string",
    "default": "swift",
    "description": "The system group running the Swift service."
  },
  "config_file": {
    "type": "string",
    "default": "object-expirer",
    "description": "The base configuration filename for the object expirer."
  }
}
