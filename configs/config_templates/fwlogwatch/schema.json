{
  "CRON_EMAIL": {
    "type": "string",
    "default": "",
    "description": "Email address to send the fwlogwatch report to. Set to 'none' to disable email delivery."
  },
  "CRON_PARAMS": {
    "type": "string",
    "default": "",
    "description": "Additional command-line parameters passed to fwlogwatch when invoked by cron."
  }
}
