# Exported directories and their options
{% for export in exports %}{{ export.path }}{% if export.clients %}
{% for client in export.clients %}	{{ client.host }}{{ client.opts | join(', ') }}{% endfor %}
{% else %}	*(ro,sync,no_subtree_check)
{% endif %}{% endfor %}