{
  "ipsec_charon_binary": {
    "type": "string",
    "default": "/usr/lib/ipsec/charon",
    "description": "The path to the StrongSwan charon daemon binary."
  },
  "charon_run_path": {
    "type": "string",
    "default": "/run/charon.*",
    "description": "The path pattern for the charon run directory/files."
  }
}
