{
 "/etc/.pwd.lock": {
  "container_artifact": false,
  "families": [
   "debian",
   "redhat"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/BUILDTIME": {
  "container_artifact": true,
  "families": [
   "redhat"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/apt/apt.conf.d/docker-autoremove-suggests": {
  "container_artifact": true,
  "families": [
   "debian"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/apt/apt.conf.d/docker-clean": {
  "container_artifact": true,
  "families": [
   "debian"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/apt/apt.conf.d/docker-gzip-indexes": {
  "container_artifact": true,
  "families": [
   "debian"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/apt/apt.conf.d/docker-no-languages": {
  "container_artifact": true,
  "families": [
   "debian"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/apt/sources.list.d/debian.sources": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/dnf/vars/infra": {
  "container_artifact": true,
  "families": [
   "redhat"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/dpkg/dpkg.cfg.d/docker-apt-speedup": {
  "container_artifact": true,
  "families": [
   "debian"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/environment": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/fstab": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/group": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/group-": {
  "container_artifact": false,
  "families": [
   "debian",
   "redhat"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/gshadow": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/gshadow-": {
  "container_artifact": false,
  "families": [
   "redhat"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/hostname": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/hosts": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/inputrc": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/ld.so.cache": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/motd": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/nsswitch.conf": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/nsswitch.conf.bak": {
  "container_artifact": true,
  "families": [
   "redhat"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/pam.d/common-account": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/pam.d/common-auth": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/pam.d/common-password": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/pam.d/common-session": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/pam.d/common-session-noninteractive": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/passwd": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/passwd-": {
  "container_artifact": false,
  "families": [
   "debian",
   "redhat"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/profile": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/python3/debian_config": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/resolv.conf": {
  "container_artifact": false,
  "families": [
   "debian",
   "redhat"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/rpm/macros.image-language-conf": {
  "container_artifact": true,
  "families": [
   "redhat"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/security/opasswd": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/shadow": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/shadow-": {
  "container_artifact": false,
  "families": [
   "redhat"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/shells": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/subgid": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/subuid": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/sysconfig/sshd-permitrootlogin": {
  "container_artifact": true,
  "families": [
   "redhat"
  ],
  "note": "present in the container image only \u2014 not evidence that an installed system creates it"
 },
 "/etc/timezone": {
  "container_artifact": false,
  "families": [
   "debian"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 },
 "/etc/vconsole.conf": {
  "container_artifact": false,
  "families": [
   "redhat"
  ],
  "note": "exists in the base image and no package owns it: created by the installer or by the base system, so \"the package ships nothing here\" is expected and means nothing"
 }
}