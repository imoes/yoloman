name: arbor_peakflow_tms_disk_usage
fqcn: checkmk.arbor_peakflow_tms_disk_usage
collection: checkmk
short_description: Disk Usage %s
description: 'Checkmk check ''arbor_peakflow_tms_disk_usage'' (service: Disk Usage
  %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  product:
    type: string
    default: peakflow_tms
    description: product (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
