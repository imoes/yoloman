name: mssql_counters_locks_per_batch
fqcn: checkmk.mssql_counters_locks_per_batch
collection: checkmk
short_description: MSSQL %s Locks per Batch
description: 'Monitoring check ''mssql_counters_locks_per_batch'' (service: MSSQL
  %s Locks per Batch), translated to a read-only on-host Starlark check module.'
options:
  locks_per_batch:
    type: list
    description: locks_per_batch (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
