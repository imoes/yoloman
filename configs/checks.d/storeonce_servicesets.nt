name: storeonce_servicesets
fqcn: checkmk.storeonce_servicesets
collection: checkmk
short_description: ServiceSet %s Status
description: Checkmk check 'storeonce_servicesets' (service: ServiceSet %s Status), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: monitor
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
