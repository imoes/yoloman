name: sap_value_groups
fqcn: checkmk.sap_value_groups
collection: checkmk
short_description: %s
description: Monitoring check 'sap_value_groups' (service: %s), translated to a read-only on-host Starlark check module.
options:
    data_file:
        type: string
        default: /var/cache/sap_agent/sap.txt
        description: data_file (from the check's parameters).
    grouping_patterns:
        type: list
        description: grouping_patterns (from the check's parameters).
    _group_relevant_patterns:
        type: list
        description: _group_relevant_patterns (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
