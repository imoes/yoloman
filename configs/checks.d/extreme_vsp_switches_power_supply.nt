name: extreme_vsp_switches_power_supply
fqcn: checkmk.extreme_vsp_switches_power_supply
collection: checkmk
short_description: VSP Switch Power Supply %s
description: Monitoring check 'extreme_vsp_switches_power_supply' (service: VSP Switch Power Supply %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
