name: blade_powerfan
fqcn: checkmk.blade_powerfan
collection: checkmk
short_description: Power Module Cooling Device %s
description: 'Checkmk check ''blade_powerfan'' (service: Power Module Cooling Device
  %s), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  warn:
    type: number
    default: '50'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '40'
    description: crit (from the check's parameters).
  rpm_warn:
    type: string
    description: rpm_warn (from the check's parameters).
  rpm_crit:
    type: string
    description: rpm_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
