name: checkpoint_powersupply
fqcn: checkmk.checkpoint_powersupply
collection: checkmk
short_description: Power Supply %s
description: Monitoring check 'checkpoint_powersupply' (service: Power Supply %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    up:
        type: number
        default: 0
        description: up (from the check's parameters).
    ok:
        type: number
        default: 0
        description: ok (from the check's parameters).
    present:
        type: number
        default: 2
        description: present (from the check's parameters).
    no_redundancy:
        type: number
        default: 1
        description: no_redundancy (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
