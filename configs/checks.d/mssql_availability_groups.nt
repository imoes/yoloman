name: mssql_availability_groups
fqcn: checkmk.mssql_availability_groups
collection: checkmk
short_description: MSSQL Availability Group %s
description: Monitoring check 'mssql_availability_groups' (service: MSSQL Availability Group %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 1433
        description: port (from the check's parameters).
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    instance:
        type: string
        default:
        description: instance (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
