name: cmctc_lcp_access
fqcn: checkmk.cmctc_lcp_access
collection: checkmk
short_description: Access %s
description: 'Checkmk check ''cmctc_lcp_access'' (service: Access %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  sensortype:
    type: string
    default: access
    description: sensortype (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
