name: emc_isilon_fans
fqcn: checkmk.emc_isilon_fans
collection: checkmk
short_description: Fan %s
description: 'Monitoring check ''emc_isilon_fans'' (service: Fan %s), translated to
  a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  lower:
    type: list
    description: lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
