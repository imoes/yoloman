name: redis_info_persistence
fqcn: checkmk.redis_info_persistence
collection: checkmk
short_description: Redis %s Persistence
description: Checkmk check 'redis_info_persistence' (service: Redis %s Persistence), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: string
        default: 6379
        description: port (from the check's parameters).
    rdb_last_bgsave_state:
        type: number
        default: 1
        description: rdb_last_bgsave_state (from the check's parameters).
    aof_last_rewrite_state:
        type: number
        default: 1
        description: aof_last_rewrite_state (from the check's parameters).
    rdb_changes_count:
        type: string
        description: rdb_changes_count (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
