name: netscaler_ha
fqcn: checkmk.netscaler_ha
collection: checkmk
short_description: HA Node Status
description: Checkmk check 'netscaler_ha' (service: HA Node Status), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    failover_monitoring:
        type: list
        description: failover_monitoring (from the check's parameters).
    discovered_failover_mode:
        type: string
        description: discovered_failover_mode (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
