name: checkpoint_vsx_traffic
fqcn: checkmk.checkpoint_vsx_traffic
collection: checkmk
short_description: VS %s Traffic
description: 'Checkmk check ''checkpoint_vsx_traffic'' (service: VS %s Traffic), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  metric:
    type: string
    default: bytes_accepted
    description: metric (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  levels_upper:
    type: string
    description: levels_upper (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
