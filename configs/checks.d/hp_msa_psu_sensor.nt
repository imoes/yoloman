name: hp_msa_psu_sensor
fqcn: checkmk.hp_msa_psu_sensor
collection: checkmk
short_description: Power Supply Voltage %s
description: Monitoring check 'hp_msa_psu_sensor' (service: Power Supply Voltage %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: manage
        description: username (from the check's parameters).
    password:
        type: string
        default: !manage
        description: password (from the check's parameters).
    levels_12v_lower:
        type: string
        description: levels_12v_lower (from the check's parameters).
    levels_12v_upper:
        type: string
        description: levels_12v_upper (from the check's parameters).
    levels_5v_lower:
        type: string
        description: levels_5v_lower (from the check's parameters).
    levels_5v_upper:
        type: string
        description: levels_5v_upper (from the check's parameters).
    levels_33v_lower:
        type: string
        description: levels_33v_lower (from the check's parameters).
    levels_33v_upper:
        type: string
        description: levels_33v_upper (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
