name: nimble_latency
fqcn: checkmk.nimble_latency
collection: checkmk
short_description: Volume %s Read IO
description: Monitoring check 'nimble_latency' (service: Volume %s Read IO), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    type:
        type: string
        description: type (from the check's parameters).
    range_reference:
        type: string
        default: 20
        description: range_reference (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
