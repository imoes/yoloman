name: lnx_thermal
fqcn: checkmk.lnx_thermal
collection: checkmk
short_description: Temperature %s
description: 'Checkmk check ''lnx_thermal'' (service: Temperature %s), translated
  to a read-only on-host Starlark check module.'
options:
  warn:
    type: number
    default: '70.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '80.0'
    description: crit (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
