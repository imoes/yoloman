name: blade_bx_blades
fqcn: checkmk.blade_bx_blades
collection: checkmk
short_description: Blade %s
description: 'Checkmk check ''blade_bx_blades'' (service: Blade %s), translated to
  a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
