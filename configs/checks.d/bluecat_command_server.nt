name: bluecat_command_server
fqcn: checkmk.bluecat_command_server
collection: checkmk
short_description: Command Server
description: Checkmk check 'bluecat_command_server' (service: Command Server), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    oper_states:
        type: string
        description: oper_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
