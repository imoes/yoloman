name: diskstat_io_director
fqcn: checkmk.diskstat_io_director
collection: checkmk
short_description: Disk IO Director %s
description: Monitoring check 'diskstat_io_director' (service: Disk IO Director %s), translated to a read-only on-host Starlark check module.
options:
    latency:
        type: list
        description: latency (from the check's parameters).
    total_utilization:
        type: list
        description: total_utilization (from the check's parameters).
    url:
        type: string
        default: http://localhost:23222
        description: url (from the check's parameters).
    username:
        type: string
        default:
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
