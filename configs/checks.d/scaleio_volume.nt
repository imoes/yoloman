name: scaleio_volume
fqcn: checkmk.scaleio_volume
collection: checkmk
short_description: ScaleIO Volume %s
description: Monitoring check 'scaleio_volume' (service: ScaleIO Volume %s), translated to a read-only on-host Starlark check module.
options:
    mdm_ip:
        type: string
        default:
        description: mdm_ip (from the check's parameters).
    username:
        type: string
        default:
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    read_throughput:
        type: string
        description: read_throughput (from the check's parameters).
    write_throughput:
        type: string
        description: write_throughput (from the check's parameters).
    read_ios:
        type: string
        description: read_ios (from the check's parameters).
    write_ios:
        type: string
        description: write_ios (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
