name: huawei_osn_laser
fqcn: checkmk.huawei_osn_laser
collection: checkmk
short_description: Laser %s
description: 'Checkmk check ''huawei_osn_laser'' (service: Laser %s), translated to
  a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels_low_in:
    type: list
    description: levels_low_in (from the check's parameters).
  levels_low_out:
    type: list
    description: levels_low_out (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
