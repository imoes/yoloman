name: cisco_asa_conn
fqcn: checkmk.cisco_asa_conn
collection: checkmk
short_description: Connection %s
description: Checkmk check 'cisco_asa_conn' (service: Connection %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
