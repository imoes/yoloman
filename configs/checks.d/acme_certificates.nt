name: acme_certificates
fqcn: checkmk.acme_certificates
collection: checkmk
short_description: Certificate %s
description: Checkmk check 'acme_certificates' (service: Certificate %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    expire_lower:
        type: list
        description: expire_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
