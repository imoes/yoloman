name: wut_webio
fqcn: checkmk.wut_webio
collection: checkmk
short_description: Check plug-in for W&T WebIO device
description: 'Checkmk check ''wut_webio'' (service: W&T %s), translated to a read-only
  on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  evaluation_mode:
    type: string
    description: evaluation_mode (from the check's parameters).
  states_during_discovery:
    type: string
    description: states_during_discovery (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
