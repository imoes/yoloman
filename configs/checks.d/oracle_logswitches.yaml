name: oracle_logswitches
fqcn: checkmk.oracle_logswitches
collection: checkmk
short_description: ORA %s Logswitches
description: 'Checkmk check ''oracle_logswitches'' (service: ORA %s Logswitches),
  translated to a read-only on-host Starlark check module.'
options:
  warn:
    type: number
    default: '50'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '100'
    description: crit (from the check's parameters).
  lowarn:
    type: number
    default: '-1'
    description: lowarn (from the check's parameters).
  locrit:
    type: number
    default: '-1'
    description: locrit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
