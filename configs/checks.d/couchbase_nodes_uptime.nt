name: couchbase_nodes_uptime
fqcn: checkmk.couchbase_nodes_uptime
collection: checkmk
short_description: Couchbase %s Uptime
description: Checkmk check 'couchbase_nodes_uptime' (service: Couchbase %s Uptime), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8091
        description: port (from the check's parameters).
    user:
        type: string
        default: Administrator
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    max:
        type: string
        description: max (from the check's parameters).
    min:
        type: string
        description: min (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
