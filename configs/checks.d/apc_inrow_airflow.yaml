name: apc_inrow_airflow
fqcn: checkmk.apc_inrow_airflow
collection: checkmk
short_description: Airflow
description: 'Checkmk check ''apc_inrow_airflow'' (service: Airflow), translated to
  a read-only on-host Starlark check module.'
options:
  host:
    type: string
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  snmp_version:
    type: string
    default: 2c
    description: snmp_version (from the check's parameters).
  level_low:
    type: list
    description: level_low (from the check's parameters).
  level_high:
    type: list
    description: level_high (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
