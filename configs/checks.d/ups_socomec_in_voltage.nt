name: ups_socomec_in_voltage
fqcn: checkmk.ups_socomec_in_voltage
collection: checkmk
short_description: IN voltage phase %s
description: Monitoring check 'ups_socomec_in_voltage' (service: IN voltage phase %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels_lower:
        type: string
        description: levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
