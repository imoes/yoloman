name: docsis_channels_downstream
fqcn: checkmk.docsis_channels_downstream
collection: checkmk
short_description: Downstream Channel %s
description: 'Monitoring check ''docsis_channels_downstream'' (service: Downstream
  Channel %s), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  power:
    type: string
    description: power (from the check's parameters).
  frequency:
    type: string
    description: frequency (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
