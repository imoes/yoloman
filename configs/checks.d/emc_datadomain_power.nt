name: emc_datadomain_power
fqcn: checkmk.emc_datadomain_power
collection: checkmk
short_description: Power Module %s
description: Checkmk check 'emc_datadomain_power' (service: Power Module %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
