name: f5_bigip_vcmpguests
fqcn: checkmk.f5_bigip_vcmpguests
collection: checkmk
short_description: F5-BIGIP-Cluster-Status SNMP Sections and Checks
description: 'Checkmk check ''f5_bigip_vcmpguests'' (service: BIG-IP vCMP Guests),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
