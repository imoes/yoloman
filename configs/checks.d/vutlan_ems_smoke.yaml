name: vutlan_ems_smoke
fqcn: checkmk.vutlan_ems_smoke
collection: checkmk
short_description: Smoke Detector %s
description: 'Checkmk check ''vutlan_ems_smoke'' (service: Smoke Detector %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
