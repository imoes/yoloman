name: bluenet2_powerrail_temp
fqcn: checkmk.bluenet2_powerrail_temp
collection: checkmk
short_description: Temperature %s
description: Checkmk check 'bluenet2_powerrail_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 30.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 35.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
