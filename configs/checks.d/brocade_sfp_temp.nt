name: brocade_sfp_temp
fqcn: checkmk.brocade_sfp_temp
collection: checkmk
short_description: SFP Temperature %s
description: Checkmk check 'brocade_sfp_temp' (service: SFP Temperature %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    warn:
        type: number
        default: 55.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 60.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
