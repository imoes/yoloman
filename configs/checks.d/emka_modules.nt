name: emka_modules
fqcn: checkmk.emka_modules
collection: checkmk
short_description: Module %s
description: Monitoring check 'emka_modules' (service: Module %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
