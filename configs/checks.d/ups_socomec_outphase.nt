name: ups_socomec_outphase
fqcn: checkmk.ups_socomec_outphase
collection: checkmk
short_description: Output %s
description: Monitoring check 'ups_socomec_outphase' (service: Output %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    voltage:
        type: list
        description: voltage (from the check's parameters).
    output_load:
        type: list
        description: output_load (from the check's parameters).
    current:
        type: string
        description: current (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
