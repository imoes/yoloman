name: papouch_th2e_sensors
fqcn: checkmk.papouch_th2e_sensors
collection: checkmk
short_description: Temperature %s
description: Checkmk check 'papouch_th2e_sensors' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 70
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 80
        description: crit (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
