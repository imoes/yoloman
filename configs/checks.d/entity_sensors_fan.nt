name: entity_sensors_fan
fqcn: checkmk.entity_sensors_fan
collection: checkmk
short_description: Fan %s
description: Monitoring check 'entity_sensors_fan' (service: Fan %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        description: community (from the check's parameters).
    host:
        type: string
        description: host (from the check's parameters).
    lower:
        type: string
        description: lower (from the check's parameters).
    output_metrics:
        type: bool
        default: False
        description: output_metrics (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
