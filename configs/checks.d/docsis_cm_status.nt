name: docsis_cm_status
fqcn: checkmk.docsis_cm_status
collection: checkmk
short_description: Cable Modem %s Status
description: Checkmk check 'docsis_cm_status' (service: Cable Modem %s Status), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    tx_power:
        type: list
        description: tx_power (from the check's parameters).
    error_states:
        type: list
        description: error_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
