name: mssql_counters_file_sizes
fqcn: checkmk.mssql_counters_file_sizes
collection: checkmk
short_description: MSSQL %s File Sizes
description: 'Checkmk check ''mssql_counters_file_sizes'' (service: MSSQL %s File
  Sizes), translated to a read-only on-host Starlark check module.'
options:
  data_files:
    type: string
    description: data_files (from the check's parameters).
  log_files:
    type: string
    description: log_files (from the check's parameters).
  log_files_used:
    type: string
    description: log_files_used (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
