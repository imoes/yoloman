name: netscaler_sslcertificates
fqcn: checkmk.netscaler_sslcertificates
collection: checkmk
short_description: SSL Certificate %s
description: Checkmk check 'netscaler_sslcertificates' (service: SSL Certificate %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    age_levels:
        type: list
        description: age_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
