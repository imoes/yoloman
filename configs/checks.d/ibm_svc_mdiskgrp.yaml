name: ibm_svc_mdiskgrp
fqcn: checkmk.ibm_svc_mdiskgrp
collection: checkmk
short_description: Pool Capacity %s
description: 'Checkmk check ''ibm_svc_mdiskgrp'' (service: Pool Capacity %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  warn:
    type: number
    default: '80'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90'
    description: crit (from the check's parameters).
  provisioning_levels:
    type: string
    description: provisioning_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
