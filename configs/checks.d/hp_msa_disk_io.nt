name: hp_msa_disk_io
fqcn: checkmk.hp_msa_disk_io
collection: checkmk
short_description: Disk IO %s
description: Checkmk check 'hp_msa_disk_io' (service: Disk IO %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
    warn_lower:
        type: string
        description: warn_lower (from the check's parameters).
    crit_lower:
        type: string
        description: crit_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
