name: fortimail_disk_usage
fqcn: checkmk.fortimail_disk_usage
collection: checkmk
short_description: Disk usage
description: Checkmk check 'fortimail_disk_usage' (service: Disk usage), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    disk_usage:
        type: list
        description: disk_usage (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
