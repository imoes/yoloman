name: 3par_volumes
fqcn: checkmk.3par_volumes
collection: checkmk
short_description: Volume %s
description: Monitoring check '3par_volumes' (service: Volume %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8080
        description: port (from the check's parameters).
    username:
        type: string
        default: 3paradm
        description: username (from the check's parameters).
    password:
        type: string
        default: 3pardata
        description: password (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
