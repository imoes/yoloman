name: cisco_meraki_org_device_status
fqcn: checkmk.cisco_meraki_org_device_status
collection: checkmk
short_description: Device Status
description: Checkmk check 'cisco_meraki_org_device_status' (service: Device Status), translated to a read-only on-host Starlark check module.
options:
    status_map:
        type: string
        description: status_map (from the check's parameters).
    last_reported_upper_levels:
        type: list
        description: last_reported_upper_levels (from the check's parameters).
    state_not_powering:
        type: number
        default: 1
        description: state_not_powering (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
