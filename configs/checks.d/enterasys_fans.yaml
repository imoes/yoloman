name: enterasys_fans
fqcn: checkmk.enterasys_fans
collection: checkmk
short_description: FAN %s
description: 'Checkmk check ''enterasys_fans'' (service: FAN %s), translated to a
  read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
