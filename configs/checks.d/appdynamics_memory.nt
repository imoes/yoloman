name: appdynamics_memory
fqcn: checkmk.appdynamics_memory
collection: checkmk
short_description: AppDynamics Memory %s
description: Monitoring check 'appdynamics_memory' (service: AppDynamics Memory %s), translated to a read-only on-host Starlark check module.
options:
    scheme:
        type: string
        default: http
        description: scheme (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8090
        description: port (from the check's parameters).
    username:
        type: string
        default: admin
        description: username (from the check's parameters).
    account:
        type: string
        default: customer1
        description: account (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    application:
        type: string
        default:
        description: application (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
