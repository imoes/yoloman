name: arris_cmts_cpu
fqcn: checkmk.arris_cmts_cpu
collection: checkmk
short_description: CPU utilization Module %s
description: Monitoring check 'arris_cmts_cpu' (service: CPU utilization Module %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
