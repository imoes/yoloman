name: ciena_tunnel
fqcn: checkmk.ciena_tunnel
collection: checkmk
short_description: Tunnel %s
description: 'Checkmk check ''ciena_tunnel'' (service: Tunnel %s), translated to a
  read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  discovered_oper_state:
    type: string
    default: ''
    description: discovered_oper_state (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
