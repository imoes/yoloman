name: enviromux_sems_external_humidity
fqcn: checkmk.enviromux_sems_external_humidity
collection: checkmk
short_description: Sensor External %s
description: Monitoring check 'enviromux_sems_external_humidity' (service: Sensor External %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
