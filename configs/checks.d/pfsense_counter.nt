name: pfsense_counter
fqcn: checkmk.pfsense_counter
collection: checkmk
short_description: pfSense Firewall Packet Rates
description: Checkmk check 'pfsense_counter' (service: pfSense Firewall Packet Rates), translated to a read-only on-host Starlark check module.
options:
    average:
        type: number
        default: 3
        description: average (from the check's parameters).
    badoffset:
        type: list
        description: badoffset (from the check's parameters).
    fragment:
        type: list
        description: fragment (from the check's parameters).
    short:
        type: list
        description: short (from the check's parameters).
    normalized:
        type: list
        description: normalized (from the check's parameters).
    memdrop:
        type: list
        description: memdrop (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
