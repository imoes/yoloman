name: tsm_stagingpools
fqcn: checkmk.tsm_stagingpools
collection: checkmk
short_description: TSM Stagingpool %s
description: 'Checkmk check ''tsm_stagingpools'' (service: TSM Stagingpool %s), translated
  to a read-only on-host Starlark check module.'
options:
  free_below:
    type: string
    description: free_below (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
