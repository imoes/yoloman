name: cisco_cpu_multiitem
fqcn: checkmk.cisco_cpu_multiitem
collection: checkmk
short_description: CPU utilization %s
description: Monitoring check 'cisco_cpu_multiitem' (service: CPU utilization %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    individual:
        type: bool
        default: True
        description: individual (from the check's parameters).
    average:
        type: bool
        default: False
        description: average (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
