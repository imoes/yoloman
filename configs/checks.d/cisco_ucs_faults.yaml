name: cisco_ucs_faults
fqcn: checkmk.cisco_ucs_faults
collection: checkmk
short_description: Cisco UCS Faults
description: 'Checkmk check ''cisco_ucs_faults'' (service: Cisco UCS Faults), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
