name: nimble_latency_write
fqcn: checkmk.nimble_latency_write
collection: checkmk
short_description: Volume %s Write IO
description: Checkmk check 'nimble_latency_write' (service: Volume %s Write IO), translated to a read-only on-host Starlark check module.
options:
    range_reference:
        type: string
        description: range_reference (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
