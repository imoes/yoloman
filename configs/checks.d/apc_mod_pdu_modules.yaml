name: apc_mod_pdu_modules
fqcn: checkmk.apc_mod_pdu_modules
collection: checkmk
short_description: Module %s
description: 'Checkmk check ''apc_mod_pdu_modules'' (service: Module %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
