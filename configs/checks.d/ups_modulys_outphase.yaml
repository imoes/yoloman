name: ups_modulys_outphase
fqcn: checkmk.ups_modulys_outphase
collection: checkmk
short_description: Output %s
description: 'Monitoring check ''ups_modulys_outphase'' (service: Output %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
