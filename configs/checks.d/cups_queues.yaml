name: cups_queues
fqcn: checkmk.cups_queues
collection: checkmk
short_description: CUPS Queue %s
description: 'Checkmk check ''cups_queues'' (service: CUPS Queue %s), translated to
  a read-only on-host Starlark check module.'
options:
  job_count:
    type: list
    description: job_count (from the check's parameters).
  job_age:
    type: list
    description: job_age (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
