name: domino_users
fqcn: checkmk.domino_users
collection: checkmk
short_description: Domino Users
description: Monitoring check 'domino_users' (service: Domino Users), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
