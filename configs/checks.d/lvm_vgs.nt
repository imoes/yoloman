name: lvm_vgs
fqcn: checkmk.lvm_vgs
collection: checkmk
short_description: LVM VG %s
description: Checkmk check 'lvm_vgs' (service: LVM VG %s), translated to a read-only on-host Starlark check module.
options:
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
