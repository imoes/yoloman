name: citrix_hostsystem
fqcn: checkmk.citrix_hostsystem
collection: checkmk
short_description: Citrix Host Info
description: 'Checkmk check ''citrix_hostsystem'' (service: Citrix Host Info), translated
  to a read-only on-host Starlark check module.'
options:
  _type:
    type: string
    default: ''
    description: _type (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
