name: oracle_diva_csm_tapes
fqcn: checkmk.oracle_diva_csm_tapes
collection: checkmk
short_description: DIVA Blank Tapes
description: 'Checkmk check ''oracle_diva_csm_tapes'' (service: DIVA Blank Tapes),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels_lower:
    type: list
    description: levels_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
