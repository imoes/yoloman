name: cmciii_phase
fqcn: checkmk.cmciii_phase
collection: checkmk
short_description: Input %s
description: 'Checkmk check ''cmciii_phase'' (service: Input %s), translated to a
  read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  version:
    type: string
    default: 2c
    description: version (from the check's parameters).
  use_sensor_description:
    type: bool
    default: 'False'
    description: use_sensor_description (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  _item_key:
    type: string
    description: _item_key (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
