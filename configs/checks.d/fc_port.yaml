name: fc_port
fqcn: checkmk.fc_port
collection: checkmk
short_description: FC Interface %s
description: 'Checkmk check ''fc_port'' (service: FC Interface %s), translated to
  a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  rxcrcs:
    type: list
    description: rxcrcs (from the check's parameters).
  rxencoutframes:
    type: list
    description: rxencoutframes (from the check's parameters).
  notxcredits:
    type: list
    description: notxcredits (from the check's parameters).
  c3discards:
    type: list
    description: c3discards (from the check's parameters).
  assumed_speed:
    type: number
    default: '16.0'
    description: assumed_speed (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
