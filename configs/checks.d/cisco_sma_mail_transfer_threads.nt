name: cisco_sma_mail_transfer_threads
fqcn: checkmk.cisco_sma_mail_transfer_threads
collection: checkmk
short_description: Mail transfer threads
description: Checkmk check 'cisco_sma_mail_transfer_threads' (service: Mail transfer threads), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels_upper_total_threads:
        type: list
        description: levels_upper_total_threads (from the check's parameters).
    levels_lower_total_threads:
        type: string
        description: levels_lower_total_threads (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
