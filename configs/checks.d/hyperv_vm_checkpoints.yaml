name: hyperv_vm_checkpoints
fqcn: checkmk.hyperv_vm_checkpoints
collection: checkmk
short_description: Hyper-V VM Checkpoints
description: 'Checkmk check ''hyperv_vm_checkpoints'' (service: Hyper-V VM Checkpoints),
  translated to a read-only on-host Starlark check module.'
options:
  age:
    type: string
    description: age (from the check's parameters).
  age_oldest:
    type: string
    description: age_oldest (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
