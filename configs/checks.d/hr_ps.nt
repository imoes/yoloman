name: hr_ps
fqcn: checkmk.hr_ps
collection: checkmk
short_description: Process %s
description: Checkmk check 'hr_ps' (service: Process %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    match_name_or_path:
        type: string
        description: match_name_or_path (from the check's parameters).
    match_status:
        type: string
        description: match_status (from the check's parameters).
    match_groups:
        type: string
        description: match_groups (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    status:
        type: list
        description: status (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
