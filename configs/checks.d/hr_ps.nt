name: hr_ps
fqcn: checkmk.hr_ps
collection: checkmk
short_description: Process %s
description: Monitoring check 'hr_ps' (service: Process %s), translated to a read-only on-host Starlark check module.
options:
    match_name_or_path:
        type: string
        description: match_name_or_path (from the check's parameters).
    match_status:
        type: string
        description: match_status (from the check's parameters).
    match_groups:
        type: string
        description: match_groups (from the check's parameters).
    discovery_default_parameters:
        type: string
        description: discovery_default_parameters (from the check's parameters).
    discovery_rules:
        type: list
        description: discovery_rules (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    status:
        type: list
        description: status (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
