name: orion_system_dc
fqcn: checkmk.orion_system_dc
collection: checkmk
short_description: Direct Current %s
description: 'Checkmk check ''orion_system_dc'' (service: Direct Current %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  voltage_warn:
    type: string
    description: voltage_warn (from the check's parameters).
  voltage_crit:
    type: string
    description: voltage_crit (from the check's parameters).
  current_warn:
    type: string
    description: current_warn (from the check's parameters).
  current_crit:
    type: string
    description: current_crit (from the check's parameters).
  power_warn:
    type: string
    description: power_warn (from the check's parameters).
  power_crit:
    type: string
    description: power_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
