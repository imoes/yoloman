name: ups_out_load
fqcn: checkmk.ups_out_load
collection: checkmk
short_description: OUT load phase %s
description: Monitoring check 'ups_out_load' (service: OUT load phase %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
