name: windows_tasks
fqcn: checkmk.windows_tasks
collection: checkmk
short_description: Task %s
description: 'Monitoring check ''windows_tasks'' (service: Task %s), translated to
  a read-only on-host Starlark check module.'
options:
  discover_disabled:
    type: bool
    default: 'False'
    description: discover_disabled (from the check's parameters).
  exit_code_to_state:
    type: list
    description: exit_code_to_state (from the check's parameters).
  state_not_enabled:
    type: number
    default: '1'
    description: state_not_enabled (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
