name: apc_symmetra
fqcn: checkmk.apc_symmetra
collection: checkmk
short_description: APC Symmetra status
description: 'Checkmk check ''apc_symmetra'' (service: APC Symmetra status), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
