name: scaleio_storage_pool_rebalancerw
fqcn: checkmk.scaleio_storage_pool_rebalancerw
collection: checkmk
short_description: ScaleIO SP rebalance IO %s
description: Checkmk check 'scaleio_storage_pool_rebalancerw' (service: ScaleIO SP rebalance IO %s), translated to a read-only on-host Starlark check module.
options:
    mdm_ip:
        type: string
        default:
        description: mdm_ip (from the check's parameters).
    read_ios:
        type: string
        description: read_ios (from the check's parameters).
    write_ios:
        type: string
        description: write_ios (from the check's parameters).
    read_throughput:
        type: string
        description: read_throughput (from the check's parameters).
    write_throughput:
        type: string
        description: write_throughput (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
