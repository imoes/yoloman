name: liebert_system_events
fqcn: checkmk.liebert_system_events
collection: checkmk
short_description: System events
description: 'Checkmk check ''liebert_system_events'' (service: System events), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
