name: bluecat_dns_queries
fqcn: checkmk.bluecat_dns_queries
collection: checkmk
short_description: DNS Queries
description: Checkmk check 'bluecat_dns_queries' (service: DNS Queries), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
