name: datadog_monitors
fqcn: checkmk.datadog_monitors
collection: checkmk
short_description: Datadog Monitor %s
description: 'Checkmk check ''datadog_monitors'' (service: Datadog Monitor %s), translated
  to a read-only on-host Starlark check module.'
options:
  api_url:
    type: string
    default: https://api.datadoghq.com
    description: api_url (from the check's parameters).
  api_key:
    type: string
    description: api_key (from the check's parameters).
  states_discover:
    type: string
    description: states_discover (from the check's parameters).
  state_mapping:
    type: string
    description: state_mapping (from the check's parameters).
  tags_to_show:
    type: list
    description: tags_to_show (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
