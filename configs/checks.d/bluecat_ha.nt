name: bluecat_ha
fqcn: checkmk.bluecat_ha
collection: checkmk
short_description: HA State
description: Checkmk check 'bluecat_ha' (service: HA State), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    oper_states:
        type: string
        description: oper_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
