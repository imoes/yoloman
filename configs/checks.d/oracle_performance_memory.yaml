name: oracle_performance_memory
fqcn: checkmk.oracle_performance_memory
collection: checkmk
short_description: ORA %s Performance Memory
description: 'Checkmk check ''oracle_performance_memory'' (service: ORA %s Performance
  Memory), translated to a read-only on-host Starlark check module.'
options:
  connect:
    type: string
    default: / as sysdba
    description: connect (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
