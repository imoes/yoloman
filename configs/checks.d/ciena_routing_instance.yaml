name: ciena_routing_instance
fqcn: checkmk.ciena_routing_instance
collection: checkmk
short_description: Routing instance %s
description: 'Checkmk check ''ciena_routing_instance'' (service: Routing instance
  %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  tx_warn:
    type: string
    description: tx_warn (from the check's parameters).
  tx_crit:
    type: string
    description: tx_crit (from the check's parameters).
  rx_warn:
    type: string
    description: rx_warn (from the check's parameters).
  rx_crit:
    type: string
    description: rx_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
