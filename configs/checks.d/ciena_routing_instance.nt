name: ciena_routing_instance
fqcn: checkmk.ciena_routing_instance
collection: checkmk
short_description: Routing instance %s
description: Monitoring check 'ciena_routing_instance' (service: Routing instance %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
