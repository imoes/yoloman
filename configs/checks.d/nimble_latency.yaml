name: nimble_latency
fqcn: checkmk.nimble_latency
collection: checkmk
short_description: Volume %s Read IO
description: 'Checkmk check ''nimble_latency'' (service: Volume %s Read IO), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: ''
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  direction:
    type: string
    default: read
    description: direction (from the check's parameters).
  range_reference:
    type: string
    default: '20'
    description: range_reference (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
