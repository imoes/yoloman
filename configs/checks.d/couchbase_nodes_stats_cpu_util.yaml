name: couchbase_nodes_stats_cpu_util
fqcn: checkmk.couchbase_nodes_stats_cpu_util
collection: checkmk
short_description: Couchbase %s CPU utilization
description: 'Checkmk check ''couchbase_nodes_stats_cpu_util'' (service: Couchbase
  %s CPU utilization), translated to a read-only on-host Starlark check module.'
options:
  warn:
    type: number
    default: '80'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
