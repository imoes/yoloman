name: msexch_replhealth
fqcn: checkmk.msexch_replhealth
collection: checkmk
short_description: Exchange Replication Health %s
description: Checkmk check 'msexch_replhealth' (service: Exchange Replication Health %s), translated to a read-only on-host Starlark check module.
options:
    state:
        type: string
        default: running
        description: state (from the check's parameters).
    warn:
        type: number
        default: 90
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 80
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
