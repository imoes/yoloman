name: postfix_mailq
fqcn: checkmk.postfix_mailq
collection: checkmk
short_description: Postfix Queue %s
description: Checkmk check 'postfix_mailq' (service: Postfix Queue %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: number
        default: 10
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 20
        description: crit (from the check's parameters).
    active_levels:
        type: string
        description: active_levels (from the check's parameters).
    active_warn:
        type: number
        default: 200
        description: active_warn (from the check's parameters).
    active_crit:
        type: number
        default: 300
        description: active_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
