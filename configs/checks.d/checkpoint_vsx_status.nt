name: checkpoint_vsx_status
fqcn: checkmk.checkpoint_vsx_status
collection: checkmk
short_description: VS %s Status
description: Monitoring check 'checkpoint_vsx_status' (service: VS %s Status), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
