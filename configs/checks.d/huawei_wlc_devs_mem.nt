name: huawei_wlc_devs_mem
fqcn: checkmk.huawei_wlc_devs_mem
collection: checkmk
short_description: Device %s Memory
description: Checkmk check 'huawei_wlc_devs_mem' (service: Device %s Memory), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
