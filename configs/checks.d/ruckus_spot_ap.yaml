name: ruckus_spot_ap
fqcn: checkmk.ruckus_spot_ap
collection: checkmk
short_description: Ruckus Spot Access Points %s
description: 'Checkmk check ''ruckus_spot_ap'' (service: Ruckus Spot Access Points
  %s), translated to a read-only on-host Starlark check module.'
options:
  ap_devices_total:
    type: string
    description: ap_devices_total (from the check's parameters).
  levels_drifted:
    type: string
    description: levels_drifted (from the check's parameters).
  levels_not_responding:
    type: string
    description: levels_not_responding (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
