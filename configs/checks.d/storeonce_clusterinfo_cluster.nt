name: storeonce_clusterinfo_cluster
fqcn: checkmk.storeonce_clusterinfo_cluster
collection: checkmk
short_description: Appliance Status
description: Checkmk check 'storeonce_clusterinfo_cluster' (service: Appliance Status), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: Admin
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
