name: allnet_ip_sensoric_pressure
fqcn: checkmk.allnet_ip_sensoric_pressure
collection: checkmk
short_description: Pressure %s
description: 'Checkmk check ''allnet_ip_sensoric_pressure'' (service: Pressure %s),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
