name: emc_datadomain_fs
fqcn: checkmk.emc_datadomain_fs
collection: checkmk
short_description: DD-Filesystem %s
description: Monitoring check 'emc_datadomain_fs' (service: DD-Filesystem %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
