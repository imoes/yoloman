name: hp_hh3c_ext_cpu
fqcn: checkmk.hp_hh3c_ext_cpu
collection: checkmk
short_description: CPU utilization %s
description: Monitoring check 'hp_hh3c_ext_cpu' (service: CPU utilization %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
