name: cmciii_status
fqcn: checkmk.cmciii_status
collection: checkmk
short_description: %s
description: Monitoring check 'cmciii_status' (service: %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    use_sensor_description:
        type: bool
        default: False
        description: use_sensor_description (from the check's parameters).
    _item_key:
        type: string
        description: _item_key (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
