name: cmciii_lcp_water
fqcn: checkmk.cmciii_lcp_water
collection: checkmk
short_description: Temperature Water LCP %s
description: 'Checkmk check ''cmciii_lcp_water'' (service: Temperature Water LCP %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  warn:
    type: number
    default: '70.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '80.0'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
