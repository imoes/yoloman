name: emc_datadomain_mtree
fqcn: checkmk.emc_datadomain_mtree
collection: checkmk
short_description: MTree %s
description: Monitoring check 'emc_datadomain_mtree' (service: MTree %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
