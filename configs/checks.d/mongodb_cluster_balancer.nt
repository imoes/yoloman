name: mongodb_cluster_balancer
fqcn: checkmk.mongodb_cluster_balancer
collection: checkmk
short_description: MongoDB Balancer
description: Monitoring check 'mongodb_cluster_balancer' (service: MongoDB Balancer), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 27017
        description: port (from the check's parameters).
    username:
        type: string
        default:
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    authdb:
        type: string
        default: admin
        description: authdb (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
