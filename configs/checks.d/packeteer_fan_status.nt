name: packeteer_fan_status
fqcn: checkmk.packeteer_fan_status
collection: checkmk
short_description: Fan Status %s
description: Checkmk check 'packeteer_fan_status' (service: Fan Status %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
