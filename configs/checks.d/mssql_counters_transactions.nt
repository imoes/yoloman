name: mssql_counters_transactions
fqcn: checkmk.mssql_counters_transactions
collection: checkmk
short_description: MSSQL %s Transactions
description: Checkmk check 'mssql_counters_transactions' (service: MSSQL %s Transactions), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 1433
        description: port (from the check's parameters).
    username:
        type: string
        default: sa
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
