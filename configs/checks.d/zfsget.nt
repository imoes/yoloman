name: zfsget
fqcn: checkmk.zfsget
collection: checkmk
short_description: Filesystem %s
description: Checkmk check 'zfsget' (service: Filesystem %s), translated to a read-only on-host Starlark check module.
options:
    warn_used:
        type: number
        default: 80
        description: warn_used (from the check's parameters).
    crit_used:
        type: number
        default: 90
        description: crit_used (from the check's parameters).
    crit_free:
        type: number
        default: 5
        description: crit_free (from the check's parameters).
    warn_free:
        type: number
        default: 10
        description: warn_free (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
