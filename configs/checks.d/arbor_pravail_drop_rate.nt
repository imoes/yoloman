name: arbor_pravail_drop_rate
fqcn: checkmk.arbor_pravail_drop_rate
collection: checkmk
short_description: %s drop rate
description: Checkmk check 'arbor_pravail_drop_rate' (service: %s drop rate), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        description: community (from the check's parameters).
    host:
        type: string
        description: host (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    levels_lower:
        type: string
        description: levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
