name: fortisandbox_disk_usage
fqcn: checkmk.fortisandbox_disk_usage
collection: checkmk
short_description: Disk usage %s
description: Checkmk check 'fortisandbox_disk_usage' (service: Disk usage %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    version:
        type: string
        default: 2c
        description: version (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
