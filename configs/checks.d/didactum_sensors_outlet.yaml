name: didactum_sensors_outlet
fqcn: checkmk.didactum_sensors_outlet
collection: checkmk
short_description: Relay %s
description: 'Checkmk check ''didactum_sensors_outlet'' (service: Relay %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
