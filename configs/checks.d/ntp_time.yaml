name: ntp_time
fqcn: checkmk.ntp_time
collection: checkmk
short_description: NTP Time
description: 'Checkmk check ''ntp_time'' (service: NTP Time), translated to a read-only
  on-host Starlark check module.'
options:
  mode:
    type: string
    default: summary
    description: mode (from the check's parameters).
  ntp_levels:
    type: list
    description: ntp_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
