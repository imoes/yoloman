name: skype_conferencing
fqcn: checkmk.skype_conferencing
collection: checkmk
short_description: Skype Conferencing
description: Monitoring check 'skype_conferencing' (service: Skype Conferencing), translated to a read-only on-host Starlark check module.
options:
    incomplete_calls_warn:
        type: number
        default: 20.0
        description: incomplete_calls_warn (from the check's parameters).
    incomplete_calls_crit:
        type: number
        default: 40.0
        description: incomplete_calls_crit (from the check's parameters).
    create_conference_latency_warn:
        type: number
        default: 5000.0
        description: create_conference_latency_warn (from the check's parameters).
    create_conference_latency_crit:
        type: number
        default: 10000.0
        description: create_conference_latency_crit (from the check's parameters).
    allocation_latency_warn:
        type: number
        default: 5000.0
        description: allocation_latency_warn (from the check's parameters).
    allocation_latency_crit:
        type: number
        default: 10000.0
        description: allocation_latency_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
