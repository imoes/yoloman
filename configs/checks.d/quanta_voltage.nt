name: quanta_voltage
fqcn: checkmk.quanta_voltage
collection: checkmk
short_description: Voltage %s
description: Checkmk check 'quanta_voltage' (service: Voltage %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    levels_lower:
        type: string
        description: levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
