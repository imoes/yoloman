name: ciena_port_power
fqcn: checkmk.ciena_port_power
collection: checkmk
short_description: Port %s XCVR Power
description: 'Checkmk check ''ciena_port_power'' (service: Port %s XCVR Power), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels_rx_upper_warn:
    type: string
    description: levels_rx_upper_warn (from the check's parameters).
  levels_rx_upper_crit:
    type: string
    description: levels_rx_upper_crit (from the check's parameters).
  levels_rx_lower_warn:
    type: string
    description: levels_rx_lower_warn (from the check's parameters).
  levels_rx_lower_crit:
    type: string
    description: levels_rx_lower_crit (from the check's parameters).
  levels_tx_upper_warn:
    type: string
    description: levels_tx_upper_warn (from the check's parameters).
  levels_tx_upper_crit:
    type: string
    description: levels_tx_upper_crit (from the check's parameters).
  levels_tx_lower_warn:
    type: string
    description: levels_tx_lower_warn (from the check's parameters).
  levels_tx_lower_crit:
    type: string
    description: levels_tx_lower_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
