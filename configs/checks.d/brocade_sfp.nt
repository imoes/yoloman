name: brocade_sfp
fqcn: checkmk.brocade_sfp
collection: checkmk
short_description: SFP %s
description: Monitoring check 'brocade_sfp' (service: SFP %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    rx_power:
        type: list
        description: rx_power (from the check's parameters).
    tx_power:
        type: list
        description: tx_power (from the check's parameters).
    current:
        type: list
        description: current (from the check's parameters).
    voltage:
        type: list
        description: voltage (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
