name: emc_vplex_cpu
fqcn: checkmk.emc_vplex_cpu
collection: checkmk
short_description: CPU Utilization %s
description: Checkmk check 'emc_vplex_cpu' (service: CPU Utilization %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
