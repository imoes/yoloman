name: storeonce4x_cat_stores
fqcn: checkmk.storeonce4x_cat_stores
collection: checkmk
short_description: Catalyst Stores %s
description: Checkmk check 'storeonce4x_cat_stores' (service: Catalyst Stores %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: admin
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    warn:
        type: number
        default: 80
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
