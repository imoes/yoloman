name: safenet_ntls
fqcn: checkmk.safenet_ntls
collection: checkmk
short_description: NTLS Operation Status
description: Checkmk check 'safenet_ntls' (service: NTLS Operation Status), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    version:
        type: string
        default: 2c
        description: version (from the check's parameters).
    _plugin:
        type: string
        description: _plugin (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
