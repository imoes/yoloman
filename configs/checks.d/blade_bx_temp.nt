name: blade_bx_temp
fqcn: checkmk.blade_bx_temp
collection: checkmk
short_description: Temperature Blade %s
description: Monitoring check 'blade_bx_temp' (service: Temperature Blade %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 20.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 40.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
