name: ceph_df
fqcn: checkmk.ceph_df
collection: checkmk
short_description: Ceph Pool %s
description: 'Checkmk check ''ceph_df'' (service: Ceph Pool %s), translated to a read-only
  on-host Starlark check module.'
options:
  warn:
    type: number
    default: '80'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
