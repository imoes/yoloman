name: salesforce_instances
fqcn: checkmk.salesforce_instances
collection: checkmk
short_description: Salesforce Instance %s
description: 'Checkmk check ''salesforce_instances'' (service: Salesforce Instance
  %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: status.salesforce.com
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
