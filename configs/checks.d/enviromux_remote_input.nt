name: enviromux_remote_input
fqcn: checkmk.enviromux_remote_input
collection: checkmk
short_description: Remote Input %s
description: Monitoring check 'enviromux_remote_input' (service: Remote Input %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
