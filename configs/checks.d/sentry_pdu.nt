name: sentry_pdu
fqcn: checkmk.sentry_pdu
collection: checkmk
short_description: Plug %s
description: Checkmk check 'sentry_pdu' (service: Plug %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    required_state:
        type: string
        description: required_state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
