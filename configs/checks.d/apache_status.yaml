name: apache_status
fqcn: checkmk.apache_status
collection: checkmk
short_description: Apache %s Status
description: 'Checkmk check ''apache_status'' (service: Apache %s Status), translated
  to a read-only on-host Starlark check module.'
options:
  status_url:
    type: string
    default: http://localhost/server-status?auto
    description: status_url (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
