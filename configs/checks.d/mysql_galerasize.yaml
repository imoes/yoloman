name: mysql_galerasize
fqcn: checkmk.mysql_galerasize
collection: checkmk
short_description: MySQL Galera Size %s
description: 'Checkmk check ''mysql_galerasize'' (service: MySQL Galera Size %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  user:
    type: string
    default: ''
    description: user (from the check's parameters).
  password:
    type: string
    default: ''
    description: password (from the check's parameters).
  socket:
    type: string
    default: ''
    description: socket (from the check's parameters).
  invsize:
    type: string
    description: invsize (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
