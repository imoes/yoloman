name: hepta_syncmoduletimesyncstate
fqcn: checkmk.hepta_syncmoduletimesyncstate
collection: checkmk
short_description: '%s'
description: 'Monitoring check ''hepta_syncmoduletimesyncstate'' (service: %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
