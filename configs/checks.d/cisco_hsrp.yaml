name: cisco_hsrp
fqcn: checkmk.cisco_hsrp
collection: checkmk
short_description: HSRP Group %s
description: 'Checkmk check ''cisco_hsrp'' (service: HSRP Group %s), translated to
  a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  group:
    type: string
    default: ''
    description: group (from the check's parameters).
  state:
    type: number
    default: '0'
    description: state (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
