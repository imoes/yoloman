name: mobileiron_versions
fqcn: checkmk.mobileiron_versions
collection: checkmk
short_description: Mobileiron versions
description: Monitoring check 'mobileiron_versions' (service: Mobileiron versions), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
    device_uuid:
        type: string
        default:
        description: device_uuid (from the check's parameters).
    patchlevel_unparsable:
        type: number
        default: 0
        description: patchlevel_unparsable (from the check's parameters).
    patchlevel_age:
        type: number
        default: 7776000
        description: patchlevel_age (from the check's parameters).
    os_build_unparsable:
        type: number
        default: 0
        description: os_build_unparsable (from the check's parameters).
    os_age:
        type: number
        default: 7776000
        description: os_age (from the check's parameters).
    ios_version_regexp:
        type: string
        default:
        description: ios_version_regexp (from the check's parameters).
    android_version_regexp:
        type: string
        default:
        description: android_version_regexp (from the check's parameters).
    os_version_other:
        type: number
        default: 0
        description: os_version_other (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
