name: cisco_fru_power
fqcn: checkmk.cisco_fru_power
collection: checkmk
short_description: FRU Power %s
description: Checkmk check 'cisco_fru_power' (service: FRU Power %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
