name: storeonce4x_cat_stores
fqcn: checkmk.storeonce4x_cat_stores
collection: checkmk
short_description: Catalyst Stores %s
description: 'Checkmk check ''storeonce4x_cat_stores'' (service: Catalyst Stores %s),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
