name: podman_disk_usage
fqcn: checkmk.podman_disk_usage
collection: checkmk
short_description: Podman disk usage: %s
description: Checkmk check 'podman_disk_usage' (service: Podman disk usage: %s), translated to a read-only on-host Starlark check module.
options:
    size_upper:
        type: string
        description: size_upper (from the check's parameters).
    reclaimable_upper:
        type: string
        description: reclaimable_upper (from the check's parameters).
    total:
        type: string
        description: total (from the check's parameters).
    active:
        type: string
        description: active (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
