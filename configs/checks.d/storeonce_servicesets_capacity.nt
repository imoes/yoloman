name: storeonce_servicesets_capacity
fqcn: checkmk.storeonce_servicesets_capacity
collection: checkmk
short_description: ServiceSet %s Capacity
description: Checkmk check 'storeonce_servicesets_capacity' (service: ServiceSet %s Capacity), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: Admin
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
