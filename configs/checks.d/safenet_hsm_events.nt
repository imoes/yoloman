name: safenet_hsm_events
fqcn: checkmk.safenet_hsm_events
collection: checkmk
short_description: HSM Safenet Event Stats
description: Monitoring check 'safenet_hsm_events' (service: HSM Safenet Event Stats), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    critical_events:
        type: list
        description: critical_events (from the check's parameters).
    noncritical_events:
        type: list
        description: noncritical_events (from the check's parameters).
    critical_event_rate:
        type: list
        description: critical_event_rate (from the check's parameters).
    noncritical_event_rate:
        type: list
        description: noncritical_event_rate (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
