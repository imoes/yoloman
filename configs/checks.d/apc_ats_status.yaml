name: apc_ats_status
fqcn: checkmk.apc_ats_status
collection: checkmk
short_description: ATS Status
description: 'Checkmk check ''apc_ats_status'' (service: ATS Status), translated to
  a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  version:
    type: string
    default: 2c
    description: version (from the check's parameters).
  power_source:
    type: string
    default: ''
    description: power_source (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
