name: hr_fs
fqcn: checkmk.hr_fs
collection: checkmk
short_description: Filesystem %s
description: Monitoring check 'hr_fs' (service: Filesystem %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: number
        default: 80.0
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
