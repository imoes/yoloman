name: docker_node_info_containers
fqcn: checkmk.docker_node_info_containers
collection: checkmk
short_description: Docker containers
description: 'Monitoring check ''docker_node_info_containers'' (service: Docker containers),
  translated to a read-only on-host Starlark check module.'
options:
  upper_levels:
    type: list
    description: upper_levels (from the check's parameters).
  lower_levels:
    type: list
    description: lower_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
