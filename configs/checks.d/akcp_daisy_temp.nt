name: akcp_daisy_temp
fqcn: checkmk.akcp_daisy_temp
collection: checkmk
short_description: Temperature %s
description: Monitoring check 'akcp_daisy_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 28.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 32.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
