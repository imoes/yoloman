name: jar_signature
fqcn: checkmk.jar_signature
collection: checkmk
short_description: Jar-Signature %s
description: Checkmk check 'jar_signature' (service: Jar-Signature %s), translated to a read-only on-host Starlark check module.
options:
    jar_dir:
        type: string
        default: /usr/share/java
        description: jar_dir (from the check's parameters).
    warn:
        type: number
        default: 60
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 30
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
