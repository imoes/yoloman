name: hp_sts_drvbox
fqcn: checkmk.hp_sts_drvbox
collection: checkmk
short_description: Drive Box %s
description: Monitoring check 'hp_sts_drvbox' (service: Drive Box %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
