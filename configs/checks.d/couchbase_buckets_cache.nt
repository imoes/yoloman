name: couchbase_buckets_cache
fqcn: checkmk.couchbase_buckets_cache
collection: checkmk
short_description: Couchbase Bucket %s Cache
description: Checkmk check 'couchbase_buckets_cache' (service: Couchbase Bucket %s Cache), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8091
        description: port (from the check's parameters).
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    cache_misses:
        type: string
        description: cache_misses (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
