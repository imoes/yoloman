name: cisco_cpu_memory
fqcn: checkmk.cisco_cpu_memory
collection: checkmk
short_description: CPU Memory utilization %s
description: Checkmk check 'cisco_cpu_memory' (service: CPU Memory utilization %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
