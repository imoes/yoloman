name: postgres_connections
fqcn: checkmk.postgres_connections
collection: checkmk
short_description: PostgreSQL Connections %s
description: 'Checkmk check ''postgres_connections'' (service: PostgreSQL Connections
  %s), translated to a read-only on-host Starlark check module.'
options:
  levels_perc_active:
    type: list
    description: levels_perc_active (from the check's parameters).
  levels_perc_idle:
    type: list
    description: levels_perc_idle (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
