name: ibm_svc_mdiskgrp
fqcn: checkmk.ibm_svc_mdiskgrp
collection: checkmk
short_description: Pool Capacity %s
description: Checkmk check 'ibm_svc_mdiskgrp' (service: Pool Capacity %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
    provisioning_warn:
        type: string
        description: provisioning_warn (from the check's parameters).
    provisioning_crit:
        type: string
        description: provisioning_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
