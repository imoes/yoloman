name: couchbase_nodes_services
fqcn: checkmk.couchbase_nodes_services
collection: checkmk
short_description: Couchbase %s Services
description: 'Checkmk check ''couchbase_nodes_services'' (service: Couchbase %s Services),
  translated to a read-only on-host Starlark check module.'
options:
  discovered_services:
    type: list
    description: discovered_services (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
