name: didactum_sensors_analog_voltage
fqcn: checkmk.didactum_sensors_analog_voltage
collection: checkmk
short_description: Phase %s
description: Checkmk check 'didactum_sensors_analog_voltage' (service: Phase %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels_upper:
        type: string
        description: levels_upper (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
