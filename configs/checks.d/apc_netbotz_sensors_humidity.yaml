name: apc_netbotz_sensors_humidity
fqcn: checkmk.apc_netbotz_sensors_humidity
collection: checkmk
short_description: Humidity %s
description: 'Checkmk check ''apc_netbotz_sensors_humidity'' (service: Humidity %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  warn:
    type: number
    default: '60'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '65'
    description: crit (from the check's parameters).
  warn_low:
    type: number
    default: '35'
    description: warn_low (from the check's parameters).
  crit_low:
    type: number
    default: '30'
    description: crit_low (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
