name: liebert_temp_fluid
fqcn: checkmk.liebert_temp_fluid
collection: checkmk
short_description: %s
description: Monitoring check 'liebert_temp_fluid' (service: %s), translated to a read-only on-host Starlark check module.
options:
    levels_upper:
        type: list
        description: levels_upper (from the check's parameters).
    levels_upper_critical:
        type: list
        description: levels_upper_critical (from the check's parameters).
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
    levels_lower_critical:
        type: list
        description: levels_lower_critical (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 25
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 30
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
