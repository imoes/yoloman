name: oracle_dataguard_stats
fqcn: checkmk.oracle_dataguard_stats
collection: checkmk
short_description: ORA %s Dataguard-Stats
description: 'Monitoring check ''oracle_dataguard_stats'' (service: ORA %s Dataguard-Stats),
  translated to a read-only on-host Starlark check module.'
options:
  primary_broker_state:
    type: string
    description: primary_broker_state (from the check's parameters).
  missing_apply_lag_state:
    type: string
    description: missing_apply_lag_state (from the check's parameters).
  apply_lag:
    type: string
    description: apply_lag (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
