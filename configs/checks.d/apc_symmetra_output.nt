name: apc_symmetra_output
fqcn: checkmk.apc_symmetra_output
collection: checkmk
short_description: Phase %s
description: Checkmk check 'apc_symmetra_output' (service: Phase %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    voltage:
        type: list
        description: voltage (from the check's parameters).
    current:
        type: string
        description: current (from the check's parameters).
    output_load:
        type: string
        description: output_load (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
