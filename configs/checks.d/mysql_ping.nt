name: mysql_ping
fqcn: checkmk.mysql_ping
collection: checkmk
short_description: MySQL Instance %s
description: Monitoring check 'mysql_ping' (service: MySQL Instance %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default:
        description: host (from the check's parameters).
    port:
        type: number
        default: 0
        description: port (from the check's parameters).
    socket:
        type: string
        default:
        description: socket (from the check's parameters).
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    defaults_extra_file:
        type: string
        default:
        description: defaults_extra_file (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
