name: hp_msa_volume_df
fqcn: checkmk.hp_msa_volume_df
collection: checkmk
short_description: Filesystem %s
description: 'Checkmk check ''hp_msa_volume_df'' (service: Filesystem %s), translated
  to a read-only on-host Starlark check module.'
options:
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
