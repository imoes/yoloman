name: allnet_ip_sensoric_humidity
fqcn: checkmk.allnet_ip_sensoric_humidity
collection: checkmk
short_description: Humidity %s
description: 'Checkmk check ''allnet_ip_sensoric_humidity'' (service: Humidity %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  levels_lower:
    type: string
    description: levels_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
