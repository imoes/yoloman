name: brocade_fcport
fqcn: checkmk.brocade_fcport
collection: checkmk
short_description: Port %s
description: 'Checkmk check ''brocade_fcport'' (service: Port %s), translated to a
  read-only on-host Starlark check module.'
options:
  assumed_speed:
    type: number
    default: '2.0'
    description: assumed_speed (from the check's parameters).
  average:
    type: string
    description: average (from the check's parameters).
  bw:
    type: string
    description: bw (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
