name: sap_hana_backup
fqcn: checkmk.sap_hana_backup
collection: checkmk
short_description: SAP HANA Backup %s
description: 'Checkmk check ''sap_hana_backup'' (service: SAP HANA Backup %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: string
    default: '30015'
    description: port (from the check's parameters).
  user:
    type: string
    default: SYSTEM
    description: user (from the check's parameters).
  secret:
    type: string
    default: hdbsql_secret
    description: secret (from the check's parameters).
  backup_age:
    type: list
    description: backup_age (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
