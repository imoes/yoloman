name: avaya_45xx_fan
fqcn: checkmk.avaya_45xx_fan
collection: checkmk
short_description: Fan Chassis %s
description: 'Checkmk check ''avaya_45xx_fan'' (service: Fan Chassis %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
