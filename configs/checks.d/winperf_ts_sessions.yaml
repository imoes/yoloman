name: winperf_ts_sessions
fqcn: checkmk.winperf_ts_sessions
collection: checkmk
short_description: Sessions
description: 'Checkmk check ''winperf_ts_sessions'' (service: Sessions), translated
  to a read-only on-host Starlark check module.'
options:
  active:
    type: list
    description: active (from the check's parameters).
  inactive:
    type: list
    description: inactive (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
