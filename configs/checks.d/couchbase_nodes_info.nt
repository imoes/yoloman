name: couchbase_nodes_info
fqcn: checkmk.couchbase_nodes_info
collection: checkmk
short_description: Couchbase %s Info
description: Checkmk check 'couchbase_nodes_info' (service: Couchbase %s Info), translated to a read-only on-host Starlark check module.
options:
    username:
        type: string
        default: admin
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8091
        description: port (from the check's parameters).
    warmup_state:
        type: number
        default: 0
        description: warmup_state (from the check's parameters).
    unhealthy_state:
        type: number
        default: 2
        description: unhealthy_state (from the check's parameters).
    inactive_added_state:
        type: number
        default: 1
        description: inactive_added_state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
