name: 3par_system
fqcn: checkmk.3par_system
collection: checkmk
short_description: 3PAR %s
description: Checkmk check '3par_system' (service: 3PAR %s), translated to a read-only on-host Starlark check module.
options:
    password:
        type: string
        default:
        description: password (from the check's parameters).
    username:
        type: string
        default: 3paradm
        description: username (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
