name: arris_cmts_cpu
fqcn: checkmk.arris_cmts_cpu
collection: checkmk
short_description: CPU utilization Module %s
description: 'Checkmk check ''arris_cmts_cpu'' (service: CPU utilization Module %s),
  translated to a read-only on-host Starlark check module.'
options:
  levels:
    type: string
    description: levels (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
