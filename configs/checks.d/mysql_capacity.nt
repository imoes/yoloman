name: mysql_capacity
fqcn: checkmk.mysql_capacity
collection: checkmk
short_description: MySQL DB Size %s
description: Checkmk check 'mysql_capacity' (service: MySQL DB Size %s), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default: root
        description: user (from the check's parameters).
    password:
        type: string
        description: password (from the check's parameters).
    socket:
        type: string
        description: socket (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 3306
        description: port (from the check's parameters).
    instance:
        type: string
        default: mysql
        description: instance (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
