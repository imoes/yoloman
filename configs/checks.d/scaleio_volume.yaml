name: scaleio_volume
fqcn: checkmk.scaleio_volume
collection: checkmk
short_description: ScaleIO Volume %s
description: 'Checkmk check ''scaleio_volume'' (service: ScaleIO Volume %s), translated
  to a read-only on-host Starlark check module.'
options:
  scaleio_volume:
    type: string
    description: scaleio_volume (from the check's parameters).
  read_ios_warn:
    type: string
    description: read_ios_warn (from the check's parameters).
  read_ios_crit:
    type: string
    description: read_ios_crit (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
