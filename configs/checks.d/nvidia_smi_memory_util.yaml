name: nvidia_smi_memory_util
fqcn: checkmk.nvidia_smi_memory_util
collection: checkmk
short_description: Nvidia GPU Memory utilization %s
description: 'Checkmk check ''nvidia_smi_memory_util'' (service: Nvidia GPU Memory
  utilization %s), translated to a read-only on-host Starlark check module.'
options:
  levels_total:
    type: list
    description: levels_total (from the check's parameters).
  levels_bar1:
    type: list
    description: levels_bar1 (from the check's parameters).
  levels_fb:
    type: list
    description: levels_fb (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
