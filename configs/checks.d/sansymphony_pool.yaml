name: sansymphony_pool
fqcn: checkmk.sansymphony_pool
collection: checkmk
short_description: Sansymphony Pool %s
description: 'Monitoring check ''sansymphony_pool'' (service: Sansymphony Pool %s),
  translated to a read-only on-host Starlark check module.'
options:
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
