name: ibm_svc_systemstats_disk_latency
fqcn: checkmk.ibm_svc_systemstats_disk_latency
collection: checkmk
short_description: Latency %s Total
description: 'Checkmk check ''ibm_svc_systemstats_disk_latency'' (service: Latency
  %s Total), translated to a read-only on-host Starlark check module.'
options:
  source:
    type: string
    default: /var/lib/cmk/ibm_svc_systemstats
    description: source (from the check's parameters).
  read:
    type: string
    description: read (from the check's parameters).
  crit_read:
    type: string
    description: crit_read (from the check's parameters).
  write:
    type: string
    description: write (from the check's parameters).
  crit_write:
    type: string
    description: crit_write (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
