name: couchbase_nodes_size_docs
fqcn: checkmk.couchbase_nodes_size_docs
collection: checkmk
short_description: Couchbase %s Documents
description: Monitoring check 'couchbase_nodes_size_docs' (service: Couchbase %s Documents), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8091
        description: port (from the check's parameters).
    user:
        type: string
        default: Administrator
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    community:
        type: string
        default:
        description: community (from the check's parameters).
    size_on_disk:
        type: string
        description: size_on_disk (from the check's parameters).
    size:
        type: string
        description: size (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
