name: alertmanager_rules
fqcn: checkmk.alertmanager_rules
collection: checkmk
short_description: Alertmanager Check
description: 'Checkmk check ''alertmanager_rules'' (service: Alert Rule %s), translated
  to a read-only on-host Starlark check module.'
options:
  api_url:
    type: string
    default: http://localhost:9093/api/v2/status
    description: api_url (from the check's parameters).
  timeout:
    type: number
    default: '10'
    description: timeout (from the check's parameters).
  alert_remapping:
    type: string
    description: alert_remapping (from the check's parameters).
  summary_service:
    type: bool
    default: 'True'
    description: summary_service (from the check's parameters).
  group_services:
    type: string
    description: group_services (from the check's parameters).
  no_group_services:
    type: list
    description: no_group_services (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
