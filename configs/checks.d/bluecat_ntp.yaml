name: bluecat_ntp
fqcn: checkmk.bluecat_ntp
collection: checkmk
short_description: NTP
description: 'Checkmk check ''bluecat_ntp'' (service: NTP), translated to a read-only
  on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  oper_states:
    type: string
    description: oper_states (from the check's parameters).
  stratum:
    type: list
    description: stratum (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
