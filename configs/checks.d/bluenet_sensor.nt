name: bluenet_sensor
fqcn: checkmk.bluenet_sensor
collection: checkmk
short_description: Temperature %s
description: Monitoring check 'bluenet_sensor' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
