name: ra3s_sensors_power
fqcn: checkmk.ra3s_sensors_power
collection: checkmk
short_description: Power State %s
description: Monitoring check 'ra3s_sensors_power' (service: Power State %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
