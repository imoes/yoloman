name: emka_modules_relay
fqcn: checkmk.emka_modules_relay
collection: checkmk
short_description: Relay %s
description: Checkmk check 'emka_modules_relay' (service: Relay %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
