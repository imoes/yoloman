name: fjdarye_expanders
fqcn: checkmk.fjdarye_expanders
collection: checkmk
short_description: Expander %s
description: Checkmk check 'fjdarye_expanders' (service: Expander %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
