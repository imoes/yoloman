name: mssql_datafiles
fqcn: checkmk.mssql_datafiles
collection: checkmk
short_description: MSSQL Datafile %s
description: 'Monitoring check ''mssql_datafiles'' (service: MSSQL Datafile %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  instance:
    type: string
    default: ''
    description: instance (from the check's parameters).
  user:
    type: string
    default: ''
    description: user (from the check's parameters).
  password:
    type: string
    default: ''
    description: password (from the check's parameters).
  port:
    type: number
    default: '1433'
    description: port (from the check's parameters).
  used_levels:
    type: list
    description: used_levels (from the check's parameters).
  allocated_used_levels:
    type: list
    description: allocated_used_levels (from the check's parameters).
  allocated_levels:
    type: list
    description: allocated_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
