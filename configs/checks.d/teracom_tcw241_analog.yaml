name: teracom_tcw241_analog
fqcn: checkmk.teracom_tcw241_analog
collection: checkmk
short_description: Analog Sensor %s
description: 'Monitoring check ''teracom_tcw241_analog'' (service: Analog Sensor %s),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
