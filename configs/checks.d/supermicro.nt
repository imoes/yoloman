name: supermicro
fqcn: checkmk.supermicro
collection: checkmk
short_description: Overall Hardware Health
description: Checkmk check 'supermicro' (service: Overall Hardware Health), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
