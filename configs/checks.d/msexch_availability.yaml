name: msexch_availability
fqcn: checkmk.msexch_availability
collection: checkmk
short_description: Exchange Availability Service
description: 'Checkmk check ''msexch_availability'' (service: Exchange Availability
  Service), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  warn:
    type: number
    default: '20'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '40'
    description: crit (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
