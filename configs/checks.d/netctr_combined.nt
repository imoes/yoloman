name: netctr_combined
fqcn: checkmk.netctr_combined
collection: checkmk
short_description: NIC %s counters
description: Checkmk check 'netctr_combined' (service: NIC %s counters), translated to a read-only on-host Starlark check module.
options:
    linux_nic_check:
        type: string
        default: lnx_if
        description: linux_nic_check (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: number
        default: 0.01
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 0.1
        description: crit (from the check's parameters).
    _this_time:
        type: string
        default: 0
        description: _this_time (from the check's parameters).
    _rate_store:
        type: string
        default: {}
        description: _rate_store (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
