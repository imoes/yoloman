name: checkpoint_memory
fqcn: checkmk.checkpoint_memory
collection: checkmk
short_description: Memory System
description: Checkmk check 'checkpoint_memory' (service: Memory System), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
