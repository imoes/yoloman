name: couchbase_nodes_size_spacial_views
fqcn: checkmk.couchbase_nodes_size_spacial_views
collection: checkmk
short_description: Couchbase %s Spacial Views
description: Checkmk check 'couchbase_nodes_size_spacial_views' (service: Couchbase %s Spacial Views), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    username:
        type: string
        default: Administrator
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    port:
        type: string
        default: 8091
        description: port (from the check's parameters).
    size_on_disk:
        type: string
        description: size_on_disk (from the check's parameters).
    size:
        type: string
        description: size (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
