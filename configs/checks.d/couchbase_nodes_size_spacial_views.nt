name: couchbase_nodes_size_spacial_views
fqcn: checkmk.couchbase_nodes_size_spacial_views
collection: checkmk
short_description: Couchbase %s Spacial Views
description: Monitoring check 'couchbase_nodes_size_spacial_views' (service: Couchbase %s Spacial Views), translated to a read-only on-host Starlark check module.
options:
    username:
        type: string
        default: Administrator
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    url:
        type: string
        default: http://localhost:8091/pools/default/buckets
        description: url (from the check's parameters).
    size_on_disk:
        type: string
        description: size_on_disk (from the check's parameters).
    size:
        type: string
        description: size (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
