name: cmctc_ports
fqcn: checkmk.cmctc_ports
collection: checkmk
short_description: Port %s
description: Checkmk check 'cmctc_ports' (service: Port %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
