name: hepta_syncmoduletimelocal
fqcn: checkmk.hepta_syncmoduletimelocal
collection: checkmk
short_description: '%s'
description: 'Checkmk check ''hepta_syncmoduletimelocal'' (service: %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    description: host (from the check's parameters).
  community:
    type: string
    description: community (from the check's parameters).
  version:
    type: string
    description: version (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
