name: mail_loop
fqcn: checkmk.mail_loop
collection: checkmk
short_description: ''
description: Checkmk active check 'mail_loop', translated to a read-only on-host Starlark
  check module.
options:
  fetch:
    type: string
    description: fetch (from the check's parameters).
  connect_timeout:
    type: number
    default: '0.0'
    description: connect_timeout (from the check's parameters).
  send:
    type: string
    description: send (from the check's parameters).
  mail_from:
    type: string
    default: ''
    description: mail_from (from the check's parameters).
  mail_to:
    type: string
    default: ''
    description: mail_to (from the check's parameters).
  delete_messages:
    type: bool
    default: 'False'
    description: delete_messages (from the check's parameters).
  duration:
    type: string
    description: duration (from the check's parameters).
  subject:
    type: string
    description: subject (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
