name: ssh
fqcn: checkmk.ssh
collection: checkmk
short_description:
description: Checkmk active check 'ssh', translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        description: host (from the check's parameters).
    port:
        type: number
        default: 22
        description: port (from the check's parameters).
    description:
        type: string
        default:
        description: description (from the check's parameters).
    timeout:
        type: number
        default: 10
        description: timeout (from the check's parameters).
    remote_version:
        type: string
        description: remote_version (from the check's parameters).
    remote_protocol:
        type: string
        description: remote_protocol (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
