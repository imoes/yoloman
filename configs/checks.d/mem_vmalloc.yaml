name: mem_vmalloc
fqcn: checkmk.mem_vmalloc
collection: checkmk
short_description: Vmalloc address space
description: 'Checkmk check ''mem_vmalloc'' (service: Vmalloc address space), translated
  to a read-only on-host Starlark check module.'
options:
  levels_used_perc:
    type: list
    description: levels_used_perc (from the check's parameters).
  levels_lower_chunk_mb:
    type: list
    description: levels_lower_chunk_mb (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
