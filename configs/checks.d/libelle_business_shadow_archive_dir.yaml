name: libelle_business_shadow_archive_dir
fqcn: checkmk.libelle_business_shadow_archive_dir
collection: checkmk
short_description: Libelle Business Shadow %s
description: 'Checkmk check ''libelle_business_shadow_archive_dir'' (service: Libelle
  Business Shadow %s), translated to a read-only on-host Starlark check module.'
options:
  warn:
    type: number
    default: '70'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90'
    description: crit (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
