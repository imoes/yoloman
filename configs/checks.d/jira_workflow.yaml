name: jira_workflow
fqcn: checkmk.jira_workflow
collection: checkmk
short_description: Jira Workflow %s
description: 'Monitoring check ''jira_workflow'' (service: Jira Workflow %s), translated
  to a read-only on-host Starlark check module.'
options:
  workflow_count_upper:
    type: list
    description: workflow_count_upper (from the check's parameters).
  workflow_count_lower:
    type: list
    description: workflow_count_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
