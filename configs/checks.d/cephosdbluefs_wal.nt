name: cephosdbluefs_wal
fqcn: checkmk.cephosdbluefs_wal
collection: checkmk
short_description: Ceph OSD %s WAL
description: Monitoring check 'cephosdbluefs_wal' (service: Ceph OSD %s WAL), translated to a read-only on-host Starlark check module.
options:
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
