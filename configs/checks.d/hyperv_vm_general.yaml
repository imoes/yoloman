name: hyperv_vm_general
fqcn: checkmk.hyperv_vm_general
collection: checkmk
short_description: Hyper-V VM summary
description: 'Checkmk check ''hyperv_vm_general'' (service: Hyper-V VM summary), translated
  to a read-only on-host Starlark check module.'
options:
  power_state:
    type: string
    description: power_state (from the check's parameters).
  vm_generation:
    type: string
    description: vm_generation (from the check's parameters).
  state_if_not_expected:
    type: number
    default: '1'
    description: state_if_not_expected (from the check's parameters).
  section_path:
    type: string
    default: /var/lib/hyperv_vm_general/section.json
    description: section_path (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
