name: postgres_connections
fqcn: checkmk.postgres_connections
collection: checkmk
short_description: PostgreSQL Connections %s
description: Monitoring check 'postgres_connections' (service: PostgreSQL Connections %s), translated to a read-only on-host Starlark check module.
options:
    levels_abs_active:
        type: list
        description: levels_abs_active (from the check's parameters).
    levels_abs_idle:
        type: list
        description: levels_abs_idle (from the check's parameters).
    levels_perc_active:
        type: string
        description: levels_perc_active (from the check's parameters).
    levels_perc_idle:
        type: string
        description: levels_perc_idle (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
