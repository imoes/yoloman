name: netscaler_tcp_conns
fqcn: checkmk.netscaler_tcp_conns
collection: checkmk
short_description: TCP Connections
description: Checkmk check 'netscaler_tcp_conns' (service: TCP Connections), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    server_conns:
        type: string
        description: server_conns (from the check's parameters).
    client_conns:
        type: string
        description: client_conns (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
