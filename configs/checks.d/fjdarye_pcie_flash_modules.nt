name: fjdarye_pcie_flash_modules
fqcn: checkmk.fjdarye_pcie_flash_modules
collection: checkmk
short_description: PCIe flash module %s
description: Monitoring check 'fjdarye_pcie_flash_modules' (service: PCIe flash module %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    health_lifetime_perc:
        type: list
        description: health_lifetime_perc (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
