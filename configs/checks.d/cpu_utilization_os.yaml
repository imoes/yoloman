name: cpu_utilization_os
fqcn: checkmk.cpu_utilization_os
collection: checkmk
short_description: CPU utilization
description: 'Checkmk check ''cpu_utilization_os'' (service: CPU utilization), translated
  to a read-only on-host Starlark check module.'
options:
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
