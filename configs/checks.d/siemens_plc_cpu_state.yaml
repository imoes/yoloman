name: siemens_plc_cpu_state
fqcn: checkmk.siemens_plc_cpu_state
collection: checkmk
short_description: CPU state
description: 'Checkmk check ''siemens_plc_cpu_state'' (service: CPU state), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
