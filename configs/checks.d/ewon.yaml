name: ewon
fqcn: checkmk.ewon
collection: checkmk
short_description: '%s'
description: 'Monitoring check ''ewon'' (service: %s), translated to a read-only on-host
  Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  device:
    type: string
    description: device (from the check's parameters).
  scale:
    type: number
    default: '1.0'
    description: scale (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
  unit:
    type: string
    default: ''
    description: unit (from the check's parameters).
  perfvar:
    type: string
    default: ''
    description: perfvar (from the check's parameters).
  flags:
    type: string
    default: ''
    description: flags (from the check's parameters).
  flag_names:
    type: list
    description: flag_names (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
