name: acme_sbc_snmp
fqcn: checkmk.acme_sbc_snmp
collection: checkmk
short_description: ACME SBC health
description: 'Checkmk check ''acme_sbc_snmp'' (service: ACME SBC health), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  lower_levels:
    type: list
    description: lower_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
