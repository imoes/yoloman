name: extreme_vsp_switches_temperature
fqcn: checkmk.extreme_vsp_switches_temperature
collection: checkmk
short_description: VSP Switch %s Temperature
description: Checkmk check 'extreme_vsp_switches_temperature' (service: VSP Switch %s Temperature), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
