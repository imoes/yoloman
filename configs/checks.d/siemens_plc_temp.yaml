name: siemens_plc_temp
fqcn: checkmk.siemens_plc_temp
collection: checkmk
short_description: Temperature %s
description: 'Monitoring check ''siemens_plc_temp'' (service: Temperature %s), translated
  to a read-only on-host Starlark check module.'
options:
  warn:
    type: number
    default: '70.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '80.0'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
