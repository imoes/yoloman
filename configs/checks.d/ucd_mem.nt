name: ucd_mem
fqcn: checkmk.ucd_mem
collection: checkmk
short_description: Memory
description: Checkmk check 'ucd_mem' (service: Memory), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels_ram:
        type: string
        description: levels_ram (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_swap:
        type: string
        description: levels_swap (from the check's parameters).
    levels_virtual:
        type: string
        description: levels_virtual (from the check's parameters).
    swap_errors:
        type: number
        default: 0
        description: swap_errors (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
