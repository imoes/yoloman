name: pulse_secure_users
fqcn: checkmk.pulse_secure_users
collection: checkmk
short_description: Pulse Secure users
description: Checkmk check 'pulse_secure_users' (service: Pulse Secure users), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    upper_number_of_users:
        type: string
        description: upper_number_of_users (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
