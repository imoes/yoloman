name: couchbase_nodes_uptime
fqcn: checkmk.couchbase_nodes_uptime
collection: checkmk
short_description: Couchbase %s Uptime
description: 'Checkmk check ''couchbase_nodes_uptime'' (service: Couchbase %s Uptime),
  translated to a read-only on-host Starlark check module.'
options:
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
