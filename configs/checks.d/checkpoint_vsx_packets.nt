name: checkpoint_vsx_packets
fqcn: checkmk.checkpoint_vsx_packets
collection: checkmk
short_description: VS %s Packets
description: Monitoring check 'checkpoint_vsx_packets' (service: VS %s Packets), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    packets:
        type: string
        description: packets (from the check's parameters).
    packets_accepted:
        type: string
        description: packets_accepted (from the check's parameters).
    packets_dropped:
        type: string
        description: packets_dropped (from the check's parameters).
    packets_rejected:
        type: string
        description: packets_rejected (from the check's parameters).
    packets_logged:
        type: string
        description: packets_logged (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
