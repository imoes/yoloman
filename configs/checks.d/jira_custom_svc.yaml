name: jira_custom_svc
fqcn: checkmk.jira_custom_svc
collection: checkmk
short_description: Jira %s
description: 'Monitoring check ''jira_custom_svc'' (service: Jira %s), translated
  to a read-only on-host Starlark check module.'
options:
  jira_url:
    type: string
    default: ''
    description: jira_url (from the check's parameters).
  jira_user:
    type: string
    default: ''
    description: jira_user (from the check's parameters).
  jira_token:
    type: string
    default: ''
    description: jira_token (from the check's parameters).
  services:
    type: list
    description: services (from the check's parameters).
  custom_svc_count_upper:
    type: list
    description: custom_svc_count_upper (from the check's parameters).
  custom_svc_count_lower:
    type: list
    description: custom_svc_count_lower (from the check's parameters).
  custom_svc_sum_upper:
    type: list
    description: custom_svc_sum_upper (from the check's parameters).
  custom_svc_sum_lower:
    type: list
    description: custom_svc_sum_lower (from the check's parameters).
  custom_svc_avg_upper:
    type: list
    description: custom_svc_avg_upper (from the check's parameters).
  custom_svc_avg_lower:
    type: list
    description: custom_svc_avg_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
