name: ups_eaton_enviroment
fqcn: checkmk.ups_eaton_enviroment
collection: checkmk
short_description: Enviroment
description: Checkmk check 'ups_eaton_enviroment' (service: Enviroment), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    version:
        type: string
        default: 2c
        description: version (from the check's parameters).
    temp:
        type: string
        description: temp (from the check's parameters).
    remote_temp:
        type: string
        description: remote_temp (from the check's parameters).
    humidity:
        type: string
        description: humidity (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
