name: fortisandbox_mem_usage
fqcn: checkmk.fortisandbox_mem_usage
collection: checkmk
short_description: Memory
description: 'Checkmk check ''fortisandbox_mem_usage'' (service: Memory), translated
  to a read-only on-host Starlark check module.'
options:
  levels:
    type: list
    description: levels (from the check's parameters).
  average:
    type: string
    description: average (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
