name: atto_fibrebridge_fcport
fqcn: checkmk.atto_fibrebridge_fcport
collection: checkmk
short_description: FC Port %s
description: Monitoring check 'atto_fibrebridge_fcport' (service: FC Port %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        description: community (from the check's parameters).
    fc_tx_words:
        type: string
        description: fc_tx_words (from the check's parameters).
    fc_rx_words:
        type: string
        description: fc_rx_words (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
