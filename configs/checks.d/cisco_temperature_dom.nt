name: cisco_temperature_dom
fqcn: checkmk.cisco_temperature_dom
collection: checkmk
short_description: DOM %s
description: Checkmk check 'cisco_temperature_dom' (service: DOM %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    power_levels_upper:
        type: bool
        default: True
        description: power_levels_upper (from the check's parameters).
    power_levels_lower:
        type: bool
        default: True
        description: power_levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
