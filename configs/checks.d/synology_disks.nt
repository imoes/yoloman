name: synology_disks
fqcn: checkmk.synology_disks
collection: checkmk
short_description: Disks %s
description: Monitoring check 'synology_disks' (service: Disks %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
