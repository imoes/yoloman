name: oracle_processes
fqcn: checkmk.oracle_processes
collection: checkmk
short_description: ORA %s Processes
description: Checkmk check 'oracle_processes' (service: ORA %s Processes), translated to a read-only on-host Starlark check module.
options:
    warn:
        type: number
        default: 70.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
    oracle_home:
        type: string
        description: oracle_home (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
