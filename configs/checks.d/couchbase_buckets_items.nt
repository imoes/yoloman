name: couchbase_buckets_items
fqcn: checkmk.couchbase_buckets_items
collection: checkmk
short_description: Couchbase Bucket %s Items
description: Checkmk check 'couchbase_buckets_items' (service: Couchbase Bucket %s Items), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8091
        description: port (from the check's parameters).
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    ssl:
        type: bool
        default: False
        description: ssl (from the check's parameters).
    curr_items_tot:
        type: string
        description: curr_items_tot (from the check's parameters).
    disk_write_ql:
        type: string
        description: disk_write_ql (from the check's parameters).
    fetched_items:
        type: string
        description: fetched_items (from the check's parameters).
    disk_fill_rate:
        type: string
        description: disk_fill_rate (from the check's parameters).
    disk_drain_rate:
        type: string
        description: disk_drain_rate (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
