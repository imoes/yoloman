name: netstat
fqcn: checkmk.netstat
collection: checkmk
short_description: TCP Connection %s
description: Checkmk check 'netstat' (service: TCP Connection %s), translated to a read-only on-host Starlark check module.
options:
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
    proto:
        type: string
        description: proto (from the check's parameters).
    state:
        type: string
        description: state (from the check's parameters).
    local_ip:
        type: string
        description: local_ip (from the check's parameters).
    local_port:
        type: string
        description: local_port (from the check's parameters).
    remote_ip:
        type: string
        description: remote_ip (from the check's parameters).
    remote_port:
        type: string
        description: remote_port (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
