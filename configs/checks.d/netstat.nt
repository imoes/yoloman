name: netstat
fqcn: checkmk.netstat
collection: checkmk
short_description: TCP Connection %s
description: Checkmk check 'netstat' (service: TCP Connection %s), translated to a read-only on-host Starlark check module.
options:
    max_states:
        type: string
        description: max_states (from the check's parameters).
    min_states:
        type: string
        description: min_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
