name: couchbase_nodes_stats_mem
fqcn: checkmk.couchbase_nodes_stats_mem
collection: checkmk
short_description: Couchbase %s Memory
description: Checkmk check 'couchbase_nodes_stats_mem' (service: Couchbase %s Memory), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    api_user:
        type: string
        default: admin
        description: api_user (from the check's parameters).
    api_password:
        type: string
        default:
        description: api_password (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
