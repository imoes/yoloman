name: f5_bigip_vcmpfailover
fqcn: checkmk.f5_bigip_vcmpfailover
collection: checkmk
short_description: F5-BIGIP-Cluster-Status SNMP Sections and Checks
description: Monitoring check 'f5_bigip_vcmpfailover' (service: BIG-IP vCMP Guest Failover Status), translated to a read-only on-host Starlark check module.
options:
    type:
        type: string
        description: type (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
