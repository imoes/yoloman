name: cisco_vpn_tunnel
fqcn: checkmk.cisco_vpn_tunnel
collection: checkmk
short_description: VPN Tunnel %s
description: Checkmk check 'cisco_vpn_tunnel' (service: VPN Tunnel %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    state:
        type: number
        default: 2
        description: state (from the check's parameters).
    tunnels:
        type: list
        description: tunnels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
