name: cisco_vpn_tunnel
fqcn: checkmk.cisco_vpn_tunnel
collection: checkmk
short_description: VPN Tunnel %s
description: Monitoring check 'cisco_vpn_tunnel' (service: VPN Tunnel %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
