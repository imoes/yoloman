name: fireeye_bypass
fqcn: checkmk.fireeye_bypass
collection: checkmk
short_description: Bypass Mail Rate
description: 'Checkmk check ''fireeye_bypass'' (service: Bypass Mail Rate), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  value:
    type: number
    default: '0'
    description: value (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
