name: scaleio_mdm
fqcn: checkmk.scaleio_mdm
collection: checkmk
short_description: ScaleIO cluster status
description: Monitoring check 'scaleio_mdm' (service: ScaleIO cluster status), translated to a read-only on-host Starlark check module.
options:
    scli_path:
        type: string
        default: scli
        description: scli_path (from the check's parameters).
    mdm_ip:
        type: string
        default:
        description: mdm_ip (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
