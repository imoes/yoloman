name: mcafee_webgateway_https_client_requests
fqcn: checkmk.mcafee_webgateway_https_client_requests
collection: checkmk
short_description: The McAfee Web Gateway has been rebranded to Skyhigh Secure Web Gateway with its release 12.2.2.
description: Checkmk check 'mcafee_webgateway_https_client_requests' (service: HTTPS Client Request Rate), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 500
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 1000
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
