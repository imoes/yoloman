name: liebert_fans_condenser
fqcn: checkmk.liebert_fans_condenser
collection: checkmk
short_description: %s
description: Monitoring check 'liebert_fans_condenser' (service: %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_lower:
        type: string
        description: levels_lower (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
