name: couchbase_buckets_operations
fqcn: checkmk.couchbase_buckets_operations
collection: checkmk
short_description: Couchbase Bucket %s Operations
description: Monitoring check 'couchbase_buckets_operations' (service: Couchbase Bucket %s Operations), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    url:
        type: string
        default: http://localhost:8091/pools/default/buckets
        description: url (from the check's parameters).
    ops_levels:
        type: string
        description: ops_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
