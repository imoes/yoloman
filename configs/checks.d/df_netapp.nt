name: df_netapp
fqcn: checkmk.df_netapp
collection: checkmk
short_description: Filesystem %s
description: Monitoring check 'df_netapp' (service: Filesystem %s), translated to a read-only on-host Starlark check module.
options:
    groups:
        type: list
        description: groups (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
