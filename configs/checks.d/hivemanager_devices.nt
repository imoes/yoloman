name: hivemanager_devices
fqcn: checkmk.hivemanager_devices
collection: checkmk
short_description: Client %s
description: Checkmk check 'hivemanager_devices' (service: Client %s), translated to a read-only on-host Starlark check module.
options:
    warn_states:
        type: list
        description: warn_states (from the check's parameters).
    crit_states:
        type: list
        description: crit_states (from the check's parameters).
    alert_on_loss:
        type: bool
        default: True
        description: alert_on_loss (from the check's parameters).
    max_clients:
        type: list
        description: max_clients (from the check's parameters).
    max_uptime:
        type: string
        description: max_uptime (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
