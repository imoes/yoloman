name: safenet_ntls_links
fqcn: checkmk.safenet_ntls_links
collection: checkmk
short_description: NTLS Links
description: Monitoring check 'safenet_ntls_links' (service: NTLS Links), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
