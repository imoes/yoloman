name: bluecoat_sensors_temp
fqcn: checkmk.bluecoat_sensors_temp
collection: checkmk
short_description: Temperature %s
description: Monitoring check 'bluecoat_sensors_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    device_levels_handling:
        type: string
        default: devdefault
        description: device_levels_handling (from the check's parameters).
    warn:
        type: list
        description: warn (from the check's parameters).
    crit:
        type: list
        description: crit (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
