name: couchbase_nodes_operations
fqcn: checkmk.couchbase_nodes_operations
collection: checkmk
short_description: Couchbase %s Operations
description: Checkmk check 'couchbase_nodes_operations' (service: Couchbase %s Operations), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: string
        default: 8091
        description: port (from the check's parameters).
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    password:
        type: string
        default: password
        description: password (from the check's parameters).
    protocol:
        type: string
        default: http
        description: protocol (from the check's parameters).
    ops:
        type: string
        description: ops (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
