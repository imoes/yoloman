name: ups_cps_inphase
fqcn: checkmk.ups_cps_inphase
collection: checkmk
short_description: UPS Input Phase %s
description: 'Checkmk check ''ups_cps_inphase'' (service: UPS Input Phase %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
