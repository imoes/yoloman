name: hp_mcs_system
fqcn: checkmk.hp_mcs_system
collection: checkmk
short_description: %s
description: Monitoring check 'hp_mcs_system' (service: %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
