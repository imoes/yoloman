name: traceroute
fqcn: checkmk.traceroute
collection: checkmk
short_description: ''
description: Checkmk active check 'traceroute', translated to a read-only on-host
  Starlark check module.
options:
  dns:
    type: string
    description: dns (from the check's parameters).
  method:
    type: string
    description: method (from the check's parameters).
  address_family:
    type: string
    description: address_family (from the check's parameters).
  routers:
    type: string
    description: routers (from the check's parameters).
  host:
    type: string
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
