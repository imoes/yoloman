name: pulse_secure_mem_util
fqcn: checkmk.pulse_secure_mem_util
collection: checkmk
short_description: Pulse Secure IVE memory utilization
description: Monitoring check 'pulse_secure_mem_util' (service: Pulse Secure IVE memory utilization), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    mem_used_percent:
        type: list
        description: mem_used_percent (from the check's parameters).
    swap_used_percent:
        type: list
        description: swap_used_percent (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
