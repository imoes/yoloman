name: mem_linux
fqcn: checkmk.mem_linux
collection: checkmk
short_description: Memory
description: Checkmk check 'mem_linux' (service: Memory), translated to a read-only on-host Starlark check module.
options:
    levels_virtual:
        type: list
        description: levels_virtual (from the check's parameters).
    levels_shm:
        type: list
        description: levels_shm (from the check's parameters).
    levels_pagetables:
        type: list
        description: levels_pagetables (from the check's parameters).
    levels_committed:
        type: list
        description: levels_committed (from the check's parameters).
    levels_commitlimit:
        type: list
        description: levels_commitlimit (from the check's parameters).
    levels_vmalloc:
        type: list
        description: levels_vmalloc (from the check's parameters).
    levels_hardwarecorrupted:
        type: list
        description: levels_hardwarecorrupted (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
