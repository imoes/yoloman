name: enterasys_temp
fqcn: checkmk.enterasys_temp
collection: checkmk
short_description: Temperature %s
description: Checkmk check 'enterasys_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    warn:
        type: number
        default: 30.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 35.0
        description: crit (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
