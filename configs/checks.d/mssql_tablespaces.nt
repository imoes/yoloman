name: mssql_tablespaces
fqcn: checkmk.mssql_tablespaces
collection: checkmk
short_description: MSSQL %s Sizes
description: Checkmk check 'mssql_tablespaces' (service: MSSQL %s Sizes), translated to a read-only on-host Starlark check module.
options:
    size:
        type: string
        description: size (from the check's parameters).
    unallocated:
        type: string
        description: unallocated (from the check's parameters).
    reserved:
        type: string
        description: reserved (from the check's parameters).
    data:
        type: string
        description: data (from the check's parameters).
    indexes:
        type: string
        description: indexes (from the check's parameters).
    unused:
        type: string
        description: unused (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
