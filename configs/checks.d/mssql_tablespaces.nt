name: mssql_tablespaces
fqcn: checkmk.mssql_tablespaces
collection: checkmk
short_description: MSSQL %s Sizes
description: Monitoring check 'mssql_tablespaces' (service: MSSQL %s Sizes), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 1433
        description: port (from the check's parameters).
    user:
        type: string
        default: sa
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    instance:
        type: string
        default: MSSQLSERVER
        description: instance (from the check's parameters).
    size_warn:
        type: string
        description: size_warn (from the check's parameters).
    size_crit:
        type: string
        description: size_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
