name: stormshield_cluster_node
fqcn: checkmk.stormshield_cluster_node
collection: checkmk
short_description: HA Member %s
description: 'Checkmk check ''stormshield_cluster_node'' (service: HA Member %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  quality:
    type: string
    description: quality (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
