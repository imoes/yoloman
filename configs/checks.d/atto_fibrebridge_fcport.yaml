name: atto_fibrebridge_fcport
fqcn: checkmk.atto_fibrebridge_fcport
collection: checkmk
short_description: FC Port %s
description: 'Checkmk check ''atto_fibrebridge_fcport'' (service: FC Port %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  fc_tx_words_warn:
    type: string
    description: fc_tx_words_warn (from the check's parameters).
  fc_tx_words_crit:
    type: string
    description: fc_tx_words_crit (from the check's parameters).
  fc_rx_words_warn:
    type: string
    description: fc_rx_words_warn (from the check's parameters).
  fc_rx_words_crit:
    type: string
    description: fc_rx_words_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
