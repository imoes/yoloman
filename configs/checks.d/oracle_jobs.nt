name: oracle_jobs
fqcn: checkmk.oracle_jobs
collection: checkmk
short_description: ORA %s Job
description: Monitoring check 'oracle_jobs' (service: ORA %s Job), translated to a read-only on-host Starlark check module.
options:
    status_missing_jobs:
        type: string
        description: status_missing_jobs (from the check's parameters).
    consider_job_status:
        type: string
        default: ignore
        description: consider_job_status (from the check's parameters).
    missinglog:
        type: number
        default: 1
        description: missinglog (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
