name: hp_msa_psu_sensor
fqcn: checkmk.hp_msa_psu_sensor
collection: checkmk
short_description: Power Supply Voltage %s
description: 'Checkmk check ''hp_msa_psu_sensor'' (service: Power Supply Voltage %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels_12v_lower:
    type: list
    description: levels_12v_lower (from the check's parameters).
  levels_12v_upper:
    type: list
    description: levels_12v_upper (from the check's parameters).
  levels_5v_lower:
    type: list
    description: levels_5v_lower (from the check's parameters).
  levels_5v_upper:
    type: list
    description: levels_5v_upper (from the check's parameters).
  levels_33v_lower:
    type: list
    description: levels_33v_lower (from the check's parameters).
  levels_33v_upper:
    type: list
    description: levels_33v_upper (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
