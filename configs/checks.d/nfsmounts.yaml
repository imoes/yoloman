name: nfsmounts
fqcn: checkmk.nfsmounts
collection: checkmk
short_description: NFS mount %s
description: 'Checkmk check ''nfsmounts'' (service: NFS mount %s), translated to a
  read-only on-host Starlark check module.'
options:
  warn:
    type: number
    default: '80'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
