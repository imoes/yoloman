name: aruba_psu_status
fqcn: checkmk.aruba_psu_status
collection: checkmk
short_description: PSU Status %s
description: Checkmk check 'aruba_psu_status' (service: PSU Status %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    temperature_levels:
        type: string
        description: temperature_levels (from the check's parameters).
    levels_abs_upper:
        type: string
        description: levels_abs_upper (from the check's parameters).
    temperature:
        type: string
        description: temperature (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    wattage:
        type: string
        description: wattage (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
