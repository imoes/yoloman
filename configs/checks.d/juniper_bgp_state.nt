name: juniper_bgp_state
fqcn: checkmk.juniper_bgp_state
collection: checkmk
short_description: BGP Status Peer %s
description: Checkmk check 'juniper_bgp_state' (service: BGP Status Peer %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
