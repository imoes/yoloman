name: fireeye_bypass
fqcn: checkmk.fireeye_bypass
collection: checkmk
short_description: Bypass Mail Rate
description: Checkmk check 'fireeye_bypass' (service: Bypass Mail Rate), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    value:
        type: string
        description: value (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
