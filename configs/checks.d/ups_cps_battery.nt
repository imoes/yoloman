name: ups_cps_battery
fqcn: checkmk.ups_cps_battery
collection: checkmk
short_description: UPS Battery
description: Checkmk check 'ups_cps_battery' (service: UPS Battery), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    version:
        type: string
        default: 2c
        description: version (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    capacity:
        type: list
        description: capacity (from the check's parameters).
    battime:
        type: list
        description: battime (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
