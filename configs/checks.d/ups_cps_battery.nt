name: ups_cps_battery
fqcn: checkmk.ups_cps_battery
collection: checkmk
short_description: UPS Battery
description: Monitoring check 'ups_cps_battery' (service: UPS Battery), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels_upper:
        type: list
        description: levels_upper (from the check's parameters).
    capacity:
        type: list
        description: capacity (from the check's parameters).
    battime:
        type: list
        description: battime (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
