name: f5_bigip_pool
fqcn: checkmk.f5_bigip_pool
collection: checkmk
short_description: Load Balancing Pool %s
description: Checkmk check 'f5_bigip_pool' (service: Load Balancing Pool %s), translated to a read-only on-host Starlark check module.
options:
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
