name: pulse_secure_disk_util
fqcn: checkmk.pulse_secure_disk_util
collection: checkmk
short_description: Pulse Secure disk utilization
description: Checkmk check 'pulse_secure_disk_util' (service: Pulse Secure disk utilization), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    version:
        type: string
        default: 2c
        description: version (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
    upper_levels:
        type: string
        description: upper_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
