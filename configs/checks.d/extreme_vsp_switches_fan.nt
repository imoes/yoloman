name: extreme_vsp_switches_fan
fqcn: checkmk.extreme_vsp_switches_fan
collection: checkmk
short_description: VSP Switch Fan %s
description: Monitoring check 'extreme_vsp_switches_fan' (service: VSP Switch Fan %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    params:
        type: string
        description: params (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
