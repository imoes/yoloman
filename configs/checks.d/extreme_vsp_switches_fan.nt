name: extreme_vsp_switches_fan
fqcn: checkmk.extreme_vsp_switches_fan
collection: checkmk
short_description: VSP Switch Fan %s
description: Checkmk check 'extreme_vsp_switches_fan' (service: VSP Switch Fan %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    lower:
        type: list
        description: lower (from the check's parameters).
    upper:
        type: list
        description: upper (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
