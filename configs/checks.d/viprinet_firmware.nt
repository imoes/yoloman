name: viprinet_firmware
fqcn: checkmk.viprinet_firmware
collection: checkmk
short_description: Firmware Version
description: Checkmk check 'viprinet_firmware' (service: Firmware Version), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
