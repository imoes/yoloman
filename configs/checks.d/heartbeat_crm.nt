name: heartbeat_crm
fqcn: checkmk.heartbeat_crm
collection: checkmk
short_description: Heartbeat CRM General
description: Monitoring check 'heartbeat_crm' (service: Heartbeat CRM General), translated to a read-only on-host Starlark check module.
options:
    max_age:
        type: number
        default: 60
        description: max_age (from the check's parameters).
    dc:
        type: string
        description: dc (from the check's parameters).
    num_nodes:
        type: string
        description: num_nodes (from the check's parameters).
    num_resources:
        type: string
        description: num_resources (from the check's parameters).
    show_failed_actions:
        type: bool
        default: False
        description: show_failed_actions (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
