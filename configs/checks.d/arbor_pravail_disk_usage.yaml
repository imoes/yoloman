name: arbor_pravail_disk_usage
fqcn: checkmk.arbor_pravail_disk_usage
collection: checkmk
short_description: Disk Usage %s
description: 'Checkmk check ''arbor_pravail_disk_usage'' (service: Disk Usage %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
  base_oid:
    type: string
    default: .1.3.6.1.4.1.9694.1.6.2
    description: base_oid (from the check's parameters).
  col_oid:
    type: string
    default: '6.0'
    description: col_oid (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
