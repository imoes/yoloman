name: 3par_cpgs_usage
fqcn: checkmk.3par_cpgs_usage
collection: checkmk
short_description: CPG %s
description: Monitoring check '3par_cpgs_usage' (service: CPG %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
