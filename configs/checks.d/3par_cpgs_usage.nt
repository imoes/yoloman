name: 3par_cpgs_usage
fqcn: checkmk.3par_cpgs_usage
collection: checkmk
short_description: CPG %s
description: Checkmk check '3par_cpgs_usage' (service: CPG %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
    token:
        type: string
        description: token (from the check's parameters).
    username:
        type: string
        description: username (from the check's parameters).
    password:
        type: string
        description: password (from the check's parameters).
    path:
        type: string
        default: /api/v1/cpgs
        description: path (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
