name: citrix_controller_sessions
fqcn: checkmk.citrix_controller_sessions
collection: checkmk
short_description: Citrix Total Sessions
description: Checkmk check 'citrix_controller_sessions' (service: Citrix Total Sessions), translated to a read-only on-host Starlark check module.
options:
    total:
        type: string
        description: total (from the check's parameters).
    total_crit:
        type: string
        description: total_crit (from the check's parameters).
    active:
        type: string
        description: active (from the check's parameters).
    active_crit:
        type: string
        description: active_crit (from the check's parameters).
    inactive:
        type: string
        description: inactive (from the check's parameters).
    inactive_crit:
        type: string
        description: inactive_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
