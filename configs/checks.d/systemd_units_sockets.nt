name: systemd_units_sockets
fqcn: checkmk.systemd_units_sockets
collection: checkmk
short_description: Systemd Socket %s
description: Checkmk check 'systemd_units_sockets' (service: Systemd Socket %s), translated to a read-only on-host Starlark check module.
options:
    states:
        type: string
        description: states (from the check's parameters).
    states_default:
        type: number
        default: 2
        description: states_default (from the check's parameters).
    else:
        type: number
        default: 2
        description: else (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
