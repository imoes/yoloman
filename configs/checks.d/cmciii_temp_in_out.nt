name: cmciii_temp_in_out
fqcn: checkmk.cmciii_temp_in_out
collection: checkmk
short_description: Temperature %s
description: Monitoring check 'cmciii_temp_in_out' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    use_sensor_description:
        type: bool
        default: False
        description: use_sensor_description (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
