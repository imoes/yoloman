name: juniper_trpz_cpu_util
fqcn: checkmk.juniper_trpz_cpu_util
collection: checkmk
short_description: CPU utilization
description: 'Checkmk check ''juniper_trpz_cpu_util'' (service: CPU utilization),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  util:
    type: list
    description: util (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
