name: postgres_stat_database
fqcn: checkmk.postgres_stat_database
collection: checkmk
short_description: PostgreSQL DB %s Statistics
description: Checkmk check 'postgres_stat_database' (service: PostgreSQL DB %s Statistics), translated to a read-only on-host Starlark check module.
options:
    username:
        type: string
        default: postgres
        description: username (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
