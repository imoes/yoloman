name: storeonce4x_appliances
fqcn: checkmk.storeonce4x_appliances
collection: checkmk
short_description: Appliance %s Status
description: Checkmk check 'storeonce4x_appliances' (service: Appliance %s Status), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
