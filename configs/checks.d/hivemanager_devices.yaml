name: hivemanager_devices
fqcn: checkmk.hivemanager_devices
collection: checkmk
short_description: Client %s
description: 'Checkmk check ''hivemanager_devices'' (service: Client %s), translated
  to a read-only on-host Starlark check module.'
options:
  max_uptime:
    type: string
    description: max_uptime (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
