name: infoblox_services
fqcn: checkmk.infoblox_services
collection: checkmk
short_description: Infoblox services and node services
description: 'Monitoring check ''infoblox_services'' (service: Service %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
