name: icom_repeater_ps_volt
fqcn: checkmk.icom_repeater_ps_volt
collection: checkmk
short_description: Power Supply Voltage
description: 'Checkmk check ''icom_repeater_ps_volt'' (service: Power Supply Voltage),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels_lower:
    type: list
    description: levels_lower (from the check's parameters).
  levels_upper:
    type: list
    description: levels_upper (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
