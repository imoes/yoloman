name: mssql_backup
fqcn: checkmk.mssql_backup
collection: checkmk
short_description: MSSQL %s Backup
description: 'Checkmk check ''mssql_backup'' (service: MSSQL %s Backup), translated
  to a read-only on-host Starlark check module.'
options:
  user:
    type: string
    description: user (from the check's parameters).
  password:
    type: string
    description: password (from the check's parameters).
  not_found:
    type: number
    default: '1'
    description: not_found (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
