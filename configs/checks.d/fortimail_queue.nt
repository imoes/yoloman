name: fortimail_queue
fqcn: checkmk.fortimail_queue
collection: checkmk
short_description: FortiMail %s
description: Checkmk check 'fortimail_queue' (service: FortiMail %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    queue_length:
        type: list
        description: queue_length (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
