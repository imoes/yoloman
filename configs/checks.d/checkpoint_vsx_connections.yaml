name: checkpoint_vsx_connections
fqcn: checkmk.checkpoint_vsx_connections
collection: checkmk
short_description: VS %s Connections
description: 'Monitoring check ''checkpoint_vsx_connections'' (service: VS %s Connections),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels_perc:
    type: list
    description: levels_perc (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
