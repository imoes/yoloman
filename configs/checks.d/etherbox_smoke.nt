name: etherbox_smoke
fqcn: checkmk.etherbox_smoke
collection: checkmk
short_description: Sensor %s
description: Monitoring check 'etherbox_smoke' (service: Sensor %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    smoke_handling:
        type: list
        description: smoke_handling (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
