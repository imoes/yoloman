name: domino_mailqueues
fqcn: checkmk.domino_mailqueues
collection: checkmk
short_description: Domino Queue %s
description: Checkmk check 'domino_mailqueues' (service: Domino Queue %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    queue_length:
        type: string
        description: queue_length (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
