name: ldap
fqcn: checkmk.ldap
collection: checkmk
short_description: ''
description: Checkmk active check 'ldap', translated to a read-only on-host Starlark
  check module.
options:
  hostname:
    type: string
    description: hostname (from the check's parameters).
  base_dn:
    type: string
    default: ''
    description: base_dn (from the check's parameters).
  bind_dn:
    type: string
    description: bind_dn (from the check's parameters).
  password:
    type: string
    description: password (from the check's parameters).
  port:
    type: string
    description: port (from the check's parameters).
  version:
    type: string
    description: version (from the check's parameters).
  ssl:
    type: bool
    default: 'False'
    description: ssl (from the check's parameters).
  timeout:
    type: string
    description: timeout (from the check's parameters).
  response_time:
    type: string
    description: response_time (from the check's parameters).
  name:
    type: string
    default: ''
    description: name (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
