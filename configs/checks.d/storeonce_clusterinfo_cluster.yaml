name: storeonce_clusterinfo_cluster
fqcn: checkmk.storeonce_clusterinfo_cluster
collection: checkmk
short_description: Appliance Status
description: 'Checkmk check ''storeonce_clusterinfo_cluster'' (service: Appliance
  Status), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  port:
    type: number
    default: '161'
    description: port (from the check's parameters).
  warn:
    type: number
    default: '1'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '2'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
