name: apc_symmetra
fqcn: checkmk.apc_symmetra
collection: checkmk
short_description: APC Symmetra status
description: Checkmk check 'apc_symmetra' (service: APC Symmetra status), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    snmp_version:
        type: string
        default: 2c
        description: snmp_version (from the check's parameters).
    battery_replace_state:
        type: number
        default: 1
        description: battery_replace_state (from the check's parameters).
    calibration_state:
        type: number
        default: 0
        description: calibration_state (from the check's parameters).
    capacity_warn:
        type: number
        default: 95.0
        description: capacity_warn (from the check's parameters).
    capacity_crit:
        type: number
        default: 80.0
        description: capacity_crit (from the check's parameters).
    battime_warn:
        type: number
        default: 0.0
        description: battime_warn (from the check's parameters).
    battime_crit:
        type: number
        default: 0.0
        description: battime_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
