name: 3par_ports
fqcn: checkmk.3par_ports
collection: checkmk
short_description: Port %s
description: Monitoring check '3par_ports' (service: Port %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    api_port:
        type: string
        default: 8080
        description: api_port (from the check's parameters).
    username:
        type: string
        default: 3paradm
        description: username (from the check's parameters).
    password:
        type: string
        default: 3pardata
        description: password (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
