name: couchbase_nodes_services
fqcn: checkmk.couchbase_nodes_services
collection: checkmk
short_description: Couchbase %s Services
description: Checkmk check 'couchbase_nodes_services' (service: Couchbase %s Services), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8091
        description: port (from the check's parameters).
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    discovered_services:
        type: list
        description: discovered_services (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
