name: watchdog_sensors_temp
fqcn: checkmk.watchdog_sensors_temp
collection: checkmk
short_description: %s 
description: Checkmk check 'watchdog_sensors_temp' (service: %s ), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: number
        default: 30
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 35
        description: crit (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    version:
        type: number
        default: 2
        description: version (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
