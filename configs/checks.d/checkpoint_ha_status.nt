name: checkpoint_ha_status
fqcn: checkmk.checkpoint_ha_status
collection: checkmk
short_description: HA Status
description: Checkmk check 'checkpoint_ha_status' (service: HA Status), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
