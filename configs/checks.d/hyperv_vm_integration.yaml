name: hyperv_vm_integration
fqcn: checkmk.hyperv_vm_integration
collection: checkmk
short_description: Hyper-V VM integration services
description: 'Checkmk check ''hyperv_vm_integration'' (service: Hyper-V VM integration
  services), translated to a read-only on-host Starlark check module.'
options:
  default_status:
    type: string
    description: default_status (from the check's parameters).
  state_if_not_default:
    type: string
    description: state_if_not_default (from the check's parameters).
  match_services:
    type: list
    description: match_services (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
