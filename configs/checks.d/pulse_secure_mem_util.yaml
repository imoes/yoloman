name: pulse_secure_mem_util
fqcn: checkmk.pulse_secure_mem_util
collection: checkmk
short_description: Pulse Secure IVE memory utilization
description: 'Checkmk check ''pulse_secure_mem_util'' (service: Pulse Secure IVE memory
  utilization), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  params:
    type: string
    description: params (from the check's parameters).
  mem_used_percent:
    type: list
    description: mem_used_percent (from the check's parameters).
  swap_used_percent:
    type: list
    description: swap_used_percent (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
