name: uptime
fqcn: checkmk.uptime
collection: checkmk
short_description: Uptime
description: 'Checkmk check ''uptime'' (service: Uptime), translated to a read-only
  on-host Starlark check module.'
options:
  warn:
    type: number
    default: '8760'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '0'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
