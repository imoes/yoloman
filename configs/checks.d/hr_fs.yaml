name: hr_fs
fqcn: checkmk.hr_fs
collection: checkmk
short_description: Filesystem %s
description: 'Checkmk check ''hr_fs'' (service: Filesystem %s), translated to a read-only
  on-host Starlark check module.'
options:
  usage_on_disk:
    type: list
    description: usage_on_disk (from the check's parameters).
  num_inodes:
    type: string
    description: num_inodes (from the check's parameters).
  size:
    type: string
    description: size (from the check's parameters).
  growth:
    type: string
    description: growth (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
