name: ups_cps_outphase
fqcn: checkmk.ups_cps_outphase
collection: checkmk
short_description: UPS Output Phase %s
description: Checkmk check 'ups_cps_outphase' (service: UPS Output Phase %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels_upper_voltage:
        type: list
        description: levels_upper_voltage (from the check's parameters).
    levels_upper_current:
        type: list
        description: levels_upper_current (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
