name: tsm_storagepools
fqcn: checkmk.tsm_storagepools
collection: checkmk
short_description: TSM Storagepool %s
description: 'Checkmk check ''tsm_storagepools'' (service: TSM Storagepool %s), translated
  to a read-only on-host Starlark check module.'
options:
  _section:
    type: string
    description: _section (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
