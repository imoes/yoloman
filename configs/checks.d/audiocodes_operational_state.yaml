name: audiocodes_operational_state
fqcn: checkmk.audiocodes_operational_state
collection: checkmk
short_description: Operational state module %s
description: 'Checkmk check ''audiocodes_operational_state'' (service: Operational
  state module %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
