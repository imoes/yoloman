name: checkpoint_ha_problems
fqcn: checkmk.checkpoint_ha_problems
collection: checkmk
short_description: HA Problem %s
description: Checkmk check 'checkpoint_ha_problems' (service: HA Problem %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
