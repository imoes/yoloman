name: rmon_stats
fqcn: checkmk.rmon_stats
collection: checkmk
short_description: RMON Stats IF %s
description: Monitoring check 'rmon_stats' (service: RMON Stats IF %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
