name: emc_isilon_cpu
fqcn: checkmk.emc_isilon_cpu
collection: checkmk
short_description: Node CPU utilization
description: Checkmk check 'emc_isilon_cpu' (service: Node CPU utilization), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    util:
        type: list
        description: util (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
