name: zypper
fqcn: checkmk.zypper
collection: checkmk
short_description: Zypper Updates
description: 'Checkmk check ''zypper'' (service: Zypper Updates), translated to a
  read-only on-host Starlark check module.'
options:
  security:
    type: string
    description: security (from the check's parameters).
  recommended:
    type: string
    description: recommended (from the check's parameters).
  other:
    type: string
    description: other (from the check's parameters).
  locks:
    type: string
    description: locks (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
