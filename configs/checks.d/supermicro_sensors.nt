name: supermicro_sensors
fqcn: checkmk.supermicro_sensors
collection: checkmk
short_description: Sensor %s
description: Checkmk check 'supermicro_sensors' (service: Sensor %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    upper_levels:
        type: string
        description: upper_levels (from the check's parameters).
    lower_levels:
        type: string
        description: lower_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
