name: emka_modules_sensor_temp
fqcn: checkmk.emka_modules_sensor_temp
collection: checkmk
short_description: Temperature %s
description: 'Monitoring check ''emka_modules_sensor_temp'' (service: Temperature
  %s), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  levels_lower:
    type: string
    description: levels_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
