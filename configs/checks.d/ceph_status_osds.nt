name: ceph_status_osds
fqcn: checkmk.ceph_status_osds
collection: checkmk
short_description: Ceph OSDs
description: Checkmk check 'ceph_status_osds' (service: Ceph OSDs), translated to a read-only on-host Starlark check module.
options:
    epoch:
        type: list
        description: epoch (from the check's parameters).
    num_out_osds:
        type: list
        description: num_out_osds (from the check's parameters).
    num_down_osds:
        type: list
        description: num_down_osds (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
