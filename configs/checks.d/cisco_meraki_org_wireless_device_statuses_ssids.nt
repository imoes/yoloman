name: cisco_meraki_org_wireless_device_statuses_ssids
fqcn: checkmk.cisco_meraki_org_wireless_device_statuses_ssids
collection: checkmk
short_description: SSID %s
description: Checkmk check 'cisco_meraki_org_wireless_device_statuses_ssids' (service: SSID %s), translated to a read-only on-host Starlark check module.
options:
    token:
        type: string
        default:
        description: token (from the check's parameters).
    org_id:
        type: string
        default:
        description: org_id (from the check's parameters).
    host:
        type: string
        default: api.meraki.com
        description: host (from the check's parameters).
    net_id:
        type: string
        default:
        description: net_id (from the check's parameters).
    state_if_not_enabled:
        type: number
        default: 1
        description: state_if_not_enabled (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
