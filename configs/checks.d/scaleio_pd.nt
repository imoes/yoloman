name: scaleio_pd
fqcn: checkmk.scaleio_pd
collection: checkmk
short_description: ScaleIO PD capacity %s
description: Monitoring check 'scaleio_pd' (service: ScaleIO PD capacity %s), translated to a read-only on-host Starlark check module.
options:
    mdm_ip:
        type: string
        default:
        description: mdm_ip (from the check's parameters).
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
