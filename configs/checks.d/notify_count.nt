name: notify_count
fqcn: checkmk.notify_count
collection: checkmk
short_description:
description: Active check 'notify_count', translated to a read-only on-host Starlark check module.
options:
    warn:
        type: number
        default: 10
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 20
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
