name: disk_io_utilization
fqcn: checkmk.disk_io_utilization
collection: checkmk
short_description: Disk IO Utilization
description: Monitoring check 'disk_io_utilization' (service: Disk IO Utilization), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    upper_levels:
        type: list
        description: upper_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
