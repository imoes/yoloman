name: timesyncd
fqcn: checkmk.timesyncd
collection: checkmk
short_description: Systemd Timesyncd Time
description: 'Checkmk check ''timesyncd'' (service: Systemd Timesyncd Time), translated
  to a read-only on-host Starlark check module.'
options:
  quality_levels:
    type: string
    description: quality_levels (from the check's parameters).
  alert_delay:
    type: string
    description: alert_delay (from the check's parameters).
  last_ntp_message:
    type: string
    description: last_ntp_message (from the check's parameters).
  stratum_level:
    type: string
    description: stratum_level (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
