name: ra32e_sensors_humidity
fqcn: checkmk.ra32e_sensors_humidity
collection: checkmk
short_description: Humidity %s
description: 'Checkmk check ''ra32e_sensors_humidity'' (service: Humidity %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
