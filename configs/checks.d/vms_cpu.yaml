name: vms_cpu
fqcn: checkmk.vms_cpu
collection: checkmk
short_description: CPU utilization
description: 'Monitoring check ''vms_cpu'' (service: CPU utilization), translated
  to a read-only on-host Starlark check module.'
options:
  iowait:
    type: string
    description: iowait (from the check's parameters).
  user:
    type: string
    description: user (from the check's parameters).
  system:
    type: string
    description: system (from the check's parameters).
  util:
    type: string
    description: util (from the check's parameters).
  crit_util:
    type: number
    default: '90'
    description: crit_util (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
