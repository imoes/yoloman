name: printer_input
fqcn: checkmk.printer_input
collection: checkmk
short_description: Input %s
description: Monitoring check 'printer_input' (service: Input %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    capacity_levels:
        type: list
        description: capacity_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
