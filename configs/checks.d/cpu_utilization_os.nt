name: cpu_utilization_os
fqcn: checkmk.cpu_utilization_os
collection: checkmk
short_description: CPU utilization
description: Monitoring check 'cpu_utilization_os' (service: CPU utilization), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
