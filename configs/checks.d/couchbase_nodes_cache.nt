name: couchbase_nodes_cache
fqcn: checkmk.couchbase_nodes_cache
collection: checkmk
short_description: Couchbase %s Cache
description: Monitoring check 'couchbase_nodes_cache' (service: Couchbase %s Cache), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    url:
        type: string
        default: http://localhost:8091/pools/default/buckets/default/nodes
        description: url (from the check's parameters).
    cache_misses:
        type: list
        description: cache_misses (from the check's parameters).
    cache_hits:
        type: list
        description: cache_hits (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
