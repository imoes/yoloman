name: ups_cps_battery_temp
fqcn: checkmk.ups_cps_battery_temp
collection: checkmk
short_description: Temperature %s
description: Monitoring check 'ups_cps_battery_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels_upper:
        type: list
        description: levels_upper (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
