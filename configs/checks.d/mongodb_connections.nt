name: mongodb_connections
fqcn: checkmk.mongodb_connections
collection: checkmk
short_description: MongoDB %s
description: Monitoring check 'mongodb_connections' (service: MongoDB %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 27017
        description: port (from the check's parameters).
    username:
        type: string
        description: username (from the check's parameters).
    password:
        type: string
        description: password (from the check's parameters).
    levels_abs:
        type: string
        description: levels_abs (from the check's parameters).
    levels_perc:
        type: list
        description: levels_perc (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
