name: cisco_meraki_org_sensor_humidity
fqcn: checkmk.cisco_meraki_org_sensor_humidity
collection: checkmk
short_description: Cisco Meraki Humidity Relative Percentage %s
description: 'Checkmk check ''cisco_meraki_org_sensor_humidity'' (service: Cisco Meraki
  Humidity Relative Percentage %s), translated to a read-only on-host Starlark check
  module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
