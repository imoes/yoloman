name: fritz_uptime
fqcn: checkmk.fritz_uptime
collection: checkmk
short_description: Uptime
description: 'Checkmk check ''fritz_uptime'' (service: Uptime), translated to a read-only
  on-host Starlark check module.'
options:
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
