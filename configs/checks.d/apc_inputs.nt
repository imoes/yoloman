name: apc_inputs
fqcn: checkmk.apc_inputs
collection: checkmk
short_description: Input %s
description: Checkmk check 'apc_inputs' (service: Input %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    state:
        type: string
        default:
        description: state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
