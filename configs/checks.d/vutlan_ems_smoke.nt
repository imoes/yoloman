name: vutlan_ems_smoke
fqcn: checkmk.vutlan_ems_smoke
collection: checkmk
short_description: Smoke Detector %s
description: Monitoring check 'vutlan_ems_smoke' (service: Smoke Detector %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
