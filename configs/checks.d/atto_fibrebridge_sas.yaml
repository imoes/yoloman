name: atto_fibrebridge_sas
fqcn: checkmk.atto_fibrebridge_sas
collection: checkmk
short_description: SAS Port %s
description: 'Checkmk check ''atto_fibrebridge_sas'' (service: SAS Port %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
