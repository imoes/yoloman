name: inotify
fqcn: checkmk.inotify
collection: checkmk
short_description: INotify %s
description: Checkmk check 'inotify' (service: INotify %s), translated to a read-only on-host Starlark check module.
options:
    config_file:
        type: string
        description: config_file (from the check's parameters).
    age_last_operation:
        type: list
        description: age_last_operation (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
