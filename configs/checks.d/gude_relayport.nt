name: gude_relayport
fqcn: checkmk.gude_relayport
collection: checkmk
short_description: Relay port %s
description: Checkmk check 'gude_relayport' (service: Relay port %s), translated to a read-only on-host Starlark check module.
options:
    current:
        type: string
        description: current (from the check's parameters).
    voltage:
        type: string
        description: voltage (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
