name: win_printers
fqcn: checkmk.win_printers
collection: checkmk
short_description: Printer %s
description: 'Checkmk check ''win_printers'' (service: Printer %s), translated to
  a read-only on-host Starlark check module.'
options:
  warn_states:
    type: list
    description: warn_states (from the check's parameters).
  crit_states:
    type: list
    description: crit_states (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
