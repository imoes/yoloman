name: siemens_plc_cpu_state
fqcn: checkmk.siemens_plc_cpu_state
collection: checkmk
short_description: CPU state
description: Monitoring check 'siemens_plc_cpu_state' (service: CPU state), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    rack:
        type: number
        default: 0
        description: rack (from the check's parameters).
    slot:
        type: number
        default: 2
        description: slot (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
