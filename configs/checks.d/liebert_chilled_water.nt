name: liebert_chilled_water
fqcn: checkmk.liebert_chilled_water
collection: checkmk
short_description: %s
description: Checkmk check 'liebert_chilled_water' (service: %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
