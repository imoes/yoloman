name: brocade_fcport
fqcn: checkmk.brocade_fcport
collection: checkmk
short_description: Port %s
description: Monitoring check 'brocade_fcport' (service: Port %s), translated to a read-only on-host Starlark check module.
options:
    assumed_speed:
        type: number
        default: 2.0
        description: assumed_speed (from the check's parameters).
    average:
        type: string
        description: average (from the check's parameters).
    bw:
        type: string
        description: bw (from the check's parameters).
    rxcrcs:
        type: list
        description: rxcrcs (from the check's parameters).
    rxencoutframes:
        type: list
        description: rxencoutframes (from the check's parameters).
    rxencinframes:
        type: list
        description: rxencinframes (from the check's parameters).
    notxcredits:
        type: list
        description: notxcredits (from the check's parameters).
    c3discards:
        type: list
        description: c3discards (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
