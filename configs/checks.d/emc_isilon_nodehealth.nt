name: emc_isilon_nodehealth
fqcn: checkmk.emc_isilon_nodehealth
collection: checkmk
short_description: Node Health
description: Monitoring check 'emc_isilon_nodehealth' (service: Node Health), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
