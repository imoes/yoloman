name: hitachi_hnas_span
fqcn: checkmk.hitachi_hnas_span
collection: checkmk
short_description: Span %s
description: Monitoring check 'hitachi_hnas_span' (service: Span %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    groups:
        type: list
        description: groups (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
