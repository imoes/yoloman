name: pulse_secure_cpu_util
fqcn: checkmk.pulse_secure_cpu_util
collection: checkmk
short_description: Pulse Secure IVE CPU utilization
description: Checkmk check 'pulse_secure_cpu_util' (service: Pulse Secure IVE CPU utilization), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    util:
        type: list
        description: util (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
