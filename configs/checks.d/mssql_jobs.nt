name: mssql_jobs
fqcn: checkmk.mssql_jobs
collection: checkmk
short_description: MSSQL job %s
description: Monitoring check 'mssql_jobs' (service: MSSQL job %s), translated to a read-only on-host Starlark check module.
options:
    server:
        type: string
        default: .
        description: server (from the check's parameters).
    database:
        type: string
        default: master
        description: database (from the check's parameters).
    username:
        type: string
        description: username (from the check's parameters).
    password:
        type: string
        description: password (from the check's parameters).
    status_missing_jobs:
        type: string
        description: status_missing_jobs (from the check's parameters).
    consider_job_status:
        type: string
        description: consider_job_status (from the check's parameters).
    status_disabled_jobs:
        type: string
        description: status_disabled_jobs (from the check's parameters).
    status_disabled_schedule:
        type: string
        description: status_disabled_schedule (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
