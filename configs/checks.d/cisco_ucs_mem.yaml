name: cisco_ucs_mem
fqcn: checkmk.cisco_ucs_mem
collection: checkmk
short_description: Memory %s
description: 'Checkmk check ''cisco_ucs_mem'' (service: Memory %s), translated to
  a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
