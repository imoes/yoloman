name: pfsense_counter
fqcn: checkmk.pfsense_counter
collection: checkmk
short_description: pfSense Firewall Packet Rates
description: 'Checkmk check ''pfsense_counter'' (service: pfSense Firewall Packet
  Rates), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  average:
    type: number
    default: '3'
    description: average (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
