name: cisco_sma_mail_transfer_memory
fqcn: checkmk.cisco_sma_mail_transfer_memory
collection: checkmk
short_description: Mail transfer memory
description: Checkmk check 'cisco_sma_mail_transfer_memory' (service: Mail transfer memory), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    monitoring_status_memory_shortage:
        type: number
        default: 1
        description: monitoring_status_memory_shortage (from the check's parameters).
    monitoring_status_memory_full:
        type: number
        default: 2
        description: monitoring_status_memory_full (from the check's parameters).
    monitoring_status_memory_available:
        type: number
        default: 0
        description: monitoring_status_memory_available (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
