name: hp_proliant_da_phydrv
fqcn: checkmk.hp_proliant_da_phydrv
collection: checkmk
short_description: HW Phydrv %s
description: Checkmk check 'hp_proliant_da_phydrv' (service: HW Phydrv %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    snmp_version:
        type: string
        default: 2c
        description: snmp_version (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
