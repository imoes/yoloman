name: couchbase_nodes_items
fqcn: checkmk.couchbase_nodes_items
collection: checkmk
short_description: Couchbase %s vBucket items
description: 'Monitoring check ''couchbase_nodes_items'' (service: Couchbase %s vBucket
  items), translated to a read-only on-host Starlark check module.'
options:
  curr_items:
    type: string
    description: curr_items (from the check's parameters).
  non_residents:
    type: string
    description: non_residents (from the check's parameters).
  curr_items_tot:
    type: string
    description: curr_items_tot (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
