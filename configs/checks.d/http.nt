name: http
fqcn: checkmk.http
collection: checkmk
short_description:
description: Checkmk active check 'http', translated to a read-only on-host Starlark check module.
options:
    address:
        type: string
        default:
        description: address (from the check's parameters).
    port:
        type: string
        description: port (from the check's parameters).
    auth:
        type: string
        description: auth (from the check's parameters).
    name:
        type: string
        default:
        description: name (from the check's parameters).
    host:
        type: string
        description: host (from the check's parameters).
    mode:
        type: list
        description: mode (from the check's parameters).
    virthost:
        type: string
        description: virthost (from the check's parameters).
    disable_sni:
        type: bool
        default: False
        description: disable_sni (from the check's parameters).
    host_config:
        type: string
        description: host_config (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
