name: oracle_diva_csm
fqcn: checkmk.oracle_diva_csm
collection: checkmk
short_description: DIVA Status %s
description: Checkmk check 'oracle_diva_csm' (service: DIVA Status %s), translated to a read-only on-host Starlark check module.
options:
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    check_name:
        type: string
        default: oracle_diva_csm
        description: check_name (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
