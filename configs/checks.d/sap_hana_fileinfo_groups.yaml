name: sap_hana_fileinfo_groups
fqcn: checkmk.sap_hana_fileinfo_groups
collection: checkmk
short_description: File group %s
description: 'Checkmk check ''sap_hana_fileinfo_groups'' (service: File group %s),
  translated to a read-only on-host Starlark check module.'
options:
  paths:
    type: list
    description: paths (from the check's parameters).
  group_patterns:
    type: list
    description: group_patterns (from the check's parameters).
  maxcount:
    type: string
    description: maxcount (from the check's parameters).
  mincount:
    type: string
    description: mincount (from the check's parameters).
  negative_age_tolerance:
    type: number
    default: '0'
    description: negative_age_tolerance (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
