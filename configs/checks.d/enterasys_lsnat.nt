name: enterasys_lsnat
fqcn: checkmk.enterasys_lsnat
collection: checkmk
short_description: LSNAT Bindings
description: Checkmk check 'enterasys_lsnat' (service: LSNAT Bindings), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    current_bindings:
        type: string
        description: current_bindings (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
