name: entersekt_soaperrors
fqcn: checkmk.entersekt_soaperrors
collection: checkmk
short_description: Entersekt Soap Service Errors
description: 'Monitoring check ''entersekt_soaperrors'' (service: Entersekt Soap Service
  Errors), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
