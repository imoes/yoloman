name: ibm_svc_eventlog
fqcn: checkmk.ibm_svc_eventlog
collection: checkmk
short_description: Eventlog
description: Monitoring check 'ibm_svc_eventlog' (service: Eventlog), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default: superuser
        description: user (from the check's parameters).
    ssh_key:
        type: string
        default: /root/.ssh/id_rsa
        description: ssh_key (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
