name: epson_beamer_lamp
fqcn: checkmk.epson_beamer_lamp
collection: checkmk
short_description: Beamer Lamp
description: 'Monitoring check ''epson_beamer_lamp'' (service: Beamer Lamp), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
