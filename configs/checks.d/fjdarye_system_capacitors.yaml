name: fjdarye_system_capacitors
fqcn: checkmk.fjdarye_system_capacitors
collection: checkmk
short_description: System Capacitor Unit %s
description: 'Checkmk check ''fjdarye_system_capacitors'' (service: System Capacitor
  Unit %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
