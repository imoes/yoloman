name: skype_edge
fqcn: checkmk.skype_edge
collection: checkmk
short_description: Skype AV Edge %s
description: Monitoring check 'skype_edge' (service: Skype AV Edge %s), translated to a read-only on-host Starlark check module.
options:
    authentication_failures:
        type: string
        description: authentication_failures (from the check's parameters).
    allocate_requests_exceeding:
        type: string
        description: allocate_requests_exceeding (from the check's parameters).
    packets_dropped:
        type: string
        description: packets_dropped (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
