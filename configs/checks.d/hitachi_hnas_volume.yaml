name: hitachi_hnas_volume
fqcn: checkmk.hitachi_hnas_volume
collection: checkmk
short_description: Volumes %s
description: 'Checkmk check ''hitachi_hnas_volume'' (service: Volumes %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  _check:
    type: string
    default: ''
    description: _check (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  patterns:
    type: string
    description: patterns (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
