name: couchbase_buckets_items
fqcn: checkmk.couchbase_buckets_items
collection: checkmk
short_description: Couchbase Bucket %s Items
description: 'Checkmk check ''couchbase_buckets_items'' (service: Couchbase Bucket
  %s Items), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: string
    default: '8091'
    description: port (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
