name: cadvisor_diskstat
fqcn: checkmk.cadvisor_diskstat
collection: checkmk
short_description: Disk IO %s
description: Checkmk check 'cadvisor_diskstat' (service: Disk IO %s), translated to a read-only on-host Starlark check module.
options:
    utilization:
        type: list
        description: utilization (from the check's parameters).
    read_ios:
        type: list
        description: read_ios (from the check's parameters).
    write_ios:
        type: list
        description: write_ios (from the check's parameters).
    read_throughput:
        type: list
        description: read_throughput (from the check's parameters).
    write_throughput:
        type: list
        description: write_throughput (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
