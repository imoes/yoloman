name: nvidia_smi_gpu_util
fqcn: checkmk.nvidia_smi_gpu_util
collection: checkmk
short_description: Nvidia GPU utilization %s
description: 'Checkmk check ''nvidia_smi_gpu_util'' (service: Nvidia GPU utilization
  %s), translated to a read-only on-host Starlark check module.'
options:
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
