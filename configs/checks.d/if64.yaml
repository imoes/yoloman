name: if64
fqcn: checkmk.if64
collection: checkmk
short_description: Interface %s
description: 'Checkmk check ''if64'' (service: Interface %s), translated to a read-only
  on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  warn:
    type: number
    default: '90'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '95'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
