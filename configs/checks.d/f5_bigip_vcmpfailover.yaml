name: f5_bigip_vcmpfailover
fqcn: checkmk.f5_bigip_vcmpfailover
collection: checkmk
short_description: F5-BIGIP-Cluster-Status SNMP Sections and Checks
description: 'Checkmk check ''f5_bigip_vcmpfailover'' (service: BIG-IP vCMP Guest
  Failover Status), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  type:
    type: string
    default: active_standby
    description: type (from the check's parameters).
  v11_2_states:
    type: string
    description: v11_2_states (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
