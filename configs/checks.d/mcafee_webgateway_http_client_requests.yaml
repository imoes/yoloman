name: mcafee_webgateway_http_client_requests
fqcn: checkmk.mcafee_webgateway_http_client_requests
collection: checkmk
short_description: The McAfee Web Gateway has been rebranded to Skyhigh Secure Web
  Gateway with its release 12.2.2.
description: 'Checkmk check ''mcafee_webgateway_http_client_requests'' (service: HTTP
  Client Request Rate), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  client_requests_http:
    type: string
    description: client_requests_http (from the check's parameters).
  client_requests_https:
    type: string
    description: client_requests_https (from the check's parameters).
  client_requests_httpv2:
    type: string
    description: client_requests_httpv2 (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
