name: smart_scsi_temp
fqcn: checkmk.smart_scsi_temp
collection: checkmk
short_description: Temperature SMART %s
description: 'Checkmk check ''smart_scsi_temp'' (service: Temperature SMART %s), translated
  to a read-only on-host Starlark check module.'
options:
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
