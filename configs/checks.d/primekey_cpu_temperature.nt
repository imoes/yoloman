name: primekey_cpu_temperature
fqcn: checkmk.primekey_cpu_temperature
collection: checkmk
short_description: Temperature PrimeKey %s
description: Checkmk check 'primekey_cpu_temperature' (service: Temperature PrimeKey %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
