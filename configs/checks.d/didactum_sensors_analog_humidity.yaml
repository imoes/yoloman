name: didactum_sensors_analog_humidity
fqcn: checkmk.didactum_sensors_analog_humidity
collection: checkmk
short_description: Humidity %s
description: 'Monitoring check ''didactum_sensors_analog_humidity'' (service: Humidity
  %s), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
