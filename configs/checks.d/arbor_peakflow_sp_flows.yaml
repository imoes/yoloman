name: arbor_peakflow_sp_flows
fqcn: checkmk.arbor_peakflow_sp_flows
collection: checkmk
short_description: Flow Count
description: 'Checkmk check ''arbor_peakflow_sp_flows'' (service: Flow Count), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
