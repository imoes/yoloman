name: quantum_storage_status
fqcn: checkmk.quantum_storage_status
collection: checkmk
short_description: Device status
description: 'Checkmk check ''quantum_storage_status'' (service: Device status), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  map_states:
    type: string
    description: map_states (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
