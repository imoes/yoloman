name: huawei_wlc_devs_cpu
fqcn: checkmk.huawei_wlc_devs_cpu
collection: checkmk
short_description: Device %s CPU
description: Checkmk check 'huawei_wlc_devs_cpu' (service: Device %s CPU), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
