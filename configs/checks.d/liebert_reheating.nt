name: liebert_reheating
fqcn: checkmk.liebert_reheating
collection: checkmk
short_description: Reheating Utilization
description: Checkmk check 'liebert_reheating' (service: Reheating Utilization), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
