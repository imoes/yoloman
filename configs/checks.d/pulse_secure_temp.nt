name: pulse_secure_temp
fqcn: checkmk.pulse_secure_temp
collection: checkmk
short_description: Pulse Secure %s Temperature
description: Checkmk check 'pulse_secure_temp' (service: Pulse Secure %s Temperature), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
