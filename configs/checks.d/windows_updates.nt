name: windows_updates
fqcn: checkmk.windows_updates
collection: checkmk
short_description: System Updates
description: Checkmk check 'windows_updates' (service: System Updates), translated to a read-only on-host Starlark check module.
options:
    levels_important:
        type: list
        description: levels_important (from the check's parameters).
    levels_optional:
        type: list
        description: levels_optional (from the check's parameters).
    levels_lower_forced_reboot:
        type: list
        description: levels_lower_forced_reboot (from the check's parameters).
    reboot_required_show_state:
        type: number
        default: 1
        description: reboot_required_show_state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
