name: qlogic_fcport
fqcn: checkmk.qlogic_fcport
collection: checkmk
short_description: FC Port %s
description: Monitoring check 'qlogic_fcport' (service: FC Port %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
