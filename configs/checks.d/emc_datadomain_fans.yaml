name: emc_datadomain_fans
fqcn: checkmk.emc_datadomain_fans
collection: checkmk
short_description: FAN %s
description: 'Monitoring check ''emc_datadomain_fans'' (service: FAN %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
