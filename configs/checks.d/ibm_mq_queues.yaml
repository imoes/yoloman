name: ibm_mq_queues
fqcn: checkmk.ibm_mq_queues
collection: checkmk
short_description: IBM MQ Queue %s
description: 'Checkmk check ''ibm_mq_queues'' (service: IBM MQ Queue %s), translated
  to a read-only on-host Starlark check module.'
options:
  curdepth:
    type: string
    description: curdepth (from the check's parameters).
  curdepth_perc:
    type: string
    description: curdepth_perc (from the check's parameters).
  msgage:
    type: string
    description: msgage (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
