name: ups_test
fqcn: checkmk.ups_test
collection: checkmk
short_description: Self Test
description: Monitoring check 'ups_test' (service: Self Test), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels_elapsed_time:
        type: list
        description: levels_elapsed_time (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
