name: bvip_video_alerts
fqcn: checkmk.bvip_video_alerts
collection: checkmk
short_description: Video Alerts %s
description: 'Checkmk check ''bvip_video_alerts'' (service: Video Alerts %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
