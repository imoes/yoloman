name: cisco_qos
fqcn: checkmk.cisco_qos
collection: checkmk
short_description: QoS %s
description: Checkmk check 'cisco_qos' (service: QoS %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    drop:
        type: list
        description: drop (from the check's parameters).
    unit:
        type: string
        default: bit
        description: unit (from the check's parameters).
    average:
        type: string
        description: average (from the check's parameters).
    post:
        type: string
        description: post (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
