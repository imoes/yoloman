name: emc_isilon_ifs
fqcn: checkmk.emc_isilon_ifs
collection: checkmk
short_description: Filesystem %s
description: Monitoring check 'emc_isilon_ifs' (service: Filesystem %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
