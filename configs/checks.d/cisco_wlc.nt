name: cisco_wlc
fqcn: checkmk.cisco_wlc
collection: checkmk
short_description: Cisco WLC sections and checks
description: Checkmk check 'cisco_wlc' (service: AP %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
