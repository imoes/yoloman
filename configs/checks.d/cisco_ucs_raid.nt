name: cisco_ucs_raid
fqcn: checkmk.cisco_ucs_raid
collection: checkmk
short_description: RAID Controller
description: Checkmk check 'cisco_ucs_raid' (service: RAID Controller), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
