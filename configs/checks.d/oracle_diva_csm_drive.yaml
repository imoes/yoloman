name: oracle_diva_csm_drive
fqcn: checkmk.oracle_diva_csm_drive
collection: checkmk
short_description: DIVA Status %s
description: 'Checkmk check ''oracle_diva_csm_drive'' (service: DIVA Status %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
