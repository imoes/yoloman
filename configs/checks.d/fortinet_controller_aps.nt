name: fortinet_controller_aps
fqcn: checkmk.fortinet_controller_aps
collection: checkmk
short_description: AP %s
description: Checkmk check 'fortinet_controller_aps' (service: AP %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
