name: ibm_svc_nodestats_cpu_util
fqcn: checkmk.ibm_svc_nodestats_cpu_util
collection: checkmk
short_description: CPU utilization %s
description: Monitoring check 'ibm_svc_nodestats_cpu_util' (service: CPU utilization %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: number
        default: 90.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 95.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
