name: cmciii
fqcn: checkmk.cmciii
collection: checkmk
short_description: State %s
description: Checkmk check 'cmciii' (service: State %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    discovery_params:
        type: string
        description: discovery_params (from the check's parameters).
    check_params:
        type: string
        description: check_params (from the check's parameters).
    _item_key:
        type: string
        description: _item_key (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
