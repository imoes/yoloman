name: bluenet2_powerrail_sockets
fqcn: checkmk.bluenet2_powerrail_sockets
collection: checkmk
short_description: Socket %s
description: 'Monitoring check ''bluenet2_powerrail_sockets'' (service: Socket %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
  differential_current_ac_levels:
    type: string
    description: differential_current_ac_levels (from the check's parameters).
  differential_current_dc_levels:
    type: string
    description: differential_current_dc_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
