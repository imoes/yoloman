name: orion_batterytest
fqcn: checkmk.orion_batterytest
collection: checkmk
short_description: Battery Test
description: 'Checkmk check ''orion_batterytest'' (service: Battery Test), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
