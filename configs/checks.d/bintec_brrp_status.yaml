name: bintec_brrp_status
fqcn: checkmk.bintec_brrp_status
collection: checkmk
short_description: BRRP Status %s
description: 'Checkmk check ''bintec_brrp_status'' (service: BRRP Status %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
