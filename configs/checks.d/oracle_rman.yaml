name: oracle_rman
fqcn: checkmk.oracle_rman
collection: checkmk
short_description: ORA %s RMAN Backup
description: 'Checkmk check ''oracle_rman'' (service: ORA %s RMAN Backup), translated
  to a read-only on-host Starlark check module.'
options:
  password:
    type: string
    default: ''
    description: password (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
