name: cisco_meraki_org_wireless_device_statuses_bands
fqcn: checkmk.cisco_meraki_org_wireless_device_statuses_bands
collection: checkmk
short_description: Radio %s
description: Checkmk check 'cisco_meraki_org_wireless_device_statuses_bands' (service: Radio %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: api.meraki.com
        description: host (from the check's parameters).
    org_id:
        type: string
        description: org_id (from the check's parameters).
    network_id:
        type: string
        description: network_id (from the check's parameters).
    serial:
        type: string
        description: serial (from the check's parameters).
    api_key:
        type: string
        description: api_key (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
