name: ibm_svc_systemstats_cache
fqcn: checkmk.ibm_svc_systemstats_cache
collection: checkmk
short_description: Cache Total
description: 'Checkmk check ''ibm_svc_systemstats_cache'' (service: Cache Total),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
