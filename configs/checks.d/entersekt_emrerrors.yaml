name: entersekt_emrerrors
fqcn: checkmk.entersekt_emrerrors
collection: checkmk
short_description: Entersekt http EMR Errors
description: 'Monitoring check ''entersekt_emrerrors'' (service: Entersekt http EMR
  Errors), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
