name: ups_modulys_battery
fqcn: checkmk.ups_modulys_battery
collection: checkmk
short_description: Battery Charge
description: Monitoring check 'ups_modulys_battery' (service: Battery Charge), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    battime:
        type: list
        description: battime (from the check's parameters).
    capacity:
        type: list
        description: capacity (from the check's parameters).
    temp:
        type: string
        description: temp (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
