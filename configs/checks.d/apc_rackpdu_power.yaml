name: apc_rackpdu_power
fqcn: checkmk.apc_rackpdu_power
collection: checkmk
short_description: PDU %s
description: 'Checkmk check ''apc_rackpdu_power'' (service: PDU %s), translated to
  a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
