name: mqtt_uptime
fqcn: checkmk.mqtt_uptime
collection: checkmk
short_description: MQTT %s Uptime
description: 'Checkmk check ''mqtt_uptime'' (service: MQTT %s Uptime), translated
  to a read-only on-host Starlark check module.'
options:
  broker:
    type: string
    default: localhost
    description: broker (from the check's parameters).
  port:
    type: number
    default: '1883'
    description: port (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
