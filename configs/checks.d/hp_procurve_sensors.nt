name: hp_procurve_sensors
fqcn: checkmk.hp_procurve_sensors
collection: checkmk
short_description: Sensor %s
description: Monitoring check 'hp_procurve_sensors' (service: Sensor %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
