name: huawei_osn_if
fqcn: checkmk.huawei_osn_if
collection: checkmk
short_description: Interface %s
description: Checkmk check 'huawei_osn_if' (service: Interface %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    errors:
        type: list
        description: errors (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
