name: plesk_backups
fqcn: checkmk.plesk_backups
collection: checkmk
short_description: Plesk Backup %s
description: Checkmk check 'plesk_backups' (service: Plesk Backup %s), translated to a read-only on-host Starlark check module.
options:
    no_backup_configured_state:
        type: number
        default: 1
        description: no_backup_configured_state (from the check's parameters).
    no_backup_found_state:
        type: number
        default: 1
        description: no_backup_found_state (from the check's parameters).
    backup_age:
        type: string
        description: backup_age (from the check's parameters).
    total_size:
        type: string
        description: total_size (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
