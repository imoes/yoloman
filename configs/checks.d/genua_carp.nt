name: genua_carp
fqcn: checkmk.genua_carp
collection: checkmk
short_description: Carp Interface %s
description: Checkmk check 'genua_carp' (service: Carp Interface %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
