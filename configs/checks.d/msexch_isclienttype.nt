name: msexch_isclienttype
fqcn: checkmk.msexch_isclienttype
collection: checkmk
short_description: Exchange IS Client Type %s
description: Checkmk check 'msexch_isclienttype' (service: Exchange IS Client Type %s), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    clienttype_latency_s:
        type: list
        description: clienttype_latency_s (from the check's parameters).
    clienttype_requests:
        type: list
        description: clienttype_requests (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
