name: citrix_controller
fqcn: checkmk.citrix_controller
collection: checkmk
short_description: Citrix Controller State
description: Monitoring check 'citrix_controller' (service: Citrix Controller State), translated to a read-only on-host Starlark check module.
options:
    total:
        type: string
        description: total (from the check's parameters).
    active:
        type: string
        description: active (from the check's parameters).
    inactive:
        type: string
        description: inactive (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    levels_lower:
        type: string
        description: levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
