name: podman_container_restarts
fqcn: checkmk.podman_container_restarts
collection: checkmk
short_description: Restarts
description: 'Checkmk check ''podman_container_restarts'' (service: Restarts), translated
  to a read-only on-host Starlark check module.'
options:
  restarts_total_warn:
    type: string
    description: restarts_total_warn (from the check's parameters).
  restarts_total_crit:
    type: string
    description: restarts_total_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
