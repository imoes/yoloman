name: haproxy_backend
fqcn: checkmk.haproxy_backend
collection: checkmk
short_description: HAProxy Backend %s
description: 'Checkmk check ''haproxy_backend'' (service: HAProxy Backend %s), translated
  to a read-only on-host Starlark check module.'
options:
  socket:
    type: string
    description: socket (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
