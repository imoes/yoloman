name: hp_blade_manager
fqcn: checkmk.hp_blade_manager
collection: checkmk
short_description: Manager %s
description: 'Checkmk check ''hp_blade_manager'' (service: Manager %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  role:
    type: string
    description: role (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
