name: emc_isilon_iops
fqcn: checkmk.emc_isilon_iops
collection: checkmk
short_description: Disk %s IO
description: 'Checkmk check ''emc_isilon_iops'' (service: Disk %s IO), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  warn:
    type: number
    default: '0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '0'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
