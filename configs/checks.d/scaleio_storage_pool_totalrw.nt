name: scaleio_storage_pool_totalrw
fqcn: checkmk.scaleio_storage_pool_totalrw
collection: checkmk
short_description: ScaleIO SP total IO %s
description: Checkmk check 'scaleio_storage_pool_totalrw' (service: ScaleIO SP total IO %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    warn_read_iops:
        type: string
        description: warn_read_iops (from the check's parameters).
    crit_read_iops:
        type: string
        description: crit_read_iops (from the check's parameters).
    warn_write_iops:
        type: string
        description: warn_write_iops (from the check's parameters).
    crit_write_iops:
        type: string
        description: crit_write_iops (from the check's parameters).
    warn_read_tp:
        type: string
        description: warn_read_tp (from the check's parameters).
    crit_read_tp:
        type: string
        description: crit_read_tp (from the check's parameters).
    warn_write_tp:
        type: string
        description: warn_write_tp (from the check's parameters).
    crit_write_tp:
        type: string
        description: crit_write_tp (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
