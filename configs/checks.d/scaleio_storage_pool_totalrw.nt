name: scaleio_storage_pool_totalrw
fqcn: checkmk.scaleio_storage_pool_totalrw
collection: checkmk
short_description: ScaleIO SP total IO %s
description: Monitoring check 'scaleio_storage_pool_totalrw' (service: ScaleIO SP total IO %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: admin
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    read_ios_warn:
        type: string
        description: read_ios_warn (from the check's parameters).
    read_ios_crit:
        type: string
        description: read_ios_crit (from the check's parameters).
    write_ios_warn:
        type: string
        description: write_ios_warn (from the check's parameters).
    write_ios_crit:
        type: string
        description: write_ios_crit (from the check's parameters).
    read_throughput_warn:
        type: string
        description: read_throughput_warn (from the check's parameters).
    read_throughput_crit:
        type: string
        description: read_throughput_crit (from the check's parameters).
    write_throughput_warn:
        type: string
        description: write_throughput_warn (from the check's parameters).
    write_throughput_crit:
        type: string
        description: write_throughput_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
