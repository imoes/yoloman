name: bluenet_meter
fqcn: checkmk.bluenet_meter
collection: checkmk
short_description: Powermeter %s
description: 'Checkmk check ''bluenet_meter'' (service: Powermeter %s), translated
  to a read-only on-host Starlark check module.'
options:
  levels:
    type: string
    description: levels (from the check's parameters).
  thresholds:
    type: string
    description: thresholds (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  voltage:
    type: string
    description: voltage (from the check's parameters).
  current:
    type: string
    description: current (from the check's parameters).
  power:
    type: string
    description: power (from the check's parameters).
  appower:
    type: string
    description: appower (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
