name: enviromux_all_external_humidity
fqcn: checkmk.enviromux_all_external_humidity
collection: checkmk
short_description: Sensor External %s
description: 'Checkmk check ''enviromux_all_external_humidity'' (service: Sensor External
  %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
