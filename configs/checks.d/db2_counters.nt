name: db2_counters
fqcn: checkmk.db2_counters
collection: checkmk
short_description: DB2 Counters %s
description: Checkmk check 'db2_counters' (service: DB2 Counters %s), translated to a read-only on-host Starlark check module.
options:
    instance:
        type: string
        description: instance (from the check's parameters).
    deadlocks:
        type: string
        description: deadlocks (from the check's parameters).
    lockwaits:
        type: string
        description: lockwaits (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
