name: ups_socomec_capacity
fqcn: checkmk.ups_socomec_capacity
collection: checkmk
short_description: Battery capacity
description: Checkmk check 'ups_socomec_capacity' (service: Battery capacity), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    battime:
        type: list
        description: battime (from the check's parameters).
    capacity:
        type: list
        description: capacity (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
