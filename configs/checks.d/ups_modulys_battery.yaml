name: ups_modulys_battery
fqcn: checkmk.ups_modulys_battery
collection: checkmk
short_description: Battery Charge
description: 'Checkmk check ''ups_modulys_battery'' (service: Battery Charge), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  crit:
    type: number
    default: '60'
    description: crit (from the check's parameters).
  warn:
    type: number
    default: '50'
    description: warn (from the check's parameters).
  capacity:
    type: list
    description: capacity (from the check's parameters).
  battime:
    type: list
    description: battime (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
