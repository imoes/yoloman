name: stormshield_cluster_node
fqcn: checkmk.stormshield_cluster_node
collection: checkmk
short_description: HA Member %s
description: Monitoring check 'stormshield_cluster_node' (service: HA Member %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    quality:
        type: string
        description: quality (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
