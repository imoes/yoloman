name: fast_lta_headunit_replication
fqcn: checkmk.fast_lta_headunit_replication
collection: checkmk
short_description: Fast LTA Replication
description: 'Checkmk check ''fast_lta_headunit_replication'' (service: Fast LTA Replication),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
