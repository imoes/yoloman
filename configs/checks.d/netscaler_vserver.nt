name: netscaler_vserver
fqcn: checkmk.netscaler_vserver
collection: checkmk
short_description: VServer %s
description: Checkmk check 'netscaler_vserver' (service: VServer %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    health_levels:
        type: list
        description: health_levels (from the check's parameters).
    cluster_status:
        type: string
        default: best
        description: cluster_status (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
