name: netscaler_vserver
fqcn: checkmk.netscaler_vserver
collection: checkmk
short_description: VServer %s
description: Monitoring check 'netscaler_vserver' (service: VServer %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    health_levels:
        type: list
        description: health_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
