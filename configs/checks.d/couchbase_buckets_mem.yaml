name: couchbase_buckets_mem
fqcn: checkmk.couchbase_buckets_mem
collection: checkmk
short_description: Couchbase Bucket %s Memory
description: 'Monitoring check ''couchbase_buckets_mem'' (service: Couchbase Bucket
  %s Memory), translated to a read-only on-host Starlark check module.'
options:
  user:
    type: string
    default: Administrator
    description: user (from the check's parameters).
  pass:
    type: string
    default: ''
    description: pass (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
