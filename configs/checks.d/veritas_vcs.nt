name: veritas_vcs
fqcn: checkmk.veritas_vcs
collection: checkmk
short_description: VCS Cluster %s
description: Checkmk check 'veritas_vcs' (service: VCS Cluster %s), translated to a read-only on-host Starlark check module.
options:
    checkname:
        type: string
        default: veritas_vcs
        description: checkname (from the check's parameters).
    map_states:
        type: string
        description: map_states (from the check's parameters).
    map_frozen:
        type: string
        description: map_frozen (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
