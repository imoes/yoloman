name: pdu_gude
fqcn: checkmk.pdu_gude
collection: checkmk
short_description: Phase %s
description: Checkmk check 'pdu_gude' (service: Phase %s), translated to a read-only on-host Starlark check module.
options:
    pdu_gude:
        type: string
        description: pdu_gude (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
