name: allnet_ip_sensoric_humidity
fqcn: checkmk.allnet_ip_sensoric_humidity
collection: checkmk
short_description: Humidity %s
description: Checkmk check 'allnet_ip_sensoric_humidity' (service: Humidity %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 80
        description: port (from the check's parameters).
    warn:
        type: number
        default: 60.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 65.0
        description: crit (from the check's parameters).
    warn_lower:
        type: number
        default: 40.0
        description: warn_lower (from the check's parameters).
    crit_lower:
        type: number
        default: 35.0
        description: crit_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
