name: entity_sensors_fan
fqcn: checkmk.entity_sensors_fan
collection: checkmk
short_description: Fan %s
description: 'Checkmk check ''entity_sensors_fan'' (service: Fan %s), translated to
  a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  lower:
    type: list
    description: lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
