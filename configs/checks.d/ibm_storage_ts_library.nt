name: ibm_storage_ts_library
fqcn: checkmk.ibm_storage_ts_library
collection: checkmk
short_description: Library %s
description: Checkmk check 'ibm_storage_ts_library' (service: Library %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
