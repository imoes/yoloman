name: lvm_lvs
fqcn: checkmk.lvm_lvs
collection: checkmk
short_description: LVM LV Pool %s
description: 'Checkmk check ''lvm_lvs'' (service: LVM LV Pool %s), translated to a
  read-only on-host Starlark check module.'
options:
  levels_data:
    type: list
    description: levels_data (from the check's parameters).
  levels_meta:
    type: list
    description: levels_meta (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
