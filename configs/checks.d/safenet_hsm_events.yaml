name: safenet_hsm_events
fqcn: checkmk.safenet_hsm_events
collection: checkmk
short_description: HSM Safenet Event Stats
description: 'Checkmk check ''safenet_hsm_events'' (service: HSM Safenet Event Stats),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  version:
    type: string
    default: v2c
    description: version (from the check's parameters).
  critical_events:
    type: string
    description: critical_events (from the check's parameters).
  noncritical_events:
    type: string
    description: noncritical_events (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
