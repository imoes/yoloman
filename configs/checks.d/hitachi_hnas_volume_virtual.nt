name: hitachi_hnas_volume_virtual
fqcn: checkmk.hitachi_hnas_volume_virtual
collection: checkmk
short_description: Volumes %s
description: Monitoring check 'hitachi_hnas_volume_virtual' (service: Volumes %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
