name: mtr
fqcn: checkmk.mtr
collection: checkmk
short_description: Mtr to %s
description: Checkmk check 'mtr' (service: Mtr to %s), translated to a read-only on-host Starlark check module.
options:
    pl:
        type: list
        description: pl (from the check's parameters).
    rta:
        type: list
        description: rta (from the check's parameters).
    rtstddev:
        type: list
        description: rtstddev (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
