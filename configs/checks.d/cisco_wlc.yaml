name: cisco_wlc
fqcn: checkmk.cisco_wlc
collection: checkmk
short_description: Cisco WLC sections and checks
description: 'Checkmk check ''cisco_wlc'' (service: AP %s), translated to a read-only
  on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  snmp_version:
    type: string
    default: 2c
    description: snmp_version (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
