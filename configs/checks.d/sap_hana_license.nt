name: sap_hana_license
fqcn: checkmk.sap_hana_license
collection: checkmk
short_description: SAP HANA License %s
description: Checkmk check 'sap_hana_license' (service: SAP HANA License %s), translated to a read-only on-host Starlark check module.
options:
    userstore_key:
        type: string
        default:
        description: userstore_key (from the check's parameters).
    hana_user:
        type: string
        default: SYSTEM
        description: hana_user (from the check's parameters).
    hana_password:
        type: string
        default:
        description: hana_password (from the check's parameters).
    license_size:
        type: string
        description: license_size (from the check's parameters).
    license_usage_perc:
        type: list
        description: license_usage_perc (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
