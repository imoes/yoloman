name: kentix_dewpoint
fqcn: checkmk.kentix_dewpoint
collection: checkmk
short_description: Dewpoint %s
description: 'Checkmk check ''kentix_dewpoint'' (service: Dewpoint %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  warn:
    type: number
    default: '28.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '30.0'
    description: crit (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
