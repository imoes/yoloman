name: primekey_data
fqcn: checkmk.primekey_data
collection: checkmk
short_description: PrimeKey %s Status
description: Checkmk check 'primekey_data' (service: PrimeKey %s Status), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    snmp_version:
        type: string
        default: 2c
        description: snmp_version (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
