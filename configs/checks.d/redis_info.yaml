name: redis_info
fqcn: checkmk.redis_info
collection: checkmk
short_description: Redis %s Server Info
description: 'Checkmk check ''redis_info'' (service: Redis %s Server Info), translated
  to a read-only on-host Starlark check module.'
options:
  expected_mode:
    type: string
    default: ''
    description: expected_mode (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
