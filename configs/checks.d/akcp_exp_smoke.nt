name: akcp_exp_smoke
fqcn: checkmk.akcp_exp_smoke
collection: checkmk
short_description: Smoke %s
description: Checkmk check 'akcp_exp_smoke' (service: Smoke %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
