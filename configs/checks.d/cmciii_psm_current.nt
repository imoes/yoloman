name: cmciii_psm_current
fqcn: checkmk.cmciii_psm_current
collection: checkmk
short_description: Current %s
description: Checkmk check 'cmciii_psm_current' (service: Current %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    use_sensor_description:
        type: bool
        default: False
        description: use_sensor_description (from the check's parameters).
    _item_key:
        type: string
        default:
        description: _item_key (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
