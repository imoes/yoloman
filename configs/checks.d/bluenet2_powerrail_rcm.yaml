name: bluenet2_powerrail_rcm
fqcn: checkmk.bluenet2_powerrail_rcm
collection: checkmk
short_description: Inlet %s
description: 'Monitoring check ''bluenet2_powerrail_rcm'' (service: Inlet %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  differential_current_ac:
    type: list
    description: differential_current_ac (from the check's parameters).
  differential_current_dc:
    type: list
    description: differential_current_dc (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
