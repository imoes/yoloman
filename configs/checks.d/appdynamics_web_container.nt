name: appdynamics_web_container
fqcn: checkmk.appdynamics_web_container
collection: checkmk
short_description: AppDynamics Web Container %s
description: Monitoring check 'appdynamics_web_container' (service: AppDynamics Web Container %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8090
        description: port (from the check's parameters).
    ssl:
        type: bool
        default: False
        description: ssl (from the check's parameters).
    application:
        type: string
        default:
        description: application (from the check's parameters).
    username:
        type: string
        default:
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    tier:
        type: string
        default:
        description: tier (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
