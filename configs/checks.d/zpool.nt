name: zpool
fqcn: checkmk.zpool
collection: checkmk
short_description: Storage Pool %s
description: Checkmk check 'zpool' (service: Storage Pool %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    trend_range:
        type: number
        default: 24
        description: trend_range (from the check's parameters).
    trend_mb:
        type: string
        description: trend_mb (from the check's parameters).
    trend_percent:
        type: string
        description: trend_percent (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
