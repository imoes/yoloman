name: mailboxes
fqcn: checkmk.mailboxes
collection: checkmk
short_description: ''
description: Checkmk active check 'mailboxes', translated to a read-only on-host Starlark
  check module.
options:
  fetch_protocol:
    type: string
    default: IMAP
    description: fetch_protocol (from the check's parameters).
  age_warn:
    type: string
    description: age_warn (from the check's parameters).
  age_crit:
    type: string
    description: age_crit (from the check's parameters).
  count_warn:
    type: string
    description: count_warn (from the check's parameters).
  count_crit:
    type: string
    description: count_crit (from the check's parameters).
  age_newest_warn:
    type: string
    description: age_newest_warn (from the check's parameters).
  age_newest_crit:
    type: string
    description: age_newest_crit (from the check's parameters).
  mailbox:
    type: string
    default: /var/mail
    description: mailbox (from the check's parameters).
  tls:
    type: bool
    default: 'True'
    description: tls (from the check's parameters).
  disable_tls:
    type: bool
    default: 'False'
    description: disable_tls (from the check's parameters).
  port:
    type: string
    description: port (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
