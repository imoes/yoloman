name: liebert_cooling_position
fqcn: checkmk.liebert_cooling_position
collection: checkmk
short_description: %s
description: Checkmk check 'liebert_cooling_position' (service: %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    min_capacity:
        type: string
        description: min_capacity (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
