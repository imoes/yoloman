name: couchbase_nodes_size_couch_views
fqcn: checkmk.couchbase_nodes_size_couch_views
collection: checkmk
short_description: Couchbase %s Couch Views
description: 'Checkmk check ''couchbase_nodes_size_couch_views'' (service: Couchbase
  %s Couch Views), translated to a read-only on-host Starlark check module.'
options:
  size_on_disk:
    type: string
    description: size_on_disk (from the check's parameters).
  size:
    type: string
    description: size (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: string
    default: '8091'
    description: port (from the check's parameters).
  username:
    type: string
    default: ''
    description: username (from the check's parameters).
  password:
    type: string
    default: ''
    description: password (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
