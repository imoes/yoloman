name: wut_webtherm_pressure
fqcn: checkmk.wut_webtherm_pressure
collection: checkmk
short_description: Pressure %s
description: 'Checkmk check ''wut_webtherm_pressure'' (service: Pressure %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
