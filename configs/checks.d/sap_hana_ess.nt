name: sap_hana_ess
fqcn: checkmk.sap_hana_ess
collection: checkmk
short_description: SAP HANA ESS %s
description: Checkmk check 'sap_hana_ess' (service: SAP HANA ESS %s), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    store_key:
        type: string
        default:
        description: store_key (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
