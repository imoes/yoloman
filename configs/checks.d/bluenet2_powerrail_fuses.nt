name: bluenet2_powerrail_fuses
fqcn: checkmk.bluenet2_powerrail_fuses
collection: checkmk
short_description: Fuse %s
description: Checkmk check 'bluenet2_powerrail_fuses' (service: Fuse %s), translated to a read-only on-host Starlark check module.
options:
    warn_current:
        type: number
        default: 16.0
        description: warn_current (from the check's parameters).
    crit_current:
        type: number
        default: 20.0
        description: crit_current (from the check's parameters).
    warn_power:
        type: number
        default: 3680.0
        description: warn_power (from the check's parameters).
    crit_power:
        type: number
        default: 4600.0
        description: crit_power (from the check's parameters).
    warn_appower:
        type: number
        default: 3680.0
        description: warn_appower (from the check's parameters).
    crit_appower:
        type: number
        default: 4600.0
        description: crit_appower (from the check's parameters).
    warn_voltage:
        type: number
        default: 230.0
        description: warn_voltage (from the check's parameters).
    crit_voltage:
        type: number
        default: 240.0
        description: crit_voltage (from the check's parameters).
    warn_frequency:
        type: number
        default: 50.0
        description: warn_frequency (from the check's parameters).
    crit_frequency:
        type: number
        default: 50.0
        description: crit_frequency (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
