name: bluenet2_powerrail_fuses
fqcn: checkmk.bluenet2_powerrail_fuses
collection: checkmk
short_description: Fuse %s
description: Monitoring check 'bluenet2_powerrail_fuses' (service: Fuse %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 10.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 15.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
