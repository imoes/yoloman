name: domino_transactions
fqcn: checkmk.domino_transactions
collection: checkmk
short_description: Domino Server Transactions
description: Monitoring check 'domino_transactions' (service: Domino Server Transactions), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        description: community (from the check's parameters).
    host:
        type: string
        description: host (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
