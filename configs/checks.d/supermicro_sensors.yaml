name: supermicro_sensors
fqcn: checkmk.supermicro_sensors
collection: checkmk
short_description: Sensor %s
description: 'Checkmk check ''supermicro_sensors'' (service: Sensor %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  check_name:
    type: string
    default: supermicro_sensors
    description: check_name (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
