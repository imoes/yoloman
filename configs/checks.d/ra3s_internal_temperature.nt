name: ra3s_internal_temperature
fqcn: checkmk.ra3s_internal_temperature
collection: checkmk
short_description: Temperature %s
description: Checkmk check 'ra3s_internal_temperature' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
