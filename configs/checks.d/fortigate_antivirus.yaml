name: fortigate_antivirus
fqcn: checkmk.fortigate_antivirus
collection: checkmk
short_description: AntiVirus %s
description: 'Checkmk check ''fortigate_antivirus'' (service: AntiVirus %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  detections:
    type: list
    description: detections (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
