name: allnet_ip_sensoric_tension
fqcn: checkmk.allnet_ip_sensoric_tension
collection: checkmk
short_description: Electric Tension %s
description: Checkmk check 'allnet_ip_sensoric_tension' (service: Electric Tension %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: string
        default: 80
        description: port (from the check's parameters).
    path:
        type: string
        default: /json
        description: path (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
