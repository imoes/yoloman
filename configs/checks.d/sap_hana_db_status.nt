name: sap_hana_db_status
fqcn: checkmk.sap_hana_db_status
collection: checkmk
short_description: SAP HANA Database Status %s
description: Monitoring check 'sap_hana_db_status' (service: SAP HANA Database Status %s), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default: SYSTEM
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    key_user_store:
        type: string
        default:
        description: key_user_store (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
