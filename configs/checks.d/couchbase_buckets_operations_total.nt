name: couchbase_buckets_operations_total
fqcn: checkmk.couchbase_buckets_operations_total
collection: checkmk
short_description: Couchbase Bucket Operations
description: Checkmk check 'couchbase_buckets_operations_total' (service: Couchbase Bucket Operations), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default: Administrator
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8091
        description: port (from the check's parameters).
    ops:
        type: string
        description: ops (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
