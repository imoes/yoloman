name: fortigate_node_memory
fqcn: checkmk.fortigate_node_memory
collection: checkmk
short_description: Memory %s
description: Checkmk check 'fortigate_node_memory' (service: Memory %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
