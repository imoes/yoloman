name: cisco_ace_rserver
fqcn: checkmk.cisco_ace_rserver
collection: checkmk
short_description: ACE RServer %s
description: Monitoring check 'cisco_ace_rserver' (service: ACE RServer %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
