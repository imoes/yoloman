name: janitza_umg_temp
fqcn: checkmk.janitza_umg_temp
collection: checkmk
short_description: Temperature External %s
description: Monitoring check 'janitza_umg_temp' (service: Temperature External %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
