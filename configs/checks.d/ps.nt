name: ps
fqcn: checkmk.ps
collection: checkmk
short_description: Process %s
description: Monitoring check 'ps' (service: Process %s), translated to a read-only on-host Starlark check module.
options:
    process:
        type: string
        description: process (from the check's parameters).
    user:
        type: string
        description: user (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    cpu:
        type: string
        description: cpu (from the check's parameters).
    resident_levels:
        type: string
        description: resident_levels (from the check's parameters).
    virtual_levels:
        type: string
        description: virtual_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
