name: extreme_vsp_switches_cpu_util
fqcn: checkmk.extreme_vsp_switches_cpu_util
collection: checkmk
short_description: VSP Switches CPU Utilization
description: Checkmk check 'extreme_vsp_switches_cpu_util' (service: VSP Switches CPU Utilization), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    version:
        type: string
        default: 2c
        description: version (from the check's parameters).
    util:
        type: list
        description: util (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
