name: fireeye_sys_status
fqcn: checkmk.fireeye_sys_status
collection: checkmk
short_description: System status
description: 'Checkmk check ''fireeye_sys_status'' (service: System status), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
