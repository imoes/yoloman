name: hp_psu_temp
fqcn: checkmk.hp_psu_temp
collection: checkmk
short_description: Temperature Power Supply %s
description: Checkmk check 'hp_psu_temp' (service: Temperature Power Supply %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
