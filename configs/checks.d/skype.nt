name: skype
fqcn: checkmk.skype
collection: checkmk
short_description: Skype Web Components
description: Checkmk check 'skype' (service: Skype Web Components), translated to a read-only on-host Starlark check module.
options:
    failed_search_requests:
        type: string
        description: failed_search_requests (from the check's parameters).
    failed_locations_requests:
        type: string
        description: failed_locations_requests (from the check's parameters).
    timedout_ad_requests:
        type: string
        description: timedout_ad_requests (from the check's parameters).
    asp_requests_rejected:
        type: string
        description: asp_requests_rejected (from the check's parameters).
    failed_file_requests:
        type: string
        description: failed_file_requests (from the check's parameters).
    join_failures:
        type: string
        description: join_failures (from the check's parameters).
    failed_validate_cert:
        type: string
        description: failed_validate_cert (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
