name: brocade_optical
fqcn: checkmk.brocade_optical
collection: checkmk
short_description: Interface %s Optical
description: Checkmk check 'brocade_optical' (service: Interface %s Optical), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    temp_warn:
        type: number
        default: 70.0
        description: temp_warn (from the check's parameters).
    temp_crit:
        type: number
        default: 80.0
        description: temp_crit (from the check's parameters).
    tx_light_warn:
        type: number
        default: -10.0
        description: tx_light_warn (from the check's parameters).
    tx_light_crit:
        type: number
        default: -20.0
        description: tx_light_crit (from the check's parameters).
    rx_light_warn:
        type: number
        default: -10.0
        description: rx_light_warn (from the check's parameters).
    rx_light_crit:
        type: number
        default: -20.0
        description: rx_light_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
