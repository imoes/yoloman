name: printer_supply
fqcn: checkmk.printer_supply
collection: checkmk
short_description: Supply %s
description: Checkmk check 'printer_supply' (service: Supply %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    some_remaining_ink:
        type: number
        default: 1
        description: some_remaining_ink (from the check's parameters).
    some_remaining_space:
        type: number
        default: 1
        description: some_remaining_space (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    upturn_toner:
        type: bool
        default: False
        description: upturn_toner (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
