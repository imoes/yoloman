name: siemens_plc_info
fqcn: checkmk.siemens_plc_info
collection: checkmk
short_description: Info %s
description: 'Checkmk check ''siemens_plc_info'' (service: Info %s), translated to
  a read-only on-host Starlark check module.'
options:
  section:
    type: string
    description: section (from the check's parameters).
  _check:
    type: string
    default: info
    description: _check (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  expected_state:
    type: bool
    default: 'False'
    description: expected_state (from the check's parameters).
  duration:
    type: string
    description: duration (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
