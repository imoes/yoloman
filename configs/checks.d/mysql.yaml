name: mysql
fqcn: checkmk.mysql
collection: checkmk
short_description: MySQL Version %s
description: 'Checkmk check ''mysql'' (service: MySQL Version %s), translated to a
  read-only on-host Starlark check module.'
options:
  plugin:
    type: string
    default: mysql
    description: plugin (from the check's parameters).
  total:
    type: string
    description: total (from the check's parameters).
  running:
    type: string
    description: running (from the check's parameters).
  connections:
    type: string
    description: connections (from the check's parameters).
  average:
    type: number
    default: '0'
    description: average (from the check's parameters).
  perc_used:
    type: string
    description: perc_used (from the check's parameters).
  perc_conn_threads:
    type: string
    description: perc_conn_threads (from the check's parameters).
  wsrep_sst_donor:
    type: string
    default: ''
    description: wsrep_sst_donor (from the check's parameters).
  invsize:
    type: string
    description: invsize (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
