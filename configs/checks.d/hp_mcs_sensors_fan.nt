name: hp_mcs_sensors_fan
fqcn: checkmk.hp_mcs_sensors_fan
collection: checkmk
short_description: Sensor %s
description: Checkmk check 'hp_mcs_sensors_fan' (service: Sensor %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    lower:
        type: string
        description: lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
