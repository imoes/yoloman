name: cisco_fru_power
fqcn: checkmk.cisco_fru_power
collection: checkmk
short_description: FRU Power %s
description: 'Checkmk check ''cisco_fru_power'' (service: FRU Power %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  snmp_version:
    type: string
    default: 2c
    description: snmp_version (from the check's parameters).
  oid_end:
    type: string
    default: ''
    description: oid_end (from the check's parameters).
  name:
    type: string
    default: ''
    description: name (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
