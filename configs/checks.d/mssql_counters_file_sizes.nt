name: mssql_counters_file_sizes
fqcn: checkmk.mssql_counters_file_sizes
collection: checkmk
short_description: MSSQL %s File Sizes
description: Monitoring check 'mssql_counters_file_sizes' (service: MSSQL %s File Sizes), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default:
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    instance:
        type: string
        default:
        description: instance (from the check's parameters).
    data_files:
        type: string
        description: data_files (from the check's parameters).
    log_files:
        type: string
        description: log_files (from the check's parameters).
    log_files_used:
        type: string
        description: log_files_used (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
