name: bluenet2_powerrail
fqcn: checkmk.bluenet2_powerrail
collection: checkmk
short_description: Inlet %s
description: Checkmk check 'bluenet2_powerrail' (service: Inlet %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    el_inphase_levels:
        type: list
        description: el_inphase_levels (from the check's parameters).
    differential_current_ac:
        type: list
        description: differential_current_ac (from the check's parameters).
    differential_current_dc:
        type: list
        description: differential_current_dc (from the check's parameters).
    ups_outphase_levels:
        type: list
        description: ups_outphase_levels (from the check's parameters).
    temp_levels:
        type: list
        description: temp_levels (from the check's parameters).
    humidity_levels:
        type: list
        description: humidity_levels (from the check's parameters).
    humidity_levels_lower:
        type: list
        description: humidity_levels_lower (from the check's parameters).
    check_type:
        type: string
        default: phases
        description: check_type (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
