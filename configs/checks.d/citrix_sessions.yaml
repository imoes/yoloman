name: citrix_sessions
fqcn: checkmk.citrix_sessions
collection: checkmk
short_description: Citrix Sessions
description: 'Checkmk check ''citrix_sessions'' (service: Citrix Sessions), translated
  to a read-only on-host Starlark check module.'
options:
  total:
    type: list
    description: total (from the check's parameters).
  active:
    type: list
    description: active (from the check's parameters).
  inactive:
    type: list
    description: inactive (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
