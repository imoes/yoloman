name: checkpoint_tunnels
fqcn: checkmk.checkpoint_tunnels
collection: checkmk
short_description: Tunnel %s
description: Monitoring check 'checkpoint_tunnels' (service: Tunnel %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
