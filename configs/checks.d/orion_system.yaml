name: orion_system
fqcn: checkmk.orion_system
collection: checkmk
short_description: Temperature %s
description: 'Checkmk check ''orion_system'' (service: Temperature %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  _plugin:
    type: string
    default: orion_system_temp
    description: _plugin (from the check's parameters).
  warn:
    type: number
    default: '35'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '40'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
