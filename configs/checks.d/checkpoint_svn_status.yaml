name: checkpoint_svn_status
fqcn: checkmk.checkpoint_svn_status
collection: checkmk
short_description: SVN Status
description: 'Checkmk check ''checkpoint_svn_status'' (service: SVN Status), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
