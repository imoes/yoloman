name: mssql_transactionlogs
fqcn: checkmk.mssql_transactionlogs
collection: checkmk
short_description: MSSQL Transactionlog %s
description: 'Checkmk check ''mssql_transactionlogs'' (service: MSSQL Transactionlog
  %s), translated to a read-only on-host Starlark check module.'
options:
  summarize_transactionlogs:
    type: string
    description: summarize_transactionlogs (from the check's parameters).
  used_levels:
    type: string
    description: used_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
