name: postgres_bloat
fqcn: checkmk.postgres_bloat
collection: checkmk
short_description: PostgreSQL Bloat %s
description: Checkmk check 'postgres_bloat' (service: PostgreSQL Bloat %s), translated to a read-only on-host Starlark check module.
options:
    table_bloat_perc:
        type: list
        description: table_bloat_perc (from the check's parameters).
    index_bloat_perc:
        type: list
        description: index_bloat_perc (from the check's parameters).
    table_bloat_abs:
        type: list
        description: table_bloat_abs (from the check's parameters).
    index_bloat_abs:
        type: list
        description: index_bloat_abs (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
