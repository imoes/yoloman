name: ovs_bonding
fqcn: checkmk.ovs_bonding
collection: checkmk
short_description: OVS Bonding interface %s
description: Checkmk check 'ovs_bonding' (service: OVS Bonding interface %s), translated to a read-only on-host Starlark check module.
options:
    bonding_mode_states:
        type: string
        description: bonding_mode_states (from the check's parameters).
    ieee_302_3ad_agg_id_missmatch_state:
        type: number
        default: 1
        description: ieee_302_3ad_agg_id_missmatch_state (from the check's parameters).
    primary:
        type: string
        default: None
        description: primary (from the check's parameters).
    expect_active:
        type: string
        default: ignore
        description: expect_active (from the check's parameters).
    expected_interfaces:
        type: string
        description: expected_interfaces (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
