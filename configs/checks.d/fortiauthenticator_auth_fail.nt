name: fortiauthenticator_auth_fail
fqcn: checkmk.fortiauthenticator_auth_fail
collection: checkmk
short_description: Authentication Failures
description: Checkmk check 'fortiauthenticator_auth_fail' (service: Authentication Failures), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    auth_fails:
        type: list
        description: auth_fails (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
