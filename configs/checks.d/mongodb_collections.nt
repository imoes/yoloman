name: mongodb_collections
fqcn: checkmk.mongodb_collections
collection: checkmk
short_description: MongoDB Collection: %s
description: Monitoring check 'mongodb_collections' (service: MongoDB Collection: %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 27017
        description: port (from the check's parameters).
    username:
        type: string
        default:
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    authdb:
        type: string
        default: admin
        description: authdb (from the check's parameters).
    levels_size:
        type: string
        description: levels_size (from the check's parameters).
    levels_storageSize:
        type: string
        description: levels_storageSize (from the check's parameters).
    levels_totalIndexSize:
        type: string
        description: levels_totalIndexSize (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
