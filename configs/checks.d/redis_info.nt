name: redis_info
fqcn: checkmk.redis_info
collection: checkmk
short_description: Redis %s Server Info
description: Checkmk check 'redis_info' (service: Redis %s Server Info), translated to a read-only on-host Starlark check module.
options:
    expected_mode:
        type: string
        description: expected_mode (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
