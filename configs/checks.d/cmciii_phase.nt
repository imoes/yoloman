name: cmciii_phase
fqcn: checkmk.cmciii_phase
collection: checkmk
short_description: Input %s
description: Checkmk check 'cmciii_phase' (service: Input %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    use_sensor_description:
        type: bool
        default: False
        description: use_sensor_description (from the check's parameters).
    _item_key:
        type: string
        default:
        description: _item_key (from the check's parameters).
    current_warn:
        type: string
        description: current_warn (from the check's parameters).
    current_crit:
        type: string
        description: current_crit (from the check's parameters).
    voltage_warn:
        type: string
        description: voltage_warn (from the check's parameters).
    voltage_crit:
        type: string
        description: voltage_crit (from the check's parameters).
    frequency_warn:
        type: string
        description: frequency_warn (from the check's parameters).
    frequency_crit:
        type: string
        description: frequency_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
