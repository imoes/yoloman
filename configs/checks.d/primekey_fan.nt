name: primekey_fan
fqcn: checkmk.primekey_fan
collection: checkmk
short_description: PrimeKey Fan %s
description: Checkmk check 'primekey_fan' (service: PrimeKey Fan %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
