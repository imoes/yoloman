name: sap_hana_events
fqcn: checkmk.sap_hana_events
collection: checkmk
short_description: SAP HANA Events %s
description: Checkmk check 'sap_hana_events' (service: SAP HANA Events %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default: SYSTEM
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    port:
        type: string
        description: port (from the check's parameters).
    hdbsql_bin:
        type: string
        default:
        description: hdbsql_bin (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
