name: ibm_mq_channels
fqcn: checkmk.ibm_mq_channels
collection: checkmk
short_description: IBM MQ Channel %s
description: Monitoring check 'ibm_mq_channels' (service: IBM MQ Channel %s), translated to a read-only on-host Starlark check module.
options:
    mapped_states:
        type: string
        description: mapped_states (from the check's parameters).
    mapped_states_default:
        type: string
        description: mapped_states_default (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
