name: sap_hana_data_volume
fqcn: checkmk.sap_hana_data_volume
collection: checkmk
short_description: SAP HANA Volume %s
description: Monitoring check 'sap_hana_data_volume' (service: SAP HANA Volume %s), translated to a read-only on-host Starlark check module.
options:
    hdbsql:
        type: string
        default: hdbsql
        description: hdbsql (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 30015
        description: port (from the check's parameters).
    user:
        type: string
        default: SYSTEM
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
