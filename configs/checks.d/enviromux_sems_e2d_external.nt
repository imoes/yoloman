name: enviromux_sems_e2d_external
fqcn: checkmk.enviromux_sems_e2d_external
collection: checkmk
short_description: Sensor External %s
description: Monitoring check 'enviromux_sems_e2d_external' (service: Sensor External %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_lower:
        type: string
        description: levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
