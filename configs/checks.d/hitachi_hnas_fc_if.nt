name: hitachi_hnas_fc_if
fqcn: checkmk.hitachi_hnas_fc_if
collection: checkmk
short_description: Interface FC %s
description: Monitoring check 'hitachi_hnas_fc_if' (service: Interface FC %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
