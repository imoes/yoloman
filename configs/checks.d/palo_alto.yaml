name: palo_alto
fqcn: checkmk.palo_alto
collection: checkmk
short_description: Palo Alto State
description: 'Checkmk check ''palo_alto'' (service: Palo Alto State), translated to
  a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
