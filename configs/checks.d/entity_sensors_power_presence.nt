name: entity_sensors_power_presence
fqcn: checkmk.entity_sensors_power_presence
collection: checkmk
short_description: Power %s
description: Monitoring check 'entity_sensors_power_presence' (service: Power %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    power_off_criticality:
        type: number
        default: 1
        description: power_off_criticality (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
