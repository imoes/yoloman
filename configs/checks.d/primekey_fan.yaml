name: primekey_fan
fqcn: checkmk.primekey_fan
collection: checkmk
short_description: PrimeKey Fan %s
description: 'Checkmk check ''primekey_fan'' (service: PrimeKey Fan %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  lower:
    type: list
    description: lower (from the check's parameters).
  output_metrics:
    type: bool
    default: 'True'
    description: output_metrics (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
