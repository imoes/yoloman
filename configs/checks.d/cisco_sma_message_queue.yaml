name: cisco_sma_message_queue
fqcn: checkmk.cisco_sma_message_queue
collection: checkmk
short_description: Queue
description: 'Checkmk check ''cisco_sma_message_queue'' (service: Queue), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels_oldest_message_age:
    type: string
    description: levels_oldest_message_age (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
