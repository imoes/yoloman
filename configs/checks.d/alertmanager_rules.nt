name: alertmanager_rules
fqcn: checkmk.alertmanager_rules
collection: checkmk
short_description: Alertmanager Check
description: Checkmk check 'alertmanager_rules' (service: Alert Rule %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 9090
        description: port (from the check's parameters).
    min_amount_rules:
        type: number
        default: 3
        description: min_amount_rules (from the check's parameters).
    no_group_services:
        type: list
        description: no_group_services (from the check's parameters).
    alert_remapping:
        type: string
        description: alert_remapping (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
