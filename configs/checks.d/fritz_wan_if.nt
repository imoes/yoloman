name: fritz_wan_if
fqcn: checkmk.fritz_wan_if
collection: checkmk
short_description: Interface %s
description: Checkmk check 'fritz_wan_if' (service: Interface %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: fritz.box
        description: host (from the check's parameters).
    port:
        type: number
        default: 49000
        description: port (from the check's parameters).
    warn_speed_in:
        type: number
        default: 90
        description: warn_speed_in (from the check's parameters).
    crit_speed_in:
        type: number
        default: 95
        description: crit_speed_in (from the check's parameters).
    warn_speed_out:
        type: number
        default: 90
        description: warn_speed_out (from the check's parameters).
    crit_speed_out:
        type: number
        default: 95
        description: crit_speed_out (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
