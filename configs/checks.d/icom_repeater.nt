name: icom_repeater
fqcn: checkmk.icom_repeater
collection: checkmk
short_description: Repeater Info
description: Checkmk check 'icom_repeater' (service: Repeater Info), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
    levels_upper:
        type: list
        description: levels_upper (from the check's parameters).
    pll_levels:
        type: string
        description: pll_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
