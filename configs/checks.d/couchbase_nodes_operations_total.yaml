name: couchbase_nodes_operations_total
fqcn: checkmk.couchbase_nodes_operations_total
collection: checkmk
short_description: Couchbase Total Operations
description: 'Checkmk check ''couchbase_nodes_operations_total'' (service: Couchbase
  Total Operations), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: number
    default: '8091'
    description: port (from the check's parameters).
  username:
    type: string
    default: Administrator
    description: username (from the check's parameters).
  password:
    type: string
    default: password
    description: password (from the check's parameters).
  warn:
    type: number
    default: '80.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90.0'
    description: crit (from the check's parameters).
  ops:
    type: list
    description: ops (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
