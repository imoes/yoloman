name: mysql_sessions
fqcn: checkmk.mysql_sessions
collection: checkmk
short_description: MySQL Sessions %s
description: Checkmk check 'mysql_sessions' (service: MySQL Sessions %s), translated to a read-only on-host Starlark check module.
options:
    total:
        type: string
        description: total (from the check's parameters).
    running:
        type: string
        description: running (from the check's parameters).
    connections:
        type: string
        description: connections (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
