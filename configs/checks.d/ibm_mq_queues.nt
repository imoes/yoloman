name: ibm_mq_queues
fqcn: checkmk.ibm_mq_queues
collection: checkmk
short_description: IBM MQ Queue %s
description: Monitoring check 'ibm_mq_queues' (service: IBM MQ Queue %s), translated to a read-only on-host Starlark check module.
options:
    curdepth:
        type: string
        description: curdepth (from the check's parameters).
    curdepth_perc:
        type: string
        description: curdepth_perc (from the check's parameters).
    msgage:
        type: string
        description: msgage (from the check's parameters).
    lgetage:
        type: string
        description: lgetage (from the check's parameters).
    lputage:
        type: string
        description: lputage (from the check's parameters).
    ipprocs:
        type: string
        description: ipprocs (from the check's parameters).
    opprocs:
        type: string
        description: opprocs (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
