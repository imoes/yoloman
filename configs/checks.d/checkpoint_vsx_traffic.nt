name: checkpoint_vsx_traffic
fqcn: checkmk.checkpoint_vsx_traffic
collection: checkmk
short_description: VS %s Traffic
description: Monitoring check 'checkpoint_vsx_traffic' (service: VS %s Traffic), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    bytes_accepted:
        type: list
        description: bytes_accepted (from the check's parameters).
    bytes_dropped:
        type: list
        description: bytes_dropped (from the check's parameters).
    bytes_rejected:
        type: list
        description: bytes_rejected (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
