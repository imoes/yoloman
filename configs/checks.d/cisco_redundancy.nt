name: cisco_redundancy
fqcn: checkmk.cisco_redundancy
collection: checkmk
short_description: Redundancy Framework Status
description: Checkmk check 'cisco_redundancy' (service: Redundancy Framework Status), translated to a read-only on-host Starlark check module.
options:
    init_states:
        type: string
        description: init_states (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    snmp_version:
        type: string
        default: 2c
        description: snmp_version (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
