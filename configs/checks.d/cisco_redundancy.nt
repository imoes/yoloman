name: cisco_redundancy
fqcn: checkmk.cisco_redundancy
collection: checkmk
short_description: Redundancy Framework Status
description: Monitoring check 'cisco_redundancy' (service: Redundancy Framework Status), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    init_states:
        type: list
        description: init_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
