name: mcafee_webgateway_misc
fqcn: checkmk.mcafee_webgateway_misc
collection: checkmk
short_description: The McAfee Web Gateway has been rebranded to Skyhigh Secure Web
  Gateway with its release 12.2.2.
description: 'Checkmk check ''mcafee_webgateway_misc'' (service: Web gateway miscellaneous),
  translated to a read-only on-host Starlark check module.'
options:
  clients:
    type: string
    description: clients (from the check's parameters).
  network_sockets:
    type: string
    description: network_sockets (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: number
    default: '443'
    description: port (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
