name: nginx_status
fqcn: checkmk.nginx_status
collection: checkmk
short_description: Nginx %s Status
description: 'Checkmk check ''nginx_status'' (service: Nginx %s Status), translated
  to a read-only on-host Starlark check module.'
options:
  ports:
    type: list
    description: ports (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  active_connections:
    type: string
    description: active_connections (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
