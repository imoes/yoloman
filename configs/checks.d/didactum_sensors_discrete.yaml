name: didactum_sensors_discrete
fqcn: checkmk.didactum_sensors_discrete
collection: checkmk
short_description: Discrete sensor %s
description: 'Monitoring check ''didactum_sensors_discrete'' (service: Discrete sensor
  %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
