name: hp_msa_controller_io
fqcn: checkmk.hp_msa_controller_io
collection: checkmk
short_description: Controller IO %s
description: 'Monitoring check ''hp_msa_controller_io'' (service: Controller IO %s),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
