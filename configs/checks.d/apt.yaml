name: apt
fqcn: checkmk.apt
collection: checkmk
short_description: APT Updates
description: 'Checkmk check ''apt'' (service: APT Updates), translated to a read-only
  on-host Starlark check module.'
options:
  normal:
    type: number
    default: '1'
    description: normal (from the check's parameters).
  removals:
    type: number
    default: '1'
    description: removals (from the check's parameters).
  security:
    type: number
    default: '2'
    description: security (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
