name: entersekt
fqcn: checkmk.entersekt
collection: checkmk
short_description: Entersekt Server Status
description: Checkmk check 'entersekt' (service: Entersekt Server Status), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 100
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 200
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
