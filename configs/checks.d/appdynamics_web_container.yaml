name: appdynamics_web_container
fqcn: checkmk.appdynamics_web_container
collection: checkmk
short_description: AppDynamics Web Container %s
description: 'Checkmk check ''appdynamics_web_container'' (service: AppDynamics Web
  Container %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: number
    default: '8080'
    description: port (from the check's parameters).
  user:
    type: string
    description: user (from the check's parameters).
  password:
    type: string
    description: password (from the check's parameters).
  app:
    type: string
    description: app (from the check's parameters).
  tier:
    type: string
    description: tier (from the check's parameters).
  warn:
    type: number
    default: '80'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
