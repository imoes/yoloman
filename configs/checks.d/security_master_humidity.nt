name: security_master_humidity
fqcn: checkmk.security_master_humidity
collection: checkmk
short_description: Sensor %s
description: Checkmk check 'security_master_humidity' (service: Sensor %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
