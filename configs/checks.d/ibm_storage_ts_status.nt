name: ibm_storage_ts_status
fqcn: checkmk.ibm_storage_ts_status
collection: checkmk
short_description: Status
description: Checkmk check 'ibm_storage_ts_status' (service: Status), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    _service_description:
        type: string
        default:
        description: _service_description (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
