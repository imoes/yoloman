name: postgres_stats
fqcn: checkmk.postgres_stats
collection: checkmk
short_description: PostgreSQL %s
description: Monitoring check 'postgres_stats' (service: PostgreSQL %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default:
        description: host (from the check's parameters).
    port:
        type: number
        default: 5432
        description: port (from the check's parameters).
    user:
        type: string
        default: postgres
        description: user (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
