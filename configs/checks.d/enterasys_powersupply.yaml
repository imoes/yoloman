name: enterasys_powersupply
fqcn: checkmk.enterasys_powersupply
collection: checkmk
short_description: PSU %s
description: 'Checkmk check ''enterasys_powersupply'' (service: PSU %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  redundancy_ok_states:
    type: list
    description: redundancy_ok_states (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
