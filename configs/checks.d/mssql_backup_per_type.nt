name: mssql_backup_per_type
fqcn: checkmk.mssql_backup_per_type
collection: checkmk
short_description: MSSQL %s Backup
description: Checkmk check 'mssql_backup_per_type' (service: MSSQL %s Backup), translated to a read-only on-host Starlark check module.
options:
    server:
        type: string
        default: localhost
        description: server (from the check's parameters).
    instance:
        type: string
        default:
        description: instance (from the check's parameters).
    port:
        type: number
        default: 1433
        description: port (from the check's parameters).
    username:
        type: string
        default:
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
