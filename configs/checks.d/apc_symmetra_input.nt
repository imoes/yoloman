name: apc_symmetra_input
fqcn: checkmk.apc_symmetra_input
collection: checkmk
short_description: Phase %s
description: Checkmk check 'apc_symmetra_input' (service: Phase %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    voltage_warn:
        type: number
        default: 240.0
        description: voltage_warn (from the check's parameters).
    voltage_crit:
        type: number
        default: 250.0
        description: voltage_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
