name: apc_symmetra_input
fqcn: checkmk.apc_symmetra_input
collection: checkmk
short_description: Phase %s
description: Monitoring check 'apc_symmetra_input' (service: Phase %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    voltage:
        type: string
        description: voltage (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
