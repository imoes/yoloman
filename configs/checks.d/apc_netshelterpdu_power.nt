name: apc_netshelterpdu_power
fqcn: checkmk.apc_netshelterpdu_power
collection: checkmk
short_description: PDU %s
description: Monitoring check 'apc_netshelterpdu_power' (service: PDU %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    output_load:
        type: list
        description: output_load (from the check's parameters).
    warn_current:
        type: string
        description: warn_current (from the check's parameters).
    crit_current:
        type: string
        description: crit_current (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
