name: apc_netshelterpdu_power
fqcn: checkmk.apc_netshelterpdu_power
collection: checkmk
short_description: PDU %s
description: Checkmk check 'apc_netshelterpdu_power' (service: PDU %s), translated to a read-only on-host Starlark check module.
options:
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
    output_load_warn:
        type: number
        default: 80
        description: output_load_warn (from the check's parameters).
    output_load_crit:
        type: number
        default: 90
        description: output_load_crit (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
