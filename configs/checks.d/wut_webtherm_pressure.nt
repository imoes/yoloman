name: wut_webtherm_pressure
fqcn: checkmk.wut_webtherm_pressure
collection: checkmk
short_description: Pressure %s
description: Monitoring check 'wut_webtherm_pressure' (service: Pressure %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
