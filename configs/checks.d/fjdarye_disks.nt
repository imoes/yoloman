name: fjdarye_disks
fqcn: checkmk.fjdarye_disks
collection: checkmk
short_description: Disk %s
description: Checkmk check 'fjdarye_disks' (service: Disk %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    expected_state:
        type: string
        default:
        description: expected_state (from the check's parameters).
    use_device_states:
        type: bool
        default: False
        description: use_device_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
