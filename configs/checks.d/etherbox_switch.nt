name: etherbox_switch
fqcn: checkmk.etherbox_switch
collection: checkmk
short_description: Sensor %s
description: Checkmk check 'etherbox_switch' (service: Sensor %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: number
        default: 25.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 30.0
        description: crit (from the check's parameters).
    state:
        type: string
        default: ignore
        description: state (from the check's parameters).
    smoke_handling:
        type: list
        description: smoke_handling (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
