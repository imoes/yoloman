name: lparstat_aix_cpu_util
fqcn: checkmk.lparstat_aix_cpu_util
collection: checkmk
short_description: CPU utilization
description: 'Checkmk check ''lparstat_aix_cpu_util'' (service: CPU utilization),
  translated to a read-only on-host Starlark check module.'
options:
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
