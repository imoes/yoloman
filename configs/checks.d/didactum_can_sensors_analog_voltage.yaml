name: didactum_can_sensors_analog_voltage
fqcn: checkmk.didactum_can_sensors_analog_voltage
collection: checkmk
short_description: Phase CAN %s
description: 'Monitoring check ''didactum_can_sensors_analog_voltage'' (service: Phase
  CAN %s), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  levels_lower:
    type: string
    description: levels_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
