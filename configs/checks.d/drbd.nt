name: drbd
fqcn: checkmk.drbd
collection: checkmk
short_description: DRBD %s status
description: Checkmk check 'drbd' (service: DRBD %s status), translated to a read-only on-host Starlark check module.
options:
    roles:
        type: string
        description: roles (from the check's parameters).
    roles_inventory:
        type: string
        description: roles_inventory (from the check's parameters).
    diskstates:
        type: string
        description: diskstates (from the check's parameters).
    diskstates_inventory:
        type: string
        description: diskstates_inventory (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
