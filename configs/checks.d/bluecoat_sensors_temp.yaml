name: bluecoat_sensors_temp
fqcn: checkmk.bluecoat_sensors_temp
collection: checkmk
short_description: Temperature %s
description: 'Checkmk check ''bluecoat_sensors_temp'' (service: Temperature %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  warn:
    type: number
    default: '30'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '40'
    description: crit (from the check's parameters).
  lower_warn:
    type: string
    description: lower_warn (from the check's parameters).
  lower_crit:
    type: string
    description: lower_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
