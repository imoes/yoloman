name: msexch_database
fqcn: checkmk.msexch_database
collection: checkmk
short_description: Exchange Database %s
description: 'Checkmk check ''msexch_database'' (service: Exchange Database %s), translated
  to a read-only on-host Starlark check module.'
options:
  read_attached_latency_s:
    type: string
    description: read_attached_latency_s (from the check's parameters).
  read_recovery_latency_s:
    type: string
    description: read_recovery_latency_s (from the check's parameters).
  write_latency_s:
    type: string
    description: write_latency_s (from the check's parameters).
  log_latency_s:
    type: string
    description: log_latency_s (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
