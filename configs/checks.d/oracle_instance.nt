name: oracle_instance
fqcn: checkmk.oracle_instance
collection: checkmk
short_description: ORA %s Instance
description: Checkmk check 'oracle_instance' (service: ORA %s Instance), translated to a read-only on-host Starlark check module.
options:
    oracle_home:
        type: string
        description: oracle_home (from the check's parameters).
    logins:
        type: number
        default: 2
        description: logins (from the check's parameters).
    noforcelogging:
        type: number
        default: 1
        description: noforcelogging (from the check's parameters).
    noarchivelog:
        type: number
        default: 1
        description: noarchivelog (from the check's parameters).
    primarynotopen:
        type: number
        default: 2
        description: primarynotopen (from the check's parameters).
    archivelog:
        type: number
        default: 0
        description: archivelog (from the check's parameters).
    forcelogging:
        type: number
        default: 0
        description: forcelogging (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
