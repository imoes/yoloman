name: janitza_umg
fqcn: checkmk.janitza_umg
collection: checkmk
short_description: Input %s
description: 'Checkmk check ''janitza_umg'' (service: Input %s), translated to a read-only
  on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  voltage:
    type: string
    description: voltage (from the check's parameters).
  current:
    type: string
    description: current (from the check's parameters).
  power:
    type: string
    description: power (from the check's parameters).
  appower:
    type: string
    description: appower (from the check's parameters).
  energy:
    type: string
    description: energy (from the check's parameters).
  cosphi:
    type: string
    description: cosphi (from the check's parameters).
  levels_lower:
    type: list
    description: levels_lower (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
