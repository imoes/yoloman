name: emc_isilon_names
fqcn: checkmk.emc_isilon_names
collection: checkmk
short_description: Isilon Info
description: Checkmk check 'emc_isilon_names' (service: Isilon Info), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
