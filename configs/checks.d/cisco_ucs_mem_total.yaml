name: cisco_ucs_mem_total
fqcn: checkmk.cisco_ucs_mem_total
collection: checkmk
short_description: Memory total
description: 'Checkmk check ''cisco_ucs_mem_total'' (service: Memory total), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
