name: allnet_ip_sensoric_pressure
fqcn: checkmk.allnet_ip_sensoric_pressure
collection: checkmk
short_description: Pressure %s
description: Checkmk check 'allnet_ip_sensoric_pressure' (service: Pressure %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    snmp_version:
        type: string
        default: 2c
        description: snmp_version (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
