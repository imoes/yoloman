name: fjdarye_pools
fqcn: checkmk.fjdarye_pools
collection: checkmk
short_description: Thin Provisioning Pool %s
description: Monitoring check 'fjdarye_pools' (service: Thin Provisioning Pool %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
