name: fjdarye_pools
fqcn: checkmk.fjdarye_pools
collection: checkmk
short_description: Thin Provisioning Pool %s
description: Checkmk check 'fjdarye_pools' (service: Thin Provisioning Pool %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    warn:
        type: number
        default: 90
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 95
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
