name: adva_fsp_temp
fqcn: checkmk.adva_fsp_temp
collection: checkmk
short_description: Temperature %s
description: Monitoring check 'adva_fsp_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels_upper_warn:
        type: number
        default: 25
        description: levels_upper_warn (from the check's parameters).
    levels_upper_crit:
        type: number
        default: 40
        description: levels_upper_crit (from the check's parameters).
    levels_lower_warn:
        type: number
        default: -10
        description: levels_lower_warn (from the check's parameters).
    levels_lower_crit:
        type: number
        default: -20
        description: levels_lower_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
