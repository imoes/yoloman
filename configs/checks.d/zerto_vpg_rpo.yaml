name: zerto_vpg_rpo
fqcn: checkmk.zerto_vpg_rpo
collection: checkmk
short_description: Zerto VPG RPO %s
description: 'Checkmk check ''zerto_vpg_rpo'' (service: Zerto VPG RPO %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: number
    default: '443'
    description: port (from the check's parameters).
  token:
    type: string
    default: ''
    description: token (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
