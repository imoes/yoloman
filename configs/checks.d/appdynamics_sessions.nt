name: appdynamics_sessions
fqcn: checkmk.appdynamics_sessions
collection: checkmk
short_description: AppDynamics Sessions %s
description: Checkmk check 'appdynamics_sessions' (service: AppDynamics Sessions %s), translated to a read-only on-host Starlark check module.
options:
    levels_upper:
        type: string
        description: levels_upper (from the check's parameters).
    levels_lower:
        type: string
        description: levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
