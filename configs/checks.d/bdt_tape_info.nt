name: bdt_tape_info
fqcn: checkmk.bdt_tape_info
collection: checkmk
short_description: Tape Library Info
description: Checkmk check 'bdt_tape_info' (service: Tape Library Info), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
