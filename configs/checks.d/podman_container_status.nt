name: podman_container_status
fqcn: checkmk.podman_container_status
collection: checkmk
short_description: Status
description: Checkmk check 'podman_container_status' (service: Status), translated to a read-only on-host Starlark check module.
options:
    created:
        type: number
        default: 2
        description: created (from the check's parameters).
    running:
        type: number
        default: 0
        description: running (from the check's parameters).
    paused:
        type: number
        default: 2
        description: paused (from the check's parameters).
    restarting:
        type: number
        default: 2
        description: restarting (from the check's parameters).
    removing:
        type: number
        default: 2
        description: removing (from the check's parameters).
    exited_with_zero:
        type: number
        default: 0
        description: exited_with_zero (from the check's parameters).
    exited_with_non_zero:
        type: number
        default: 2
        description: exited_with_non_zero (from the check's parameters).
    dead:
        type: number
        default: 2
        description: dead (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
