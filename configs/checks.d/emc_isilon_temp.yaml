name: emc_isilon_temp
fqcn: checkmk.emc_isilon_temp
collection: checkmk
short_description: Temperature %s
description: 'Checkmk check ''emc_isilon_temp'' (service: Temperature %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  is_cpu:
    type: bool
    default: 'False'
    description: is_cpu (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
