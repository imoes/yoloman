name: juniper_screenos_cpu
fqcn: checkmk.juniper_screenos_cpu
collection: checkmk
short_description: CPU utilization
description: Checkmk check 'juniper_screenos_cpu' (service: CPU utilization), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    util:
        type: string
        description: util (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
