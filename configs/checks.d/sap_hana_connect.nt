name: sap_hana_connect
fqcn: checkmk.sap_hana_connect
collection: checkmk
short_description: SAP HANA CONNECT %s
description: Monitoring check 'sap_hana_connect' (service: SAP HANA CONNECT %s), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default: SYSTEM
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
