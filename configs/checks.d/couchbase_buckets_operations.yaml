name: couchbase_buckets_operations
fqcn: checkmk.couchbase_buckets_operations
collection: checkmk
short_description: Couchbase Bucket %s Operations
description: 'Checkmk check ''couchbase_buckets_operations'' (service: Couchbase Bucket
  %s Operations), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: number
    default: '8091'
    description: port (from the check's parameters).
  username:
    type: string
    default: Administrator
    description: username (from the check's parameters).
  password:
    type: string
    default: password
    description: password (from the check's parameters).
  ops:
    type: string
    description: ops (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
