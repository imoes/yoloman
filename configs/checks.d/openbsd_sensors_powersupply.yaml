name: openbsd_sensors_powersupply
fqcn: checkmk.openbsd_sensors_powersupply
collection: checkmk
short_description: Powersupply %s
description: 'Checkmk check ''openbsd_sensors_powersupply'' (service: Powersupply
  %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
