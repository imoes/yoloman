name: synology_raid
fqcn: checkmk.synology_raid
collection: checkmk
short_description: Raid %s
description: Checkmk check 'synology_raid' (service: Raid %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
