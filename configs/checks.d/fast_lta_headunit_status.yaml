name: fast_lta_headunit_status
fqcn: checkmk.fast_lta_headunit_status
collection: checkmk
short_description: Fast LTA Headunit Status
description: 'Checkmk check ''fast_lta_headunit_status'' (service: Fast LTA Headunit
  Status), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
