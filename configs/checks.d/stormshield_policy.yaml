name: stormshield_policy
fqcn: checkmk.stormshield_policy
collection: checkmk
short_description: Policy %s
description: 'Monitoring check ''stormshield_policy'' (service: Policy %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
