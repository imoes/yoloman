name: ibm_svc_host
fqcn: checkmk.ibm_svc_host
collection: checkmk
short_description: Hosts
description: 'Checkmk check ''ibm_svc_host'' (service: Hosts), translated to a read-only
  on-host Starlark check module.'
options:
  active_hosts:
    type: string
    description: active_hosts (from the check's parameters).
  inactive_hosts:
    type: string
    description: inactive_hosts (from the check's parameters).
  degraded_hosts:
    type: string
    description: degraded_hosts (from the check's parameters).
  offline_hosts:
    type: string
    description: offline_hosts (from the check's parameters).
  other_hosts:
    type: string
    description: other_hosts (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
