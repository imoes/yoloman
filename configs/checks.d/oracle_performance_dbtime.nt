name: oracle_performance_dbtime
fqcn: checkmk.oracle_performance_dbtime
collection: checkmk
short_description: ORA %s Performance DB-Time
description: Checkmk check 'oracle_performance_dbtime' (service: ORA %s Performance DB-Time), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 1521
        description: port (from the check's parameters).
    sid:
        type: string
        default: ORCL
        description: sid (from the check's parameters).
    user:
        type: string
        description: user (from the check's parameters).
    password:
        type: string
        description: password (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
