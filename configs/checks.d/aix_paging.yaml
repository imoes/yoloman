name: aix_paging
fqcn: checkmk.aix_paging
collection: checkmk
short_description: Page Space %s
description: 'Checkmk check ''aix_paging'' (service: Page Space %s), translated to
  a read-only on-host Starlark check module.'
options:
  levels:
    type: string
    description: levels (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
