name: oracle_diva_csm_archive
fqcn: checkmk.oracle_diva_csm_archive
collection: checkmk
short_description: DIVA Status %s
description: Monitoring check 'oracle_diva_csm_archive' (service: DIVA Status %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
