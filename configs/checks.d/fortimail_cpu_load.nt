name: fortimail_cpu_load
fqcn: checkmk.fortimail_cpu_load
collection: checkmk
short_description: CPU load
description: Monitoring check 'fortimail_cpu_load' (service: CPU load), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    cpu_load:
        type: string
        description: cpu_load (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
