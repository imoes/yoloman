name: oracle_instance_uptime
fqcn: checkmk.oracle_instance_uptime
collection: checkmk
short_description: ORA %s Uptime
description: Monitoring check 'oracle_instance_uptime' (service: ORA %s Uptime), translated to a read-only on-host Starlark check module.
options:
    oracle_home:
        type: string
        description: oracle_home (from the check's parameters).
    min:
        type: string
        description: min (from the check's parameters).
    max:
        type: string
        description: max (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
