name: by_ssh
fqcn: checkmk.by_ssh
collection: checkmk
short_description: ''
description: Checkmk active check 'by_ssh', translated to a read-only on-host Starlark
  check module.
options:
  command:
    type: string
    default: ''
    description: command (from the check's parameters).
  hostname:
    type: string
    default: ''
    description: hostname (from the check's parameters).
  port:
    type: string
    description: port (from the check's parameters).
  ip_version:
    type: string
    description: ip_version (from the check's parameters).
  timeout:
    type: string
    description: timeout (from the check's parameters).
  logname:
    type: string
    description: logname (from the check's parameters).
  identity:
    type: string
    description: identity (from the check's parameters).
  accept_new_host_keys:
    type: bool
    default: 'False'
    description: accept_new_host_keys (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
