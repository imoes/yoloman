name: sentry_pdu_v4
fqcn: checkmk.sentry_pdu_v4
collection: checkmk
short_description: Plug %s
description: Checkmk check 'sentry_pdu_v4' (service: Plug %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    required_state:
        type: string
        description: required_state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
