name: cephosdbluefs_db
fqcn: checkmk.cephosdbluefs_db
collection: checkmk
short_description: Ceph OSD %s DB
description: 'Checkmk check ''cephosdbluefs_db'' (service: Ceph OSD %s DB), translated
  to a read-only on-host Starlark check module.'
options:
  _checkmk_mode:
    type: string
    default: db
    description: _checkmk_mode (from the check's parameters).
  warn:
    type: number
    default: '80'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
