name: ibm_rsa_health
fqcn: checkmk.ibm_rsa_health
collection: checkmk
short_description: System health
description: 'Checkmk check ''ibm_rsa_health'' (service: System health), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
