name: safenet_hsm
fqcn: checkmk.safenet_hsm
collection: checkmk
short_description: HSM Operation Stats
description: Checkmk check 'safenet_hsm' (service: HSM Operation Stats), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    _check_name:
        type: string
        default: safenet_hsm
        description: _check_name (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
