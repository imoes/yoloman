name: diskstat_io_director
fqcn: checkmk.diskstat_io_director
collection: checkmk
short_description: Disk IO Director %s
description: 'Checkmk check ''diskstat_io_director'' (service: Disk IO Director %s),
  translated to a read-only on-host Starlark check module.'
options:
  util_warn:
    type: string
    description: util_warn (from the check's parameters).
  util_crit:
    type: string
    description: util_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
