name: hp_hh3c_ext_states
fqcn: checkmk.hp_hh3c_ext_states
collection: checkmk
short_description: Status %s
description: Checkmk check 'hp_hh3c_ext_states' (service: Status %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    admin:
        type: string
        description: admin (from the check's parameters).
    oper:
        type: string
        description: oper (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
