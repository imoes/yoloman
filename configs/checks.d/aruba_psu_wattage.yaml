name: aruba_psu_wattage
fqcn: checkmk.aruba_psu_wattage
collection: checkmk
short_description: PSU Wattage %s
description: 'Checkmk check ''aruba_psu_wattage'' (service: PSU Wattage %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels_abs_upper:
    type: string
    description: levels_abs_upper (from the check's parameters).
  levels_perc_upper:
    type: string
    description: levels_perc_upper (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
