name: solaris_services
fqcn: checkmk.solaris_services
collection: checkmk
short_description: SMF Service %s
description: Checkmk check 'solaris_services' (service: SMF Service %s), translated to a read-only on-host Starlark check module.
options:
    states:
        type: list
        description: states (from the check's parameters).
    else:
        type: number
        default: 2
        description: else (from the check's parameters).
    additional_servicenames:
        type: list
        description: additional_servicenames (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
