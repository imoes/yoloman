name: vms_diskstat_df
fqcn: checkmk.vms_diskstat_df
collection: checkmk
short_description: Filesystem %s
description: 'Monitoring check ''vms_diskstat_df'' (service: Filesystem %s), translated
  to a read-only on-host Starlark check module.'
options:
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
