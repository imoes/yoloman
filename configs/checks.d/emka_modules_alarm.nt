name: emka_modules_alarm
fqcn: checkmk.emka_modules_alarm
collection: checkmk
short_description: Alarm %s
description: Monitoring check 'emka_modules_alarm' (service: Alarm %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
