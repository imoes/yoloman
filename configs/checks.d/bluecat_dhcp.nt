name: bluecat_dhcp
fqcn: checkmk.bluecat_dhcp
collection: checkmk
short_description: DHCP
description: Checkmk check 'bluecat_dhcp' (service: DHCP), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    oper_states:
        type: string
        description: oper_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
