name: bgp_peer
fqcn: checkmk.bgp_peer
collection: checkmk
short_description: This is how an Arista BGP SNMP message is constructed:
description: Checkmk check 'bgp_peer' (service: BGP Peer %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    version:
        type: string
        default: 2c
        description: version (from the check's parameters).
    username:
        type: string
        description: username (from the check's parameters).
    authproto:
        type: string
        description: authproto (from the check's parameters).
    authpass:
        type: string
        description: authpass (from the check's parameters).
    privproto:
        type: string
        description: privproto (from the check's parameters).
    privpass:
        type: string
        description: privpass (from the check's parameters).
    admin_state_mapping:
        type: string
        description: admin_state_mapping (from the check's parameters).
    peer_state_mapping:
        type: string
        description: peer_state_mapping (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
