name: redis_info_clients
fqcn: checkmk.redis_info_clients
collection: checkmk
short_description: Redis %s Clients
description: Checkmk check 'redis_info_clients' (service: Redis %s Clients), translated to a read-only on-host Starlark check module.
options:
    connected_upper:
        type: string
        description: connected_upper (from the check's parameters).
    connected_lower:
        type: string
        description: connected_lower (from the check's parameters).
    output_upper:
        type: string
        description: output_upper (from the check's parameters).
    output_lower:
        type: string
        description: output_lower (from the check's parameters).
    input_upper:
        type: string
        description: input_upper (from the check's parameters).
    input_lower:
        type: string
        description: input_lower (from the check's parameters).
    blocked_upper:
        type: string
        description: blocked_upper (from the check's parameters).
    blocked_lower:
        type: string
        description: blocked_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
