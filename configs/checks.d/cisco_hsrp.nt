name: cisco_hsrp
fqcn: checkmk.cisco_hsrp
collection: checkmk
short_description: HSRP Group %s
description: Checkmk check 'cisco_hsrp' (service: HSRP Group %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    state:
        type: string
        default: 0
        description: state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
