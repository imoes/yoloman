name: ups_cps_outphase
fqcn: checkmk.ups_cps_outphase
collection: checkmk
short_description: UPS Output Phase %s
description: 'Checkmk check ''ups_cps_outphase'' (service: UPS Output Phase %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
