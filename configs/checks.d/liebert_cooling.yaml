name: liebert_cooling
fqcn: checkmk.liebert_cooling
collection: checkmk
short_description: '%s'
description: 'Checkmk check ''liebert_cooling'' (service: %s), translated to a read-only
  on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  min_capacity:
    type: list
    description: min_capacity (from the check's parameters).
  max_capacity:
    type: string
    description: max_capacity (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
