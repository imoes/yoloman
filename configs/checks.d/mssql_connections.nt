name: mssql_connections
fqcn: checkmk.mssql_connections
collection: checkmk
short_description: MSSQL Connections %s
description: Checkmk check 'mssql_connections' (service: MSSQL Connections %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    instance:
        type: string
        default: MSSQLSERVER
        description: instance (from the check's parameters).
    port:
        type: number
        default: 1433
        description: port (from the check's parameters).
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
