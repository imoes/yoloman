name: zertificon_mail_queues
fqcn: checkmk.zertificon_mail_queues
collection: checkmk
short_description: Zertificon Mail Queues
description: 'Checkmk check ''zertificon_mail_queues'' (service: Zertificon Mail Queues),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
