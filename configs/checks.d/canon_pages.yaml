name: canon_pages
fqcn: checkmk.canon_pages
collection: checkmk
short_description: Pages
description: 'Checkmk check ''canon_pages'' (service: Pages), translated to a read-only
  on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  pages_total_warn:
    type: number
    default: '0'
    description: pages_total_warn (from the check's parameters).
  pages_total_crit:
    type: number
    default: '0'
    description: pages_total_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
