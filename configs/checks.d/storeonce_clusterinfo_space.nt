name: storeonce_clusterinfo_space
fqcn: checkmk.storeonce_clusterinfo_space
collection: checkmk
short_description: %s
description: Monitoring check 'storeonce_clusterinfo_space' (service: %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
    username:
        type: string
        default: Admin
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    api_path:
        type: string
        default: /storeonceservices/cluster/
        description: api_path (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
