name: oracle_instance
fqcn: checkmk.oracle_instance
collection: checkmk
short_description: ORA %s Instance
description: 'Checkmk check ''oracle_instance'' (service: ORA %s Instance), translated
  to a read-only on-host Starlark check module.'
options:
  oracle_home:
    type: string
    default: ''
    description: oracle_home (from the check's parameters).
  sid:
    type: string
    default: ''
    description: sid (from the check's parameters).
  query:
    type: string
    default: SELECT * FROM v$instance
    description: query (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
