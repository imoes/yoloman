name: cisco_ucs_psu
fqcn: checkmk.cisco_ucs_psu
collection: checkmk
short_description: psu %s
description: Checkmk check 'cisco_ucs_psu' (service: psu %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
