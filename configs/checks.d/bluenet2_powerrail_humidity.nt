name: bluenet2_powerrail_humidity
fqcn: checkmk.bluenet2_powerrail_humidity
collection: checkmk
short_description: Humidity %s
description: Checkmk check 'bluenet2_powerrail_humidity' (service: Humidity %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
    warn_lower:
        type: string
        description: warn_lower (from the check's parameters).
    crit_lower:
        type: string
        description: crit_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
