name: bonding
fqcn: checkmk.bonding
collection: checkmk
short_description: Bonding Interface %s
description: Monitoring check 'bonding' (service: Bonding Interface %s), translated to a read-only on-host Starlark check module.
options:
    bonding_mode_states:
        type: string
        description: bonding_mode_states (from the check's parameters).
    primary:
        type: string
        default: None
        description: primary (from the check's parameters).
    expect_active:
        type: string
        default: ignore
        description: expect_active (from the check's parameters).
    expected_interfaces:
        type: string
        description: expected_interfaces (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
