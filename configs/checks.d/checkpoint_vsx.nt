name: checkpoint_vsx
fqcn: checkmk.checkpoint_vsx
collection: checkmk
short_description: VS %s Info
description: Checkmk check 'checkpoint_vsx' (service: VS %s Info), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    subcheck:
        type: string
        default: vsx_status
        description: subcheck (from the check's parameters).
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
