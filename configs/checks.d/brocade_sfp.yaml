name: brocade_sfp
fqcn: checkmk.brocade_sfp
collection: checkmk
short_description: SFP %s
description: 'Checkmk check ''brocade_sfp'' (service: SFP %s), translated to a read-only
  on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  version:
    type: string
    default: v2c
    description: version (from the check's parameters).
  temperature_levels:
    type: string
    description: temperature_levels (from the check's parameters).
  rx_power_levels:
    type: string
    description: rx_power_levels (from the check's parameters).
  tx_power_levels:
    type: string
    description: tx_power_levels (from the check's parameters).
  current_levels:
    type: string
    description: current_levels (from the check's parameters).
  voltage_levels:
    type: string
    description: voltage_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
