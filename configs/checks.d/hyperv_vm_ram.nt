name: hyperv_vm_ram
fqcn: checkmk.hyperv_vm_ram
collection: checkmk
short_description: Hyper-V RAM
description: Monitoring check 'hyperv_vm_ram' (service: Hyper-V RAM), translated to a read-only on-host Starlark check module.
options:
    min_ram:
        type: list
        description: min_ram (from the check's parameters).
    max_ram:
        type: list
        description: max_ram (from the check's parameters).
    check_demand:
        type: bool
        default: False
        description: check_demand (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
