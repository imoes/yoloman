name: postgres_query_duration
fqcn: checkmk.postgres_query_duration
collection: checkmk
short_description: PostgreSQL Query Duration %s
description: Checkmk check 'postgres_query_duration' (service: PostgreSQL Query Duration %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 5432
        description: port (from the check's parameters).
    user:
        type: string
        default: postgres
        description: user (from the check's parameters).
    dbname:
        type: string
        default: postgres
        description: dbname (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
