name: cmciii_can_current
fqcn: checkmk.cmciii_can_current
collection: checkmk
short_description: %s
description: Monitoring check 'cmciii_can_current' (service: %s), translated to a read-only on-host Starlark check module.
options:
    use_sensor_description:
        type: string
        description: use_sensor_description (from the check's parameters).
    _item_key:
        type: string
        description: _item_key (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
