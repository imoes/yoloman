name: haproxy_frontend
fqcn: checkmk.haproxy_frontend
collection: checkmk
short_description: HAProxy Frontend %s
description: Checkmk check 'haproxy_frontend' (service: HAProxy Frontend %s), translated to a read-only on-host Starlark check module.
options:
    socket_path:
        type: string
        default: /var/run/haproxy.sock
        description: socket_path (from the check's parameters).
    csv_url:
        type: string
        description: csv_url (from the check's parameters).
    states:
        type: string
        description: states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
