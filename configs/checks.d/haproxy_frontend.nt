name: haproxy_frontend
fqcn: checkmk.haproxy_frontend
collection: checkmk
short_description: HAProxy Frontend %s
description: Monitoring check 'haproxy_frontend' (service: HAProxy Frontend %s), translated to a read-only on-host Starlark check module.
options:
    haproxy_socket:
        type: string
        default: /run/haproxy.sock
        description: haproxy_socket (from the check's parameters).
    params:
        type: string
        description: params (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
