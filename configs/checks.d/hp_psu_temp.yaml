name: hp_psu_temp
fqcn: checkmk.hp_psu_temp
collection: checkmk
short_description: Temperature Power Supply %s
description: 'Checkmk check ''hp_psu_temp'' (service: Temperature Power Supply %s),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
