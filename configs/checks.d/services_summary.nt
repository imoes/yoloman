name: services_summary
fqcn: checkmk.services_summary
collection: checkmk
short_description: Service Summary
description: Checkmk check 'services_summary' (service: Service Summary), translated to a read-only on-host Starlark check module.
options:
    ignored:
        type: list
        description: ignored (from the check's parameters).
    state_if_stopped:
        type: number
        default: 0
        description: state_if_stopped (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
