name: fjdarye_channel_modules
fqcn: checkmk.fjdarye_channel_modules
collection: checkmk
short_description: Controller Module %s
description: Monitoring check 'fjdarye_channel_modules' (service: Controller Module %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
