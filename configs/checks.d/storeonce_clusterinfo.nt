name: storeonce_clusterinfo
fqcn: checkmk.storeonce_clusterinfo
collection: checkmk
short_description: %s
description: Monitoring check 'storeonce_clusterinfo' (service: %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
    username:
        type: string
        default: Admin
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    api_path:
        type: string
        default: /storeonceservices/cluster/
        description: api_path (from the check's parameters).
    min_warn:
        type: string
        description: min_warn (from the check's parameters).
    min_crit:
        type: string
        description: min_crit (from the check's parameters).
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
