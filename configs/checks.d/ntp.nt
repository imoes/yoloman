name: ntp
fqcn: checkmk.ntp
collection: checkmk
short_description: NTP Peer %s
description: Monitoring check 'ntp' (service: NTP Peer %s), translated to a read-only on-host Starlark check module.
options:
    mode:
        type: string
        default: summary
        description: mode (from the check's parameters).
    ntp_levels:
        type: list
        description: ntp_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
