name: mail
fqcn: checkmk.mail
collection: checkmk
short_description: ''
description: Checkmk active check 'mail', translated to a read-only on-host Starlark
  check module.
options:
  service_description:
    type: string
    default: Mail Delivery
    description: service_description (from the check's parameters).
  fetch:
    type: string
    description: fetch (from the check's parameters).
  connect_timeout:
    type: string
    description: connect_timeout (from the check's parameters).
  forward:
    type: string
    description: forward (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
