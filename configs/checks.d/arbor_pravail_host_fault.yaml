name: arbor_pravail_host_fault
fqcn: checkmk.arbor_pravail_host_fault
collection: checkmk
short_description: Host Fault
description: 'Checkmk check ''arbor_pravail_host_fault'' (service: Host Fault), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
