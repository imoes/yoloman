name: stormshield_cpu_temp
fqcn: checkmk.stormshield_cpu_temp
collection: checkmk
short_description: CPU Temp %s
description: 'Checkmk check ''stormshield_cpu_temp'' (service: CPU Temp %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  warn:
    type: number
    default: '70'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '80'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
