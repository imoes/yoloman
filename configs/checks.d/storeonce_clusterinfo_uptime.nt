name: storeonce_clusterinfo_uptime
fqcn: checkmk.storeonce_clusterinfo_uptime
collection: checkmk
short_description: Uptime
description: Monitoring check 'storeonce_clusterinfo_uptime' (service: Uptime), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
    username:
        type: string
        default: admin
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
