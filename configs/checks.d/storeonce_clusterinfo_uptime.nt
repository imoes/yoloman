name: storeonce_clusterinfo_uptime
fqcn: checkmk.storeonce_clusterinfo_uptime
collection: checkmk
short_description: Uptime
description: Checkmk check 'storeonce_clusterinfo_uptime' (service: Uptime), translated to a read-only on-host Starlark check module.
options:
    api_token:
        type: string
        description: api_token (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    protocol:
        type: string
        default: https
        description: protocol (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
