name: huawei_switch_stack
fqcn: checkmk.huawei_switch_stack
collection: checkmk
short_description: Stack role %s
description: Checkmk check 'huawei_switch_stack' (service: Stack role %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    expected_role:
        type: string
        description: expected_role (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
