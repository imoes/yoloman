name: stormshield_route
fqcn: checkmk.stormshield_route
collection: checkmk
short_description: Gateway %s
description: Monitoring check 'stormshield_route' (service: Gateway %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
