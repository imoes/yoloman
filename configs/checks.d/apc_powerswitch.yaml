name: apc_powerswitch
fqcn: checkmk.apc_powerswitch
collection: checkmk
short_description: Power Outlet Port %s
description: 'Monitoring check ''apc_powerswitch'' (service: Power Outlet Port %s),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
