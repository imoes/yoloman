name: bluecoat_sensors
fqcn: checkmk.bluecoat_sensors
collection: checkmk
short_description: '%s'
description: 'Checkmk check ''bluecoat_sensors'' (service: %s), translated to a read-only
  on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  plugin:
    type: string
    default: ''
    description: plugin (from the check's parameters).
  temp_warn:
    type: number
    default: '60'
    description: temp_warn (from the check's parameters).
  temp_crit:
    type: number
    default: '80'
    description: temp_crit (from the check's parameters).
  warn:
    type: number
    default: '60'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '80'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
