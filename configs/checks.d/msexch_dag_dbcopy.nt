name: msexch_dag_dbcopy
fqcn: checkmk.msexch_dag_dbcopy
collection: checkmk
short_description: Exchange DAG DBCopy for %s
description: Checkmk check 'msexch_dag_dbcopy' (service: Exchange DAG DBCopy for %s), translated to a read-only on-host Starlark check module.
options:
    inv_key:
        type: string
        default: Status
        description: inv_key (from the check's parameters).
    inv_val:
        type: string
        description: inv_val (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
