name: wagner_titanus_topsense_info
fqcn: checkmk.wagner_titanus_topsense_info
collection: checkmk
short_description: Topsense Info
description: Checkmk check 'wagner_titanus_topsense_info' (service: Topsense Info), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
    levels_upper:
        type: list
        description: levels_upper (from the check's parameters).
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
    check_name:
        type: string
        default: wagner_titanus_topsense_info
        description: check_name (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
