name: huawei_wlc_aps_temp
fqcn: checkmk.huawei_wlc_aps_temp
collection: checkmk
short_description: AP %s Temperature
description: 'Checkmk check ''huawei_wlc_aps_temp'' (service: AP %s Temperature),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
