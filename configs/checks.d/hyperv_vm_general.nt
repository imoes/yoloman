name: hyperv_vm_general
fqcn: checkmk.hyperv_vm_general
collection: checkmk
short_description: Hyper-V VM summary
description: Monitoring check 'hyperv_vm_general' (service: Hyper-V VM summary), translated to a read-only on-host Starlark check module.
options:
    power_state:
        type: string
        description: power_state (from the check's parameters).
    vm_generation:
        type: string
        description: vm_generation (from the check's parameters).
    expected_generation:
        type: string
        default: generation_2
        description: expected_generation (from the check's parameters).
    state_if_not_expected:
        type: string
        description: state_if_not_expected (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
