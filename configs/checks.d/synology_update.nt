name: synology_update
fqcn: checkmk.synology_update
collection: checkmk
short_description: Update
description: Checkmk check 'synology_update' (service: Update), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    ok_states:
        type: list
        description: ok_states (from the check's parameters).
    warn_states:
        type: list
        description: warn_states (from the check's parameters).
    crit_states:
        type: list
        description: crit_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
