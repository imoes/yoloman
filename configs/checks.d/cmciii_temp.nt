name: cmciii_temp
fqcn: checkmk.cmciii_temp
collection: checkmk
short_description: Temperature %s
description: Monitoring check 'cmciii_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    warn:
        type: number
        default: 25.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 30.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
