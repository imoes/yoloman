name: ibm_svc_system
fqcn: checkmk.ibm_svc_system
collection: checkmk
short_description: Info
description: Monitoring check 'ibm_svc_system' (service: Info), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default: monitor
        description: user (from the check's parameters).
    ssh_key:
        type: string
        default:
        description: ssh_key (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
