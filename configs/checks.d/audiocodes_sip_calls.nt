name: audiocodes_sip_calls
fqcn: checkmk.audiocodes_sip_calls
collection: checkmk
short_description: SIP calls
description: Checkmk check 'audiocodes_sip_calls' (service: SIP calls), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    warn:
        type: number
        default: 0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
