name: enterasys_powersupply
fqcn: checkmk.enterasys_powersupply
collection: checkmk
short_description: PSU %s
description: Checkmk check 'enterasys_powersupply' (service: PSU %s), translated to a read-only on-host Starlark check module.
options:
    redundancy_ok_states:
        type: string
        description: redundancy_ok_states (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
