name: watchdog_sensors_dew
fqcn: checkmk.watchdog_sensors_dew
collection: checkmk
short_description: %s
description: Checkmk check 'watchdog_sensors_dew' (service: %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
