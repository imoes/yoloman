name: alcatel_timetra_chassis
fqcn: checkmk.alcatel_timetra_chassis
collection: checkmk
short_description: Device %s
description: 'Checkmk check ''alcatel_timetra_chassis'' (service: Device %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
