name: emc_datadomain_disks
fqcn: checkmk.emc_datadomain_disks
collection: checkmk
short_description: Hard Disk %s
description: 'Monitoring check ''emc_datadomain_disks'' (service: Hard Disk %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
