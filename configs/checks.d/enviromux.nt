name: enviromux
fqcn: checkmk.enviromux
collection: checkmk
short_description: Sensor %s
description: Monitoring check 'enviromux' (service: Sensor %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    temperature:
        type: string
        description: temperature (from the check's parameters).
    voltage:
        type: string
        description: voltage (from the check's parameters).
    humidity:
        type: string
        description: humidity (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
