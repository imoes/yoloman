name: fortigate_ipsecvpn
fqcn: checkmk.fortigate_ipsecvpn
collection: checkmk
short_description: VPN IPSec Tunnels
description: 'Checkmk check ''fortigate_ipsecvpn'' (service: VPN IPSec Tunnels), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  tunnels_ignore_levels:
    type: list
    description: tunnels_ignore_levels (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
