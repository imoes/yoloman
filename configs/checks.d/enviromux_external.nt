name: enviromux_external
fqcn: checkmk.enviromux_external
collection: checkmk
short_description: Sensor External %s
description: Monitoring check 'enviromux_external' (service: Sensor External %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
    warn_lower:
        type: string
        description: warn_lower (from the check's parameters).
    crit_lower:
        type: string
        description: crit_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
