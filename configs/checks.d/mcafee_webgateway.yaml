name: mcafee_webgateway
fqcn: checkmk.mcafee_webgateway
collection: checkmk
short_description: The McAfee Web Gateway has been rebranded to Skyhigh Secure Web
  Gateway with its release 12.2.2.
description: 'Checkmk check ''mcafee_webgateway'' (service: Web gateway statistics),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  infections:
    type: list
    description: infections (from the check's parameters).
  connections_blocked:
    type: list
    description: connections_blocked (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
