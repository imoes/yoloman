name: sftp
fqcn: checkmk.sftp
collection: checkmk
short_description: ''
description: Checkmk active check 'sftp', translated to a read-only on-host Starlark
  check module.
options:
  host:
    type: string
    default: ''
    description: host (from the check's parameters).
  user:
    type: string
    default: ''
    description: user (from the check's parameters).
  secret:
    type: string
    default: ''
    description: secret (from the check's parameters).
  port:
    type: string
    description: port (from the check's parameters).
  timeout:
    type: string
    description: timeout (from the check's parameters).
  timestamp:
    type: string
    description: timestamp (from the check's parameters).
  put:
    type: string
    description: put (from the check's parameters).
  get:
    type: string
    description: get (from the check's parameters).
  look_for_keys:
    type: bool
    default: 'False'
    description: look_for_keys (from the check's parameters).
  description:
    type: string
    description: description (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
