name: cisco_sma_resource_conservation
fqcn: checkmk.cisco_sma_resource_conservation
collection: checkmk
short_description: Resource conservation
description: 'Checkmk check ''cisco_sma_resource_conservation'' (service: Resource
  conservation), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
