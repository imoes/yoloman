name: cadvisor_if
fqcn: checkmk.cadvisor_if
collection: checkmk
short_description: Interface %s
description: Checkmk check 'cadvisor_if' (service: Interface %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 8080
        description: port (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
