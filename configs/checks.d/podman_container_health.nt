name: podman_container_health
fqcn: checkmk.podman_container_health
collection: checkmk
short_description: Health
description: Monitoring check 'podman_container_health' (service: Health), translated to a read-only on-host Starlark check module.
options:
    healthy:
        type: number
        default: 0
        description: healthy (from the check's parameters).
    starting:
        type: number
        default: 0
        description: starting (from the check's parameters).
    unhealthy:
        type: number
        default: 2
        description: unhealthy (from the check's parameters).
    no_healthcheck:
        type: number
        default: 0
        description: no_healthcheck (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
