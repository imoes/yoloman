name: fjdarye_inlet_thermal_sensors
fqcn: checkmk.fjdarye_inlet_thermal_sensors
collection: checkmk
short_description: Inlet Thermal %s
description: 'Checkmk check ''fjdarye_inlet_thermal_sensors'' (service: Inlet Thermal
  %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
