name: sap_hana_proc
fqcn: checkmk.sap_hana_proc
collection: checkmk
short_description: SAP HANA Process %s
description: Monitoring check 'sap_hana_proc' (service: SAP HANA Process %s), translated to a read-only on-host Starlark check module.
options:
    sid:
        type: string
        default:
        description: sid (from the check's parameters).
    instance:
        type: string
        default: 00
        description: instance (from the check's parameters).
    userstore_key:
        type: string
        description: userstore_key (from the check's parameters).
    user:
        type: string
        default: SYSTEM
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    coordin:
        type: string
        default:
        description: coordin (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
