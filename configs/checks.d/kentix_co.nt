name: kentix_co
fqcn: checkmk.kentix_co
collection: checkmk
short_description: Carbon Monoxide
description: Checkmk check 'kentix_co' (service: Carbon Monoxide), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels_ppm:
        type: list
        description: levels_ppm (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
