name: kentix_amp_sensors_smoke
fqcn: checkmk.kentix_amp_sensors_smoke
collection: checkmk
short_description: Smoke Detector %s
description: Monitoring check 'kentix_amp_sensors_smoke' (service: Smoke Detector %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
