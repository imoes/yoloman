name: audiocodes_calls
fqcn: checkmk.audiocodes_calls
collection: checkmk
short_description: SBC calls
description: Monitoring check 'audiocodes_calls' (service: SBC calls), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    answer_seizure_ratio_lower_levels:
        type: string
        description: answer_seizure_ratio_lower_levels (from the check's parameters).
    network_effectiveness_ratio_lower_levels:
        type: string
        description: network_effectiveness_ratio_lower_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
