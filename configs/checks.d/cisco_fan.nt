name: cisco_fan
fqcn: checkmk.cisco_fan
collection: checkmk
short_description: FAN %s
description: Checkmk check 'cisco_fan' (service: FAN %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
