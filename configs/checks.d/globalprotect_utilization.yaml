name: globalprotect_utilization
fqcn: checkmk.globalprotect_utilization
collection: checkmk
short_description: GlobalProtect Gateway Utilization
description: 'Checkmk check ''globalprotect_utilization'' (service: GlobalProtect
  Gateway Utilization), translated to a read-only on-host Starlark check module.'
options:
  levels:
    type: string
    description: levels (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  utilization:
    type: string
    description: utilization (from the check's parameters).
  active_tunnels:
    type: string
    description: active_tunnels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
