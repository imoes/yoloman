name: diskstat_io_volumes
fqcn: checkmk.diskstat_io_volumes
collection: checkmk
short_description: Disk IO Volumes %s
description: Monitoring check 'diskstat_io_volumes' (service: Disk IO Volumes %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: manage
        description: username (from the check's parameters).
    password:
        type: string
        default: !manage
        description: password (from the check's parameters).
    warn_read_throughput:
        type: string
        description: warn_read_throughput (from the check's parameters).
    crit_read_throughput:
        type: string
        description: crit_read_throughput (from the check's parameters).
    warn_write_throughput:
        type: string
        description: warn_write_throughput (from the check's parameters).
    crit_write_throughput:
        type: string
        description: crit_write_throughput (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
