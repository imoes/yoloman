name: ups_battery_state
fqcn: checkmk.ups_battery_state
collection: checkmk
short_description: Battery state
description: Monitoring check 'ups_battery_state' (service: Battery state), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
