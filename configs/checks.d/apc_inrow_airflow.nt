name: apc_inrow_airflow
fqcn: checkmk.apc_inrow_airflow
collection: checkmk
short_description: Airflow
description: Checkmk check 'apc_inrow_airflow' (service: Airflow), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    level_low:
        type: string
        description: level_low (from the check's parameters).
    level_high:
        type: string
        description: level_high (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
