name: janitza_umg
fqcn: checkmk.janitza_umg
collection: checkmk
short_description: Input %s
description: Checkmk check 'janitza_umg' (service: Input %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    voltage_levels:
        type: string
        description: voltage_levels (from the check's parameters).
    current_levels:
        type: string
        description: current_levels (from the check's parameters).
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
