name: postgres_locks
fqcn: checkmk.postgres_locks
collection: checkmk
short_description: PostgreSQL Locks %s
description: Checkmk check 'postgres_locks' (service: PostgreSQL Locks %s), translated to a read-only on-host Starlark check module.
options:
    levels_shared:
        type: string
        description: levels_shared (from the check's parameters).
    levels_exclusive:
        type: string
        description: levels_exclusive (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
