name: apc_symmetra_temp
fqcn: checkmk.apc_symmetra_temp
collection: checkmk
short_description: Temperature %s
description: Checkmk check 'apc_symmetra_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels_battery:
        type: string
        description: levels_battery (from the check's parameters).
    levels_sensors:
        type: string
        description: levels_sensors (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
