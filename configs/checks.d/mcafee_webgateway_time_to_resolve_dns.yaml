name: mcafee_webgateway_time_to_resolve_dns
fqcn: checkmk.mcafee_webgateway_time_to_resolve_dns
collection: checkmk
short_description: The McAfee Web Gateway has been rebranded to Skyhigh Secure Web
  Gateway with its release 12.2.2.
description: 'Checkmk check ''mcafee_webgateway_time_to_resolve_dns'' (service: Time
  to resolve DNS), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  version:
    type: string
    default: 2c
    description: version (from the check's parameters).
  warn:
    type: number
    default: '1.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '5.0'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
