name: apc_inrow_system_events
fqcn: checkmk.apc_inrow_system_events
collection: checkmk
short_description: System events
description: Monitoring check 'apc_inrow_system_events' (service: System events), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    state:
        type: number
        default: 2
        description: state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
