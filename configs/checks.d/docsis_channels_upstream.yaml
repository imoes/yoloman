name: docsis_channels_upstream
fqcn: checkmk.docsis_channels_upstream
collection: checkmk
short_description: Upstream Channel %s
description: 'Monitoring check ''docsis_channels_upstream'' (service: Upstream Channel
  %s), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  signal_noise:
    type: list
    description: signal_noise (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
