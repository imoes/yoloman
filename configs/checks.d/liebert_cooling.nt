name: liebert_cooling
fqcn: checkmk.liebert_cooling
collection: checkmk
short_description: %s
description: Checkmk check 'liebert_cooling' (service: %s), translated to a read-only on-host Starlark check module.
options:
    min_capacity:
        type: list
        description: min_capacity (from the check's parameters).
    max_capacity:
        type: string
        description: max_capacity (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
