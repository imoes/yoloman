name: mssql_databases
fqcn: checkmk.mssql_databases
collection: checkmk
short_description: MSSQL %s Database
description: Checkmk check 'mssql_databases' (service: MSSQL %s Database), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    instance:
        type: string
        default:
        description: instance (from the check's parameters).
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    instance_prefix:
        type: string
        default:
        description: instance_prefix (from the check's parameters).
    map_db_states:
        type: string
        description: map_db_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
