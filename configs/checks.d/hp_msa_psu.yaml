name: hp_msa_psu
fqcn: checkmk.hp_msa_psu
collection: checkmk
short_description: Power Supply Health %s
description: 'Checkmk check ''hp_msa_psu'' (service: Power Supply Health %s), translated
  to a read-only on-host Starlark check module.'
options:
  levels_12v_lower:
    type: list
    description: levels_12v_lower (from the check's parameters).
  levels_12v_upper:
    type: list
    description: levels_12v_upper (from the check's parameters).
  levels_5v_lower:
    type: list
    description: levels_5v_lower (from the check's parameters).
  levels_5v_upper:
    type: list
    description: levels_5v_upper (from the check's parameters).
  levels_33v_lower:
    type: list
    description: levels_33v_lower (from the check's parameters).
  levels_33v_upper:
    type: list
    description: levels_33v_upper (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
