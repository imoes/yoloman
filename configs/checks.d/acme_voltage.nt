name: acme_voltage
fqcn: checkmk.acme_voltage
collection: checkmk
short_description: Voltage %s
description: Checkmk check 'acme_voltage' (service: Voltage %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    warn_upper:
        type: number
        default: 2.8
        description: warn_upper (from the check's parameters).
    crit_upper:
        type: number
        default: 3.0
        description: crit_upper (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
