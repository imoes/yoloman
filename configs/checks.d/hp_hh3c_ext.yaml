name: hp_hh3c_ext
fqcn: checkmk.hp_hh3c_ext
collection: checkmk
short_description: Temperature %s
description: 'Checkmk check ''hp_hh3c_ext'' (service: Temperature %s), translated
  to a read-only on-host Starlark check module.'
options:
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  admin_overrides:
    type: string
    description: admin_overrides (from the check's parameters).
  oper_overrides:
    type: string
    description: oper_overrides (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  subcheck:
    type: string
    default: temperature
    description: subcheck (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
