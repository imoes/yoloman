name: mqtt_broker
fqcn: checkmk.mqtt_broker
collection: checkmk
short_description: MQTT %s Broker
description: 'Monitoring check ''mqtt_broker'' (service: MQTT %s Broker), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: number
    default: '1883'
    description: port (from the check's parameters).
  username:
    type: string
    default: ''
    description: username (from the check's parameters).
  password:
    type: string
    default: ''
    description: password (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
