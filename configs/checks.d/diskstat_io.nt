name: diskstat_io
fqcn: checkmk.diskstat_io
collection: checkmk
short_description: Disk IO %s
description: Checkmk check 'diskstat_io' (service: Disk IO %s), translated to a read-only on-host Starlark check module.
options:
    read_throughput:
        type: string
        description: read_throughput (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
    write_throughput:
        type: string
        description: write_throughput (from the check's parameters).
    util:
        type: string
        description: util (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
