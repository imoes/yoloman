name: bluenet_sensor
fqcn: checkmk.bluenet_sensor
collection: checkmk
short_description: Temperature %s
description: 'Checkmk check ''bluenet_sensor'' (service: Temperature %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  _kind:
    type: string
    default: temperature
    description: _kind (from the check's parameters).
  warn:
    type: number
    default: '28'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '35'
    description: crit (from the check's parameters).
  warn_low:
    type: number
    default: '13'
    description: warn_low (from the check's parameters).
  crit_low:
    type: number
    default: '17'
    description: crit_low (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
