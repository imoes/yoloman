name: hr_cpu
fqcn: checkmk.hr_cpu
collection: checkmk
short_description: CPU utilization
description: Monitoring check 'hr_cpu' (service: CPU utilization), translated to a read-only on-host Starlark check module.
options:
    util:
        type: list
        description: util (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
