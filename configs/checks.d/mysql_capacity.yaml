name: mysql_capacity
fqcn: checkmk.mysql_capacity
collection: checkmk
short_description: MySQL DB Size %s
description: 'Checkmk check ''mysql_capacity'' (service: MySQL DB Size %s), translated
  to a read-only on-host Starlark check module.'
options:
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
