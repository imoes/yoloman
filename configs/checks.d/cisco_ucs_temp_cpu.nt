name: cisco_ucs_temp_cpu
fqcn: checkmk.cisco_ucs_temp_cpu
collection: checkmk
short_description: Temperature CPU %s
description: Checkmk check 'cisco_ucs_temp_cpu' (service: Temperature CPU %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
