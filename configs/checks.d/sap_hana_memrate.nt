name: sap_hana_memrate
fqcn: checkmk.sap_hana_memrate
collection: checkmk
short_description: SAP HANA Memory %s
description: Checkmk check 'sap_hana_memrate' (service: SAP HANA Memory %s), translated to a read-only on-host Starlark check module.
options:
    user:
        type: string
        default: SYSTEM
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    warn:
        type: number
        default: 70.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 80.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
