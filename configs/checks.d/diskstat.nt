name: diskstat
fqcn: checkmk.diskstat
collection: checkmk
short_description: Disk IO %s
description: Checkmk check 'diskstat' (service: Disk IO %s), translated to a read-only on-host Starlark check module.
options:
    util_warn:
        type: number
        default: 80.0
        description: util_warn (from the check's parameters).
    util_crit:
        type: number
        default: 90.0
        description: util_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
