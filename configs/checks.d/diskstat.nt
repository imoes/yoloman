name: diskstat
fqcn: checkmk.diskstat
collection: checkmk
short_description: Disk IO %s
description: Monitoring check 'diskstat' (service: Disk IO %s), translated to a read-only on-host Starlark check module.
options:
    utilization:
        type: number
        default: 70.0
        description: utilization (from the check's parameters).
    latency:
        type: number
        default: 0.06
        description: latency (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
