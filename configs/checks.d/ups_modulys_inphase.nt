name: ups_modulys_inphase
fqcn: checkmk.ups_modulys_inphase
collection: checkmk
short_description: Input %s
description: Checkmk check 'ups_modulys_inphase' (service: Input %s), translated to a read-only on-host Starlark check module.
options:
    voltage_low:
        type: number
        default: 198.0
        description: voltage_low (from the check's parameters).
    voltage_critical_low:
        type: number
        default: 180.0
        description: voltage_critical_low (from the check's parameters).
    current_low:
        type: number
        default: 0.0
        description: current_low (from the check's parameters).
    current_critical_low:
        type: number
        default: 0.0
        description: current_critical_low (from the check's parameters).
    frequency_low:
        type: number
        default: 49.0
        description: frequency_low (from the check's parameters).
    frequency_critical_low:
        type: number
        default: 47.0
        description: frequency_critical_low (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
