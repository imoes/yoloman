name: mongodb_replication_info
fqcn: checkmk.mongodb_replication_info
collection: checkmk
short_description: MongoDB Replication Info
description: Checkmk check 'mongodb_replication_info' (service: MongoDB Replication Info), translated to a read-only on-host Starlark check module.
options:
    binary:
        type: string
        default: mongosh
        description: binary (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    port:
        type: number
        default: 27017
        description: port (from the check's parameters).
    username:
        type: string
        default:
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    auth_db:
        type: string
        default: admin
        description: auth_db (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
