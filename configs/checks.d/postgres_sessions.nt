name: postgres_sessions
fqcn: checkmk.postgres_sessions
collection: checkmk
short_description: PostgreSQL Daemon Sessions %s
description: Checkmk check 'postgres_sessions' (service: PostgreSQL Daemon Sessions %s), translated to a read-only on-host Starlark check module.
options:
    total:
        type: list
        description: total (from the check's parameters).
    running:
        type: list
        description: running (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
