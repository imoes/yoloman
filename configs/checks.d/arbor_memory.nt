name: arbor_memory
fqcn: checkmk.arbor_memory
collection: checkmk
short_description: Memory
description: Checkmk check 'arbor_memory' (service: Memory), translated to a read-only on-host Starlark check module.
options:
    levels_ram:
        type: list
        description: levels_ram (from the check's parameters).
    levels_swap:
        type: list
        description: levels_swap (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
