name: datapower_ldrive
fqcn: checkmk.datapower_ldrive
collection: checkmk
short_description: Logical Drive %s
description: Checkmk check 'datapower_ldrive' (service: Logical Drive %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
