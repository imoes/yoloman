name: bdt_tape_status
fqcn: checkmk.bdt_tape_status
collection: checkmk
short_description: Tape Library Status
description: Checkmk check 'bdt_tape_status' (service: Tape Library Status), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
