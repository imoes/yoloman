name: brocade_mlx_module_status
fqcn: checkmk.brocade_mlx_module_status
collection: checkmk
short_description: Status Module %s
description: 'Checkmk check ''brocade_mlx_module_status'' (service: Status Module
  %s), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
