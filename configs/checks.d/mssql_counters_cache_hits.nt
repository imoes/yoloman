name: mssql_counters_cache_hits
fqcn: checkmk.mssql_counters_cache_hits
collection: checkmk
short_description: MSSQL %s
description: Checkmk check 'mssql_counters_cache_hits' (service: MSSQL %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    instance:
        type: string
        default:
        description: instance (from the check's parameters).
    port:
        type: number
        default: 1433
        description: port (from the check's parameters).
    user:
        type: string
        default:
        description: user (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    add_zero_based_services:
        type: bool
        default: False
        description: add_zero_based_services (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
