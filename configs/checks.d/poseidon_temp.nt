name: poseidon_temp
fqcn: checkmk.poseidon_temp
collection: checkmk
short_description: Temperatur: %s
description: Monitoring check 'poseidon_temp' (service: Temperatur: %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        description: community (from the check's parameters).
    host:
        type: string
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_upper:
        type: string
        description: levels_upper (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
