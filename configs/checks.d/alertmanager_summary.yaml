name: alertmanager_summary
fqcn: checkmk.alertmanager_summary
collection: checkmk
short_description: Alertmanager Check
description: 'Checkmk check ''alertmanager_summary'' (service: Alertmanager Summary),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  summary_service:
    type: bool
    default: 'True'
    description: summary_service (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
