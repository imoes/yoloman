name: w32time_status
fqcn: checkmk.w32time_status
collection: checkmk
short_description: Windows time service
description: Checkmk check 'w32time_status' (service: Windows time service), translated to a read-only on-host Starlark check module.
options:
    states:
        type: string
        description: states (from the check's parameters).
    offset:
        type: list
        description: offset (from the check's parameters).
    time_since_last_successful_sync:
        type: list
        description: time_since_last_successful_sync (from the check's parameters).
    stratum:
        type: list
        description: stratum (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
