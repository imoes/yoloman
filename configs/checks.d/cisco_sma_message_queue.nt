name: cisco_sma_message_queue
fqcn: checkmk.cisco_sma_message_queue
collection: checkmk
short_description: Queue
description: Checkmk check 'cisco_sma_message_queue' (service: Queue), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    monitoring_status_memory_available:
        type: string
        description: monitoring_status_memory_available (from the check's parameters).
    monitoring_status_memory_shortage:
        type: string
        description: monitoring_status_memory_shortage (from the check's parameters).
    monitoring_status_queue_full:
        type: string
        description: monitoring_status_queue_full (from the check's parameters).
    levels_queue_utilization:
        type: string
        description: levels_queue_utilization (from the check's parameters).
    levels_queue_length:
        type: string
        description: levels_queue_length (from the check's parameters).
    levels_oldest_message_age:
        type: string
        description: levels_oldest_message_age (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
