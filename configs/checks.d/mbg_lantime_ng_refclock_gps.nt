name: mbg_lantime_ng_refclock_gps
fqcn: checkmk.mbg_lantime_ng_refclock_gps
collection: checkmk
short_description: LANTIME Refclock %s
description: Checkmk check 'mbg_lantime_ng_refclock_gps' (service: LANTIME Refclock %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels_lower:
        type: list
        description: levels_lower (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
