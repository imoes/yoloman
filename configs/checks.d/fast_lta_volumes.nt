name: fast_lta_volumes
fqcn: checkmk.fast_lta_volumes
collection: checkmk
short_description: Fast LTA Volume %s
description: Checkmk check 'fast_lta_volumes' (service: Fast LTA Volume %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
