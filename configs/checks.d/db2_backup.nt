name: db2_backup
fqcn: checkmk.db2_backup
collection: checkmk
short_description: DB2 Backup %s
description: Checkmk check 'db2_backup' (service: DB2 Backup %s), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: string
        description: levels (from the check's parameters).
    warn:
        type: string
        description: warn (from the check's parameters).
    crit:
        type: string
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
