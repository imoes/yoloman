name: bintec_sensors_fan
fqcn: checkmk.bintec_sensors_fan
collection: checkmk
short_description: '%s'
description: 'Checkmk check ''bintec_sensors_fan'' (service: %s), translated to a
  read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  lower:
    type: list
    description: lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
