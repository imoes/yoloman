name: wmic_process
fqcn: checkmk.wmic_process
collection: checkmk
short_description: Process %s
description: Checkmk check 'wmic_process' (service: Process %s), translated to a read-only on-host Starlark check module.
options:
    name:
        type: string
        default:
        description: name (from the check's parameters).
    mem_levels:
        type: string
        description: mem_levels (from the check's parameters).
    page_levels:
        type: string
        description: page_levels (from the check's parameters).
    cpu_levels:
        type: string
        description: cpu_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
