name: win_dhcp_pools
fqcn: checkmk.win_dhcp_pools
collection: checkmk
short_description: DHCP Pool %s
description: Checkmk check 'win_dhcp_pools' (service: DHCP Pool %s), translated to a read-only on-host Starlark check module.
options:
    empty_pools:
        type: bool
        default: False
        description: empty_pools (from the check's parameters).
    free_leases:
        type: list
        description: free_leases (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
