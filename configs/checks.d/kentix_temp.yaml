name: kentix_temp
fqcn: checkmk.kentix_temp
collection: checkmk
short_description: Temperature %s
description: 'Checkmk check ''kentix_temp'' (service: Temperature %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    description: host (from the check's parameters).
  warn:
    type: number
    default: '30'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '35'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
