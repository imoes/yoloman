name: timesyncd
fqcn: checkmk.timesyncd
collection: checkmk
short_description: Systemd Timesyncd Time
description: Checkmk check 'timesyncd' (service: Systemd Timesyncd Time), translated to a read-only on-host Starlark check module.
options:
    stratum_level:
        type: number
        default: 10
        description: stratum_level (from the check's parameters).
    quality_levels:
        type: list
        description: quality_levels (from the check's parameters).
    last_ntp_message:
        type: list
        description: last_ntp_message (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
