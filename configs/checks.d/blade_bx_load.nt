name: blade_bx_load
fqcn: checkmk.blade_bx_load
collection: checkmk
short_description: CPU load
description: Monitoring check 'blade_bx_load' (service: CPU load), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels1:
        type: string
        description: levels1 (from the check's parameters).
    levels5:
        type: string
        description: levels5 (from the check's parameters).
    levels15:
        type: list
        description: levels15 (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
