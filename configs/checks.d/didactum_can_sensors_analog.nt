name: didactum_can_sensors_analog
fqcn: checkmk.didactum_can_sensors_analog
collection: checkmk
short_description: Temperature CAN %s
description: Checkmk check 'didactum_can_sensors_analog' (service: Temperature CAN %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: string
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
