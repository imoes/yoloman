name: arbor_memory
fqcn: checkmk.arbor_memory
collection: checkmk
short_description: Memory
description: 'Checkmk check ''arbor_memory'' (service: Memory), translated to a read-only
  on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels_ram:
    type: list
    description: levels_ram (from the check's parameters).
  levels_swap:
    type: list
    description: levels_swap (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
