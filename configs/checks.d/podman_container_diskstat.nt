name: podman_container_diskstat
fqcn: checkmk.podman_container_diskstat
collection: checkmk
short_description: Container IO %s
description: Checkmk check 'podman_container_diskstat' (service: Container IO %s), translated to a read-only on-host Starlark check module.
options:
    warn_read_ios:
        type: string
        description: warn_read_ios (from the check's parameters).
    crit_read_ios:
        type: string
        description: crit_read_ios (from the check's parameters).
    warn_write_ios:
        type: string
        description: warn_write_ios (from the check's parameters).
    crit_write_ios:
        type: string
        description: crit_write_ios (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
