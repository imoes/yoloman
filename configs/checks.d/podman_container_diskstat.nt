name: podman_container_diskstat
fqcn: checkmk.podman_container_diskstat
collection: checkmk
short_description: Container IO %s
description: Checkmk check 'podman_container_diskstat' (service: Container IO %s), translated to a read-only on-host Starlark check module.
options:
    read_throughput_warn:
        type: string
        description: read_throughput_warn (from the check's parameters).
    read_throughput_crit:
        type: string
        description: read_throughput_crit (from the check's parameters).
    write_throughput_warn:
        type: string
        description: write_throughput_warn (from the check's parameters).
    write_throughput_crit:
        type: string
        description: write_throughput_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
