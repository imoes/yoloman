name: dns
fqcn: checkmk.dns
collection: checkmk
short_description: ''
description: Checkmk active check 'dns', translated to a read-only on-host Starlark
  check module.
options:
  hostname:
    type: string
    default: ''
    description: hostname (from the check's parameters).
  name:
    type: string
    description: name (from the check's parameters).
  server:
    type: string
    description: server (from the check's parameters).
  expect_all_addresses:
    type: bool
    default: 'True'
    description: expect_all_addresses (from the check's parameters).
  expected_addresses:
    type: list
    description: expected_addresses (from the check's parameters).
  expected_authority:
    type: string
    description: expected_authority (from the check's parameters).
  response_time:
    type: list
    description: response_time (from the check's parameters).
  timeout:
    type: number
    default: '30'
    description: timeout (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
