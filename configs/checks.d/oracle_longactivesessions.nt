name: oracle_longactivesessions
fqcn: checkmk.oracle_longactivesessions
collection: checkmk
short_description: ORA %s Long Active Sessions
description: Checkmk check 'oracle_longactivesessions' (service: ORA %s Long Active Sessions), translated to a read-only on-host Starlark check module.
options:
    warn:
        type: number
        default: 500
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 1000
        description: crit (from the check's parameters).
    oracle_home:
        type: string
        default:
        description: oracle_home (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
