name: ibm_svc_license
fqcn: checkmk.ibm_svc_license
collection: checkmk
short_description: License %s
description: Monitoring check 'ibm_svc_license' (service: License %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default: monitor
        description: user (from the check's parameters).
    port:
        type: number
        default: 22
        description: port (from the check's parameters).
    levels_kind:
        type: string
        default: crit_on_all
        description: levels_kind (from the check's parameters).
    levels_warn:
        type: number
        default: 0.0
        description: levels_warn (from the check's parameters).
    levels_crit:
        type: number
        default: 0.0
        description: levels_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
