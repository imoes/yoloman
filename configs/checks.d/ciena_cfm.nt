name: ciena_cfm
fqcn: checkmk.ciena_cfm
collection: checkmk
short_description: CFM-Service %s
description: Monitoring check 'ciena_cfm' (service: CFM-Service %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    discovered_oper_state:
        type: string
        default:
        description: discovered_oper_state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
