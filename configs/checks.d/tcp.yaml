name: tcp
fqcn: checkmk.tcp
collection: checkmk
short_description: ''
description: Checkmk active check 'tcp', translated to a read-only on-host Starlark
  check module.
options:
  port:
    type: number
    default: '80'
    description: port (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  warn:
    type: number
    default: '1.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '2.0'
    description: crit (from the check's parameters).
  timeout:
    type: string
    description: timeout (from the check's parameters).
  expect:
    type: list
    description: expect (from the check's parameters).
  send_string:
    type: string
    description: send_string (from the check's parameters).
  quit_string:
    type: string
    description: quit_string (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
