name: checkpoint_connections
fqcn: checkmk.checkpoint_connections
collection: checkmk
short_description: Connections
description: Monitoring check 'checkpoint_connections' (service: Connections), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
