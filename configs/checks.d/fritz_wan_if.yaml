name: fritz_wan_if
fqcn: checkmk.fritz_wan_if
collection: checkmk
short_description: Interface %s
description: 'Checkmk check ''fritz_wan_if'' (service: Interface %s), translated to
  a read-only on-host Starlark check module.'
options:
  warn:
    type: number
    default: '80'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90'
    description: crit (from the check's parameters).
  assumed_speed_out:
    type: string
    description: assumed_speed_out (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
