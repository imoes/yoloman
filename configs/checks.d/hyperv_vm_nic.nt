name: hyperv_vm_nic
fqcn: checkmk.hyperv_vm_nic
collection: checkmk
short_description: HyperV NIC %s
description: Checkmk check 'hyperv_vm_nic' (service: HyperV NIC %s), translated to a read-only on-host Starlark check module.
options:
    connection_state:
        type: string
        description: connection_state (from the check's parameters).
    connected:
        type: string
        default: true
        description: connected (from the check's parameters).
    state_if_not_expected:
        type: string
        description: state_if_not_expected (from the check's parameters).
    dynamic_mac:
        type: string
        description: dynamic_mac (from the check's parameters).
    dynamic_mac_enabled:
        type: string
        default: true
        description: dynamic_mac_enabled (from the check's parameters).
    expected_vswitch:
        type: string
        description: expected_vswitch (from the check's parameters).
    name:
        type: string
        default:
        description: name (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
