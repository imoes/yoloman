name: apc_ats_output
fqcn: checkmk.apc_ats_output
collection: checkmk
short_description: Phase %s output
description: Monitoring check 'apc_ats_output' (service: Phase %s output), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    output_voltage_max:
        type: string
        description: output_voltage_max (from the check's parameters).
    output_power_max:
        type: string
        description: output_power_max (from the check's parameters).
    output_current_max:
        type: string
        description: output_current_max (from the check's parameters).
    load_perc_min:
        type: string
        description: load_perc_min (from the check's parameters).
    load_perc_max:
        type: string
        description: load_perc_max (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
