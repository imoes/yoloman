name: hp_msa_fan
fqcn: checkmk.hp_msa_fan
collection: checkmk
short_description: Fan %s
description: Monitoring check 'hp_msa_fan' (service: Fan %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: manage
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
