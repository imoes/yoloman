name: scaleio_storage_pool_rebalancerw
fqcn: checkmk.scaleio_storage_pool_rebalancerw
collection: checkmk
short_description: ScaleIO SP rebalance IO %s
description: 'Checkmk check ''scaleio_storage_pool_rebalancerw'' (service: ScaleIO
  SP rebalance IO %s), translated to a read-only on-host Starlark check module.'
options:
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
