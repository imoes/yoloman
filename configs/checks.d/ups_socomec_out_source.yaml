name: ups_socomec_out_source
fqcn: checkmk.ups_socomec_out_source
collection: checkmk
short_description: Output Source
description: 'Checkmk check ''ups_socomec_out_source'' (service: Output Source), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  version:
    type: string
    default: 2c
    description: version (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
