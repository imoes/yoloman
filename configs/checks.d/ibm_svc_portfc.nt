name: ibm_svc_portfc
fqcn: checkmk.ibm_svc_portfc
collection: checkmk
short_description: FC %s
description: Checkmk check 'ibm_svc_portfc' (service: FC %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    user:
        type: string
        default: admin
        description: user (from the check's parameters).
    ssh_key:
        type: string
        default:
        description: ssh_key (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
