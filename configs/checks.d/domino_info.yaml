name: domino_info
fqcn: checkmk.domino_info
collection: checkmk
short_description: Domino Info
description: 'Checkmk check ''domino_info'' (service: Domino Info), translated to
  a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
