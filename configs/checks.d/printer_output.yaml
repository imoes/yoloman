name: printer_output
fqcn: checkmk.printer_output
collection: checkmk
short_description: Output %s
description: 'Checkmk check ''printer_output'' (service: Output %s), translated to
  a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  capacity_levels:
    type: list
    description: capacity_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
