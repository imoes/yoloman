name: ibm_svc_disks
fqcn: checkmk.ibm_svc_disks
collection: checkmk
short_description: Disk Summary
description: 'Checkmk check ''ibm_svc_disks'' (service: Disk Summary), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  failed_spare_ratio:
    type: list
    description: failed_spare_ratio (from the check's parameters).
  offline_spare_ratio:
    type: list
    description: offline_spare_ratio (from the check's parameters).
  number_of_spare_disks:
    type: string
    description: number_of_spare_disks (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
