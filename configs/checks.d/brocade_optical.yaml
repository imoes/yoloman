name: brocade_optical
fqcn: checkmk.brocade_optical
collection: checkmk
short_description: Interface %s Optical
description: 'Checkmk check ''brocade_optical'' (service: Interface %s Optical), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  temp:
    type: bool
    default: 'False'
    description: temp (from the check's parameters).
  tx_light:
    type: bool
    default: 'False'
    description: tx_light (from the check's parameters).
  rx_light:
    type: bool
    default: 'False'
    description: rx_light (from the check's parameters).
  lanes:
    type: string
    description: lanes (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
