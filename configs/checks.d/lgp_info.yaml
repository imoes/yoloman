name: lgp_info
fqcn: checkmk.lgp_info
collection: checkmk
short_description: Liebert Info
description: 'Checkmk check ''lgp_info'' (service: Liebert Info), translated to a
  read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
