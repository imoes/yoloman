name: mysql_connections
fqcn: checkmk.mysql_connections
collection: checkmk
short_description: MySQL Connections %s
description: 'Checkmk check ''mysql_connections'' (service: MySQL Connections %s),
  translated to a read-only on-host Starlark check module.'
options:
  conn_warn:
    type: number
    default: '80'
    description: conn_warn (from the check's parameters).
  conn_crit:
    type: number
    default: '90'
    description: conn_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
