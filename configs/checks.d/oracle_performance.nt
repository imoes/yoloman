name: oracle_performance
fqcn: checkmk.oracle_performance
collection: checkmk
short_description: ORA %s Performance
description: Checkmk check 'oracle_performance' (service: ORA %s Performance), translated to a read-only on-host Starlark check module.
options:
    check_dbtime:
        type: bool
        default: True
        description: check_dbtime (from the check's parameters).
    check_memory:
        type: bool
        default: True
        description: check_memory (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
