name: raritan_px2_residual_current
fqcn: checkmk.raritan_px2_residual_current
collection: checkmk
short_description: Residual Current %s
description: Checkmk check 'raritan_px2_residual_current' (service: Residual Current %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    warn_missing_data:
        type: bool
        default: True
        description: warn_missing_data (from the check's parameters).
    warn_missing_levels:
        type: bool
        default: True
        description: warn_missing_levels (from the check's parameters).
    residual_levels:
        type: list
        description: residual_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
