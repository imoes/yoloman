name: couchbase_buckets_vbuckets_replica
fqcn: checkmk.couchbase_buckets_vbuckets_replica
collection: checkmk
short_description: Couchbase Bucket %s replica vBuckets
description: Checkmk check 'couchbase_buckets_vbuckets_replica' (service: Couchbase Bucket %s replica vBuckets), translated to a read-only on-host Starlark check module.
options:
    vb_replica_num:
        type: string
        description: vb_replica_num (from the check's parameters).
    item_memory:
        type: string
        description: item_memory (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
