name: cisco_ucs_mem
fqcn: checkmk.cisco_ucs_mem
collection: checkmk
short_description: Memory %s
description: Monitoring check 'cisco_ucs_mem' (service: Memory %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
