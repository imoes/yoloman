name: fjdarye_ca_ports
fqcn: checkmk.fjdarye_ca_ports
collection: checkmk
short_description: CA Port IO %s
description: Checkmk check 'fjdarye_ca_ports' (service: CA Port IO %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    indices:
        type: list
        description: indices (from the check's parameters).
    modes:
        type: list
        description: modes (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    read_ios_levels:
        type: list
        description: read_ios_levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
