name: aruba_chassis_temp
fqcn: checkmk.aruba_chassis_temp
collection: checkmk
short_description: Temperature %s
description: Monitoring check 'aruba_chassis_temp' (service: Temperature %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    levels_crit:
        type: string
        description: levels_crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
