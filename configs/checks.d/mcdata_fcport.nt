name: mcdata_fcport
fqcn: checkmk.mcdata_fcport
collection: checkmk
short_description: Port %s
description: Monitoring check 'mcdata_fcport' (service: Port %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
