name: citrix_controller_registered
fqcn: checkmk.citrix_controller_registered
collection: checkmk
short_description: Citrix Desktops Registered
description: 'Checkmk check ''citrix_controller_registered'' (service: Citrix Desktops
  Registered), translated to a read-only on-host Starlark check module.'
options:
  total:
    type: string
    description: total (from the check's parameters).
  active:
    type: string
    description: active (from the check's parameters).
  inactive:
    type: string
    description: inactive (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  levels:
    type: string
    description: levels (from the check's parameters).
  levels_lower:
    type: string
    description: levels_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
