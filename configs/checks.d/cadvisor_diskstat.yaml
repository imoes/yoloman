name: cadvisor_diskstat
fqcn: checkmk.cadvisor_diskstat
collection: checkmk
short_description: Disk IO %s
description: 'Checkmk check ''cadvisor_diskstat'' (service: Disk IO %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: number
    default: '8080'
    description: port (from the check's parameters).
  warn:
    type: number
    default: '80.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '90.0'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
