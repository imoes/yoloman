name: openbsd_sensors_indicator
fqcn: checkmk.openbsd_sensors_indicator
collection: checkmk
short_description: Indicator %s
description: Checkmk check 'openbsd_sensors_indicator' (service: Indicator %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
