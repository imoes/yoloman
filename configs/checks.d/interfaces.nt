name: interfaces
fqcn: checkmk.interfaces
collection: checkmk
short_description: Interface %s
description: Checkmk check 'interfaces' (service: Interface %s), translated to a read-only on-host Starlark check module.
options:
    state:
        type: list
        description: state (from the check's parameters).
    default_states:
        type: string
        description: default_states (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
