name: httpv2
fqcn: checkmk.httpv2
collection: checkmk
short_description:
description: Checkmk active check 'httpv2', translated to a read-only on-host Starlark check module.
options:
    endpoints:
        type: list
        description: endpoints (from the check's parameters).
    host_config:
        type: string
        description: host_config (from the check's parameters).
    warn:
        type: number
        default: 2.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 5.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
