name: domino_tasks
fqcn: checkmk.domino_tasks
collection: checkmk
short_description: Domino Task %s
description: 'Checkmk check ''domino_tasks'' (service: Domino Task %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  item_oid:
    type: string
    default: ''
    description: item_oid (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
