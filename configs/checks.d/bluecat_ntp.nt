name: bluecat_ntp
fqcn: checkmk.bluecat_ntp
collection: checkmk
short_description: NTP
description: Checkmk check 'bluecat_ntp' (service: NTP), translated to a read-only on-host Starlark check module.
options:
    oper_states:
        type: string
        description: oper_states (from the check's parameters).
    stratum:
        type: string
        description: stratum (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
