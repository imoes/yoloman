name: apc_sts_inputs
fqcn: checkmk.apc_sts_inputs
collection: checkmk
short_description: Input %s
description: Checkmk check 'apc_sts_inputs' (service: Input %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    voltage_levels_lower:
        type: number
        default: 200.0
        description: voltage_levels_lower (from the check's parameters).
    voltage_levels_lower_critical:
        type: number
        default: 180.0
        description: voltage_levels_lower_critical (from the check's parameters).
    current_levels_lower:
        type: number
        default: 0.0
        description: current_levels_lower (from the check's parameters).
    current_levels_lower_critical:
        type: number
        default: 0.0
        description: current_levels_lower_critical (from the check's parameters).
    power_levels_lower:
        type: number
        default: 0.0
        description: power_levels_lower (from the check's parameters).
    power_levels_lower_critical:
        type: number
        default: 0.0
        description: power_levels_lower_critical (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
