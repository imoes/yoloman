name: brocade_tm
fqcn: checkmk.brocade_tm
collection: checkmk
short_description: TM %s
description: Monitoring check 'brocade_tm' (service: TM %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
