name: juniper_mem
fqcn: checkmk.juniper_mem
collection: checkmk
short_description: Memory %s
description: Checkmk check 'juniper_mem' (service: Memory %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    warn:
        type: number
        default: 80.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 90.0
        description: crit (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
