name: hp_msa_disk
fqcn: checkmk.hp_msa_disk
collection: checkmk
short_description: Disk Health %s
description: Monitoring check 'hp_msa_disk' (service: Disk Health %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    username:
        type: string
        default: manage
        description: username (from the check's parameters).
    password:
        type: string
        default: !manage
        description: password (from the check's parameters).
    protocol:
        type: string
        default: https
        description: protocol (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
