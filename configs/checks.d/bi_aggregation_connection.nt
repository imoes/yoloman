name: bi_aggregation_connection
fqcn: checkmk.bi_aggregation_connection
collection: checkmk
short_description: BI Datasource Connection
description: Monitoring check 'bi_aggregation_connection' (service: BI Datasource Connection), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    site:
        type: string
        default:
        description: site (from the check's parameters).
    user:
        type: string
        default: automation
        description: user (from the check's parameters).
    secret:
        type: string
        default:
        description: secret (from the check's parameters).
    port:
        type: number
        default: 443
        description: port (from the check's parameters).
    protocol:
        type: string
        default: https
        description: protocol (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
