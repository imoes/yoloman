name: mqtt_clients
fqcn: checkmk.mqtt_clients
collection: checkmk
short_description: MQTT %s Clients
description: 'Checkmk check ''mqtt_clients'' (service: MQTT %s Clients), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  timeout:
    type: number
    default: '5'
    description: timeout (from the check's parameters).
  warn_connected:
    type: string
    description: warn_connected (from the check's parameters).
  crit_connected:
    type: string
    description: crit_connected (from the check's parameters).
  warn_maximum:
    type: string
    description: warn_maximum (from the check's parameters).
  crit_maximum:
    type: string
    description: crit_maximum (from the check's parameters).
  warn_total:
    type: string
    description: warn_total (from the check's parameters).
  crit_total:
    type: string
    description: crit_total (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
