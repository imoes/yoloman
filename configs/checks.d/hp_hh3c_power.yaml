name: hp_hh3c_power
fqcn: checkmk.hp_hh3c_power
collection: checkmk
short_description: Power %s
description: 'Checkmk check ''hp_hh3c_power'' (service: Power %s), translated to a
  read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
