name: cisco_sma_dns_requests
fqcn: checkmk.cisco_sma_dns_requests
collection: checkmk
short_description: DNS Requests
description: 'Checkmk check ''cisco_sma_dns_requests'' (service: DNS Requests), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  pending_dns_levels:
    type: list
    description: pending_dns_levels (from the check's parameters).
  outstanding_dns_levels:
    type: list
    description: outstanding_dns_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
