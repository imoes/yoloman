name: alcatel_power
fqcn: checkmk.alcatel_power
collection: checkmk
short_description: Power Supply %s
description: 'Checkmk check ''alcatel_power'' (service: Power Supply %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
