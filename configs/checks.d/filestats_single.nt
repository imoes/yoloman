name: filestats_single
fqcn: checkmk.filestats_single
collection: checkmk
short_description: File %s
description: Monitoring check 'filestats_single' (service: File %s), translated to a read-only on-host Starlark check module.
options:
    max_size:
        type: list
        description: max_size (from the check's parameters).
    min_size:
        type: list
        description: min_size (from the check's parameters).
    max_age:
        type: list
        description: max_age (from the check's parameters).
    min_age:
        type: list
        description: min_age (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
