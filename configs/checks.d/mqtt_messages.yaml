name: mqtt_messages
fqcn: checkmk.mqtt_messages
collection: checkmk
short_description: MQTT %s Messages
description: 'Checkmk check ''mqtt_messages'' (service: MQTT %s Messages), translated
  to a read-only on-host Starlark check module.'
options:
  subscriptions_warn:
    type: string
    description: subscriptions_warn (from the check's parameters).
  subscriptions_crit:
    type: string
    description: subscriptions_crit (from the check's parameters).
  retained_messages_warn:
    type: string
    description: retained_messages_warn (from the check's parameters).
  retained_messages_crit:
    type: string
    description: retained_messages_crit (from the check's parameters).
  stored_messages_warn:
    type: string
    description: stored_messages_warn (from the check's parameters).
  stored_messages_crit:
    type: string
    description: stored_messages_crit (from the check's parameters).
  stored_messages_bytes_warn:
    type: string
    description: stored_messages_bytes_warn (from the check's parameters).
  stored_messages_bytes_crit:
    type: string
    description: stored_messages_bytes_crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
