name: audiocodes_operational_state_redundant
fqcn: checkmk.audiocodes_operational_state_redundant
collection: checkmk
short_description: Operational state redundant module %s
description: 'Checkmk check ''audiocodes_operational_state_redundant'' (service: Operational
  state redundant module %s), translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
