name: apc_symmetra_ext_temp
fqcn: checkmk.apc_symmetra_ext_temp
collection: checkmk
short_description: Temperature External %s
description: Checkmk check 'apc_symmetra_ext_temp' (service: Temperature External %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    version:
        type: string
        default: 2c
        description: version (from the check's parameters).
    warn:
        type: number
        default: 30.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 35.0
        description: crit (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
