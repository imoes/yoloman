name: scaleio_pd
fqcn: checkmk.scaleio_pd
collection: checkmk
short_description: ScaleIO PD capacity %s
description: 'Checkmk check ''scaleio_pd'' (service: ScaleIO PD capacity %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  port:
    type: number
    default: '443'
    description: port (from the check's parameters).
  username:
    type: string
    default: ''
    description: username (from the check's parameters).
  password:
    type: string
    default: ''
    description: password (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
