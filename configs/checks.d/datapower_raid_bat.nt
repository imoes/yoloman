name: datapower_raid_bat
fqcn: checkmk.datapower_raid_bat
collection: checkmk
short_description: Raid Battery %s
description: Checkmk check 'datapower_raid_bat' (service: Raid Battery %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
