name: enviromux_sems_e2d_voltage
fqcn: checkmk.enviromux_sems_e2d_voltage
collection: checkmk
short_description: Sensor %s
description: 'Monitoring check ''enviromux_sems_e2d_voltage'' (service: Sensor %s),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
  levels_lower:
    type: list
    description: levels_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
