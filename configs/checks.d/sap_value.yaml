name: sap_value
fqcn: checkmk.sap_value
collection: checkmk
short_description: '%s'
description: 'Monitoring check ''sap_value'' (service: %s), translated to a read-only
  on-host Starlark check module.'
options:
  data_file:
    type: string
    default: /var/lib/agentic/sap/data.tsv
    description: data_file (from the check's parameters).
  limit_item_levels:
    type: string
    description: limit_item_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
