name: oracle_undostat
fqcn: checkmk.oracle_undostat
collection: checkmk
short_description: ORA %s Undo Retention
description: Monitoring check 'oracle_undostat' (service: ORA %s Undo Retention), translated to a read-only on-host Starlark check module.
options:
    username:
        type: string
        description: username (from the check's parameters).
    password:
        type: string
        description: password (from the check's parameters).
    database:
        type: string
        description: database (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
    nospaceerrcnt_state:
        type: number
        default: 2
        description: nospaceerrcnt_state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
