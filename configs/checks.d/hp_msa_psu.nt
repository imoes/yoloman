name: hp_msa_psu
fqcn: checkmk.hp_msa_psu
collection: checkmk
short_description: Power Supply Health %s
description: Checkmk check 'hp_msa_psu' (service: Power Supply Health %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default:
        description: host (from the check's parameters).
    username:
        type: string
        default: monitor
        description: username (from the check's parameters).
    password:
        type: string
        default:
        description: password (from the check's parameters).
    verify_ssl:
        type: bool
        default: False
        description: verify_ssl (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
