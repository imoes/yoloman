name: systemd_units_sockets_summary
fqcn: checkmk.systemd_units_sockets_summary
collection: checkmk
short_description: Systemd Socket Summary
description: 'Checkmk check ''systemd_units_sockets_summary'' (service: Systemd Socket
  Summary), translated to a read-only on-host Starlark check module.'
options:
  states:
    type: string
    description: states (from the check's parameters).
  states_default:
    type: number
    default: '2'
    description: states_default (from the check's parameters).
  ignored:
    type: list
    description: ignored (from the check's parameters).
  disabled_critical:
    type: bool
    default: 'False'
    description: disabled_critical (from the check's parameters).
  deactivating_levels:
    type: list
    description: deactivating_levels (from the check's parameters).
  reloading_levels:
    type: string
    description: reloading_levels (from the check's parameters).
  activating_levels:
    type: string
    description: activating_levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
