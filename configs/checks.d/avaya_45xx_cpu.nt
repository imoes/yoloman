name: avaya_45xx_cpu
fqcn: checkmk.avaya_45xx_cpu
collection: checkmk
short_description: CPU utilization CPU %s
description: Monitoring check 'avaya_45xx_cpu' (service: CPU utilization CPU %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
