name: msexch_isstore
fqcn: checkmk.msexch_isstore
collection: checkmk
short_description: Exchange IS Store %s
description: 'Checkmk check ''msexch_isstore'' (service: Exchange IS Store %s), translated
  to a read-only on-host Starlark check module.'
options:
  store_latency_s:
    type: list
    description: store_latency_s (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
