name: vnx_quotas
fqcn: checkmk.vnx_quotas
collection: checkmk
short_description: VNX Quota %s
description: 'Checkmk check ''vnx_quotas'' (service: VNX Quota %s), translated to
  a read-only on-host Starlark check module.'
options:
  pattern:
    type: string
    description: pattern (from the check's parameters).
  levels:
    type: list
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
