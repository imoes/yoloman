name: wagner_titanus_topsense_airflow_deviation
fqcn: checkmk.wagner_titanus_topsense_airflow_deviation
collection: checkmk
short_description: Airflow Deviation Detector %s
description: 'Checkmk check ''wagner_titanus_topsense_airflow_deviation'' (service:
  Airflow Deviation Detector %s), translated to a read-only on-host Starlark check
  module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  levels_upper:
    type: string
    description: levels_upper (from the check's parameters).
  levels_lower:
    type: string
    description: levels_lower (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
