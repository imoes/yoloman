name: kentix_motion
fqcn: checkmk.kentix_motion
collection: checkmk
short_description: Motion Detector %s
description: Checkmk check 'kentix_motion' (service: Motion Detector %s), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    time_periods:
        type: string
        description: time_periods (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
