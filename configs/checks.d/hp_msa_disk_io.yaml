name: hp_msa_disk_io
fqcn: checkmk.hp_msa_disk_io
collection: checkmk
short_description: Disk IO %s
description: 'Checkmk check ''hp_msa_disk_io'' (service: Disk IO %s), translated to
  a read-only on-host Starlark check module.'
options:
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
