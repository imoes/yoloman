name: oracle_asm_diskgroup
fqcn: checkmk.oracle_asm_diskgroup
collection: checkmk
short_description: ASM Diskgroup %s
description: Checkmk check 'oracle_asm_diskgroup' (service: ASM Diskgroup %s), translated to a read-only on-host Starlark check module.
options:
    req_mir_free:
        type: string
        description: req_mir_free (from the check's parameters).
    warn:
        type: number
        default: 90
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 95
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
