name: akcp_exp_drycontact
fqcn: checkmk.akcp_exp_drycontact
collection: checkmk
short_description: Dry Contact %s
description: 'Checkmk check ''akcp_exp_drycontact'' (service: Dry Contact %s), translated
  to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
