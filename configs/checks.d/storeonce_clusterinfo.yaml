name: storeonce_clusterinfo
fqcn: checkmk.storeonce_clusterinfo
collection: checkmk
short_description: '%s'
description: 'Checkmk check ''storeonce_clusterinfo'' (service: %s), translated to
  a read-only on-host Starlark check module.'
options:
  _check_name:
    type: string
    default: storeonce_clusterinfo
    description: _check_name (from the check's parameters).
  warn:
    type: string
    description: warn (from the check's parameters).
  crit:
    type: string
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
