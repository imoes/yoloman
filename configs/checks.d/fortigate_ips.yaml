name: fortigate_ips
fqcn: checkmk.fortigate_ips
collection: checkmk
short_description: IPS %s
description: 'Checkmk check ''fortigate_ips'' (service: IPS %s), translated to a read-only
  on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  warn:
    type: number
    default: '100.0'
    description: warn (from the check's parameters).
  crit:
    type: number
    default: '300.0'
    description: crit (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
