name: cisco_ucs_faults
fqcn: checkmk.cisco_ucs_faults
collection: checkmk
short_description: Cisco UCS Faults
description: Monitoring check 'cisco_ucs_faults' (service: Cisco UCS Faults), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
