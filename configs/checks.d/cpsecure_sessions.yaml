name: cpsecure_sessions
fqcn: checkmk.cpsecure_sessions
collection: checkmk
short_description: Number of %s sessions
description: 'Checkmk check ''cpsecure_sessions'' (service: Number of %s sessions),
  translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
