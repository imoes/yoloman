name: cisco_stack
fqcn: checkmk.cisco_stack
collection: checkmk
short_description: Switch stack status %s
description: Checkmk check 'cisco_stack' (service: Switch stack status %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
