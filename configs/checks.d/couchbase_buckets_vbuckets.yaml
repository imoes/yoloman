name: couchbase_buckets_vbuckets
fqcn: checkmk.couchbase_buckets_vbuckets
collection: checkmk
short_description: Couchbase Bucket %s active vBuckets
description: 'Checkmk check ''couchbase_buckets_vbuckets'' (service: Couchbase Bucket
  %s active vBuckets), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  username:
    type: string
    default: ''
    description: username (from the check's parameters).
  password:
    type: string
    default: ''
    description: password (from the check's parameters).
  node:
    type: string
    description: node (from the check's parameters).
  port:
    type: number
    default: '8091'
    description: port (from the check's parameters).
  resident_items_ratio:
    type: list
    description: resident_items_ratio (from the check's parameters).
  item_memory:
    type: string
    description: item_memory (from the check's parameters).
  vb_pending_num:
    type: string
    description: vb_pending_num (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
