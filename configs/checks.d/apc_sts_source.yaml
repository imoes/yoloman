name: apc_sts_source
fqcn: checkmk.apc_sts_source
collection: checkmk
short_description: Source
description: 'Checkmk check ''apc_sts_source'' (service: Source), translated to a
  read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  source1:
    type: string
    description: source1 (from the check's parameters).
  source2:
    type: string
    description: source2 (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
