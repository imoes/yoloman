name: aironet_errors
fqcn: checkmk.aironet_errors
collection: checkmk
short_description: MAC CRC errors radio %s
description: 'Checkmk check ''aironet_errors'' (service: MAC CRC errors radio %s),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
