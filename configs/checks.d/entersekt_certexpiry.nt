name: entersekt_certexpiry
fqcn: checkmk.entersekt_certexpiry
collection: checkmk
short_description: Entersekt Certificate Expiration
description: Checkmk check 'entersekt_certexpiry' (service: Entersekt Certificate Expiration), translated to a read-only on-host Starlark check module.
options:
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    levels:
        type: list
        description: levels (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
