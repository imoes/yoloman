name: bvip_link
fqcn: checkmk.bvip_link
collection: checkmk
short_description: Network Link
description: 'Checkmk check ''bvip_link'' (service: Network Link), translated to a
  read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  ok_states:
    type: list
    description: ok_states (from the check's parameters).
  warn_states:
    type: list
    description: warn_states (from the check's parameters).
  crit_states:
    type: list
    description: crit_states (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
