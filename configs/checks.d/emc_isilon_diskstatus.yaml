name: emc_isilon_diskstatus
fqcn: checkmk.emc_isilon_diskstatus
collection: checkmk
short_description: Disk bay %s Status
description: 'Monitoring check ''emc_isilon_diskstatus'' (service: Disk bay %s Status),
  translated to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
