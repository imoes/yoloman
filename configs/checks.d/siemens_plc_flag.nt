name: siemens_plc_flag
fqcn: checkmk.siemens_plc_flag
collection: checkmk
short_description: Flag %s
description: Checkmk check 'siemens_plc_flag' (service: Flag %s), translated to a read-only on-host Starlark check module.
options:
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
    community:
        type: string
        default:
        description: community (from the check's parameters).
    _section_siemens_plc:
        type: string
        default:
        description: _section_siemens_plc (from the check's parameters).
    expected_state:
        type: bool
        default: False
        description: expected_state (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
