name: oracle_instance_uptime
fqcn: checkmk.oracle_instance_uptime
collection: checkmk
short_description: ORA %s Uptime
description: 'Checkmk check ''oracle_instance_uptime'' (service: ORA %s Uptime), translated
  to a read-only on-host Starlark check module.'
options:
  min:
    type: string
    description: min (from the check's parameters).
  max:
    type: string
    description: max (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: local
prompt_version: c21f0f5a7229
