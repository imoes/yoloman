name: mcafee_webgateway_time_to_resolve_dns
fqcn: checkmk.mcafee_webgateway_time_to_resolve_dns
collection: checkmk
short_description: The McAfee Web Gateway has been rebranded to Skyhigh Secure Web Gateway with its release 12.2.2.
description: Monitoring check 'mcafee_webgateway_time_to_resolve_dns' (service: Time to resolve DNS), translated to a read-only on-host Starlark check module.
options:
    time_to_resolve_dns:
        type: string
        description: time_to_resolve_dns (from the check's parameters).
    warn:
        type: number
        default: 2.0
        description: warn (from the check's parameters).
    crit:
        type: number
        default: 5.0
        description: crit (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
