name: safenet_ntls_expiration
fqcn: checkmk.safenet_ntls_expiration
collection: checkmk
short_description: NTLS Expiration Date
description: 'Checkmk check ''safenet_ntls_expiration'' (service: NTLS Expiration
  Date), translated to a read-only on-host Starlark check module.'
options:
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  community:
    type: string
    default: public
    description: community (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
