name: ucd_cpu_util
fqcn: checkmk.ucd_cpu_util
collection: checkmk
short_description: CPU utilization
description: Monitoring check 'ucd_cpu_util' (service: CPU utilization), translated to a read-only on-host Starlark check module.
options:
    levels:
        type: list
        description: levels (from the check's parameters).
    community:
        type: string
        default: public
        description: community (from the check's parameters).
    host:
        type: string
        default: localhost
        description: host (from the check's parameters).
writes: False
runtime: starlark
source: translated
kind: check
