name: siemens_plc_counter
fqcn: checkmk.siemens_plc_counter
collection: checkmk
short_description: Counter %s
description: 'Monitoring check ''siemens_plc_counter'' (service: Counter %s), translated
  to a read-only on-host Starlark check module.'
options:
  levels:
    type: string
    description: levels (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
