name: pfsense_if
fqcn: checkmk.pfsense_if
collection: checkmk
short_description: Firewall Interface %s
description: 'Checkmk check ''pfsense_if'' (service: Firewall Interface %s), translated
  to a read-only on-host Starlark check module.'
options:
  community:
    type: string
    default: public
    description: community (from the check's parameters).
  host:
    type: string
    default: localhost
    description: host (from the check's parameters).
  ipv4_in_blocked:
    type: list
    description: ipv4_in_blocked (from the check's parameters).
writes: false
runtime: starlark
source: translated
kind: check
execution: snmp
prompt_version: c21f0f5a7229
